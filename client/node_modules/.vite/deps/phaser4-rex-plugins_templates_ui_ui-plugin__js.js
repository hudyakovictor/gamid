import { Actions as __webpack_exports__Actions, Animations as __webpack_exports__Animations, BlendModes as __webpack_exports__BlendModes, CANVAS as __webpack_exports__CANVAS, Cameras as __webpack_exports__Cameras, Class as __webpack_exports__Class, DOM as __webpack_exports__DOM, Display as __webpack_exports__Display, Events as __webpack_exports__Events, Filters as __webpack_exports__Filters, Game as __webpack_exports__Game, GameObjects as __webpack_exports__GameObjects, Geom as __webpack_exports__Geom, Input as __webpack_exports__Input, Math as __webpack_exports__Math, Plugins as __webpack_exports__Plugins, Renderer as __webpack_exports__Renderer, Scene as __webpack_exports__Scene, Scenes as __webpack_exports__Scenes, Sound as __webpack_exports__Sound, Structs as __webpack_exports__Structs, Textures as __webpack_exports__Textures, TintModes as __webpack_exports__TintModes, Tweens as __webpack_exports__Tweens, Utils as __webpack_exports__Utils, VERSION as __webpack_exports__VERSION } from "./phaser.js";
//#region \0rolldown/runtime.js
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJSMin = (cb, mod) => () => (mod || (cb((mod = { exports: {} }).exports, mod), cb = null), mod.exports);
var __copyProps = (to, from, except, desc) => {
	if (from && typeof from === "object" || typeof from === "function") for (var keys = __getOwnPropNames(from), i = 0, n = keys.length, key; i < n; i++) {
		key = keys[i];
		if (!__hasOwnProp.call(to, key) && key !== except) __defProp(to, key, {
			get: ((k) => from[k]).bind(null, key),
			enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable
		});
	}
	return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(isNodeMode || !mod || !mod.__esModule || !__hasOwnProp.call(mod, "default") ? __defProp(target, "default", {
	value: mod,
	enumerable: true
}) : target, mod));
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/ObjectFactory.js
var ObjectFactory = class ObjectFactory {
	constructor(scene) {
		this.scene = scene;
		scene.events.once("destroy", this.destroy, this);
	}
	destroy() {
		this.scene = null;
	}
	static register(type, callback) {
		ObjectFactory.prototype[type] = callback;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/ninepatch/texture/SetGetFrameNameCallback.js
var SetGetFrameNameCallback = function(callback) {
	if (callback === void 0) callback = DefaultGetFrameNameCallback;
	this.getFrameNameCallback = callback;
	return this;
};
var DefaultGetFrameNameCallback = function(colIndex, rowIndex, baseFrameName) {
	if (baseFrameName === "__BASE") return `${colIndex},${rowIndex}`;
	else return `${baseFrameName}:${colIndex},${rowIndex}`;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/object/DeepClone.js
function DeepClone(obj) {
	if (obj === null || typeof obj !== "object") return obj;
	if (Array.isArray(obj)) return obj.map((item) => DeepClone(item));
	if (obj instanceof Date) return new Date(obj);
	if (obj instanceof RegExp) return new RegExp(obj);
	if (Object.getPrototypeOf(obj) !== Object.prototype) return obj;
	const clonedObj = {};
	for (let key in obj) if (obj.hasOwnProperty(key)) clonedObj[key] = DeepClone(obj[key]);
	return clonedObj;
}
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/ninepatch/texture/SetBaseTexture.js
var SetBaseTexture = function(key, baseFrameName, columns, rows) {
	if (Array.isArray(baseFrameName)) {
		rows = columns;
		columns = baseFrameName;
		baseFrameName = void 0;
	}
	if (baseFrameName == null) baseFrameName = "__BASE";
	if (typeof columns === "number" && arguments.length >= 6) {
		columns = [
			arguments[2],
			void 0,
			arguments[3]
		];
		rows = [
			arguments[4],
			void 0,
			arguments[5]
		];
	} else if (columns === void 0 && rows === void 0 && this.columns.data !== void 0 && this.rows.data !== void 0) {
		columns = this.columns.data;
		rows = this.rows.data;
	} else {
		columns = DeepClone(columns);
		rows = DeepClone(rows);
	}
	this.textureKey = key;
	this.baseFrameName = baseFrameName;
	this.columns.data = columns;
	this.columns.count = columns ? columns.length : 0;
	this.columns.stretch = 0;
	this.columns.minWidth = 0;
	this.columns.scale = 1;
	this.rows.data = rows;
	this.rows.count = rows ? rows.length : 0;
	this.rows.stretch = 0;
	this.rows.minHeight = 0;
	this.rows.scale = 1;
	var texture = this.scene.sys.textures.get(key);
	if (!texture) {
		this.clear();
		return this;
	}
	if (!columns || !rows) {
		this.clear();
		return this;
	}
	var baseFrame = texture.get(baseFrameName);
	var remainderTextureWidth = baseFrame.width;
	var unknownColumnWidthCount = 0;
	for (var i = 0, cnt = columns.length; i < cnt; i++) if (columns[i] === void 0) unknownColumnWidthCount++;
	else if (typeof columns[i] === "number") remainderTextureWidth -= columns[i];
	else remainderTextureWidth -= columns[i].width;
	var unknownColumnWidth = unknownColumnWidthCount > 0 ? remainderTextureWidth / unknownColumnWidthCount : 0;
	var remainderTextureHeight = baseFrame.height;
	var unknownRowHeightCount = 0;
	for (var i = 0, cnt = rows.length; i < cnt; i++) if (rows[i] === void 0) unknownRowHeightCount++;
	else if (typeof rows[i] === "number") remainderTextureHeight -= rows[i];
	else remainderTextureHeight -= rows[i].width;
	var unknownRowHeight = unknownRowHeightCount ? remainderTextureHeight / unknownRowHeightCount : 0;
	var row, col, rowHeight, colWidth, frameName;
	var offsetX = 0, offsetY = 0;
	for (var j = 0, jcnt = rows.length; j < jcnt; j++) {
		if (rows[j] === void 0) rows[j] = unknownRowHeight;
		if (typeof rows[j] === "number") rows[j] = {
			height: rows[j],
			stretch: j % 2
		};
		row = rows[j];
		rowHeight = row.height;
		this.rows.stretch += row.stretch | 0;
		this.rows.minHeight += row.stretch > 0 ? 0 : rowHeight;
		offsetX = 0;
		for (var i = 0, icnt = columns.length; i < icnt; i++) {
			if (columns[i] === void 0) columns[i] = unknownColumnWidth;
			if (typeof columns[i] === "number") columns[i] = {
				width: columns[i],
				stretch: i % 2
			};
			col = columns[i];
			colWidth = col.width;
			if (j === 0) {
				this.columns.stretch += col.stretch | 0;
				this.columns.minWidth += col.stretch > 0 ? 0 : colWidth;
			}
			if (colWidth >= 1 && rowHeight >= 1) {
				frameName = this.getFrameNameCallback(i, j, baseFrameName);
				var frameNameType = typeof frameName;
				if (frameNameType === "string" || frameNameType === "number") texture.add(frameName, 0, offsetX + baseFrame.cutX, offsetY + baseFrame.cutY, colWidth, rowHeight);
			}
			offsetX += colWidth;
		}
		offsetY += rowHeight;
	}
	this.updateTexture();
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/ninepatch/texture/UpdateTexture.js
var UpdateTexture = function() {
	this.clear();
	if (this.textureKey === void 0) return this;
	var texture = this.scene.sys.textures.get(this.textureKey);
	if (!texture) return this;
	var minWidth = this.columns.minWidth * this.maxFixedPartScaleX;
	var minHeight = this.rows.minHeight * this.maxFixedPartScaleY;
	var stretchWidth = this.width - minWidth;
	var stretchHeight = this.height - minHeight;
	var fixedPartScaleX = stretchWidth >= 0 ? this.maxFixedPartScaleX : this.width / minWidth;
	var fixedPartScaleY = stretchHeight >= 0 ? this.maxFixedPartScaleY : this.height / minHeight;
	if (this.preserveRatio) {
		var minScale = Math.min(fixedPartScaleX, fixedPartScaleY);
		if (fixedPartScaleX > minScale) {
			var compensationWidth = (fixedPartScaleX - minScale) * minWidth;
			if (stretchWidth >= 0) stretchWidth += compensationWidth;
			else stretchWidth = compensationWidth;
			fixedPartScaleX = minScale;
		}
		if (fixedPartScaleY > minScale) {
			var compensationHeight = (fixedPartScaleY - minScale) * minHeight;
			if (stretchHeight >= 0) stretchHeight += compensationHeight;
			else stretchHeight = compensationHeight;
			fixedPartScaleY = minScale;
		}
	}
	this.columns.scale = fixedPartScaleX;
	this.rows.scale = fixedPartScaleY;
	var proportionWidth;
	if (stretchWidth > 0) proportionWidth = this.columns.stretch > 0 ? stretchWidth / this.columns.stretch : 0;
	else proportionWidth = 0;
	var proportionHeight;
	if (stretchHeight > 0) proportionHeight = this.rows.stretch > 0 ? stretchHeight / this.rows.stretch : 0;
	else proportionHeight = 0;
	var frameName, col, row, colWidth, rowHeight;
	var offsetX = 0, offsetY = 0;
	var imageType;
	this._beginDraw();
	for (var j = 0, jcnt = this.rows.count; j < jcnt; j++) {
		row = this.rows.data[j];
		rowHeight = row.stretch === 0 ? row.height * fixedPartScaleY : proportionHeight * row.stretch;
		offsetX = 0;
		for (var i = 0, icnt = this.columns.count; i < icnt; i++) {
			col = this.columns.data[i];
			colWidth = col.stretch === 0 ? col.width * fixedPartScaleX : proportionWidth * col.stretch;
			frameName = this.getFrameNameCallback(i, j, this.baseFrameName);
			if (texture.has(frameName) && colWidth > 0 && rowHeight > 0) {
				if (row.stretch === 0 && col.stretch === 0) imageType = 0;
				else if (this.getStretchMode(i, j) === 0) imageType = 0;
				else imageType = 1;
				if (imageType === 0) this._drawImage(this.textureKey, frameName, offsetX, offsetY, colWidth, rowHeight);
				else this._drawTileSprite(this.textureKey, frameName, offsetX, offsetY, colWidth, rowHeight);
			}
			offsetX += colWidth;
		}
		offsetY += rowHeight;
	}
	this._endDraw();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/ninepatch/texture/SetStretchMode.js
var IsPlainObject$60 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$283 = __webpack_exports__Utils.Objects.GetValue;
var SetStretchMode = function(mode) {
	if (IsPlainObject$60(mode)) {
		this.stretchMode.edge = parseMode(GetValue$283(mode, "edge", 0));
		this.stretchMode.internal = parseMode(GetValue$283(mode, "internal", 0));
	} else {
		mode = parseMode(mode);
		this.stretchMode.edge = mode;
		this.stretchMode.internal = mode;
	}
	return this;
};
var parseMode = function(mode) {
	if (typeof mode === "string") mode = EXTENDMODE[mode];
	return mode;
};
var EXTENDMODE = {
	scale: 0,
	repeat: 1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/ninepatch/utils/IsEdge.js
var IsEdge = function(colIndex, rowIndex) {
	return colIndex === 0 || colIndex === this.columns.count - 1 || rowIndex === 0 || rowIndex === this.rows.count - 1;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/ninepatch/texture/GetStretchMode.js
var GetStretchMode = function(colIndex, rowIndex) {
	return IsEdge.call(this, colIndex, rowIndex) ? this.stretchMode.edge : this.stretchMode.internal;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/ninepatch/texture/SetPreserveRatio.js
var SetPreserveRatio = function(enable) {
	if (enable == void 0) enable = true;
	this.preserveRatio = enable;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/ninepatch/texture/SetMaxFixedPartScale.js
var SetMaxFixedPartScale = function(scaleX, scaleY) {
	if (scaleY === void 0) scaleY = scaleX;
	this.maxFixedPartScaleX = scaleX;
	this.maxFixedPartScaleY = scaleY;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/object/NOOP.js
var NOOP = function() {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/ninepatch/Methods.js
var Methods_default$1 = {
	_beginDraw: NOOP,
	_drawImage: NOOP,
	_drawTileSprite: NOOP,
	_endDraw: NOOP,
	setGetFrameNameCallback: SetGetFrameNameCallback,
	setBaseTexture: SetBaseTexture,
	updateTexture: UpdateTexture,
	setStretchMode: SetStretchMode,
	getStretchMode: GetStretchMode,
	setPreserveRatio: SetPreserveRatio,
	setMaxFixedPartScale: SetMaxFixedPartScale
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/ninepatch/NinePatch.js
var IsPlainObject$59 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$282 = __webpack_exports__Utils.Objects.GetValue;
var NinePatchBase = function(GOClass, type) {
	class NinePatch extends GOClass {
		constructor(scene, x, y, width, height, key, baseFrame, columns, rows, config) {
			if (IsPlainObject$59(x)) {
				config = x;
				x = GetValue$282(config, "x", 0);
				y = GetValue$282(config, "y", 0);
				width = GetValue$282(config, "width", 1);
				height = GetValue$282(config, "height", 1);
				key = GetValue$282(config, "key", void 0);
				baseFrame = GetValue$282(config, "baseFrame", void 0);
				columns = GetValue$282(config, "columns", void 0);
				rows = GetValue$282(config, "rows", void 0);
			} else if (IsPlainObject$59(width)) {
				config = width;
				width = GetValue$282(config, "width", 1);
				height = GetValue$282(config, "height", 1);
				key = GetValue$282(config, "key", void 0);
				baseFrame = GetValue$282(config, "baseFrame", void 0);
				columns = GetValue$282(config, "columns", void 0);
				rows = GetValue$282(config, "rows", void 0);
			} else if (IsPlainObject$59(key)) {
				config = key;
				key = GetValue$282(config, "key", void 0);
				baseFrame = GetValue$282(config, "baseFrame", void 0);
				columns = GetValue$282(config, "columns", void 0);
				rows = GetValue$282(config, "rows", void 0);
			} else if (IsPlainObject$59(baseFrame)) {
				config = baseFrame;
				baseFrame = GetValue$282(config, "baseFrame", void 0);
				columns = GetValue$282(config, "columns", void 0);
				rows = GetValue$282(config, "rows", void 0);
			} else if (Array.isArray(baseFrame)) {
				config = rows;
				rows = columns;
				columns = baseFrame;
				baseFrame = GetValue$282(config, "baseFrame", void 0);
			} else if (IsPlainObject$59(columns)) {
				config = columns;
				columns = GetValue$282(config, "columns", void 0);
				rows = GetValue$282(config, "rows", void 0);
			}
			if (baseFrame === void 0) baseFrame = GetValue$282(config, "frame", void 0);
			if (columns === void 0) {
				var leftWidth = GetValue$282(config, "leftWidth", void 0);
				var rightWidth = GetValue$282(config, "rightWidth", void 0);
				if (leftWidth !== void 0 && rightWidth !== void 0) columns = [
					leftWidth,
					void 0,
					rightWidth
				];
			}
			if (rows === void 0) {
				var topHeight = GetValue$282(config, "topHeight", void 0);
				var bottomHeight = GetValue$282(config, "bottomHeight", void 0);
				if (topHeight !== void 0 && bottomHeight !== void 0) rows = [
					topHeight,
					void 0,
					bottomHeight
				];
			}
			super(scene);
			this.type = type;
			this.setPosition(x, y).resize(width, height).setOrigin(.5, .5);
			this.columns = {};
			this.rows = {};
			this.stretchMode = {};
			this._tileSprite = void 0;
			this._image = void 0;
			this.setGetFrameNameCallback(GetValue$282(config, "getFrameNameCallback", void 0));
			this.setStretchMode(GetValue$282(config, "stretchMode", 0));
			this.setPreserveRatio(GetValue$282(config, "preserveRatio", true));
			var maxFixedPartScale = GetValue$282(config, "maxFixedPartScale", 1);
			var maxFixedPartScaleX = GetValue$282(config, "maxFixedPartScaleX", maxFixedPartScale);
			var maxFixedPartScaleY = GetValue$282(config, "maxFixedPartScaleY", void 0);
			this.setMaxFixedPartScale(maxFixedPartScaleX, maxFixedPartScaleY);
			this.setBaseTexture(key, baseFrame, columns, rows);
		}
		get minWidth() {
			return this.columns.minWidth;
		}
		get minHeight() {
			return this.rows.minHeight;
		}
		get fixedPartScaleX() {
			return this.columns.scale;
		}
		get fixedPartScaleY() {
			return this.rows.scale;
		}
		resize(width, height) {
			if (this.width === width && this.height === height) return this;
			if (super.resize) super.resize(width, height);
			else super.setSize(width, height);
			this.updateTexture();
			return this;
		}
		get leftWidth() {
			return this.columns.data[0];
		}
		get rightWidth() {
			return this.columns.data[this.columns.count - 1];
		}
		get topHeight() {
			return this.rows.data[0];
		}
		get bottomHeight() {
			return this.rows.data[this.rows.count - 1];
		}
	}
	Object.assign(NinePatch.prototype, Methods_default$1);
	return NinePatch;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/rendertexture/utils/DrawImage.js
var DrawImage$2 = function(key, frame, x, y, width, height) {
	var textureFrame = this.scene.sys.textures.getFrame(key, frame);
	if (!textureFrame) return;
	this.stamp(key, frame, x, y, {
		originX: 0,
		originY: 0,
		scaleX: width / textureFrame.realWidth,
		scaleY: height / textureFrame.realHeight
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/rendertexture/utils/DrawTileSprite.js
var DrawTileSprite$1 = function(key, frame, x, y, width, height) {
	this.repeat(key, frame, x, y, width, height);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/rendertexture/utils/EndDraw.js
var EndDraw = function() {
	this.render();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/rendertexture/ninepatch/NinePatch.js
var RenderTexture$1 = __webpack_exports__GameObjects.RenderTexture;
var NinePatch$1 = class extends NinePatchBase(RenderTexture$1, "rexNinePatch") {};
var Methods$25 = {
	_drawImage: DrawImage$2,
	_drawTileSprite: DrawTileSprite$1,
	_endDraw: EndDraw
};
Object.assign(NinePatch$1.prototype, Methods$25);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/ninepatch/NinePatch.js
var NinePatch_default$1 = NinePatch$1;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/object/SetValue.js
var IsNil = function(value) {
	return value === null || value === void 0;
};
var IsObjectLike = function(value) {
	return value !== null && typeof value === "object";
};
var NormalizePath = function(path, delimiter) {
	if (Array.isArray(path)) {} else if (typeof path !== "string") path = [];
	else if (path.trim() === "") path = [];
	else path = path.split(delimiter).filter(Boolean);
	return path;
};
/**
* Set a nested value into target by path (mutates target).
*
* - If keys is a string and does NOT contain delimiter, write directly.
* - Intermediate non-plain-object values are always overwritten with {}.
*
* @param {object} target
* @param {string|string[]} keys
* @param {*} value
* @param {string} [delimiter='.']
* @returns {object} the same target reference
*/
var SetValue = function(target, keys, value, delimiter = ".") {
	if (!IsObjectLike(target)) return target;
	if (IsNil(keys) || keys === "" || Array.isArray(keys) && keys.length === 0) return target;
	if (typeof keys === "string" && keys.indexOf(delimiter) === -1 || typeof keys === "number") {
		target[keys] = value;
		return target;
	}
	var pathSegments = NormalizePath(keys, delimiter);
	if (pathSegments.length === 0) return target;
	var cursor = target;
	var pathSegmentsCount = pathSegments.length;
	for (var index = 0; index < pathSegmentsCount - 1; index++) {
		var segment = pathSegments[index];
		var next = cursor[segment];
		if (!IsObjectLike(next)) cursor[segment] = {};
		cursor = cursor[segment];
	}
	cursor[pathSegments[pathSegmentsCount - 1]] = value;
	return target;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/ninepatch/Factory.js
ObjectFactory.register("ninePatch", function(x, y, width, height, key, columns, rows, config) {
	var gameObject = new NinePatch_default$1(this.scene, x, y, width, height, key, columns, rows, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.NinePatch", NinePatch_default$1);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/render/WebGLRenderer.js
var GetCalcMatrix$3 = __webpack_exports__GameObjects.GetCalcMatrix;
var TransformMatrix$5 = __webpack_exports__GameObjects.Components.TransformMatrix;
var tempMatrix$3 = new TransformMatrix$5();
var WebGLRenderer$5 = function(renderer, src, drawingContext, parentMatrix) {
	var bobs = src.getRenderList();
	var camera = drawingContext.camera;
	if (bobs.length === 0 || alpha === 0) return;
	var result = GetCalcMatrix$3(src, camera, parentMatrix, !drawingContext.useCanvas);
	var calcMatrix = tempMatrix$3.copyFrom(result.calc);
	var alpha = camera.alpha * src.alpha;
	var dx = src._displayOriginX;
	var dy = src._displayOriginY;
	var customRenderNodes = src.customRenderNodes;
	var defaultRenderNodes = src.defaultRenderNodes;
	var Submitter = customRenderNodes.Submitter || defaultRenderNodes.Submitter;
	for (var i = 0, cnt = bobs.length; i < cnt; i++) bobs[i].webglRender(Submitter, drawingContext, parentMatrix, calcMatrix, alpha, dx, dy);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/render/CanvasRenderer.js
var SetTransform$2 = __webpack_exports__Renderer.Canvas.SetTransform;
var CanvasRenderer$4 = function(renderer, src, camera, parentMatrix) {
	var ctx = renderer.currentContext;
	var bobs = src.getRenderList();
	if (bobs.length === 0 || !SetTransform$2(renderer, ctx, src, camera, parentMatrix)) return;
	camera.addToRenderList(src);
	var roundPixels = camera.roundPixels;
	var dx = -src._displayOriginX, dy = -src._displayOriginY;
	ctx.translate(dx, dy);
	for (var i = 0, cnt = bobs.length; i < cnt; i++) bobs[i].canvasRender(ctx, dx, dy, roundPixels);
	ctx.restore();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/render/Render.js
var Render_default$5 = {
	renderWebGL: WebGLRenderer$5,
	renderCanvas: CanvasRenderer$4
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/methods/SetTexture.js
var SetTexture = function(key, frame) {
	this.texture = this.scene.sys.textures.get(key);
	this.frame = this.texture.get(frame);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/methods/Resize.js
var Resize$2 = function(width, height) {
	if (this.width === width && this.height === height) return this;
	this.width = width;
	this.height = height;
	this.updateDisplayOrigin();
	var input = this.input;
	if (input && !input.customHitArea) {
		input.hitArea.width = width;
		input.hitArea.height = height;
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/methods/AddChild.js
var AddChild$3 = function(bob) {
	this.lastAppendedChildren.length = 0;
	if (Array.isArray(bob)) {
		this.children.add(bob);
		this.lastAppendedChildren.push(...bob);
	} else {
		this.children.add(bob);
		this.lastAppendedChildren.push(bob);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/methods/RemoveChild.js
var RemoveItem$14 = __webpack_exports__Utils.Array.Remove;
var RemoveChild$2 = function(bob) {
	if (this.poolManager) this.poolManager.free(bob);
	RemoveItem$14(this.children.list, bob);
	this.lastAppendedChildren.length = 0;
	this.dirty = true;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/methods/RemoveChildren.js
var RemoveChildren$1 = function() {
	if (this.poolManager) this.poolManager.freeMultiple(this.children.list);
	this.children.list.length = 0;
	this.lastAppendedChildren.length = 0;
	this.dirty = true;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/methods/GetLastAppendedChildren.js
var GetLastAppendedChildren$1 = function() {
	return this.lastAppendedChildren;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/methods/GetChildren.js
var GetChildren$1 = function() {
	return this.children.list;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/methods/TintMethods.js
var TintMethods_default = {
	setTint(tint) {
		this.tint = tint;
		return this;
	},
	setTintFill(mode) {
		if (mode === void 0 || mode === true) mode = __webpack_exports__TintModes.FILL;
		else if (mode === false) mode = __webpack_exports__TintModes.MULTIPLY;
		this.tintFill = mode;
		return this;
	},
	clearTint() {
		this.tint = 16777215;
		this.tintFill = __webpack_exports__TintModes.MULTIPLY;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/methods/Methods.js
var methods$44 = {
	setTexture: SetTexture,
	resize: Resize$2,
	setSize: Resize$2,
	addChild: AddChild$3,
	removeChild: RemoveChild$2,
	removeChildren: RemoveChildren$1,
	clear: RemoveChildren$1,
	getLastAppendedChildren: GetLastAppendedChildren$1,
	getChildren: GetChildren$1
};
Object.assign(methods$44, TintMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/struct/Stack.js
var Stack = class {
	constructor() {
		this.items = [];
	}
	destroy() {
		this.clear();
		this.items = void 0;
	}
	pop() {
		return this.items.length > 0 ? this.items.pop() : null;
	}
	push(l) {
		this.items.push(l);
		return this;
	}
	pushMultiple(arr) {
		this.items.push.apply(this.items, arr);
		arr.length = 0;
		return this;
	}
	clear() {
		this.items.length = 0;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/pool.js
var pool_default = Stack;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/poolmanager/PoolManager.js
var GetValue$281 = __webpack_exports__Utils.Objects.GetValue;
var Pools$1 = {};
var PoolManager$1 = class {
	constructor(config) {
		this.pools = GetValue$281(config, "pools", Pools$1);
	}
	destroy() {
		this.pools = void 0;
	}
	free(bob) {
		if (!this.pools) return this;
		var bobType = bob.type;
		if (!this.pools.hasOwnProperty(bobType)) this.pools[bobType] = new pool_default();
		this.pools[bobType].push(bob);
		bob.onFree();
		return this;
	}
	freeMultiple(bobs) {
		if (!this.pools) return this;
		for (var i = 0, cnt = bobs.length; i < cnt; i++) this.free(bobs[i]);
		return this;
	}
	allocate(bobType) {
		if (!this.pools || !this.pools.hasOwnProperty(bobType)) return null;
		return this.pools[bobType].pop();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/system/CheckPhaserVersion.js
var MainVersionNumber = 4;
var SubVersionNumber = 2;
var IsChecked = false;
var CheckPhaserVersion = function(minVersion) {
	if (IsChecked) return;
	if (minVersion === void 0) minVersion = SubVersionNumber;
	var version = __webpack_exports__VERSION.split(".");
	var mainVersion = parseInt(version[0]);
	if (mainVersion === MainVersionNumber) {
		var subVersion = parseInt(version[1]);
		if (subVersion < minVersion) console.error(`Minimum supported version : ${mainVersion}.${subVersion}`);
	} else console.error(`Can't supported version : ${mainVersion}`);
	IsChecked = true;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/BlitterBase.js
CheckPhaserVersion();
var GameObject$4 = __webpack_exports__GameObjects.GameObject;
var IsPlainObject$58 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$280 = __webpack_exports__Utils.Objects.GetValue;
var List$1 = __webpack_exports__Structs.List;
var StableSort$1 = __webpack_exports__Utils.Array.StableSort;
var DefaultBlitterNodes = __webpack_exports__Renderer.WebGL.RenderNodes.Defaults.DefaultBlitterNodes;
var Blitter = class extends GameObject$4 {
	constructor(scene, x, y, texture, frame, config) {
		if (IsPlainObject$58(x)) {
			config = x;
			x = GetValue$280(config, "x", 0);
			y = GetValue$280(config, "y", 0);
			texture = GetValue$280(config, "texture");
			frame = GetValue$280(config, "frame");
		}
		if (x === void 0) x = 0;
		if (y === void 0) y = 0;
		super(scene, "rexBlitter");
		this.children = new List$1();
		this.renderList = [];
		this.displayListDirty = false;
		this.lastAppendedChildren = [];
		var reuseBob = GetValue$280(config, "reuseBob", true);
		this.poolManager = reuseBob ? new PoolManager$1(config) : void 0;
		this.setTexture(texture, frame);
		this.setPosition(x, y);
		this.initRenderNodes(this._defaultRenderNodesMap);
		this.setOrigin(0, 0);
		this.clearTint();
	}
	get _defaultRenderNodesMap() {
		return DefaultBlitterNodes;
	}
	preDestroy() {
		this.removeChildren();
		this.children.destroy();
		this.renderList.length = 0;
		if (this.poolManager) this.poolManager.destroy();
	}
	getRenderList() {
		if (this.displayListDirty) {
			this.renderList.length = 0;
			var needDepthSort = false;
			var children = this.children.list;
			for (var i = 0, cnt = children.length; i < cnt; i++) {
				var child = children[i];
				if (ChildCanRender(child)) {
					this.renderList.push(child);
					if (!needDepthSort) needDepthSort = child.depth !== 0;
				}
			}
			if (needDepthSort) StableSort$1(this.renderList, SortByDepth);
			this.displayListDirty = false;
		}
		return this.renderList;
	}
};
var ChildCanRender = function(child) {
	return child.active && child.visible && child.alpha > 0;
};
var SortByDepth = function(childA, childB) {
	return childA._depth - childB._depth;
};
var Components$4 = __webpack_exports__GameObjects.Components;
__webpack_exports__Class.mixin(Blitter, [
	Components$4.Alpha,
	Components$4.BlendMode,
	Components$4.ComputedSize,
	Components$4.Depth,
	Components$4.GetBounds,
	Components$4.Mask,
	Components$4.RenderNodes,
	Components$4.Origin,
	Components$4.ScrollFactor,
	Components$4.Transform,
	Components$4.Visible,
	Render_default$5,
	methods$44
]);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/bob/Types.js
var ImageTypeName$1 = "image";
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/object/GetValue.js
var GetValue$279 = function(source, key, defaultValue, altSource) {
	var isValidSource = source && (typeof source === "object" || typeof source === "function");
	var isValidAltSource = altSource && (typeof altSource === "object" || typeof altSource === "function");
	if (!isValidSource && !isValidAltSource) return defaultValue;
	var keyPath = String(key);
	if (isValidSource && keyPath in source) return source[keyPath];
	if (isValidAltSource && keyPath in altSource) return altSource[keyPath];
	if (keyPath.indexOf(".") === -1) return defaultValue;
	var keys = keyPath.split(".");
	if (isValidSource) {
		var sourceResult = WalkPath(source, keys, defaultValue);
		if (sourceResult.found) return sourceResult.value;
	}
	if (isValidAltSource) {
		var altSourceResult = WalkPath(altSource, keys, defaultValue);
		if (altSourceResult.found) return altSourceResult.value;
	}
	return defaultValue;
};
var WalkPath = function(source, keys, defaultValue) {
	var parent = source;
	var value = defaultValue;
	var found;
	for (var index = 0, cnt = keys.length; index < cnt; index++) {
		var partKey = keys[index];
		if (parent && (typeof parent === "object" || typeof parent === "function")) found = partKey in parent;
		else found = false;
		if (!found) {
			WalkPathResult.found = false;
			return WalkPathResult;
		}
		value = parent[partKey];
		parent = value;
	}
	WalkPathResult.found = true;
	WalkPathResult.value = value;
	return WalkPathResult;
};
var WalkPathResult = {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/object/Clear.js
var Clear$1 = function(obj) {
	if (typeof obj !== "object" || obj === null) return obj;
	if (Array.isArray(obj)) obj.length = 0;
	else for (var key in obj) delete obj[key];
	return obj;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/data/DataMethods.js
var DataMethods_default$2 = {
	enableData() {
		if (this.data === void 0) this.data = {};
		return this;
	},
	setData(key, value) {
		this.enableData();
		if (arguments.length === 1) {
			var data = key;
			for (key in data) this.data[key] = data[key];
		} else this.data[key] = value;
		return this;
	},
	getData(key, defaultValue) {
		this.enableData();
		return key === void 0 ? this.data : GetValue$279(this.data, key, defaultValue);
	},
	incData(key, inc, defaultValue) {
		if (defaultValue === void 0) defaultValue = 0;
		this.enableData();
		this.setData(key, this.getData(key, defaultValue) + inc);
		return this;
	},
	mulData(key, mul, defaultValue) {
		if (defaultValue === void 0) defaultValue = 0;
		this.enableData();
		this.setData(key, this.getData(key, defaultValue) * mul);
		return this;
	},
	clearData() {
		if (this.data) Clear$1(this.data);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/bob/Base.js
var Base$6 = class {
	constructor(parent, type) {
		this.type = type;
		this.data = void 0;
		this.setParent(parent).reset().setActive();
	}
	destroy() {
		if (this.parent) this.parent.removeChild(this);
	}
	setParent(parent) {
		this.parent = parent;
		return this;
	}
	setDisplayListDirty(displayListDirty) {
		if (displayListDirty && this.parent) this.parent.displayListDirty = true;
		return this;
	}
	get active() {
		return this._active;
	}
	set active(value) {
		this.setDisplayListDirty(this._active != value);
		this._active = value;
	}
	setActive(active) {
		if (active === void 0) active = true;
		this.active = active;
		return this;
	}
	modifyPorperties(o) {
		return this;
	}
	reset() {
		this.clearData();
	}
	onFree() {
		this.reset().setActive(false).setParent();
	}
};
Object.assign(Base$6.prototype, DataMethods_default$2);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/bob/RenderBase.js
var DegToRad$17 = __webpack_exports__Math.DegToRad;
var RadToDeg$14 = __webpack_exports__Math.RadToDeg;
var GetValue$278 = __webpack_exports__Utils.Objects.GetValue;
var RenderBase$1 = class extends Base$6 {
	get visible() {
		return this._visible;
	}
	set visible(value) {
		this.setDisplayListDirty(this._visible != value);
		this._visible = value;
	}
	setVisible(visible) {
		if (visible === void 0) visible = true;
		this.visible = visible;
		return this;
	}
	get alpha() {
		return this._alpha;
	}
	set alpha(value) {
		this.setDisplayListDirty(!!this._alpha !== !!value);
		this._alpha = value;
	}
	setAlpha(alpha) {
		this.alpha = alpha;
		return this;
	}
	setX(x) {
		this.x = x;
		return this;
	}
	setY(y) {
		this.y = y;
		return this;
	}
	setPosition(x, y) {
		this.x = x;
		this.y = y;
		return this;
	}
	setRotation(rotation) {
		this.rotation = rotation;
		return this;
	}
	get angle() {
		return RadToDeg$14(this.rotation);
	}
	set angle(value) {
		this.rotation = DegToRad$17(value);
	}
	setAngle(angle) {
		this.angle = angle;
		return this;
	}
	setScaleX(scaleX) {
		this.scaleX = scaleX;
		return this;
	}
	get width() {
		return this._width;
	}
	set width(value) {
		this._width = value;
	}
	setWidth(width, keepAspectRatio) {
		if (keepAspectRatio === void 0) keepAspectRatio = false;
		this.width = width;
		if (keepAspectRatio) this.scaleY = this.scaleX;
		return this;
	}
	setScaleY(scaleY) {
		this.scaleY = scaleY;
		return this;
	}
	setScale(scaleX, scaleY) {
		if (scaleY === void 0) scaleY = scaleX;
		this.scaleX = scaleX;
		this.scaleY = scaleY;
		return this;
	}
	get height() {
		return this._height;
	}
	set height(value) {
		this._height = value;
	}
	setHeight(height, keepAspectRatio) {
		if (keepAspectRatio === void 0) keepAspectRatio = false;
		this.height = height;
		if (keepAspectRatio) this.scaleX = this.scaleY;
		return this;
	}
	setScale(scaleX, scaleY) {
		if (scaleY === void 0) scaleY = scaleX;
		this.scaleX = scaleX;
		this.scaleY = scaleY;
		return this;
	}
	get displayWidth() {
		return this._width * this.scaleX;
	}
	set displayWidth(value) {
		this.scaleX = value / this._width;
	}
	setDisplayWidth(width, keepAspectRatio) {
		if (keepAspectRatio === void 0) keepAspectRatio = false;
		this.displayWidth = width;
		if (keepAspectRatio) this.scaleY = this.scaleX;
		return this;
	}
	get displayHeight() {
		return this._height * this.scaleY;
	}
	set displayHeight(value) {
		this.scaleY = value / this._height;
	}
	setDisplayHeight(height, keepAspectRatio) {
		if (keepAspectRatio === void 0) keepAspectRatio = false;
		this.displayHeight = height;
		if (keepAspectRatio) this.scaleX = this.scaleY;
		return this;
	}
	setOriginX(originX) {
		this.originX = originX;
		this._displayOriginX = this.width * originX;
		return this;
	}
	setOriginY(originY) {
		this.originY = originY;
		this._displayOriginY = this.height * originY;
		return this;
	}
	setOrigin(originX, originY) {
		if (originY === void 0) originY = originX;
		this.setOriginX(originX).setOriginY(originY);
		return this;
	}
	get depth() {
		return this._depth;
	}
	set depth(value) {
		this.setDisplayListDirty(this._depth != value);
		this._depth = value;
	}
	setDepth(depth) {
		if (depth === void 0) depth = 0;
		this.depth = depth;
		return this;
	}
	modifyPorperties(o) {
		if (!o) return this;
		if (o.hasOwnProperty("x")) this.setX(o.x);
		if (o.hasOwnProperty("y")) this.setY(o.y);
		if (o.hasOwnProperty("rotation")) this.setRotation(o.rotation);
		else if (o.hasOwnProperty("angle")) this.setAngle(o.angle);
		if (o.hasOwnProperty("alpha")) this.setAlpha(o.alpha);
		var width = GetValue$278(o, "width", void 0);
		var height = GetValue$278(o, "height", void 0);
		var scale = GetValue$278(o, "scale", void 0);
		var scaleX = GetValue$278(o, "scaleX", scale);
		var scaleY = GetValue$278(o, "scaleY", scale);
		if (width !== void 0) {
			if (height === void 0 && scaleY === void 0) this.setWidth(width, true);
			else this.setWidth(width);
		} else if (scaleX !== void 0) this.setScaleX(scaleX);
		else if (o.hasOwnProperty("displayWidth")) this.setDisplayWidth(o.displayWidth);
		if (height !== void 0) {
			if (width === void 0 && scaleX === void 0) this.setHeight(height, true);
			else this.setHeight(height);
		} else if (scaleY !== void 0) this.setScaleY(scaleY);
		else if (o.hasOwnProperty("displayHeight")) this.setDisplayHeight(o.displayHeight);
		var origin = GetValue$278(o, "origin", void 0);
		if (origin !== void 0) this.setOrigin(origin);
		else {
			if (o.hasOwnProperty("originX")) this.setOriginX(o.originX);
			if (o.hasOwnProperty("originY")) this.setOriginY(o.originY);
		}
		if (o.hasOwnProperty("depth")) this.setDepth(o.depth);
		return this;
	}
	reset() {
		super.reset();
		this.setVisible().setAlpha(1).setPosition(0, 0).setRotation(0).setScale(1, 1).setOrigin(0).setDepth(0);
		return this;
	}
	webglRender(submitter, drawingContext, parentMatrix, calcMatrix, alpha, dx, dy) {}
	canvasRender(ctx, dx, dy, roundPixels) {}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/bob/image/WebglRender.js
var TransformMatrix$4 = __webpack_exports__GameObjects.Components.TransformMatrix;
var GetTint = __webpack_exports__Renderer.WebGL.Utils.getTintAppendFloatAlpha;
var tempMatrix$2 = new TransformMatrix$4();
var tempTransformer = { quad: /* @__PURE__ */ new Float32Array(8) };
var tempTexturer = {};
var tempTinter = {};
var WebglRender = function(submitter, drawingContext, parentMatrix, calcMatrix, alpha, dx, dy) {
	var frame = this.frame;
	if (!frame) return;
	var width = this._width, height = this._height;
	var displayOriginX = width * this.originX, displayOriginY = height * this.originY;
	var x = this.x - dx, y = this.y - dy;
	var frameX, frameY;
	var frameWidth, frameHeight;
	if (this.isCropped) {
		var crop = this._crop;
		if (crop.flipX !== this.flipX || crop.flipY !== this.flipY) frame.updateCropUVs(crop, this.flipX, this.flipY);
		crop.u0;
		crop.v0;
		crop.u1;
		crop.v1;
		frameWidth = crop.width;
		frameHeight = crop.height;
		frameX = crop.x;
		frameY = crop.y;
	} else {
		this.frame.u0;
		this.frame.v0;
		this.frame.u1;
		this.frame.v1;
		frameWidth = width;
		frameHeight = height;
		frameX = 0;
		frameY = 0;
	}
	var flipX = 1;
	var flipY = 1;
	if (this.flipX) {
		x += width - displayOriginX * 2;
		flipX = -1;
	}
	if (this.flipY) {
		y += height - displayOriginY * 2;
		flipY = -1;
	}
	tempMatrix$2.applyITRS(x, y, this.rotation, this.scaleX * flipX, this.scaleY * flipY);
	calcMatrix.multiply(tempMatrix$2, tempMatrix$2);
	var tx = -displayOriginX + frameX;
	var ty = -displayOriginY + frameY;
	var tw = tx + frameWidth;
	var th = ty + frameHeight;
	tempMatrix$2.setQuad(tx, ty, tw, th, tempTransformer.quad);
	tempTexturer.frame = frame;
	tempTexturer.uvSource = frame;
	var tint = GetTint(this.tint, this.alpha * alpha);
	tempTinter.tintEffect = this.tintFill;
	tempTinter.tintTopLeft = tint;
	tempTinter.tintBottomLeft = tint;
	tempTinter.tintTopRight = tint;
	tempTinter.tintBottomRight = tint;
	submitter.run(drawingContext, this.parent, parentMatrix, 0, tempTexturer, tempTransformer, tempTinter, void 0, 0);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/bob/image/CanvasRender.js
var CanvasRender = function(ctx, dx, dy, roundPixels) {
	var frame = this.frame;
	if (!frame) return;
	ctx.save();
	var width = this._width, height = this._height;
	var displayOriginX = width * this.originX, displayOriginY = height * this.originY;
	var x = this.x - displayOriginX, y = this.y - displayOriginY;
	var frameX, frameY;
	var frameWidth, frameHeight;
	if (this.isCropped) {
		var crop = this._crop;
		if (crop.flipX !== this.flipX || crop.flipY !== this.flipY) frame.updateCropUVs(crop, this.flipX, this.flipY);
		frameWidth = crop.cw;
		frameHeight = crop.ch;
		frameX = crop.cx;
		frameY = crop.cy;
	} else {
		frameWidth = frame.cutWidth;
		frameHeight = frame.cutHeight;
		frameX = frame.cutX;
		frameY = frame.cutY;
	}
	var flipX = 1;
	var flipY = 1;
	if (this.flipX) {
		x += width;
		flipX = -1;
	}
	if (this.flipY) {
		y += height;
		flipY = -1;
	}
	var res = frame.source.resolution;
	var fw = frameWidth / res;
	var fh = frameHeight / res;
	if (roundPixels) {
		x = Math.floor(x + .5);
		y = Math.floor(y + .5);
		fw += .5;
		fh += .5;
	}
	ctx.translate(x, y);
	ctx.rotate(this.rotation);
	ctx.scale(this.scaleX * flipX, this.scaleY * flipY);
	ctx.drawImage(frame.source.image, frameX, frameY, frameWidth, frameHeight, 0, 0, fw, fh);
	ctx.restore();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/bob/image/ImageData.js
var IsPlainObject$57 = __webpack_exports__Utils.Objects.IsPlainObject;
var ImageData$1 = class extends RenderBase$1 {
	constructor(parent, frame) {
		super(parent, ImageTypeName$1);
		this._crop = ResetCropObject();
		this.setFrame(frame);
	}
	get width() {
		return this._width;
	}
	set width(value) {}
	get height() {
		return this._height;
	}
	set height(value) {}
	setFrame(frame) {
		if (arguments.length > 0 && !IsPlainObject$57(frame)) frame = this.parent.texture.get(frame);
		this.frame = frame;
		if (frame) {
			this._width = frame.realWidth;
			this._height = frame.realHeight;
		} else {
			this._width = 0;
			this._height = 0;
		}
		return this;
	}
	setFlipX(flipX) {
		if (flipX === void 0) flipX = true;
		this.flipX = flipX;
		return this;
	}
	setFlipY(flipY) {
		if (flipY === void 0) flipY = true;
		this.flipY = flipY;
		return this;
	}
	resetFlip() {
		this.flipX = false;
		this.flipY = false;
		return this;
	}
	get tint() {
		if (this._tint === void 0) return this.parent.tint;
		else return this._tint;
	}
	set tint(value) {
		this._tint = value;
	}
	setTint(value) {
		this.tint = value;
		return this;
	}
	setTintFill(mode) {
		if (mode === void 0 || mode === true) mode = __webpack_exports__TintModes.FILL;
		else if (mode === false) mode = __webpack_exports__TintModes.MULTIPLY;
		this.tintFill = mode;
		return this;
	}
	clearTint() {
		this.tint = 16777215;
		this.tintFill = void 0;
		return this;
	}
	resetTint() {
		this.tint = void 0;
		this.tintFill = void 0;
		return this;
	}
	get tintFill() {
		if (this._tintFill === void 0) return this.parent.tintFill;
		else return this._tintFill;
	}
	set tintFill(value) {
		this._tintFill = value;
	}
	setCrop(x, y, width, height) {
		if (x === void 0) {
			this.isCropped = false;
			return this;
		}
		if (!this.frame) return this;
		if (x === 0 && y === 0 && width === this._width && height === this._height) {
			this.isCropped = false;
			return this;
		}
		this.frame.setCropUVs(this._crop, x, y, width, height, this.flipX, this.flipY);
		this.isCropped = true;
		return this;
	}
	reset() {
		super.reset();
		this.resetFlip().resetTint().setFrame().setCrop();
		return this;
	}
	modifyPorperties(o) {
		if (!o) return this;
		if (o.hasOwnProperty("width")) {
			o.displayWidth = o.width;
			delete o.width;
		}
		if (o.hasOwnProperty("height")) {
			o.displayHeight = o.height;
			delete o.height;
		}
		if (o.hasOwnProperty("frame")) this.setFrame(o.frame);
		super.modifyPorperties(o);
		if (o.hasOwnProperty("flipX")) this.setFlipX(o.flipX);
		if (o.hasOwnProperty("flipY")) this.setFlipY(o.flipY);
		if (o.hasOwnProperty("tint")) this.setTint(o.tint);
		if (o.hasOwnProperty("tintFill")) this.setTintFill(o.tintFill);
		return this;
	}
};
var ResetCropObject = function(out) {
	if (out === void 0) out = {};
	out.u0 = 0;
	out.v0 = 0;
	out.u1 = 0;
	out.v1 = 0;
	out.x = 0;
	out.y = 0;
	out.width = 0;
	out.height = 0;
	out.flipX = false;
	out.flipY = false;
	out.cx = 0;
	out.cy = 0;
	out.cw = 0, out.ch = 0;
	return out;
};
var methods$43 = {
	webglRender: WebglRender,
	canvasRender: CanvasRender
};
Object.assign(ImageData$1.prototype, methods$43);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/blitterbase/utils/AddImage.js
var AddImage$2 = function(blitter, config) {
	if (typeof config === "string") config = { frame: config };
	var bob = blitter.poolManager ? blitter.poolManager.allocate(ImageTypeName$1) : null;
	if (bob === null) bob = new ImageData$1(blitter);
	else bob.setParent(blitter).setActive();
	bob.modifyPorperties(config);
	blitter.addChild(bob);
	return bob;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/ninepatch/texture/DrawImage.js
var DrawImage$1 = function(key, frame, x, y, width, height) {
	AddImage$2(this, {
		frame,
		x,
		y,
		width,
		height
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/ninepatch/texture/DrawTileSprite.js
var DrawTileSprite = function(key, frame, x, y, width, height) {
	var frameObj = this.texture.get(frame);
	var frameWidth = frameObj.width, frameHeight = frameObj.height;
	var lastFrameWidth = width % frameWidth, lastFrameHeight = height % frameHeight;
	if (lastFrameWidth === 0) lastFrameWidth = frameWidth;
	if (lastFrameHeight === 0) lastFrameHeight = frameHeight;
	var colCount = Math.ceil(width / frameWidth), rowCount = Math.ceil(height / frameHeight);
	var lastColCount = colCount - 1, lastRowCount = rowCount - 1;
	for (var colIndex = 0; colIndex < colCount; colIndex++) for (var rowIndex = 0; rowIndex < rowCount; rowIndex++) {
		let bob = AddImage$2(this, {
			frame,
			x: x + colIndex * frameWidth,
			y: y + rowIndex * frameHeight
		});
		var cropWidth = colIndex === lastColCount ? lastFrameWidth : frameWidth;
		var cropHeight = rowIndex === lastRowCount ? lastFrameHeight : frameHeight;
		if (cropWidth !== frameWidth || cropHeight !== frameHeight) bob.setCrop(0, 0, cropWidth, cropHeight);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/ninepatch/Methods.js
var Methods_default = {
	_drawImage: DrawImage$1,
	_drawTileSprite: DrawTileSprite
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/blitter/ninepatch/NinePatch.js
var NinePatch = class extends NinePatchBase(Blitter, "rexNinePatch2") {
	setBaseTexture(key, baseFrameName, columns, rows) {
		this.setTexture(key, baseFrameName);
		super.setBaseTexture(key, baseFrameName, columns, rows);
		return this;
	}
};
Object.assign(NinePatch.prototype, Methods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/ninepatch2/NinePatch.js
var NinePatch_default = NinePatch;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/ninepatch2/Factory.js
ObjectFactory.register("ninePatch2", function(x, y, width, height, key, columns, rows, config) {
	var gameObject = new NinePatch_default(this.scene, x, y, width, height, key, columns, rows, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.NinePatch2", NinePatch_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/utils/render/FillPathWebGL.js
var Utils$2 = __webpack_exports__Renderer.WebGL.Utils;
var FillPathWebGL = function(drawingContext, submitter, calcMatrix, gameObject, shapeData, alpha, dx, dy) {
	var fillTintColor = Utils$2.getTintAppendFloatAlpha(shapeData.fillColor, shapeData.fillAlpha * alpha);
	var path = shapeData.pathData;
	var pathIndexes = shapeData.pathIndexes;
	var length = path.length;
	var pathIndex, pointX, pointY, x, y;
	var vertices = Array(length * 2);
	var colors = Array(length);
	var verticesIndex = 0;
	var colorsIndex = 0;
	for (pathIndex = 0; pathIndex < length; pathIndex += 2) {
		pointX = path[pathIndex] - dx;
		pointY = path[pathIndex + 1] - dy;
		x = calcMatrix.getX(pointX, pointY);
		y = calcMatrix.getY(pointX, pointY);
		vertices[verticesIndex++] = x;
		vertices[verticesIndex++] = y;
		colors[colorsIndex++] = fillTintColor;
	}
	submitter.batch(drawingContext, pathIndexes, vertices, colors, gameObject.lighting);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/utils/render/StrokePathWebGL.js
var Utils$1 = __webpack_exports__Renderer.WebGL.Utils;
var StrokePathWebGL = function(drawingContext, submitter, calcMatrix, gameObject, shapeData, alpha, dx, dy) {
	var strokeTintColor = Utils$1.getTintAppendFloatAlpha(shapeData.strokeColor, shapeData.strokeAlpha * alpha);
	var strokePath = gameObject.customRenderNodes.StrokePath || gameObject.defaultRenderNodes.StrokePath;
	var lineWidth = shapeData.lineWidth;
	var openPath = !shapeData.closePath;
	var isDashed = shapeData.isDashed && !!shapeData.strokePathData && !!shapeData.strokePathMask;
	var RunStrokePath = function(pointPath, pathIsOpen) {
		if (pointPath.length < 2) return;
		strokePath.run(drawingContext, submitter, pointPath, lineWidth, pathIsOpen, calcMatrix, strokeTintColor, strokeTintColor, strokeTintColor, strokeTintColor, void 0, gameObject.lighting);
	};
	if (!isDashed) {
		var path = shapeData.pathData;
		if (!path || path.length < 4) return;
		var pathLength = path.length - 1;
		if (openPath) pathLength -= 2;
		var pointPath = [];
		for (var i = 0; i < pathLength; i += 2) pointPath.push({
			x: path[i] - dx,
			y: path[i + 1] - dy,
			width: lineWidth
		});
		RunStrokePath(pointPath, openPath);
	} else {
		var dashedPath = shapeData.strokePathData;
		if (!dashedPath || dashedPath.length < 4) return;
		var strokePathMask = shapeData.strokePathMask;
		var dashedPathLength = dashedPath.length - 1;
		if (openPath) dashedPathLength -= 2;
		var px1 = dashedPath[0] - dx;
		var py1 = dashedPath[1] - dy;
		var drawMaskIdx = 0;
		var pointPath = [];
		for (var j = 2; j < dashedPathLength; j += 2) {
			var px2 = dashedPath[j] - dx;
			var py2 = dashedPath[j + 1] - dy;
			if (strokePathMask[drawMaskIdx]) {
				if (pointPath.length === 0 || pointPath[pointPath.length - 1].x !== px1 || pointPath[pointPath.length - 1].y !== py1) pointPath.push({
					x: px1,
					y: py1,
					width: lineWidth
				});
				pointPath.push({
					x: px2,
					y: py2,
					width: lineWidth
				});
			} else {
				RunStrokePath(pointPath, true);
				pointPath = [];
			}
			px1 = px2;
			py1 = py2;
			drawMaskIdx++;
		}
		RunStrokePath(pointPath, true);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/utils/polygonbase/render/WebGLRenderer.js
var GetCalcMatrix$2 = __webpack_exports__GameObjects.GetCalcMatrix;
var PolygonWebGLRenderer = function(renderer, src, drawingContext, parentMatrix) {
	if (src.dirty) {
		src.updateData();
		src.dirty = false;
	}
	var camera = drawingContext.camera;
	camera.addToRenderList(src);
	var calcMatrix = GetCalcMatrix$2(src, camera, parentMatrix, !drawingContext.useCanvas).calc;
	var dx = src._displayOriginX;
	var dy = src._displayOriginY;
	var alpha = src.alpha;
	var submitter = src.customRenderNodes.Submitter || src.defaultRenderNodes.Submitter;
	if (src.isFilled) FillPathWebGL(drawingContext, submitter, calcMatrix, src, src, alpha, dx, dy);
	if (src.isStroked) StrokePathWebGL(drawingContext, submitter, calcMatrix, src, src, alpha, dx, dy);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/utils/render/FillStyleCanvas.js
var FillStyleCanvas = function(ctx, src, altColor, altAlpha) {
	var fillColor = altColor ? altColor : src.fillColor;
	var fillAlpha = altAlpha ? altAlpha : src.fillAlpha;
	var red = (fillColor & 16711680) >>> 16;
	var green = (fillColor & 65280) >>> 8;
	var blue = fillColor & 255;
	ctx.fillStyle = "rgba(" + red + "," + green + "," + blue + "," + fillAlpha + ")";
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/utils/render/FillPathCanvas.js
var FillPathCanvas = function(ctx, src, dx, dy) {
	var path = src.pathData;
	if (!path || path.length < 4) return;
	var pathLength = path.length - 1;
	var px1 = path[0] - dx;
	var py1 = path[1] - dy;
	ctx.beginPath();
	ctx.moveTo(px1, py1);
	if (!src.closePath) pathLength -= 2;
	for (var i = 2; i < pathLength; i += 2) {
		var px2 = path[i] - dx;
		var py2 = path[i + 1] - dy;
		ctx.lineTo(px2, py2);
	}
	if (src.closePath) ctx.closePath();
	FillStyleCanvas(ctx, src);
	ctx.fill();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/utils/render/LineStyleCanvas.js
var LineStyleCanvas = function(ctx, src, altColor, altAlpha) {
	var strokeColor = altColor ? altColor : src.strokeColor;
	var strokeAlpha = altAlpha ? altAlpha : src.strokeAlpha;
	var red = (strokeColor & 16711680) >>> 16;
	var green = (strokeColor & 65280) >>> 8;
	var blue = strokeColor & 255;
	ctx.strokeStyle = "rgba(" + red + "," + green + "," + blue + "," + strokeAlpha + ")";
	ctx.lineWidth = src.lineWidth;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/utils/render/StrokePathCanvas.js
var StrokePathCanvas = function(ctx, src, dx, dy) {
	var isDashed = src.isDashed && !!src.strokePathData;
	var path = !isDashed ? src.pathData : src.strokePathData;
	if (!path || path.length < 4) return;
	var pathLength = path.length - 1;
	var px1 = path[0] - dx;
	var py1 = path[1] - dy;
	LineStyleCanvas(ctx, src);
	ctx.beginPath();
	if (!src.closePath) pathLength -= 2;
	if (!isDashed) {
		ctx.moveTo(px1, py1);
		for (var i = 2; i < pathLength; i += 2) {
			var px2 = path[i] - dx;
			var py2 = path[i + 1] - dy;
			ctx.lineTo(px2, py2);
		}
		if (src.closePath) ctx.closePath();
	} else {
		var strokePathMask = src.strokePathMask;
		var drawMaskIdx = 0;
		for (var i = 2; i < pathLength; i += 2) {
			var px2 = path[i] - dx;
			var py2 = path[i + 1] - dy;
			if (strokePathMask[drawMaskIdx]) {
				ctx.moveTo(px1, py1);
				ctx.lineTo(px2, py2);
			}
			px1 = px2;
			py1 = py2;
			drawMaskIdx++;
		}
	}
	ctx.stroke();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/utils/polygonbase/render/CanvasRenderer.js
var SetTransform$1 = __webpack_exports__Renderer.Canvas.SetTransform;
var PolygonCanvasRenderer = function(renderer, src, camera, parentMatrix) {
	if (src.dirty) {
		src.updateData();
		src.dirty = false;
	}
	camera.addToRenderList(src);
	var ctx = renderer.currentContext;
	if (SetTransform$1(renderer, ctx, src, camera, parentMatrix)) {
		var dx = src._displayOriginX;
		var dy = src._displayOriginY;
		if (src.isFilled) FillPathCanvas(ctx, src, dx, dy);
		if (src.isStroked) StrokePathCanvas(ctx, src, dx, dy);
		ctx.restore();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/utils/polygonbase/render/Render.js
var Render_default$4 = {
	renderWebGL: PolygonWebGLRenderer,
	renderCanvas: PolygonCanvasRenderer
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/utils/strokepath/BuildDashStroke.js
var EPSILON = 1e-6;
var DEFAULT_SEGMENT_COUNT = 10;
var DEFAULT_DRAW_RATIO = .5;
var NormalizeDashArray = function(dashPattern) {
	if (!Array.isArray(dashPattern)) return null;
	var normalized = [];
	for (var i = 0, cnt = dashPattern.length; i < cnt; i++) {
		var d = Number(dashPattern[i]);
		if (isFinite(d) && d > 0) normalized.push(d);
	}
	return normalized.length > 0 ? normalized : null;
};
var BuildAutoDashPattern = function(dashPattern, totalPathLength) {
	var { segments = DEFAULT_SEGMENT_COUNT, drawRatio = DEFAULT_DRAW_RATIO } = dashPattern;
	segments = Math.round(segments);
	if (!isFinite(segments) || segments <= 0) return null;
	if (!(totalPathLength > EPSILON)) return null;
	var segmentLength = totalPathLength / segments;
	if (!(segmentLength > EPSILON)) return null;
	drawRatio = Math.max(0, Math.min(1, drawRatio));
	if (drawRatio >= .999999) return null;
	var drawLength = segmentLength * drawRatio;
	if (drawLength <= EPSILON) drawLength = EPSILON;
	var gapLength = segmentLength - drawLength;
	if (gapLength <= EPSILON) return null;
	return [drawLength, gapLength];
};
var NormalizeDashPattern = function(dashPattern, totalPathLength) {
	return NormalizeDashArray(dashPattern) || BuildAutoDashPattern(dashPattern, totalPathLength);
};
var WrapOffset = function(offset, totalLength) {
	if (!isFinite(offset)) offset = 0;
	offset = offset % totalLength;
	if (offset < 0) offset += totalLength;
	return offset;
};
var ForEachStrokeSegment = function(pathData, closePath, callback) {
	if (!pathData || pathData.length < 4) return;
	var pathLength = pathData.length - 1;
	if (!closePath) pathLength -= 2;
	if (pathLength < 2) return;
	var px1 = pathData[0];
	var py1 = pathData[1];
	for (var i = 2; i < pathLength; i += 2) {
		var px2 = pathData[i];
		var py2 = pathData[i + 1];
		callback(px1, py1, px2, py2);
		px1 = px2;
		py1 = py2;
	}
};
var GetTotalPathLength = function(pathData, closePath) {
	var totalLength = 0;
	ForEachStrokeSegment(pathData, closePath, function(x0, y0, x1, y1) {
		var dx = x1 - x0;
		var dy = y1 - y0;
		totalLength += Math.sqrt(dx * dx + dy * dy);
	});
	return totalLength;
};
var BuildDashStroke = function(pathData, config, out) {
	if (config === void 0) config = {};
	if (out === void 0) out = {};
	var { closePath = false, dashPattern, dashOffset = 0 } = config;
	var totalPathLength = GetTotalPathLength(pathData, closePath);
	dashPattern = NormalizeDashPattern(dashPattern, totalPathLength);
	if (dashPattern === null) return null;
	var strokePathData = [];
	var strokePathMask = [];
	var totalPatternLength = 0;
	for (var i = 0, cnt = dashPattern.length; i < cnt; i++) totalPatternLength += dashPattern[i];
	if (totalPatternLength <= EPSILON) {
		out.strokePathData = pathData ? pathData.slice() : [];
		out.strokePathMask = void 0;
		return out;
	}
	var patternIndex = 0;
	var draw = true;
	var patternRemain = dashPattern[patternIndex];
	var AdvancePattern = function() {
		patternIndex = (patternIndex + 1) % dashPattern.length;
		draw = !draw;
		patternRemain = dashPattern[patternIndex];
	};
	var offset = WrapOffset(dashOffset, totalPatternLength);
	while (offset > EPSILON) if (offset < patternRemain - EPSILON) {
		patternRemain -= offset;
		offset = 0;
	} else {
		offset -= patternRemain;
		AdvancePattern();
	}
	var PushSegment = function(x0, y0, x1, y1, drawState) {
		if (strokePathData.length === 0) strokePathData.push(x0, y0);
		else {
			var lastX = strokePathData[strokePathData.length - 2];
			var lastY = strokePathData[strokePathData.length - 1];
			if (lastX !== x0 || lastY !== y0) strokePathData.push(x0, y0);
		}
		strokePathData.push(x1, y1);
		strokePathMask.push(drawState ? 1 : 0);
	};
	ForEachStrokeSegment(pathData, closePath, function(x0, y0, x1, y1) {
		var dx = x1 - x0;
		var dy = y1 - y0;
		var segLength = Math.sqrt(dx * dx + dy * dy);
		if (segLength <= EPSILON) return;
		var traveled = 0;
		while (traveled < segLength - EPSILON) {
			var step = Math.min(patternRemain, segLength - traveled);
			if (step <= EPSILON) {
				AdvancePattern();
				continue;
			}
			var t0 = traveled / segLength;
			var t1 = (traveled + step) / segLength;
			PushSegment(x0 + dx * t0, y0 + dy * t0, x0 + dx * t1, y0 + dy * t1, draw);
			traveled += step;
			patternRemain -= step;
			if (patternRemain <= EPSILON) AdvancePattern();
		}
	});
	if (!closePath && strokePathData.length >= 2) strokePathData.push(strokePathData[strokePathData.length - 2], strokePathData[strokePathData.length - 1]);
	out.strokePathData = strokePathData;
	out.strokePathMask = strokePathMask;
	return out;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/utils/strokepath/StrokePathMethods.js
var SetDashPattern = function(dashPattern, dashOffset) {
	this.dashPattern = dashPattern;
	this.dashOffset = dashOffset || 0;
	this.isDashed = !!dashPattern;
	return this;
};
var ClearDashPattern = function() {
	this.setDashPattern();
	return this;
};
var SetDashed = function(enable) {
	if (enable === void 0) enable = true;
	this.isDashed = enable;
	return this;
};
var BuildStrokePath = function() {
	if (this.isDashed) {
		var result = BuildDashStroke(this.pathData, {
			closePath: this.closePath,
			dashPattern: this.dashPattern,
			dashOffset: this.dashOffset
		}, this);
		if (result) {
			this.strokePathData = result.strokePathData;
			this.strokePathMask = result.strokePathMask;
		} else this.isDashed = false;
	}
	return this;
};
var StrokePathConfigMethods = {
	setDashPattern: SetDashPattern,
	clearDashPattern: ClearDashPattern,
	setDashed: SetDashed
};
var Methods$24 = { buildStrokePath: BuildStrokePath };
Object.assign(Methods$24, StrokePathConfigMethods);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/utils/polygonbase/PolygnBase.js
var Shape$1 = __webpack_exports__GameObjects.Shape;
var PolygnBase = class extends Shape$1 {
	init() {
		this.isDashed = false;
		this.strokePathData = void 0;
		this.strokePathMask = void 0;
		this.dashPattern = void 0;
		this.dashOffset = 0;
	}
	get fillColor() {
		return this._fillColor;
	}
	set fillColor(value) {
		this._fillColor = value;
		this.isFilled = value != null && this._fillAlpha > 0;
	}
	get fillAlpha() {
		return this._fillAlpha;
	}
	set fillAlpha(value) {
		this._fillAlpha = value;
		this.isFilled = value > 0 && this._fillColor != null;
	}
	setFillStyle(color, alpha) {
		if (alpha === void 0) alpha = 1;
		this.fillColor = color;
		this.fillAlpha = alpha;
		return this;
	}
	get strokeColor() {
		return this._strokeColor;
	}
	set strokeColor(value) {
		this._strokeColor = value;
		this.isStroked = value != null && this._strokeAlpha > 0 && this._lineWidth > 0;
	}
	get strokeAlpha() {
		return this._strokeAlpha;
	}
	set strokeAlpha(value) {
		this._strokeAlpha = value;
		this.isStroked = value > 0 && this._strokeColor != null && this._lineWidth > 0;
	}
	get lineWidth() {
		return this._lineWidth;
	}
	set lineWidth(value) {
		this._lineWidth = value;
		this.isStroked = value > 0 && this._strokeColor != null;
	}
	setStrokeStyle(lineWidth, color, alpha) {
		if (alpha === void 0) alpha = 1;
		this.lineWidth = lineWidth;
		this.strokeColor = color;
		this.strokeAlpha = alpha;
		return this;
	}
	updateData() {
		this.buildStrokePath();
		return this;
	}
	get width() {
		return this.geom.width;
	}
	set width(value) {
		this.resize(value, this.height);
	}
	get height() {
		return this.geom.height;
	}
	set height(value) {
		this.resize(this.width, value);
	}
	setSize(width, height) {
		var input = this.input;
		if (input && !input.customHitArea) {
			input.hitArea.width = width;
			input.hitArea.height = height;
		}
		return this;
	}
	resize(width, height) {
		this.setSize(width, height);
		return this;
	}
};
Object.assign(PolygnBase.prototype, Methods$24, Render_default$4);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/geom/roundrectangle/RoundRectangle.js
var GetValue$277 = __webpack_exports__Utils.Objects.GetValue;
var RoundRectangle$3 = class {
	constructor(x, y, width, height, radiusConfig) {
		if (x === void 0) x = 0;
		if (y === void 0) y = x;
		if (width === void 0) width = 0;
		if (height === void 0) height = 0;
		if (radiusConfig === void 0) radiusConfig = 0;
		this.cornerRadius = {};
		this._width = 0;
		this._height = 0;
		this.setTo(x, y, width, height, radiusConfig);
	}
	setTo(x, y, width, height, radiusConfig) {
		this.setPosition(x, y);
		this.setRadius(radiusConfig);
		this.setSize(width, height);
		return this;
	}
	setPosition(x, y) {
		this.x = x;
		this.y = y;
		return this;
	}
	setRadius(value) {
		if (value === void 0) value = 0;
		this.radius = value;
		return this;
	}
	setSize(width, height) {
		this.width = width;
		this.height = height;
		return this;
	}
	get minWidth() {
		var radius = this.cornerRadius;
		return Math.max(radius.tl.x + radius.tr.x, radius.bl.x + radius.br.x);
	}
	get minHeight() {
		var radius = this.cornerRadius;
		return Math.max(radius.tl.y + radius.bl.y, radius.tr.y + radius.br.y);
	}
	get width() {
		return this._width;
	}
	set width(value) {
		if (value == null) value = 0;
		this._width = Math.max(value, this.minWidth);
	}
	get height() {
		return this._height;
	}
	set height(value) {
		if (value == null) value = 0;
		this._height = Math.max(value, this.minHeight);
	}
	get radius() {
		var radius = this.cornerRadius;
		return Math.max(radius.tl.x, radius.tl.y, radius.tr.x, radius.tr.y, radius.bl.x, radius.bl.y, radius.br.x, radius.br.y);
	}
	set radius(value) {
		var defaultRadiusX, defaultRadiusY;
		if (typeof value === "number") {
			defaultRadiusX = value;
			defaultRadiusY = value;
		} else {
			defaultRadiusX = GetValue$277(value, "x", 0);
			defaultRadiusY = GetValue$277(value, "y", 0);
		}
		var radius = this.cornerRadius;
		radius.tl = GetRadius(GetValue$277(value, "tl", void 0), defaultRadiusX, defaultRadiusY);
		radius.tr = GetRadius(GetValue$277(value, "tr", void 0), defaultRadiusX, defaultRadiusY);
		radius.bl = GetRadius(GetValue$277(value, "bl", void 0), defaultRadiusX, defaultRadiusY);
		radius.br = GetRadius(GetValue$277(value, "br", void 0), defaultRadiusX, defaultRadiusY);
	}
	get radiusTL() {
		var radius = this.cornerRadius.tl;
		return Math.max(radius.x, radius.y);
	}
	set radiusTL(value) {
		SetRadius(this.cornerRadius.tl, value);
	}
	get radiusTR() {
		var radius = this.cornerRadius.tr;
		return Math.max(radius.x, radius.y);
	}
	set radiusTR(value) {
		SetRadius(this.cornerRadius.tr, value);
	}
	get radiusBL() {
		var radius = this.cornerRadius.bl;
		return Math.max(radius.x, radius.y);
	}
	set radiusBL(value) {
		SetRadius(this.cornerRadius.bl, value);
	}
	get radiusBR() {
		var radius = this.cornerRadius.br;
		return Math.max(radius.x, radius.y);
	}
	set radiusBR(value) {
		SetRadius(this.cornerRadius.br, value);
	}
};
var GetRadius = function(radius, defaultRadiusX, defaultRadiusY) {
	if (radius === void 0) radius = {
		x: defaultRadiusX,
		y: defaultRadiusY
	};
	else if (typeof radius === "number") radius = {
		x: radius,
		y: radius
	};
	SetConvex(radius);
	return radius;
};
var SetRadius = function(radius, value) {
	if (typeof value === "number") {
		radius.x = value;
		radius.y = value;
	} else {
		radius.x = GetValue$277(value, "x", 0);
		radius.y = GetValue$277(value, "y", 0);
	}
	SetConvex(radius);
};
var SetConvex = function(radius) {
	radius.convex = radius.x >= 0 || radius.y >= 0;
	radius.x = Math.abs(radius.x);
	radius.y = Math.abs(radius.y);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/utils/IsArcCorner.js
var IsArcCorner$1 = function(radius) {
	return radius.x > 0 && radius.y > 0;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/geom/pathdata/LineTo.js
var LineTo = function(x, y, pathData) {
	var cnt = pathData.length;
	if (cnt >= 2) {
		var lastX = pathData[cnt - 2];
		var lastY = pathData[cnt - 1];
		if (x === lastX && y === lastY) return pathData;
	}
	pathData.push(x, y);
	return pathData;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/geom/pathdata/ArcTo.js
var DegToRad$16 = __webpack_exports__Math.DegToRad;
var ArcTo$1 = function(centerX, centerY, radiusX, radiusY, startAngle, endAngle, antiClockWise, iteration, pathData) {
	if (antiClockWise && endAngle > startAngle) endAngle -= 360;
	else if (!antiClockWise && endAngle < startAngle) endAngle += 360;
	var step = DegToRad$16(endAngle - startAngle) / iteration;
	startAngle = DegToRad$16(startAngle);
	for (var i = 0; i <= iteration; i++) {
		var angle = startAngle + step * i;
		LineTo(centerX + radiusX * Math.cos(angle), centerY + radiusY * Math.sin(angle), pathData);
	}
	return pathData;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/roundrectangle/RoundRectangle.js
var IsPlainObject$56 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$276 = __webpack_exports__Utils.Objects.GetValue;
var Earcut$3 = __webpack_exports__Geom.Polygon.Earcut;
var GetDefaultIteration = function(radius) {
	return Math.max(6, Math.min(16, Math.ceil(radius / 2)));
};
var RoundRectangle$2 = class extends PolygnBase {
	constructor(scene, x, y, width, height, radiusConfig, fillColor, fillAlpha) {
		var strokeColor, strokeAlpha, strokeWidth, shapeType;
		var dashPattern, dashOffset;
		if (IsPlainObject$56(x)) {
			var config = x;
			x = config.x;
			y = config.y;
			width = config.width;
			height = config.height;
			radiusConfig = config.radius;
			fillColor = config.color;
			fillAlpha = config.alpha;
			strokeColor = config.strokeColor;
			strokeAlpha = config.strokeAlpha;
			strokeWidth = config.strokeWidth;
			shapeType = config.shape;
			dashPattern = config.dashPattern;
			dashOffset = config.dashOffset;
		}
		if (x === void 0) x = 0;
		if (y === void 0) y = 0;
		if (width === void 0) width = 1;
		if (height === void 0) height = width;
		if (radiusConfig === void 0) radiusConfig = 0;
		if (shapeType === void 0) shapeType = 0;
		var geom = new RoundRectangle$3();
		super(scene, "rexRoundRectangleShape", geom);
		this.init();
		this.setShapeType(shapeType);
		if (this.shapeType === 0) {
			var radius = GetValue$276(radiusConfig, "radius", radiusConfig);
			geom.setTo(0, 0, width, height, radius);
		} else {
			var radius = {
				x: width / 2,
				y: height / 2
			};
			geom.setTo(0, 0, width, height, radius);
		}
		this.setIteration(GetValue$276(radiusConfig, "iteration", void 0));
		this.setPosition(x, y);
		this.setFillStyle(fillColor, fillAlpha);
		if (strokeWidth === void 0) strokeWidth = 2;
		this.setStrokeStyle(strokeWidth, strokeColor, strokeAlpha);
		if (dashPattern) this.setDashPattern(dashPattern, dashOffset);
		this.updateDisplayOrigin();
		this.dirty = true;
	}
	updateData() {
		var geom = this.geom;
		var pathData = this.pathData;
		pathData.length = 0;
		var width = geom.width, height = geom.height, cornerRadius = geom.cornerRadius, radius, iteration = this.iteration + 1;
		radius = cornerRadius.tl;
		if (IsArcCorner$1(radius)) {
			if (radius.convex) {
				var centerX = radius.x;
				var centerY = radius.y;
				ArcTo$1(centerX, centerY, radius.x, radius.y, 180, 270, false, iteration, pathData);
			} else {
				var centerX = 0;
				var centerY = 0;
				ArcTo$1(centerX, centerY, radius.x, radius.y, 90, 0, true, iteration, pathData);
			}
		} else LineTo(0, 0, pathData);
		radius = cornerRadius.tr;
		if (IsArcCorner$1(radius)) {
			if (radius.convex) {
				var centerX = width - radius.x;
				var centerY = radius.y;
				ArcTo$1(centerX, centerY, radius.x, radius.y, 270, 360, false, iteration, pathData);
			} else {
				var centerX = width;
				var centerY = 0;
				ArcTo$1(centerX, centerY, radius.x, radius.y, 180, 90, true, iteration, pathData);
			}
		} else LineTo(width, 0, pathData);
		radius = cornerRadius.br;
		if (IsArcCorner$1(radius)) {
			if (radius.convex) {
				var centerX = width - radius.x;
				var centerY = height - radius.y;
				ArcTo$1(centerX, centerY, radius.x, radius.y, 0, 90, false, iteration, pathData);
			} else {
				var centerX = width;
				var centerY = height;
				ArcTo$1(centerX, centerY, radius.x, radius.y, 270, 180, true, iteration, pathData);
			}
		} else LineTo(width, height, pathData);
		radius = cornerRadius.bl;
		if (IsArcCorner$1(radius)) {
			if (radius.convex) {
				var centerX = radius.x;
				var centerY = height - radius.y;
				ArcTo$1(centerX, centerY, radius.x, radius.y, 90, 180, false, iteration, pathData);
			} else {
				var centerX = 0;
				var centerY = height;
				ArcTo$1(centerX, centerY, radius.x, radius.y, 360, 270, true, iteration, pathData);
			}
		} else LineTo(0, height, pathData);
		pathData.push(pathData[0], pathData[1]);
		this.pathIndexes = Earcut$3(pathData);
		super.updateData();
		return this;
	}
	setShapeType(shapeType) {
		if (typeof shapeType === "string") shapeType = ShapeTypeMap[shapeType];
		this.shapeType = shapeType;
		return this;
	}
	setSize(width, height) {
		if (height === void 0) height = width;
		if (this.geom.width === width && this.geom.height === height) return this;
		this.geom.setSize(width, height);
		if (this.shapeType === 1) this.setRadius({
			x: width / 2,
			y: height / 2
		});
		this.updateDisplayOrigin();
		this.dirty = true;
		super.setSize(width, height);
		return this;
	}
	get radius() {
		return this.geom.radius;
	}
	set radius(value) {
		this.geom.setRadius(value);
		this.updateDisplayOrigin();
		this.dirty = true;
	}
	get radiusTL() {
		return this.geom.radiusTL;
	}
	set radiusTL(value) {
		this.geom.radiusTL = value;
		this.dirty = true;
	}
	get radiusTR() {
		return this.geom.radiusTR;
	}
	set radiusTR(value) {
		this.geom.radiusTR = value;
		this.dirty = true;
	}
	get radiusBL() {
		return this.geom.radiusBL;
	}
	set radiusBL(value) {
		this.geom.radiusBL = value;
		this.dirty = true;
	}
	get radiusBR() {
		return this.geom.radiusBR;
	}
	set radiusBR(value) {
		this.geom.radiusBR = value;
		this.dirty = true;
	}
	setRadius(value) {
		if (value === void 0) value = 0;
		this.radius = value;
		return this;
	}
	setRadiusTL(value) {
		if (value === void 0) value = 0;
		this.radiusTL = value;
		return this;
	}
	setRadiusTR(value) {
		if (value === void 0) value = 0;
		this.radiusTR = value;
		return this;
	}
	setRadiusBL(value) {
		if (value === void 0) value = 0;
		this.radiusBL = value;
		return this;
	}
	setRadiusBR(value) {
		if (value === void 0) value = 0;
		this.radiusBR = value;
		return this;
	}
	get cornerRadius() {
		return this.geom.cornerRadius;
	}
	set cornerRadius(value) {
		this.radius = value;
	}
	setCornerRadius(value) {
		return this.setRadius(value);
	}
	get iteration() {
		if (this._useDynamicIteration) return GetDefaultIteration(this.radius);
		return this._iteration;
	}
	set iteration(value) {
		var useDynamicIteration = value === void 0;
		if (this._useDynamicIteration === void 0) {
			this._useDynamicIteration = useDynamicIteration;
			this._iteration = value;
			return;
		}
		if (this._iteration === value && this._useDynamicIteration === useDynamicIteration) return;
		this._useDynamicIteration = useDynamicIteration;
		this._iteration = value;
		this.dirty = true;
	}
	setIteration(iteration) {
		this.iteration = iteration;
		return this;
	}
};
var ShapeTypeMap = {
	rectangle: 0,
	circle: 1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/roundrectangle/RoundRectangle.js
var RoundRectangle_default = RoundRectangle$2;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/roundrectangle/Factory.js
ObjectFactory.register("roundRectangle", function(x, y, width, height, radiusConfig, fillColor, fillAlpha) {
	var gameObject = new RoundRectangle_default(this.scene, x, y, width, height, radiusConfig, fillColor, fillAlpha);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.RoundRectangle", RoundRectangle_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/canvas/canvasbase/render/WebGLRenderer.js
var WebGLRenderer$4 = function(renderer, src, drawingContext, parentMatrix) {
	if (src.dirty) {
		src.updateTexture();
		src.dirty = false;
	}
	if (src.width === 0 || src.height === 0) return;
	drawingContext.camera.addToRenderList(src);
	var customRenderNodes = src.customRenderNodes;
	var defaultRenderNodes = src.defaultRenderNodes;
	(customRenderNodes.Submitter || defaultRenderNodes.Submitter).run(drawingContext, src, parentMatrix, 0, customRenderNodes.Texturer || defaultRenderNodes.Texturer, customRenderNodes.Transformer || defaultRenderNodes.Transformer);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/canvas/canvasbase/render/CanvasRenderer.js
var CanvasRenderer$3 = function(renderer, src, camera, parentMatrix) {
	if (src.dirty) {
		src.updateTexture();
		src.dirty = false;
	}
	if (src.width === 0 || src.height === 0) return;
	camera.addToRenderList(src);
	renderer.batchSprite(src, src.frame, camera, parentMatrix);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/canvas/canvasbase/render/Render.js
var Render_default$3 = {
	renderWebGL: WebGLRenderer$4,
	renderCanvas: CanvasRenderer$3
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/canvas/canvasbase/CanvasMethods.js
var Color$4 = __webpack_exports__Display.Color;
var CanvasMethods_default = {
	clear() {
		this.context.clearRect(0, 0, this.canvas.width, this.canvas.height);
		this.dirty = true;
		return this;
	},
	fill(color) {
		this.context.fillStyle = color;
		this.context.fillRect(0, 0, this.canvas.width, this.canvas.height);
		this.dirty = true;
		return this;
	},
	drawFrame(key, frame, dx, dy, dWidth, dHeight, sxOffset, syOffset, sWidth, sHeight) {
		var textureFrame = this.scene.sys.textures.getFrame(key, frame);
		if (!textureFrame) return this;
		var frameWidth = textureFrame.cutWidth, frameHeight = textureFrame.cutHeight;
		if (dx === void 0) dx = 0;
		if (dy === void 0) dy = 0;
		if (dWidth === void 0) dWidth = frameWidth;
		if (dHeight === void 0) dHeight = frameHeight;
		if (sxOffset === void 0) sxOffset = 0;
		if (syOffset === void 0) syOffset = 0;
		if (sWidth === void 0) sWidth = frameWidth;
		if (sHeight === void 0) sHeight = frameHeight;
		var sx = textureFrame.cutX + sxOffset;
		var sy = textureFrame.cutY + syOffset;
		this.context.drawImage(textureFrame.source.image, sx, sy, sWidth, sHeight, dx, dy, dWidth, dHeight);
		this.dirty = true;
		return this;
	},
	getDataURL(type, encoderOptions) {
		return this.canvas.toDataURL(type, encoderOptions);
	},
	getPixel(x, y, out) {
		if (out === void 0) out = new Color$4();
		var rgb = this.context.getImageData(x, y, 1, 1);
		out.setTo(rgb.data[0], rgb.data[1], rgb.data[2], rgb.data[3]);
		return out;
	},
	setPixel(x, y, r, g, b, a) {
		if (typeof r !== "number") {
			var color = r;
			r = color.red;
			g = color.green;
			b = color.blue;
			a = color.alpha;
		}
		if (a === void 0) a = r !== 0 || g !== 0 || b !== 0 ? 255 : 0;
		var imgData = this.context.createImageData(1, 1);
		imgData.data[0] = r;
		imgData.data[1] = g;
		imgData.data[2] = b;
		imgData.data[3] = a;
		this.context.putImageData(imgData, x, y);
		this.dirty = true;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/texture/CopyCanvasToTexture.js
var CopyCanvasToTexture = function(scene, srcCanvas, key, x, y, width, height) {
	var textures = scene.sys.textures;
	var renderer = scene.renderer;
	if (x === void 0) x = 0;
	if (y === void 0) y = 0;
	if (width === void 0) width = srcCanvas.width;
	if (height === void 0) height = srcCanvas.height;
	var texture;
	if (textures.exists(key)) texture = textures.get(key);
	else texture = textures.createCanvas(key, width, height);
	var destCanvas = texture.getSourceImage();
	if (destCanvas.width !== width) destCanvas.width = width;
	if (destCanvas.height !== height) destCanvas.height = height;
	var destCtx = destCanvas.getContext("2d", { willReadFrequently: true });
	destCtx.clearRect(0, 0, width, height);
	destCtx.drawImage(srcCanvas, x, y, width, height);
	if (renderer.gl && texture) renderer.canvasToTexture(destCanvas, texture.source[0].glTexture, true, 0);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/canvas/canvasbase/TextureMethods.js
var TextureMethods_default = {
	updateTexture(callback, scope) {
		var canvas = this.canvas;
		var context = this.context;
		if (callback) {
			var scale = this.resolution;
			if (scale !== 1) {
				this.context.save();
				this.context.scale(scale, scale);
			}
			if (scope) callback.call(scope, canvas, context);
			else callback(canvas, context);
			if (scale !== 1) this.context.restore();
		}
		var newWidth = canvas.width, newHeight = canvas.height;
		if (newWidth !== this.frame.width || newHeight !== this.frame.height) {
			this.frame.setSize(newWidth, newHeight);
			this.frame.source.updateSize(newWidth, newHeight);
			this.frame.updateUVs();
		}
		if (this.renderer && this.renderer.gl) {
			this.frame.source.glTexture = this.renderer.canvasToTexture(canvas, this.frame.source.glTexture, true);
			if (typeof WEBGL_DEBUG) this.frame.glTexture.spectorMetadata = { textureKey: "Canvas Game Object" };
		}
		this.dirty = false;
		var input = this.input;
		if (input && !input.customHitArea) {
			input.hitArea.width = this.width;
			input.hitArea.height = this.height;
		}
		return this;
	},
	generateTexture(key, x, y, width, height) {
		var srcCanvas = this.canvas;
		if (width === void 0) width = srcCanvas.width;
		else width *= this.resolution;
		if (height === void 0) height = srcCanvas.height;
		else height *= this.resolution;
		CopyCanvasToTexture(this.scene, srcCanvas, key, x, y, width, height);
		return this;
	},
	loadTexture(key, frame) {
		var textureFrame = this.scene.sys.textures.getFrame(key, frame);
		if (!textureFrame) return this;
		if (this.width !== textureFrame.cutWidth || this.height !== textureFrame.cutHeight) this.setSize(textureFrame.cutWidth, textureFrame.cutHeight);
		else this.clear();
		this.drawFrame(key, frame);
		this.dirty = true;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/canvas/canvasbase/Canvas.js
CheckPhaserVersion();
var CanvasPool$4 = __webpack_exports__Display.Canvas.CanvasPool;
var GameObject$3 = __webpack_exports__GameObjects.GameObject;
var UUID$5 = __webpack_exports__Utils.String.UUID;
var DefaultImageNodes$1 = __webpack_exports__Renderer.WebGL.RenderNodes.Defaults.DefaultImageNodes;
var Canvas$1 = class extends GameObject$3 {
	constructor(scene, x, y, width, height, resolution) {
		if (x === void 0) x = 0;
		if (y === void 0) y = 0;
		if (width === void 0) width = 1;
		if (height === void 0) height = 1;
		if (resolution === void 0) resolution = 1;
		super(scene, "rexCanvas");
		this.renderer = scene.sys.game.renderer;
		this._width = width;
		this._height = height;
		this.resolution = resolution;
		width = Math.max(Math.ceil(width * this.resolution), 1);
		height = Math.max(Math.ceil(height * this.resolution), 1);
		this.canvas = CanvasPool$4.create(this, width, height);
		this.dirty = false;
		this.setPosition(x, y);
		this.setOrigin(.5, .5);
		this.initRenderNodes(this._defaultRenderNodesMap);
		this._crop = this.resetCropObject();
		this._textureKey = UUID$5();
		this.texture = scene.sys.textures.addCanvas(this._textureKey, this.canvas);
		this.context = this.texture.context;
		this.frame = this.texture.get();
		this.frame.source.resolution = this.resolution;
		if (this.renderer && this.renderer.gl) {
			this.renderer.deleteTexture(this.frame.source.glTexture);
			this.frame.source.glTexture = null;
		}
		this.dirty = true;
	}
	preDestroy() {
		CanvasPool$4.remove(this.canvas);
		this.canvas = null;
		this.context = null;
		var texture = this.texture;
		if (texture) texture.destroy();
	}
	get _defaultRenderNodesMap() {
		return DefaultImageNodes$1;
	}
	setResolution(resolution) {
		if (this.resolution === resolution) return this;
		this.resolution = resolution;
		var width = Math.max(Math.ceil(this.width * resolution), 1);
		var height = Math.max(Math.ceil(this.height * resolution), 1);
		this.canvas.width = width;
		this.canvas.height = height;
		this.frame.source.resolution = resolution;
		this.dirty = true;
		return this;
	}
	get width() {
		return this._width;
	}
	set width(value) {
		this.setSize(value, this._height);
	}
	get height() {
		return this._height;
	}
	set height(value) {
		this.setSize(this._width, value);
	}
	setCanvasSize(width, height) {
		if (this._width === width && this._height === height) return this;
		this._width = width;
		this._height = height;
		this.updateDisplayOrigin();
		width = Math.max(Math.ceil(width * this.resolution), 1);
		height = Math.max(Math.ceil(height * this.resolution), 1);
		this.canvas.width = width;
		this.canvas.height = height;
		this.frame.setSize(width, height);
		this.frame.source.updateSize(width, height);
		this.frame.updateUVs();
		this.dirty = true;
		return this;
	}
	setSize(width, height) {
		this.setCanvasSize(width, height);
		return this;
	}
	get displayWidth() {
		return this.scaleX * this._width;
	}
	set displayWidth(value) {
		this.scaleX = value / this._width;
	}
	get displayHeight() {
		return this.scaleY * this._height;
	}
	set displayHeight(value) {
		this.scaleY = value / this._height;
	}
	setDisplaySize(width, height) {
		this.displayWidth = width;
		this.displayHeight = height;
		return this;
	}
	getCanvas(readOnly) {
		if (!readOnly) this.dirty = true;
		return this.canvas;
	}
	getContext(readOnly) {
		if (!readOnly) this.dirty = true;
		return this.context;
	}
	needRedraw() {
		this.dirty = true;
		return this;
	}
	resize(width, height) {
		this.setSize(width, height);
		return this;
	}
};
var Components$3 = __webpack_exports__GameObjects.Components;
__webpack_exports__Class.mixin(Canvas$1, [
	Components$3.Alpha,
	Components$3.BlendMode,
	Components$3.Crop,
	Components$3.Depth,
	Components$3.Flip,
	Components$3.GetBounds,
	Components$3.Lighting,
	Components$3.Mask,
	Components$3.Origin,
	Components$3.RenderNodes,
	Components$3.ScrollFactor,
	Components$3.Tint,
	Components$3.Transform,
	Components$3.Visible,
	Render_default$3,
	CanvasMethods_default,
	TextureMethods_default
]);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/canvas/GetStyle.js
var Pad$1 = __webpack_exports__Utils.String.Pad;
var GetStyle = function(style, canvas, context) {
	if (style == null) return style;
	switch (typeof style) {
		case "string": return style;
		case "number": return `#${Pad$1(Math.floor(style).toString(16), 6, "0", 1)}`;
		case "function": return style(canvas, context);
		case "object": if (style.hasOwnProperty("r")) {
			if (style.hasOwnProperty("a")) return `rgba(${style.r},${style.g},${style.b},${style.a})`;
			else return `rgb(${style.r},${style.g},${style.b})`;
		} else if (style.hasOwnProperty("h")) {
			if (style.hasOwnProperty("a")) return `hsla(${style.h},${style.s},${style.l},${style.a})`;
			else return `hsl(${style.h},${style.s},${style.l})`;
		} else return style;
		default: return style;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/canvas/AddRoundRectanglePath.js
var DegToRad$15 = __webpack_exports__Math.DegToRad;
var AddRoundRectanglePath = function(context, x, y, width, height, radiusConfig, iteration) {
	var geom = new RoundRectangle$3(x, y, width, height, radiusConfig), minWidth = geom.minWidth, minHeight = geom.minHeight, scaleRX = width >= minWidth ? 1 : width / minWidth, scaleRY = height >= minHeight ? 1 : height / minHeight;
	var cornerRadius = geom.cornerRadius;
	var radius, radiusX, radiusY, centerX, centerY;
	var startX, startY;
	context.save();
	context.beginPath();
	context.translate(x, y);
	radius = cornerRadius.tl;
	if (IsArcCorner(radius)) {
		radiusX = radius.x * scaleRX;
		radiusY = radius.y * scaleRY;
		if (IsConvexArc(radius)) {
			centerX = radiusX;
			centerY = radiusY;
			ArcTo(context, centerX, centerY, radiusX, radiusY, 180, 270, false, iteration);
		} else {
			centerX = 0;
			centerY = 0;
			ArcTo(context, centerX, centerY, radiusX, radiusY, 90, 0, true, iteration);
		}
		startX = 0;
		startY = radiusY;
	} else {
		context.lineTo(0, 0);
		startX = 0;
		startY = 0;
	}
	radius = cornerRadius.tr;
	if (IsArcCorner(radius)) {
		radiusX = radius.x * scaleRX;
		radiusY = radius.y * scaleRY;
		if (IsConvexArc(radius)) {
			centerX = width - radiusX;
			centerY = radiusY;
			ArcTo(context, centerX, centerY, radiusX, radiusY, 270, 360, false, iteration);
		} else {
			centerX = width;
			centerY = 0;
			ArcTo(context, centerX, centerY, radiusX, radiusY, 180, 90, true, iteration);
		}
	} else context.lineTo(width, 0);
	radius = cornerRadius.br;
	if (IsArcCorner(radius)) {
		radiusX = radius.x * scaleRX;
		radiusY = radius.y * scaleRY;
		if (IsConvexArc(radius)) {
			centerX = width - radiusX;
			centerY = height - radiusY;
			ArcTo(context, centerX, centerY, radiusX, radiusY, 0, 90, false, iteration);
		} else {
			centerX = width;
			centerY = height;
			ArcTo(context, centerX, centerY, radiusX, radiusY, 270, 180, true, iteration);
		}
	} else context.lineTo(width, height);
	radius = cornerRadius.bl;
	if (IsArcCorner(radius)) {
		radiusX = radius.x * scaleRX;
		radiusY = radius.y * scaleRY;
		if (IsConvexArc(radius)) {
			centerX = radiusX;
			centerY = height - radiusY;
			ArcTo(context, centerX, centerY, radiusX, radiusY, 90, 180, false, iteration);
		} else {
			centerX = 0;
			centerY = height;
			ArcTo(context, centerX, centerY, radiusX, radiusY, 360, 270, true, iteration);
		}
	} else context.lineTo(0, height);
	context.lineTo(startX, startY);
	context.closePath();
	context.restore();
};
var IsConvexArc = function(radius) {
	return !radius.hasOwnProperty("convex") || radius.convex;
};
var IsArcCorner = function(radius) {
	return radius.x > 0 && radius.y > 0;
};
var ArcTo = function(context, centerX, centerY, radiusX, radiusY, startAngle, endAngle, antiClockWise, iteration) {
	if (antiClockWise && endAngle > startAngle) endAngle -= 360;
	else if (!antiClockWise && endAngle < startAngle) endAngle += 360;
	startAngle = DegToRad$15(startAngle);
	endAngle = DegToRad$15(endAngle);
	if (iteration == null) context.ellipse(centerX, centerY, radiusX, radiusY, 0, startAngle, endAngle, antiClockWise);
	else {
		iteration += 1;
		var x, y, angle;
		var step = (endAngle - startAngle) / iteration;
		for (var i = 0; i <= iteration; i++) {
			angle = startAngle + step * i;
			x = centerX + radiusX * Math.cos(angle);
			y = centerY + radiusY * Math.sin(angle);
			context.lineTo(x, y);
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/canvas/DrawRoundRectangle.js
var DrawRoundRectangle = function(canvas, context, x, y, width, height, radiusConfig, fillStyle, strokeStyle, lineWidth, fillColor2, isHorizontalGradient, iteration) {
	AddRoundRectanglePath(context, x, y, width, height, radiusConfig, iteration);
	if (fillStyle != null) {
		if (fillColor2 != null) {
			var grd;
			if (isHorizontalGradient) grd = context.createLinearGradient(0, 0, width, 0);
			else grd = context.createLinearGradient(0, 0, 0, height);
			grd.addColorStop(0, fillStyle);
			grd.addColorStop(1, fillColor2);
			fillStyle = grd;
		}
		context.fillStyle = fillStyle;
		context.fill();
	}
	if (strokeStyle != null && lineWidth > 0) {
		context.strokeStyle = strokeStyle;
		context.lineWidth = lineWidth;
		context.stroke();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/canvas/utils/DrawRoundRectangleBackground.js
var DrawRoundRectangleBackground = function(canvasObject, color, strokeColor, strokeLineWidth, radius, color2, isHorizontalGradient, iteration) {
	if (color == null && strokeColor == null) return;
	var width = canvasObject.canvas.width, height = canvasObject.canvas.height;
	if (strokeColor == null) strokeLineWidth = 0;
	var x = strokeLineWidth / 2;
	width = Math.max(1, width - strokeLineWidth);
	height = Math.max(1, height - strokeLineWidth);
	DrawRoundRectangle(canvasObject.canvas, canvasObject.context, x, x, width, height, radius, color, strokeColor, strokeLineWidth, color2, isHorizontalGradient, iteration);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/canvas/roundrectangle/DrawContent.js
var DrawContent$2 = function() {
	DrawRoundRectangleBackground(this, this.fillStyle, this.strokeStyle, this.lineWidth, this.radius, this.fillColor2, this.isHorizontalGradient, this.iteration);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/canvas/roundrectangle/RoundRectangle.js
var GetValue$275 = __webpack_exports__Utils.Objects.GetValue;
var RoundRectangle$1 = class extends Canvas$1 {
	constructor(scene, x, y, width, height, radiusConfig, fillStyle, strokeStyle, lineWidth, fillColor2, isHorizontalGradient, resolution) {
		if (x === void 0) x = 0;
		if (y === void 0) y = 0;
		if (width === void 0) width = 1;
		if (height === void 0) height = width;
		if (radiusConfig === void 0) radiusConfig = 0;
		if (resolution === void 0) resolution = 1;
		super(scene, x, y, width, height, resolution);
		this.type = "rexRoundRectangleCanvas";
		var radius = GetValue$275(radiusConfig, "radius", radiusConfig);
		var iteration = GetValue$275(radiusConfig, "iteration", void 0);
		this.setRadius(radius);
		this.setIteration(iteration);
		this.setFillStyle(fillStyle, fillColor2, isHorizontalGradient);
		this.setStrokeStyle(strokeStyle, lineWidth);
	}
	get radius() {
		return this._radius;
	}
	set radius(value) {
		this.dirty |= this._radius != value;
		this._radius = value;
	}
	setRadius(radius) {
		this.radius = radius;
		return this;
	}
	get iteration() {
		return this._iteration;
	}
	set iteration(value) {
		this.dirty |= this._iteration != value;
		this._iteration = value;
	}
	setIteration(iteration) {
		this.iteration = iteration;
		return this;
	}
	get fillStyle() {
		return this._fillStyle;
	}
	set fillStyle(value) {
		value = GetStyle(value, this.canvas, this.context);
		this.dirty |= this._fillStyle != value;
		this._fillStyle = value;
	}
	get fillColor2() {
		return this._fillColor2;
	}
	set fillColor2(value) {
		value = GetStyle(value, this.canvas, this.context);
		this.dirty |= this._fillColor2 != value;
		this._fillColor2 = value;
	}
	get isHorizontalGradient() {
		return this._isHorizontalGradient;
	}
	set isHorizontalGradient(value) {
		this.dirty |= this._isHorizontalGradient != value;
		this._isHorizontalGradient = value;
	}
	setFillStyle(fillStyle, fillColor2, isHorizontalGradient) {
		if (isHorizontalGradient === void 0) isHorizontalGradient = true;
		this.fillStyle = fillStyle;
		this.fillColor2 = fillColor2;
		this.isHorizontalGradient = isHorizontalGradient;
		return this;
	}
	get strokeStyle() {
		return this._strokeStyle;
	}
	set strokeStyle(value) {
		value = GetStyle(value, this.canvas, this.context);
		this.dirty |= this._strokeStyle != value;
		this._strokeStyle = value;
	}
	get lineWidth() {
		return this._lineWidth;
	}
	set lineWidth(value) {
		this.dirty |= this._lineWidth != value;
		this._lineWidth = value;
	}
	setStrokeStyle(strokeStyle, lineWidth) {
		this.strokeStyle = strokeStyle;
		this.lineWidth = lineWidth;
		return this;
	}
	updateTexture() {
		super.updateTexture(function() {
			this.clear();
			DrawContent$2.call(this);
		}, this);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/roundrectanglecanvas/RoundRectangleCanvas.js
var RoundRectangleCanvas_default = RoundRectangle$1;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/roundrectanglecanvas/Factory.js
ObjectFactory.register("roundRectangleCanvas", function(x, y, width, height, radius, fillStyle, strokeStyle, lineWidth, fillColor2, isHorizontalGradient) {
	var gameObject = new RoundRectangleCanvas_default(this.scene, x, y, width, height, radius, fillStyle, strokeStyle, lineWidth, fillColor2, isHorizontalGradient);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.RoundRectangleCanvas", RoundRectangleCanvas_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/object/HasProperty.js
var HasProperty = function(obj, prop) {
	if (!obj) return false;
	if (obj.hasOwnProperty(prop)) return true;
	while (obj) {
		if (Object.getOwnPropertyDescriptor(obj, prop)) return true;
		obj = obj.__proto__;
	}
	return false;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/quad/methods/PointMethods.js
var InjectPointAccessProperties = function(gameObject, key, point) {
	if (!key || HasProperty(gameObject, `${key}X`)) return;
	Object.defineProperty(gameObject, `${key}X`, {
		get: function() {
			return point.x;
		},
		set: function(value) {
			point.x = value;
			gameObject.dirty = true;
		}
	});
	Object.defineProperty(gameObject, `${key}Y`, {
		get: function() {
			return point.y;
		},
		set: function(value) {
			point.y = value;
			gameObject.dirty = true;
		}
	});
	Object.defineProperty(gameObject, `${key}T`, {
		get: function() {
			return point.t;
		},
		set: function(value) {
			point.t = value;
			gameObject.dirty = true;
		}
	});
};
var PointMethods_default = {
	setTLPosition(x, y) {
		this.geom.setTLPosition(x, y);
		this.dirty = true;
		return this;
	},
	setTRPosition(x, y) {
		this.geom.setTRPosition(x, y);
		this.dirty = true;
		return this;
	},
	setBLPosition(x, y) {
		this.geom.setBLPosition(x, y);
		this.dirty = true;
		return this;
	},
	setBRPosition(x, y) {
		this.geom.setBRPosition(x, y);
		this.dirty = true;
		return this;
	},
	resetCornerPosition() {
		this.geom.resetCornerPosition();
		this.dirty = true;
		return this;
	},
	insertTopSidePoint(t, x, y, key) {
		var points = this.geom.topSidePoints;
		if (Array.isArray(t)) {
			var points = t, point;
			for (var i = 0, cnt = points.length; i < cnt; i++) {
				point = points[i];
				this.geom.insertTopSidePoint(point.t, point.x, point.y);
				InjectPointAccessProperties(this, point.key, points[points.length - 1]);
			}
		} else {
			this.geom.insertTopSidePoint(t, x, y);
			InjectPointAccessProperties(this, key, points[points.length - 1]);
		}
		this.dirty = true;
		return this;
	},
	insertRightSidePoint(t, x, y, key) {
		var points = this.geom.rightSidePoints;
		if (Array.isArray(t)) {
			var points = t, point;
			for (var i = 0, cnt = points.length; i < cnt; i++) {
				point = points[i];
				this.geom.insertRightSidePoint(point.t, point.x, point.y);
				InjectPointAccessProperties(this, point.key, points[points.length - 1]);
			}
		} else {
			this.geom.insertRightSidePoint(t, x, y);
			InjectPointAccessProperties(this, key, points[points.length - 1]);
		}
		this.dirty = true;
		return this;
	},
	insertBottomSidePoint(t, x, y, key) {
		var points = this.geom.bottomSidePoints;
		if (Array.isArray(t)) {
			var points = t, point;
			for (var i = 0, cnt = points.length; i < cnt; i++) {
				point = points[i];
				this.geom.insertBottomSidePoint(point.t, point.x, point.y);
				InjectPointAccessProperties(this, point.key, points[points.length - 1]);
			}
		} else {
			this.geom.insertBottomSidePoint(t, x, y);
			InjectPointAccessProperties(this, key, points[points.length - 1]);
		}
		this.dirty = true;
		return this;
	},
	insertLeftSidePoint(t, x, y, key) {
		var points = this.geom.leftSidePoints;
		if (Array.isArray(t)) {
			var points = t, point;
			for (var i = 0, cnt = points.length; i < cnt; i++) {
				point = points[i];
				this.geom.insertLeftSidePoint(point.t, point.x, point.y);
				InjectPointAccessProperties(this, point.key, points[points.length - 1]);
			}
		} else {
			this.geom.insertLeftSidePoint(t, x, y);
			InjectPointAccessProperties(this, key, points[points.length - 1]);
		}
		this.dirty = true;
		return this;
	},
	clearTopSidePoints() {
		this.geom.clearTopSidePoints();
		this.dirty = true;
		return this;
	},
	clearRightSidePoints() {
		this.geom.clearRightSidePoints();
		this.dirty = true;
		return this;
	},
	clearBottomSidePoints() {
		this.geom.clearBottomSidePoints();
		this.dirty = true;
		return this;
	},
	clearLeftSidePoints() {
		this.geom.clearLeftSidePoints();
		this.dirty = true;
		return this;
	},
	clearAllSidesPoints() {
		this.geom.clearAllSidesPoints();
		this.dirty = true;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/quad/methods/QuadGeom.js
var QuadGeom = class {
	constructor(x, y, width, height) {
		if (x === void 0) x = 0;
		if (y === void 0) y = x;
		if (width === void 0) width = 0;
		if (height === void 0) height = 0;
		this.setTo(x, y, width, height);
		this.tlx = 0;
		this.tly = 0;
		this.trx = 0;
		this.try = 0;
		this.blx = 0;
		this.bly = 0;
		this.brx = 0;
		this.bry = 0;
		this.topSidePoints = [];
		this.rightSidePoints = [];
		this.bottomSidePoints = [];
		this.leftSidePoints = [];
	}
	setTo(x, y, width, height) {
		this.setPosition(x, y);
		this.setSize(width, height);
		return this;
	}
	setPosition(x, y) {
		this.x = x;
		this.y = y;
		return this;
	}
	setSize(width, height) {
		this.width = width;
		this.height = height;
		return this;
	}
	setTLPosition(x, y) {
		this.tlx = x;
		this.tly = y;
		return this;
	}
	setTRPosition(x, y) {
		this.trx = x;
		this.try = y;
		return this;
	}
	setBLPosition(x, y) {
		this.blx = x;
		this.bly = y;
		return this;
	}
	setBRPosition(x, y) {
		this.brx = x;
		this.bry = y;
		return this;
	}
	resetCornerPosition() {
		this.setTLPosition(0, 0).setTRPosition(0, 0).setBLPosition(0, 0).setBRPosition(0, 0);
		return this;
	}
	insertTopSidePoint(t, x, y) {
		AddPoint(this.topSidePoints, t, x, y);
		return this;
	}
	insertRightSidePoint(t, x, y) {
		AddPoint(this.rightSidePoints, t, x, y);
		return this;
	}
	insertBottomSidePoint(t, x, y) {
		AddPoint(this.bottomSidePoints, t, x, y);
		return this;
	}
	insertLeftSidePoint(t, x, y) {
		AddPoint(this.leftSidePoints, t, x, y);
		return this;
	}
	clearTopSidePoints() {
		this.topSidePoints.length = 0;
		return this;
	}
	clearRightSidePoints() {
		this.rightSidePoints.length = 0;
		return this;
	}
	clearBottomSidePoints() {
		this.bottomSidePoints.length = 0;
		return this;
	}
	clearLeftSidePoints() {
		this.leftSidePoints.length = 0;
		return this;
	}
	clearAllSidesPoints() {
		this.clearTopSidePoints().clearRightSidePoints().clearBottomSidePoints().clearLeftSidePoints();
		return this;
	}
};
var AddPoint = function(points, t, x, y) {
	if (typeof t !== "number") {
		var config = t;
		t = config.t;
		x = config.x;
		y = config.y;
	}
	points.push({
		t,
		x,
		y
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/quad/Quad.js
var IsPlainObject$55 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$274 = __webpack_exports__Utils.Objects.GetValue;
var Linear$27 = __webpack_exports__Math.Linear;
var Earcut$2 = __webpack_exports__Geom.Polygon.Earcut;
var Quad = class extends PolygnBase {
	constructor(scene, x, y, width, height, fillColor, fillAlpha) {
		var strokeColor, strokeAlpha, strokeWidth;
		if (IsPlainObject$55(x)) {
			var config = x;
			x = config.x;
			y = config.y;
			width = config.width;
			height = config.height;
			fillColor = config.color;
			fillAlpha = config.alpha;
			strokeColor = config.strokeColor;
			strokeAlpha = config.strokeAlpha;
			strokeWidth = config.strokeWidth;
		}
		if (x === void 0) x = 0;
		if (y === void 0) y = 0;
		if (width === void 0) width = 1;
		if (height === void 0) height = width;
		var geom = new QuadGeom();
		super(scene, "rexQuadShape", geom);
		this.init();
		geom.setTo(0, 0, width, height);
		this.setPosition(x, y);
		this.setFillStyle(fillColor, fillAlpha);
		if (strokeColor !== void 0 && strokeWidth === void 0) strokeWidth = 2;
		this.setStrokeStyle(strokeWidth, strokeColor, strokeAlpha);
		this.setTLPosition(GetValue$274(config, "tlx", 0), GetValue$274(config, "tly", 0)).setTRPosition(GetValue$274(config, "trx", 0), GetValue$274(config, "try", 0)).setBLPosition(GetValue$274(config, "blx", 0), GetValue$274(config, "bly", 0)).setBRPosition(GetValue$274(config, "brx", 0), GetValue$274(config, "bry", 0));
		var leftSidePoints = GetValue$274(config, "leftSidePoints");
		if (leftSidePoints) this.insertLeftSidePoint(leftSidePoints);
		var topSidePoints = GetValue$274(config, "topSidePoints");
		if (topSidePoints) this.insertTopSidePoint(topSidePoints);
		var rightSidePoints = GetValue$274(config, "rightSidePoints");
		if (rightSidePoints) this.insertRightSidePoint(rightSidePoints);
		var bottomSidePoints = GetValue$274(config, "bottomSidePoints");
		if (bottomSidePoints) this.insertBottomSidePoint(bottomSidePoints);
		this.updateDisplayOrigin();
		this.dirty = true;
	}
	updateData() {
		var geom = this.geom;
		var pathData = this.pathData;
		pathData.length = 0;
		var width = geom.width;
		var height = geom.height;
		var tlx = 0 + geom.tlx;
		var tly = 0 + geom.tly;
		var trx = width + geom.trx;
		var try_ = 0 + geom.try;
		var brx = width + geom.brx;
		var bry = height + geom.bry;
		var blx = 0 + geom.blx;
		var bly = height + geom.bly;
		var topSidePoints = geom.topSidePoints;
		var rightSidePoints = geom.rightSidePoints;
		var bottomSidePoints = geom.bottomSidePoints;
		var leftSidePoints = geom.leftSidePoints;
		LineTo(tlx, tly, pathData);
		SortPoints(topSidePoints);
		for (var i = 0, cnt = topSidePoints.length; i < cnt; i++) {
			var point = topSidePoints[i];
			var px = Linear$27(tlx, trx, point.t) + point.x;
			var py = Linear$27(tly, try_, point.t) + point.y;
			LineTo(px, py, pathData);
		}
		LineTo(trx, try_, pathData);
		SortPoints(rightSidePoints);
		for (var i = 0, cnt = rightSidePoints.length; i < cnt; i++) {
			var point = rightSidePoints[i];
			var px = Linear$27(trx, brx, point.t) + point.x;
			var py = Linear$27(try_, bry, point.t) + point.y;
			LineTo(px, py, pathData);
		}
		LineTo(brx, bry, pathData);
		SortPoints(bottomSidePoints);
		for (var i = bottomSidePoints.length - 1; i >= 0; i--) {
			var point = bottomSidePoints[i];
			var px = Linear$27(blx, brx, point.t) + point.x;
			var py = Linear$27(bly, bry, point.t) + point.y;
			LineTo(px, py, pathData);
		}
		LineTo(blx, bly, pathData);
		SortPoints(leftSidePoints);
		for (var i = leftSidePoints.length - 1; i >= 0; i--) {
			var point = leftSidePoints[i];
			var px = Linear$27(tlx, blx, point.t) + point.x;
			var py = Linear$27(tly, bly, point.t) + point.y;
			LineTo(px, py, pathData);
		}
		pathData.push(pathData[0], pathData[1]);
		this.pathIndexes = Earcut$2(pathData);
		super.updateData();
		return this;
	}
	get tlx() {
		return this.geom.tlx;
	}
	set tlx(value) {
		this.geom.tlx = value;
		this.dirty = true;
	}
	get tly() {
		return this.geom.tly;
	}
	set tly(value) {
		this.geom.tly = value;
		this.dirty = true;
	}
	get trx() {
		return this.geom.trx;
	}
	set trx(value) {
		this.geom.trx = value;
		this.dirty = true;
	}
	get try() {
		return this.geom.try;
	}
	set try(value) {
		this.geom.try = value;
		this.dirty = true;
	}
	get blx() {
		return this.geom.blx;
	}
	set blx(value) {
		this.geom.blx = value;
		this.dirty = true;
	}
	get bly() {
		return this.geom.bly;
	}
	set bly(value) {
		this.geom.bly = value;
		this.dirty = true;
	}
	get brx() {
		return this.geom.brx;
	}
	set brx(value) {
		this.geom.brx = value;
		this.dirty = true;
	}
	get bry() {
		return this.geom.bry;
	}
	set bry(value) {
		this.geom.bry = value;
		this.dirty = true;
	}
	get leftSidePoints() {
		return this.geom.leftSidePoints;
	}
	get topSidePoints() {
		return this.geom.topSidePoints;
	}
	get bottomSidePoints() {
		return this.geom.bottomSidePoints;
	}
	get rightSidePoints() {
		return this.geom.rightSidePoints;
	}
};
var SortPoints = function(points) {
	if (points.length <= 1) return;
	points.sort(function(pointA, pointB) {
		return pointA.t - pointB.t;
	});
};
Object.assign(Quad.prototype, PointMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/quadshape/QuadShape.js
var QuadShape_default = Quad;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/quadshape/Factory.js
ObjectFactory.register("quadShape", function(x, y, width, height, fillColor, fillAlpha) {
	var gameObject = new QuadShape_default(this.scene, x, y, width, height, fillColor, fillAlpha);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.QuadShape", QuadShape_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/textbase/render/WebGLRenderer.js
var WebGLRenderer$3 = function(renderer, src, drawingContext, parentMatrix) {
	if (src.width === 0 || src.height === 0) return;
	drawingContext.camera.addToRenderList(src);
	var customRenderNodes = src.customRenderNodes;
	var defaultRenderNodes = src.defaultRenderNodes;
	(customRenderNodes.Submitter || defaultRenderNodes.Submitter).run(drawingContext, src, parentMatrix, 0, customRenderNodes.Texturer || defaultRenderNodes.Texturer, customRenderNodes.Transformer || defaultRenderNodes.Transformer);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/textbase/render/CanvasRenderer.js
var CanvasRenderer$2 = function(renderer, src, camera, parentMatrix) {
	if (src.width === 0 || src.height === 0) return;
	camera.addToRenderList(src);
	renderer.batchSprite(src, src.frame, camera, parentMatrix);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/textbase/render/Render.js
var Render_default$2 = {
	renderWebGL: WebGLRenderer$3,
	renderCanvas: CanvasRenderer$2
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/textbase/textstyle/MeasureTextMargins.js
var CanvasPool$3 = __webpack_exports__Display.Canvas.CanvasPool;
var MeasureTextMargins = function(textStyle, testString, out) {
	if (out === void 0) out = {};
	var canvas = CanvasPool$3.create(this);
	var context = canvas.getContext("2d", { willReadFrequently: true });
	textStyle.syncFont(canvas, context);
	var metrics = context.measureText(testString);
	var width = Math.ceil(metrics.width * textStyle.baselineX);
	var baseline = width;
	var height = 2 * baseline;
	baseline = baseline * textStyle.baselineY | 0;
	canvas.width = width;
	canvas.height = height;
	context.fillStyle = "#f00";
	context.fillRect(0, 0, width, height);
	context.font = textStyle._font;
	context.textBaseline = "alphabetic";
	context.fillStyle = "#000";
	context.fillText(textStyle.testString, 0, baseline);
	out.left = 0;
	if (width === 0 || height === 0 || !context.getImageData(0, 0, width, height)) {
		CanvasPool$3.remove(canvas);
		return out;
	}
	var imagedata = context.getImageData(0, 0, width, height).data;
	var stop = false;
	for (var x = 0; x < width; x++) {
		for (var y = 0; y < height; y++) if (imagedata[(y * width + x) * 4] !== 255) {
			out.left = x;
			stop = true;
			break;
		}
		if (stop) break;
	}
	CanvasPool$3.remove(canvas);
	return out;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/textbase/TextBase.js
CheckPhaserVersion();
var GameObject$2 = __webpack_exports__GameObjects.GameObject;
var TextBase = class extends GameObject$2 {
	setStyle(style) {
		return this.style.setStyle(style);
	}
	setFont(font) {
		return this.style.setFont(font);
	}
	setFontFamily(family) {
		return this.style.setFontFamily(family);
	}
	setFontSize(size) {
		return this.style.setFontSize(size);
	}
	setFontStyle(style) {
		return this.style.setFontStyle(style);
	}
	setTestString(string) {
		return this.style.setTestString(string);
	}
	setFixedSize(width, height) {
		return this.style.setFixedSize(width, height);
	}
	setBackgroundColor(color, color2, isHorizontalGradient) {
		return this.style.setBackgroundColor(color, color2, isHorizontalGradient);
	}
	setBackgroundStrokeColor(color, lineWidth) {
		return this.style.setBackgroundStrokeColor(color, lineWidth);
	}
	setBackgroundCornerRadius(radius, iteration) {
		return this.style.setBackgroundCornerRadius(radius, iteration);
	}
	setFill(color) {
		return this.style.setFill(color);
	}
	setColor(color) {
		return this.style.setColor(color);
	}
	setStroke(color, thickness) {
		return this.style.setStroke(color, thickness);
	}
	setShadow(x, y, color, blur, shadowStroke, shadowFill) {
		return this.style.setShadow(x, y, color, blur, shadowStroke, shadowFill);
	}
	setShadowOffset(x, y) {
		return this.style.setShadowOffset(x, y);
	}
	setShadowColor(color) {
		return this.style.setShadowColor(color);
	}
	setShadowBlur(blur) {
		return this.style.setShadowBlur(blur);
	}
	setShadowStroke(enabled) {
		return this.style.setShadowStroke(enabled);
	}
	setShadowFill(enabled) {
		return this.style.setShadowFill(enabled);
	}
	setUnderline(color, thickness, offset) {
		return this.style.setUnderline(color, thickness, offset);
	}
	setUnderlineColor(color) {
		return this.style.setUnderlineColor(color);
	}
	setUnderlineThickness(thickness) {
		return this.style.setUnderlineThickness(thickness);
	}
	setUnderlineOffset(offset) {
		return this.style.setUnderlineOffset(offset);
	}
	setStrikethrough(color, thickness, offset) {
		return this.style.setStrikethrough(color, thickness, offset);
	}
	setStrikethroughColor(color) {
		return this.style.setStrikethroughColor(color);
	}
	setStrikethroughThickness(thickness) {
		return this.style.setStrikethroughThickness(thickness);
	}
	setStrikethroughOffset(offset) {
		return this.style.setStrikethroughOffset(offset);
	}
	setWrapMode(mode) {
		return this.style.setWrapMode(mode);
	}
	setWrapWidth(width) {
		return this.style.setWrapWidth(width);
	}
	setWordWrapWidth(width) {
		return this.style.setWrapWidth(width);
	}
	setAlign(align) {
		return this.style.setHAlign(align);
	}
	setHAlign(align) {
		return this.style.setHAlign(align);
	}
	setVAlign(align) {
		return this.style.setVAlign(align);
	}
	get lineSpacing() {
		return this.style.lineSpacing;
	}
	set lineSpacing(value) {
		this.style.lineSpacing = value;
	}
	setLineSpacing(value) {
		this.style.lineSpacing = value;
		this.updateText(true);
		return this;
	}
	get letterSpacing() {
		return this.style.letterSpacing;
	}
	set letterSpacing(value) {
		this.style.letterSpacing = value;
	}
	setLetterSpacing(value) {
		this.style.letterSpacing = value;
		this.updateText(true);
		return this;
	}
	setXOffset(value) {
		return this.style.setXOffset(value);
	}
	setMaxLines(max) {
		return this.style.setMaxLines(max);
	}
	setResolution(value) {
		return this.style.setResolution(value);
	}
	getTextMetrics() {
		return this.style.getTextMetrics();
	}
	setTextMetrics(metrics, font) {
		return this.style.setTextMetrics(metrics, font);
	}
	measureTextMargins(testString, out) {
		return MeasureTextMargins(this.style, testString, out);
	}
};
var Components$2 = __webpack_exports__GameObjects.Components;
__webpack_exports__Class.mixin(TextBase, [
	Components$2.Alpha,
	Components$2.BlendMode,
	Components$2.ComputedSize,
	Components$2.Crop,
	Components$2.Depth,
	Components$2.Flip,
	Components$2.GetBounds,
	Components$2.Lighting,
	Components$2.Mask,
	Components$2.Origin,
	Components$2.RenderNodes,
	Components$2.ScrollFactor,
	Components$2.Tint,
	Components$2.Transform,
	Components$2.Visible,
	Render_default$2
]);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/textbase/textstyle/PropertyMap.js
var PropertyMap = {
	backgroundColor: [
		"backgroundColor",
		null,
		GetStyle
	],
	backgroundColor2: [
		"backgroundColor2",
		null,
		GetStyle
	],
	backgroundHorizontalGradient: [
		"backgroundHorizontalGradient",
		true,
		null
	],
	backgroundStrokeColor: [
		"backgroundStrokeColor",
		null,
		GetStyle
	],
	backgroundStrokeLineWidth: [
		"backgroundStrokeLineWidth",
		2,
		null
	],
	backgroundCornerRadius: [
		"backgroundCornerRadius",
		0,
		null
	],
	backgroundCornerIteration: [
		"backgroundCornerIteration",
		null,
		null
	],
	fontFamily: [
		"fontFamily",
		"Courier",
		null
	],
	fontSize: [
		"fontSize",
		"16px",
		null
	],
	fontStyle: [
		"fontStyle",
		"",
		null
	],
	color: [
		"color",
		"#fff",
		GetStyle
	],
	stroke: [
		"stroke",
		"#fff",
		GetStyle
	],
	strokeThickness: [
		"strokeThickness",
		0,
		null
	],
	shadowOffsetX: [
		"shadow.offsetX",
		0,
		null
	],
	shadowOffsetY: [
		"shadow.offsetY",
		0,
		null
	],
	shadowColor: [
		"shadow.color",
		"#000",
		GetStyle
	],
	shadowBlur: [
		"shadow.blur",
		0,
		null
	],
	shadowStroke: [
		"shadow.stroke",
		false,
		null
	],
	shadowFill: [
		"shadow.fill",
		false,
		null
	],
	underlineColor: [
		"underline.color",
		"#000",
		GetStyle
	],
	underlineThickness: [
		"underline.thickness",
		0,
		null
	],
	underlineOffset: [
		"underline.offset",
		0,
		null
	],
	strikethroughColor: [
		"strikethrough.color",
		"#000",
		GetStyle
	],
	strikethroughThickness: [
		"strikethrough.thickness",
		0,
		null
	],
	strikethroughOffset: [
		"strikethrough.offset",
		0,
		null
	],
	halign: [
		"halign",
		"left",
		null
	],
	valign: [
		"valign",
		"top",
		null
	],
	maxLines: [
		"maxLines",
		0,
		null
	],
	fixedWidth: [
		"fixedWidth",
		0,
		null
	],
	fixedHeight: [
		"fixedHeight",
		0,
		null
	],
	resolution: [
		"resolution",
		0,
		null
	],
	lineSpacing: [
		"lineSpacing",
		0,
		null
	],
	letterSpacing: [
		"letterSpacing",
		0,
		null
	],
	xOffset: [
		"xOffset",
		0,
		null
	],
	fixedLineHeightMode: [
		"fixedLineHeightMode",
		true,
		null
	],
	rtl: [
		"rtl",
		false,
		null
	],
	testString: [
		"testString",
		"|MÃ‰qgy",
		null
	],
	baselineX: [
		"baselineX",
		1.2,
		null
	],
	baselineY: [
		"baselineY",
		1.4,
		null
	],
	wrapMode: [
		"wrap.mode",
		0,
		null
	],
	wrapWidth: [
		"wrap.width",
		0,
		null
	],
	wrapCallback: ["wrap.callback", null],
	wrapCallbackScope: ["wrap.callbackScope", null]
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/textbase/textstyle/MeasureText.js
/**
* @author       Richard Davey <rich@photonstorm.com>
* @copyright    2018 Photon Storm Ltd.
* @license      {@link https://github.com/photonstorm/phaser/blob/master/license.txt|MIT License}
*/
var CanvasPool$2 = __webpack_exports__Display.Canvas.CanvasPool;
/**
* Calculates the ascent, descent and fontSize of a given font style.
*
* @function Phaser.GameObjects.MeasureText
* @since 3.0.0
*
* @param {Phaser.GameObjects.Text.TextStyle} textStyle - The TextStyle object to measure.
*
* @return {object} An object containing the ascent, descent and fontSize of the TextStyle.
*/
var MeasureText = function(textStyle) {
	var canvas = CanvasPool$2.create(this);
	var context = canvas.getContext("2d", { willReadFrequently: true });
	textStyle.syncFont(canvas, context);
	var metrics = context.measureText(textStyle.testString);
	if ("actualBoundingBoxAscent" in metrics) {
		var ascent = metrics.actualBoundingBoxAscent;
		var descent = metrics.actualBoundingBoxDescent;
		var output = {
			ascent,
			descent,
			fontSize: ascent + descent
		};
		CanvasPool$2.remove(canvas);
		return output;
	}
	var width = Math.ceil(metrics.width * textStyle.baselineX);
	var baseline = width;
	var height = 2 * baseline;
	baseline = baseline * textStyle.baselineY | 0;
	canvas.width = width;
	canvas.height = height;
	context.fillStyle = "#f00";
	context.fillRect(0, 0, width, height);
	context.font = textStyle._font;
	context.textBaseline = "alphabetic";
	context.fillStyle = "#000";
	context.fillText(textStyle.testString, 0, baseline);
	var output = {
		ascent: 0,
		descent: 0,
		fontSize: 0
	};
	if (!context.getImageData(0, 0, width, height)) {
		output.ascent = baseline;
		output.descent = baseline + 6;
		output.fontSize = output.ascent + output.descent;
		CanvasPool$2.remove(canvas);
		return output;
	}
	var imagedata = context.getImageData(0, 0, width, height).data;
	var pixels = imagedata.length;
	var line = width * 4;
	var i;
	var j;
	var idx = 0;
	var stop = false;
	for (i = 0; i < baseline; i++) {
		for (j = 0; j < line; j += 4) if (imagedata[idx + j] !== 255) {
			stop = true;
			break;
		}
		if (!stop) idx += line;
		else break;
	}
	output.ascent = baseline - i;
	idx = pixels - line;
	stop = false;
	for (i = height; i > baseline; i--) {
		for (j = 0; j < line; j += 4) if (imagedata[idx + j] !== 255) {
			stop = true;
			break;
		}
		if (!stop) idx -= line;
		else break;
	}
	output.descent = i - baseline;
	output.fontSize = output.ascent + output.descent;
	CanvasPool$2.remove(canvas);
	return output;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/textbase/const.js
var CONST = {
	NO_NEWLINE: 0,
	RAW_NEWLINE: 1,
	WRAPPED_NEWLINE: 2,
	NO_WRAP: 0,
	WORD_WRAP: 1,
	CHAR_WRAP: 2,
	MIX_WRAP: 3,
	SPLITREGEXP: /(?:\r\n|\r|\n)/
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/textbase/textstyle/WrapModes.js
var WRAPMODE$1 = {
	none: CONST.NO_WRAP,
	word: CONST.WORD_WRAP,
	char: CONST.CHAR_WRAP,
	character: CONST.CHAR_WRAP,
	mix: CONST.MIX_WRAP
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/textbase/textstyle/TextStyle.js
var GetAdvancedValue$6 = __webpack_exports__Utils.Objects.GetAdvancedValue;
var GetValue$273 = __webpack_exports__Utils.Objects.GetValue;
var TextStyle$1 = class {
	constructor(text, style, propertyMap) {
		this.parent = text;
		if (propertyMap === void 0) propertyMap = PropertyMap;
		this.propertyMap = propertyMap;
		this.backgroundColor;
		this.backgroundColor2;
		this.backgroundHorizontalGradient;
		this.backgroundStrokeColor;
		this.backgroundStrokeLineWidth;
		this.backgroundCornerRadius;
		this.backgroundCornerIteration;
		this.fontFamily;
		this.fontSize;
		this.fontStyle;
		this.color;
		this.stroke;
		this.strokeThickness;
		this.shadowOffsetX;
		this.shadowOffsetY;
		this.shadowColor;
		this.shadowBlur;
		this.shadowStroke;
		this.shadowFill;
		this.underlineColor;
		this.underlineThickness;
		this.underlineOffset;
		this.strikethroughColor;
		this.strikethroughThickness;
		this.strikethroughOffset;
		this.halign;
		this.valign;
		this.maxLines;
		this.fixedWidth;
		this.fixedHeight;
		this.resolution;
		this.xOffset;
		this.rtl;
		this.testString;
		this.baselineX;
		this.baselineY;
		this.wrapMode;
		this.wrapWidth;
		this.wrapCallback;
		this.wrapCallbackScope;
		this._font;
		this.setStyle(style, false, true);
	}
	get isWrapFitMode() {
		return this.fixedWidth > 0 && this.wrapMode !== CONST.NO_WRAP && this.wrapWidth === 0;
	}
	setStyle(style, updateText, setDefaults) {
		if (updateText === void 0) updateText = true;
		if (setDefaults === void 0) setDefaults = false;
		if (style && style.hasOwnProperty("wordWrap")) {
			var wordWrap = style.wordWrap;
			if (wordWrap.hasOwnProperty("width")) style.wrap = {
				mode: "word",
				width: wordWrap.width
			};
		}
		if (style && style.hasOwnProperty("wrap")) {
			var wrap = style.wrap;
			if (wrap.hasOwnProperty("mode")) {
				var mode = wrap.mode;
				if (typeof mode === "string") wrap.mode = WRAPMODE$1[mode];
			} else if (wrap.hasOwnProperty("width")) wrap.mode = 1;
		}
		if (style && style.rtl && setDefaults && !style.hasOwnProperty("halign")) style.halign = "right";
		if (style && style.hasOwnProperty("fontSize") && typeof style.fontSize === "number") style.fontSize = style.fontSize.toString() + "px";
		var propertyMap = this.propertyMap;
		for (var key in propertyMap) {
			var prop = propertyMap[key];
			var objKey = prop[0];
			var defaultValue = setDefaults ? prop[1] : this[key];
			var postCallback = prop[2];
			if (key === "wrapCallback" || key === "wrapCallbackScope") this[key] = GetValue$273(style, objKey, defaultValue);
			else {
				var value = GetAdvancedValue$6(style, objKey, defaultValue);
				if (postCallback) value = postCallback(value);
				this[key] = value;
			}
		}
		var font = GetValue$273(style, "font", null);
		if (font === null) this._font = this.fontStyle + " " + this.fontSize + " " + this.fontFamily;
		else this._font = font;
		var fill = GetValue$273(style, "fill", null);
		if (fill !== null) this.color = GetStyle(fill);
		var metrics = GetValue$273(style, "metrics", false);
		if (metrics) this.metrics = {
			ascent: GetValue$273(metrics, "ascent", 0),
			descent: GetValue$273(metrics, "descent", 0),
			fontSize: GetValue$273(metrics, "fontSize", 0)
		};
		else if (updateText || !this.metrics) this.metrics = MeasureText(this);
		if (updateText) return this.parent.updateText();
		else return this.parent;
	}
	syncFont(canvas, context) {
		context.font = this._font;
	}
	syncStyle(canvas, context) {
		context.textBaseline = "alphabetic";
		context.fillStyle = this.color;
		context.strokeStyle = this.stroke;
		context.lineWidth = this.strokeThickness;
		context.lineCap = "round";
		context.lineJoin = "round";
	}
	syncShadow(context, enabled) {
		if (enabled) {
			context.shadowOffsetX = this.shadowOffsetX;
			context.shadowOffsetY = this.shadowOffsetY;
			context.shadowColor = this.shadowColor;
			context.shadowBlur = this.shadowBlur;
		} else {
			context.shadowOffsetX = 0;
			context.shadowOffsetY = 0;
			context.shadowColor = 0;
			context.shadowBlur = 0;
		}
	}
	update(recalculateMetrics) {
		if (recalculateMetrics) {
			this._font = `${this.fontStyle} ${this.fontSize} ${this.fontFamily}`.trim();
			this.metrics = MeasureText(this);
		}
		return this.parent.updateText(recalculateMetrics);
	}
	buildFont() {
		var newFont = `${this.fontStyle} ${this.fontSize} ${this.fontFamily}`.trim();
		if (newFont !== this._font) this._font = newFont;
		return this;
	}
	setFont(font) {
		if (typeof font === "string") {
			this.fontFamily = font;
			this.fontSize = "";
			this.fontStyle = "";
		} else {
			this.fontFamily = GetValue$273(font, "fontFamily", "Courier");
			this.fontSize = GetValue$273(font, "fontSize", "16px");
			this.fontStyle = GetValue$273(font, "fontStyle", "");
		}
		return this.update(true);
	}
	setFontFamily(family) {
		this.fontFamily = family;
		return this.update(true);
	}
	setFontStyle(style) {
		this.fontStyle = style;
		return this.update(true);
	}
	setFontSize(size) {
		if (typeof size === "number") size = size.toString() + "px";
		this.fontSize = size;
		return this.update(true);
	}
	setTestString(string) {
		this.testString = string;
		return this.update(true);
	}
	setFixedSize(width, height) {
		this.fixedWidth = width;
		this.fixedHeight = height;
		if (width) this.parent.width = width;
		if (height) this.parent.height = height;
		return this.update(this.isWrapFitMode);
	}
	setResolution(value) {
		this.resolution = value;
		return this.update(false);
	}
	setXOffset(value) {
		this.xOffset = value;
		return this.update(false);
	}
	setBackgroundColor(color, color2, isHorizontalGradient) {
		if (isHorizontalGradient === void 0) isHorizontalGradient = true;
		this.backgroundColor = GetStyle(color, this.parent.canvas, this.parent.context);
		this.backgroundColor2 = GetStyle(color2, this.parent.canvas, this.parent.context);
		this.backgroundHorizontalGradient = isHorizontalGradient;
		return this.update(false);
	}
	setBackgroundStrokeColor(color, lineWidth) {
		this.backgroundStrokeColor = GetStyle(color, this.parent.canvas, this.parent.context);
		this.backgroundStrokeLineWidth = lineWidth;
		return this.update(false);
	}
	setBackgroundCornerRadius(radius, iteration) {
		this.backgroundCornerRadius = radius;
		this.backgroundCornerIteration = iteration;
		return this.update(false);
	}
	setFill(color) {
		this.color = GetStyle(color, this.parent.canvas, this.parent.context);
		return this.update(false);
	}
	setColor(color) {
		this.color = GetStyle(color, this.parent.canvas, this.parent.context);
		return this.update(false);
	}
	setStroke(color, thickness) {
		if (color === void 0) this.strokeThickness = 0;
		else {
			if (thickness === void 0) thickness = this.strokeThickness;
			this.stroke = GetStyle(color, this.parent.canvas, this.parent.context);
			this.strokeThickness = thickness;
		}
		return this.update(true);
	}
	setShadow(x, y, color, blur, shadowStroke, shadowFill) {
		if (x === void 0) x = 0;
		if (y === void 0) y = 0;
		if (color === void 0) color = "#000";
		if (blur === void 0) blur = 0;
		if (shadowStroke === void 0) shadowStroke = false;
		if (shadowFill === void 0) shadowFill = true;
		this.shadowOffsetX = x;
		this.shadowOffsetY = y;
		this.shadowColor = GetStyle(color, this.parent.canvas, this.parent.context);
		this.shadowBlur = blur;
		this.shadowStroke = shadowStroke;
		this.shadowFill = shadowFill;
		return this.update(false);
	}
	setShadowOffset(x, y) {
		if (x === void 0) x = 0;
		if (y === void 0) y = x;
		this.shadowOffsetX = x;
		this.shadowOffsetY = y;
		return this.update(false);
	}
	setShadowColor(color) {
		if (color === void 0) color = "#000";
		this.shadowColor = GetStyle(color, this.parent.canvas, this.parent.context);
		return this.update(false);
	}
	setShadowBlur(blur) {
		if (blur === void 0) blur = 0;
		this.shadowBlur = blur;
		return this.update(false);
	}
	setShadowStroke(enabled) {
		this.shadowStroke = enabled;
		return this.update(false);
	}
	setShadowFill(enabled) {
		this.shadowFill = enabled;
		return this.update(false);
	}
	setUnderline(color, thickness, offset) {
		if (color === void 0) color = "#000";
		if (thickness === void 0) thickness = 0;
		if (offset === void 0) offset = 0;
		this.underlineColor = GetStyle(color, this.parent.canvas, this.parent.context);
		this.underlineThickness = thickness;
		this.underlineOffset = offset;
		return this.update(false);
	}
	setUnderlineColor(color) {
		if (color === void 0) color = "#000";
		this.underlineColor = GetStyle(color, this.parent.canvas, this.parent.context);
		return this.update(false);
	}
	setUnderlineThickness(thickness) {
		if (thickness === void 0) thickness = 0;
		this.underlineThickness = thickness;
		return this.update(false);
	}
	setUnderlineOffset(offset) {
		if (offset === void 0) offset = 0;
		this.underlineOffset = offset;
		return this.update(false);
	}
	setStrikethrough(color, thickness, offset) {
		if (color === void 0) color = "#000";
		if (thickness === void 0) thickness = 0;
		if (offset === void 0) offset = 0;
		this.strikethroughColor = GetStyle(color, this.parent.canvas, this.parent.context);
		this.strikethroughThickness = thickness;
		this.strikethroughOffset = offset;
		return this.update(false);
	}
	setStrikethroughColor(color) {
		if (color === void 0) color = "#000";
		this.strikethroughColor = GetStyle(color, this.parent.canvas, this.parent.context);
		return this.update(false);
	}
	setStrikethroughThickness(thickness) {
		if (thickness === void 0) thickness = 0;
		this.strikethroughThickness = thickness;
		return this.update(false);
	}
	setStrikethroughOffset(offset) {
		if (offset === void 0) offset = 0;
		this.strikethroughOffset = offset;
		return this.update(false);
	}
	setWrapMode(mode) {
		if (typeof mode === "string") mode = WRAPMODE$1[mode.toLowerCase()] || 0;
		this.wrapMode = mode;
		return this.update(true);
	}
	setWrapWidth(width) {
		this.wrapWidth = width;
		return this.update(false);
	}
	setAlign(halign, valign) {
		if (halign === void 0) halign = "left";
		if (valign === void 0) valign = "top";
		this.halign = halign;
		this.valign = valign;
		return this.update(false);
	}
	setHAlign(halign) {
		if (halign === void 0) halign = "left";
		this.halign = halign;
		return this.update(false);
	}
	setVAlign(valign) {
		if (valign === void 0) valign = "top";
		this.valign = valign;
		return this.update(false);
	}
	setMaxLines(max) {
		if (max === void 0) max = 0;
		this.maxLines = max;
		return this.update(false);
	}
	getTextMetrics() {
		var metrics = this.metrics;
		return {
			ascent: metrics.ascent,
			descent: metrics.descent,
			fontSize: metrics.fontSize
		};
	}
	setTextMetrics(metrics, font) {
		this.metrics.ascent = metrics.ascent;
		this.metrics.descent = metrics.descent;
		this.metrics.fontSize = metrics.fontSize;
		if (font) {
			if (typeof font === "string") {
				this.fontFamily = font;
				this.fontSize = "";
				this.fontStyle = "";
			} else {
				this.fontFamily = GetValue$273(font, "fontFamily", this.fontFamily);
				this.fontSize = GetValue$273(font, "fontSize", this.fontSize);
				this.fontStyle = GetValue$273(font, "fontStyle", this.fontStyle);
			}
		}
		return this.parent.updateText(true);
	}
	get lineHeight() {
		return this.metrics.fontSize + this.parent.lineSpacing;
	}
	toJSON() {
		var output = {};
		for (var key in this.propertyMap) output[key] = this[key];
		output.metrics = this.getTextMetrics();
		return output;
	}
	destroy() {
		this.parent = void 0;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/text/GetString.js
var GetString = function(value) {
	if (value == null) value = "";
	else if (Array.isArray(value)) value = value.join("\n");
	else if (typeof value === "number") value = value.toString();
	return value;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/textbase/canvastext/DrawMethods.js
var GetStartLineIndex$1 = function(lines, targetOffset) {
	var left = 0;
	var right = lines.length - 1;
	var result = lines.length;
	while (left <= right) {
		var mid = left + right >> 1;
		if (lines[mid].endOffset > targetOffset) {
			result = mid;
			right = mid - 1;
		} else left = mid + 1;
	}
	return result;
};
var GetEndLineIndex = function(lines, targetOffset) {
	var left = 0;
	var right = lines.length - 1;
	var result = lines.length;
	while (left <= right) {
		var mid = left + right >> 1;
		if (lines[mid].startOffset >= targetOffset) {
			result = mid;
			right = mid - 1;
		} else left = mid + 1;
	}
	return result;
};
var DrawMethods_default = {
	draw(startX, startY, textWidth, textHeight) {
		var penManager = this.penManager;
		this.hitAreaManager.clear();
		var context = this.context;
		context.save();
		var defaultStyle = this.defaultStyle;
		this.clear();
		DrawRoundRectangleBackground(this, defaultStyle.backgroundColor, defaultStyle.backgroundStrokeColor, defaultStyle.backgroundStrokeLineWidth, defaultStyle.backgroundCornerRadius, defaultStyle.backgroundColor2, defaultStyle.backgroundHorizontalGradient, defaultStyle.backgroundCornerIteration);
		var parent = this.parent;
		var padding = parent.padding;
		var viewportWidth = parent.width - padding.left - padding.right;
		var viewportHeight = parent.height - padding.top - padding.bottom;
		var contentHeight = this.linesHeight;
		var hasViewport = viewportWidth > 0 && viewportHeight > 0;
		var hasFixedHeight = parent.style.fixedHeight > 0;
		var hasOverflow = contentHeight > viewportHeight;
		var maxLines = defaultStyle.maxLines;
		var useVisibleRange = maxLines <= 0 && defaultStyle.valign === "top" && hasViewport && hasFixedHeight && hasOverflow;
		var clipText = useVisibleRange;
		if (clipText) {
			context.save();
			context.beginPath();
			context.rect(padding.left, padding.top, viewportWidth, viewportHeight);
			context.clip();
		}
		startX += this.startXOffset;
		startY += this.startYOffset;
		var defaultHalign = defaultStyle.halign, valign = defaultStyle.valign;
		var lineWidth;
		var lines = penManager.lines;
		var totalLinesNum = lines.length;
		var drawLinesNum, drawLineStartIdx, drawLineEndIdx;
		if (useVisibleRange && totalLinesNum > 0) {
			var visibleStart = -parent.scrollY;
			var visibleEnd = visibleStart + viewportHeight;
			drawLineStartIdx = GetStartLineIndex$1(lines, visibleStart);
			drawLineEndIdx = GetEndLineIndex(lines, visibleEnd);
			if (drawLineStartIdx < 0) drawLineStartIdx = 0;
			if (drawLineEndIdx > totalLinesNum) drawLineEndIdx = totalLinesNum;
			if (drawLineEndIdx < drawLineStartIdx) drawLineEndIdx = drawLineStartIdx;
		} else if (maxLines > 0 && totalLinesNum > maxLines) {
			drawLinesNum = maxLines;
			if (valign === "center") drawLineStartIdx = Math.floor((totalLinesNum - drawLinesNum) / 2);
			else if (valign === "bottom") drawLineStartIdx = totalLinesNum - drawLinesNum;
			else drawLineStartIdx = 0;
			drawLineEndIdx = drawLineStartIdx + drawLinesNum;
		} else {
			drawLineStartIdx = 0;
			drawLineEndIdx = totalLinesNum;
		}
		var offsetX, offsetY;
		var rtl = this.rtl, rtlOffset = rtl ? this.parent.width : void 0;
		if (useVisibleRange) offsetY = startY;
		else {
			var totalLinesHeight = this.getLinesHeight(drawLineStartIdx, drawLineEndIdx);
			if (valign === "center") offsetY = Math.max((textHeight - totalLinesHeight) / 2, 0);
			else if (valign === "bottom") offsetY = Math.max(textHeight - totalLinesHeight - 2, 0);
			else offsetY = 0;
			offsetY += startY;
		}
		for (var lineIdx = drawLineStartIdx; lineIdx < drawLineEndIdx; lineIdx++) {
			lineWidth = penManager.getLineWidth(lineIdx);
			if (lineWidth === 0) continue;
			var line = lines[lineIdx];
			if (!line) continue;
			var pens = line.pens, penCount = pens.length;
			var halign = defaultHalign;
			for (var penIdx = 0; penIdx < penCount; penIdx++) {
				var penAlign = pens[penIdx].prop.align;
				if (penAlign !== void 0) {
					halign = penAlign;
					break;
				}
			}
			if (halign === "center") offsetX = (textWidth - lineWidth) / 2;
			else if (halign === "right") offsetX = !rtl ? textWidth - lineWidth : 0;
			else offsetX = !rtl ? 0 : textWidth - lineWidth;
			offsetX += startX;
			var hitAreaHeight = this.getLineHeight(line) + defaultStyle.lineSpacing;
			for (var penIdx = 0; penIdx < penCount; penIdx++) this.drawPen(pens[penIdx], offsetX, offsetY, rtlOffset, hitAreaHeight);
		}
		if (clipText) context.restore();
		context.restore();
	},
	drawPen(pen, offsetX, offsetY, rtlOffset, lineHeight) {
		offsetX += pen.x;
		offsetY += pen.y + (pen.prop.y || 0);
		if (rtlOffset !== void 0) offsetX = rtlOffset - offsetX;
		var canvas = this.canvas;
		var context = this.context;
		context.save();
		var curStyle = this.parser.propToContextStyle(this.defaultStyle, pen.prop);
		if (curStyle.bgcolor !== null && pen.width > 0) {
			var metrics = this.defaultStyle.metrics;
			var bgTLY = offsetY - metrics.ascent;
			var bgHeight = metrics.fontSize;
			this.drawRectangle(offsetX, bgTLY, pen.width, bgHeight, curStyle.bgcolor, curStyle);
		}
		if (curStyle.underlineThickness > 0 && pen.width > 0) {
			var lineOffsetY = offsetY + curStyle.underlineOffset - curStyle.underlineThickness / 2;
			this.drawLine(offsetX, lineOffsetY, pen.width, curStyle.underlineThickness, curStyle.underlineColor, curStyle);
		}
		if (pen.isTextPen) {
			curStyle.buildFont();
			curStyle.syncFont(canvas, context);
			curStyle.syncStyle(canvas, context);
			this.drawText(offsetX, offsetY, pen.text, curStyle);
		}
		if (pen.isImagePen) this.drawImage(offsetX, offsetY, pen.prop.img, pen.prop.color, curStyle);
		if (curStyle.strikethroughThickness > 0 && pen.width > 0) {
			var lineOffsetY = offsetY + curStyle.strikethroughOffset - curStyle.strikethroughThickness / 2;
			this.drawLine(offsetX, lineOffsetY, pen.width, curStyle.strikethroughThickness, curStyle.strikethroughColor, curStyle);
		}
		context.restore();
		if (pen.hasAreaMarker && pen.width > 0) {
			var data;
			var areaKey = pen.prop.area;
			if (areaKey) data = { key: areaKey };
			else {
				var url = pen.prop.url;
				data = {
					key: `url:${url}`,
					url
				};
			}
			this.hitAreaManager.add(offsetX, offsetY - this.startYOffset, pen.width, lineHeight || this.defaultStyle.lineHeight, data);
		}
	},
	clear() {
		var canvas = this.canvas;
		this.context.clearRect(0, 0, canvas.width, canvas.height);
	},
	drawRectangle(x, y, width, height, color, style) {
		if (this.autoRound) {
			x = Math.round(x);
			y = Math.round(y);
		}
		var context = this.context;
		context.fillStyle = color;
		context.fillRect(x, y, width, height);
	},
	drawLine(x, y, width, height, color, style) {
		if (this.autoRound) {
			x = Math.round(x);
			y = Math.round(y);
		}
		var context = this.context;
		style.syncShadow(context, style.shadowStroke);
		var savedLineCap = context.lineCap;
		context.lineCap = "butt";
		context.strokeStyle = color;
		context.lineWidth = height;
		context.beginPath();
		context.moveTo(x, y);
		context.lineTo(x + width, y);
		context.stroke();
		context.lineCap = savedLineCap;
	},
	drawText(x, y, text, style) {
		if (this.autoRound) {
			x = Math.round(x);
			y = Math.round(y);
		}
		var context = this.context;
		var letterSpacing = style.letterSpacing;
		if (letterSpacing === 0) {
			if (style.stroke && style.stroke !== "none" && style.strokeThickness > 0) {
				style.syncShadow(context, style.shadowStroke);
				context.strokeText(text, x, y);
			}
			if (style.color && style.color !== "none") {
				style.syncShadow(context, style.shadowFill);
				context.fillText(text, x, y);
			}
		} else {
			var charcters = text.split("");
			for (var i = 0, cnt = charcters.length; i < cnt; i++) {
				var character = charcters[i];
				if (style.stroke && style.stroke !== "none" && style.strokeThickness > 0) {
					style.syncShadow(context, style.shadowStroke);
					context.strokeText(character, x, y);
				}
				if (style.color && style.color !== "none") {
					style.syncShadow(context, style.shadowFill);
					context.fillText(character, x, y);
				}
				x += context.measureText(character).width + letterSpacing;
			}
		}
	},
	drawImage(x, y, imgKey, color, style) {
		y -= this.startYOffset;
		this.parent.imageManager.draw(imgKey, this.context, x, y, color, this.autoRound);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/textbase/penmanger/Pen.js
var GetValue$272 = __webpack_exports__Utils.Objects.GetValue;
var NO_NEWLINE$3 = CONST.NO_NEWLINE;
var RAW_NEWLINE$1 = CONST.RAW_NEWLINE;
var Pen = class {
	constructor(config) {
		this.prop = {};
		this.resetFromJSON(config);
	}
	resetFromJSON(o) {
		this.text = GetValue$272(o, "text", "");
		this.x = GetValue$272(o, "x", 0);
		this.y = GetValue$272(o, "y", 0);
		this.width = GetValue$272(o, "width", 0);
		this.ascent = GetValue$272(o, "ascent", 0);
		this.descent = GetValue$272(o, "descent", 0);
		var height = GetValue$272(o, "height", null);
		if (height == null) height = this.ascent + this.descent;
		this.height = height;
		var prop = GetValue$272(o, "prop", null);
		if (prop === null) prop = {};
		this.prop = prop;
		this.newLineMode = GetValue$272(o, "newLineMode", 0);
		this.startIndex = GetValue$272(o, "startIndex", 0);
	}
	get plainText() {
		var txt = this.text;
		if (this.newLineMode === RAW_NEWLINE$1) txt += "\n";
		return txt;
	}
	get wrapText() {
		var txt = this.text;
		if (this.newLineMode !== NO_NEWLINE$3) txt += "\n";
		return txt;
	}
	get rawTextLength() {
		var len = this.text.length;
		if (this.newLineMode === RAW_NEWLINE$1) len += 1;
		return len;
	}
	get endIndex() {
		return this.startIndex + this.rawTextLength;
	}
	get lastX() {
		return this.x + this.width;
	}
	get isTextPen() {
		return this.text !== "";
	}
	get isImagePen() {
		return !!this.prop.img;
	}
	get hasAreaMarker() {
		return !!this.prop.area || !!this.prop.url;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/textbase/penmanger/Line.js
var Line$1 = class {
	constructor(pens) {
		this.pens = pens || [];
		this.maxAscent = 0;
		this.maxDescent = 0;
		this.lineHeight = 0;
		this.startOffset = 0;
		this.endOffset = 0;
	}
	reset() {
		this.pens.length = 0;
		this.maxAscent = 0;
		this.maxDescent = 0;
		this.lineHeight = 0;
		this.startOffset = 0;
		this.endOffset = 0;
		return this;
	}
	addPen(pen) {
		this.pens.push(pen);
		if (pen.ascent > this.maxAscent) this.maxAscent = pen.ascent;
		if (pen.descent > this.maxDescent) this.maxDescent = pen.descent;
		return this;
	}
	get length() {
		return this.pens.length;
	}
	get lastPen() {
		return this.pens[this.pens.length - 1];
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/object/Clone.js
/**
* Shallow Object Clone. Will not out nested objects.
* @param {object} obj JSON object
* @param {object} ret JSON object to return, set null to return a new object
* @returns {object} this object
*/
var Clone$2 = function(obj, out) {
	var objIsArray = Array.isArray(obj);
	if (out === void 0) out = objIsArray ? [] : {};
	else Clear$1(out);
	if (objIsArray) {
		out.length = obj.length;
		for (var i = 0, cnt = obj.length; i < cnt; i++) out[i] = obj[i];
	} else for (var key in obj) out[key] = obj[key];
	return out;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/textbase/penmanger/PenManager.js
var GetFastValue$2 = __webpack_exports__Utils.Objects.GetFastValue;
var NO_NEWLINE$2 = CONST.NO_NEWLINE;
var WRAPPED_NEWLINE$2 = CONST.WRAPPED_NEWLINE;
var PenManager = class PenManager {
	constructor(config) {
		this.pens = [];
		this.lines = [];
		this.maxLinesWidth = void 0;
		this.pensPool = config.pensPool;
		this.linesPool = config.linesPool;
		this.tagToText = GetFastValue$2(config, "tagToText", NOOP);
		this.tagToTextScope = GetFastValue$2(config, "tagToTextScope", void 0);
	}
	destroy() {
		this.clear();
		this.tagToText = void 0;
		this.tagToTextScope = void 0;
	}
	clear() {
		for (var i = 0, len = this.lines.length; i < len; i++) {
			var line = this.lines[i];
			if (line && line.reset) line.reset();
			else if (line) line.length = 0;
		}
		this.pensPool.pushMultiple(this.pens);
		this.linesPool.pushMultiple(this.lines);
		this.maxLinesWidth = void 0;
	}
	addTextPen(text, x, y, width, prop, newLineMode, metrics) {
		var pen = this.pensPool.pop();
		if (pen == null) pen = new Pen();
		PEN_CONFIG.text = text;
		PEN_CONFIG.x = x;
		PEN_CONFIG.y = y;
		PEN_CONFIG.width = width;
		if (metrics) {
			var ascent = metrics.ascent != null ? metrics.ascent : 0;
			var descent = metrics.descent != null ? metrics.descent : 0;
			var height = metrics.height != null ? metrics.height : ascent + descent;
			PEN_CONFIG.ascent = ascent;
			PEN_CONFIG.descent = descent;
			PEN_CONFIG.height = height;
		} else {
			PEN_CONFIG.ascent = 0;
			PEN_CONFIG.descent = 0;
			PEN_CONFIG.height = 0;
		}
		PEN_CONFIG.prop = prop;
		PEN_CONFIG.newLineMode = newLineMode;
		pen.resetFromJSON(PEN_CONFIG);
		this.addPen(pen);
		return this;
	}
	addImagePen(x, y, width, prop, metrics) {
		this.addTextPen("", x, y, width, prop, NO_NEWLINE$2, metrics);
		return this;
	}
	addNewLinePen() {
		var previousPen = this.lastPen;
		var x = previousPen ? previousPen.lastX : 0;
		var y = previousPen ? previousPen.y : 0;
		var prop = previousPen ? Clone$2(previousPen.prop) : null;
		this.addTextPen("", x, y, 0, prop, WRAPPED_NEWLINE$2);
		return this;
	}
	addPen(pen) {
		var previousPen = this.lastPen;
		if (previousPen == null) pen.startIndex = 0;
		else pen.startIndex = previousPen.endIndex;
		this.pens.push(pen);
		var line = this.lastLine;
		if (line == null) {
			line = this.newLine();
			this.lines.push(line);
		} else if (!line.pens) {
			line = this.convertLine(line);
			this.lines[this.lines.length - 1] = line;
		}
		if (line.addPen) line.addPen(pen);
		else line.push(pen);
		if (pen.newLineMode !== NO_NEWLINE$2) {
			line = this.newLine();
			this.lines.push(line);
		}
		this.maxLinesWidth = void 0;
	}
	clone(targetPenManager) {
		if (targetPenManager == null) targetPenManager = new PenManager();
		targetPenManager.clear();
		for (var li = 0, llen = this.lines.length; li < llen; li++) {
			var line = this.lines[li];
			var pens = line && line.pens ? line.pens : line;
			for (var pi = 0, plen = pens.length; pi < plen; pi++) {
				var pen = pens[pi];
				targetPenManager.addPen(pen.text, pen.x, pen.y, pen.width, Clone$2(pen.prop), pen.newLineMode);
			}
		}
		return targetPenManager;
	}
	get lastPen() {
		return this.pens[this.pens.length - 1];
	}
	get lastLine() {
		return this.lines[this.lines.length - 1];
	}
	getLineStartIndex(i) {
		if (i >= this.lines.length) return this.getLineEndIndex(i);
		else {
			var line = this.lines[i];
			var pens = line && line.pens ? line.pens : line;
			return pens && pens[0] ? pens[0].startIndex : 0;
		}
	}
	getLineEndIndex(i) {
		if (i >= this.lines.length) i = this.lines.length - 1;
		var li, hasLastPen = false, line;
		for (li = i; li >= 0; li--) {
			line = this.lines[li];
			var pens = line && line.pens ? line.pens : line;
			hasLastPen = pens != null && pens.length > 0;
			if (hasLastPen) break;
		}
		if (!hasLastPen) return 0;
		var pens = line && line.pens ? line.pens : line;
		return pens[pens.length - 1].endIndex;
	}
	getLineWidth(i) {
		var line = this.lines[i];
		if (!line) return 0;
		var pens = line && line.pens ? line.pens : line;
		var lastPen = pens[pens.length - 1];
		if (lastPen == null) return 0;
		return lastPen.lastX;
	}
	getMaxLineWidth() {
		if (this.maxLinesWidth !== void 0) return this.maxLinesWidth;
		var w, maxW = 0;
		for (var i = 0, len = this.lines.length; i < len; i++) {
			w = this.getLineWidth(i);
			if (w > maxW) maxW = w;
		}
		this.maxLinesWidth = maxW;
		return maxW;
	}
	getLineWidths() {
		var result = [];
		for (var i = 0, len = this.lines.length; i < len; i++) result.push(this.getLineWidth(i));
		return result;
	}
	get linesCount() {
		return this.lines.length;
	}
	newLine() {
		return this.convertLine(this.linesPool.pop());
	}
	convertLine(line) {
		if (!line) return new Line$1();
		if (line.pens) return line.reset();
		if (Array.isArray(line)) return new Line$1(line).reset();
		return new Line$1();
	}
	get plainText() {
		var txt = "", pens = this.pens;
		for (var i = 0, len = pens.length; i < len; i++) txt += pens[i].plainText;
		return txt;
	}
	get rawTextLength() {
		var l = 0, pens = this.pens;
		for (var i = 0, len = this.pens.length; i < len; i++) l += pens[i].rawTextLength;
		return l;
	}
	getSliceTagText(start, end, wrap) {
		var lastPen = this.lastPen;
		if (lastPen == null) return "";
		var lastPenEnd = lastPen.endIndex;
		if (start === void 0 || start === 0) start = -1;
		if (end === void 0 || end === lastPenEnd) end = lastPenEnd + 1;
		if (wrap === void 0) wrap = false;
		var txt = "", pen, penTxt, penStartIdx, penEndIdx, isInRange;
		var currentProp, previousProp;
		for (var i = 0, len = this.pens.length; i < len; i++) {
			pen = this.pens[i];
			penEndIdx = pen.endIndex;
			if (penEndIdx <= start) continue;
			pen = this.pens[i];
			penTxt = !wrap ? pen.plainText : pen.wrapText;
			currentProp = pen.prop;
			penStartIdx = pen.startIndex;
			isInRange = penStartIdx >= start && penEndIdx <= end;
			if (!isInRange) penTxt = penTxt.substring(start - penStartIdx, end - penStartIdx);
			if (this.tagToTextScope) txt += this.tagToText.call(this.tagToTextScope, penTxt, currentProp, previousProp);
			else txt += this.tagToText(penTxt, currentProp, previousProp);
			previousProp = currentProp;
			if (penEndIdx >= end) break;
		}
		return txt;
	}
	get length() {
		return this.lines.length;
	}
	set length(value) {
		this.clear();
	}
};
var PEN_CONFIG = {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/textbase/hitareamanager/HitAreaManager.js
var Rectangle$6 = __webpack_exports__Geom.Rectangle;
var RectanglePool = new pool_default();
var HitAreaManager = class {
	constructor() {
		this.hitAreas = [];
	}
	destroy() {
		this.clear();
	}
	clear() {
		for (var i = 0, cnt = this.hitAreas.length; i < cnt; i++) this.hitAreas[i].data = null;
		RectanglePool.pushMultiple(this.hitAreas);
		return this;
	}
	add(x, y, width, height, data) {
		if (data === void 0) data = {};
		var rectangle = RectanglePool.pop();
		if (rectangle === null) rectangle = new Rectangle$6(x, y, width, height);
		else rectangle.setTo(x, y, width, height);
		rectangle.data = data;
		this.hitAreas.push(rectangle);
		return this;
	}
	getFirst(x, y) {
		for (var i = 0, cnt = this.hitAreas.length; i < cnt; i++) {
			var hitArea = this.hitAreas[i];
			if (hitArea.contains(x, y)) return hitArea;
		}
		return null;
	}
	getByKey(key) {
		for (var i = 0, cnt = this.hitAreas.length; i < cnt; i++) {
			var hitArea = this.hitAreas[i];
			if (hitArea.data.key === key) return hitArea;
		}
		return null;
	}
	drawBounds(graphics, color, parent) {
		if (color === void 0) color = 16777215;
		if (parent) graphics.save().translateCanvas(parent.x, parent.y).rotateCanvas(parent.rotation).scaleCanvas(parent.scaleX, parent.scaleY);
		for (var i = 0, cnt = this.hitAreas.length; i < cnt; i++) {
			var hitArea = this.hitAreas[i];
			graphics.lineStyle(1, color).strokeRect(hitArea.x, hitArea.y, hitArea.width, hitArea.height);
		}
		if (parent) graphics.restore();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/textbase/canvastext/HitAreaCursorStyle.js
var HasOwn = Object.prototype.hasOwnProperty;
var HitAreaCursorStyle = class {
	constructor() {
		this.url = null;
		this.tags = {};
		this.default = null;
	}
	destroy() {
		this.tags = void 0;
	}
	setURL(cursorStyle) {
		this.url = cursorStyle;
		return this;
	}
	setTag(key, cursorStyle) {
		this.tags[key] = cursorStyle;
		return this;
	}
	setDefault(cursorStyle) {
		this.default = cursorStyle;
		return this;
	}
	has(data) {
		if (!data) return false;
		return !!(this.url && data.url || HasOwn.call(this.tags, data.key) || !!this.default);
	}
	get(data) {
		if (!data) return null;
		if (this.url && data.url) return this.url;
		else if (HasOwn.call(this.tags, data.key)) return this.tags[data.key];
		else if (this.default) return this.default;
		else return null;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/textbase/canvastext/SetInteractive.js
var SetInteractive$1 = function() {
	this.parent.on("pointerdown", OnAreaDown, this).on("pointerup", OnAreaUp, this).on("pointermove", OnAreaOverOut, this).on("pointerover", OnAreaOverOut, this).on("pointerout", function(pointer, event) {
		OnAreaOverOut.call(this, pointer, null, null, event);
	}, this);
};
var OnAreaDown = function(pointer, localX, localY, event) {
	var area = this.hitAreaManager.getFirst(localX, localY);
	if (area === null) return;
	var key = area.data.key;
	FireEvent$2.call(this, "areadown", key, pointer, localX, localY, event);
	if (!area.data) return;
	area.data.isDown = true;
};
var OnAreaUp = function(pointer, localX, localY, event) {
	var area = this.hitAreaManager.getFirst(localX, localY);
	if (area === null) return;
	var key = area.data.key;
	FireEvent$2.call(this, "areaup", key, pointer, localX, localY, event);
	if (!area.data) return;
	if (area.data.isDown) {
		FireEvent$2.call(this, "areaclick", key, pointer, localX, localY, event);
		if (!area.data) return;
		var url = area.data.url;
		if (url) window.open(url, "_blank");
	}
	area.data.isDown = false;
};
var OnAreaOverOut = function(pointer, localX, localY, event) {
	if (localX === null) {
		if (this.lastHitAreaKey !== null) {
			var area = this.hitAreaManager.getByKey(this.lastHitAreaKey);
			var hasCursorStyle = area && area.data && this.hitAreaCursorStyle.has(area.data);
			FireEvent$2.call(this, "areaout", this.lastHitAreaKey, pointer, localX, localY, event);
			if (hasCursorStyle) SetCursorStyle(this.scene, "");
			if (area && area.data) area.data.isDown = false;
			this.lastHitAreaKey = null;
		}
		return;
	}
	var area = this.hitAreaManager.getFirst(localX, localY);
	var key = area ? area.data.key : null;
	if (this.lastHitAreaKey === key) return;
	if (this.lastHitAreaKey !== null) {
		var prevHitArea = this.hitAreaManager.getByKey(this.lastHitAreaKey);
		var prevHasCursorStyle = prevHitArea && prevHitArea.data && this.hitAreaCursorStyle.has(prevHitArea.data);
		FireEvent$2.call(this, "areaout", this.lastHitAreaKey, pointer, localX, localY, event);
		if (prevHasCursorStyle) SetCursorStyle(this.scene, "");
		if (prevHitArea && prevHitArea.data) prevHitArea.data.isDown = false;
	}
	if (key !== null) {
		FireEvent$2.call(this, "areaover", key, pointer, localX, localY, event);
		if (area.data) {
			if (this.hitAreaCursorStyle.has(area.data)) SetCursorStyle(this.scene, this.hitAreaCursorStyle.get(area.data));
		}
	}
	this.lastHitAreaKey = key;
};
var FireEvent$2 = function(eventName, key, pointer, localX, localY, event) {
	this.parent.emit(`${eventName}-${key}`, pointer, localX, localY, event);
	this.parent.emit(eventName, key, pointer, localX, localY, event);
};
var SetCursorStyle = function(scene, cursorStyle) {
	scene.input.manager.canvas.style.cursor = cursorStyle;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/position/WorldXYToGameObjectLocalXY.js
var TransformMatrix$3 = __webpack_exports__GameObjects.Components.TransformMatrix;
var TransformXY = __webpack_exports__Math.TransformXY;
var WorldXYToGameObjectLocalXY = function(gameObject, worldX, worldY, camera, out) {
	if (camera === void 0) camera = gameObject.scene.cameras.main;
	if (out === void 0) out = {};
	else if (out === true) out = globOut$2;
	var csx = camera.scrollX;
	var csy = camera.scrollY;
	var px = worldX + csx * gameObject.scrollFactorX - csx;
	var py = worldY + csy * gameObject.scrollFactorY - csy;
	if (gameObject.parentContainer) {
		if (tempMatrix$1 === void 0) {
			tempMatrix$1 = new TransformMatrix$3();
			parentMatrix$1 = new TransformMatrix$3();
		}
		gameObject.getWorldTransformMatrix(tempMatrix$1, parentMatrix$1);
		tempMatrix$1.applyInverse(px, py, out);
	} else TransformXY(px, py, gameObject.x, gameObject.y, gameObject.rotation, gameObject.scaleX, gameObject.scaleY, out);
	out.x += gameObject.displayOriginX;
	out.y += gameObject.displayOriginY;
	return out;
};
var tempMatrix$1;
var parentMatrix$1;
var globOut$2 = {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/textbase/canvastext/GetHitArea.js
var GetHitArea = function(worldX, worldY, camera) {
	var localXY = WorldXYToGameObjectLocalXY(this.parent, worldX, worldY, camera, true);
	var area = this.hitAreaManager.getFirst(localXY.x, localXY.y);
	if (area === null) return;
	return area.data.key;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/string/IsASCIIString.js
var RE_ASCII = /^[\x00-\x7F]+$/;
var IsASCIIString = function(s) {
	return RE_ASCII.test(s);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/textbase/wraptext/WrapText.js
var NO_NEWLINE$1 = CONST.NO_NEWLINE;
var RAW_NEWLINE = CONST.RAW_NEWLINE;
var WRAPPED_NEWLINE$1 = CONST.WRAPPED_NEWLINE;
var NO_WRAP$1 = CONST.NO_WRAP;
var WORD_WRAP = CONST.WORD_WRAP;
var CHAR_WRAP = CONST.CHAR_WRAP;
CONST.MIX_WRAP;
var splitRegExp = CONST.SPLITREGEXP;
var WrapText = function(text, context, wrapMode, wrapWidth, letterSpacing, offset, wrapTextLinesPool) {
	if (wrapWidth <= 0) wrapMode = NO_WRAP$1;
	var retLines = [];
	if (!text || !text.length) return retLines;
	var isNoWrap = wrapMode === NO_WRAP$1;
	var lines = text.split(splitRegExp), line, remainWidth, newLineMode;
	for (var i = 0, linesLen = lines.length; i < linesLen; i++) {
		line = lines[i];
		newLineMode = i === linesLen - 1 ? NO_NEWLINE$1 : RAW_NEWLINE;
		if (isNoWrap) {
			var textWidth = context.measureText(line).width + letterSpacing * line.length;
			retLines.push(wrapTextLinesPool.getLine(line, textWidth, newLineMode));
			continue;
		}
		remainWidth = i === 0 ? wrapWidth - offset : wrapWidth;
		if (line.length <= 100) {
			var textWidth = context.measureText(line).width + letterSpacing * line.length;
			if (textWidth <= remainWidth) {
				retLines.push(wrapTextLinesPool.getLine(line, textWidth, newLineMode));
				continue;
			}
		}
		var tokenArray = ParseLine(line, wrapMode);
		var token, tokenWidth;
		var lineText = "", lineWidth = 0;
		var currLineWidth;
		for (var j = 0, tokenLen = tokenArray.length; j < tokenLen; j++) {
			token = tokenArray[j];
			tokenWidth = context.measureText(token).width + letterSpacing * token.length;
			if (tokenWidth > wrapWidth && IsWord(token)) {
				if (lineText !== "") retLines.push(wrapTextLinesPool.getLine(lineText, lineWidth, WRAPPED_NEWLINE$1));
				else if (j === 0 && offset > 0) retLines.push(wrapTextLinesPool.getLine("", 0, WRAPPED_NEWLINE$1));
				retLines.push(...WrapText(token, context, CHAR_WRAP, wrapWidth, letterSpacing, 0, wrapTextLinesPool));
				var lastwordBreakLine = retLines.pop();
				lineText = lastwordBreakLine.text;
				lineWidth = lastwordBreakLine.width;
				wrapTextLinesPool.freeLine(lastwordBreakLine);
				if (lineText === " ") {
					lineText = "";
					lineWidth = 0;
				}
			} else {
				currLineWidth = lineWidth + tokenWidth;
				if (currLineWidth > remainWidth) {
					retLines.push(wrapTextLinesPool.getLine(lineText, lineWidth, WRAPPED_NEWLINE$1));
					lineText = token;
					lineWidth = tokenWidth;
					remainWidth = wrapWidth;
				} else {
					lineText += token;
					lineWidth = currLineWidth;
				}
			}
			if (j === tokenLen - 1) retLines.push(wrapTextLinesPool.getLine(lineText, lineWidth, newLineMode));
		}
	}
	return retLines;
};
var ParseLine = function(s, mode) {
	var tokens;
	switch (mode) {
		case WORD_WRAP:
			tokens = [];
			s = s.split(" ");
			for (var i = 0, icnt = s.length; i < icnt; i++) {
				var token = s[i];
				if (i < icnt - 1) tokens.push(token + " ");
				else if (token !== "") tokens.push(token);
			}
			break;
		case CHAR_WRAP:
			tokens = s.split("");
			break;
		default:
			tokens = [];
			s = s.split(" ");
			for (var i = 0, icnt = s.length; i < icnt; i++) {
				var token = s[i];
				if (i < icnt - 1) {
					if (IsASCIIString(token)) tokens.push(token + " ");
					else {
						tokens.push(...token.split(""));
						tokens.push(" ");
					}
				} else if (token !== "") {
					if (IsASCIIString(token)) tokens.push(token);
					else tokens.push(...token.split(""));
				}
			}
	}
	return tokens;
};
var IsWord = function(s) {
	switch (s.length) {
		case 1: return false;
		case 2: return s.charAt(1) !== " ";
		default: return true;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/textbase/canvastext/CanvasText.js
var GetValue$271 = __webpack_exports__Utils.Objects.GetValue;
var NO_WRAP = CONST.NO_WRAP;
var NO_NEWLINE = CONST.NO_NEWLINE;
CONST.RAW_NEWLINE;
var WRAPPED_NEWLINE = CONST.WRAPPED_NEWLINE;
var CanvasText = class {
	constructor(config) {
		this.parent = config.parent;
		this.scene = this.parent.scene;
		this.context = GetValue$271(config, "context", null);
		this.canvas = this.context.canvas;
		this.parser = GetValue$271(config, "parser", null);
		this.defaultStyle = GetValue$271(config, "style", null);
		this.autoRound = true;
		this.pensPool = config.pensPool;
		this.linesPool = config.linesPool;
		this.wrapTextLinesPool = config.wrapTextLinesPool;
		this.penManager = this.newPenManager();
		this._tmpPenManager = null;
		this.hitAreaManager = new HitAreaManager();
		this.hitAreaCursorStyle = new HitAreaCursorStyle();
		this.lastHitAreaKey = null;
	}
	destroy() {
		this.parent = void 0;
		this.scene = void 0;
		this.context = void 0;
		this.canvas = void 0;
		this.parser = void 0;
		this.defaultStyle = void 0;
		if (this.penManager) {
			this.penManager.destroy();
			this.penManager = void 0;
		}
		if (this._tmpPenManager) {
			this._tmpPenManager.destroy();
			this._tmpPenManager = void 0;
		}
		if (this.hitAreaManager) {
			this.hitAreaManager.destroy();
			this.hitAreaManager = void 0;
		}
		if (this.hitAreaCursorStyle) {
			this.hitAreaCursorStyle.destroy();
			this.hitAreaCursorStyle = void 0;
		}
		this.pensPool = void 0;
		this.linesPool = void 0;
		this.wrapTextLinesPool = void 0;
	}
	updatePenManager(text, wrapMode, wrapWidth, lineHeight, penManager) {
		if (penManager === void 0) penManager = this.penManager;
		penManager.clear();
		if (text === "") return penManager;
		var textStyle = this.parent.style;
		if (textStyle.isWrapFitMode) {
			var padding = this.parent.padding;
			wrapWidth = textStyle.fixedWidth - padding.left - padding.right;
		}
		var canvas = this.canvas;
		var context = this.context;
		var cursorX = 0, cursorY = 0;
		var customTextWrapCallback = textStyle.wrapCallback, customTextWrapCallbackScope = textStyle.wrapCallbackScope;
		var isBuiltInWrappingMode = !customTextWrapCallback;
		var reuseLines = true;
		var plainText, curProp, curStyle;
		var match = this.parser.splitText(text), result, wrapLines, wrapTextLinesPool = this.wrapTextLinesPool;
		for (var i = 0, len = match.length; i < len; i++) {
			result = this.parser.tagTextToProp(match[i], curProp);
			plainText = result.plainText;
			curProp = result.prop;
			if (curProp.img) {
				var cursor = this.addImagePen(curProp, cursorX, cursorY, wrapWidth, wrapMode, lineHeight, penManager);
				cursorX = cursor.x;
				cursorY = cursor.y;
			} else if (plainText !== "") {
				context.save();
				curStyle = this.parser.propToContextStyle(this.defaultStyle, curProp);
				curStyle.buildFont();
				curStyle.syncFont(canvas, context);
				curStyle.syncStyle(canvas, context);
				var metrics = MeasureText(curStyle);
				if (isBuiltInWrappingMode) wrapLines = WrapText(plainText, context, wrapMode, wrapWidth, curStyle.letterSpacing, cursorX, wrapTextLinesPool);
				else {
					wrapLines = customTextWrapCallback.call(customTextWrapCallbackScope, plainText, context, wrapWidth, curStyle.letterSpacing, cursorX);
					if (typeof wrapLines === "string") wrapLines = wrapLines.split("\n");
					var segment;
					for (var j = 0, jLen = wrapLines.length; j < jLen; j++) {
						segment = wrapLines[j];
						if (typeof segment === "string") wrapLines[j] = wrapTextLinesPool.getLine(segment, context.measureText(segment).width, j < jLen - 1 ? WRAPPED_NEWLINE : NO_NEWLINE);
						else reuseLines = false;
					}
				}
				var segment;
				for (var j = 0, jLen = wrapLines.length; j < jLen; j++) {
					segment = wrapLines[j];
					penManager.addTextPen(segment.text, cursorX, cursorY, segment.width, Clone$2(curProp), segment.newLineMode, metrics);
					if (segment.newLineMode !== NO_NEWLINE) {
						cursorX = 0;
						cursorY += lineHeight;
					} else cursorX += segment.width;
				}
				if (reuseLines) wrapTextLinesPool.freeLines(wrapLines);
				wrapLines = null;
				context.restore();
			}
		}
		var lines = penManager.lines;
		for (var i = 0, len = lines.length; i < len; i++) {
			var pens = lines[i].pens;
			var lastPen = pens[pens.length - 1];
			if (lastPen) {
				lastPen.width += this.parser.getStrokeThinkness(this.defaultStyle, lastPen.prop);
				lastPen.width -= this.parser.getLetterSpacing(this.defaultStyle, lastPen.prop);
			}
		}
		this.updateLineOffsets(lines, textStyle);
		return penManager;
	}
	addImagePen(prop, cursorX, cursorY, wrapWidth, wrapMode, lineHeight, penManager) {
		var imgWidth = this.imageManager.getOuterWidth(prop.img);
		var imgHeight = this.imageManager.getOuterHeight(prop.img);
		var imgMetrics;
		if (imgHeight > 0) {
			var ascent = this.defaultStyle.metrics.ascent;
			var descent = imgHeight - ascent;
			if (descent < 0) descent = 0;
			imgMetrics = {
				ascent,
				descent,
				height: ascent + descent
			};
		}
		if (wrapWidth > 0 && wrapMode !== NO_WRAP) {
			if (wrapWidth < cursorX + imgWidth) {
				penManager.addNewLinePen();
				cursorY += lineHeight;
				cursorX = 0;
			}
		}
		penManager.addImagePen(cursorX, cursorY, imgWidth, Clone$2(prop), imgMetrics);
		cursorX += imgWidth;
		return {
			x: cursorX,
			y: cursorY
		};
	}
	updateLineOffsets(lines, textStyle) {
		var lineSpacing = textStyle.lineSpacing;
		var defaultMetrics = this.defaultStyle.metrics;
		var defaultAscent = defaultMetrics.ascent;
		var defaultDescent = defaultMetrics.descent;
		var offsetY = 0;
		var fixedLineHeightMode = textStyle.fixedLineHeightMode;
		for (var i = 0, len = lines.length; i < len; i++) {
			var line = lines[i];
			var lineAscent = line.maxAscent;
			var lineDescent = line.maxDescent;
			if (fixedLineHeightMode || lineAscent === 0 && lineDescent === 0) {
				lineAscent = defaultAscent;
				lineDescent = defaultDescent;
			}
			line.maxAscent = lineAscent;
			line.maxDescent = lineDescent;
			line.lineHeight = lineAscent + lineDescent;
			line.startOffset = offsetY;
			line.endOffset = offsetY + line.lineHeight;
			var baselineOffset = offsetY + lineAscent - defaultAscent;
			var pens = line.pens;
			for (var penIdx = 0, penCnt = pens.length; penIdx < penCnt; penIdx++) pens[penIdx].y = baselineOffset;
			offsetY = line.endOffset + lineSpacing;
		}
	}
	get startXOffset() {
		return this.defaultStyle.xOffset;
	}
	get startYOffset() {
		return this.defaultStyle.metrics.ascent;
	}
	get lines() {
		return this.penManager.lines;
	}
	get displayLinesCount() {
		var linesCount = this.penManager.linesCount, maxLines = this.defaultStyle.maxLines;
		if (maxLines > 0 && linesCount > maxLines) linesCount = maxLines;
		return linesCount;
	}
	get linesWidth() {
		return Math.ceil(this.penManager.getMaxLineWidth());
	}
	get linesHeight() {
		var linesCount = this.displayLinesCount;
		return this.getLinesHeight(0, linesCount);
	}
	getLineHeight(line) {
		var lineHeight = line ? line.lineHeight : 0;
		if (lineHeight > 0) return lineHeight;
		var metrics = this.defaultStyle.metrics;
		return metrics.ascent + metrics.descent;
	}
	getLinesHeight(start, end) {
		var lines = this.penManager.lines;
		var linesCount = lines.length;
		if (start === void 0) start = 0;
		if (end === void 0 || end > linesCount) end = linesCount;
		if (end <= start) return 0;
		var height = 0;
		for (var i = start; i < end; i++) height += this.getLineHeight(lines[i]);
		var count = end - start;
		if (count > 1) height += this.defaultStyle.lineSpacing * (count - 1);
		return height;
	}
	get imageManager() {
		return this.parent.imageManager;
	}
	get rtl() {
		return this.parent.style.rtl;
	}
	newPenManager() {
		return new PenManager({
			pensPool: this.pensPool,
			linesPool: this.linesPool,
			tagToText: this.parser.propToTagText,
			tagToTextScope: this.parser
		});
	}
	get tmpPenManager() {
		if (this._tmpPenManager === null) this._tmpPenManager = this.newPenManager();
		return this._tmpPenManager;
	}
	getPlainText(text, start, end) {
		var plainText;
		if (text == null) plainText = this.penManager.plainText;
		else {
			var match = this.parser.splitText(text, 1);
			plainText = "";
			for (var i = 0, len = match.length; i < len; i++) plainText += match[i];
		}
		if (start != null || end != null) {
			if (start == null) start = 0;
			if (end == null) end = plainText.length;
			plainText = plainText.substring(start, end);
		}
		return plainText;
	}
	getPenManager(text, retPenManager) {
		if (text === void 0) return this.copyPenManager(retPenManager, this.penManager);
		if (retPenManager === void 0) retPenManager = this.newPenManager();
		var defaultStyle = this.defaultStyle;
		this.updatePenManager(text, defaultStyle.wrapMode, defaultStyle.wrapWidth, defaultStyle.lineHeight, retPenManager);
		return retPenManager;
	}
	getText(text, start, end, wrap) {
		if (text == null) return this.penManager.getSliceTagText(start, end, wrap);
		var penManager = this.tmpPenManager;
		var defaultStyle = this.defaultStyle;
		this.updatePenManager(text, defaultStyle.wrapMode, defaultStyle.wrapWidth, defaultStyle.lineHeight, penManager);
		return penManager.getSliceTagText(start, end, wrap);
	}
	copyPenManager(ret, src) {
		if (src === void 0) src = this.penManager;
		return src.copy(ret);
	}
	getTextWidth(penManager) {
		if (penManager === void 0) penManager = this.penManager;
		return penManager.getMaxLineWidth();
	}
	getLastPen(penManager) {
		if (penManager === void 0) penManager = this.penManager;
		return penManager.lastPen;
	}
	get urlTagCursorStyle() {
		return this.hitAreaCursorStyle.url;
	}
	set urlTagCursorStyle(value) {
		this.hitAreaCursorStyle.setURL(value);
	}
	get hitAreaTagsCursorStyle() {
		return this.hitAreaCursorStyle.tags;
	}
	set hitAreaTagsCursorStyle(value) {
		this.hitAreaCursorStyle.tags = value;
	}
	get defaultHitAreaTagCursorStyle() {
		return this.hitAreaCursorStyle.default;
	}
	set defaultHitAreaTagCursorStyle(value) {
		this.hitAreaCursorStyle.setDefault(value);
	}
};
var methods$42 = {
	setInteractive: SetInteractive$1,
	getHitArea: GetHitArea
};
Object.assign(CanvasText.prototype, DrawMethods_default, methods$42);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/textbase/wraptext/WrapTextLinesPool.js
var WrapTextLinesPool = class extends pool_default {
	freeLine(line) {
		if (!line) return;
		this.push(line);
		return this;
	}
	freeLines(lines) {
		if (!lines) return;
		this.pushMultiple(lines);
		return this;
	}
	getLine(text, width, newLineMode) {
		var l = this.pop();
		if (l === null) l = {};
		l.text = text;
		l.width = width;
		l.newLineMode = newLineMode;
		return l;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/texture/imagemanager/AddImage.js
var IsPlainObject$54 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$270 = __webpack_exports__Utils.Objects.GetValue;
var TintModes$2 = __webpack_exports__TintModes;
var AddImage$1 = function(key, config) {
	if (IsPlainObject$54(key)) {
		config = key;
		key = config.key;
	} else if (config === void 0) config = { key };
	if (!config.hasOwnProperty("key")) config.key = key;
	var textureKey = config.key, frameKey = config.frame;
	var width = config.width, height = config.height;
	if (width === void 0 || height === void 0) {
		var frame = this.textureManager.getFrame(textureKey, frameKey);
		var frameWidth = frame ? frame.cutWidth : 0;
		var frameHeight = frame ? frame.cutHeight : 0;
		if (width === void 0 && height === void 0) {
			width = frameWidth;
			height = frameHeight;
		} else if (width === void 0) width = frameWidth * (height / frameHeight);
		else if (height === void 0) height = frameHeight * (width / frameWidth);
	}
	var tintMode = config.tintMode;
	if (tintMode === void 0 && config.tintFill === true) tintMode = TintModes$2.FILL;
	this.images[key] = {
		key: textureKey,
		frame: frameKey,
		width,
		height,
		y: GetValue$270(config, "y", 0),
		left: GetValue$270(config, "left", 0),
		right: GetValue$270(config, "right", 0),
		originX: GetValue$270(config, "originX", 0),
		originY: GetValue$270(config, "originY", 0),
		tintMode
	};
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/texture/DrawFrameToCanvas.js
var CanvasPool$1 = __webpack_exports__Display.Canvas.CanvasPool;
var TintModes$1 = __webpack_exports__TintModes;
var GetContext2D = function(canvasOrContext) {
	if (canvasOrContext && typeof canvasOrContext.drawImage === "function" && canvasOrContext.canvas) return canvasOrContext;
	if (canvasOrContext && typeof canvasOrContext.getContext === "function") return canvasOrContext.getContext("2d", { willReadFrequently: true });
	return null;
};
var DrawFrameToCanvas = function(frame, canvasOrContext, x, y, width, height, color, autoRound, tintMode) {
	if (x === void 0) x = 0;
	if (y === void 0) y = 0;
	if (width === void 0) width = frame.cutWidth;
	if (height === void 0) height = frame.cutHeight;
	if (autoRound === void 0) autoRound = false;
	if (autoRound) {
		x = Math.round(x);
		y = Math.round(y);
	}
	var context = GetContext2D(canvasOrContext);
	if (color === void 0 || color === null || typeof tintMode !== "number") context.drawImage(frame.source.image, frame.cutX, frame.cutY, frame.cutWidth, frame.cutHeight, x, y, width, height);
	else {
		var tempCanvas = CanvasPool$1.create(null, width, height, __webpack_exports__CANVAS, true);
		var tempContext = tempCanvas.getContext("2d", { willReadFrequently: true });
		var sourceImage = frame.source.image;
		var sourceX = frame.cutX;
		var sourceY = frame.cutY;
		var sourceWidth = frame.cutWidth;
		var sourceHeight = frame.cutHeight;
		tempContext.drawImage(sourceImage, sourceX, sourceY, sourceWidth, sourceHeight, 0, 0, width, height);
		if (tintMode === TintModes$1.FILL) {
			tempContext.globalCompositeOperation = "source-in";
			tempContext.fillStyle = color;
			tempContext.fillRect(0, 0, width, height);
		} else {
			var compositeOperation = "source-in";
			switch (tintMode) {
				case TintModes$1.MULTIPLY:
					compositeOperation = "multiply";
					break;
				case TintModes$1.ADD:
					compositeOperation = "lighter";
					break;
				case TintModes$1.SCREEN:
					compositeOperation = "screen";
					break;
				case TintModes$1.OVERLAY:
					compositeOperation = "overlay";
					break;
				case TintModes$1.HARD_LIGHT: compositeOperation = "hard-light";
			}
			if (compositeOperation === "source-in") {
				tempContext.globalCompositeOperation = "source-in";
				tempContext.fillStyle = color;
				tempContext.fillRect(0, 0, width, height);
			} else {
				tempContext.globalCompositeOperation = compositeOperation;
				tempContext.fillStyle = color;
				tempContext.fillRect(0, 0, width, height);
				tempContext.globalCompositeOperation = "destination-in";
				tempContext.drawImage(sourceImage, sourceX, sourceY, sourceWidth, sourceHeight, 0, 0, width, height);
			}
		}
		context.drawImage(tempCanvas, 0, 0, width, height, x, y, width, height);
		CanvasPool$1.remove(tempCanvas);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/texture/imagemanager/DrawImage.js
var DrawImage = function(key, context, x, y, color, autoRound) {
	var imgData = this.get(key);
	if (!imgData) return;
	var frame = this.textureManager.getFrame(imgData.key, imgData.frame);
	var width = imgData.width, height = imgData.height;
	x += imgData.left - imgData.originX * width;
	y += imgData.y - imgData.originY * height;
	var tintMode = imgData.tintMode;
	if (tintMode === void 0) color = void 0;
	DrawFrameToCanvas(frame, context, x, y, width, height, color, autoRound, tintMode);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/texture/imagemanager/ImageManager.js
var ImageManager = class {
	constructor(scene) {
		this.textureManager = scene.sys.textures;
		this.images = {};
	}
	destroy() {
		this.textureManager = void 0;
		this.images = void 0;
	}
	add(key, config) {
		if (typeof key === "string") AddImage$1.call(this, key, config);
		else if (Array.isArray(key)) {
			var data = key;
			for (var i = 0, cnt = data.length; i < cnt; i++) AddImage$1.call(this, data[i]);
		} else {
			var data = key;
			for (var key in data) AddImage$1.call(this, key, data[key]);
		}
		return this;
	}
	has(key) {
		return this.images.hasOwnProperty(key);
	}
	remove(key) {
		if (this.has(key)) delete this.images[key];
		return this;
	}
	get(key) {
		if (!this.has(key)) {
			if (this.textureManager.exists(key)) this.add(key);
		}
		return this.images[key];
	}
	getOuterWidth(key) {
		var data = this.get(key);
		return data ? data.width + data.left + data.right : 0;
	}
	getOuterHeight(key) {
		var data = this.get(key);
		return data ? data.height : 0;
	}
	getFrame(key) {
		var data = this.get(key);
		return data ? this.textureManager.getFrame(data.key, data.frame) : void 0;
	}
	hasTexture(key) {
		return !!this.getFrame(key);
	}
};
var methods$41 = { draw: DrawImage };
Object.assign(ImageManager.prototype, methods$41);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/text/AppendText.js
var AppendText$1 = function(value, addCR) {
	if (!value && value !== 0) value = "";
	if (addCR === void 0) addCR = true;
	if (Array.isArray(value)) value = value.join("\n");
	var newText;
	if (addCR) newText = `${this.text}\n${value}`;
	else newText = `${this.text}${value}`;
	if (newText != this.text) this.setText(newText);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/textbase/Text.js
var IsPlainObject$53 = __webpack_exports__Utils.Objects.IsPlainObject;
var AddToDOM = __webpack_exports__DOM.AddToDOM;
var CanvasPool = __webpack_exports__Display.Canvas.CanvasPool;
var GameObject$1 = __webpack_exports__GameObjects.GameObject;
var GetValue$269 = __webpack_exports__Utils.Objects.GetValue;
var RemoveFromDOM$1 = __webpack_exports__DOM.RemoveFromDOM;
var SPLITREGEXP = CONST.SPLITREGEXP;
var UUID$4 = __webpack_exports__Utils.String.UUID;
var DefaultImageNodes = __webpack_exports__Renderer.WebGL.RenderNodes.Defaults.DefaultImageNodes;
var SharedPensPools = null;
var SharedLinesPool = null;
var SharedWrapTextLinesPool = null;
var Text = class extends TextBase {
	constructor(scene, x, y, text, style, type, parser) {
		if (IsPlainObject$53(x)) {
			var config = x;
			x = GetValue$269(config, "x", 0);
			y = GetValue$269(config, "y", 0);
			text = GetValue$269(config, "text", "");
			style = GetValue$269(config, "style");
		}
		if (x === void 0) x = 0;
		if (y === void 0) y = 0;
		super(scene, type);
		this.renderer = scene.sys.game.renderer;
		this.setPosition(x, y);
		this.setOrigin(0, 0);
		this.initRenderNodes(this._defaultRenderNodesMap);
		this.canvas = CanvasPool.create(this);
		this._imageManager = void 0;
		if (style) {
			if (style.hasOwnProperty("align")) {
				var halign = style.align;
				delete style.align;
				style.halign = halign;
			}
			if (style.hasOwnProperty("stroke") && !style.hasOwnProperty("strokeThickness")) style.strokeThickness = 1;
		}
		this.style = new TextStyle$1(this, style);
		var imageData = GetValue$269(style, "images", void 0);
		if (imageData) this.addImage(imageData);
		this.autoRound = true;
		this._text = void 0;
		this.padding = {
			left: 0,
			right: 0,
			top: 0,
			bottom: 0
		};
		this.width = 1;
		this.height = 1;
		this.dirty = false;
		this._scrollY = 0;
		if (this.style.resolution === 0) this.style.resolution = 1;
		this._crop = this.resetCropObject();
		this._textureKey = UUID$4();
		this.texture = scene.sys.textures.addCanvas(this._textureKey, this.canvas);
		this.context = this.texture.context;
		this.frame = this.texture.get();
		this.frame.source.resolution = this.style.resolution;
		if (this.renderer && this.renderer.gl) {
			this.renderer.deleteTexture(this.frame.source.glTexture);
			this.frame.source.glTexture = null;
		}
		var sharedPoolMode = GetValue$269(style, "sharedPool", true);
		var pensPool, linesPool, wrapTextLinesPool;
		if (sharedPoolMode) {
			if (!SharedPensPools) {
				SharedPensPools = {};
				SharedLinesPool = new pool_default();
				SharedWrapTextLinesPool = new WrapTextLinesPool();
				this.scene.game.events.once("destroy", function() {
					SharedPensPools = null;
					SharedLinesPool = null;
					SharedWrapTextLinesPool = null;
				});
			}
			if (!SharedPensPools.hasOwnProperty(type)) SharedPensPools[type] = new pool_default();
			pensPool = SharedPensPools[type];
			linesPool = SharedLinesPool;
			wrapTextLinesPool = SharedWrapTextLinesPool;
		} else {
			pensPool = new pool_default();
			linesPool = new pool_default();
			wrapTextLinesPool = new WrapTextLinesPool();
		}
		this.canvasText = new CanvasText({
			parent: this,
			context: this.context,
			parser,
			style: this.style,
			pensPool,
			linesPool,
			wrapTextLinesPool
		});
		this.parser = parser;
		this.initRTL();
		if (style && style.padding) this.setPadding(style.padding);
		this.setText(text);
		this.setUrlTagCursor(GetValue$269(style, "urlTagCursorStyle", "pointer"));
		if (GetValue$269(style, "interactive", false)) this.setInteractive();
	}
	get _defaultRenderNodesMap() {
		return DefaultImageNodes;
	}
	preDestroy() {
		RemoveFromDOM$1(this.canvas);
		this.canvasText.destroy();
		this.canvasText = void 0;
		if (this._imageManager) {
			this._imageManager.destroy();
			this._imageManager = void 0;
		}
		CanvasPool.remove(this.canvas);
		var texture = this.texture;
		if (texture) texture.destroy();
	}
	set text(value) {
		this.setText(value);
	}
	get text() {
		return this._text;
	}
	get topScrollY() {
		return 0;
	}
	get bottomScrollY() {
		var overflow = this.contentHeight - this.viewportHeight;
		return overflow > 0 ? -overflow : 0;
	}
	get scrollY() {
		return this._scrollY;
	}
	set scrollY(value) {
		if (this._scrollY === value) return;
		this._scrollY = value;
		this.updateText(false);
	}
	setScrollY(value, clamp) {
		if (clamp === void 0) clamp = false;
		if (clamp) value = Clamp(value, this.bottomScrollY, this.topScrollY);
		this.scrollY = value;
		return this;
	}
	addScrollY(inc, clamp) {
		return this.setScrollY(this.scrollY + inc, clamp);
	}
	get t() {
		var bottom = this.bottomScrollY;
		if (bottom === 0) return 0;
		return this._scrollY / bottom;
	}
	set t(value) {
		this.setT(value);
	}
	setT(value, clamp) {
		if (clamp === void 0) clamp = false;
		if (clamp) value = Clamp(value, 0, 1);
		var scrollY = this.bottomScrollY * value;
		return this.setScrollY(scrollY, false);
	}
	addT(inc, clamp) {
		return this.setT(this.t + inc, clamp);
	}
	scrollToTop() {
		return this.setScrollY(this.topScrollY);
	}
	scrollToBottom() {
		return this.setScrollY(this.bottomScrollY);
	}
	initRTL() {
		if (!this.style.rtl) return;
		this.canvas.dir = "rtl";
		this.context.direction = "rtl";
		this.canvas.style.display = "none";
		AddToDOM(this.canvas, this.scene.sys.canvas);
		this.originX = 1;
	}
	setRTL(rtl) {
		if (rtl === void 0) rtl = true;
		var style = this.style;
		if (style.rtl === rtl) return this;
		style.rtl = rtl;
		if (rtl) {
			this.canvas.dir = "rtl";
			this.context.direction = "rtl";
			this.canvas.style.display = "none";
			AddToDOM(this.canvas, this.scene.sys.canvas);
		} else {
			this.canvas.dir = "ltr";
			this.context.direction = "ltr";
		}
		if (style.halign === "left") style.halign = "right";
		else if (style.halign === "right") style.halign = "left";
		if (this._imageManager) {
			var images = this._imageManager.images;
			for (var key in images) images[key].originX = 1 - images[key].originX;
		}
		return this;
	}
	setText(value) {
		value = GetString(value);
		if (value === this._text) return this;
		this._text = value;
		this.updateText();
		return this;
	}
	setPadding(left, top, right, bottom) {
		if (typeof left === "object") {
			var config = left;
			var x = GetValue$269(config, "x", null);
			if (x !== null) {
				left = x;
				right = x;
			} else {
				left = GetValue$269(config, "left", 0);
				right = GetValue$269(config, "right", left);
			}
			var y = GetValue$269(config, "y", null);
			if (y !== null) {
				top = y;
				bottom = y;
			} else {
				top = GetValue$269(config, "top", 0);
				bottom = GetValue$269(config, "bottom", top);
			}
		} else {
			if (left === void 0) left = 0;
			if (top === void 0) top = left;
			if (right === void 0) right = left;
			if (bottom === void 0) bottom = top;
		}
		this.padding.left = left;
		this.padding.top = top;
		this.padding.right = right;
		this.padding.bottom = bottom;
		return this.updateText(false);
	}
	updateText(runWrap) {
		if (runWrap === void 0) runWrap = true;
		var canvasText = this.canvasText;
		var style = this.style;
		if (runWrap) canvasText.updatePenManager(this._text, style.wrapMode, style.wrapWidth, style.lineHeight);
		var padding = this.padding;
		var textWidth, textHeight;
		var linesWidth = Math.ceil(canvasText.linesWidth);
		if (style.fixedWidth === 0) {
			this.width = linesWidth + padding.left + padding.right;
			textWidth = linesWidth;
		} else {
			this.width = style.fixedWidth;
			textWidth = this.width - padding.left - padding.right;
			if (textWidth < linesWidth) textWidth = linesWidth;
		}
		if (style.fixedHeight === 0) {
			this.height = canvasText.linesHeight + padding.top + padding.bottom;
			textHeight = canvasText.linesHeight;
		} else {
			this.height = style.fixedHeight;
			textHeight = this.height - padding.top - padding.bottom;
			if (textHeight < canvasText.linesHeight) textHeight = canvasText.linesHeight;
		}
		var w = this.width;
		var h = this.height;
		this.updateDisplayOrigin();
		var resolution = style.resolution;
		w *= resolution;
		h *= resolution;
		w = Math.max(Math.ceil(w), 1);
		h = Math.max(Math.ceil(h), 1);
		var canvas = this.canvas;
		var context = this.context;
		if (canvas.width !== w || canvas.height !== h) {
			canvas.width = w;
			canvas.height = h;
			this.frame.setSize(w, h);
			this.frame.source.updateSize(w, h);
			this.frame.updateUVs();
		} else context.clearRect(0, 0, w, h);
		context.save();
		context.scale(resolution, resolution);
		var startX = !this.style.rtl ? padding.left : padding.right;
		var startY = padding.top + this._scrollY;
		canvasText.draw(startX, startY, textWidth, textHeight);
		context.restore();
		if (this.renderer && this.renderer.gl) {
			this.frame.source.glTexture = this.renderer.canvasToTexture(canvas, this.frame.source.glTexture, true);
			if (typeof WEBGL_DEBUG) this.frame.glTexture.spectorMetadata = { textureKey: "BBCodeText Game Object" };
		}
		this.dirty = true;
		var input = this.input;
		if (input && !input.customHitArea) {
			input.hitArea.width = this.width;
			input.hitArea.height = this.height;
		}
		return this;
	}
	toJSON() {
		var out = Components.ToJSON(this);
		out.data = {
			autoRound: this.autoRound,
			text: this._text,
			style: this.style.toJSON(),
			resolution: this.resolution,
			padding: {
				left: this.padding.left,
				right: this.padding.right,
				top: this.padding.top,
				bottom: this.padding.bottom
			}
		};
		return out;
	}
	setInteractive(hitArea, hitAreaCallback, dropZone) {
		var isInteractived = !!this.input;
		GameObject$1.prototype.setInteractive.call(this, hitArea, hitAreaCallback, dropZone);
		if (!isInteractived) this.canvasText.setInteractive();
		return this;
	}
	setUrlTagCursor(cursorStyle) {
		this.urlTagCursor = cursorStyle;
		return this;
	}
	get urlTagCursor() {
		return this.canvasText.urlTagCursorStyle;
	}
	set urlTagCursor(value) {
		this.canvasText.hitAreaCursorStyle.setURL(value);
	}
	setHitAreaCursor(key, cursorStyle) {
		this.canvasText.hitAreaCursorStyle.setTag(key, cursorStyle);
		return this;
	}
	setDefaultHitAreaCursor(cursorStyle) {
		this.canvasText.hitAreaCursorStyle.setDefault(cursorStyle);
		return this;
	}
	getWrappedText(text, start, end) {
		if (typeof text === "number") {
			end = start;
			start = text;
			text = void 0;
		}
		text = this.canvasText.getText(text, start, end, true);
		return text.split(SPLITREGEXP);
	}
	getPlainText(text, start, end) {
		if (typeof text === "number") {
			end = start;
			start = text;
			text = void 0;
		}
		return this.canvasText.getPlainText(text, start, end);
	}
	getText(text, start, end, wrap) {
		if (typeof text === "number") {
			wrap = end;
			end = start;
			start = text;
			text = void 0;
		}
		if (wrap === void 0) wrap = false;
		return this.canvasText.getText(text, start, end, wrap);
	}
	getSubString(text, start, end) {
		if (typeof text === "number") {
			end = start;
			start = text;
			text = void 0;
		}
		return this.getText(text, start, end);
	}
	copyPenManager(penManager) {
		return this.canvasText.copyPenManager(penManager);
	}
	getPenManager(text, penManager) {
		return this.canvasText.getPenManager(text, penManager);
	}
	setSize(width, height) {
		return this.setFixedSize(width, height);
	}
	resize(width, height) {
		return this.setFixedSize(width, height);
	}
	get imageManager() {
		if (!this._imageManager) this._imageManager = new ImageManager(this.scene);
		return this._imageManager;
	}
	addImage(key, config) {
		this.imageManager.add(key, config);
		return this;
	}
	drawAreaBounds(graphics, color) {
		this.canvasText.hitAreaManager.drawBounds(graphics, color, this);
		return this;
	}
	generateTexture(key, x, y, width, height) {
		var srcCanvas = this.canvas;
		if (width === void 0) width = srcCanvas.width;
		else width *= this.resolution;
		if (height === void 0) height = srcCanvas.height;
		else height *= this.resolution;
		CopyCanvasToTexture(this.scene, srcCanvas, key, x, y, width, height);
		return this;
	}
	getHitArea(worldX, worldY, camera) {
		return this.canvasText.getHitArea(worldX, worldY, camera);
	}
	get viewportHeight() {
		var padding = this.padding;
		var height = this.height - padding.top - padding.bottom;
		return Math.max(height, 0);
	}
	get contentHeight() {
		return this.canvasText.linesHeight;
	}
};
var methods$40 = { appendText: AppendText$1 };
Object.assign(Text.prototype, methods$40);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/bbcodetext/parser/SplitText.js
var SplitText = function(text, mode) {
	var TagRegex = this.tagRegex;
	var result = [];
	var charIdx = 0;
	var rawMode = false, escMode = false;
	while (true) {
		var regexResult = TagRegex.RE_SPLITTEXT.exec(text);
		if (!regexResult) break;
		var match = regexResult[0];
		if (escMode) {
			if (TagRegex.RE_ESC_CLOSE.test(match)) escMode = false;
			else continue;
		} else if (rawMode) {
			if (TagRegex.RE_RAW_CLOSE.test(match)) rawMode = false;
			else continue;
		} else if (TagRegex.RE_ESC_OPEN.test(match)) escMode = true;
		else if (TagRegex.RE_RAW_OPEN.test(match)) rawMode = true;
		var matchEnd = TagRegex.RE_SPLITTEXT.lastIndex;
		var matchStart = matchEnd - match.length;
		if (charIdx < matchStart) {
			var content = text.substring(charIdx, matchStart);
			result.push(content);
		}
		if (mode === void 0) result.push(match);
		charIdx = matchEnd;
	}
	var totalLen = text.length;
	if (charIdx < totalLen) result.push(text.substring(charIdx, totalLen));
	return result;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/bbcodetext/parser/TagTextToProp.js
var PROP_REMOVE = false;
var PROP_ADD = true;
var GETPROP_RESULT$1 = {
	plainText: null,
	prevProp: null
};
var TagTextToProp = function(text, prevProp) {
	var TagRegex = this.tagRegex;
	if (prevProp == null) prevProp = {};
	var plainText = "";
	if (prevProp.img) UpdateProp(prevProp, PROP_REMOVE, "img");
	if (prevProp.esc) {
		if (TagRegex.RE_ESC_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "esc");
		else plainText = text;
	} else if (prevProp.raw) {
		if (TagRegex.RE_RAW_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "raw");
		else plainText = text;
	} else if (TagRegex.RE_ESC_OPEN.test(text)) UpdateProp(prevProp, PROP_ADD, "esc", true);
	else if (TagRegex.RE_ESC_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "esc");
	else if (TagRegex.RE_RAW_OPEN.test(text)) UpdateProp(prevProp, PROP_ADD, "raw", true);
	else if (TagRegex.RE_RAW_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "raw");
	else if (TagRegex.RE_BLOD_OPEN.test(text)) UpdateProp(prevProp, PROP_ADD, "b", true);
	else if (TagRegex.RE_BLOD_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "b");
	else if (TagRegex.RE_ITALICS_OPEN.test(text)) UpdateProp(prevProp, PROP_ADD, "i", true);
	else if (TagRegex.RE_ITALICS_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "i");
	else if (TagRegex.RE_WEIGHT_OPEN.test(text)) {
		var innerMatch = text.match(TagRegex.RE_WEIGHT_OPEN);
		UpdateProp(prevProp, PROP_ADD, "weight", innerMatch[1]);
	} else if (TagRegex.RE_WEIGHT_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "weight");
	else if (TagRegex.RE_SIZE_OPEN.test(text)) {
		var innerMatch = text.match(TagRegex.RE_SIZE_OPEN);
		UpdateProp(prevProp, PROP_ADD, "size", `${innerMatch[1]}px`);
	} else if (TagRegex.RE_SIZE_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "size");
	else if (TagRegex.RE_FAMILY_OPEN.test(text)) {
		var innerMatch = text.match(TagRegex.RE_FAMILY_OPEN);
		UpdateProp(prevProp, PROP_ADD, "family", innerMatch[1]);
	} else if (TagRegex.RE_FAMILY_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "family");
	else if (TagRegex.RE_COLOR_OPEN.test(text)) {
		var innerMatch = text.match(TagRegex.RE_COLOR_OPEN);
		UpdateProp(prevProp, PROP_ADD, "color", innerMatch[1]);
	} else if (TagRegex.RE_COLOR_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "color");
	else if (TagRegex.RE_UNDERLINE_OPEN.test(text)) UpdateProp(prevProp, PROP_ADD, "u", true);
	else if (TagRegex.RE_UNDERLINE_OPENC.test(text)) {
		var innerMatch = text.match(TagRegex.RE_UNDERLINE_OPENC);
		UpdateProp(prevProp, PROP_ADD, "u", innerMatch[1]);
	} else if (TagRegex.RE_UNDERLINE_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "u");
	else if (TagRegex.RE_STRIKETHROUGH_OPEN.test(text)) UpdateProp(prevProp, PROP_ADD, "s", true);
	else if (TagRegex.RE_STRIKETHROUGH_OPENC.test(text)) {
		var innerMatch = text.match(TagRegex.RE_STRIKETHROUGH_OPENC);
		UpdateProp(prevProp, PROP_ADD, "s", innerMatch[1]);
	} else if (TagRegex.RE_STRIKETHROUGH_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "s");
	else if (TagRegex.RE_SHADOW_OPEN.test(text)) UpdateProp(prevProp, PROP_ADD, "shadow", true);
	else if (TagRegex.RE_SHADOW_OPENC.test(text)) {
		var innerMatch = text.match(TagRegex.RE_SHADOW_OPENC);
		UpdateProp(prevProp, PROP_ADD, "shadow", innerMatch[1]);
	} else if (TagRegex.RE_SHADOW_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "shadow");
	else if (TagRegex.RE_STROKE_OPEN.test(text)) UpdateProp(prevProp, PROP_ADD, "stroke", true);
	else if (TagRegex.RE_STROKE_OPENC.test(text)) {
		var innerMatch = text.match(TagRegex.RE_STROKE_OPENC);
		UpdateProp(prevProp, PROP_ADD, "stroke", innerMatch[1]);
	} else if (TagRegex.RE_STROKE_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "stroke");
	else if (TagRegex.RE_BGCOLOR_OPEN.test(text)) {
		var innerMatch = text.match(TagRegex.RE_BGCOLOR_OPEN);
		UpdateProp(prevProp, PROP_ADD, "bgcolor", innerMatch[1]);
	} else if (TagRegex.RE_BGCOLOR_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "bgcolor");
	else if (TagRegex.RE_OFFSETY_OPEN.test(text)) {
		var innerMatch = text.match(TagRegex.RE_OFFSETY_OPEN);
		UpdateProp(prevProp, PROP_ADD, "y", parseFloat(innerMatch[1]));
	} else if (TagRegex.RE_OFFSETY_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "y");
	else if (TagRegex.RE_SPACING_OPEN.test(text)) {
		var innerMatch = text.match(TagRegex.RE_SPACING_OPEN);
		UpdateProp(prevProp, PROP_ADD, "spacing", parseFloat(innerMatch[1]));
	} else if (TagRegex.RE_SPACING_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "spacing");
	else if (TagRegex.RE_IMAGE_OPEN.test(text)) {
		var innerMatch = text.match(TagRegex.RE_IMAGE_OPEN);
		UpdateProp(prevProp, PROP_ADD, "img", innerMatch[1]);
	} else if (TagRegex.RE_IMAGE_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "img");
	else if (TagRegex.RE_AREA_OPEN.test(text)) {
		var innerMatch = text.match(TagRegex.RE_AREA_OPEN);
		UpdateProp(prevProp, PROP_ADD, "area", innerMatch[1]);
	} else if (TagRegex.RE_AREA_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "area");
	else if (TagRegex.RE_URL_OPEN.test(text)) {
		var innerMatch = text.match(TagRegex.RE_URL_OPEN);
		UpdateProp(prevProp, PROP_ADD, "url", innerMatch[1]);
	} else if (TagRegex.RE_URL_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "url");
	else if (TagRegex.RE_ALIGN_OPEN.test(text)) {
		var innerMatch = text.match(TagRegex.RE_ALIGN_OPEN);
		UpdateProp(prevProp, PROP_ADD, "align", innerMatch[1]);
	} else if (TagRegex.RE_ALIGN_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "align");
	else if (TagRegex.RE_ID_OPEN.test(text)) {
		var innerMatch = text.match(TagRegex.RE_ID_OPEN);
		UpdateProp(prevProp, PROP_ADD, "id", innerMatch[1]);
	} else if (TagRegex.RE_ID_CLOSE.test(text)) UpdateProp(prevProp, PROP_REMOVE, "id");
	else plainText = text;
	var result = GETPROP_RESULT$1;
	result.plainText = plainText;
	result.prop = prevProp;
	return result;
};
var UpdateProp = function(prop, op, key, value) {
	if (op === PROP_ADD) prop[key] = value;
	else if (prop.hasOwnProperty(key)) delete prop[key];
	return prop;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/bbcodetext/parser/PropToContextStyle.js
var PropToContextStyle = function(defaultStyle, prop) {
	var result = STYLE_RESULT$1;
	if (!prop.hasOwnProperty("img")) {
		result.image = null;
		if (prop.hasOwnProperty("family")) result.fontFamily = prop.family;
		else result.fontFamily = defaultStyle.fontFamily;
		if (prop.hasOwnProperty("size")) {
			var size = prop.size;
			if (typeof size === "number") size = `${size}px`;
			result.fontSize = size;
		} else result.fontSize = defaultStyle.fontSize;
		result.fontStyle = GetFontStyle(prop);
		if (prop.hasOwnProperty("color")) result.color = prop.color;
		else result.color = defaultStyle.color;
		if (prop.hasOwnProperty("stroke")) {
			if (prop.stroke === true) {
				result.stroke = defaultStyle.stroke;
				result.strokeThickness = defaultStyle.strokeThickness;
			} else {
				result.stroke = prop.stroke;
				result.strokeThickness = defaultStyle.strokeThickness;
			}
		} else {
			result.stroke = defaultStyle.stroke;
			result.strokeThickness = 0;
		}
	} else result.image = prop.img;
	if (prop.hasOwnProperty("shadow")) {
		if (prop.shadow === true) {
			result.shadowColor = defaultStyle.shadowColor;
			result.shadowOffsetX = defaultStyle.shadowOffsetX;
			result.shadowOffsetY = defaultStyle.shadowOffsetY;
			result.shadowBlur = defaultStyle.shadowBlur;
			result.shadowStroke = true;
			result.shadowFill = true;
		} else {
			result.shadowColor = prop.shadow;
			result.shadowOffsetX = defaultStyle.shadowOffsetX;
			result.shadowOffsetY = defaultStyle.shadowOffsetY;
			result.shadowBlur = defaultStyle.shadowBlur;
			result.shadowStroke = true;
			result.shadowFill = true;
		}
	} else {
		result.shadowColor = "#000";
		result.shadowOffsetX = 0;
		result.shadowOffsetY = 0;
		result.shadowBlur = 0;
		result.shadowStroke = false;
		result.shadowFill = false;
	}
	if (prop.hasOwnProperty("u")) {
		if (prop.u === true) {
			result.underlineColor = defaultStyle.underlineColor;
			result.underlineThickness = defaultStyle.underlineThickness;
			result.underlineOffset = defaultStyle.underlineOffset;
		} else {
			result.underlineColor = prop.u;
			result.underlineThickness = defaultStyle.underlineThickness;
			result.underlineOffset = defaultStyle.underlineOffset;
		}
	} else {
		result.underlineColor = "#000";
		result.underlineThickness = 0;
		result.underlineOffset = 0;
	}
	if (prop.hasOwnProperty("s")) {
		if (prop.s === true) {
			result.strikethroughColor = defaultStyle.strikethroughColor;
			result.strikethroughThickness = defaultStyle.strikethroughThickness;
			result.strikethroughOffset = defaultStyle.strikethroughOffset;
		} else {
			result.strikethroughColor = prop.s;
			result.strikethroughThickness = defaultStyle.strikethroughThickness;
			result.strikethroughOffset = defaultStyle.strikethroughOffset;
		}
	} else {
		result.strikethroughColor = "#000";
		result.strikethroughThickness = 0;
		result.strikethroughOffset = 0;
	}
	if (prop.hasOwnProperty("bgcolor")) result.bgcolor = prop.bgcolor;
	else result.bgcolor = null;
	if (prop.hasOwnProperty("spacing")) result.letterSpacing = prop.spacing;
	else result.letterSpacing = defaultStyle.letterSpacing;
	return result;
};
var GetFontStyle = function(prop) {
	var isBold = prop.b;
	var weight = prop.weight;
	var isItalic = prop.i;
	if (isBold || weight || isItalic) {
		if (isItalic) {
			if (isBold) return "bold italic";
			else if (weight) return `${weight} italic`;
			else return "italic";
		} else if (isBold) return "bold";
		else return weight.toString();
	} else return "";
};
var STYLE_RESULT$1 = new TextStyle$1();
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/bbcodetext/parser/PropToTagText.js
var PropToTagText = function(text, prop, prevProp) {
	if (prevProp == null) prevProp = EMPTYPROP;
	var delimiterLeft = this.delimiters[0];
	var delimiterRight = this.delimiters[1];
	var headers = [];
	for (var k in prevProp) if (!prop.hasOwnProperty(k)) headers.push(`${delimiterLeft}/${k}${delimiterRight}`);
	for (var k in prop) {
		var value = prop[k];
		if (k === "img") {} else if (prevProp[k] === value) continue;
		switch (k) {
			case "size":
				headers.push(`${delimiterLeft}size=${value.replace("px", "")}${delimiterRight}`);
				break;
			case "color":
			case "weight":
			case "family":
			case "stroke":
			case "bgcolor":
			case "y":
			case "spacing":
			case "img":
			case "area":
			case "url":
			case "align":
			case "id":
				headers.push(`${delimiterLeft}${k}=${value}${delimiterRight}`);
				break;
			case "u":
			case "s":
				if (value === true) headers.push(`${delimiterLeft}${k}${delimiterRight}`);
				else headers.push(`${delimiterLeft}${k}=${value}${delimiterRight}`);
				break;
			default: headers.push(`${delimiterLeft}${k}${delimiterRight}`);
		}
	}
	headers.push(text);
	return headers.join("");
};
var EMPTYPROP = {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/string/EscapeRegex.js
var EscapeRegex = function(s) {
	return s.replace(re0, "\\$&").replace(re1, "\\x2d");
};
var re0 = /[|\\{}()[\]^$+*?.]/g;
var re1 = /-/g;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/bbcodetext/parser/TagRegex.js
var DelimiterLeftSave;
var DelimiterRightSave;
var TagRegexSave = {};
var GetOpenTagRegString = function(delimiterLeft, delimiterRight, tagName, param) {
	if (param === void 0) return `${delimiterLeft}${tagName}${delimiterRight}`;
	else return `${delimiterLeft}${tagName}=(${param})${delimiterRight}`;
};
var GetCloseTagRegString = function(delimiterLeft, delimiterRight, tagName) {
	return `${delimiterLeft}\/${tagName}${delimiterRight}`;
};
var GenerateStringRegEx = function(delimiterRight) {
	return `[^${delimiterRight}]+`;
};
var NUMBER_PARAM = "[-.0-9]+";
var COLOR_PARAM = [
	"[a-zA-Z]+",
	"#[0-9abcdef]+",
	"rgba?\\(s*[.0-9]+s*(,s*[.0-9]+s*){2,3}\\)"
].join("|");
var SetDelimiters = function(delimiterLeft, delimiterRight) {
	if (delimiterRight === void 0) {
		var delimeters = delimiterLeft;
		delimiterLeft = delimeters[0];
		delimiterRight = delimeters[1];
	}
	if (DelimiterLeftSave === delimiterLeft && DelimiterRightSave === delimiterRight) return false;
	DelimiterLeftSave = delimiterLeft;
	DelimiterRightSave = delimiterRight;
	delimiterLeft = EscapeRegex(delimiterLeft);
	delimiterRight = EscapeRegex(delimiterRight);
	var STR_PARAM = GenerateStringRegEx(delimiterRight);
	var ESC = "esc";
	var ESC_OPEN = GetOpenTagRegString(delimiterLeft, delimiterRight, ESC);
	var ESC_CLOSE = GetCloseTagRegString(delimiterLeft, delimiterRight, ESC);
	var RAW = "raw";
	var RAW_OPEN = GetOpenTagRegString(delimiterLeft, delimiterRight, RAW);
	var RAW_CLOSE = GetCloseTagRegString(delimiterLeft, delimiterRight, RAW);
	var BLOD = "b";
	var BLOD_OPEN = GetOpenTagRegString(delimiterLeft, delimiterRight, BLOD);
	var BLOD_CLOSE = GetCloseTagRegString(delimiterLeft, delimiterRight, BLOD);
	var ITALICS = "i";
	var ITALICS_OPEN = GetOpenTagRegString(delimiterLeft, delimiterRight, ITALICS);
	var ITALICS_CLOSE = GetCloseTagRegString(delimiterLeft, delimiterRight, ITALICS);
	var WEIGHT = "weight";
	var WEIGHT_OPEN = GetOpenTagRegString(delimiterLeft, delimiterRight, WEIGHT, NUMBER_PARAM);
	var WEIGHT_CLOSE = GetCloseTagRegString(delimiterLeft, delimiterRight, WEIGHT);
	var SIZE = "size";
	var SIZE_OPEN = GetOpenTagRegString(delimiterLeft, delimiterRight, SIZE, NUMBER_PARAM);
	var SIZE_CLOSE = GetCloseTagRegString(delimiterLeft, delimiterRight, SIZE);
	var FAMILY = "family";
	var FAMILY_OPEN = GetOpenTagRegString(delimiterLeft, delimiterRight, FAMILY, STR_PARAM);
	var FAMILY_CLOSE = GetCloseTagRegString(delimiterLeft, delimiterRight, FAMILY);
	var COLOR = "color";
	var COLOR_OPEN = GetOpenTagRegString(delimiterLeft, delimiterRight, COLOR, COLOR_PARAM);
	var COLOR_CLOSE = GetCloseTagRegString(delimiterLeft, delimiterRight, COLOR);
	var UNDERLINE = "u";
	var UNDERLINE_OPEN = GetOpenTagRegString(delimiterLeft, delimiterRight, UNDERLINE);
	var UNDERLINE_OPENC = GetOpenTagRegString(delimiterLeft, delimiterRight, UNDERLINE, COLOR_PARAM);
	var UNDERLINE_CLOSE = GetCloseTagRegString(delimiterLeft, delimiterRight, UNDERLINE);
	var STRIKETHROUGH = "s";
	var STRIKETHROUGH_OPEN = GetOpenTagRegString(delimiterLeft, delimiterRight, STRIKETHROUGH);
	var STRIKETHROUGH_OPENC = GetOpenTagRegString(delimiterLeft, delimiterRight, STRIKETHROUGH, COLOR_PARAM);
	var STRIKETHROUGH_CLOSE = GetCloseTagRegString(delimiterLeft, delimiterRight, STRIKETHROUGH);
	var SHADOW = "shadow";
	var SHADOW_OPEN = GetOpenTagRegString(delimiterLeft, delimiterRight, SHADOW);
	var SHADOW_OPENC = GetOpenTagRegString(delimiterLeft, delimiterRight, SHADOW, COLOR_PARAM);
	var SHADOW_CLOSE = GetCloseTagRegString(delimiterLeft, delimiterRight, SHADOW);
	var STROKE = "stroke";
	var STROKE_OPEN = GetOpenTagRegString(delimiterLeft, delimiterRight, STROKE);
	var STROKE_OPENC = GetOpenTagRegString(delimiterLeft, delimiterRight, STROKE, COLOR_PARAM);
	var STROKE_CLOSE = GetCloseTagRegString(delimiterLeft, delimiterRight, STROKE);
	var BGCOLOR = "bgcolor";
	var BGCOLOR_OPEN = GetOpenTagRegString(delimiterLeft, delimiterRight, BGCOLOR, COLOR_PARAM);
	var BGCOLOR_CLOSE = GetCloseTagRegString(delimiterLeft, delimiterRight, BGCOLOR);
	var OFFSETY = "y";
	var OFFSETY_OPEN = GetOpenTagRegString(delimiterLeft, delimiterRight, OFFSETY, NUMBER_PARAM);
	var OFFSETY_CLOSE = GetCloseTagRegString(delimiterLeft, delimiterRight, OFFSETY);
	var SPACING = "spacing";
	var SPACING_OPEN = GetOpenTagRegString(delimiterLeft, delimiterRight, SPACING, NUMBER_PARAM);
	var SPACING_CLOSE = GetCloseTagRegString(delimiterLeft, delimiterRight, SPACING);
	var IMAGE = "img";
	var IMAGE_OPEN = GetOpenTagRegString(delimiterLeft, delimiterRight, IMAGE, STR_PARAM);
	var IMAGE_CLOSE = GetCloseTagRegString(delimiterLeft, delimiterRight, IMAGE);
	var AREA = "area";
	var AREA_OPEN = GetOpenTagRegString(delimiterLeft, delimiterRight, AREA, STR_PARAM);
	var AREA_CLOSE = GetCloseTagRegString(delimiterLeft, delimiterRight, AREA);
	var URL = "url";
	var URL_OPEN = GetOpenTagRegString(delimiterLeft, delimiterRight, URL, STR_PARAM);
	var URL_CLOSE = GetCloseTagRegString(delimiterLeft, delimiterRight, URL);
	var ALIGN = "align";
	var ALIGN_OPEN = GetOpenTagRegString(delimiterLeft, delimiterRight, ALIGN, STR_PARAM);
	var ALIGN_CLOSE = GetCloseTagRegString(delimiterLeft, delimiterRight, ALIGN);
	var ID = "id";
	var ID_OPEN = GetOpenTagRegString(delimiterLeft, delimiterRight, ID, STR_PARAM);
	var ID_CLOSE = GetCloseTagRegString(delimiterLeft, delimiterRight, ID);
	TagRegexSave.RE_ESC_OPEN = new RegExp(ESC_OPEN, "i");
	TagRegexSave.RE_ESC_CLOSE = new RegExp(ESC_CLOSE, "i");
	TagRegexSave.RE_RAW_OPEN = new RegExp(RAW_OPEN, "i");
	TagRegexSave.RE_RAW_CLOSE = new RegExp(RAW_CLOSE, "i");
	TagRegexSave.RE_BLOD_OPEN = new RegExp(BLOD_OPEN, "i");
	TagRegexSave.RE_BLOD_CLOSE = new RegExp(BLOD_CLOSE, "i");
	TagRegexSave.RE_ITALICS_OPEN = new RegExp(ITALICS_OPEN, "i");
	TagRegexSave.RE_ITALICS_CLOSE = new RegExp(ITALICS_CLOSE, "i");
	TagRegexSave.RE_WEIGHT_OPEN = new RegExp(WEIGHT_OPEN, "i");
	TagRegexSave.RE_WEIGHT_CLOSE = new RegExp(WEIGHT_CLOSE, "i");
	TagRegexSave.RE_SIZE_OPEN = new RegExp(SIZE_OPEN, "i");
	TagRegexSave.RE_SIZE_CLOSE = new RegExp(SIZE_CLOSE, "i");
	TagRegexSave.RE_FAMILY_OPEN = new RegExp(FAMILY_OPEN, "i");
	TagRegexSave.RE_FAMILY_CLOSE = new RegExp(FAMILY_CLOSE, "i");
	TagRegexSave.RE_COLOR_OPEN = new RegExp(COLOR_OPEN, "i");
	TagRegexSave.RE_COLOR_CLOSE = new RegExp(COLOR_CLOSE, "i");
	TagRegexSave.RE_UNDERLINE_OPEN = new RegExp(UNDERLINE_OPEN, "i");
	TagRegexSave.RE_UNDERLINE_OPENC = new RegExp(UNDERLINE_OPENC, "i");
	TagRegexSave.RE_UNDERLINE_CLOSE = new RegExp(UNDERLINE_CLOSE, "i");
	TagRegexSave.RE_STRIKETHROUGH_OPEN = new RegExp(STRIKETHROUGH_OPEN, "i");
	TagRegexSave.RE_STRIKETHROUGH_OPENC = new RegExp(STRIKETHROUGH_OPENC, "i");
	TagRegexSave.RE_STRIKETHROUGH_CLOSE = new RegExp(STRIKETHROUGH_CLOSE, "i");
	TagRegexSave.RE_SHADOW_OPEN = new RegExp(SHADOW_OPEN, "i");
	TagRegexSave.RE_SHADOW_OPENC = new RegExp(SHADOW_OPENC, "i");
	TagRegexSave.RE_SHADOW_CLOSE = new RegExp(SHADOW_CLOSE, "i");
	TagRegexSave.RE_STROKE_OPEN = new RegExp(STROKE_OPEN, "i");
	TagRegexSave.RE_STROKE_OPENC = new RegExp(STROKE_OPENC, "i");
	TagRegexSave.RE_STROKE_CLOSE = new RegExp(STROKE_CLOSE, "i");
	TagRegexSave.RE_BGCOLOR_OPEN = new RegExp(BGCOLOR_OPEN, "i");
	TagRegexSave.RE_BGCOLOR_CLOSE = new RegExp(BGCOLOR_CLOSE, "i");
	TagRegexSave.RE_OFFSETY_OPEN = new RegExp(OFFSETY_OPEN, "i");
	TagRegexSave.RE_OFFSETY_CLOSE = new RegExp(OFFSETY_CLOSE, "i");
	TagRegexSave.RE_SPACING_OPEN = new RegExp(SPACING_OPEN, "i");
	TagRegexSave.RE_SPACING_CLOSE = new RegExp(SPACING_CLOSE, "i");
	TagRegexSave.RE_IMAGE_OPEN = new RegExp(IMAGE_OPEN, "i");
	TagRegexSave.RE_IMAGE_CLOSE = new RegExp(IMAGE_CLOSE, "i");
	TagRegexSave.RE_AREA_OPEN = new RegExp(AREA_OPEN, "i");
	TagRegexSave.RE_AREA_CLOSE = new RegExp(AREA_CLOSE, "i");
	TagRegexSave.RE_URL_OPEN = new RegExp(URL_OPEN, "i");
	TagRegexSave.RE_URL_CLOSE = new RegExp(URL_CLOSE, "i");
	TagRegexSave.RE_ALIGN_OPEN = new RegExp(ALIGN_OPEN, "i");
	TagRegexSave.RE_ALIGN_CLOSE = new RegExp(ALIGN_CLOSE, "i");
	TagRegexSave.RE_ID_OPEN = new RegExp(ID_OPEN, "i");
	TagRegexSave.RE_ID_CLOSE = new RegExp(ID_CLOSE, "i");
	TagRegexSave.RE_SPLITTEXT = new RegExp([
		RAW_OPEN,
		RAW_CLOSE,
		ESC_OPEN,
		ESC_CLOSE,
		BLOD_OPEN,
		BLOD_CLOSE,
		ITALICS_OPEN,
		ITALICS_CLOSE,
		WEIGHT_OPEN,
		WEIGHT_CLOSE,
		SIZE_OPEN,
		SIZE_CLOSE,
		FAMILY_OPEN,
		FAMILY_CLOSE,
		COLOR_OPEN,
		COLOR_CLOSE,
		UNDERLINE_OPEN,
		UNDERLINE_OPENC,
		UNDERLINE_CLOSE,
		STRIKETHROUGH_OPEN,
		STRIKETHROUGH_OPENC,
		STRIKETHROUGH_CLOSE,
		SHADOW_OPEN,
		SHADOW_OPENC,
		SHADOW_CLOSE,
		STROKE_OPEN,
		STROKE_OPENC,
		STROKE_CLOSE,
		BGCOLOR_OPEN,
		BGCOLOR_CLOSE,
		OFFSETY_OPEN,
		OFFSETY_CLOSE,
		SPACING_OPEN,
		SPACING_CLOSE,
		IMAGE_OPEN,
		IMAGE_CLOSE,
		AREA_OPEN,
		AREA_CLOSE,
		URL_OPEN,
		URL_CLOSE,
		ALIGN_OPEN,
		ALIGN_CLOSE,
		ID_OPEN,
		ID_CLOSE
	].join("|"), "ig");
	return true;
};
var GetTagRegex = function(delimiterLeft, delimiterRight) {
	if (delimiterLeft !== void 0) SetDelimiters(delimiterLeft, delimiterRight);
	return Object.assign({}, TagRegexSave);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/bbcodetext/parser/Parser.js
var GetValue$268 = __webpack_exports__Utils.Objects.GetValue;
var Parser$2 = class {
	constructor(style) {
		var delimiters = GetValue$268(style, "delimiters", "[]");
		this.tagRegex = GetTagRegex(delimiters);
		this.delimiters = delimiters;
	}
	getStrokeThinkness(defaultStyle, prop) {
		var strokeThickness;
		if (prop.hasOwnProperty("stroke")) strokeThickness = defaultStyle.strokeThickness;
		else strokeThickness = 0;
		return strokeThickness;
	}
	getLetterSpacing(defaultStyle, prop) {
		var letterSpacing;
		if (prop.hasOwnProperty("spacing")) letterSpacing = prop.spacing;
		else letterSpacing = defaultStyle.letterSpacing;
		return letterSpacing;
	}
	setDelimiters(delimiterLeft, delimiterRight) {
		if (SetDelimiters(delimiterLeft, delimiterRight)) this.tagRegex = GetTagRegex();
		return this;
	}
};
var methods$39 = {
	splitText: SplitText,
	tagTextToProp: TagTextToProp,
	propToContextStyle: PropToContextStyle,
	propToTagText: PropToTagText
};
Object.assign(Parser$2.prototype, methods$39);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/bbcodetext/BBCodeText.js
var BBCodeText = class extends Text {
	constructor(scene, x, y, text, style) {
		var parser = new Parser$2(style);
		super(scene, x, y, text, style, "rexBBCodeText", parser);
	}
	setDelimiters(delimiterLeft, delimiterRight) {
		this.parser.setDelimiters(delimiterLeft, delimiterRight);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/bbcodetext/BBCodeText.js
var BBCodeText_default = BBCodeText;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/bbcodetext/Factory.js
ObjectFactory.register("BBCodeText", function(x, y, text, style) {
	var gameObject = new BBCodeText_default(this.scene, x, y, text, style);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.BBCodeText", BBCodeText_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/tagtext/Parser.js
var GETPROP_RESULT = {
	plainText: null,
	prevProp: null
};
var STYLE_RESULT = new TextStyle$1();
var Parser$1 = class {
	constructor(tags) {
		if (tags === void 0) tags = {};
		this.tags = tags;
	}
	addTag(name, prop) {
		this.tags[name] = prop;
	}
	getTag(name) {
		return this.tags[name];
	}
	splitText(text, mode) {
		var result = [];
		var charIdx = 0;
		while (true) {
			var regexResult = RE_SPLITTEXT.exec(text);
			if (!regexResult) break;
			var match = regexResult[0];
			var matchStart = RE_SPLITTEXT.lastIndex - match.length;
			if (charIdx < matchStart) result.push(text.substring(charIdx, matchStart));
			if (mode === void 0) result.push(match);
			else if (mode === 1) {
				if (RE_CLASS_HEADER.test(match)) {
					var innerMatch = match.match(RE_CLASS);
					result.push(innerMatch[2]);
				} else if (RE_STYLE_HEADER.test(match)) {
					var innerMatch = match.match(RE_STYLE);
					result.push(innerMatch[2]);
				}
			}
			charIdx = RE_SPLITTEXT.lastIndex;
		}
		var totalLen = text.length;
		if (charIdx < totalLen) result.push(text.substring(charIdx, totalLen));
		return result;
	}
	tagTextToProp(text, prevProp) {
		var plainText, propOut;
		if (RE_CLASS_HEADER.test(text)) {
			var innerMatch = text.match(RE_CLASS);
			if (innerMatch != null) {
				var name = innerMatch[1];
				var tags = this.tags;
				if (tags.hasOwnProperty(name)) propOut = tags[name];
				else propOut = {};
				propOut._class = name;
				plainText = innerMatch[2];
			}
		} else if (RE_STYLE_HEADER.test(text)) {
			var innerMatch = text.match(RE_STYLE);
			if (innerMatch != null) {
				var style = innerMatch[1];
				propOut = StyleToProp(style);
				propOut._style = style;
				plainText = innerMatch[2];
			}
		}
		if (plainText == null) plainText = text;
		if (propOut == null) propOut = {};
		var result = GETPROP_RESULT;
		result.plainText = plainText;
		result.prop = propOut;
		return result;
	}
	propToContextStyle(defaultStyle, prop) {
		var result = STYLE_RESULT;
		if (!prop.hasOwnProperty("img")) {
			result.image = null;
			if (prop.hasOwnProperty("family") || prop.hasOwnProperty("fontFamily") || prop.hasOwnProperty("font-family")) result.fontFamily = prop.hasOwnProperty("family") ? prop.family : prop.hasOwnProperty("fontFamily") ? prop.fontFamily : prop["font-family"];
			else result.fontFamily = defaultStyle.fontFamily;
			if (prop.hasOwnProperty("size") || prop.hasOwnProperty("fontSize") || prop.hasOwnProperty("font-size")) {
				var size = prop.hasOwnProperty("size") ? prop.size : prop.hasOwnProperty("fontSize") ? prop.fontSize : prop["font-size"];
				if (typeof size === "number") size = `${size}px`;
				result.fontSize = size;
			} else result.fontSize = defaultStyle.fontSize;
			if (prop.hasOwnProperty("style") || prop.hasOwnProperty("fontStyle") || prop.hasOwnProperty("font-style")) result.fontStyle = prop.hasOwnProperty("style") ? prop.style : prop.hasOwnProperty("fontStyle") ? prop.fontStyle : prop["font-style"];
			else result.fontStyle = defaultStyle.fontStyle;
			if (prop.hasOwnProperty("color") || prop.hasOwnProperty("font-color")) result.color = prop.hasOwnProperty("color") ? prop.color : prop["font-color"];
			else result.color = defaultStyle.color;
			if (prop.hasOwnProperty("stroke")) {
				var stroke = prop.stroke;
				result.stroke = stroke.hasOwnProperty("color") ? stroke.color : defaultStyle.stroke;
				result.strokeThickness = stroke.hasOwnProperty("thickness") ? stroke.thickness : defaultStyle.strokeThickness;
			} else {
				result.stroke = defaultStyle.stroke;
				result.strokeThickness = defaultStyle.strokeThickness;
			}
		} else result.image = prop.img;
		if (prop.hasOwnProperty("shadow")) {
			var shadow = prop.shadow;
			result.shadowColor = shadow.hasOwnProperty("color") ? shadow.color : defaultStyle.shadowColor;
			result.shadowOffsetX = shadow.hasOwnProperty("offsetX") ? shadow.offsetX : defaultStyle.shadowOffsetX;
			result.shadowOffsetY = shadow.hasOwnProperty("offsetY") ? shadow.offsetY : defaultStyle.shadowOffsetY;
			result.shadowBlur = shadow.hasOwnProperty("blur") ? shadow.blur : defaultStyle.shadowBlur;
			result.shadowStroke = true;
			result.shadowFill = true;
		} else {
			result.shadowColor = defaultStyle.shadowColor;
			result.shadowOffsetX = defaultStyle.shadowOffsetX;
			result.shadowOffsetY = defaultStyle.shadowOffsetY;
			result.shadowBlur = defaultStyle.shadowBlur;
			result.shadowStroke = defaultStyle.shadowStroke;
			result.shadowFill = defaultStyle.shadowFill;
		}
		if (prop.hasOwnProperty("u") || prop.hasOwnProperty("underline")) {
			var u = prop.hasOwnProperty("u") ? prop.u : prop.underline;
			result.underlineColor = u.hasOwnProperty("color") ? u.color : defaultStyle.underlineColor;
			result.underlineThickness = u.hasOwnProperty("thickness") ? u.thickness : defaultStyle.underlineThickness;
			result.underlineOffset = u.hasOwnProperty("offset") ? u.offset : defaultStyle.underlineOffset;
		} else {
			result.underlineColor = defaultStyle.underlineColor;
			result.underlineThickness = defaultStyle.underlineThickness;
			result.underlineOffset = defaultStyle.underlineOffset;
		}
		if (prop.hasOwnProperty("s") || prop.hasOwnProperty("strikethrough")) {
			var s = prop.hasOwnProperty("s") ? prop.s : prop.strikethrough;
			result.strikethroughColor = s.hasOwnProperty("color") ? s.color : defaultStyle.strikethroughColor;
			result.strikethroughThickness = s.hasOwnProperty("thickness") ? s.thickness : defaultStyle.strikethroughThickness;
			result.strikethroughOffset = s.hasOwnProperty("offset") ? s.offset : defaultStyle.strikethroughOffset;
		} else {
			result.strikethroughColor = defaultStyle.strikethroughColor;
			result.strikethroughThickness = defaultStyle.strikethroughThickness;
			result.strikethroughOffset = defaultStyle.strikethroughOffset;
		}
		if (prop.hasOwnProperty("bgcolor")) result.bgcolor = prop.bgcolor;
		else result.bgcolor = null;
		if (prop.hasOwnProperty("spacing")) result.letterSpacing = prop.spacing;
		else result.letterSpacing = defaultStyle.letterSpacing;
		return result;
	}
	getStrokeThinkness(defaultStyle, prop) {
		var strokeThinkness;
		if (prop.hasOwnProperty("stroke")) {
			var stroke = prop.stroke;
			strokeThinkness = stroke.hasOwnProperty("thickness") ? stroke.thickness : defaultStyle.strokeThickness;
		} else strokeThinkness = defaultStyle.strokeThickness;
		return strokeThinkness;
	}
	getLetterSpacing(defaultStyle, prop) {
		var letterSpacing;
		if (prop.hasOwnProperty("spacing")) letterSpacing = prop.spacing;
		else letterSpacing = defaultStyle.letterSpacing;
		return letterSpacing;
	}
	propToTagText(text, prop, prevProp) {
		if (prop.hasOwnProperty("_class")) {
			if (text === "") {
				if (this.isTextTag(prop._class)) return "";
			}
			return `<class='${prop._class}'>${text}</class>`;
		} else if (prop.hasOwnProperty("_style")) return `<style='${prop._style}'>${text}</style>`;
		else return text;
	}
	destroy() {
		this.tags = void 0;
	}
	isTextTag(tagName) {
		var tag = this.tags[tagName];
		if (tag) return tag.img == null;
		else return false;
	}
};
var StyleToProp = function(s) {
	s = s.split(";");
	var result = {}, prop, k, v;
	for (var i = 0, slen = s.length; i < slen; i++) {
		prop = s[i].split(":");
		k = prop[0], v = prop[1];
		if (isEmpty(k) || isEmpty(v)) continue;
		switch (k) {
			case "stroke":
				var params = v.split(" ");
				var len = params.length;
				v = {};
				if (len >= 1) v.color = params[0];
				if (len >= 2) v.thickness = parseInt(params[1].replace("px", ""));
				break;
			case "shadow":
				var params = v.split(" ");
				var len = params.length;
				v = {};
				if (len >= 1) v.color = params[0];
				if (len >= 2) v.offsetX = parseInt(params[1].replace("px", ""));
				if (len >= 3) v.offsetY = parseInt(params[2].replace("px", ""));
				if (len >= 4) v.blur = parseInt(params[3].replace("px", ""));
				break;
			case "u":
			case "underline":
			case "s":
			case "strikethrough":
				var params = v.split(" ");
				var len = params.length;
				v = {};
				if (len >= 1) v.color = params[0];
				if (len >= 2) v.thickness = parseInt(params[1].replace("px", ""));
				if (len >= 3) v.offset = parseInt(params[2].replace("px", ""));
				if (k === "underline") k = "u";
				else if (k === "strikethrough") k = "s";
				break;
			case "y":
			case "spacing": v = parseFloat(v);
		}
		result[k] = v;
	}
	return result;
};
var isEmpty = function(s) {
	s = s.replace(RE_SPACE, "");
	return s.length === 0;
};
var RE_SPLITTEXT = /<\s*class=["|']([^"|']+)["|']\s*\>([\s\S]*?)<\s*\/class\s*\>|<\s*style=["|']([^"|']+)["|']\s*\>([\s\S]*?)<\s*\/style\s*\>/g;
var RE_CLASS_HEADER = /<\s*class=/i;
var RE_CLASS = /<\s*class=["|']([^"|']+)["|']\s*\>([\s\S]*?)<\s*\/class\s*\>/;
var RE_STYLE_HEADER = /<\s*style=/i;
var RE_STYLE = /<\s*style=["|']([^"|']+)["|']\s*\>([\s\S]*?)<\s*\/style\s*\>/;
var RE_SPACE = /^\s+|\s+$/;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/tagtext/tagtext/TagText.js
var GetValue$267 = __webpack_exports__Utils.Objects.GetValue;
var TagText = class extends Text {
	constructor(scene, x, y, text, style) {
		var parser = new Parser$1(GetValue$267(style, "tags", void 0));
		super(scene, x, y, text, style, "rexTagText", parser);
	}
	addTag(name, prop) {
		this.parser.addTag(name, prop);
		return this.updateText(true);
	}
	addTags(tags) {
		for (var name in tags) this.parser.addTag(name, tags[name]);
		return this.updateText(true);
	}
	getTag(name) {
		return this.parser.getTag(name);
	}
	preDestroy() {
		super.preDestroy();
		this.parser.destroy();
		this.parser = void 0;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tagtext/TagText.js
var TagText_default = TagText;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tagtext/Factory.js
ObjectFactory.register("tagText", function(x, y, text, style) {
	var gameObject = new TagText_default(this.scene, x, y, text, style);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.TagText", TagText_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/padding/PaddingMethods.js
var GetValue$266 = __webpack_exports__Utils.Objects.GetValue;
var GetPadding$1 = function(padding, key) {
	if (key === void 0) return padding;
	return padding[key];
};
var SetPadding$1 = function(padding, key, value) {
	if (padding === void 0) padding = {};
	if (key === void 0) key = 0;
	var keyType = typeof key;
	if (keyType === "string") padding[key] = value;
	else if (keyType === "number") {
		padding.left = key;
		padding.right = key;
		padding.top = key;
		padding.bottom = key;
	} else {
		padding.left = GetValue$266(key, "left", 0);
		padding.right = GetValue$266(key, "right", 0);
		padding.top = GetValue$266(key, "top", 0);
		padding.bottom = GetValue$266(key, "bottom", 0);
	}
	return padding;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/bob/Base.js
var Base$5 = class {
	constructor(parent, type) {
		this.setParent(parent);
		this.type = type;
		this.renderable = false;
		this.reset().setActive();
	}
	destroy() {
		this.parent.removeChild(this);
	}
	setParent(parent) {
		this.parent = parent;
		return this;
	}
	get scene() {
		return this.parent.scene;
	}
	get canvas() {
		return this.parent ? this.parent.canvas : null;
	}
	get context() {
		return this.parent ? this.parent.context : null;
	}
	setDirty(dirty) {
		if (dirty && this.parent) this.parent.dirty = true;
		return this;
	}
	get active() {
		return this._active;
	}
	set active(value) {
		this.setDirty(this._active != value);
		this._active = value;
	}
	setActive(active) {
		if (active === void 0) active = true;
		this.active = active;
		return this;
	}
	modifyPorperties(o) {
		return this;
	}
	onFree() {
		this.reset().setParent();
	}
	reset() {
		return this;
	}
	render() {}
	contains(x, y) {
		return false;
	}
};
Object.assign(Base$5.prototype, DataMethods_default$2);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/bob/renderbase/RenderMethods.js
var RenderMethods_default$1 = {
	renderContent() {},
	render() {
		if (!this.willRender) return this;
		var context = this.context;
		context.save();
		context.globalAlpha = this.alpha;
		if (this.toLocalPosition) {
			var x = this.drawX, y = this.drawY;
			if (this.autoRound) {
				x = Math.round(x);
				y = Math.round(y);
			}
			context.translate(x, y);
			context.scale(this.scaleX, this.scaleY);
			context.rotate(this.rotation);
		}
		if (this.drawBelowCallback) this.drawBelowCallback(this);
		this.renderContent();
		if (this.drawAboveCallback) this.drawAboveCallback(this);
		context.restore();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/utils/transform/CanvasPositionToBobPosition.js
var RotateAround$8 = __webpack_exports__Math.RotateAround;
var CanvasPositionToBobPosition = function(canvasX, canvasY, bob, out) {
	if (out === void 0) out = {};
	else if (out === true) {
		if (globPoint$1 === void 0) globPoint$1 = {};
		out = globPoint$1;
	}
	out.x = (canvasX - bob.drawX) / bob.scaleX;
	out.y = (canvasY - bob.drawY) / bob.scaleY;
	if (bob.rotation !== 0) RotateAround$8(out, 0, 0, -bob.rotation);
	return out;
};
var globPoint$1;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/bob/renderbase/Contains.js
var Rectangle$5 = __webpack_exports__Geom.Rectangle;
var Contains$2 = function(canvasX, canvasY) {
	if (this.width === 0 || this.height === 0) return false;
	var bobPosition = CanvasPositionToBobPosition(canvasX, canvasY, this, true);
	return GetBobBounds(this).contains(bobPosition.x, bobPosition.y);
};
var GetBobBounds = function(bob) {
	if (bobBounds === void 0) bobBounds = new Rectangle$5();
	var x = bob.drawTLX, y = bob.drawTLY;
	bobBounds.setTo(x, y, bob.drawTRX - x, bob.drawBLY - y);
	return bobBounds;
};
var bobBounds;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/utils/transform/BobPositionToCanvasPosition.js
var RotateAround$7 = __webpack_exports__Math.RotateAround;
var BobPositionToCanvasPosition = function(bob, bobX, bobY, out) {
	if (out === void 0) out = {};
	else if (out === true) {
		if (globPoint === void 0) globPoint = {};
		out = globPoint;
	}
	out.x = bobX;
	out.y = bobY;
	if (bob.rotation !== 0) RotateAround$7(out, 0, 0, bob.rotation);
	out.x = out.x * bob.scaleX + bob.drawX;
	out.y = out.y * bob.scaleY + bob.drawY;
	return out;
};
var globPoint;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/position/GameObjectLocalXYToWorldXY.js
var TransformMatrix$2 = __webpack_exports__GameObjects.Components.TransformMatrix;
var GameObjectLocalXYToWorldXY = function(gameObject, localX, localY, out) {
	if (out === void 0) out = {};
	else if (out === true) out = globOut$1;
	var px = localX - gameObject.width * gameObject.originX;
	var py = localY - gameObject.height * gameObject.originY;
	if (tempMatrix === void 0) {
		tempMatrix = new TransformMatrix$2();
		parentMatrix = new TransformMatrix$2();
	}
	if (gameObject.parentContainer) gameObject.getWorldTransformMatrix(tempMatrix, parentMatrix);
	else tempMatrix.applyITRS(gameObject.x, gameObject.y, gameObject.rotation, gameObject.scaleX, gameObject.scaleY);
	tempMatrix.transformPoint(px, py, out);
	return out;
};
var tempMatrix;
var parentMatrix;
var globOut$1 = {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/utils/transform/BobPositionToWorldPosition.js
var BobPositionToWorldPosition = function(dynamicText, bob, bobX, bobY, out) {
	var localXY = BobPositionToCanvasPosition(bob, bobX, bobY, true);
	return GameObjectLocalXYToWorldXY(dynamicText, localXY.x, localXY.y, out);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/utils/transform/GetBobWorldPosition.js
var GetBobWorldPosition = function(dynamicText, bob, offsetX, offsetY, out) {
	if (typeof offsetX !== "number") {
		out = offsetX;
		offsetX = 0;
		offsetY = 0;
	}
	return BobPositionToWorldPosition(dynamicText, bob, bob.drawCenterX + offsetX, bob.drawCenterY + offsetY, out);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/bob/renderbase/GetWorldPosition.js
var GetWorldPosition = function(offsetX, offsetY, out) {
	return GetBobWorldPosition(this.parent, this, offsetX, offsetY, out);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/bob/renderbase/Methods.js
var Methods$23 = {
	contains: Contains$2,
	getWorldPosition: GetWorldPosition
};
Object.assign(Methods$23, RenderMethods_default$1);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/bob/renderbase/RenderBase.js
var DegToRad$14 = __webpack_exports__Math.DegToRad;
var RadToDeg$13 = __webpack_exports__Math.RadToDeg;
var GetValue$265 = __webpack_exports__Utils.Objects.GetValue;
var RenderBase = class extends Base$5 {
	constructor(parent, type) {
		super(parent, type);
		this.renderable = true;
		this.scrollFactorX = 1;
		this.scrollFactorY = 1;
		this.toLocalPosition = true;
		this.originX = 0;
		this.offsetX = 0;
		this.offsetY = 0;
	}
	get visible() {
		return this._visible;
	}
	set visible(value) {
		this.setDirty(this._visible != value);
		this._visible = value;
	}
	setVisible(visible) {
		if (visible === void 0) visible = true;
		this.visible = visible;
		return this;
	}
	get alpha() {
		return this._alpha;
	}
	set alpha(value) {
		this.setDirty(this._alpha != value);
		this._alpha = value;
	}
	setAlpha(alpha) {
		this.alpha = alpha;
		return this;
	}
	get x() {
		return this._x;
	}
	set x(value) {
		this.setDirty(this._x != value);
		this._x = value;
	}
	setX(x) {
		this.x = x;
		return this;
	}
	get y() {
		return this._y;
	}
	set y(value) {
		this.setDirty(this._y != value);
		this._y = value;
	}
	setY(y) {
		this.y = y;
		return this;
	}
	setPosition(x, y) {
		this.x = x;
		this.y = y;
		return this;
	}
	setInitialPosition(x, y) {
		this.x0 = x;
		this.y0 = y;
		return this;
	}
	setScrollFactorX(x) {
		this.scrollFactorX = x;
		return this;
	}
	setScrollFactorY(y) {
		this.scrollFactorY = y;
		return this;
	}
	setScrollFactor(x, y) {
		if (y === void 0) y = x;
		this.scrollFactorX = x;
		this.scrollFactorY = y;
		return this;
	}
	get rotation() {
		return this._rotation;
	}
	set rotation(value) {
		this.setDirty(this._rotation != value);
		this._rotation = value;
	}
	setRotation(rotation) {
		this.rotation = rotation;
		return this;
	}
	get angle() {
		return RadToDeg$13(this._rotation);
	}
	set angle(value) {
		this.rotation = DegToRad$14(value);
	}
	setAngle(angle) {
		this.angle = angle;
		return this;
	}
	get scaleX() {
		return this._scaleX;
	}
	set scaleX(value) {
		this.setDirty(this._scaleX !== value);
		this._scaleX = value;
	}
	setScaleX(scaleX) {
		this.scaleX = scaleX;
		return this;
	}
	get width() {
		return 0;
	}
	set width(value) {}
	setWidth(width, keepAspectRatio) {
		if (keepAspectRatio === void 0) keepAspectRatio = false;
		this.width = width;
		if (keepAspectRatio) this.scaleY = this.scaleX;
		return this;
	}
	get leftSpace() {
		return this._leftSpace;
	}
	set leftSpace(value) {
		this.setDirty(this._leftSpace !== value);
		this._leftSpace = value;
	}
	setLeftSpace(value) {
		this.leftSpace = value;
		return this;
	}
	get rightSpace() {
		return this._rightSpace;
	}
	set rightSpace(value) {
		this.setDirty(this._rightSpace !== value);
		this._rightSpace = value;
	}
	setRightSpace(value) {
		this.rightSpace = value;
		return this;
	}
	get outerWidth() {
		return this.width + this.leftSpace + this.rightSpace;
	}
	get scaleY() {
		return this._scaleY;
	}
	set scaleY(value) {
		this.setDirty(this._scaleY !== value);
		this._scaleY = value;
	}
	setScaleY(scaleY) {
		this.scaleY = scaleY;
		return this;
	}
	get height() {
		return 0;
	}
	set height(value) {}
	setHeight(height, keepAspectRatio) {
		if (keepAspectRatio === void 0) keepAspectRatio = false;
		this.height = height;
		if (keepAspectRatio) this.scaleX = this.scaleY;
		return this;
	}
	setScale(scaleX, scaleY) {
		if (scaleY === void 0) scaleY = scaleX;
		this.scaleX = scaleX;
		this.scaleY = scaleY;
		return this;
	}
	setOrigin(x) {
		this.originX = x;
		return this;
	}
	setAlign(align) {
		this.align = align;
		return this;
	}
	modifyPorperties(o) {
		if (!o) return this;
		if (o.hasOwnProperty("x")) this.setX(o.x);
		if (o.hasOwnProperty("y")) this.setY(o.y);
		if (o.hasOwnProperty("rotation")) this.setRotation(o.rotation);
		else if (o.hasOwnProperty("angle")) this.setAngle(o.angle);
		if (o.hasOwnProperty("alpha")) this.setAlpha(o.alpha);
		var width = GetValue$265(o, "width", void 0);
		var height = GetValue$265(o, "height", void 0);
		var scaleX = GetValue$265(o, "scaleX", void 0);
		var scaleY = GetValue$265(o, "scaleY", void 0);
		if (width !== void 0) {
			if (height === void 0 && scaleY === void 0) this.setWidth(width, true);
			else this.setWidth(width);
		} else if (scaleX !== void 0) this.setScaleX(scaleX);
		if (height !== void 0) {
			if (width === void 0 && scaleX === void 0) this.setHeight(height, true);
			else this.setHeight(height);
		} else if (scaleY !== void 0) this.setScaleY(scaleY);
		if (o.hasOwnProperty("leftSpace")) this.setLeftSpace(o.leftSpace);
		if (o.hasOwnProperty("rightSpace")) this.setRightSpace(o.rightSpace);
		if (o.hasOwnProperty("align")) this.setAlign(o.align);
		return this;
	}
	setDrawBelowCallback(callback) {
		this.drawBelowCallback = callback;
		return this;
	}
	setDrawAboveCallback(callback) {
		this.drawAboveCallback = callback;
		return this;
	}
	reset() {
		this.setVisible().setAlpha(1).setPosition(0, 0).setRotation(0).setScale(1, 1).setLeftSpace(0).setRightSpace(0).setOrigin(0).setAlign().setDrawBelowCallback().setDrawAboveCallback();
		return this;
	}
	get willRender() {
		return this.visible && this.alpha > 0;
	}
	get drawX() {
		var x = this.x + this.leftSpace + this.offsetX - this.originX * this.width;
		return this.parent._textOX * this.scrollFactorX + x;
	}
	get drawY() {
		var y = this.y + this.offsetY;
		return this.parent._textOY * this.scrollFactorY + y;
	}
	get drawTLX() {
		return 0;
	}
	get drawTLY() {
		return 0;
	}
	get drawBLX() {
		return 0;
	}
	get drawBLY() {
		return 0;
	}
	get drawTRX() {
		return 0;
	}
	get drawTRY() {
		return 0;
	}
	get drawBRX() {
		return 0;
	}
	get drawBRY() {
		return 0;
	}
	get drawCenterX() {
		return (this.drawTRX + this.drawTLX) / 2;
	}
	get drawCenterY() {
		return (this.drawBLY + this.drawTLY) / 2;
	}
};
Object.assign(RenderBase.prototype, Methods$23);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/bob/utils/GetProperty.js
var GetProperty = function(name, config, defaultConfig) {
	if (config.hasOwnProperty(name)) return config[name];
	else return defaultConfig[name];
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/bob/background/Background.js
var GetValue$264 = __webpack_exports__Utils.Objects.GetValue;
var Background = class extends RenderBase {
	constructor(parent, config) {
		super(parent, "background");
		this.setScrollFactor(0);
		this.setColor(GetValue$264(config, "color", null), GetValue$264(config, "color2", null), GetValue$264(config, "horizontalGradient", true));
		this.setStroke(GetValue$264(config, "stroke", null), GetValue$264(config, "strokeThickness", 2));
		this.setCornerRadius(GetValue$264(config, "cornerRadius", 0), GetValue$264(config, "cornerIteration", null));
	}
	set color(value) {
		value = GetStyle(value, this.canvas, this.context);
		this.setDirty(this._color != value);
		this._color = value;
	}
	get color() {
		return this._color;
	}
	set color2(value) {
		value = GetStyle(value, this.canvas, this.context);
		this.setDirty(this._color2 != value);
		this._color2 = value;
	}
	get color2() {
		return this._color2;
	}
	set horizontalGradient(value) {
		this.setDirty(this._horizontalGradient != value);
		this._horizontalGradient = value;
	}
	get horizontalGradient() {
		return this._horizontalGradient;
	}
	setColor(color, color2, isHorizontalGradient) {
		if (isHorizontalGradient === void 0) isHorizontalGradient = true;
		this.color = color;
		this.color2 = color2;
		this.horizontalGradient = isHorizontalGradient;
		return this;
	}
	set stroke(value) {
		value = GetStyle(value, this.canvas, this.context);
		this.setDirty(this._stroke != value);
		this._stroke = value;
	}
	get stroke() {
		return this._stroke;
	}
	set strokeThickness(value) {
		this.setDirty(this._strokeThickness != value);
		this._strokeThickness = value;
	}
	get strokeThickness() {
		return this._strokeThickness;
	}
	setStroke(color, lineWidth) {
		if (color != null) {
			if (lineWidth === void 0) lineWidth = 2;
		}
		this.stroke = color;
		this.strokeThickness = lineWidth;
		return this;
	}
	set cornerRadius(value) {
		this.setDirty(this._cornerRadius != value);
		this._cornerRadius = value;
	}
	get cornerRadius() {
		return this._cornerRadius;
	}
	set cornerIteration(value) {
		this.setDirty(this._cornerIteration != value);
		this._cornerIteration = value;
	}
	get cornerIteration() {
		return this._cornerIteration;
	}
	modifyStyle(o) {
		if (o.hasOwnProperty("color")) this.setColor(o.color, GetProperty("color2", o, this), GetProperty("horizontalGradient", o, this));
		if (o.hasOwnProperty("stroke")) this.setStroke(o.stroke, GetProperty("strokeThickness", o, this));
		if (o.hasOwnProperty("cornerRadius")) this.setCornerRadius(o.cornerRadius, GetProperty("cornerIteration", o, this));
		return this;
	}
	modifyPorperties(o) {
		super.modifyPorperties(o);
		this.modifyStyle(o);
		return this;
	}
	setCornerRadius(radius, iteration) {
		this.cornerRadius = radius;
		this.cornerIteration = iteration;
		return this;
	}
	renderContent() {
		DrawRoundRectangleBackground(this.parent, this.color, this.stroke, this.strokeThickness, this.cornerRadius, this.color2, this.horizontalGradient, this.cornerIteration);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/bob/innerbounds/InnerBounds.js
var GetValue$263 = __webpack_exports__Utils.Objects.GetValue;
var InnerBounds = class extends RenderBase {
	constructor(parent, config) {
		super(parent, "innerbounds");
		this.setScrollFactor(0);
		this.setColor(GetValue$263(config, "color", null), GetValue$263(config, "color2", null), GetValue$263(config, "horizontalGradient", true));
		this.setStroke(GetValue$263(config, "stroke", null), GetValue$263(config, "strokeThickness", 2));
	}
	set color(value) {
		value = GetStyle(value, this.canvas, this.context);
		this.setDirty(this._color != value);
		this._color = value;
	}
	get color() {
		return this._color;
	}
	set color2(value) {
		value = GetStyle(value, this.canvas, this.context);
		this.setDirty(this._color2 != value);
		this._color2 = value;
	}
	get color2() {
		return this._color2;
	}
	set horizontalGradient(value) {
		this.setDirty(this._horizontalGradient != value);
		this._horizontalGradient = value;
	}
	get horizontalGradient() {
		return this._horizontalGradient;
	}
	setColor(color, color2, isHorizontalGradient) {
		if (isHorizontalGradient === void 0) isHorizontalGradient = true;
		this.color = color;
		this.color2 = color2;
		this.horizontalGradient = isHorizontalGradient;
		return this;
	}
	set stroke(value) {
		value = GetStyle(value, this.canvas, this.context);
		this.setDirty(this._stroke != value);
		this._stroke = value;
	}
	get stroke() {
		return this._stroke;
	}
	set strokeThickness(value) {
		this.setDirty(this._strokeThickness != value);
		this._strokeThickness = value;
	}
	get strokeThickness() {
		return this._strokeThickness;
	}
	setStroke(color, lineWidth) {
		if (color != null) {
			if (lineWidth === void 0) lineWidth = 2;
		}
		this.stroke = color;
		this.strokeThickness = lineWidth;
		return this;
	}
	modifyPorperties(o) {
		super.modifyPorperties(o);
		if (o.hasOwnProperty("color")) this.setColor(o.color, GetValue$263(o, "color2", null), GetValue$263(o, "horizontalGradient", true));
		if (o.hasOwnProperty("stroke")) this.setStroke(o.stroke, GetValue$263(o, "strokeThickness", 2));
	}
	renderContent() {
		var padding = this.parent.padding;
		var x = padding.left, y = padding.top, width = this.parent.width - padding.left - padding.right, height = this.parent.height - padding.top - padding.bottom;
		var context = this.context;
		if (this.color != null) {
			var fillStyle;
			if (this.color2 != null) {
				var grd;
				if (this.horizontalGradient) grd = context.createLinearGradient(0, 0, width, 0);
				else grd = context.createLinearGradient(0, 0, 0, height);
				grd.addColorStop(0, this.color);
				grd.addColorStop(1, this.color2);
				fillStyle = grd;
			} else fillStyle = this.color;
			context.fillStyle = fillStyle;
			context.fillRect(x, y, width, height);
		}
		if (this.stroke != null && this.strokeThickness > 0) {
			context.strokeStyle = this.stroke;
			context.lineWidth = this.strokeThickness;
			context.strokeRect(x, y, width, height);
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/bob/char/TextStyle.js
var GetValue$262 = __webpack_exports__Utils.Objects.GetValue;
var TextStyle = class TextStyle {
	constructor(parent, config) {
		this.parent = parent;
		this.set(config);
	}
	toJSON() {
		return {
			bold: this.bold,
			italic: this.italic,
			fontSize: this.fontSize,
			fontFamily: this.fontFamily,
			color: this.color,
			stroke: this.stroke,
			strokeThickness: this.strokeThickness,
			shaodwColor: this.shadowColor,
			shadowBlur: this.shadowBlur,
			shadowOffsetX: this.shadowOffsetX,
			shadowOffsetY: this.shadowOffsetY,
			offsetX: this.offsetX,
			offsetY: this.offsetY,
			leftSpace: this.leftSpace,
			rightSpace: this.rightSpace,
			backgroundHeight: this.backgroundHeight,
			backgroundBottomY: this.backgroundBottomY,
			align: this.align
		};
	}
	set(o) {
		this.setBold(GetValue$262(o, "bold", false));
		this.setItalic(GetValue$262(o, "italic", false));
		this.setFontSize(GetValue$262(o, "fontSize", "16px"));
		this.setFontFamily(GetValue$262(o, "fontFamily", "Courier"));
		this.setColor(GetValue$262(o, "color", "#fff"));
		this.setStrokeStyle(GetValue$262(o, "stroke", null), GetValue$262(o, "strokeThickness", 0));
		this.setShadow(GetValue$262(o, "shadowColor", null), GetValue$262(o, "shadowOffsetX", 0), GetValue$262(o, "shadowOffsetY", 0), GetValue$262(o, "shadowBlur", 0));
		this.setOffset(GetValue$262(o, "offsetX", 0), GetValue$262(o, "offsetY", 0));
		this.setSpace(GetValue$262(o, "leftSpace", 0), GetValue$262(o, "rightSpace", 0));
		this.setAlign(GetValue$262(o, "align", void 0));
		this.setBackgroundColor(GetValue$262(o, "backgroundColor", null));
		this.setBackgroundHeight(GetValue$262(o, "backgroundHeight", void 0));
		this.setBackgroundBottomY(GetValue$262(o, "backgroundBottomY", void 0));
		this.setBackgroundLeftX(GetValue$262(o, "backgroundLeftX", 0));
		this.setBackgroundRightX(GetValue$262(o, "backgroundRightX", 0));
		return this;
	}
	modify(o) {
		if (o.hasOwnProperty("bold")) this.setBold(o.bold);
		if (o.hasOwnProperty("italic")) this.setItalic(o.italic);
		if (o.hasOwnProperty("fontSize")) this.setFontSize(o.fontSize);
		if (o.hasOwnProperty("fontFamily")) this.setFontFamily(o.fontFamily);
		if (o.hasOwnProperty("color")) this.setColor(o.color);
		if (o.hasOwnProperty("stroke") || o.hasOwnProperty("strokeThickness")) this.setStrokeStyle(GetProperty("stroke", o, this), GetProperty("strokeThickness", o, this));
		if (o.hasOwnProperty("shadowColor")) this.setShadowColor(o.shadowColor);
		if (o.hasOwnProperty("shadowOffsetX") || o.hasOwnProperty("shadowOffsetY")) this.setShadowOffset(GetProperty("shadowOffsetX", o, this), GetProperty("shadowOffsetY", o, this));
		if (o.hasOwnProperty("shadowBlur")) this.setShadowBlur(o.shaodwBlur);
		if (o.hasOwnProperty("offsetX")) this.setOffsetX(o.offsetX);
		if (o.hasOwnProperty("offsetY")) this.setOffsetY(o.offsetY);
		if (o.hasOwnProperty("leftSpace")) this.setLeftSpace(o.leftSpace);
		if (o.hasOwnProperty("rightSpace")) this.setRightSpace(o.rightSpace);
		if (o.hasOwnProperty("align")) this.setAlign(o.align);
		if (o.hasOwnProperty("backgroundColor")) this.setBackgroundColor(o.backgroundColor);
		if (o.hasOwnProperty("backgroundHeight")) this.setBackgroundHeight(o.backgroundHeight);
		if (o.hasOwnProperty("backgroundBottomY")) this.setBackgroundBottomY(o.backgroundBottomY);
		if (o.hasOwnProperty("backgroundLeftX")) this.setBackgroundLeftX(o.backgroundLeftX);
		if (o.hasOwnProperty("backgroundRightX")) this.setBackgroundRightX(o.backgroundRightX);
		return this;
	}
	setUpdateTextFlag() {
		if (this.parent) this.parent.updateTextFlag = true;
		return this;
	}
	clone() {
		return new TextStyle(null, this.toJSON());
	}
	copyFrom(sourceTextStyle) {
		this.set(sourceTextStyle.toJSON());
		return this;
	}
	copyTo(targetTextStyle) {
		targetTextStyle.set(this.toJSON());
		return this;
	}
	setBold(value) {
		if (value === void 0) value = true;
		this.bold = value;
		this.setUpdateTextFlag();
		return this;
	}
	setItalic(value) {
		if (value === void 0) value = true;
		this.italic = value;
		this.setUpdateTextFlag();
		return this;
	}
	get fontStyle() {
		if (this.bold && this.italic) return "bold italic";
		else if (this.bold) return "bold";
		else if (this.italic) return "italic";
		else return "";
	}
	setFontSize(fontSize) {
		if (typeof fontSize === "number") fontSize = `${fontSize}px`;
		this.fontSize = fontSize;
		this.setUpdateTextFlag();
		return this;
	}
	setFontFamily(fontFamily) {
		this.fontFamily = fontFamily;
		this.setUpdateTextFlag();
		return this;
	}
	get font() {
		return `${this.fontStyle} ${this.fontSize} ${this.fontFamily}`;
	}
	setColor(color) {
		this.color = GetStyle(color);
		return this;
	}
	get hasFill() {
		return this.color != null;
	}
	setStrokeStyle(stroke, strokeThickness) {
		this.stroke = GetStyle(stroke);
		if (strokeThickness !== void 0) this.strokeThickness = strokeThickness;
		return this;
	}
	setStrokeThickness(strokeThickness) {
		this.strokeThickness = strokeThickness;
		return this;
	}
	get hasStroke() {
		return this.stroke != null && this.strokeThickness > 0;
	}
	setShadowColor(color) {
		this.shadowColor = GetStyle(color);
		return this;
	}
	setShadowOffset(offsetX, offsetY) {
		if (offsetX === void 0) offsetX = 0;
		if (offsetY === void 0) offsetY = 0;
		this.shadowOffsetX = offsetX;
		this.shadowOffsetY = offsetY;
		return this;
	}
	setShadowBlur(blur) {
		if (blur === void 0) blur = 0;
		this.shaodwBlur = blur;
		return this;
	}
	setShadow(color, offsetX, offsetY, blur) {
		this.setShadowColor(color).setShadowOffset(offsetX, offsetY).setShadowBlur(blur);
		return this;
	}
	setBackgroundColor(color) {
		this.backgroundColor = GetStyle(color);
		return this;
	}
	get hasBackgroundColor() {
		return this.backgroundColor != null;
	}
	setBackgroundHeight(height) {
		this.backgroundHeight = height;
		return this;
	}
	setBackgroundBottomY(y) {
		this.backgroundBottomY = y;
		return this;
	}
	setBackgroundLeftX(x) {
		this.backgroundLeftX = x;
		return this;
	}
	setBackgroundRightX(x) {
		this.backgroundRightX = x;
		return this;
	}
	setOffsetX(offsetX) {
		if (offsetX === void 0) offsetX = 0;
		this.offsetX = offsetX;
		return this;
	}
	setOffsetY(offsetY) {
		if (offsetY === void 0) offsetY = 0;
		this.offsetY = offsetY;
		return this;
	}
	setOffset(offsetX, offsetY) {
		this.setOffsetX(offsetX).setOffsetY(offsetY);
		return this;
	}
	setLeftSpace(space) {
		if (space === void 0) space = 0;
		this.leftSpace = space;
		return this;
	}
	setRightSpace(space) {
		if (space === void 0) space = 0;
		this.rightSpace = space;
		return this;
	}
	setSpace(leftSpace, rightSpace) {
		this.setLeftSpace(leftSpace).setRightSpace(rightSpace);
		return this;
	}
	setAlign(align) {
		this.align = align;
		return this;
	}
	syncFont(context) {
		context.font = this.font;
		return this;
	}
	syncStyle(context) {
		context.textBaseline = "alphabetic";
		var hasFill = this.hasFill;
		var hasStroke = this.hasStroke;
		context.fillStyle = hasFill ? this.color : "#000";
		context.strokeStyle = hasStroke ? this.stroke : "#000";
		context.lineWidth = hasStroke ? this.strokeThickness : 0;
		context.lineCap = "round";
		context.lineJoin = "round";
		return this;
	}
	syncShadow(context) {
		if (context.shadowColor != null) {
			context.shadowColor = this.shadowColor;
			context.shadowOffsetX = this.shadowOffsetX;
			context.shadowOffsetY = this.shadowOffsetY;
			context.shadowBlur = this.shadowBlur;
		} else {
			context.shadowColor = 0;
			context.shadowOffsetX = 0;
			context.shadowOffsetY = 0;
			context.shadowBlur = 0;
		}
	}
	getTextMetrics(context, text) {
		this.syncFont(context).syncStyle(context);
		return context.measureText(text);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/SetFixedSize.js
var SetFixedSize = function(width, height) {
	if (width === void 0) width = 0;
	if (height === void 0) height = 0;
	if (this.fixedWidth === width && this.fixedHeight === height) return this;
	this.fixedWidth = width;
	this.fixedHeight = height;
	this.dirty = true;
	this.setCanvasSize(width > 0 ? width : this.width, height > 0 ? height : this.height);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/SetPadding.js
var SetPadding = function(key, value) {
	var padding = this.padding;
	var paddingLeft = padding.left, paddingRight = padding.right, paddingTop = padding.top, paddingBottom = padding.bottom;
	SetPadding$1(padding, key, value);
	this.dirty = this.dirty || paddingLeft != padding.left || paddingRight != padding.right || paddingTop != padding.top || paddingBottom != padding.bottom;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/GetPadding.js
var GetPadding = function(key) {
	return GetPadding$1(this.padding, key);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/ModifyTextStyle.js
var ModifyTextStyle = function(style) {
	this.textStyle.modify(style);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/ModifyDefaultTextStyle.js
var ModifyDefaultTextStyle = function(style) {
	this.defaultTextStyle.modify(style);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/ResetTextStyle.js
var ResetTextStyle = function() {
	this.textStyle.copyFrom(this.defaultTextStyle);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/SetTestString.js
var SetTestString = function(testString) {
	this.testString = testString;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/RemoveChild.js
var RemoveItem$13 = __webpack_exports__Utils.Array.Remove;
var RemoveChild$1 = function(child) {
	this.poolManager.free(child);
	RemoveItem$13(this.children, child);
	this.lastAppendedChildren.length = 0;
	this.lastOverChild = null;
	this.dirty = true;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/RemoveChildren.js
var RemoveChildren = function() {
	this.poolManager.freeMultiple(this.children);
	this.children.length = 0;
	this.lastAppendedChildren.length = 0;
	this.lastOverChild = null;
	this.dirty = true;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/PopChild.js
var RemoveItem$12 = __webpack_exports__Utils.Array.Remove;
var PopChild = function(child) {
	RemoveItem$12(this.children, child);
	this.lastAppendedChildren.length = 0;
	this.lastOverChild = null;
	this.dirty = true;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/ClearContent.js
var ClearContent = function() {
	this.setText();
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/AddChild.js
var AddChild$2 = function(child, index) {
	var areChildren = Array.isArray(child);
	if (index === void 0 || index === this.children.length) {
		if (areChildren) this.children.push(...child);
		else this.children.push(child);
	} else if (areChildren) this.children.splice(index, 0, ...child);
	else this.children.splice(index, 0, child);
	this.lastAppendedChildren.length = 0;
	if (areChildren) this.lastAppendedChildren.push(...child);
	else this.lastAppendedChildren.push(child);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/bob/Types.js
var CharTypeName = "text";
var ImageTypeName = "image";
var DrawerTypeName = "drawer";
var SpaceTypeName = "space";
var CmdTypeName = "command";
var IsNewLineChar = function(bob) {
	return bob.type === "text" && bob.text === "\n";
};
var IsPageBreakChar = function(bob) {
	return bob.type === "text" && bob.text === "\f";
};
var IsSpaceChar = function(bob) {
	return bob.type === "text" && bob.text === " ";
};
var IsChar = function(bob) {
	return bob.type === CharTypeName;
};
var IsCommand = function(bob) {
	return bob.type === CmdTypeName;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/bob/char/CharData.js
var CharData = class extends RenderBase {
	constructor(parent, text, style) {
		super(parent, CharTypeName);
		this.updateTextFlag = false;
		this.style = new TextStyle(this, style);
		this.setText(text);
	}
	get autoRound() {
		return this.parent.autoRound;
	}
	get offsetX() {
		return this.style.offsetX;
	}
	set offsetX(value) {
		if (this.style) this.style.offsetX = value;
	}
	get offsetY() {
		return this.style.offsetY;
	}
	set offsetY(value) {
		if (this.style) this.style.offsetY = value;
	}
	get leftSpace() {
		return this.style.leftSpace * this.scaleX;
	}
	set leftSpace(value) {
		if (this.style) this.style.leftSpace = value;
		super.leftSpace = value;
	}
	get rightSpace() {
		return this.style.rightSpace * this.scaleX;
	}
	set rightSpace(value) {
		if (this.style) this.style.rightSpace = value;
		super.rightSpace = value;
	}
	get align() {
		return this.style.align;
	}
	set align(value) {
		if (this.style) this.style.align = value;
	}
	modifyStyle(style) {
		this.setDirty(true);
		this.style.modify(style);
		if (this.updateTextFlag) this.updateTextSize();
		return this;
	}
	modifyPorperties(o) {
		if (!o) return this;
		this.modifyStyle(o);
		super.modifyPorperties(o);
		return this;
	}
	setText(text) {
		this.setDirty(this.text != text);
		this.text = text;
		this.updateTextSize();
		return this;
	}
	updateTextSize() {
		var text = this.text;
		if (text === "\n" || text === "\f" || text === "") this.clearTextSize();
		else {
			var metrics = this.style.getTextMetrics(this.context, this.text);
			this.textWidth = metrics.width;
			var ascent, descent;
			if ("actualBoundingBoxAscent" in metrics) {
				ascent = metrics.actualBoundingBoxAscent;
				descent = metrics.actualBoundingBoxDescent;
			} else {
				ascent = 0;
				descent = 0;
			}
			this.textHeight = ascent + descent;
			this.ascent = ascent;
			this.descent = descent;
		}
		this.updateTextFlag = false;
		return this;
	}
	clearTextSize() {
		this.textWidth = 0;
		this.textHeight = 0;
		this.ascent = 0;
		this.descent = 0;
		return this;
	}
	copyTextSize(child) {
		this.textWidth = child.textWidth;
		this.textHeight = child.textHeight;
		this.ascent = child.ascent;
		this.descent = child.descent;
		return this;
	}
	get width() {
		return this.textWidth * this.scaleX;
	}
	set width(value) {
		if (this.textWidth > 0) this.scaleX = value / this.textWidth;
		else this.scaleX = 1;
	}
	get height() {
		return this.textHeight * this.scaleY;
	}
	set height(value) {
		if (this.textHeight > 0) this.scaleY = value / this.textHeight;
		else this.scaleY = 1;
	}
	get willRender() {
		if (this.textWidth === 0) return false;
		return super.willRender;
	}
	renderContent() {
		var context = this.context;
		var textStyle = this.style;
		if (textStyle.hasBackgroundColor) {
			context.fillStyle = textStyle.backgroundColor;
			var leftX = this.drawTLX + textStyle.backgroundLeftX;
			var rightX = this.drawTRX + textStyle.backgroundRightX;
			var x = leftX;
			var width = rightX - leftX + 1;
			if (width > 0) {
				var bottomY = textStyle.backgroundBottomY;
				if (bottomY == null) bottomY = this.drawBLY;
				var height = textStyle.backgroundHeight;
				if (height == null) height = bottomY - this.drawTLY;
				var y = bottomY - height;
				context.fillRect(x, y, width, height);
			}
		}
		var hasFill = textStyle.hasFill, hasStroke = textStyle.hasStroke;
		if (!hasFill && !hasStroke) return;
		textStyle.syncFont(context).syncStyle(context);
		if (hasStroke) {
			textStyle.syncShadow(context);
			context.strokeText(this.text, 0, 0);
		}
		if (hasFill) {
			textStyle.syncShadow(context);
			context.fillText(this.text, 0, 0);
		}
	}
	get drawTLX() {
		return -this.leftSpace;
	}
	get drawTLY() {
		return -this.ascent;
	}
	get drawBLX() {
		return -this.leftSpace;
	}
	get drawBLY() {
		return this.descent;
	}
	get drawTRX() {
		return this.textWidth + this.rightSpace;
	}
	get drawTRY() {
		return -this.ascent;
	}
	get drawBRX() {
		return this.textWidth + this.rightSpace;
	}
	get drawBRY() {
		return this.descent;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/CreateCharChild.js
var CreateCharChild = function(text, style) {
	if (style) this.textStyle.modify(style);
	var child = this.poolManager.allocate(CharTypeName);
	if (child === null) child = new CharData(this, text, this.textStyle);
	else child.setParent(this).setActive().modifyStyle(this.textStyle).setText(text);
	return child;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/CreateCharChildren.js
var CreateCharChildren = function(text, style) {
	if (style) this.textStyle.modify(style);
	var children = [];
	for (var i = 0, cnt = text.length; i < cnt; i++) {
		var char = text.charAt(i);
		var child = this.poolManager.allocate(CharTypeName);
		if (child === null) child = new CharData(this, char, this.textStyle);
		else child.setParent(this).setActive().modifyStyle(this.textStyle).setText(char);
		children.push(child);
	}
	return children;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/AppendText.js
var AppendText = function(text, style) {
	var children = this.createCharChildren(text, style);
	this.addChild(children);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/SetText.js
var SetText$1 = function(text, style) {
	if (text === void 0) text = "";
	this.removeChildren();
	AppendText.call(this, text, style);
	this.dirty = true;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/InsertText.js
var InsertText = function(index, text, style) {
	var children = this.createCharChildren(text, style);
	index = this.getCharChildIndex(index, true);
	this.addChild(children, index);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/RemoveText.js
var RemoveText = function(index, length) {
	if (length === void 0) length = 1;
	for (var i = 0; i < length; i++) {
		var childIndex = this.getCharChildIndex(index, true);
		if (childIndex === void 0) break;
		this.removeChild(this.children[childIndex]);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/GetText.js
var GetText = function(activeOnly) {
	var text = "";
	this.forEachCharChild(function(child) {
		text += child.text;
	}, void 0, activeOnly);
	return text;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/bob/image/ImageData.js
var TintModes = __webpack_exports__TintModes;
var ImageData = class extends RenderBase {
	constructor(parent, key, frame) {
		super(parent, ImageTypeName);
		this.setTexture(key, frame);
		this.color = void 0;
		this.tintMode = void 0;
	}
	get frameWidth() {
		return this.frameObj ? this.frameObj.cutWidth : 0;
	}
	get frameHeight() {
		return this.frameObj ? this.frameObj.cutHeight : 0;
	}
	get offsetY() {
		return -this.height;
	}
	set offsetY(value) {}
	get key() {
		return this._key;
	}
	set key(value) {
		this.setDirty(this._key != value);
		this._key = value;
	}
	get frame() {
		return this._frame;
	}
	set frame(value) {
		this.setDirty(this._frame != value);
		this._frame = value;
	}
	setTexture(key, frame) {
		this.key = key;
		this.frame = frame;
		this.frameObj = this.scene.sys.textures.getFrame(key, frame);
		return this;
	}
	get width() {
		return this.frameWidth * this.scaleX;
	}
	set width(value) {
		this.setDirty(this.width !== value);
		this.scaleX = value / this.frameWidth;
	}
	get height() {
		return this.frameHeight * this.scaleY;
	}
	set height(value) {
		this.setDirty(this.height !== value);
		this.scaleY = value / this.frameHeight;
	}
	setHeight(height, keepAspectRatio) {
		if (keepAspectRatio === void 0) keepAspectRatio = false;
		this.height = height;
		if (keepAspectRatio) this.scaleX = this.scaleY;
		return this;
	}
	setColor(color) {
		this.color = color;
		return this;
	}
	setTintMode(tintMode) {
		this.tintMode = tintMode;
		return this;
	}
	modifyPorperties(o) {
		if (o.hasOwnProperty("color")) this.setColor(o.color);
		if (o.hasOwnProperty("tintMode")) this.setTintMode(o.tintMode);
		super.modifyPorperties(o);
		return this;
	}
	renderContent() {
		var tintMode = void 0;
		if (this.color !== void 0 && this.color !== null) tintMode = this.tintMode === void 0 ? TintModes.FILL : this.tintMode;
		DrawFrameToCanvas(this.frameObj, this.context, 0, 0, this.frameWidth, this.frameHeight, this.color, false, tintMode);
	}
	get drawTLX() {
		return -this.leftSpace;
	}
	get drawTLY() {
		return 0;
	}
	get drawBLX() {
		return -this.leftSpace;
	}
	get drawBLY() {
		return this.frameHeight;
	}
	get drawTRX() {
		return this.frameWidth + this.rightSpace;
	}
	get drawTRY() {
		return 0;
	}
	get drawBRX() {
		return this.frameWidth + this.rightSpace;
	}
	get drawBRY() {
		return this.frameHeight;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/CreateImageChild.js
var CreateImageChild = function(key, frame, properties) {
	var child = this.poolManager.allocate(ImageTypeName);
	if (child === null) child = new ImageData(this, key, frame);
	else child.setParent(this).setActive().setTexture(key, frame);
	child.modifyPorperties(properties);
	return child;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/AppendImage.js
var AppendImage = function(key, frame, properties) {
	var child = this.createImageChild(key, frame, properties);
	this.addChild(child);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/bob/drawer/Drawer.js
var Drawer = class extends RenderBase {
	constructor(parent, renderCallback, width, height) {
		super(parent, DrawerTypeName);
		this.setRenderCallback(renderCallback);
		this.setDrawerSize(width, height);
	}
	setRenderCallback(callback) {
		if (callback) this.renderContent = callback.bind(this);
		else delete this.renderContent;
		return this;
	}
	setDrawerSize(width, height) {
		if (width === true) {
			this.toLocalPosition = false;
			width = void 0;
			height = void 0;
		} else this.toLocalPosition = true;
		if (width === void 0) width = 0;
		if (height === void 0) height = width;
		this.drawerWidth = width;
		this.drawerHeight = height;
		return this;
	}
	onFree() {
		super.onFree();
		this.setRenderCallback();
	}
	get width() {
		return this.drawerWidth * this.scaleX;
	}
	set width(value) {
		this.setDirty(this.width !== value);
		this.scaleX = this.drawerWidth > 0 ? value / this.drawerWidth : 1;
	}
	get height() {
		return this.drawerHeight * this.scaleY;
	}
	set height(value) {
		this.setDirty(this.height !== value);
		this.scaleY = this.drawerHeight > 0 ? value / this.drawerHeight : 1;
	}
	get offsetY() {
		return -this.height;
	}
	set offsetY(value) {}
	get drawTLX() {
		return -this.leftSpace;
	}
	get drawTLY() {
		return 0;
	}
	get drawBLX() {
		return -this.leftSpace;
	}
	get drawBLY() {
		return this.drawerHeight;
	}
	get drawTRX() {
		return this.drawerWidth + this.rightSpace;
	}
	get drawTRY() {
		return 0;
	}
	get drawBRX() {
		return this.drawerWidth + this.rightSpace;
	}
	get drawBRY() {
		return this.drawerHeight;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/CreateDrawerChild.js
var CreateDrawerChild = function(renderCallback, width, height) {
	var child = this.poolManager.allocate(DrawerTypeName);
	if (child === null) child = new Drawer(this, renderCallback, width, height);
	else child.setParent(this).setActive().setRenderCallback(renderCallback).setDrawerSize(width, height);
	return child;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/AppendDrawer.js
var AppendDrawer = function(renderCallback, width, height) {
	var child = this.createDrawerChild(renderCallback, width, height);
	this.addChild(child);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/bob/space/Space.js
var Space$1 = class extends RenderBase {
	constructor(parent, width) {
		super(parent, SpaceTypeName);
		this.setSpaceWidth(width);
	}
	get width() {
		return this.spaceWidth * this.scaleX;
	}
	set width(value) {
		if (this.spaceWidth > 0) this.scaleX = value / this.spaceWidth;
		else this.scaleX = 1;
	}
	setSpaceWidth(width) {
		this.spaceWidth = width;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/CreateSpaceChild.js
var CreateSpaceChild = function(width) {
	var child = this.poolManager.allocate(SpaceTypeName);
	if (child === null) child = new Space$1(this, width);
	else child.setParent(this).setActive().setSpaceWidth(width);
	return child;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/AppendSpace.js
var AppendSpace = function(width) {
	var child = this.createSpaceChild(width);
	this.addChild(child);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/bob/command/Command.js
var Command = class extends Base$5 {
	constructor(parent, name, callback, param, scope) {
		super(parent, CmdTypeName);
		this.setName(name).setParameter(param).setCallback(callback, scope);
	}
	setName(name) {
		this.name = name;
		return this;
	}
	setParameter(param) {
		this.param = param;
		return this;
	}
	setCallback(callback, scope) {
		this.callback = callback;
		this.scope = scope;
		return this;
	}
	exec() {
		var result;
		if (this.scope) result = this.callback.call(this.scope, this.param, this.name);
		else result = this.callback(this.param, this.name);
		return result;
	}
	onFree() {
		super.onFree();
		this.setName().setCallback().setParameter();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/CreateCommandChild.js
var CreateCommandChild = function(name, callback, param, scope) {
	var child = this.poolManager.allocate(CmdTypeName);
	if (child === null) child = new Command(this, name, callback, param, scope);
	else child.setParent(this).setActive().setName(name).setCallback(callback, scope).setParameter(param);
	return child;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/AppendCommand.js
var AppendCommand$3 = function(name, callback, param, scope) {
	var child = this.createCommandChild(name, callback, param, scope);
	this.addChild(child);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/SetWrapConfig.js
var SetWrapConfig = function(config) {
	if (config === void 0) config = {};
	else if (typeof config === "object") config = DeepClone(config);
	this.wrapConfig = config;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/wrap/CreateWrapResultData.js
var CreateWrapResultData = function(config) {
	return Object.assign({
		callback: void 0,
		start: 0,
		isLastPage: false,
		maxLines: void 0,
		padding: void 0,
		letterSpacing: void 0,
		hAlign: void 0,
		vAlign: void 0,
		children: [],
		lines: [],
		maxLineWidth: 0,
		linesHeight: 0,
		lineHeight: void 0,
		maxLineHeight: 0,
		linesWidth: 0,
		lineWidth: void 0
	}, config);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/wrap/Const.js
var WRAPMODE = {
	none: 0,
	word: 1,
	char: 2,
	character: 2,
	mix: 3
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/wrap/runwordwrap/GetWord.js
var GetWord = function(children, startIndex, wrapMode, result) {
	if (result === void 0) result = {
		word: [],
		width: 0
	};
	result.word.length = 0;
	var isCharWrap = wrapMode === 2;
	var isMixWrap = wrapMode === 3;
	var isWordWrap = !isCharWrap && !isMixWrap;
	var endIndex = children.length;
	var currentIndex = startIndex;
	var word = result.word;
	var wordWidth = 0;
	var hasAnyASCIICharacter = false;
	while (currentIndex < endIndex) {
		var child = children[currentIndex];
		if (!child.renderable) {
			word.push(child);
			currentIndex++;
			continue;
		}
		var text = child.type === "text" ? child.text : null;
		if (text === null || text === "\n" || text === "\f") {
			if (currentIndex === startIndex) {
				word.push(child);
				wordWidth += child.outerWidth;
			}
			break;
		}
		if (isWordWrap) {
			word.push(child);
			wordWidth += child.outerWidth;
			if (text === " ") break;
			currentIndex++;
		} else if (isCharWrap) {
			word.push(child);
			wordWidth += child.outerWidth;
			break;
		} else if (isMixWrap) {
			if (!IsASCIIString(text)) {
				if (!hasAnyASCIICharacter) {
					word.push(child);
					wordWidth += child.outerWidth;
					var nextChild = children[currentIndex + 1];
					if (nextChild && nextChild.type === "text" && nextChild.text === " ") {
						word.push(nextChild);
						wordWidth += nextChild.outerWidth;
					}
					break;
				} else break;
			} else {
				word.push(child);
				wordWidth += child.outerWidth;
				if (text === " ") break;
				currentIndex++;
				hasAnyASCIICharacter = true;
			}
		}
	}
	result.width = wordWidth;
	return result;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/wrap/GetChildrenAlign.js
var GetChildrenAlign = function(children) {
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		if (child.align !== void 0) return child.align;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/wrap/OffsetChildren.js
var OffsetChildren = function(children, offsetX, offsetY) {
	if (offsetX === 0 && offsetY === 0) return;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		if (!child.renderable) continue;
		child.x += offsetX;
		child.y += offsetY;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/wrap/runwordwrap/AlignLines.js
var AlignLines$1 = function(result, width, height) {
	var hAlign = result.hAlign, vAlign = result.vAlign, justifyPercentage = result.justifyPercentage;
	var lines = result.lines;
	var offsetX, offsetY;
	for (var li = 0, lcnt = lines.length; li < lcnt; li++) {
		var line = lines[li];
		var lineWidth = line.width, children = line.children;
		var lineHAlign = GetChildrenAlign(children);
		if (lineHAlign === void 0) lineHAlign = hAlign;
		switch (lineHAlign) {
			case 0:
			case "left":
				offsetX = 0;
				break;
			case 1:
			case "center":
				var remainderWidth = width - lineWidth;
				offsetX = remainderWidth / 2;
				break;
			case 2:
			case "right":
				var remainderWidth = width - lineWidth;
				offsetX = remainderWidth;
				break;
			case 3:
			case "justify":
			case "justify-left":
				var remainderWidth = width - lineWidth;
				var remainderPercentage = remainderWidth / width;
				if (remainderPercentage < justifyPercentage) {
					JustifyChildren(children, remainderWidth);
					offsetX = 0;
				} else offsetX = 0;
				break;
			case 4:
			case "justify-center":
				var remainderWidth = width - lineWidth;
				var remainderPercentage = remainderWidth / width;
				if (remainderPercentage < justifyPercentage) {
					JustifyChildren(children, remainderWidth);
					offsetX = 0;
				} else offsetX = remainderWidth / 2;
				break;
			case 5:
			case "justify-right":
				var remainderWidth = width - lineWidth;
				var remainderPercentage = remainderWidth / width;
				if (remainderPercentage < justifyPercentage) {
					JustifyChildren(children, remainderWidth);
					offsetX = 0;
				} else offsetX = remainderWidth;
				break;
			default: offsetX = 0;
		}
		var linesHeight = result.linesHeight;
		switch (vAlign) {
			case 1:
			case "center":
				offsetY = (height - linesHeight) / 2;
				break;
			case 2:
			case "bottom":
				offsetY = height - linesHeight;
				break;
			default: offsetY = 0;
		}
		OffsetChildren(children, offsetX, offsetY);
	}
};
var JustifyChildren = function(children, remainderWidth) {
	var offset = remainderWidth / children.length;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		if (!child.renderable) continue;
		child.x += offset * i;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/wrap/runwordwrap/GetDefaultTextHeight.js
var GetDefaultTextHeight = function() {
	var metrics = this.defaultTextStyle.getTextMetrics(this.context, this.testString);
	var ascent, descent;
	if ("actualBoundingBoxAscent" in metrics) {
		ascent = metrics.actualBoundingBoxAscent;
		descent = metrics.actualBoundingBoxDescent;
	} else {
		ascent = 0;
		descent = 0;
	}
	Result.ascent = ascent;
	Result.descent = descent;
	Result.height = ascent + descent;
	return Result;
};
var Result = {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/wrap/runwordwrap/RunWordWrap.js
var GetValue$261 = __webpack_exports__Utils.Objects.GetValue;
var RunWordWrap$1 = function(config) {
	var startIndex = GetValue$261(config, "start", 0);
	SetPadding$1(this.wrapPadding, GetValue$261(config, "padding", 0));
	var paddingVertical = this.padding.top + this.padding.bottom + this.wrapPadding.top + this.wrapPadding.bottom;
	var paddingHorizontal = this.padding.left + this.padding.right + this.wrapPadding.left + this.wrapPadding.right;
	var lineHeight = GetValue$261(config, "lineHeight");
	var ascent = GetValue$261(config, "ascent", lineHeight);
	var maxLines;
	if (lineHeight === void 0) {
		var useDefaultTextHeight = GetValue$261(config, "useDefaultTextHeight", false);
		maxLines = GetValue$261(config, "maxLines", 0);
		if (this.fixedHeight > 0 && !useDefaultTextHeight) {
			var innerHeight = this.fixedHeight - paddingVertical;
			if (maxLines > 0) lineHeight = innerHeight / maxLines;
			else {
				var textHeightResult = GetDefaultTextHeight.call(this);
				lineHeight = textHeightResult.height;
				ascent = textHeightResult.ascent;
				maxLines = Math.floor((innerHeight - ascent) / lineHeight);
			}
		} else {
			var textHeightResult = GetDefaultTextHeight.call(this);
			lineHeight = textHeightResult.height;
			ascent = textHeightResult.ascent;
		}
	} else if (this.fixedHeight > 0) {
		maxLines = GetValue$261(config, "maxLines");
		if (maxLines === void 0) {
			var innerHeight = this.fixedHeight - paddingVertical;
			maxLines = Math.floor(innerHeight / lineHeight);
		}
	} else maxLines = GetValue$261(config, "maxLines", 0);
	if (ascent === void 0) ascent = lineHeight;
	var showAllLines = maxLines === 0;
	var wrapMode = GetValue$261(config, "wrapMode");
	if (wrapMode === void 0) wrapMode = GetValue$261(config, "charWrap", false) ? "char" : "word";
	if (typeof wrapMode === "string") wrapMode = WRAPMODE[wrapMode];
	var wrapWidth = GetValue$261(config, "wrapWidth", void 0);
	if (wrapWidth === void 0) {
		if (this.fixedWidth > 0) wrapWidth = this.fixedWidth - paddingHorizontal;
		else {
			wrapWidth = Infinity;
			wrapMode = 0;
		}
	}
	var letterSpacing = GetValue$261(config, "letterSpacing", 0);
	var hAlign = GetValue$261(config, "hAlign", 0);
	var vAlign = GetValue$261(config, "vAlign", 0);
	var justifyPercentage = GetValue$261(config, "justifyPercentage", .25);
	var result = CreateWrapResultData({
		callback: "runWordWrap",
		start: startIndex,
		padding: this.wrapPadding,
		letterSpacing,
		maxLines,
		hAlign,
		vAlign,
		justifyPercentage,
		ascent,
		lineHeight,
		wrapWidth,
		wrapMode
	});
	var children = this.children;
	for (var i = 0, cnt = children.length; i < cnt; i++) children[i].setActive(false);
	wrapWidth += letterSpacing;
	var startX = this.padding.left + this.wrapPadding.left, startY = this.padding.top + this.wrapPadding.top + ascent, x = startX, y = startY;
	var remainderWidth = wrapWidth, childIndex = startIndex, lastChildIndex = children.length;
	var resultChildren = result.children;
	var resultLines = result.lines, lastLine = [], lastLineWidth = 0, maxLineWidth = 0;
	var wordResult;
	var isPageBreakChar = false;
	while (childIndex < lastChildIndex) {
		wordResult = GetWord(children, childIndex, wrapMode, wordResult);
		var word = wordResult.word;
		var charCnt = word.length;
		var wordWidth = wordResult.width + charCnt * letterSpacing;
		childIndex += charCnt;
		var isNewLineChar = IsNewLineChar(word[0]);
		isPageBreakChar = IsPageBreakChar(word[0]);
		var isControlChar = isNewLineChar || isPageBreakChar;
		if (remainderWidth < wordWidth || isControlChar) {
			if (isControlChar) {
				var char = word[0];
				char.setActive().setPosition(x, y);
				resultChildren.push(char);
				lastLine.push(char);
			}
			x = startX;
			y += lineHeight;
			remainderWidth = wrapWidth;
			resultLines.push({
				children: lastLine,
				width: lastLineWidth
			});
			maxLineWidth = Math.max(maxLineWidth, lastLineWidth);
			lastLineWidth = 0;
			lastLine = [];
			if (isPageBreakChar || !showAllLines && resultLines.length === maxLines) break;
			else if (isControlChar) continue;
		}
		remainderWidth -= wordWidth;
		lastLineWidth += wordWidth;
		for (var i = 0, cnt = word.length; i < cnt; i++) {
			var child = word[i];
			child.setActive();
			resultChildren.push(child);
			lastLine.push(child);
			if (child.renderable) {
				child.setPosition(x, y);
				x += child.outerWidth + letterSpacing;
			}
		}
	}
	if (lastLine.length > 0) {
		resultLines.push({
			children: lastLine,
			width: lastLineWidth
		});
		maxLineWidth = Math.max(maxLineWidth, lastLineWidth);
	}
	result.start += resultChildren.length;
	result.isLastPage = !isPageBreakChar && result.start === lastChildIndex;
	result.maxLineWidth = maxLineWidth;
	result.linesHeight = resultLines.length * lineHeight;
	var width = this.fixedWidth > 0 ? this.fixedWidth : result.maxLineWidth + paddingHorizontal;
	var height = this.fixedHeight > 0 ? this.fixedHeight : result.linesHeight + paddingVertical;
	var innerWidth = width - paddingHorizontal;
	var innerHeight = height - paddingVertical;
	AlignLines$1(result, innerWidth, innerHeight);
	this.setCanvasSize(width, height);
	for (var i = 0, cnt = resultChildren.length; i < cnt; i++) {
		var child = resultChildren[i];
		if (!child.renderable) continue;
		child.x0 = child.x;
		child.y0 = child.y;
	}
	return result;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/RunWordWrap.js
var Merge$6 = __webpack_exports__Utils.Objects.Merge;
var RunWordWrap = function(config) {
	if (config === void 0) config = {};
	return RunWordWrap$1.call(this, Merge$6(config, this.wrapConfig));
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/wrap/runverticalwrap/AlignLines.js
var AlignLines = function(result, width, height) {
	var hAlign = result.hAlign, vAlign = result.vAlign;
	var offsetX, offsetY;
	var rtl = result.rtl;
	var lines = result.lines, lineWidth = result.lineWidth, linesWidth = result.linesWidth;
	switch (hAlign) {
		case 1:
		case "center":
			offsetX = (width - linesWidth) / 2;
			break;
		case 2:
		case "right":
			offsetX = width - linesWidth;
			break;
		default: offsetX = 0;
	}
	if (rtl) offsetX += lineWidth;
	for (var li = 0, lcnt = lines.length; li < lcnt; li++) {
		var line = lines[rtl ? lcnt - li - 1 : li];
		var children = line.children;
		var lineHeight = line.height;
		var lineVAlign = GetChildrenAlign(children);
		if (lineVAlign === void 0) lineVAlign = vAlign;
		switch (lineVAlign) {
			case 1:
			case "center":
				offsetY = (height - lineHeight) / 2;
				break;
			case 2:
			case "bottom":
				offsetY = height - lineHeight;
				break;
			default: offsetY = 0;
		}
		OffsetChildren(children, offsetX, offsetY);
		offsetX += lineWidth;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/wrap/runverticalwrap/RunVerticalWrap.js
var GetValue$260 = __webpack_exports__Utils.Objects.GetValue;
var RunVerticalWrap$1 = function(config) {
	var startIndex = GetValue$260(config, "start", 0);
	SetPadding$1(this.wrapPadding, GetValue$260(config, "padding", 0));
	var paddingVertical = this.padding.top + this.padding.bottom + this.wrapPadding.top + this.wrapPadding.bottom;
	var paddingHorizontal = this.padding.left + this.padding.right + this.wrapPadding.left + this.wrapPadding.right;
	var lineWidth = GetValue$260(config, "lineWidth", void 0);
	var maxLines;
	if (lineWidth === void 0) {
		maxLines = GetValue$260(config, "maxLines", 0);
		if (this.fixedWidth > 0) {
			var innerWidth = this.fixedWidth - paddingHorizontal;
			lineWidth = innerWidth / maxLines;
		} else lineWidth = 0;
	} else if (this.fixedWidth > 0) {
		maxLines = GetValue$260(config, "maxLines", void 0);
		if (maxLines === void 0) {
			var innerWidth = this.fixedWidth - paddingHorizontal;
			maxLines = Math.floor(innerWidth / lineWidth) + 1;
		}
	} else maxLines = GetValue$260(config, "maxLines", 0);
	var showAllLines = maxLines === 0;
	var fixedCharacterHeight = GetValue$260(config, "fixedCharacterHeight", void 0);
	if (fixedCharacterHeight === void 0) {
		var charPerLine = GetValue$260(config, "charPerLine", void 0);
		if (charPerLine !== void 0) {
			var innerHeight = this.fixedHeight - paddingVertical;
			fixedCharacterHeight = Math.floor(innerHeight / charPerLine);
		}
	}
	var wrapHeight = GetValue$260(config, "wrapHeight", void 0);
	if (wrapHeight === void 0) {
		if (this.fixedHeight > 0) wrapHeight = this.fixedHeight - paddingVertical;
		else wrapHeight = Infinity;
	}
	var letterSpacing = GetValue$260(config, "letterSpacing", 0);
	var rtl = GetValue$260(config, "rtl", true);
	var hAlign = GetValue$260(config, "hAlign", rtl ? 2 : 0);
	var vAlign = GetValue$260(config, "vAlign", 0);
	var result = CreateWrapResultData({
		callback: "runVerticalWrap",
		start: startIndex,
		padding: this.wrapPadding,
		letterSpacing,
		maxLines,
		hAlign,
		vAlign,
		lineWidth,
		fixedCharacterHeight,
		wrapHeight,
		rtl
	});
	var children = this.children;
	for (var i = 0, cnt = children.length; i < cnt; i++) children[i].setActive(false);
	wrapHeight += letterSpacing;
	var startX = this.padding.left + this.wrapPadding.left, startY = this.padding.top + this.wrapPadding.top, x = startX, y = startY;
	var remainderHeight = wrapHeight, childIndex = startIndex, lastChildIndex = children.length;
	var resultChildren = result.children;
	var resultLines = result.lines, lastLine = [], lastLineHeight = 0, maxLineHeight = 0;
	while (childIndex < lastChildIndex) {
		var child = children[childIndex];
		childIndex++;
		if (!child.renderable) {
			child.setActive();
			resultChildren.push(child);
			lastLine.push(child);
			continue;
		}
		var childHeight = (fixedCharacterHeight !== void 0 ? fixedCharacterHeight : child.height) + letterSpacing;
		var isNewLineChar = IsNewLineChar(child);
		var isPageBreakChar = IsPageBreakChar(child);
		var isControlChar = isNewLineChar || isPageBreakChar;
		if (remainderHeight < childHeight || isControlChar) {
			if (isNewLineChar) {
				child.setActive().setPosition(x, y).setOrigin(.5);
				resultChildren.push(child);
				lastLine.push(child);
			}
			x = startX;
			y = startY;
			remainderHeight = wrapHeight;
			resultLines.push({
				children: lastLine,
				height: lastLineHeight
			});
			maxLineHeight = Math.max(maxLineHeight, lastLineHeight);
			lastLineHeight = 0;
			lastLine = [];
			if (isPageBreakChar || !showAllLines && resultLines.length === maxLines) break;
			else if (isControlChar) continue;
		}
		remainderHeight -= childHeight;
		lastLineHeight += childHeight;
		child.setActive().setPosition(x, y).setOrigin(.5);
		resultChildren.push(child);
		lastLine.push(child);
		y += childHeight;
	}
	if (lastLine.length > 0) {
		resultLines.push({
			children: lastLine,
			height: lastLineHeight
		});
		maxLineHeight = Math.max(maxLineHeight, lastLineHeight);
	}
	result.start += resultChildren.length;
	result.isLastPage = result.start === lastChildIndex;
	result.maxLineHeight = maxLineHeight;
	result.linesWidth = resultLines.length * lineWidth;
	var width = this.fixedWidth > 0 ? this.fixedWidth : result.linesWidth + paddingHorizontal;
	var height = this.fixedHeight > 0 ? this.fixedHeight : result.maxLineHeight + paddingVertical;
	var innerWidth = width - paddingHorizontal;
	var innerHeight = height - paddingVertical;
	AlignLines(result, innerWidth, innerHeight);
	this.setCanvasSize(width, height);
	for (var i = 0, cnt = resultChildren.length; i < cnt; i++) {
		var child = resultChildren[i];
		if (!child.renderable) continue;
		child.x0 = child.x;
		child.y0 = child.y;
	}
	return result;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/RunVerticalWrap.js
var Merge$5 = __webpack_exports__Utils.Objects.Merge;
var RunVerticalWrap = function(config) {
	if (config === void 0) config = {};
	return RunVerticalWrap$1.call(this, Merge$5(config, this.wrapConfig));
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/RunWrap.js
var GetValue$259 = __webpack_exports__Utils.Objects.GetValue;
var RunWrap = function(config) {
	var wrapCallback = GetValue$259(this.wrapConfig, "callback");
	if (!wrapCallback) wrapCallback = GetValue$259(config, "callback", this.runWordWrap);
	if (typeof wrapCallback === "string") wrapCallback = this[wrapCallback];
	return wrapCallback.call(this, config);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/SetAlignMethods.js
var SetAlignMethods_default = {
	setVAlign(align) {
		this.wrapConfig.vAlign = align;
		return this;
	},
	setHAlign(align) {
		this.wrapConfig.hAlign = align;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/SetTextOXYMethods.js
var SetTextOXYMethods_default$1 = {
	setTextOX(ox) {
		if (ox === this._textOX) return this;
		this._textOX = ox;
		return this;
	},
	setTextOY(oy) {
		if (oy === this._textOY) return this;
		this._textOY = oy;
		return this;
	},
	setTextOXY(ox, oy) {
		if (ox === this._textOX && oy === this._textOY) return;
		this._textOX = ox;
		this._textOY = oy;
		return this;
	},
	addTextOX(incX) {
		this.setTextOX(this._textOX + incX);
		return this;
	},
	addTextOY(incY) {
		this.setTextOY(this._textOY + incY);
		return this;
	},
	addTextOXY(incX, incY) {
		this.setTextOXY(this._textOX + incX, this._textOY + incY);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/RenderContent.js
var RenderContent = function() {
	this.clear();
	this.setCanvasSize(this.width, this.height);
	if (this.background.active) this.background.render();
	var child;
	for (var i = 0, cnt = this.children.length; i < cnt; i++) {
		child = this.children[i];
		if (child.active) child.render();
	}
	if (this.innerBounds.active) this.innerBounds.render();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/ForEachChild.js
var ForEachChild = function(callback, scope, activeOnly) {
	if (activeOnly === void 0) activeOnly = true;
	var children = this.children.filter(function(child) {
		if (activeOnly && !child.active) return false;
		return true;
	});
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		var isBreak;
		if (scope) isBreak = callback.call(this, child, i, children);
		else isBreak = callback(child, i, children);
		if (isBreak) break;
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/ForEachRenderableChild.js
var ForEachRenderableChild = function(callback, scope, activeOnly) {
	if (activeOnly === void 0) activeOnly = true;
	var children = this.children.filter(function(child) {
		if (activeOnly && !child.active) return false;
		if (!child.renderable || child.removed) return false;
		return true;
	});
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		var isBreak;
		if (scope) isBreak = callback.call(this, child, i, children);
		else isBreak = callback(child, i, children);
		if (isBreak) break;
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/ForEachCharChild.js
var ForEachCharChild = function(callback, scope, activeOnly) {
	if (activeOnly === void 0) activeOnly = true;
	var children = this.children.filter(function(child) {
		if (activeOnly && !child.active) return false;
		if (!IsChar(child) || child.removed) return false;
		return true;
	});
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		var isBreak;
		if (scope) isBreak = callback.call(this, child, i, children);
		else isBreak = callback(child, i, children);
		if (isBreak) break;
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/GetChildren.js
var GetChildren = function() {
	return this.children;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/GetActiveChildren.js
var GetAll = __webpack_exports__Utils.Array.GetAll;
var GetActiveChildren = function() {
	return GetAll(this.children, "active", true);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/GetCharChildren.js
var GetCharChildren = function(activeOnly, out) {
	if (out === void 0) out = [];
	this.forEachCharChild(function(child) {
		out.push(child);
	}, void 0, activeOnly);
	return out;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/GetLastAppendedChildren.js
var GetLastAppendedChildren = function() {
	return this.lastAppendedChildren;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/utils/transform/GetBobCenterPosition.js
var GetBobCenterPosition = function(bob, offsetX, offsetY, out) {
	if (typeof offsetX !== "number") {
		out = offsetX;
		offsetX = 0;
		offsetY = 0;
	}
	return BobPositionToCanvasPosition(bob, bob.drawCenterX + offsetX, bob.drawCenterY + offsetY, out);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/GetNearestChild.js
var GetDistance$1 = __webpack_exports__Math.Distance.BetweenPointsSquared;
var GetNearestChild = function(canvasX, canvasY) {
	var pointA = {
		x: canvasX,
		y: canvasY
	};
	var minDistance = Infinity;
	var nearestChild = null;
	this.forEachRenderableChild(function(child) {
		var distance = GetDistance$1(pointA, GetBobCenterPosition(child, true));
		if (minDistance > distance) {
			minDistance = distance;
			nearestChild = child;
		}
	});
	return nearestChild;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/GetCharWorldPosition.js
var GetCharWorldPosition = function(child, offsetX, offsetY, out) {
	if (typeof child === "number") child = this.getCharChild(child, true);
	return GetBobWorldPosition(this, child, offsetX, offsetY, out);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/SetToMinSize.js
var SetToMinSize = function() {
	var children = this.children;
	var maxX = 0, maxY = 0;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		if (!child.renderable || !child.active || !child.visible) continue;
		var x0 = child.x0 !== void 0 ? child.x0 : child.x;
		var y0 = child.y0 !== void 0 ? child.y0 : child.y;
		maxX = Math.max(maxX, x0);
		maxY = Math.max(maxY, y0);
	}
	var width = maxX + this.padding.left + this.padding.right + this.wrapPadding.left + this.wrapPadding.right;
	var height = maxY + this.padding.top + this.padding.bottom + this.wrapPadding.top + this.wrapPadding.bottom;
	if (this.width !== width || this.height !== height) {
		this.dirty = true;
		this.setCanvasSize(width, height);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/GetCharChildIndex.js
var GetCharChildIndex = function(charIndex, activeOnly) {
	if (activeOnly === void 0) activeOnly = true;
	var children = this.children;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		if (activeOnly && !child.active) continue;
		if (IsChar(child) && !child.removed) {
			if (charIndex === 0) return i;
			else charIndex--;
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/GetCharChild.js
var GetCharChild = function(charIndex, activeOnly) {
	if (activeOnly === void 0) activeOnly = true;
	var children = this.children;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		if (activeOnly && !child.active) continue;
		if (IsChar(child) && !child.removed) {
			if (charIndex === 0) return child;
			else charIndex--;
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/GetCharIndex.js
var GetCharIndex = function(childIndex, activeOnly) {
	if (typeof childIndex !== "number") {
		childIndex = this.children.indexOf(childIndex);
		if (childIndex < 0) return null;
	}
	if (activeOnly === void 0) activeOnly = true;
	var children = this.children;
	if (childIndex >= children.length) childIndex = children.length;
	var charIndex = 0;
	for (var i = 0; i < childIndex; i++) {
		var child = children[i];
		if (activeOnly && !child.active) continue;
		if (IsChar(child) && !child.removed) charIndex++;
	}
	return charIndex;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/input/SetChildrenInteractiveEnable.js
var SetChildrenInteractiveEnable = function(enable) {
	if (enable === void 0) enable = true;
	if (this.childrenInteractiveEnable !== enable) this.lastOverChild = null;
	this.childrenInteractiveEnable = enable;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/input/GetFirstChildContains.js
var GetFirstChildContains = function(children, x, y) {
	var children = children;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		if (!child.active || !child.renderable) continue;
		if (child.contains(x, y)) return child;
	}
	return null;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/input/SetChildrenInteractive.js
var SetChildrenInteractive$2 = function() {
	this.on("pointerdown", OnPointerDown$1, this).on("pointerdown", OnPointerUp$1, this).on("pointermove", OnPointOverOut, this).on("pointerover", OnPointOverOut, this).on("pointerout", function(pointer, event) {
		OnPointOverOut.call(this, pointer, null, null, event);
	}, this);
	return this;
};
var OnPointerDown$1 = function(pointer, localX, localY, event) {
	if (!this.childrenInteractiveEnable) return;
	var child = GetFirstChildContains(this.children, localX, localY);
	if (!child) return;
	this.emit("child.pointerdown", child, pointer, localX, localY, event);
};
var OnPointerUp$1 = function(pointer, localX, localY, event) {
	if (!this.childrenInteractiveEnable) return;
	var child = GetFirstChildContains(this.children, localX, localY);
	if (!child) return;
	this.emit("child.pointerup", child, pointer, localX, localY, event);
};
var OnPointOverOut = function(pointer, localX, localY, event) {
	if (!this.childrenInteractiveEnable) return;
	if (localX === null) {
		if (this.lastOverChild !== null) {
			this.emit("child.pointerout", this.lastOverChild, pointer, localX, localY, event);
			this.lastOverChild = null;
		}
		return;
	}
	var child = GetFirstChildContains(this.children, localX, localY);
	if (child === this.lastOverChild) return;
	if (this.lastOverChild !== null) this.emit("child.pointerout", this.lastOverChild, pointer, localX, localY, event);
	if (child !== null) this.emit("child.pointerover", child, pointer, localX, localY, event);
	this.lastOverChild = child;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/input/SetInteractive.js
var GameObject = __webpack_exports__GameObjects.GameObject;
var SetInteractive = function(hitArea, hitAreaCallback, dropZone) {
	var isInteractived = !!this.input;
	GameObject.prototype.setInteractive.call(this, hitArea, hitAreaCallback, dropZone);
	if (!isInteractived) SetChildrenInteractive$2.call(this);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/MoveChildMethods.js
var BringToTop = __webpack_exports__Utils.Array.BringToTop;
var SendToBack = __webpack_exports__Utils.Array.SendToBack;
var MoveUp = __webpack_exports__Utils.Array.MoveUp;
var MoveDown = __webpack_exports__Utils.Array.MoveDown;
var MoveAbove = __webpack_exports__Utils.Array.MoveAbove;
var MoveBelow = __webpack_exports__Utils.Array.MoveBelow;
var MoveChildMethods_default = {
	moveChildToFist(child) {
		SendToBack(this.children, child);
		return this;
	},
	moveChildToLast(child) {
		BringToTop(this.children, child);
		return this;
	},
	movechildUp(child) {
		MoveUp(this.children, child);
		return this;
	},
	movechildDown(child) {
		MoveDown(this.children, child);
		return this;
	},
	movechildAbove(child, baseChild) {
		MoveAbove(this.children, child, baseChild);
		return this;
	},
	movechildBelow(child, baseChild) {
		MoveBelow(this.children, child, baseChild);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/BackgroundMethods.js
var BackgroundMethods_default = {
	setBackgroundColor(color, color2, isHorizontalGradient) {
		this.background.setColor(color, color2, isHorizontalGradient);
		return this;
	},
	setBackgroundStroke(color, lineWidth) {
		this.background.setStroke(color, lineWidth);
		return this;
	},
	setBackgroundCornerRadius(radius, iteration) {
		this.background.setCornerRadius(radius, iteration);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/InnerBoundsMethods.js
var InnerBoundsMethods_default = {
	setInnerBoundsColor(color, color2, isHorizontalGradient) {
		this.innerBounds.setColor(color, color2, isHorizontalGradient);
		return this;
	},
	setInnerBoundsStroke(color, lineWidth) {
		this.innerBounds.setStroke(color, lineWidth);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/methods/Methods.js
var Methods$22 = {
	setFixedSize: SetFixedSize,
	setPadding: SetPadding,
	getPadding: GetPadding,
	modifyTextStyle: ModifyTextStyle,
	modifyDefaultTextStyle: ModifyDefaultTextStyle,
	resetTextStyle: ResetTextStyle,
	setTestString: SetTestString,
	removeChild: RemoveChild$1,
	removeChildren: RemoveChildren,
	popChild: PopChild,
	clearContent: ClearContent,
	addChild: AddChild$2,
	createCharChild: CreateCharChild,
	createCharChildren: CreateCharChildren,
	setText: SetText$1,
	appendText: AppendText,
	insertText: InsertText,
	removeText: RemoveText,
	getText: GetText,
	createImageChild: CreateImageChild,
	appendImage: AppendImage,
	createDrawerChild: CreateDrawerChild,
	appendDrawer: AppendDrawer,
	createSpaceChild: CreateSpaceChild,
	appendSpace: AppendSpace,
	createCommandChild: CreateCommandChild,
	appendCommand: AppendCommand$3,
	setWrapConfig: SetWrapConfig,
	runWordWrap: RunWordWrap,
	runVerticalWrap: RunVerticalWrap,
	runWrap: RunWrap,
	renderContent: RenderContent,
	forEachChild: ForEachChild,
	forEachRenderableChild: ForEachRenderableChild,
	forEachCharChild: ForEachCharChild,
	getChildren: GetChildren,
	getActiveChildren: GetActiveChildren,
	getCharChildren: GetCharChildren,
	getLastAppendedChildren: GetLastAppendedChildren,
	getNearestChild: GetNearestChild,
	getCharWorldPosition: GetCharWorldPosition,
	setToMinSize: SetToMinSize,
	getCharChildIndex: GetCharChildIndex,
	getCharChild: GetCharChild,
	getCharIndex: GetCharIndex,
	setChildrenInteractiveEnable: SetChildrenInteractiveEnable,
	setInteractive: SetInteractive
};
Object.assign(Methods$22, MoveChildMethods_default, BackgroundMethods_default, InnerBoundsMethods_default, SetAlignMethods_default, SetTextOXYMethods_default$1);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/poolmanager/PoolManager.js
var GetFastValue$1 = __webpack_exports__Utils.Objects.GetFastValue;
var Pools = {};
var PoolManager = class {
	constructor(config) {
		this.pools = GetFastValue$1(config, "pools", Pools);
	}
	free(bob) {
		if (!this.pools) return this;
		var bobType = bob.type;
		if (!this.pools.hasOwnProperty(bobType)) this.pools[bobType] = new pool_default();
		this.pools[bobType].push(bob);
		bob.onFree();
		return this;
	}
	freeMultiple(arr) {
		if (!this.pools) return this;
		for (var i = 0, cnt = arr.length; i < cnt; i++) this.free(arr[i]);
		return this;
	}
	allocate(bobType) {
		if (!this.pools || !this.pools.hasOwnProperty(bobType)) return null;
		return this.pools[bobType].pop();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/DynamicText.js
var IsPlainObject$52 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$258 = __webpack_exports__Utils.Objects.GetValue;
var DynamicText = class extends Canvas$1 {
	constructor(scene, x, y, fixedWidth, fixedHeight, resolution, config) {
		if (IsPlainObject$52(x)) {
			config = x;
			x = GetValue$258(config, "x", 0);
			y = GetValue$258(config, "y", 0);
			fixedWidth = GetValue$258(config, "width", 0);
			fixedHeight = GetValue$258(config, "height", 0);
			resolution = GetValue$258(config, "resolution", 1);
		} else if (IsPlainObject$52(fixedWidth)) {
			config = fixedWidth;
			fixedWidth = GetValue$258(config, "width", 0);
			fixedHeight = GetValue$258(config, "height", 0);
			resolution = GetValue$258(config, "resolution", 1);
		} else if (IsPlainObject$52(resolution)) {
			config = resolution;
			resolution = GetValue$258(config, "resolution", 1);
		}
		super(scene, x, y, fixedWidth === 0 ? 1 : fixedWidth, fixedHeight === 0 ? 1 : fixedHeight, resolution);
		this.type = "rexDynamicText";
		this.autoRound = true;
		this.padding = SetPadding$1();
		this.wrapPadding = SetPadding$1();
		var textStyleConfig = GetValue$258(config, "style", void 0);
		this.defaultTextStyle = new TextStyle(null, textStyleConfig);
		this.textStyle = this.defaultTextStyle.clone();
		this.setTestString(GetValue$258(config, "testString", "|MÉqgy"));
		this._textOX = 0;
		this._textOY = 0;
		this.background = new Background(this, GetValue$258(config, "background", void 0));
		this.innerBounds = new InnerBounds(this, GetValue$258(config, "innerBounds", void 0));
		this.children = [];
		this.lastAppendedChildren = [];
		this.lastOverChild = null;
		this.poolManager = new PoolManager(config);
		this.setFixedSize(fixedWidth, fixedHeight);
		this.setPadding(GetValue$258(config, "padding", 0));
		this.setWrapConfig(GetValue$258(config, "wrap", void 0));
		this.setChildrenInteractiveEnable(GetValue$258(config, "childrenInteractive", false));
		var text = GetValue$258(config, "text", void 0);
		if (text) this.setText(text);
	}
	updateTexture() {
		super.updateTexture(function() {
			this.renderContent();
		}, this);
		return this;
	}
	get text() {
		return this.getText(true);
	}
	set text(value) {
		this.setText(value);
	}
	setSize(width, height) {
		this.setFixedSize(width, height);
		return this;
	}
	get textOX() {
		return this._textOX;
	}
	set textOX(value) {
		this.setTextOX(value);
	}
	get textOY() {
		return this._textOY;
	}
	set textOY(value) {
		this.setTextOY(value);
	}
};
Object.assign(DynamicText.prototype, Methods$22);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/dynamictext/DynamicText.js
var DynamicText_default = DynamicText;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/dynamictext/Factory.js
ObjectFactory.register("dynamicText", function(x, y, width, height, config) {
	var gameObject = new DynamicText_default(this.scene, x, y, width, height, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.DynamicText", DynamicText_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/eventemitter/EventEmitterMethods.js
var EventEmitterMethods_default = {
	setEventEmitter(eventEmitter, EventEmitterClass) {
		if (EventEmitterClass === void 0) EventEmitterClass = __webpack_exports__Events.EventEmitter;
		this._privateEE = eventEmitter === true || eventEmitter === void 0;
		this._eventEmitter = this._privateEE ? new EventEmitterClass() : eventEmitter;
		return this;
	},
	destroyEventEmitter() {
		if (this._eventEmitter && this._privateEE) this._eventEmitter.shutdown();
		return this;
	},
	getEventEmitter() {
		return this._eventEmitter;
	},
	on() {
		if (this._eventEmitter) this._eventEmitter.on.apply(this._eventEmitter, arguments);
		return this;
	},
	once() {
		if (this._eventEmitter) this._eventEmitter.once.apply(this._eventEmitter, arguments);
		return this;
	},
	off() {
		if (this._eventEmitter) this._eventEmitter.off.apply(this._eventEmitter, arguments);
		return this;
	},
	emit(event) {
		if (this._eventEmitter && event) this._eventEmitter.emit.apply(this._eventEmitter, arguments);
		return this;
	},
	addListener() {
		if (this._eventEmitter) this._eventEmitter.addListener.apply(this._eventEmitter, arguments);
		return this;
	},
	removeListener() {
		if (this._eventEmitter) this._eventEmitter.removeListener.apply(this._eventEmitter, arguments);
		return this;
	},
	removeAllListeners() {
		if (this._eventEmitter) this._eventEmitter.removeAllListeners.apply(this._eventEmitter, arguments);
		return this;
	},
	listenerCount() {
		if (this._eventEmitter) return this._eventEmitter.listenerCount.apply(this._eventEmitter, arguments);
		return 0;
	},
	listeners() {
		if (this._eventEmitter) return this._eventEmitter.listeners.apply(this._eventEmitter, arguments);
		return [];
	},
	eventNames() {
		if (this._eventEmitter) return this._eventEmitter.eventNames.apply(this._eventEmitter, arguments);
		return [];
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/gomanager/bobbase/PropertyMethods.js
var PropertyMethods_default$1 = {
	hasProperty(property) {
		var gameObject = this.gameObject;
		if (gameObject.hasOwnProperty(property)) return true;
		else return gameObject[property] !== void 0;
	},
	getProperty(property) {
		return this.gameObject[property];
	},
	setProperty(property, value) {
		this.gameObject[property] = value;
		return this;
	},
	easeProperty(config) {
		var property = config.property;
		var value = config.value;
		var duration = config.duration;
		var delay = config.delay;
		var ease = config.ease;
		var repeat = config.repeat;
		var isYoyo = config.yoyo;
		var isFrom = config.from;
		var onComplete = config.complete;
		var target = config.target;
		if (duration === void 0) duration = 1e3;
		if (delay === void 0) delay = 0;
		if (ease === void 0) ease = "Linear";
		if (repeat === void 0) repeat = 0;
		if (isYoyo === void 0) isYoyo = false;
		if (target === void 0) target = this.gameObject;
		if (isFrom) {
			var startValue = value;
			value = target[property];
			target[property] = startValue;
		}
		var config = {
			targets: target,
			duration,
			delay,
			ease,
			repeat,
			yoyo: isYoyo,
			onComplete
		};
		config[property] = value;
		this.addTweenTask(property, config);
		return this;
	},
	addTweenTask(name, config) {
		var tweenTasks = this.tweens;
		var tweenTask = tweenTasks[name];
		if (tweenTask) tweenTask.remove();
		var onComplete = config.onComplete;
		config.onComplete = function() {
			tweenTasks[name].remove();
			tweenTasks[name] = null;
			if (onComplete) onComplete(config.targets, name);
		};
		tweenTask = this.scene.tweens.add(config);
		tweenTask.timeScale = this.timeScale;
		tweenTasks[name] = tweenTask;
		return this;
	},
	getTweenTask(property) {
		return this.tweens[property];
	},
	freeTweens() {
		var tweenTasks = this.tweens, tweenTask;
		for (var propName in tweenTasks) {
			tweenTask = tweenTasks[propName];
			if (tweenTask) tweenTask.remove();
			tweenTasks[propName] = null;
		}
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/gomanager/bobbase/CallMethods.js
var CallMethods_default$1 = {
	hasMethod(methodName) {
		return typeof this.gameObject[methodName] === "function";
	},
	call(methodName, ...parameters) {
		if (!this.hasMethod(methodName)) {
			console.warn(`[GameObjectManager] Game object '${this.name}' does not have method '${methodName}'`);
			return this;
		}
		var gameObject = this.gameObject;
		gameObject[methodName].apply(gameObject, parameters);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/gomanager/bobbase/DataMethods.js
var DataMethods_default$1 = {
	hasData(dataKey) {
		var gameObject = this.gameObject;
		return gameObject.data ? gameObject.data.has(dataKey) : false;
	},
	getData(dataKey) {
		return this.gameObject.getData(dataKey);
	},
	setData(dataKey, value) {
		this.gameObject.setData(dataKey, value);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/gomanager/bobbase/BobBase.js
var BobBase = class {
	constructor(GOManager, gameObject, name) {
		this.GOManager = GOManager;
		this.tweens = {};
		this.effects = {};
		this.setGO(gameObject, name);
	}
	get scene() {
		return this.GOManager.scene;
	}
	get timeScale() {
		return this.GOManager.timeScale;
	}
	destroy() {
		this.freeGO();
		this.GOManager = void 0;
	}
	freeGO() {
		this.freeTweens();
		this.gameObject.bob = void 0;
		this.gameObject.destroy();
		this.gameObject = void 0;
		return this;
	}
	setGO(gameObject, name) {
		gameObject.goName = name;
		gameObject.goType = this.GOManager.name;
		gameObject.bob = this;
		this.gameObject = gameObject;
		this.name = name;
		this.freeTweens();
		return this;
	}
	setTimeScale(timeScale) {
		var tweenTasks = this.tweens;
		for (var key in tweenTasks) {
			var tweenTask = tweenTasks[key];
			if (tweenTask) tweenTask.timeScale = timeScale;
		}
		return this;
	}
};
Object.assign(BobBase.prototype, PropertyMethods_default$1, CallMethods_default$1, DataMethods_default$1);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/object/IsEmpty.js
var IsEmpty = function(source) {
	for (var k in source) return false;
	return true;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/gomanager/methods/IsSingleBob.js
var IsSingleBob = function(name) {
	return name && name.charAt(0) !== "!";
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/gomanager/methods/GetMethods.js
var GetMethods_default = {
	has(name) {
		return this.bobs.hasOwnProperty(name);
	},
	exists(name) {
		return this.bobs.hasOwnProperty(name);
	},
	get(name, out) {
		if (IsSingleBob(name)) return this.bobs[name];
		else {
			if (out === void 0) out = [];
			if (name) name = name.substring(1);
			for (var key in this.bobs) {
				if (name && key === name) continue;
				out.push(this.bobs[key]);
			}
			return out;
		}
	},
	getFitst(excluded) {
		if (excluded && excluded.charAt(0) === "!") excluded = excluded.substring(1);
		for (var name in this.bobs) {
			if (excluded && excluded === name) continue;
			return this.bobs[name];
		}
		return null;
	},
	getGO(name, out) {
		var bob = this.get(name);
		if (!bob) return null;
		else if (!Array.isArray(bob)) return bob.gameObject;
		else {
			if (out === void 0) out = [];
			bob.forEach(function(bob) {
				out.push(bob.gameObject);
			});
			return out;
		}
	},
	forEachGO(callback, scope) {
		for (var name in this.bobs) {
			var gameObject = this.bobs[name].gameObject;
			var stopLoop;
			if (scope) stopLoop = callback.call(scope, gameObject, name, this);
			else stopLoop = callback(gameObject, name, this);
			if (stopLoop) break;
		}
		return this;
	},
	getAllGO(out) {
		if (out === void 0) out = [];
		for (var name in this.bobs) {
			var gameObject = this.bobs[name].gameObject;
			out.push(gameObject);
		}
		return out;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/color/GetRGB.js
var GetR = function(colorInt) {
	return colorInt >> 16 & 255;
};
var GetG = function(colorInt) {
	return colorInt >> 8 & 255;
};
var GetB = function(colorInt) {
	return colorInt & 255;
};
var GetRGB = function(colorInt, out) {
	if (out === void 0) out = {};
	out.r = colorInt >> 16 & 255;
	out.g = colorInt >> 8 & 255;
	out.b = colorInt & 255;
	return out;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/color/SetColor.js
var MaskR = 65535;
var MaskG = 16711935;
var MaskB = 16776960;
var SetR = function(colorInt, r) {
	return (r & 255) << 16 | colorInt & MaskR;
};
var SetG = function(colorInt, g) {
	return (g & 255) << 8 | colorInt & MaskG;
};
var SetB = function(colorInt, b) {
	return b & 255 | colorInt & MaskB;
};
var SetRGB = function(colorInt, r, g, b) {
	return (r & 255) << 16 | (g & 255) << 8 | b & 255;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/tintrgb/AddTintRGBProperties.js
var AddTintRGBProperties = function(gameObject, tintRGB) {
	if (gameObject.hasOwnProperty("tintR")) return gameObject;
	if (tintRGB === void 0) tintRGB = 16777215;
	var tintR = GetR(tintRGB);
	var tintG = GetG(tintRGB);
	var tintB = GetB(tintRGB);
	Object.defineProperty(gameObject, "tint", {
		get: function() {
			return tintRGB;
		},
		set: function(value) {
			value = Math.floor(value) & 16777215;
			if (gameObject.setTint) gameObject.setTint(value);
			if (tintRGB !== value) {
				tintRGB = value;
				tintR = GetR(tintRGB);
				tintG = GetG(tintRGB);
				tintB = GetB(tintRGB);
			}
		}
	});
	Object.defineProperty(gameObject, "tintR", {
		get: function() {
			return tintR;
		},
		set: function(value) {
			value = Math.floor(value) & 255;
			if (tintR !== value) {
				tintR = value;
				gameObject.tint = SetR(tintRGB, value);
			}
		}
	});
	Object.defineProperty(gameObject, "tintG", {
		get: function() {
			return tintG;
		},
		set: function(value) {
			value = Math.floor(value) & 255;
			if (tintG !== value) {
				tintG = value;
				gameObject.tint = SetG(tintRGB, value);
			}
		}
	});
	Object.defineProperty(gameObject, "tintB", {
		get: function() {
			return tintB;
		},
		set: function(value) {
			value = Math.floor(value) & 255;
			if (tintB !== value) {
				tintB = value;
				gameObject.tint = SetB(tintRGB, value);
			}
		}
	});
	Object.defineProperty(gameObject, "tintGray", {
		get: function() {
			return Math.floor((tintR + tintG + tintB) / 3);
		},
		set: function(value) {
			value = Math.floor(value) & 255;
			if (tintR !== value || tintG !== value || tintB !== value) {
				tintR = value;
				tintG = value;
				tintB = value;
				gameObject.tint = SetRGB(tintRGB, value, value, value);
			}
		}
	});
	gameObject.tint = tintRGB;
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/viewportcoordinate/MonitorViewport.js
var EventEmitter$1 = __webpack_exports__Events.EventEmitter;
var MonitorViewport = function(viewport) {
	if (viewport.events) return viewport;
	var events = new EventEmitter$1();
	var x = viewport.x;
	Object.defineProperty(viewport, "x", {
		get: function() {
			return x;
		},
		set: function(value) {
			if (x !== value) {
				x = value;
				events.emit("update", viewport);
			}
		}
	});
	var y = viewport.y;
	Object.defineProperty(viewport, "y", {
		get: function() {
			return y;
		},
		set: function(value) {
			if (y !== value) {
				y = value;
				events.emit("update", viewport);
			}
		}
	});
	var width = viewport.width;
	Object.defineProperty(viewport, "width", {
		get: function() {
			return width;
		},
		set: function(value) {
			if (width !== value) {
				width = value;
				events.emit("update", viewport);
			}
		}
	});
	var height = viewport.height;
	Object.defineProperty(viewport, "height", {
		get: function() {
			return height;
		},
		set: function(value) {
			if (height !== value) {
				height = value;
				events.emit("update", viewport);
			}
		}
	});
	viewport.events = events;
	return viewport;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/viewportcoordinate/VPXYToXY.js
var VPXYToXY = function(vpx, vpy, vpxOffset, vpyOffset, viewport, out) {
	if (out === void 0) out = {};
	else if (out === true) out = GlobXY;
	if (typeof vpxOffset !== "number") {
		vpxOffset = 0;
		vpyOffset = 0;
	}
	out.x = viewport.x + viewport.width * vpx + vpxOffset;
	out.y = viewport.y + viewport.height * vpy + vpyOffset;
	return out;
};
var GlobXY = {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/viewportcoordinate/AddViewportCoordinateProperties.js
var AddViewportCoordinateProperties = function(gameObject, viewport, vpx, vpy, vpxOffset, vpyOffset, transformCallback) {
	if (gameObject.hasOwnProperty("vp")) return gameObject;
	if (typeof vpx === "function") {
		transformCallback = vpx;
		vpx = void 0;
	}
	if (typeof vpxOffset === "function") {
		transformCallback = vpxOffset;
		vpxOffset = void 0;
	}
	if (vpx === void 0) vpx = .5;
	if (vpy === void 0) vpy = .5;
	if (vpxOffset === void 0) vpxOffset = 0;
	if (vpyOffset === void 0) vpyOffset = 0;
	if (transformCallback === void 0) transformCallback = VPXYToXY;
	MonitorViewport(viewport);
	var events = viewport.events;
	gameObject.vp = viewport;
	var Transform = function() {
		transformCallback(vpx, vpy, vpxOffset, vpyOffset, viewport, gameObject);
	};
	events.on("update", Transform);
	gameObject.once("destroy", function() {
		events.off("update", Transform);
		gameObject.vp = void 0;
	});
	Object.defineProperty(gameObject, "vpx", {
		get: function() {
			return vpx;
		},
		set: function(value) {
			if (vpx !== value) {
				vpx = value;
				Transform();
			}
		}
	});
	Object.defineProperty(gameObject, "vpy", {
		get: function() {
			return vpy;
		},
		set: function(value) {
			if (vpy !== value) {
				vpy = value;
				Transform();
			}
		}
	});
	Object.defineProperty(gameObject, "vpxOffset", {
		get: function() {
			return vpxOffset;
		},
		set: function(value) {
			if (vpxOffset !== value) {
				vpxOffset = value;
				Transform();
			}
		}
	});
	Object.defineProperty(gameObject, "vpyOffset", {
		get: function() {
			return vpyOffset;
		},
		set: function(value) {
			if (vpyOffset !== value) {
				vpyOffset = value;
				Transform();
			}
		}
	});
	Transform();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/renderer/filterpluginbase/GetFilterList.js
var GetFilterList = function(gameObject, external) {
	if (external === void 0) external = false;
	if (!gameObject.filters) gameObject.enableFilters().focusFilters();
	return !external ? gameObject.filters.internal : gameObject.filters.external;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/AddClearEffectCallback.js
var AddClearEffectCallback = function(gameObject, effectSwitchName) {
	if (!gameObject._effectSwitchNames) {
		gameObject._effectSwitchNames = [];
		gameObject.clearAllEffects = function() {
			var effectSwitchNames = gameObject._effectSwitchNames;
			for (var i = 0, cnt = effectSwitchNames.length; i < cnt; i++) gameObject[effectSwitchNames[i]] = null;
			return gameObject;
		};
		gameObject.on("destroy", gameObject.clearAllEffects, gameObject);
	}
	gameObject._effectSwitchNames.push(effectSwitchName);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/AddBarrelProperties.js
var AddBarrelProperties = function(gameObject) {
	if (HasProperty(gameObject, "barrel")) return gameObject;
	var filterList = GetFilterList(gameObject);
	var barrel;
	Object.defineProperty(gameObject, "barrel", {
		get: function() {
			return barrel;
		},
		set: function(value) {
			if (barrel === value) return;
			barrel = value;
			if (barrel === null || barrel === false) {
				if (gameObject._barrelEffect) {
					filterList.remove(gameObject._barrelEffect);
					gameObject._barrelEffect = void 0;
				}
			} else {
				if (!gameObject._barrelEffect) gameObject._barrelEffect = filterList.addBarrel().setPaddingOverride(null);
				gameObject._barrelEffect.amount = barrel;
			}
		}
	});
	gameObject.barrel = null;
	AddClearEffectCallback(gameObject, "barrel");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/colormatrix/AddColorMatrixEffectPropertiesBase.js
var AddColorMatrixEffectPropertiesBase = function(gameObject, effectName, inputMode) {
	if (HasProperty(gameObject, effectName)) return gameObject;
	var filterList = GetFilterList(gameObject);
	var EffectInstancePropertyName = `_${effectName}Effect`;
	var currentValue;
	Object.defineProperty(gameObject, effectName, {
		get: function() {
			return currentValue;
		},
		set: function(value) {
			if (currentValue === value) return;
			currentValue = value;
			if (currentValue === null || currentValue === false) {
				if (gameObject[EffectInstancePropertyName]) {
					filterList.remove(gameObject[EffectInstancePropertyName]);
					gameObject[EffectInstancePropertyName] = void 0;
				}
			} else {
				if (!gameObject[EffectInstancePropertyName]) gameObject[EffectInstancePropertyName] = filterList.addColorMatrix();
				gameObject[EffectInstancePropertyName].colorMatrix[effectName](inputMode === 1 ? value : void 0);
			}
		}
	});
	gameObject[effectName] = null;
	AddClearEffectCallback(gameObject, effectName);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/colormatrix/AddBlackWhiteProperties.js
var AddBlackWhiteProperties = function(gameObject) {
	AddColorMatrixEffectPropertiesBase(gameObject, "blackWhite");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/AddBlockyProperties.js
var AddBlockyProperties = function(gameObject) {
	if (HasProperty(gameObject, "blockySize")) return gameObject;
	var filterList = GetFilterList(gameObject);
	var blockySize, blockyOffsetX = 0, blockyOffsetY = 0;
	Object.defineProperty(gameObject, "blockySize", {
		get: function() {
			return blockySize;
		},
		set: function(value) {
			if (blockySize === value) return;
			blockySize = value;
			if (blockySize === null || blockySize === false) {
				if (gameObject._blockyEffect) {
					filterList.remove(gameObject._blockyEffect);
					gameObject._blockyEffect = void 0;
				}
			} else {
				if (!gameObject._blockyEffect) gameObject._blockyEffect = filterList.addBlocky({
					size: blockySize,
					offset: {
						x: blockyOffsetX,
						y: blockyOffsetY
					}
				});
				gameObject._blockyEffect.size.x = blockySize;
				gameObject._blockyEffect.size.y = blockySize;
			}
		}
	});
	Object.defineProperty(gameObject, "blockyOffsetX", {
		get: function() {
			return blockyOffsetX;
		},
		set: function(value) {
			if (blockyOffsetX === value) return;
			blockyOffsetX = value;
			if (gameObject._blockyEffect) gameObject._blockyEffect.offset.x = blockyOffsetX;
		}
	});
	Object.defineProperty(gameObject, "blockyOffsetY", {
		get: function() {
			return blockyOffsetY;
		},
		set: function(value) {
			if (blockyOffsetY === value) return;
			blockyOffsetY = value;
			if (gameObject._blockyEffect) gameObject._blockyEffect.offset.y = blockyOffsetY;
		}
	});
	AddClearEffectCallback(gameObject, "blockySize");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/AddBloomProperties.js
var AddBloomProperties = function(gameObject) {
	if (HasProperty(gameObject, "bloomColor")) return gameObject;
	var filterList = GetFilterList(gameObject);
	var bloomColor, bloomOffsetX = 1, bloomOffsetY = 1, bloomBlurStrength = 1, bloomStrength = 1, bloomSteps = 4;
	var AddBloomEffect = function() {
		gameObject._bloom = __webpack_exports__Actions.AddEffectBloom(gameObject, {
			useInternal: true,
			blurRadius: Math.max(bloomOffsetX, bloomOffsetY),
			blurSteps: bloomSteps,
			blendAmount: bloomStrength
		})[0];
		gameObject._bloom.threshold.active = false;
		gameObject._bloom.blur.x = bloomOffsetX;
		gameObject._bloom.blur.y = bloomOffsetY;
		gameObject._bloom.blur.strength = bloomBlurStrength;
		gameObject._bloom.blur.color = bloomColor;
	};
	var RemoveBloomEffect = function() {
		if (gameObject._bloom) {
			filterList.remove(gameObject._bloom.parallelFilters);
			gameObject._bloom = void 0;
		}
	};
	Object.defineProperty(gameObject, "bloomColor", {
		get: function() {
			return bloomColor;
		},
		set: function(value) {
			if (bloomColor === value) return;
			bloomColor = value;
			if (bloomColor === null || bloomColor === false) RemoveBloomEffect();
			else {
				if (!gameObject._bloom) AddBloomEffect();
				gameObject._bloom.blur.color = bloomColor;
			}
		}
	});
	Object.defineProperty(gameObject, "bloomOffsetX", {
		get: function() {
			return bloomOffsetX;
		},
		set: function(value) {
			if (bloomOffsetX === value) return;
			bloomOffsetX = value;
			if (gameObject._bloom) gameObject._bloom.blur.x = bloomOffsetX;
		}
	});
	Object.defineProperty(gameObject, "bloomOffsetY", {
		get: function() {
			return bloomOffsetY;
		},
		set: function(value) {
			if (bloomOffsetY === value) return;
			bloomOffsetY = value;
			if (gameObject._bloom) gameObject._bloom.blur.y = bloomOffsetY;
		}
	});
	Object.defineProperty(gameObject, "bloomBlurStrength", {
		get: function() {
			return bloomBlurStrength;
		},
		set: function(value) {
			if (bloomBlurStrength === value) return;
			bloomBlurStrength = value;
			if (gameObject._bloom) gameObject._bloom.blur.strength = bloomBlurStrength;
		}
	});
	Object.defineProperty(gameObject, "bloomStrength", {
		get: function() {
			return bloomStrength;
		},
		set: function(value) {
			if (bloomStrength === value) return;
			bloomStrength = value;
			if (gameObject._bloom) gameObject._bloom.parallelFilters.blend.amount = bloomStrength;
		}
	});
	Object.defineProperty(gameObject, "bloomSteps", {
		get: function() {
			return bloomSteps;
		},
		set: function(value) {
			if (bloomSteps === value) return;
			bloomSteps = value;
			if (gameObject._bloom) gameObject._bloom.blur.steps = bloomSteps;
		}
	});
	gameObject.bloomColor = null;
	AddClearEffectCallback(gameObject, "bloomColor");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/AddBlurProperties.js
var AddBlurProperties = function(gameObject) {
	if (HasProperty(gameObject, "blurColor")) return gameObject;
	var filterList = GetFilterList(gameObject);
	var blurColor, blurQuality = 0, blurX = 1, blurY = 1, blurStrength = 1, blurSteps = 4;
	Object.defineProperty(gameObject, "blurColor", {
		get: function() {
			return blurColor;
		},
		set: function(value) {
			if (blurColor === value) return;
			blurColor = value;
			if (blurColor === null || blurColor === false) {
				if (gameObject._blur) {
					filterList.remove(gameObject._blur);
					gameObject._blur = void 0;
				}
			} else {
				if (!gameObject._blur) gameObject._blur = filterList.addBlur(blurQuality, blurX, blurY, blurStrength, blurColor, blurSteps);
				gameObject._blur.color = blurColor;
			}
		}
	});
	Object.defineProperty(gameObject, "blurQuality", {
		get: function() {
			return blurQuality;
		},
		set: function(value) {
			if (blurQuality === value) return;
			blurQuality = value;
			if (gameObject._blur) gameObject._blur.quality = blurQuality;
		}
	});
	Object.defineProperty(gameObject, "blurX", {
		get: function() {
			return blurX;
		},
		set: function(value) {
			if (blurX === value) return;
			blurX = value;
			if (gameObject._blur) gameObject._blur.x = blurX;
		}
	});
	Object.defineProperty(gameObject, "blurY", {
		get: function() {
			return blurY;
		},
		set: function(value) {
			if (blurY === value) return;
			blurY = value;
			if (gameObject._blur) gameObject._blur.y = blurY;
		}
	});
	Object.defineProperty(gameObject, "blurStrength", {
		get: function() {
			return blurStrength;
		},
		set: function(value) {
			if (blurStrength === value) return;
			blurStrength = value;
			if (gameObject._blur) gameObject._blur.strength = blurStrength;
		}
	});
	Object.defineProperty(gameObject, "blurSteps", {
		get: function() {
			return blurSteps;
		},
		set: function(value) {
			if (blurSteps === value) return;
			blurSteps = value;
			if (gameObject._blur) gameObject._blur.steps = blurSteps;
		}
	});
	gameObject.blurColor = null;
	AddClearEffectCallback(gameObject, "blurColor");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/AddBokehProperties.js
var AddBokehProperties = function(gameObject) {
	if (HasProperty(gameObject, "bokehRadius")) return gameObject;
	var filterList = GetFilterList(gameObject);
	var bokehRadius, bokehAmount = 1, bokehContrast = .2;
	Object.defineProperty(gameObject, "bokehRadius", {
		get: function() {
			return bokehRadius;
		},
		set: function(value) {
			if (bokehRadius === value) return;
			bokehRadius = value;
			if (bokehRadius === null || bokehRadius === false) {
				if (gameObject._bokeh) {
					filterList.remove(gameObject._bokeh);
					gameObject._bokeh = void 0;
				}
			} else {
				if (!gameObject._bokeh) gameObject._bokeh = filterList.addBokeh(bokehRadius, bokehAmount, bokehContrast);
				gameObject._bokeh.radius = bokehRadius;
			}
		}
	});
	Object.defineProperty(gameObject, "bokehAmount", {
		get: function() {
			return bokehAmount;
		},
		set: function(value) {
			if (bokehAmount === value) return;
			bokehAmount = value;
			if (gameObject._bokeh) gameObject._bokeh.amount = bokehAmount;
		}
	});
	Object.defineProperty(gameObject, "bokehContrast", {
		get: function() {
			return bokehContrast;
		},
		set: function(value) {
			if (bokehContrast === value) return;
			bokehContrast = value;
			if (gameObject._bokeh) gameObject._bokeh.contrast = bokehContrast;
		}
	});
	gameObject.bokehRadius = null;
	AddClearEffectCallback(gameObject, "bokehRadius");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/colormatrix/AddBrightnessProperties.js
var AddBrightnessProperties = function(gameObject) {
	AddColorMatrixEffectPropertiesBase(gameObject, "brightness", 1);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/colormatrix/AddBrownProperties.js
var AddBrownProperties = function(gameObject) {
	AddColorMatrixEffectPropertiesBase(gameObject, "brown");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/system/IsGame.js
var GameClass = __webpack_exports__Game;
var IsGame = function(object) {
	return object instanceof GameClass;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/system/IsSceneObject.js
var SceneClass = __webpack_exports__Scene;
var IsSceneObject = function(object) {
	return object instanceof SceneClass;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/system/GetGame.js
var GetGame = function(object) {
	if (object == null || typeof object !== "object") return null;
	else if (IsGame(object)) return object;
	else if (IsGame(object.game)) return object.game;
	else if (IsSceneObject(object)) return object.sys.game;
	else if (IsSceneObject(object.scene)) return object.scene.sys.game;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/renderer/filterpluginbase/RegisterFilter.js
var RegisterFilter = function(game, FilterClass) {
	var filterName = FilterClass.FilterName;
	var renderNodes = GetGame(game).renderer.renderNodes;
	if (renderNodes.hasNode(filterName)) return false;
	renderNodes.addNodeConstructor(filterName, FilterClass);
	return true;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/renderer/AddFilterListMethod.js
var AddFilterListMethod = function(name, callback) {
	var FilterListComponent = __webpack_exports__GameObjects.Components.FilterList.prototype;
	if (FilterListComponent[name]) return;
	FilterListComponent[name] = callback;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/shaders/circle/const.js
var FilterName$2 = "FilterP3Circle";
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/shaders/circle/circle-frag.js
var frag$2 = `\
#pragma phaserTemplate(shaderName)

#ifdef GL_FRAGMENT_PRECISION_HIGH
#define highmedp highp
#else
#define highmedp mediump
#endif
precision highmedp float;

// Scene buffer
uniform sampler2D uMainSampler; 
varying vec2 outTexCoord;

// Effect parameters
uniform vec2 texSize;
uniform vec3 color;
uniform vec4 backgroundColor;
uniform vec3 config;

#pragma phaserTemplate(fragmentHeader)

void main (void) {
    float thickness = config.x;
    float scale = config.y;
    float feather = config.z;

    vec4 texture = texture2D(uMainSampler, outTexCoord);

    vec2 position = (gl_FragCoord.xy / texSize.xy) * 2.0 - 1.0;

    float aspectRatio = texSize.x / texSize.y;

    position.x *= aspectRatio;

    float grad = length(position);

    //  height > width
    float outer = aspectRatio;
    float inner = outer - (thickness * 2.0 / texSize.y);

    //  width > height
    if (aspectRatio >= 1.0)
    {
        float f = 2.0 + (texSize.y / texSize.x);
        outer = 1.0;
        inner = 1.0 - (thickness * f / texSize.x);
    }

    outer *= scale;
    inner *= scale;

    float circle = smoothstep(outer, outer - 0.01, grad);

    float ring = circle - smoothstep(inner, inner - feather, grad);

    texture = mix(backgroundColor * backgroundColor.a, texture, texture.a);

    texture = (texture * (circle - ring));

    gl_FragColor = vec4(texture.rgb + (ring * color), texture.a);
}
`;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/shaders/circle/CircleFilter.js
var CircleFilter = class extends __webpack_exports__Renderer.WebGL.RenderNodes.BaseFilterShader {
	static FilterName = FilterName$2;
	constructor(manager) {
		super(FilterName$2, manager, null, frag$2);
	}
	setupUniforms(controller, drawingContext) {
		const programManager = this.programManager;
		programManager.setUniform("texSize", [drawingContext.width, drawingContext.height]);
		programManager.setUniform("color", controller.glcolor);
		programManager.setUniform("backgroundColor", controller.glcolor2);
		programManager.setUniform("config", [
			controller.thickness,
			controller.scale,
			controller.feather
		]);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/shaders/circle/CircleController.js
var GetValue$257 = __webpack_exports__Utils.Objects.GetValue;
var CircleController = class extends __webpack_exports__Filters.Controller {
	static FilterName = FilterName$2;
	constructor(camera, config) {
		super(camera, FilterName$2);
		this.thickness = 8;
		this.scale = 1;
		this.feather = .005;
		this.glcolor = [
			1,
			.2,
			.7
		];
		this.glcolor2 = [
			1,
			0,
			0,
			.4
		];
		this.resetFromJSON(config);
	}
	resetFromJSON(o) {
		this.setThickness(GetValue$257(o, "thickness", 8));
		this.setScale(GetValue$257(o, "scale", 1));
		this.setFeather(GetValue$257(o, "feather", .005));
		this.setColor(GetValue$257(o, "color", 16724914));
		this.setBackgroundColor(GetValue$257(o, "backgroundColor", 16711680));
		this.setBackgroundAlpha(GetValue$257(o, "backgroundAlpha", .4));
		return this;
	}
	get color() {
		var color = this.glcolor;
		return (color[0] * 255 << 16) + (color[1] * 255 << 8) + (color[2] * 255 | 0);
	}
	set color(value) {
		var color = this.glcolor;
		color[0] = (value >> 16 & 255) / 255;
		color[1] = (value >> 8 & 255) / 255;
		color[2] = (value & 255) / 255;
	}
	get backgroundColor() {
		var color = this.glcolor2;
		return (color[0] * 255 << 16) + (color[1] * 255 << 8) + (color[2] * 255 | 0);
	}
	set backgroundColor(value) {
		var color = this.glcolor2;
		color[0] = (value >> 16 & 255) / 255;
		color[1] = (value >> 8 & 255) / 255;
		color[2] = (value & 255) / 255;
	}
	get backgroundAlpha() {
		return this.glcolor2[3];
	}
	set backgroundAlpha(value) {
		var color = this.glcolor2;
		color[3] = value;
	}
	setThickness(thickness) {
		this.thickness = thickness;
		return this;
	}
	setScale(scale) {
		this.scale = scale;
		return this;
	}
	setFeather(feather) {
		this.feather = feather;
		return this;
	}
	setColor(color) {
		this.color = color;
		return this;
	}
	setBackgroundColor(color) {
		this.backgroundColor = color;
		return this;
	}
	setBackgroundAlpha(alpha) {
		this.backgroundAlpha = alpha;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/shaders/p3fx/InstallCircleFX.js
var InstallCircleFX = function(game) {
	game = GetGame(game);
	if (!RegisterFilter(game, CircleFilter)) return false;
	AddFilterListMethod("addP3Bloom", function(color, offsetX, offsetY, blurStrength, strength, steps) {
		if (color === void 0) color = 16777215;
		if (offsetX === void 0) offsetX = 1;
		if (offsetY === void 0) offsetY = 1;
		if (blurStrength === void 0) blurStrength = 1;
		if (strength === void 0) strength = 1;
		if (steps === void 0) steps = 4;
		return this.add(new BloomController(this.camera, {
			color,
			offsetX,
			offsetY,
			blurStrength,
			strength,
			steps
		}));
	});
	AddFilterListMethod("addP3Circle", function(thickness, color, backgroundColor, scale, feather) {
		if (thickness === void 0) thickness = 8;
		if (color === void 0) color = 16724914;
		if (backgroundColor === void 0) backgroundColor = 16711680;
		if (scale === void 0) scale = 1;
		if (feather === void 0) feather = .005;
		return this.add(new CircleController(this.camera, {
			thickness,
			color,
			backgroundColor,
			scale,
			feather
		}));
	});
	return true;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/AddCircleProperties.js
var AddCircleProperties = function(gameObject) {
	if (HasProperty(gameObject, "circleColor")) return gameObject;
	InstallCircleFX(gameObject);
	var filterList = GetFilterList(gameObject);
	var circleColor, circleThickness = 8, circleBackgroundColor = 0, circleBackgroundAlpha = .4, circleScale = 1, circleFeather = .005;
	Object.defineProperty(gameObject, "circleColor", {
		get: function() {
			return circleColor;
		},
		set: function(value) {
			if (circleColor === value) return;
			circleColor = value;
			if (circleColor === null || circleColor === false) {
				if (gameObject._circle) {
					filterList.remove(gameObject._circle);
					gameObject._circle = void 0;
				}
			} else {
				if (!gameObject._circle) {
					gameObject._circle = filterList.addP3Circle(circleThickness, circleColor, circleBackgroundColor, circleScale, circleFeather);
					gameObject.circleBackgroundAlpha = circleBackgroundAlpha;
				}
				gameObject._circle.color = circleColor;
			}
		}
	});
	Object.defineProperty(gameObject, "circleThickness", {
		get: function() {
			return circleThickness;
		},
		set: function(value) {
			if (circleThickness === value) return;
			circleThickness = value;
			if (gameObject._circle) gameObject._circle.thickness = circleThickness;
		}
	});
	Object.defineProperty(gameObject, "circleBackgroundColor", {
		get: function() {
			return circleBackgroundColor;
		},
		set: function(value) {
			if (circleBackgroundColor === value) return;
			circleBackgroundColor = value;
			if (gameObject._circle) gameObject._circle.backgroundColor = circleBackgroundColor;
		}
	});
	Object.defineProperty(gameObject, "circleBackgroundAlpha", {
		get: function() {
			return circleBackgroundAlpha;
		},
		set: function(value) {
			if (circleBackgroundAlpha === value) return;
			circleBackgroundAlpha = value;
			if (gameObject._circle) gameObject._circle.glcolor2[3] = circleBackgroundAlpha;
		}
	});
	Object.defineProperty(gameObject, "circleScale", {
		get: function() {
			return circleScale;
		},
		set: function(value) {
			if (circleScale === value) return;
			circleScale = value;
			if (gameObject._circle) gameObject._circle.scale = circleScale;
		}
	});
	Object.defineProperty(gameObject, "circleFeather", {
		get: function() {
			return circleFeather;
		},
		set: function(value) {
			if (circleFeather === value) return;
			circleFeather = value;
			if (gameObject._circle) gameObject._circle.feather = circleFeather;
		}
	});
	gameObject.circleColor = null;
	AddClearEffectCallback(gameObject, "circleColor");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/colormatrix/AddContrastProperties.js
var AddContrastProperties = function(gameObject) {
	AddColorMatrixEffectPropertiesBase(gameObject, "contrast", 1);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/colormatrix/AddDesaturateProperties.js
var AddDesaturateProperties = function(gameObject) {
	AddColorMatrixEffectPropertiesBase(gameObject, "desaturate", 1);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/colormatrix/AddDesaturateLuminanceProperties.js
var AddDesaturateLuminanceProperties = function(gameObject) {
	AddColorMatrixEffectPropertiesBase(gameObject, "desaturateLuminance");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/AddDisplacementProperties.js
var AddDisplacementProperties = function(gameObject) {
	if (HasProperty(gameObject, "displacementKey")) return gameObject;
	var filterList = GetFilterList(gameObject);
	var displacementKey, displacementX = .005, displacementY = .005;
	Object.defineProperty(gameObject, "displacementKey", {
		get: function() {
			return displacementKey;
		},
		set: function(value) {
			if (displacementKey === value) return;
			displacementKey = value;
			if (displacementKey === null || displacementKey === false) {
				if (gameObject._displacement) {
					filterList.remove(gameObject._displacement);
					gameObject._displacement = void 0;
				}
			} else {
				if (!gameObject._displacement) gameObject._displacement = filterList.addDisplacement(displacementKey, displacementX, displacementY);
				gameObject._displacement.setTexture(displacementKey);
			}
		}
	});
	Object.defineProperty(gameObject, "displacementX", {
		get: function() {
			return displacementX;
		},
		set: function(value) {
			if (displacementX === value) return;
			displacementX = value;
			if (gameObject._displacement) gameObject._displacement.x = displacementX;
		}
	});
	Object.defineProperty(gameObject, "displacementY", {
		get: function() {
			return displacementY;
		},
		set: function(value) {
			if (displacementY === value) return;
			displacementY = value;
			if (gameObject._displacement) gameObject._displacement.y = displacementY;
		}
	});
	gameObject.displacementKey = null;
	AddClearEffectCallback(gameObject, "displacementKey");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/AddGlowProperties.js
var AddGlowProperties = function(gameObject) {
	if (HasProperty(gameObject, "glowColor")) return gameObject;
	var filterList = GetFilterList(gameObject);
	var glowColor, glowOuterStrength = 4, glowInnerStrength = 0;
	Object.defineProperty(gameObject, "glowColor", {
		get: function() {
			return glowColor;
		},
		set: function(value) {
			if (glowColor === value) return;
			glowColor = value;
			if (glowColor === null || glowColor === false) {
				if (gameObject._glow) {
					filterList.remove(gameObject._glow);
					gameObject._glow = void 0;
				}
			} else {
				if (!gameObject._glow) gameObject._glow = filterList.addGlow(glowColor, glowOuterStrength, glowInnerStrength).setPaddingOverride(null);
				gameObject._glow.color = glowColor;
			}
		}
	});
	Object.defineProperty(gameObject, "glowOuterStrength", {
		get: function() {
			return glowOuterStrength;
		},
		set: function(value) {
			if (glowOuterStrength === value) return;
			glowOuterStrength = value;
			if (gameObject._glow) gameObject._glow.outerStrength = glowOuterStrength;
		}
	});
	Object.defineProperty(gameObject, "glowInnerStrength", {
		get: function() {
			return glowInnerStrength;
		},
		set: function(value) {
			if (glowInnerStrength === value) return;
			glowInnerStrength = value;
			if (gameObject._glow) gameObject._glow.innerStrength = glowInnerStrength;
		}
	});
	gameObject.glowColor = null;
	AddClearEffectCallback(gameObject, "glowColor");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/shaders/gradient/const.js
var FilterName$1 = "FilterP3Gradient";
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/shaders/gradient/gradient-frag.js
var frag$1 = `\
#pragma phaserTemplate(shaderName)

#ifdef GL_FRAGMENT_PRECISION_HIGH
#define highmedp highp
#else
#define highmedp mediump
#endif
precision highmedp float;

#define SRGB_TO_LINEAR(c) pow((c), vec3(2.2))
#define LINEAR_TO_SRGB(c) pow((c), vec3(1.0 / 2.2))
#define SRGB(r, g, b) SRGB_TO_LINEAR(vec3(float(r), float(g), float(b)) / 255.0)

// Scene buffer
uniform sampler2D uMainSampler; 
varying vec2 outTexCoord;

// Effect parameters
uniform float alpha;
uniform vec2 positionFrom;
uniform vec2 positionTo;
uniform vec3 color1;
uniform vec3 color2;
uniform int size;

#pragma phaserTemplate(fragmentHeader)

float gradientNoise(in vec2 uv)
{
    const vec3 magic = vec3(0.06711056, 0.00583715, 52.9829189);
    return fract(magic.z * fract(dot(uv, magic.xy)));
}

float stepped (in float s, in float scale, in int steps)
{
    return steps > 0 ? floor( s / ((1.0 * scale) / float(steps))) * 1.0 / float(steps - 1) : s;
}

void main ()
{
    vec2 a = positionFrom;
    vec2 b = positionTo;
    vec2 ba = b - a;

    float d = dot(outTexCoord - a, ba) / dot(ba, ba);
    float t = size > 0 ? stepped(d, 1.0, size) : d;

    t = smoothstep(0.0, 1.0, clamp(t, 0.0, 1.0));

    vec3 color = mix(SRGB(color1.r, color1.g, color1.b), SRGB(color2.r, color2.g, color2.b), t);

    color = LINEAR_TO_SRGB(color);
    color += (1.0 / 255.0) * gradientNoise(outTexCoord) - (0.5 / 255.0);

    vec4 texture = texture2D(uMainSampler, outTexCoord);

    gl_FragColor = vec4(mix(color.rgb, texture.rgb, alpha), 1.0) * texture.a;
}
`;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/shaders/gradient/GradientFilter.js
var GradientFilter = class extends __webpack_exports__Renderer.WebGL.RenderNodes.BaseFilterShader {
	static FilterName = FilterName$1;
	constructor(manager) {
		super(FilterName$1, manager, null, frag$1);
	}
	setupUniforms(controller, drawingContext) {
		const programManager = this.programManager;
		programManager.setUniform("alpha", controller.alpha);
		programManager.setUniform("positionFrom", [controller.fromX, controller.fromY]);
		programManager.setUniform("positionTo", [controller.toX, controller.toY]);
		programManager.setUniform("color1", controller.glcolor1);
		programManager.setUniform("color2", controller.glcolor2);
		programManager.setUniform("size", controller.size);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/shaders/gradient/GradientController.js
var GetValue$256 = __webpack_exports__Utils.Objects.GetValue;
var GradientController = class extends __webpack_exports__Filters.Controller {
	static FilterName = FilterName$1;
	constructor(camera, config) {
		super(camera, FilterName$1);
		this.alpha = .2;
		this.fromX = 0;
		this.fromY = 0;
		this.toX = 0;
		this.toY = 1;
		this.glcolor1 = [
			255,
			0,
			0
		];
		this.glcolor2 = [
			0,
			255,
			0
		];
		this.size = 0;
		this.resetFromJSON(config);
	}
	resetFromJSON(o) {
		this.setAlpha(GetValue$256(o, "alpha", .2));
		this.setFromPosition(GetValue$256(o, "fromX", 0), GetValue$256(o, "fromY", 0));
		this.setToPosition(GetValue$256(o, "toX", 0), GetValue$256(o, "toY", 1));
		this.setColor1(GetValue$256(o, "color1", 16711680));
		this.setColor2(GetValue$256(o, "color2", 65280));
		this.setSize(GetValue$256(o, "size", 0));
		return this;
	}
	get color1() {
		var color = this.glcolor1;
		return (color[0] << 16) + (color[1] << 8) + (color[2] | 0);
	}
	set color1(value) {
		var color = this.glcolor1;
		color[0] = value >> 16 & 255;
		color[1] = value >> 8 & 255;
		color[2] = value & 255;
	}
	get color2() {
		var color = this.glcolor2;
		return (color[0] << 16) + (color[1] << 8) + (color[2] | 0);
	}
	set color2(value) {
		var color = this.glcolor2;
		color[0] = value >> 16 & 255;
		color[1] = value >> 8 & 255;
		color[2] = value & 255;
	}
	setAlpha(alpha) {
		this.alpha = alpha;
		return this;
	}
	setFromPosition(x, y) {
		this.fromX = x;
		this.fromY = y;
		return this;
	}
	setToPosition(x, y) {
		this.toX = x;
		this.toY = y;
		return this;
	}
	setColor1(color1) {
		this.color1 = color1;
		return this;
	}
	setColor2(color2) {
		this.color2 = color2;
		return this;
	}
	setSize(size) {
		this.size = size;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/shaders/p3fx/InstallGradientFX.js
var InstallGradientFX = function(game) {
	game = GetGame(game);
	if (!RegisterFilter(game, GradientFilter)) return false;
	AddFilterListMethod("addP3Gradient", function(color1, color2, alpha, fromX, fromY, toX, toY, size) {
		if (color1 === void 0) color1 = 16711680;
		if (color2 === void 0) color2 = 65280;
		if (alpha === void 0) alpha = .2;
		if (fromX === void 0) fromX = 0;
		if (fromY === void 0) fromY = 0;
		if (toX === void 0) toX = 0;
		if (toY === void 0) toY = 1;
		if (size === void 0) size = 0;
		return this.add(new GradientController(this.camera, {
			color1,
			color2,
			alpha,
			fromX,
			fromY,
			toX,
			toY,
			size
		}));
	});
	return true;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/AddGradientProperties.js
var AddGradientProperties = function(gameObject) {
	if (HasProperty(gameObject, "gradientColor")) return gameObject;
	InstallGradientFX(gameObject);
	var filterList = GetFilterList(gameObject);
	var gradientColor1, gradientColor2, gradientAlpha = .5, gradientFromX = 0, gradientFromY = 0, gradientToX = 0, gradientToY = 1, gradientSize = 0;
	Object.defineProperty(gameObject, "gradientColor", {
		get: function() {
			return [gradientColor1, gradientColor2];
		},
		set: function(value) {
			var color1, color2;
			if (value === null || value === false) {
				color1 = null;
				color2 = null;
			} else {
				color1 = value[0];
				color2 = value[1];
			}
			if (gradientColor1 === color1 && gradientColor2 === color2) return;
			gradientColor1 = color1;
			gradientColor2 = color2;
			if (gradientColor1 === null || gradientColor1 === false) {
				if (gameObject._gradient) {
					filterList.remove(gameObject._gradient);
					gameObject._gradient = void 0;
				}
			} else {
				if (!gameObject._gradient) gameObject._gradient = filterList.addP3Gradient(gradientColor1, gradientColor2, gradientAlpha, gradientFromX, gradientFromY, gradientToX, gradientToY, gradientSize);
				gameObject._gradient.color1 = gradientColor1;
				gameObject._gradient.color2 = gradientColor2;
			}
		}
	});
	Object.defineProperty(gameObject, "gradientColor1", {
		get: function() {
			return gradientColor1;
		},
		set: function(value) {
			if (value === null || value === false) {
				gameObject.gradientColor = value;
				return;
			}
			if (gradientColor1 === value) return;
			gradientColor1 = value;
			if (gameObject._gradient) gameObject._gradient.color1 = gradientColor1;
		}
	});
	Object.defineProperty(gameObject, "gradientColor2", {
		get: function() {
			return gradientColor2;
		},
		set: function(value) {
			if (value === null || value === false) {
				gameObject.gradientColor = value;
				return;
			}
			if (gradientColor2 === value) return;
			gradientColor2 = value;
			if (gameObject._gradient) gameObject._gradient.color2 = gradientColor2;
		}
	});
	Object.defineProperty(gameObject, "gradientAlpha", {
		get: function() {
			return gradientAlpha;
		},
		set: function(value) {
			if (gradientAlpha === value) return;
			gradientAlpha = value;
			if (gameObject._gradient) gameObject._gradient.alpha = gradientAlpha;
		}
	});
	Object.defineProperty(gameObject, "gradientFromX", {
		get: function() {
			return gradientFromX;
		},
		set: function(value) {
			if (gradientFromX === value) return;
			gradientFromX = value;
			if (gameObject._gradient) gameObject._gradient.fromX = gradientFromX;
		}
	});
	Object.defineProperty(gameObject, "gradientFromY", {
		get: function() {
			return gradientFromY;
		},
		set: function(value) {
			if (gradientFromY === value) return;
			gradientFromY = value;
			if (gameObject._gradient) gameObject._gradient.fromY = gradientFromY;
		}
	});
	Object.defineProperty(gameObject, "gradientToX", {
		get: function() {
			return gradientToX;
		},
		set: function(value) {
			if (gradientToX === value) return;
			gradientToX = value;
			if (gameObject._gradient) gameObject._gradient.toX = gradientToX;
		}
	});
	Object.defineProperty(gameObject, "gradientToY", {
		get: function() {
			return gradientToY;
		},
		set: function(value) {
			if (gradientToY === value) return;
			gradientToY = value;
			if (gameObject._gradient) gameObject._gradient.toY = gradientToY;
		}
	});
	Object.defineProperty(gameObject, "gradientSize", {
		get: function() {
			return gradientSize;
		},
		set: function(value) {
			if (gradientSize === value) return;
			gradientSize = value;
			if (gameObject._gradient) gameObject._gradient.size = gradientSize;
		}
	});
	gameObject.gradientColor = null;
	AddClearEffectCallback(gameObject, "gradientColor");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/colormatrix/AddGrayscaleProperties.js
var AddGrayscaleProperties = function(gameObject) {
	AddColorMatrixEffectPropertiesBase(gameObject, "grayscale", 1);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/colormatrix/AddHueProperties.js
var AddHueProperties = function(gameObject) {
	AddColorMatrixEffectPropertiesBase(gameObject, "hue", 1);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/colormatrix/AddKodachromeProperties.js
var AddKodachromeProperties = function(gameObject) {
	AddColorMatrixEffectPropertiesBase(gameObject, "kodachrome");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/colormatrix/AddLSDProperties.js
var AddLSDProperties = function(gameObject) {
	AddColorMatrixEffectPropertiesBase(gameObject, "lsd");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/colormatrix/AddNegativeProperties.js
var AddNegativeProperties = function(gameObject) {
	AddColorMatrixEffectPropertiesBase(gameObject, "negative");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/AddPixelateProperties.js
var AddPixelateProperties = function(gameObject) {
	if (HasProperty(gameObject, "pixelate")) return gameObject;
	var filterList = GetFilterList(gameObject);
	var pixelate;
	Object.defineProperty(gameObject, "pixelate", {
		get: function() {
			return pixelate;
		},
		set: function(value) {
			if (pixelate === value) return;
			pixelate = value;
			if (pixelate === null || pixelate === false) {
				if (gameObject._pixelateEffect) {
					filterList.remove(gameObject._pixelateEffect);
					gameObject._pixelateEffect = void 0;
				}
			} else {
				if (!gameObject._pixelateEffect) gameObject._pixelateEffect = filterList.addPixelate();
				gameObject._pixelateEffect.amount = pixelate;
			}
		}
	});
	gameObject.pixelate = null;
	AddClearEffectCallback(gameObject, "pixelate");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/colormatrix/AddPolaroidProperties.js
var AddPolaroidProperties = function(gameObject) {
	AddColorMatrixEffectPropertiesBase(gameObject, "polaroid");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/math/Clamp.js
/**
* @author       Richard Davey <rich@photonstorm.com>
* @copyright    2019 Photon Storm Ltd.
* @license      {@link https://opensource.org/licenses/MIT|MIT License}
*/
/**
* Force a value within the boundaries by clamping it to the range `min`, `max`.
*
* @function Phaser.Math.Clamp
* @since 3.0.0
*
* @param {number} value - The value to be clamped.
* @param {number} min - The minimum bounds.
* @param {number} max - The maximum bounds.
*
* @return {number} The clamped value.
*/
var Clamp$17 = function(value, min, max) {
	return Math.max(min, Math.min(max, value));
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/AddRevealProperties.js
var AddRevealProperties = function(gameObject) {
	if (HasProperty(gameObject, "revealLeft")) return gameObject;
	var filterList = GetFilterList(gameObject);
	var revealLeft, revealRight, revealUp, revealDown, revealWidth = .1;
	var ClearRevealFlags = function() {
		revealLeft = null;
		revealRight = null;
		revealUp = null;
		revealDown = null;
	};
	var RemoveEffect = function(gameObject) {
		if (gameObject._revealEffect) {
			filterList.remove(gameObject._revealEffect);
			gameObject._revealEffect = void 0;
		}
	};
	Object.defineProperty(gameObject, "revealLeft", {
		get: function() {
			return revealLeft;
		},
		set: function(value) {
			if (revealLeft === value) return;
			ClearRevealFlags();
			revealLeft = value;
			if (revealLeft === null || revealLeft === false) RemoveEffect(gameObject);
			else {
				revealLeft = Clamp$17(revealLeft, 0, 1);
				if (!gameObject._revealEffect) gameObject._revealEffect = filterList.addWipe(revealWidth, 0, 0, 1);
				gameObject._revealEffect.direction = 1;
				gameObject._revealEffect.axis = 0;
				gameObject._revealEffect.progress = revealLeft;
			}
		}
	});
	Object.defineProperty(gameObject, "revealRight", {
		get: function() {
			return revealRight;
		},
		set: function(value) {
			if (revealRight === value) return;
			ClearRevealFlags();
			revealRight = value;
			if (revealRight === null || revealRight === false) RemoveEffect(gameObject);
			else {
				revealRight = Clamp$17(revealRight, 0, 1);
				if (!gameObject._revealEffect) gameObject._revealEffect = filterList.addWipe(revealWidth, 0, 0, 1);
				gameObject._revealEffect.direction = 0;
				gameObject._revealEffect.axis = 0;
				gameObject._revealEffect.progress = revealRight;
			}
		}
	});
	Object.defineProperty(gameObject, "revealUp", {
		get: function() {
			return revealUp;
		},
		set: function(value) {
			if (revealUp === value) return;
			ClearRevealFlags();
			revealUp = value;
			if (revealUp === null || revealUp === false) RemoveEffect(gameObject);
			else {
				revealUp = Clamp$17(revealUp, 0, 1);
				if (!gameObject._revealEffect) gameObject._revealEffect = filterList.addWipe(revealWidth, 0, 0, 1);
				gameObject._revealEffect.direction = 1;
				gameObject._revealEffect.axis = 1;
				gameObject._revealEffect.progress = revealUp;
			}
		}
	});
	Object.defineProperty(gameObject, "revealDown", {
		get: function() {
			return revealDown;
		},
		set: function(value) {
			if (revealDown === value) return;
			ClearRevealFlags();
			revealDown = value;
			if (revealDown === null || revealDown === false) RemoveEffect(gameObject);
			else {
				revealDown = Clamp$17(revealDown, 0, 1);
				if (!gameObject._revealEffect) gameObject._revealEffect = filterList.addWipe(revealWidth, 0, 0, 1);
				gameObject._revealEffect.direction = 0;
				gameObject._revealEffect.axis = 1;
				gameObject._revealEffect.progress = revealDown;
			}
		}
	});
	Object.defineProperty(gameObject, "revealWidth", {
		get: function() {
			return revealWidth;
		},
		set: function(value) {
			if (revealWidth === value) return;
			revealWidth = Clamp$17(value, 0, 1);
			if (gameObject._revealEffect) gameObject._revealEffect.wipeWidth = revealWidth;
		}
	});
	gameObject.revealLeft = null;
	AddClearEffectCallback(gameObject, "revealLeft");
	AddClearEffectCallback(gameObject, "revealRight");
	AddClearEffectCallback(gameObject, "revealUp");
	AddClearEffectCallback(gameObject, "revealDown");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/colormatrix/AddSaturateProperties.js
var AddSaturateProperties = function(gameObject) {
	AddColorMatrixEffectPropertiesBase(gameObject, "saturate", 1);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/colormatrix/AddSepiaProperties.js
var AddSepiaProperties = function(gameObject) {
	AddColorMatrixEffectPropertiesBase(gameObject, "sepia");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/AddShadowProperties.js
var AddShadowProperties = function(gameObject) {
	if (HasProperty(gameObject, "shadowColor")) return gameObject;
	var filterList = GetFilterList(gameObject);
	var shadowColor, shadowX = 0, shadowY = 0, shadowDecay = .1, shadowPower = 1, shadowSamples = 6, shadowIntensity = 1;
	Object.defineProperty(gameObject, "shadowColor", {
		get: function() {
			return shadowColor;
		},
		set: function(value) {
			if (shadowColor === value) return;
			shadowColor = value;
			if (shadowColor === null || shadowColor === false) {
				if (gameObject._shadow) {
					filterList.remove(gameObject._shadow);
					gameObject._shadow = void 0;
				}
			} else {
				if (!gameObject._shadow) gameObject._shadow = filterList.addShadow(shadowX, shadowY, shadowDecay, shadowPower, shadowColor, shadowSamples, shadowIntensity).setPaddingOverride(null);
				gameObject._shadow.color = shadowColor;
			}
		}
	});
	Object.defineProperty(gameObject, "shadowX", {
		get: function() {
			return shadowX;
		},
		set: function(value) {
			if (shadowX === value) return;
			shadowX = value;
			if (gameObject._shadow) gameObject._shadow.x = shadowX;
		}
	});
	Object.defineProperty(gameObject, "shadowY", {
		get: function() {
			return shadowY;
		},
		set: function(value) {
			if (shadowY === value) return;
			shadowY = value;
			if (gameObject._shadow) gameObject._shadow.y = shadowY;
		}
	});
	Object.defineProperty(gameObject, "decay", {
		get: function() {
			return shadowDecay;
		},
		set: function(value) {
			if (shadowDecay === value) return;
			shadowDecay = value;
			if (gameObject._shadow) gameObject._shadow.decay = shadowDecay;
		}
	});
	Object.defineProperty(gameObject, "shadowPower", {
		get: function() {
			return shadowPower;
		},
		set: function(value) {
			if (shadowPower === value) return;
			shadowPower = value;
			if (gameObject._shadow) gameObject._shadow.power = shadowPower;
		}
	});
	Object.defineProperty(gameObject, "shadowSamples", {
		get: function() {
			return shadowSamples;
		},
		set: function(value) {
			if (shadowSamples === value) return;
			shadowSamples = value;
			if (gameObject._shadow) gameObject._shadow.samples = shadowSamples;
		}
	});
	Object.defineProperty(gameObject, "shadowIntensity", {
		get: function() {
			return shadowIntensity;
		},
		set: function(value) {
			if (shadowIntensity === value) return;
			shadowIntensity = value;
			if (gameObject._shadow) gameObject._shadow.intensity = shadowIntensity;
		}
	});
	gameObject.shadowColor = null;
	AddClearEffectCallback(gameObject, "shadowColor");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/colormatrix/AddShiftToBGRProperties.js
var AddShiftToBGRProperties = function(gameObject) {
	AddColorMatrixEffectPropertiesBase(gameObject, "shiftToBGR");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/shaders/shine/const.js
var FilterName = "FilterP3Shine";
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/shaders/shine/shine-frag.js
var frag = `\
#pragma phaserTemplate(shaderName)

#ifdef GL_FRAGMENT_PRECISION_HIGH
#define highmedp highp
#else
#define highmedp mediump
#endif
precision highmedp float;

// Scene buffer
uniform sampler2D uMainSampler; 
varying vec2 outTexCoord;

// Effect parameters
uniform vec4 config;
uniform bool reveal;
uniform vec2 texSize;

#pragma phaserTemplate(fragmentHeader)

void main (void) {
    float speed = config.x;
    float time = config.y;
    float lineWidth = config.z;
    float gradient = config.w;

	vec2 uv = gl_FragCoord.xy / texSize;

    vec4 tex = texture2D(uMainSampler, outTexCoord);

    vec4 col1 = vec4(0.3, 0.0, 0.0, 1.0);
    vec4 col2 = vec4(0.85, 0.85, 0.85, 1.0);

    uv.x = uv.x - mod(time * speed, 2.0) + 0.5;
    float y = uv.x * gradient;

    float s = smoothstep(y - lineWidth, y, uv.y) - smoothstep(y, y + lineWidth, uv.y);

    gl_FragColor = (((s * col1) + (s * col2)) * tex);

    if (!reveal)
    {
        //  Apply the shine effect
        gl_FragColor += tex;
    }
}
`;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/system/GetTickDelta.js
var GetTickDelta = function(game) {
	return GetGame(game).loop.delta;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/shaders/utils/GetCurrentTime.js
var MaxPeriod = 36e5;
var GetCurrentTime = function(scene, prevTime) {
	var currentTime = prevTime + GetTickDelta(scene);
	if (currentTime >= MaxPeriod) currentTime -= MaxPeriod;
	return currentTime;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/shaders/shine/ShineFilter.js
var ShineFilter = class extends __webpack_exports__Renderer.WebGL.RenderNodes.BaseFilterShader {
	static FilterName = FilterName;
	constructor(manager) {
		super(FilterName, manager, null, frag);
	}
	setupUniforms(controller, drawingContext) {
		const programManager = this.programManager;
		controller.now = GetCurrentTime(this.manager.renderer.game, controller.now);
		programManager.setUniform("config", [
			controller.speed,
			controller.now,
			controller.lineWidth,
			controller.gradient
		]);
		programManager.setUniform("reveal", controller.reveal);
		programManager.setUniform("texSize", [drawingContext.width, drawingContext.height]);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/shaders/shine/ShineController.js
var GetValue$255 = __webpack_exports__Utils.Objects.GetValue;
var ShineController = class extends __webpack_exports__Filters.Controller {
	static FilterName = FilterName;
	constructor(camera, config) {
		super(camera, FilterName);
		this.now = 0;
		this.speed = .5;
		this.lineWidth = .5;
		this.gradient = 3;
		this.reveal = false;
		this.resetFromJSON(config);
	}
	resetFromJSON(o) {
		this.setSpeed(GetValue$255(o, "speed", .5));
		this.setLineWidth(GetValue$255(o, "lineWidth", .5));
		this.setGradient(GetValue$255(o, "gradient", 3));
		this.setReveal(GetValue$255(o, "reveal", false));
		return this;
	}
	setSpeed(speed) {
		this.speed = speed;
		return this;
	}
	setLineWidth(lineWidth) {
		this.lineWidth = lineWidth;
		return this;
	}
	setGradient(gradient) {
		this.gradient = gradient;
		return this;
	}
	setReveal(reveal) {
		this.reveal = reveal;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/shaders/p3fx/InstallShineFX.js
var InstallShineFX = function(game) {
	game = GetGame(game);
	if (!RegisterFilter(game, ShineFilter)) return false;
	AddFilterListMethod("addP3Shine", function(speed, lineWidth, gradient, reveal) {
		if (speed === void 0) speed = .5;
		if (lineWidth === void 0) lineWidth = .5;
		if (gradient === void 0) gradient = 3;
		if (reveal === void 0) reveal = false;
		return this.add(new ShineController(this.camera, {
			speed,
			lineWidth,
			gradient,
			reveal
		}));
	});
	return true;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/AddShineProperties.js
var AddShineProperties = function(gameObject) {
	if (HasProperty(gameObject, "shineSpeed")) return gameObject;
	InstallShineFX(gameObject);
	var filterList = GetFilterList(gameObject);
	var shineSpeed, shineLineWidth = .5, shineGradient = 3;
	Object.defineProperty(gameObject, "shineSpeed", {
		get: function() {
			return shineSpeed;
		},
		set: function(value) {
			if (shineSpeed === value) return;
			shineSpeed = value;
			if (shineSpeed === null || shineSpeed === false) {
				if (gameObject._shine) {
					filterList.remove(gameObject._shine);
					gameObject._shine = void 0;
				}
			} else {
				if (!gameObject._shine) gameObject._shine = filterList.addP3Shine(shineSpeed, shineLineWidth, shineGradient);
				gameObject._shine.speed = shineSpeed;
			}
		}
	});
	Object.defineProperty(gameObject, "shineLineWidth", {
		get: function() {
			return shineLineWidth;
		},
		set: function(value) {
			if (shineLineWidth === value) return;
			shineLineWidth = value;
			if (gameObject._shine) gameObject._shine.lineWidth = shineLineWidth;
		}
	});
	Object.defineProperty(gameObject, "shineGradient", {
		get: function() {
			return shineGradient;
		},
		set: function(value) {
			if (shineGradient === value) return;
			shineGradient = value;
			if (gameObject._shine) gameObject._shine.gradient = shineGradient;
		}
	});
	gameObject.shineSpeed = null;
	AddClearEffectCallback(gameObject, "shineSpeed");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/colormatrix/AddTechnicolorProperties.js
var AddTechnicolorProperties = function(gameObject) {
	AddColorMatrixEffectPropertiesBase(gameObject, "technicolor");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/AddTiltShiftProperties.js
var AddTiltShiftProperties = function(gameObject) {
	if (HasProperty(gameObject, "tiltShiftRadius")) return gameObject;
	var filterList = GetFilterList(gameObject);
	var tiltShiftRadius, tiltShiftAmount = 1, tiltShiftContrast = .2, tiltShiftBlurX = 1, tiltShiftBlurY = 1, tiltShiftStrength = 1;
	Object.defineProperty(gameObject, "tiltShiftRadius", {
		get: function() {
			return tiltShiftRadius;
		},
		set: function(value) {
			if (tiltShiftRadius === value) return;
			tiltShiftRadius = value;
			if (tiltShiftRadius === null || tiltShiftRadius === false) {
				if (gameObject._tiltShift) {
					filterList.remove(gameObject._tiltShift);
					gameObject._tiltShift = void 0;
				}
			} else {
				if (!gameObject._tiltShift) gameObject._tiltShift = filterList.addTiltShift(tiltShiftRadius, tiltShiftAmount, tiltShiftContrast, tiltShiftBlurX, tiltShiftBlurY, tiltShiftStrength);
				gameObject._tiltShift.radius = tiltShiftRadius;
			}
		}
	});
	Object.defineProperty(gameObject, "tiltShiftAmount", {
		get: function() {
			return tiltShiftAmount;
		},
		set: function(value) {
			if (tiltShiftAmount === value) return;
			tiltShiftAmount = value;
			if (gameObject._tiltShift) gameObject._tiltShift.amount = tiltShiftAmount;
		}
	});
	Object.defineProperty(gameObject, "tiltShiftContrast", {
		get: function() {
			return tiltShiftContrast;
		},
		set: function(value) {
			if (tiltShiftContrast === value) return;
			tiltShiftContrast = value;
			if (gameObject._tiltShift) gameObject._tiltShift.contrast = tiltShiftContrast;
		}
	});
	Object.defineProperty(gameObject, "tiltShiftBlurX", {
		get: function() {
			return tiltShiftBlurX;
		},
		set: function(value) {
			if (tiltShiftBlurX === value) return;
			tiltShiftBlurX = value;
			if (gameObject._tiltShift) gameObject._tiltShift.blurX = tiltShiftBlurX;
		}
	});
	Object.defineProperty(gameObject, "tiltShiftBlurY", {
		get: function() {
			return tiltShiftBlurY;
		},
		set: function(value) {
			if (tiltShiftBlurY === value) return;
			tiltShiftBlurY = value;
			if (gameObject._tiltShift) gameObject._tiltShift.blurY = tiltShiftBlurY;
		}
	});
	Object.defineProperty(gameObject, "tiltShiftStrength", {
		get: function() {
			return tiltShiftStrength;
		},
		set: function(value) {
			if (tiltShiftStrength === value) return;
			tiltShiftStrength = value;
			if (gameObject._tiltShift) gameObject._tiltShift.strength = tiltShiftStrength;
		}
	});
	gameObject.tiltShiftRadius = null;
	AddClearEffectCallback(gameObject, "tiltShiftRadius");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/AddVignetteProperties.js
var AddVignetteProperties = function(gameObject) {
	if (HasProperty(gameObject, "vignetteRadius")) return gameObject;
	var filterList = GetFilterList(gameObject);
	var vignetteRadius, vignetteX = .5, vignetteY = .5, vignetteStrength = .5;
	Object.defineProperty(gameObject, "vignetteRadius", {
		get: function() {
			return vignetteRadius;
		},
		set: function(value) {
			if (vignetteRadius === value) return;
			vignetteRadius = value;
			if (vignetteRadius === null || vignetteRadius === false) {
				if (gameObject._vignette) {
					filterList.remove(gameObject._vignette);
					gameObject._vignette = void 0;
				}
			} else {
				if (!gameObject._vignette) gameObject._vignette = filterList.addVignette(vignetteX, 1 - vignetteY, vignetteRadius, vignetteStrength);
				gameObject._vignette.radius = vignetteRadius;
			}
		}
	});
	Object.defineProperty(gameObject, "vignetteX", {
		get: function() {
			return vignetteX;
		},
		set: function(value) {
			if (vignetteX === value) return;
			vignetteX = value;
			if (gameObject._vignette) gameObject._vignette.x = vignetteX;
		}
	});
	Object.defineProperty(gameObject, "vignetteY", {
		get: function() {
			return vignetteY;
		},
		set: function(value) {
			if (vignetteY === value) return;
			vignetteY = value;
			if (gameObject._vignette) gameObject._vignette.y = 1 - vignetteY;
		}
	});
	Object.defineProperty(gameObject, "vignetteStrength", {
		get: function() {
			return vignetteStrength;
		},
		set: function(value) {
			if (vignetteStrength === value) return;
			vignetteStrength = value;
			if (gameObject._vignette) gameObject._vignette.strength = vignetteStrength;
		}
	});
	gameObject.vignetteRadius = null;
	AddClearEffectCallback(gameObject, "vignetteRadius");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/colormatrix/AddVintagePinholeProperties.js
var AddVintagePinholeProperties = function(gameObject) {
	AddColorMatrixEffectPropertiesBase(gameObject, "vintagePinhole");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/AddWipeProperties.js
var AddWipeProperties = function(gameObject) {
	if (HasProperty(gameObject, "wipeLeft")) return gameObject;
	var filterList = GetFilterList(gameObject);
	var wipeLeft, wipeRight, wipeUp, wipeDown, wipeWidth = .1;
	var ClearWipeFlags = function() {
		wipeLeft = null;
		wipeRight = null;
		wipeUp = null;
		wipeDown = null;
	};
	var RemoveEffect = function(gameObject) {
		if (gameObject._wipeEffect) {
			filterList.remove(gameObject._wipeEffect);
			gameObject._wipeEffect = void 0;
		}
	};
	Object.defineProperty(gameObject, "wipeLeft", {
		get: function() {
			return wipeLeft;
		},
		set: function(value) {
			if (wipeLeft === value) return;
			ClearWipeFlags();
			wipeLeft = value;
			if (wipeLeft === null || wipeLeft === false) RemoveEffect(gameObject);
			else {
				wipeLeft = Clamp$17(wipeLeft, 0, 1);
				if (!gameObject._wipeEffect) gameObject._wipeEffect = filterList.addWipe(wipeWidth, 0, 0, 0);
				gameObject._wipeEffect.direction = 1;
				gameObject._wipeEffect.axis = 0;
				gameObject._wipeEffect.progress = wipeLeft;
			}
		}
	});
	Object.defineProperty(gameObject, "wipeRight", {
		get: function() {
			return wipeRight;
		},
		set: function(value) {
			if (wipeRight === value) return;
			ClearWipeFlags();
			wipeRight = value;
			if (wipeRight === null || wipeRight === false) RemoveEffect(gameObject);
			else {
				wipeRight = Clamp$17(wipeRight, 0, 1);
				if (!gameObject._wipeEffect) gameObject._wipeEffect = filterList.addWipe(wipeWidth, 0, 0, 0);
				gameObject._wipeEffect.direction = 0;
				gameObject._wipeEffect.axis = 0;
				gameObject._wipeEffect.progress = wipeRight;
			}
		}
	});
	Object.defineProperty(gameObject, "wipeUp", {
		get: function() {
			return wipeUp;
		},
		set: function(value) {
			if (wipeUp === value) return;
			ClearWipeFlags();
			wipeUp = value;
			if (wipeUp === null || wipeUp === false) RemoveEffect(gameObject);
			else {
				wipeUp = Clamp$17(wipeUp, 0, 1);
				if (!gameObject._wipeEffect) gameObject._wipeEffect = filterList.addWipe(wipeWidth, 0, 0, 0);
				gameObject._wipeEffect.direction = 1;
				gameObject._wipeEffect.axis = 1;
				gameObject._wipeEffect.progress = wipeUp;
			}
		}
	});
	Object.defineProperty(gameObject, "wipeDown", {
		get: function() {
			return wipeDown;
		},
		set: function(value) {
			if (wipeDown === value) return;
			ClearWipeFlags();
			wipeDown = value;
			if (wipeDown === null || wipeDown === false) RemoveEffect(gameObject);
			else {
				wipeDown = Clamp$17(wipeDown, 0, 1);
				if (!gameObject._wipeEffect) gameObject._wipeEffect = filterList.addWipe(wipeWidth, 0, 0, 0);
				gameObject._wipeEffect.direction = 0;
				gameObject._wipeEffect.axis = 1;
				gameObject._wipeEffect.progress = wipeDown;
			}
		}
	});
	Object.defineProperty(gameObject, "wipeWidth", {
		get: function() {
			return wipeWidth;
		},
		set: function(value) {
			if (wipeWidth === value) return;
			wipeWidth = Clamp$17(value, 0, 1);
			if (gameObject._wipeEffect) gameObject._wipeEffect.wipeWidth = wipeWidth;
		}
	});
	gameObject.wipeLeft = null;
	AddClearEffectCallback(gameObject, "wipeLeft");
	AddClearEffectCallback(gameObject, "wipeRight");
	AddClearEffectCallback(gameObject, "wipeUp");
	AddClearEffectCallback(gameObject, "wipeDown");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/EffectMap.js
var EffectMap = {
	barrel: AddBarrelProperties,
	blackWhite: AddBlackWhiteProperties,
	blocky: AddBlockyProperties,
	bloom: AddBloomProperties,
	blur: AddBlurProperties,
	bokeh: AddBokehProperties,
	brightness: AddBrightnessProperties,
	brown: AddBrownProperties,
	circle: AddCircleProperties,
	contrast: AddContrastProperties,
	desaturate: AddDesaturateProperties,
	desaturateLuminance: AddDesaturateLuminanceProperties,
	displacement: AddDisplacementProperties,
	glow: AddGlowProperties,
	gradient: AddGradientProperties,
	grayscale: AddGrayscaleProperties,
	hue: AddHueProperties,
	kodachrome: AddKodachromeProperties,
	lsd: AddLSDProperties,
	negative: AddNegativeProperties,
	pixelate: AddPixelateProperties,
	polaroid: AddPolaroidProperties,
	reveal: AddRevealProperties,
	saturate: AddSaturateProperties,
	sepia: AddSepiaProperties,
	shadow: AddShadowProperties,
	shiftToBGR: AddShiftToBGRProperties,
	shine: AddShineProperties,
	technicolor: AddTechnicolorProperties,
	tiltShift: AddTiltShiftProperties,
	vignette: AddVignetteProperties,
	vintagePinhole: AddVintagePinholeProperties,
	wipe: AddWipeProperties
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/effectproperties/AddEffectProperties.js
var AddEffectProperties = function(gameObject, config) {
	if (config === void 0) config = true;
	else if (typeof config === "string") config = { config: true };
	else if (Array.isArray(config)) {
		var nameList = config;
		var config = {};
		for (var i = 0, cnt = nameList.length; i < cnt; i++) config[nameList[i]] = true;
	}
	if (config === true) for (var name in EffectMap) EffectMap[name](gameObject);
	else for (var name in config) if (config[name] && EffectMap[name]) EffectMap[name](gameObject);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/gomanager/methods/AddMethods.js
var RemoveItem$11 = __webpack_exports__Utils.Array.Remove;
var AddMethods_default$1 = {
	addGO(name, gameObject) {
		this.remove(name, true);
		if (this.useTintFadeEffect(gameObject)) AddTintRGBProperties(gameObject);
		if (this.viewportCoordinateEnable) AddViewportCoordinateProperties(gameObject, this.viewport);
		if (this.effectPropertiesConfig) AddEffectProperties(gameObject, this.effectPropertiesConfig);
		gameObject.setName(name).once("destroy", function() {
			RemoveItem$11(this.removedGOs, gameObject);
			if (this.isEmpty) this.emit("empty");
		}, this);
		var bob = new this.BobClass(this, gameObject, name);
		this.bobs[name] = bob;
		return this;
	},
	add(name, ...args) {
		var callback = this.createGameObjectCallback;
		var scope = this.createGameObjectScope;
		var gameObject = callback.call(scope, this.scene, ...args);
		this.addGO(name, gameObject);
		if (this.gameObjectDepth != null) gameObject.setDepth(this.gameObjectDepth);
		var bob = this.get(name);
		this.fadeBob(bob, 0, 1);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/gomanager/methods/RemoveMethods.js
var RemoveMethods_default$1 = {
	remove(name, ignoreFade) {
		var bobs = this.get(name);
		if (!bobs) return this;
		else if (!Array.isArray(bobs)) bobs = [bobs];
		var self = this;
		bobs.forEach(function(bob) {
			delete self.bobs[bob.name];
			var gameObject = bob.gameObject;
			self.removedGOs.push(gameObject);
			gameObject.setName();
			if (!ignoreFade) self.fadeBob(bob, void 0, 0, function() {
				bob.destroy();
			});
			else bob.destroy();
		});
		return this;
	},
	removeAll() {
		for (var name in this.bobs) this.remove(name);
		return this;
	},
	clear(destroyChild) {
		if (destroyChild === void 0) destroyChild = true;
		var bobs = this.bobs;
		for (var name in bobs) {
			if (destroyChild) bobs[name].destroy();
			delete bobs[name];
		}
		this.removedGOs.length = 0;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/gomanager/methods/PropertyMethods.js
var PropertyMethods_default = {
	hasProperty(name, property) {
		var bob = IsSingleBob(name) ? this.get(name) : this.getFitst(name);
		if (!bob) return false;
		return bob.hasProperty(property);
	},
	getProperty(name, property) {
		var bob = IsSingleBob(name) ? this.get(name) : this.getFitst(name);
		if (!bob) return;
		return bob.getProperty(property);
	},
	isNumberProperty(name, property) {
		return typeof this.getProperty(name, property) === "number";
	},
	setProperty(name, property, value) {
		var bobs = this.get(name);
		if (!bobs) return this;
		else if (!Array.isArray(bobs)) bobs = [bobs];
		if (this.symbols && typeof value === "string" && this.isNumberProperty(name, property)) {
			if (value in this.symbols) value = this.symbols[value];
			else console.warn(`Can't find symbol ${value}`);
		}
		bobs.forEach(function(bob) {
			bob.setProperty(property, value);
		});
		return this;
	},
	easeProperty(name, config) {
		var bobs = this.get(name);
		if (!bobs) return this;
		else if (!Array.isArray(bobs)) bobs = [bobs];
		var value = config.value;
		var property = config.property;
		if (this.symbols && typeof value === "string" && this.isNumberProperty(name, property) && value in this.symbols) config.value = this.symbols[value];
		bobs.forEach(function(bob) {
			bob.easeProperty(config);
		});
		return this;
	},
	hasTweenTask(name, property) {
		var bob = IsSingleBob(name) ? this.get(name) : this.getFitst(name);
		if (!bob) return false;
		return bob.tweens.hasOwnProperty(property);
	},
	getTweenTask(name, property) {
		var bob = IsSingleBob(name) ? this.get(name) : this.getFitst(name);
		if (!bob) return null;
		return bob.tweens[property] || null;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/gomanager/methods/CallMethods.js
var CallMethods_default = {
	hasMethod(name, methodName) {
		var bob = IsSingleBob(name) ? this.get(name) : this.getFitst(name);
		if (!bob) return false;
		return bob.hasMethod(methodName);
	},
	call(name, methodName, ...parameters) {
		var bobs = this.get(name);
		if (!bobs) return this;
		else if (!Array.isArray(bobs)) bobs = [bobs];
		bobs.forEach(function(bob) {
			bob.call(methodName, ...parameters);
		});
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/gomanager/methods/DataMethods.js
var DataMethods_default = {
	hasData(name, dataKey) {
		var bob = IsSingleBob(name) ? this.get(name) : this.getFitst(name);
		if (!bob) return false;
		return bob.hasData(dataKey);
	},
	getData(name, dataKey) {
		if (!this.has(name)) return;
		return this.get(name).getData(dataKey);
	},
	setData(name, dataKey, value) {
		var bobs = this.get(name);
		if (!bobs) return this;
		else if (!Array.isArray(bobs)) bobs = [bobs];
		bobs.forEach(function(bob) {
			bob.setData(dataKey, value);
		});
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/gomanager/methods/FadeMethods.js
var FadeTint = 0;
var FadeAlpha = 1;
var FadeRevealUp = 2;
var FadeRevealDown = 3;
var FadeRevealLeft = 4;
var FadeRevealRight = 5;
var FadeMode = {
	tint: FadeTint,
	alpha: FadeAlpha,
	revealUp: FadeRevealUp,
	revealDown: FadeRevealDown,
	revealLeft: FadeRevealLeft,
	revealRight: FadeRevealRight
};
var FadeMethods_default$1 = {
	setGOFadeMode(fadeMode) {
		if (typeof fadeMode === "string") fadeMode = FadeMode[fadeMode];
		this.fadeMode = fadeMode;
		return this;
	},
	setGOFadeTime(time) {
		this.fadeTime = time;
		return this;
	},
	useTintFadeEffect(gameObject) {
		return (this.fadeMode === void 0 || this.fadeMode === FadeTint) && this.fadeTime > 0 && gameObject.setTint !== void 0;
	},
	useAlphaFadeEffect(gameObject) {
		return (this.fadeMode === void 0 || this.fadeMode === FadeAlpha) && this.fadeTime > 0 && gameObject.setAlpha !== void 0;
	},
	useRevealEffect(gameObject) {
		return this.fadeMode >= FadeRevealUp && this.fadeMode <= FadeRevealRight && this.fadeTime > 0 && (gameObject.preFX || gameObject.postFX);
	},
	fadeBob(bob, fromValue, toValue, onComplete) {
		var gameObject = bob.gameObject;
		if (this.useTintFadeEffect(gameObject)) {
			if (fromValue !== void 0) bob.setProperty("tintGray", 255 * fromValue);
			bob.easeProperty({
				property: "tintGray",
				value: Math.floor(255 * toValue),
				duration: this.fadeTime,
				delay: 0,
				ease: "Linear",
				repeat: 0,
				yoyo: false,
				from: false,
				complete: onComplete
			});
		} else if (this.useAlphaFadeEffect(gameObject)) {
			if (fromValue !== void 0) bob.setProperty("alpha", fromValue);
			bob.easeProperty({
				property: "alpha",
				value: toValue,
				duration: this.fadeTime,
				delay: 0,
				ease: "Linear",
				repeat: 0,
				yoyo: false,
				from: false,
				complete: onComplete
			});
		} else if (this.useRevealEffect(gameObject)) {
			AddEffectProperties(gameObject, "reveal");
			var propertyName;
			switch (this.fadeMode) {
				case FadeRevealUp:
					propertyName = "revealUp";
					break;
				case FadeRevealDown:
					propertyName = "revealDown";
					break;
				case FadeRevealLeft:
					propertyName = "revealLeft";
					break;
				case FadeRevealRight: propertyName = "revealRight";
			}
			if (fromValue === void 0) fromValue = 0;
			gameObject[propertyName] = fromValue;
			bob.easeProperty({
				property: propertyName,
				value: toValue,
				duration: this.fadeTime,
				delay: 0,
				ease: "Linear",
				repeat: 0,
				yoyo: false,
				from: false,
				complete: onComplete
			});
			bob.getTweenTask(propertyName).once("complete", function() {
				gameObject[propertyName] = null;
			});
		} else if (onComplete) onComplete(gameObject);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/size/GetDisplaySize.js
var GetDisplayWidth = function(gameObject) {
	if (gameObject.displayWidth !== void 0) return gameObject.displayWidth;
	else return gameObject.width;
};
var GetDisplayHeight = function(gameObject) {
	if (gameObject.displayHeight !== void 0) return gameObject.displayHeight;
	else return gameObject.height;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/bounds/GetBounds.js
var Rectangle$4 = __webpack_exports__Geom.Rectangle;
var Vector2 = __webpack_exports__Math.Vector2;
var RotateAround$6 = __webpack_exports__Math.RotateAround;
var P3Container$1 = __webpack_exports__GameObjects.Container;
var GetBounds = function(gameObject, output) {
	if (output === void 0) output = new Rectangle$4();
	else if (output === true) {
		if (GlobRect$2 === void 0) GlobRect$2 = new Rectangle$4();
		output = GlobRect$2;
	}
	if (gameObject.getBounds && !(gameObject instanceof P3Container$1)) return gameObject.getBounds(output);
	var TLx, TLy, TRx, TRy, BLx, BLy, BRx, BRy;
	if (gameObject.parentContainer) {
		var parentMatrix = gameObject.parentContainer.getBoundsTransformMatrix();
		GetTopLeft(gameObject, output);
		parentMatrix.transformPoint(output.x, output.y, output);
		TLx = output.x;
		TLy = output.y;
		GetTopRight(gameObject, output);
		parentMatrix.transformPoint(output.x, output.y, output);
		TRx = output.x;
		TRy = output.y;
		GetBottomLeft(gameObject, output);
		parentMatrix.transformPoint(output.x, output.y, output);
		BLx = output.x;
		BLy = output.y;
		GetBottomRight(gameObject, output);
		parentMatrix.transformPoint(output.x, output.y, output);
		BRx = output.x;
		BRy = output.y;
	} else {
		GetTopLeft(gameObject, output);
		TLx = output.x;
		TLy = output.y;
		GetTopRight(gameObject, output);
		TRx = output.x;
		TRy = output.y;
		GetBottomLeft(gameObject, output);
		BLx = output.x;
		BLy = output.y;
		GetBottomRight(gameObject, output);
		BRx = output.x;
		BRy = output.y;
	}
	output.x = Math.min(TLx, TRx, BLx, BRx);
	output.y = Math.min(TLy, TRy, BLy, BRy);
	output.width = Math.max(TLx, TRx, BLx, BRx) - output.x;
	output.height = Math.max(TLy, TRy, BLy, BRy) - output.y;
	return output;
};
var GlobRect$2 = void 0;
var GetTopLeft = function(gameObject, output, includeParent) {
	if (output === void 0) output = new Vector2();
	else if (output === true) {
		if (GlobVector === void 0) GlobVector = new Vector2();
		output = GlobVector;
	}
	if (gameObject.getTopLeft) return gameObject.getTopLeft(output, includeParent);
	output.x = gameObject.x - GetDisplayWidth(gameObject) * gameObject.originX;
	output.y = gameObject.y - GetDisplayHeight(gameObject) * gameObject.originY;
	return PrepareBoundsOutput(gameObject, output, includeParent);
};
var GetTopRight = function(gameObject, output, includeParent) {
	if (output === void 0) output = new Vector2();
	else if (output === true) {
		if (GlobVector === void 0) GlobVector = new Vector2();
		output = GlobVector;
	}
	if (gameObject.getTopRight) return gameObject.getTopRight(output, includeParent);
	output.x = gameObject.x - GetDisplayWidth(gameObject) * gameObject.originX + GetDisplayWidth(gameObject);
	output.y = gameObject.y - GetDisplayHeight(gameObject) * gameObject.originY;
	return PrepareBoundsOutput(gameObject, output, includeParent);
};
var GetBottomLeft = function(gameObject, output, includeParent) {
	if (output === void 0) output = new Vector2();
	else if (output === true) {
		if (GlobVector === void 0) GlobVector = new Vector2();
		output = GlobVector;
	}
	if (gameObject.getBottomLeft) return gameObject.getBottomLeft(output, includeParent);
	output.x = gameObject.x - GetDisplayWidth(gameObject) * gameObject.originX;
	output.y = gameObject.y - GetDisplayHeight(gameObject) * gameObject.originY + GetDisplayHeight(gameObject);
	return PrepareBoundsOutput(gameObject, output, includeParent);
};
var GetBottomRight = function(gameObject, output, includeParent) {
	if (output === void 0) output = new Vector2();
	else if (output === true) {
		if (GlobVector === void 0) GlobVector = new Vector2();
		output = GlobVector;
	}
	if (gameObject.getBottomRight) return gameObject.getBottomRight(output, includeParent);
	output.x = gameObject.x - GetDisplayWidth(gameObject) * gameObject.originX + GetDisplayWidth(gameObject);
	output.y = gameObject.y - GetDisplayHeight(gameObject) * gameObject.originY + GetDisplayHeight(gameObject);
	return PrepareBoundsOutput(gameObject, output, includeParent);
};
var GetCenter = function(gameObject, output, includeParent) {
	if (output === void 0) output = new Vector2();
	else if (output === true) {
		if (GlobVector === void 0) GlobVector = new Vector2();
		output = GlobVector;
	}
	var displayWidth = GetDisplayWidth(gameObject);
	var displayHeight = GetDisplayHeight(gameObject);
	output.x = gameObject.x + displayWidth * (.5 - gameObject.originX);
	output.y = gameObject.y + displayHeight * (.5 - gameObject.originY);
	return PrepareBoundsOutput(gameObject, output, includeParent);
};
var GlobVector = void 0;
var PrepareBoundsOutput = function(gameObject, output, includeParent) {
	if (includeParent === void 0) includeParent = false;
	if (gameObject.rotation !== 0) RotateAround$6(output, gameObject.x, gameObject.y, gameObject.rotation);
	if (includeParent && gameObject.parentContainer) gameObject.parentContainer.getBoundsTransformMatrix().transformPoint(output.x, output.y, output);
	return output;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/bounds/DrawBounds.js
var GetValue$254 = __webpack_exports__Utils.Objects.GetValue;
var DrawBounds$2 = function(gameObjects, graphics, config) {
	var strokeColor, lineWidth, fillColor, fillAlpha, padding, includeParent;
	if (typeof config === "number") strokeColor = config;
	else {
		strokeColor = GetValue$254(config, "color");
		lineWidth = GetValue$254(config, "lineWidth");
		fillColor = GetValue$254(config, "fillColor");
		fillAlpha = GetValue$254(config, "fillAlpha");
		padding = GetValue$254(config, "padding");
		includeParent = GetValue$254(config, "includeParent");
	}
	if (strokeColor === void 0) strokeColor = 16777215;
	if (lineWidth === void 0) lineWidth = 1;
	if (fillColor === void 0) fillColor = null;
	if (fillAlpha === void 0) fillAlpha = 1;
	if (padding === void 0) padding = 0;
	if (includeParent === void 0) includeParent = true;
	if (Array.isArray(gameObjects)) for (var i = 0, cnt = gameObjects.length; i < cnt; i++) Draw(gameObjects[i], graphics, strokeColor, lineWidth, fillColor, fillAlpha, padding, includeParent);
	else Draw(gameObjects, graphics, strokeColor, lineWidth, fillColor, fillAlpha, padding, includeParent);
};
var Draw = function(gameObject, graphics, strokeColor, lineWidth, fillColor, fillAlpha, padding, includeParent) {
	if (!(gameObject.getBounds || gameObject.width !== void 0 && gameObject.height !== void 0)) return;
	var p0 = GetTopLeft(gameObject, Points[0], includeParent);
	p0.x -= padding;
	p0.y -= padding;
	var p1 = GetTopRight(gameObject, Points[1], includeParent);
	p1.x += padding;
	p1.y -= padding;
	var p2 = GetBottomRight(gameObject, Points[2], includeParent);
	p2.x += padding;
	p2.y += padding;
	var p3 = GetBottomLeft(gameObject, Points[3], includeParent);
	p3.x -= padding;
	p3.y += padding;
	if (fillColor !== null) graphics.fillStyle(fillColor, fillAlpha).fillPoints(Points, true, true);
	if (strokeColor !== null) graphics.lineStyle(lineWidth, strokeColor).strokePoints(Points, true, true);
};
var Points = [
	{
		x: 0,
		y: 0
	},
	{
		x: 0,
		y: 0
	},
	{
		x: 0,
		y: 0
	},
	{
		x: 0,
		y: 0
	}
];
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/gomanager/methods/DrawGameObjectsBounds.js
var DrawGameObjectsBounds = function(graphics, config) {
	this.forEachGO(function(gameObject) {
		if (gameObject.drawBounds) gameObject.drawBounds(graphics, config);
		else DrawBounds$2(gameObject, graphics, config);
	});
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/camera/GetCameraByName.js
var GetCameraByName = function(scene, name) {
	var cameraManager = scene.cameras;
	var camera;
	if (name === void 0) camera = cameraManager.main;
	else switch (typeof name) {
		case "string":
			camera = cameraManager.getCamera(name);
			break;
		case "number":
			camera = cameraManager.cameras[name];
			break;
		default: camera = name;
	}
	return camera;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/gomanager/methods/CameraMethods.js
var CameraMethods_default$1 = {
	setCamera(goName, cameraName) {
		var bob = this.get(goName);
		if (!bob) return this;
		var camera = GetCameraByName(this.scene, cameraName);
		if (!camera) return this;
		bob.gameObject.cameraFilter = 4294967295 ^ camera.id;
		bob.camera = camera;
		return this;
	},
	getCamera(goName) {
		var bob = this.get(goName);
		if (!bob) return null;
		return bob.camera;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/gomanager/methods/Methods.js
var Methods$21 = { drawGameObjectsBounds: DrawGameObjectsBounds };
Object.assign(Methods$21, GetMethods_default, AddMethods_default$1, RemoveMethods_default$1, PropertyMethods_default, CallMethods_default, DataMethods_default, FadeMethods_default$1, CameraMethods_default$1);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/system/IsCameraObject.js
var CameraClass = __webpack_exports__Cameras.Scene2D.BaseCamera;
var IsCameraObject = function(object) {
	return object instanceof CameraClass;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/system/GetViewport.js
var Rectangle$3 = __webpack_exports__Geom.Rectangle;
var GetViewport = function(scene, camera, out) {
	if (!IsCameraObject(camera)) {
		out = camera;
		camera = void 0;
	}
	if (out === void 0) out = new Rectangle$3();
	else if (out === true) out = globRect;
	if (camera) return scene.scale.getViewPort(camera, out);
	else return scene.scale.getViewPort(out);
};
var globRect = new Rectangle$3();
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/gomanager/GOManager.js
var GetValue$253 = __webpack_exports__Utils.Objects.GetValue;
var GOManager = class {
	constructor(scene, config) {
		this.scene = scene;
		this.BobClass = GetValue$253(config, "BobClass", BobBase);
		this.setCreateGameObjectCallback(GetValue$253(config, "createGameObject"), GetValue$253(config, "createGameObjectScope"));
		this.setEventEmitter(GetValue$253(config, "eventEmitter", void 0));
		this.setGameObjectDepth(GetValue$253(config, "depth", void 0));
		var fadeConfig = GetValue$253(config, "fade", 500);
		if (typeof fadeConfig === "number") {
			this.setGOFadeMode();
			this.setGOFadeTime(fadeConfig);
		} else {
			this.setGOFadeMode(GetValue$253(fadeConfig, "mode"));
			this.setGOFadeTime(GetValue$253(fadeConfig, "time", 500));
		}
		var viewportCoordinateConfig = GetValue$253(config, "viewportCoordinate", false);
		if (viewportCoordinateConfig !== false) {
			this.setViewportCoordinateEnable(GetValue$253(config, "enable", true));
			this.setViewport(GetValue$253(viewportCoordinateConfig, "viewport"));
		} else this.setViewportCoordinateEnable(false);
		var effectPropertiesConfig = GetValue$253(config, "effectProperties", false);
		this.setEffectPropertiesConfig(effectPropertiesConfig);
		this.setSymbols(GetValue$253(config, "symbols"));
		this.bobs = {};
		this.removedGOs = [];
		this._timeScale = 1;
		this.name = GetValue$253(config, "name");
	}
	destroy(fromScene) {
		this.clear(!fromScene);
		this.createGameObjectCallback = void 0;
		this.viewport = void 0;
		this.scene = void 0;
	}
	set timeScale(timeScale) {
		if (this._timeScale === timeScale) return;
		this._timeScale = timeScale;
		var bobs = this.bobs;
		for (var name in bobs) bobs[name].setTimeScale(timeScale);
	}
	get timeScale() {
		return this._timeScale;
	}
	setTimeScale(timeScale) {
		this.timeScale = timeScale;
		return this;
	}
	setCreateGameObjectCallback(callback, scope) {
		this.createGameObjectCallback = callback;
		this.createGameObjectScope = scope;
		return this;
	}
	setGameObjectDepth(depth) {
		this.gameObjectDepth = depth;
		return this;
	}
	setViewportCoordinateEnable(enable) {
		if (enable === void 0) enable = true;
		this.viewportCoordinateEnable = enable;
		return this;
	}
	setEffectPropertiesConfig(config) {
		if (config === void 0) config = true;
		this.effectPropertiesConfig = config;
		return this;
	}
	setViewport(viewport) {
		if (viewport === void 0) viewport = GetViewport(this.scene, this.scene.cameras.main);
		this.viewport = viewport;
		return this;
	}
	setSymbols(symbols) {
		this.symbols = symbols;
		return this;
	}
	get isEmpty() {
		return IsEmpty(this.bobs) && this.removedGOs.length === 0;
	}
};
Object.assign(GOManager.prototype, EventEmitterMethods_default, Methods$21);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/system/IsGameObject.js
var GameObjectClass = __webpack_exports__GameObjects.GameObject;
var LayerClass$1 = __webpack_exports__GameObjects.Layer;
var IsGameObject = function(object) {
	return object instanceof GameObjectClass || object instanceof LayerClass$1;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/system/IsLayerGameObject.js
var LayerClass = __webpack_exports__GameObjects.Layer;
var IsLayerGameObject = function(gameObject) {
	return gameObject instanceof LayerClass;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/system/GetLayer.js
var GetLayer = function(gameObject) {
	var layer = gameObject.displayList;
	if (!IsLayerGameObject(layer)) return null;
	return layer;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/system/SortGameObjectsByDepth.js
var SortGameObjectsByDepth = function(gameObjects, descending) {
	if (gameObjects.length <= 1) return gameObjects;
	if (descending === void 0) descending = false;
	var itemList;
	for (var i = 0, cnt = gameObjects.length; i < cnt; i++) {
		var gameObject = gameObjects[i];
		if (gameObject.displayList) itemList = gameObject.displayList;
		else if (gameObject.parentContainer) itemList = gameObject.parentContainer.list;
		if (itemList) break;
	}
	if (!itemList) itemList = gameObject.scene.sys.displayList;
	if (itemList.depthSort) {
		itemList.depthSort();
		itemList = itemList.list;
	}
	if (descending) gameObjects.sort(function(childA, childB) {
		return itemList.indexOf(childB) - itemList.indexOf(childA);
	});
	else gameObjects.sort(function(childA, childB) {
		return itemList.indexOf(childA) - itemList.indexOf(childB);
	});
	return gameObjects;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/layermanager/methods/LayerMethods.js
var LayerMethods_default = {
	getLayer(name) {
		return this.getGO(name);
	},
	getLayers(out) {
		if (out === void 0) out = [];
		this.forEachGO(function(gameObject) {
			out.push(gameObject);
		});
		SortGameObjectsByDepth(out, false);
		return out;
	},
	addToLayer(name, gameObjects) {
		var layer = this.getGO(name);
		if (!layer) {
			console.warn(`[LayerManager] Can't get layer "${name}"`);
			return;
		}
		if (!Array.isArray(gameObjects)) gameObjects = [gameObjects];
		for (var i = 0, cnt = gameObjects.length; i < cnt; i++) {
			var gameObject = gameObjects[i];
			if (gameObject.isRexContainerLite) gameObject.addToLayer(layer);
			else layer.add(gameObject);
		}
		if (layer.scrollFactorX !== void 0) gameObject.setScrollFactor(layer.scrollFactorX, layer.scrollFactorY);
		return this;
	},
	addToBottomLayer(gameObjects) {
		var bottomLayer = this.getLayers()[0];
		this.addToLayer(bottomLayer.goName, gameObjects);
		return this;
	},
	addToTopLayer(gameObjects) {
		var layers = this.getLayers();
		var topLayer = layers[layers.length - 1];
		this.addToLayer(topLayer.goName, gameObjects);
		return this;
	},
	removeFromLayer(name, gameObject, addToScene) {
		var layer = this.getGO(name);
		if (!layer) {
			console.warn(`[LayerManager] Can't get layer "${name}"`);
			return;
		}
		if (addToScene === void 0) addToScene = true;
		if (gameObject.isRexContainerLite) gameObject.removeFromLayer(layer, addToScene);
		else {
			layer.remove(gameObject);
			if (addToScene) gameObject.addToDisplayList();
		}
		return this;
	},
	clearLayer(name, destroyChildren) {
		if (destroyChildren === void 0) destroyChildren = true;
		var layer = this.getGO(name);
		if (!layer) {
			console.warn(`Can't get layer "${name}"`);
			return;
		}
		if (destroyChildren) {
			var children = layer.getAll();
			for (var i = 0, cnt = children.length; i < cnt; i++) children[i].destroy();
		} else layer.removeAll();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/layermanager/methods/ScrollFactorMethods.js
var ScrollFactorMethods_default = { setScrollFactor(name, scrollFactorX, scrollFactorY) {
	if (scrollFactorY === void 0) scrollFactorY = scrollFactorX;
	var layer = this.getLayer(name);
	if (!layer) return this;
	layer.scrollFactorX = scrollFactorX;
	layer.scrollFactorY = scrollFactorY;
	var children = layer.getAll();
	for (var i = 0, cnt = children.length; i < cnt; i++) children[i].setScrollFactor(scrollFactorX, scrollFactorY);
	return this;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/displaylist/DisplayListMethods.js
var ArrayUtils$1 = __webpack_exports__Utils.Array;
var BringMeToTop = function() {
	var list;
	if (this.parentContainer) list = this.parentContainer.list;
	else if (this.displayList) list = this.displayList.list;
	if (!list) return this;
	ArrayUtils$1.BringToTop(list, this);
	return this;
};
var SendMeToBack = function() {
	var list;
	if (this.parentContainer) list = this.parentContainer.list;
	else if (this.displayList) list = this.displayList.list;
	if (!list) return this;
	ArrayUtils$1.SendToBack(list, this);
	return this;
};
var MoveMyDepthBelow = function(gameObject) {
	var list;
	if (gameObject.parentContainer) {
		list = gameObject.parentContainer.list;
		if (list.indexOf(this) === -1) gameObject.parentContainer.add(this);
	} else if (gameObject.displayList) {
		list = gameObject.displayList.list;
		if (list.indexOf(this) === -1) gameObject.displayList.add(this);
	}
	if (!list) return this;
	ArrayUtils$1.MoveBelow(list, this, gameObject);
	return this;
};
var MoveMyDepthAbove = function(gameObject) {
	var list;
	if (gameObject.parentContainer) {
		list = gameObject.parentContainer.list;
		if (list.indexOf(this) === -1) {
			if (gameObject.isRexContainerLite) gameObject.addToContainer(gameObject.parentContainer);
			else gameObject.parentContainer.add(gameObject);
		}
	} else if (gameObject.displayList) {
		list = gameObject.displayList.list;
		if (list.indexOf(this) === -1) {
			if (gameObject.isRexContainerLite) gameObject.addToLayer(gameObject.displayList);
			else gameObject.displayList.add(gameObject);
		}
	}
	if (!list) return this;
	ArrayUtils$1.MoveAbove(list, this, gameObject);
	return this;
};
var DisplayListMethods_default = {
	bringMeToTop: BringMeToTop,
	sendMeToBack: SendMeToBack,
	moveMyDepthBelow: MoveMyDepthBelow,
	moveMyDepthAbove: MoveMyDepthAbove
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/layermanager/methods/DepthMethods.js
var DepthMethods_default = {
	bringLayerToTop(layerName) {
		var layer = this.getLayer(layerName);
		if (!layer) return this;
		DisplayListMethods_default.bringMeToTop.call(layer);
		return this;
	},
	sendLayerToBack(layerName) {
		var layer = this.getLayer(layerName);
		if (!layer) return this;
		DisplayListMethods_default.sendMeToBack.call(layer);
		return this;
	},
	moveLayerBelow(layerName, baseLayerName) {
		if (layerName === baseLayerName) return this;
		var layer = this.getLayer(layerName);
		var baseLayer = this.getLayer(baseLayerName);
		if (!layer || !baseLayer) return this;
		DisplayListMethods_default.moveMyDepthBelow.call(layer, baseLayer);
		return this;
	},
	moveLayerAbove(layerName, baseLayerName) {
		if (layerName === baseLayerName) return this;
		var layer = this.getLayer(layerName);
		var baseLayer = this.getLayer(baseLayerName);
		if (!layer || !baseLayer) return this;
		DisplayListMethods_default.moveMyDepthAbove.call(layer, baseLayer);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/layermanager/methods/CameraMethods.js
var SetCamera = GOManager.prototype.setCamera;
var CameraMethods_default = { setCamera(layerName, cameraName) {
	var camera = GetCameraByName(this.scene, cameraName);
	if (!camera) camera = this.scene.cameras.add(void 0, void 0, void 0, void 0, false, cameraName);
	SetCamera.call(this, layerName, camera);
	return this;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/layermanager/methods/Methods.js
var methods$38 = {};
Object.assign(methods$38, LayerMethods_default, ScrollFactorMethods_default, DepthMethods_default, CameraMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/layermanager/LayerManager.js
var GetValue$252 = __webpack_exports__Utils.Objects.GetValue;
var LayerManager = class extends GOManager {
	constructor(scene, config) {
		if (config === void 0) config = {};
		else if (Array.isArray(config)) config = { layers: config };
		if (!config.hasOwnProperty("fade")) config.fade = 0;
		config.viewportCoordinate = false;
		super(scene, config);
		var rootLayer = GetValue$252(config, "rootLayer");
		this.setRootLayer(rootLayer);
		var initLayers = GetValue$252(config, "layers");
		if (initLayers) for (var i = 0, cnt = initLayers.length; i < cnt; i++) {
			var layerConfig = initLayers[i];
			if (typeof layerConfig === "string") this.add(layerConfig);
			else {
				var layerName = layerConfig.name;
				this.add(layerName);
				var scrollFactor = layerConfig.scrollFactor;
				var scrollFactorX = GetValue$252(layerConfig, "scrollFactorX", scrollFactor);
				var scrollFactorY = GetValue$252(layerConfig, "scrollFactorY", scrollFactor);
				if (scrollFactorX !== void 0) this.setScrollFactor(layerName, scrollFactorX, scrollFactorY);
				this.setCamera(layerName, layerConfig.cameraName);
			}
		}
	}
	setCreateGameObjectCallback(callback, scope) {
		if (!callback) callback = CreateLayer;
		super.setCreateGameObjectCallback(callback, scope);
		return this;
	}
	setRootLayer(rootLayer) {
		if (rootLayer === this.rootLayer) return this;
		var currentLayers = this.getAllGO();
		if (rootLayer) rootLayer.add(currentLayers);
		else this.scene.displayList.add(currentLayers);
		this.rootLayer = rootLayer;
		return this;
	}
	addGO(name, gameObject) {
		super.addGO(name, gameObject);
		gameObject.name = name;
		if (this.rootLayer) this.rootLayer.add(gameObject);
		return this;
	}
	get(name, out) {
		if (IsGameObject(name)) {
			var layer = GetLayer(name);
			if (!layer) return;
			name = layer.name;
			if (!name) return;
		}
		return super.get(name, out);
	}
};
var CreateLayer = function(scene, depth) {
	var layer = scene.add.layer();
	if (depth !== void 0) layer.setDepth(depth);
	return layer;
};
Object.assign(LayerManager.prototype, methods$38);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/system/GetSoundManager.js
var GetSoundManager = function(game) {
	if (IsSceneObject(game)) return game.sys.sound;
	return game.sound;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/audio/soundmanager/methods/HasAudio.js
var HasaAudio = function(key) {
	return this.sound.game.cache.audio.has(key);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/system/GetSceneObject.js
var GetSceneObject = function(object) {
	if (object == null || typeof object !== "object") return null;
	else if (IsSceneObject(object)) return object;
	else if (object.scene && IsSceneObject(object.scene)) return object.scene;
	else if (object.parent && object.parent.scene && IsSceneObject(object.parent.scene)) return object.parent.scene;
	else return null;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/componentbase/ComponentBase.js
var GetValue$251 = __webpack_exports__Utils.Objects.GetValue;
var ComponentBase = class {
	constructor(parent, config) {
		this.setParent(parent);
		this.isShutdown = false;
		this.setEventEmitter(GetValue$251(config, "eventEmitter", true));
		if (this.parent) {
			if (this.parent === this.scene) this.scene.sys.events.once("shutdown", this.onEnvDestroy, this);
			else if (this.parent === this.game) this.game.events.once("shutdown", this.onEnvDestroy, this);
			else if (this.parent.once) this.parent.once("destroy", this.onParentDestroy, this);
		}
	}
	shutdown(fromScene) {
		if (this.isShutdown) return;
		if (this.parent) {
			if (this.parent === this.scene) this.scene.sys.events.off("shutdown", this.onEnvDestroy, this);
			else if (this.parent === this.game) this.game.events.off("shutdown", this.onEnvDestroy, this);
			else if (this.parent.once) this.parent.off("destroy", this.onParentDestroy, this);
		}
		this.destroyEventEmitter();
		this.parent = void 0;
		this.scene = void 0;
		this.game = void 0;
		this.isShutdown = true;
	}
	destroy(fromScene) {
		this.shutdown(fromScene);
	}
	onEnvDestroy() {
		this.destroy(true);
	}
	onParentDestroy(parent, fromScene) {
		this.destroy(fromScene);
	}
	setParent(parent) {
		this.parent = parent;
		this.scene = GetSceneObject(parent);
		this.game = GetGame(parent);
		return this;
	}
};
Object.assign(ComponentBase.prototype, EventEmitterMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/componentbase/TickTask.js
var GetValue$250 = __webpack_exports__Utils.Objects.GetValue;
var TickTask = class extends ComponentBase {
	constructor(parent, config) {
		super(parent, config);
		this._isRunning = false;
		this.isPaused = false;
		this.tickingState = false;
		this.setTickingMode(GetValue$250(config, "tickingMode", 1));
	}
	boot() {
		if (this.tickingMode === 2 && !this.tickingState) this.startTicking();
	}
	shutdown(fromScene) {
		if (this.isShutdown) return;
		this.stop();
		if (this.tickingState) this.stopTicking();
		super.shutdown(fromScene);
	}
	setTickingMode(mode) {
		if (typeof mode === "string") mode = TICKINGMODE[mode];
		this.tickingMode = mode;
	}
	startTicking() {
		this.tickingState = true;
	}
	stopTicking() {
		this.tickingState = false;
	}
	get isRunning() {
		return this._isRunning;
	}
	set isRunning(value) {
		if (this._isRunning === value) return;
		this._isRunning = value;
		if (this.tickingMode === 1 && value != this.tickingState) {
			if (value) this.startTicking();
			else this.stopTicking();
		}
	}
	start() {
		this.isPaused = false;
		this.isRunning = true;
		return this;
	}
	pause() {
		if (this.isRunning) {
			this.isPaused = true;
			this.isRunning = false;
		}
		return this;
	}
	resume() {
		if (this.isPaused) {
			this.isPaused = false;
			this.isRunning = true;
		}
		return this;
	}
	stop() {
		this.isPaused = false;
		this.isRunning = false;
		return this;
	}
	complete() {
		this.isPaused = false;
		this.isRunning = false;
		this.emit("complete", this.parent, this);
	}
};
var TICKINGMODE = {
	"no": 0,
	"lazy": 1,
	"always": 2
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/componentbase/SceneUpdateTickTask.js
var GetValue$249 = __webpack_exports__Utils.Objects.GetValue;
var SceneUpdateTickTask = class extends TickTask {
	constructor(parent, config) {
		super(parent, config);
		var defaultEventName = this.scene ? "update" : "step";
		this.tickEventName = GetValue$249(config, "tickEventName", defaultEventName);
		this.isSceneTicker = !IsGameUpdateEvent(this.tickEventName);
	}
	startTicking() {
		super.startTicking();
		if (this.isSceneTicker) this.scene.sys.events.on(this.tickEventName, this.update, this);
		else this.game.events.on(this.tickEventName, this.update, this);
	}
	stopTicking() {
		super.stopTicking();
		if (this.isSceneTicker && this.scene) this.scene.sys.events.off(this.tickEventName, this.update, this);
		else if (this.game) this.game.events.off(this.tickEventName, this.update, this);
	}
};
var IsGameUpdateEvent = function(eventName) {
	return eventName === "step" || eventName === "poststep";
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/componentbase/timerticktask/Timer.js
var GetValue$248 = __webpack_exports__Utils.Objects.GetValue;
var Clamp$16 = __webpack_exports__Math.Clamp;
var Timer$1 = class {
	constructor(config) {
		this.resetFromJSON(config);
	}
	resetFromJSON(o) {
		this.state = GetValue$248(o, "state", IDLE$8);
		this.timeScale = GetValue$248(o, "timeScale", 1);
		this.delay = GetValue$248(o, "delay", 0);
		this.repeat = GetValue$248(o, "repeat", 0);
		this.repeatCounter = GetValue$248(o, "repeatCounter", 0);
		this.repeatDelay = GetValue$248(o, "repeatDelay", 0);
		this.duration = GetValue$248(o, "duration", 0);
		this.nowTime = GetValue$248(o, "nowTime", 0);
		this.justRestart = GetValue$248(o, "justRestart", false);
	}
	toJSON() {
		return {
			state: this.state,
			timeScale: this.timeScale,
			delay: this.delay,
			repeat: this.repeat,
			repeatCounter: this.repeatCounter,
			repeatDelay: this.repeatDelay,
			duration: this.duration,
			nowTime: this.nowTime,
			justRestart: this.justRestart
		};
	}
	destroy() {}
	setTimeScale(timeScale) {
		this.timeScale = timeScale;
		return this;
	}
	setDelay(delay) {
		if (delay === void 0) delay = 0;
		this.delay = delay;
		return this;
	}
	setDuration(duration) {
		this.duration = duration;
		return this;
	}
	setRepeat(repeat) {
		this.repeat = repeat;
		return this;
	}
	setRepeatInfinity() {
		this.repeat = -1;
		return this;
	}
	setRepeatDelay(repeatDelay) {
		this.repeatDelay = repeatDelay;
		return this;
	}
	start() {
		this.nowTime = this.delay > 0 ? -this.delay : 0;
		this.state = this.nowTime >= 0 ? COUNTDOWN : DELAY;
		this.repeatCounter = 0;
		return this;
	}
	stop() {
		this.state = IDLE$8;
		return this;
	}
	update(time, delta) {
		if (this.state === IDLE$8 || this.state === DONE || delta === 0 || this.timeScale === 0) return;
		this.nowTime += delta * this.timeScale;
		this.justRestart = false;
		if (this.nowTime >= this.duration) {
			if (this.repeat === -1 || this.repeatCounter < this.repeat) {
				this.repeatCounter++;
				this.justRestart = true;
				this.nowTime -= this.duration;
				if (this.repeatDelay > 0) {
					this.nowTime -= this.repeatDelay;
					this.state = REPEATDELAY;
				}
			} else {
				this.nowTime = this.duration;
				this.state = DONE;
			}
		} else if (this.nowTime >= 0) this.state = COUNTDOWN;
	}
	get t() {
		var t;
		switch (this.state) {
			case IDLE$8:
			case DELAY:
			case REPEATDELAY:
				t = 0;
				break;
			case COUNTDOWN:
				t = this.nowTime / this.duration;
				break;
			case DONE: t = 1;
		}
		return Clamp$16(t, 0, 1);
	}
	set t(value) {
		value = Clamp$16(value, -1, 1);
		if (value < 0) {
			this.state = DELAY;
			this.nowTime = -this.delay * value;
		} else {
			this.state = COUNTDOWN;
			this.nowTime = this.duration * value;
			if (value === 1 && this.repeat !== 0) this.repeatCounter++;
		}
	}
	setT(t) {
		this.t = t;
		return this;
	}
	get isIdle() {
		return this.state === IDLE$8;
	}
	get isDelay() {
		return this.state === DELAY;
	}
	get isCountDown() {
		return this.state === COUNTDOWN;
	}
	get isRunning() {
		return this.state === DELAY || this.state === COUNTDOWN;
	}
	get isDone() {
		return this.state === DONE;
	}
	get isOddIteration() {
		return (this.repeatCounter & 1) === 1;
	}
	get isEvenIteration() {
		return (this.repeatCounter & 1) === 0;
	}
};
var IDLE$8 = 0;
var DELAY = 1;
var COUNTDOWN = 2;
var REPEATDELAY = 3;
var DONE = -1;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/componentbase/timerticktask/TimerTask.js
var TimerTickTask = class extends SceneUpdateTickTask {
	constructor(parent, config) {
		super(parent, config);
		this.timer = new Timer$1();
	}
	shutdown(fromScene) {
		if (this.isShutdown) return;
		super.shutdown(fromScene);
		this.timer.destroy();
		this.timer = void 0;
	}
	start() {
		this.timer.start();
		super.start();
		return this;
	}
	stop() {
		this.timer.stop();
		super.stop();
		return this;
	}
	complete() {
		this.timer.stop();
		super.complete();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/componentbase/tweentask/EaseValueTaskBase.js
var GetValue$247 = __webpack_exports__Utils.Objects.GetValue;
var GetAdvancedValue$5 = __webpack_exports__Utils.Objects.GetAdvancedValue;
var GetEaseFunction = __webpack_exports__Tweens.Builders.GetEaseFunction;
var EaseValueTaskBase = class extends TimerTickTask {
	resetFromJSON(o) {
		this.timer.resetFromJSON(GetValue$247(o, "timer"));
		this.setEnable(GetValue$247(o, "enable", true));
		this.setTarget(GetValue$247(o, "target", this.parent));
		this.setDelay(GetAdvancedValue$5(o, "delay", 0));
		this.setDuration(GetAdvancedValue$5(o, "duration", 1e3));
		this.setEase(GetValue$247(o, "ease", "Linear"));
		this.setRepeat(GetValue$247(o, "repeat", 0));
		return this;
	}
	setEnable(e) {
		if (e == void 0) e = true;
		this.enable = e;
		return this;
	}
	setTarget(target) {
		if (target === void 0) target = this.parent;
		this.target = target;
		return this;
	}
	setDelay(time) {
		this.delay = time;
		return this;
	}
	setDuration(time) {
		this.duration = time;
		return this;
	}
	setRepeat(repeat) {
		this.repeat = repeat;
		return this;
	}
	setRepeatDelay(repeatDelay) {
		this.repeatDelay = repeatDelay;
		return this;
	}
	setEase(ease) {
		if (ease === void 0) ease = "Linear";
		this.ease = ease;
		this.easeFn = GetEaseFunction(ease);
		return this;
	}
	start() {
		if (this.timer.isRunning) return this;
		super.start();
		return this;
	}
	restart() {
		this.timer.stop();
		this.start.apply(this, arguments);
		return this;
	}
	stop(toEnd) {
		if (toEnd === void 0) toEnd = false;
		super.stop();
		if (toEnd) {
			this.timer.setT(1);
			this.updateTarget(this.target, this.timer);
			this.complete();
		}
		return this;
	}
	update(time, delta) {
		if (!this.isRunning || !this.enable || this.parent.hasOwnProperty("active") && !this.parent.active) return this;
		var target = this.target, timer = this.timer;
		timer.update(time, delta);
		if (!timer.isDelay) this.updateTarget(target, timer);
		this.emit("update", target, this);
		if (timer.isDone) this.complete();
		return this;
	}
	updateTarget(target, timer) {}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/system/IsSoundObject.js
var SoundObjectClass = __webpack_exports__Sound.BaseSound;
var IsSoundObject = function(object) {
	return object instanceof SoundObjectClass;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/audio/fade/Fade.js
var GetValue$246 = __webpack_exports__Utils.Objects.GetValue;
var GetAdvancedValue$4 = __webpack_exports__Utils.Objects.GetAdvancedValue;
var Linear$26 = __webpack_exports__Math.Linear;
var Fade$1 = class extends EaseValueTaskBase {
	constructor(scene, sound, config) {
		if (IsSoundObject(scene)) {
			config = sound;
			sound = scene;
			scene = void 0;
		}
		sound.active = true;
		sound.scene = scene;
		sound.game = sound.manager.game;
		super(sound, config);
		this.volume = {};
		this.resetFromJSON(config);
	}
	resetFromJSON(o) {
		super.resetFromJSON(o);
		this.setMode(GetValue$246(o, "mode", 0));
		this.setEnable(GetValue$246(o, "enable", true));
		this.setVolumeRange(GetAdvancedValue$4(o, "volume.start", this.parent.volume), GetAdvancedValue$4(o, "volume.end", 0));
		return this;
	}
	setMode(m) {
		if (typeof m === "string") m = MODE$4[m];
		this.mode = m;
		return this;
	}
	setVolumeRange(start, end) {
		this.volume.start = start;
		this.volume.end = end;
		return this;
	}
	start() {
		if (this.timer.isRunning) return this;
		this.parent.setVolume(this.volume.start);
		this.timer.setDelay(this.delay).setDuration(this.duration);
		super.start();
		return this;
	}
	updateTarget(parent, timer) {
		parent.volume = Linear$26(this.volume.start, this.volume.end, timer.t);
	}
	complete() {
		super.complete();
		switch (this.mode) {
			case 1:
				this.parent.stop();
				break;
			case 2:
				this.parent.stop();
				this.parent.destroy();
		}
		return this;
	}
};
var MODE$4 = {
	stop: 1,
	destroy: 2
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/audio/fade/FadeIn.js
var FadeIn$1 = function(scene, sound, duration, endVolume, startVolume) {
	if (IsSoundObject(scene)) {
		startVolume = endVolume;
		endVolume = duration;
		duration = sound;
		sound = scene;
		scene = void 0;
	}
	if (endVolume === void 0) endVolume = 1;
	if (startVolume === void 0) startVolume = 0;
	var config = {
		mode: 0,
		volume: {
			start: startVolume,
			end: endVolume
		},
		duration
	};
	if (typeof sound === "string") sound = scene.sys.sound.add(sound);
	var fade;
	if (sound.hasOwnProperty("_fade")) {
		fade = sound._fade;
		fade.stop().resetFromJSON(config);
	} else {
		fade = new Fade$1(scene, sound, config);
		sound._fade = fade;
	}
	fade.start();
	if (!sound.isPlaying) sound.setVolume(startVolume).play();
	return sound;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/audio/fade/FadeOut.js
var FadeOut = function(scene, sound, duration, destroy) {
	if (IsSoundObject(scene)) {
		destroy = duration;
		duration = sound;
		sound = scene;
		scene = void 0;
	}
	if (destroy === void 0) destroy = true;
	var config = {
		mode: destroy ? 2 : 1,
		volume: {
			start: sound.volume,
			end: 0
		},
		duration
	};
	var fade;
	if (sound.hasOwnProperty("_fade")) {
		fade = sound._fade;
		fade.stop().resetFromJSON(config);
	} else {
		fade = new Fade$1(scene, sound, config);
		sound._fade = fade;
	}
	fade.start();
	if (!sound.isPlaying) sound.play();
	return sound;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/audio/soundmanager/methods/BackgroundMusicMethods.js
var GetValue$245 = __webpack_exports__Utils.Objects.GetValue;
var BackgroundMusicMethods_default = {
	setBackgroundMusicLoop(value) {
		if (value === void 0) value = true;
		this.backgroundMusicLoop = value;
		return this;
	},
	setBackgroundMusicFadeTime(time) {
		this.backgroundMusicFadeTime = time;
		return this;
	},
	getBackgroundMusic() {
		return this.backgroundMusic;
	},
	setCurrentBackgroundMusic(music) {
		this.backgroundMusic = music;
		if (music) {
			music.once("complete", function() {
				if (this.backgroundMusic === music) {
					this.backgroundMusic.destroy();
					this.backgroundMusic = void 0;
				}
			}, this).once("destroy", function() {
				if (this.backgroundMusic === music) this.backgroundMusic = void 0;
			}, this);
			if (!music.isPlaying) music.play();
		}
		return this;
	},
	playBackgroundMusic(key, config) {
		if (!this.hasAudio(key)) {
			console.error(`[Sound manager] Audio key'${key}' is not existed`);
			return this;
		}
		if (this.backgroundMusic && this.backgroundMusic.key === key) return this;
		this.stopBackgroundMusic();
		var music = this.sound.add(key, {
			loop: GetValue$245(config, "loop", this.backgroundMusicLoop),
			mute: GetValue$245(config, "mute", this.backgroundMusicMute),
			volume: GetValue$245(config, "volume", this.backgroundMusicVolume),
			detune: GetValue$245(config, "detune", 0),
			rate: GetValue$245(config, "rate", 1)
		});
		this.setCurrentBackgroundMusic(music);
		if (this.backgroundMusicFadeTime > 0) this.fadeInBackgroundMusic(this.backgroundMusicFadeTime);
		return this;
	},
	pauseBackgroundMusic() {
		if (this.backgroundMusic) this.backgroundMusic.pause();
		return this;
	},
	resumeBackgroundMusic() {
		if (this.backgroundMusic) this.backgroundMusic.resume();
		return this;
	},
	stopBackgroundMusic() {
		if (this.backgroundMusic) {
			if (this.backgroundMusicFadeTime > 0) this.fadeOutBackgroundMusic(this.backgroundMusicFadeTime, true);
			else {
				this.backgroundMusic.stop();
				this.backgroundMusic.destroy();
				this.backgroundMusic = void 0;
			}
		}
		return this;
	},
	fadeInBackgroundMusic(time) {
		if (this.backgroundMusic) FadeIn$1(this.backgroundMusic, time, this.backgroundMusicVolume, 0);
		return this;
	},
	fadeOutBackgroundMusic(time, isStopped) {
		if (this.backgroundMusic) FadeOut(this.backgroundMusic, time, isStopped);
		return this;
	},
	crossFadeBackgroundMusic(key, time) {
		if (!this.hasAudio(key)) {
			console.error(`[Sound manager] Audio key'${key}' is not existed`);
			return this;
		}
		var backgroundMusicFadeTimeSave = this.backgroundMusicFadeTime;
		this.backgroundMusicFadeTime = 0;
		this.fadeOutBackgroundMusic(time, true).playBackgroundMusic(key).fadeInBackgroundMusic(time);
		this.backgroundMusicFadeTime = backgroundMusicFadeTimeSave;
		return this;
	},
	setBackgroundMusicMute(mute) {
		if (mute === void 0) mute = true;
		this.backgroundMusicMute = mute;
		return this;
	},
	setBackgroundMusicVolume(volume) {
		this.backgroundMusicVolume = volume;
		return this;
	},
	setBackgroundMusicRate(rate) {
		if (this.backgroundMusic) this.backgroundMusic.setRate(rate);
		return this;
	},
	setBackgroundMusicDetune(detune) {
		if (this.backgroundMusic) this.backgroundMusic.setDetune(detune);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/audio/soundmanager/methods/BackgroundMusic2Methods.js
var GetValue$244 = __webpack_exports__Utils.Objects.GetValue;
var BackgroundMusic2Methods_default = {
	setBackgroundMusic2Loop(value) {
		if (value === void 0) value = true;
		this.backgroundMusic2Loop = value;
		return this;
	},
	setBackgroundMusic2FadeTime(time) {
		this.backgroundMusic2FadeTime = time;
		return this;
	},
	getBackgroundMusic2() {
		return this.backgroundMusic2;
	},
	setCurrentBackgroundMusic2(music) {
		this.backgroundMusic2 = music;
		if (music) {
			music.once("complete", function() {
				if (this.backgroundMusic2 === music) {
					this.backgroundMusic2.destroy();
					this.backgroundMusic2 = void 0;
				}
			}, this).once("destroy", function() {
				if (this.backgroundMusic2 === music) this.backgroundMusic2 = void 0;
			}, this);
			if (!music.isPlaying) music.play();
		}
		return this;
	},
	playBackgroundMusic2(key, config) {
		if (!this.hasAudio(key)) {
			console.error(`[Sound manager] Audio key'${key}' is not existed`);
			return this;
		}
		if (this.backgroundMusic2 && this.backgroundMusic2.key === key) return this;
		this.stopBackgroundMusic2();
		var music = this.sound.add(key, {
			loop: GetValue$244(config, "loop", this.backgroundMusicLoop),
			mute: GetValue$244(config, "mute", this.backgroundMusic2Mute),
			volume: GetValue$244(config, "volume", this.backgroundMusic2Volume),
			detune: GetValue$244(config, "detune", 0),
			rate: GetValue$244(config, "rate", 1)
		});
		this.setCurrentBackgroundMusic2(music);
		if (this.backgroundMusic2FadeTime > 0) this.fadeInBackgroundMusic2(this.backgroundMusic2FadeTime);
		return this;
	},
	pauseBackgroundMusic2() {
		if (this.backgroundMusic2) this.backgroundMusic2.pause();
		return this;
	},
	resumeBackgroundMusic2() {
		if (this.backgroundMusic2) this.backgroundMusic2.resume();
		return this;
	},
	stopBackgroundMusic2() {
		if (this.backgroundMusic2) {
			if (this.backgroundMusic2FadeTime > 0) this.fadeOutBackgroundMusic2(this.backgroundMusic2FadeTime, true);
			else {
				this.backgroundMusic2.stop();
				this.backgroundMusic2.destroy();
				this.backgroundMusic2 = void 0;
			}
		}
		return this;
	},
	fadeInBackgroundMusic2(time) {
		if (this.backgroundMusic2) FadeIn$1(this.backgroundMusic2, time, this.backgroundMusic2Volume, 0);
		return this;
	},
	fadeOutBackgroundMusic2(time, isStopped) {
		if (this.backgroundMusic2) FadeOut(this.backgroundMusic2, time, isStopped);
		return this;
	},
	crossFadeBackgroundMusic2(key, time) {
		if (!this.hasAudio(key)) {
			console.error(`[Sound manager] Audio key'${key}' is not existed`);
			return this;
		}
		var backgroundMusic2FadeTimeSave = this.backgroundMusic2FadeTime;
		this.backgroundMusic2FadeTime = 0;
		this.fadeOutBackgroundMusic2(time, true).playBackgroundMusic2(key).fadeInBackgroundMusic2(time);
		this.backgroundMusic2FadeTime = backgroundMusic2FadeTimeSave;
		return this;
	},
	setBackgroundMusic2Mute(mute) {
		if (mute === void 0) mute = true;
		this.backgroundMusic2Mute = mute;
		return this;
	},
	setBackgroundMusic2Volume(volume) {
		this.backgroundMusic2Volume = volume;
		return this;
	},
	setBackgroundMusic2Rate(rate) {
		if (this.backgroundMusic2) this.backgroundMusic2.setRate(rate);
		return this;
	},
	setBackgroundMusic2Detune(detune) {
		if (this.backgroundMusic2) this.backgroundMusic2.setDetune(detune);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/audio/soundmanager/methods/SoundEffectsMethods.js
var RemoveItem$10 = __webpack_exports__Utils.Array.Remove;
var GetValue$243 = __webpack_exports__Utils.Objects.GetValue;
var SoundEffectsMethods_default = {
	getSoundEffects() {
		return this.soundEffects;
	},
	getLastSoundEffect() {
		return this.soundEffects[this.soundEffects.length - 1];
	},
	playSoundEffect(key, config) {
		if (!this.hasAudio(key)) {
			console.error(`[Sound manager] Audio key'${key}' is not existed`);
			return this;
		}
		var music = this.sound.add(key, {
			mute: GetValue$243(config, "mute", this.soundEffectsMute),
			volume: GetValue$243(config, "volume", this.soundEffectsVolume),
			detune: GetValue$243(config, "detune", 0),
			rate: GetValue$243(config, "rate", 1)
		});
		this.soundEffects.push(music);
		music.once("complete", function() {
			music.destroy();
			if (!this.sound) return;
			RemoveItem$10(this.soundEffects, music);
		}, this).once("destroy", function() {
			if (!this.sound) return;
			RemoveItem$10(this.soundEffects, music);
		}, this).play();
		return this;
	},
	stopAllSoundEffects() {
		for (var i = this.soundEffects.length - 1; i >= 0; i--) {
			var soundEffect = this.soundEffects[i];
			soundEffect.stop();
			soundEffect.destroy();
		}
		return this;
	},
	fadeInSoundEffect(time) {
		var soundEffect = this.getLastSoundEffect();
		if (soundEffect) FadeIn$1(soundEffect, time, this.soundEffectsVolume, 0);
		return this;
	},
	fadeOutSoundEffect(time, isStopped) {
		var soundEffect = this.getLastSoundEffect();
		if (soundEffect) FadeOut(soundEffect, time, isStopped);
		return this;
	},
	fadeOutAllSoundEffects(time, isStopped) {
		for (var i = this.soundEffects.length - 1; i >= 0; i--) FadeOut(this.soundEffects[i], time, isStopped);
		return this;
	},
	setSoundEffectMute(mute, lastSoundEffect) {
		if (mute === void 0) mute = true;
		if (lastSoundEffect === void 0) lastSoundEffect = false;
		if (lastSoundEffect) {
			var soundEffect = this.getLastSoundEffect();
			if (soundEffect) soundEffect.setMute(mute);
		} else this.soundEffectsMute = mute;
		return this;
	},
	setSoundEffectVolume(volume, lastSoundEffect) {
		if (lastSoundEffect === void 0) lastSoundEffect = false;
		if (lastSoundEffect) {
			var soundEffect = this.getLastSoundEffect();
			if (soundEffect) soundEffect.setVolume(volume);
		} else this.soundEffectsVolume = volume;
		return this;
	},
	setSoundEffectDetune(detune, lastSoundEffect) {
		if (lastSoundEffect === void 0) lastSoundEffect = false;
		var soundEffects;
		if (lastSoundEffect) soundEffects = [this.getLastSoundEffect()];
		else soundEffects = this.soundEffects;
		for (var i = 0, cnt = soundEffects.length; i < cnt; i++) soundEffects[i].setDetune(detune);
		return this;
	},
	setSoundEffectRate(rate, lastSoundEffect) {
		if (lastSoundEffect === void 0) lastSoundEffect = false;
		var soundEffects;
		if (lastSoundEffect) soundEffects = [this.getLastSoundEffect()];
		else soundEffects = this.soundEffects;
		for (var i = 0, cnt = soundEffects.length; i < cnt; i++) soundEffects[i].setRate(rate);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/audio/soundmanager/methods/SoundEffects2Methods.js
var RemoveItem$9 = __webpack_exports__Utils.Array.Remove;
var GetValue$242 = __webpack_exports__Utils.Objects.GetValue;
var SoundEffects2Methods_default = {
	getSoundEffects2() {
		return this.soundEffects2;
	},
	getLastSoundEffect2() {
		return this.soundEffects2[this.soundEffects2.length - 1];
	},
	playSoundEffect2(key, config) {
		if (!this.hasAudio(key)) {
			console.error(`[Sound manager] Audio key'${key}' is not existed`);
			return this;
		}
		var music = this.sound.add(key, {
			mute: GetValue$242(config, "mute", this.soundEffects2Mute),
			volume: GetValue$242(config, "volume", this.soundEffects2Volume),
			detune: GetValue$242(config, "detune", 0),
			rate: GetValue$242(config, "rate", 1)
		});
		this.soundEffects2.push(music);
		music.once("complete", function() {
			music.destroy();
			if (!this.sound) return;
			RemoveItem$9(this.soundEffects2, music);
		}, this).once("destroy", function() {
			if (!this.sound) return;
			RemoveItem$9(this.soundEffects2, music);
		}, this).play();
		return this;
	},
	stopAllSoundEffects2() {
		for (var i = this.soundEffects.length - 1; i >= 0; i--) {
			var soundEffect = this.soundEffects[i];
			soundEffect.stop();
			soundEffect.destroy();
		}
		return this;
	},
	fadeInSoundEffect2(time) {
		var soundEffect = this.getLastSoundEffect2();
		if (soundEffect) FadeIn$1(soundEffect, time, this.soundEffects2Volume, 0);
		return this;
	},
	fadeOutSoundEffect2(time, isStopped) {
		var soundEffect = this.getLastSoundEffect2();
		if (soundEffect) FadeOut(soundEffect, time, isStopped);
		return this;
	},
	fadeOutAllSoundEffects2(time, isStopped) {
		for (var i = this.soundEffects2.length - 1; i >= 0; i--) FadeOut(this.soundEffects2[i], time, isStopped);
		return this;
	},
	setSoundEffect2Mute(mute, lastSoundEffect) {
		if (mute === void 0) mute = true;
		if (lastSoundEffect === void 0) lastSoundEffect = false;
		if (lastSoundEffect) {
			var soundEffect = this.getLastSoundEffect2();
			if (soundEffect) soundEffect.setMute(mute);
		} else this.soundEffects2Mute = mute;
		return this;
	},
	setSoundEffect2Volume(volume, lastSoundEffect) {
		if (lastSoundEffect === void 0) lastSoundEffect = false;
		if (lastSoundEffect) {
			var soundEffect = this.getLastSoundEffect2();
			if (soundEffect) soundEffect.setVolume(volume);
		} else this.soundEffects2Volume = volume;
		return this;
	},
	setSoundEffect2Detune(detune, lastSoundEffect) {
		if (lastSoundEffect === void 0) lastSoundEffect = false;
		var soundEffects;
		if (lastSoundEffect) soundEffects = [this.getLastSoundEffect2()];
		else soundEffects = this.soundEffects2;
		for (var i = 0, cnt = soundEffects.length; i < cnt; i++) soundEffects[i].setDetune(detune);
		return this;
	},
	setSoundEffect2Rate(rate, lastSoundEffect) {
		if (lastSoundEffect === void 0) lastSoundEffect = false;
		var soundEffects;
		if (lastSoundEffect) soundEffects = [this.getLastSoundEffect2()];
		else soundEffects = this.soundEffects2;
		for (var i = 0, cnt = soundEffects.length; i < cnt; i++) soundEffects[i].setRate(rate);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/audio/soundmanager/methods/Methods.js
var Methods$20 = { hasAudio: HasaAudio };
Object.assign(Methods$20, BackgroundMusicMethods_default, BackgroundMusic2Methods_default, SoundEffectsMethods_default, SoundEffects2Methods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/audio/soundmanager/SoundManager.js
var GetValue$241 = __webpack_exports__Utils.Objects.GetValue;
var SoundManager = class {
	constructor(game, config) {
		this.sound = GetSoundManager(game);
		this.backgroundMusic = void 0;
		this._backgroundMusicVolume = GetValue$241(config, "bgm.volume", 1);
		this._backgroundMusicMute = GetValue$241(config, "bgm.mute", false);
		this.setBackgroundMusicLoop(GetValue$241(config, "bgm.loop", true));
		this.setBackgroundMusicFadeTime(GetValue$241(config, "bgm.fade", 500));
		this.backgroundMusic2 = void 0;
		this._backgroundMusic2Volume = GetValue$241(config, "bgm2.volume", 1);
		this._backgroundMusic2Mute = GetValue$241(config, "bgm2.mute", false);
		this.setBackgroundMusic2Loop(GetValue$241(config, "bgm2.loop", true));
		this.setBackgroundMusic2FadeTime(GetValue$241(config, "bgm2.fade", 500));
		this.soundEffects = [];
		this._soundEffectsVolume = GetValue$241(config, "soundEffect.volume", 1);
		this.soundEffects2 = [];
		this._soundEffects2Volume = GetValue$241(config, "soundEffect2.volume", 1);
		var initialBackgroundMusic = GetValue$241(config, "bgm.initial", void 0);
		if (initialBackgroundMusic) this.setCurrentBackgroundMusic(initialBackgroundMusic);
		var initialBackgroundMusic2 = GetValue$241(config, "bgm2.initial", void 0);
		if (initialBackgroundMusic2) this.setCurrentBackgroundMusic2(initialBackgroundMusic2);
	}
	destroy() {
		if (this.backgroundMusic) this.backgroundMusic.destroy();
		this.backgroundMusic = void 0;
		if (this.backgroundMusic2) this.backgroundMusic2.destroy();
		this.backgroundMusic2 = void 0;
		if (this.soundEffects.length) for (var i = this.soundEffects.length - 1; i >= 0; i--) this.soundEffects[i].destroy();
		this.soundEffects.length = 0;
		if (this.soundEffects2.length) for (var i = this.soundEffects2.length - 1; i >= 0; i--) this.soundEffects2[i].destroy();
		this.soundEffects2.length = 0;
		this.sound = void 0;
		return this;
	}
	get backgroundMusicMute() {
		return this._backgroundMusicMute;
	}
	set backgroundMusicMute(value) {
		this._backgroundMusicMute = value;
		if (this.backgroundMusic) this.backgroundMusic.setMute(mute);
	}
	get backgroundMusicVolume() {
		return this._backgroundMusicVolume;
	}
	set backgroundMusicVolume(value) {
		this._backgroundMusicVolume = value;
		if (this.backgroundMusic) this.backgroundMusic.setVolume(value);
	}
	get backgroundMusic2Mute() {
		return this._backgroundMusic2Mute;
	}
	set backgroundMusic2Mute(value) {
		this._backgroundMusic2Mute = value;
		if (this.backgroundMusic2) this.backgroundMusic2.setMute(mute);
	}
	get backgroundMusic2Volume() {
		return this._backgroundMusic2Volume;
	}
	set backgroundMusic2Volume(value) {
		this._backgroundMusic2Volume = value;
		if (this.backgroundMusic2) this.backgroundMusic2.setVolume(value);
	}
	get soundEffectsMute() {
		return this._soundEffectsMute;
	}
	set soundEffectsMute(value) {
		this._soundEffectsMute = value;
		var soundEffects = this.soundEffects;
		for (var i = 0, cnt = soundEffects.length; i < cnt; i++) soundEffects[i].setMute(value);
	}
	get soundEffectsVolume() {
		return this._soundEffectsVolume;
	}
	set soundEffectsVolume(value) {
		this._soundEffectsVolume = value;
		var soundEffects = this.soundEffects;
		for (var i = 0, cnt = soundEffects.length; i < cnt; i++) soundEffects[i].setVolume(value);
	}
	get soundEffects2Mute() {
		return this._soundEffects2Mute;
	}
	set soundEffects2Mute(value) {
		this._soundEffects2Mute = value;
		var soundEffects = this.soundEffects;
		for (var i = 0, cnt = soundEffects2.length; i < cnt; i++) soundEffects[i].setMute(value);
	}
	get soundEffects2Volume() {
		return this._soundEffects2Volume;
	}
	set soundEffects2Volume(value) {
		this._soundEffects2Volume = value;
		var soundEffects = this.soundEffects2;
		for (var i = 0, cnt = soundEffects.length; i < cnt; i++) soundEffects[i].setVolume(value);
	}
};
Object.assign(SoundManager.prototype, Methods$20);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/time/clock/BaseClock.js
var GetValue$240 = __webpack_exports__Utils.Objects.GetValue;
var BaseClock = class extends TickTask {
	constructor(parent, config) {
		super(parent, config);
		this.resetFromJSON(config);
		this.boot();
	}
	resetFromJSON(o) {
		this.isRunning = GetValue$240(o, "isRunning", false);
		this.timeScale = GetValue$240(o, "timeScale", 1);
		this.now = GetValue$240(o, "now", 0);
		return this;
	}
	toJSON() {
		return {
			isRunning: this.isRunning,
			timeScale: this.timeScale,
			now: this.now,
			tickingMode: this.tickingMode
		};
	}
	start(startAt) {
		if (startAt === void 0) startAt = 0;
		this.delta = 0;
		this.now = startAt;
		super.start();
		return this;
	}
	seek(time) {
		this.now = time;
		return this;
	}
	setTimeScale(value) {
		this.timeScale = value;
		return this;
	}
	tick(delta) {
		delta *= this.timeScale;
		this.now += delta;
		this.delta = delta;
		this.emit("update", this.now, this.delta);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/time/clock/Clock.js
var Clock = class extends BaseClock {
	startTicking() {
		super.startTicking();
		this.scene.sys.events.on("update", this.update, this);
	}
	stopTicking() {
		super.stopTicking();
		if (this.scene) this.scene.sys.events.off("update", this.update, this);
	}
	update(time, delta) {
		if (!this.isRunning || this.timeScale === 0) return this;
		this.tick(delta);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/math/Yoyo.js
var Yoyo$1 = function(t, threshold) {
	if (threshold === void 0) threshold = .5;
	if (t <= threshold) t = t / threshold;
	else t = 1 - (t - threshold) / (1 - threshold);
	return t;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/time/progresses/Timer.js
var Clamp$15 = __webpack_exports__Math.Clamp;
var Timer = class {
	constructor(timeline, config) {
		this.setTimeline(timeline).reset(config);
	}
	setTimeline(timeline) {
		this.timeline = timeline;
		return this;
	}
	setName(name) {
		this.name = name;
		return this;
	}
	setCallbacks(target, onStart, onProgress, onComplete) {
		this.target = target;
		this.onStart = onStart;
		this.onProgress = onProgress;
		this.onComplete = onComplete;
		return this;
	}
	setDuration(duration, yoyo) {
		if (yoyo === void 0) yoyo = false;
		this.duration = duration;
		this.remainder = duration;
		this.t = 0;
		this.yoyo = yoyo;
		return this;
	}
	setPaused(state) {
		this.isPaused = state;
		return this;
	}
	pause() {
		this.isPaused = true;
		return this;
	}
	resume() {
		this.isPaused = false;
		return this;
	}
	setRemoved(state) {
		this.removed = state;
		return this;
	}
	remove() {
		this.removed = true;
		return this;
	}
	seek(t) {
		this.remainder = this.duration * (1 - t);
		return this;
	}
	reset(o) {
		this.setName(o.name).setDuration(o.duration, o.yoyo).setCallbacks(o.target, o.onStart, o.onProgress, o.onComplete).setPaused(false).setRemoved(false);
		return this;
	}
	onFree() {
		this.setTimeline().setCallbacks();
	}
	getProgress() {
		var value = 1 - this.remainder / this.duration;
		value = Clamp$15(value, 0, 1);
		if (this.yoyo) value = Yoyo$1(value);
		return value;
	}
	setProgress(value) {
		value = Clamp$15(value, 0, 1);
		this.remainder = this.duration * (1 - value);
	}
	runCallback(callback) {
		if (!callback) return;
		callback(this.target, this.t, this);
	}
	update(time, delta) {
		if (this.removed) return true;
		else if (this.isPaused) return false;
		this.remainder -= delta;
		this.t = this.getProgress();
		this.runCallback(this.onProgress);
		var isCompleted = this.remainder <= 0;
		if (isCompleted) this.runCallback(this.onComplete);
		return isCompleted;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/time/progresses/TimerPool.js
var TimerPool$1 = class extends pool_default {
	allocate() {
		return this.pop();
	}
	free(timer) {
		timer.onFree();
		this.push(timer);
	}
	freeMultiple(arr) {
		for (var i = 0, cnt = arr.length; i < cnt; i++) this.free(arr[i]);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/time/progresses/Timeline.js
var GetValue$239 = __webpack_exports__Utils.Objects.GetValue;
var TimerPool = new TimerPool$1();
var Timeline = class extends Clock {
	constructor(parent, config) {
		super(parent, config);
		this.addedTimers = [];
		this.timers = [];
		this.timerPool = GetValue$239(config, "pool", TimerPool);
	}
	shutdown() {
		if (this.isShutdown) return;
		this.timerPool.freeMultiple(this.addedTimers).freeMultiple(this.timers);
		this.timerPool = void 0;
		this.addedTimers = void 0;
		this.timers = void 0;
		super.shutdown();
	}
	addTimer(config) {
		var timer = this.timerPool.allocate();
		if (!timer) timer = new Timer(this, config);
		else timer.setTimeline(this).reset(config);
		this.addedTimers.push(timer);
		timer.runCallback(timer.onStart);
		if (!this.isRunning) this.start();
		return timer;
	}
	delayCall(delay, callback, args, scope) {
		return this.addTimer({
			duration: delay,
			onComplete: function(target, t, timer) {
				if (args === void 0) args = [];
				args.push(timer);
				callback.apply(scope, args);
			}
		});
	}
	delayEvent(delay, eventName) {
		this.removeDelayEvent(eventName);
		var timer = this.delayCall(delay, function() {
			this.removeDelayEvent(eventName);
			this.emit(eventName);
		}, [], this);
		this.once(`_remove.${eventName}`, function() {
			timer.remove();
			timer = void 0;
		});
		return this;
	}
	removeDelayEvent(eventName) {
		this.emit(`_remove.${eventName}`);
		return this;
	}
	getTimers(name) {
		var timers = [];
		var timerQueues = [this.addedTimers, this.timers];
		for (var ti = 0, tcnt = timerQueues.length; ti < tcnt; ti++) {
			var timerQueue = timerQueues[ti];
			for (var i = 0, cnt = timerQueue.length; i < cnt; i++) {
				var timer = timerQueue[i];
				if (timer.name === name) timers.push(timer);
			}
		}
		return timers;
	}
	update(time, delta) {
		super.update(time, delta);
		if (!this.isRunning) return;
		this.timers.push(...this.addedTimers);
		this.addedTimers.length = 0;
		var pendingTimers = [];
		for (var i = 0, cnt = this.timers.length; i < cnt; i++) {
			var timer = this.timers[i];
			if (timer.update(this.now, this.delta)) this.timerPool.free(timer);
			else pendingTimers.push(timer);
		}
		this.timers = pendingTimers;
		if (this.timers.length === 0 && this.addedTimers.length === 0) this.complete();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/waitevent/const.js
var WaitCompleteEvent = "_wait.complete";
var RemoveWaitEvents = "_remove.wait";
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/time/PreUpdateDelayCall.js
var PreUpdateDelayCall = function(gameObject, delay, callback, scope, args) {
	var scene = GetSceneObject(gameObject);
	return scene.time.delayedCall(delay, function() {
		scene.sys.events.once("preupdate", function() {
			callback.call(scope, args);
		});
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/waitevent/WaitEvent.js
var WaitEvent$1 = class {
	constructor(parent) {
		if (!parent) {
			this.setEventEmitter(true);
			parent = this;
		}
		this.parent = parent;
		this.waitId = 0;
		this.waitCompleteEventName = WaitCompleteEvent;
		this.removeWaitEventsEventName = RemoveWaitEvents;
	}
	destroy() {
		this.removeWaitEvents();
		this.clearWaitCompleteCallbacks();
		this.parent = null;
	}
	waitEvent(eventEmitter, eventName, completeNextTick) {
		var callback = this.getWaitCompleteTriggerCallback(completeNextTick);
		eventEmitter.once(eventName, callback, this);
		this.parent.once(this.removeWaitEventsEventName, function() {
			eventEmitter.off(eventName, callback, this);
		});
		return this.parent;
	}
	getWaitCompleteTriggerCallback(completeNextTick) {
		if (completeNextTick === void 0) completeNextTick = true;
		var waitId = this.waitId;
		var self = this;
		var completeCallback = function() {
			if (waitId < self.waitId) return;
			self.waitId++;
			self.removeWaitEvents();
			self.parent.emit(self.waitCompleteEventName);
		};
		if (completeNextTick) {
			var completeCallbackNextTick = function() {
				PreUpdateDelayCall(self.parent, 0, completeCallback);
			};
			return completeCallbackNextTick;
		} else return completeCallback;
	}
	removeWaitEvents() {
		this.parent.emit(this.removeWaitEventsEventName);
		return this;
	}
	addWaitCompleteCallback(callback, scope) {
		this.parent.on(this.waitCompleteEventName, callback, scope);
		return this;
	}
	clearWaitCompleteCallbacks() {
		this.parent.off(this.waitCompleteEventName);
		return this;
	}
};
Object.assign(WaitEvent$1.prototype, EventEmitterMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/managers/waiteventmanager/WaitTimeMethods.js
var WaitTimeMethods_default = { waitTime(duration) {
	var timeline = this.parent.timeline;
	timeline.delayEvent(duration, "delay");
	this.parent.once(this.removeWaitEventsEventName, function() {
		timeline.removeDelayEvent("delay");
	});
	return this.waitEvent(timeline, "delay");
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/string/Split.js
var Split = function(s, delimiter) {
	var regexString = `(?<!\\\\)\\${delimiter}`;
	var escapeString = `\\${delimiter}`;
	return s.split(new RegExp(regexString, "g")).map(function(s) {
		return s.replace(escapeString, delimiter);
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/managers/waiteventmanager/WaitInputMethods.js
var WaitInputMethods_default = {
	setClickTarget(target) {
		this.clickTarget = target;
		if (!target) this.touchEE = null;
		else if (IsSceneObject(target)) this.touchEE = target.input;
		else this.touchEE = target.setInteractive();
		return this;
	},
	clearClickTarget() {
		this.setClickTarget();
		return this;
	},
	setClickShortcutKeys(keys) {
		this.clickShortcutKeys = keys;
		return this;
	},
	clearClickShortcutKeys() {
		this.setClickShortcutKeys();
		return this;
	},
	waitClick() {
		var touchEE = this.touchEE;
		var clickShortcutKeys = this.clickShortcutKeys;
		if (touchEE || clickShortcutKeys) {
			if (touchEE) this.waitEvent(touchEE, "pointerdown");
			if (clickShortcutKeys) this.waitKeyDown(clickShortcutKeys);
		} else this.waitTime(0);
		return this;
	},
	waitKeyDown(key) {
		var eventEmitter = this.scene.input.keyboard;
		if (typeof key === "string") {
			if (key.indexOf("|") === -1) return this.waitEvent(eventEmitter, `keydown-${key.toUpperCase()}`);
			else {
				var keys = Split(key, "|");
				for (var i = 0, cnt = keys.length; i < cnt; i++) this.waitEvent(eventEmitter, `keydown-${keys[i].toUpperCase()}`);
				return this.parent;
			}
		} else return this.waitEvent(eventEmitter, "keydown");
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/managers/waiteventmanager/WaitGameObjectMethods.js
var WaitGameObjectMethods_default = {
	waitGameObjectTweenComplete(goType, name, property) {
		var tweenTask = this.parent.getGameObjectTweenTask(goType, name, property);
		if (tweenTask) return this.waitEvent(tweenTask, "complete");
		return this.waitTime(0);
	},
	waitGameObjectDataFlag(goType, name, dataKey, trueFlag) {
		var gameObject = this.parent.getGameObject(goType, name);
		if (!gameObject) return this.waitTime(0);
		if (gameObject.getData(dataKey) === trueFlag) return this.waitTime(0);
		var eventName = `changedata-${dataKey}`;
		var callback = function(gameObject, value, previousValue) {
			value = !!value;
			if (value === trueFlag) gameObject.emit("_dataFlagMatch");
		};
		gameObject.on(eventName, callback);
		this.parent.once(this.removeWaitEventsEventName, function() {
			gameObject.off(eventName, callback);
		});
		return this.waitEvent(gameObject, "_dataFlagMatch");
	},
	waitGameObjectDestroy(goType, name) {
		var gameObject = this.parent.getGameObject(goType, name);
		if (!gameObject) return this.waitTime(0);
		return this.waitEvent(gameObject, "destroy");
	},
	waitGameObjectManagerEmpty(goType) {
		if (goType) {
			var gameObjectManager = this.parent.getGameObjectManager(goType);
			if (!gameObjectManager) return this.waitTime(0);
			return this.waitEvent(gameObjectManager, "empty");
		} else {
			var gameObjectManagers = this.parent.gameObjectManagers;
			var hasAnyWaitEvent = false;
			for (var name in gameObjectManagers) {
				hasAnyWaitEvent = true;
				this.waitEvent(gameObjectManagers[name], "empty");
			}
			if (!hasAnyWaitEvent) return this.waitTime(0);
			return this.parent;
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/managers/waiteventmanager/WaitCameraMethods.js
var WaitCameraMethods_default = {
	setCameraTarget(camera) {
		this.cameraTarget = camera;
		return this;
	},
	clearCameraTarget() {
		this.setCameraTarget();
		return this;
	},
	waitCameraEffectComplete(effectName, cameraName) {
		var camera;
		if (cameraName) camera = this.scene.cameras.getCamera(cameraName);
		else camera = this.cameraTarget;
		if (!camera) return this.waitTime(0);
		var effect, completeEventName;
		switch (effectName) {
			case "camera.fadein":
				effect = camera.fadeEffect;
				completeEventName = "camerafadeincomplete";
				break;
			case "camera.fadeout":
				effect = camera.fadeEffect;
				completeEventName = "camerafadeoutcomplete";
				break;
			case "camera.flash":
				effect = camera.flashEffect;
				completeEventName = "cameraflashcomplete";
				break;
			case "camera.shake":
				effect = camera.shakeEffect;
				completeEventName = "camerashakecomplete";
				break;
			case "camera.zoom":
				effect = camera.zoomEffect;
				completeEventName = "camerazoomcomplete";
				break;
			case "camera.rotate":
				effect = camera.rotateToEffect;
				completeEventName = "camerarotatecomplete";
				break;
			case "camera.scroll":
				effect = camera.panEffect;
				completeEventName = "camerapancomplete";
		}
		if (!effect.isRunning) return this.waitTime(0);
		return this.waitEvent(camera, completeEventName);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/managers/waiteventmanager/WaitMusicMethods.js
var WaitMusicMethods_default = {
	waitSoundEffectComplete() {
		if (!this.parent.soundManager) return this.waitTime(0);
		var music = this.parent.soundManager.getLastSoundEffect();
		if (!music) return this.waitTime(0);
		return this.waitEvent(music, "complete");
	},
	waitSoundEffect2Complete() {
		if (!this.parent.soundManager) return this.waitTime(0);
		var music = this.parent.soundManager.getLastSoundEffect2();
		if (!music) return this.waitTime(0);
		return this.waitEvent(music, "complete");
	},
	waitBackgroundMusicComplete() {
		if (!this.parent.soundManager) return this.waitTime(0);
		var music = this.parent.soundManager.getBackgroundMusic();
		if (!music) return this.waitTime(0);
		return this.waitEvent(music, "complete");
	},
	waitBackgroundMusic2Complete() {
		if (!this.parent.soundManager) return this.waitTime(0);
		var music = this.parent.soundManager.getBackgroundMusic2();
		if (!music) return this.waitTime(0);
		return this.waitEvent(music, "complete");
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/managers/waiteventmanager/WaitAny.js
var WaitAny$1 = function(config) {
	if (!config) return this.waitTime(0);
	var hasAnyWaitEvent = false;
	for (var name in config) switch (name) {
		case "time":
			hasAnyWaitEvent = true;
			this.waitTime(config.time);
			break;
		case "click":
			hasAnyWaitEvent = true;
			this.waitClick();
			break;
		case "key":
			hasAnyWaitEvent = true;
			this.waitKeyDown(config.key);
			break;
		case "bgm":
			hasAnyWaitEvent = true;
			this.waitBackgroundMusicComplete();
			break;
		case "bgm2":
			hasAnyWaitEvent = true;
			this.waitBackgroundMusic2Complete();
			break;
		case "se":
			hasAnyWaitEvent = true;
			this.waitSoundEffectComplete();
			break;
		case "se2":
			hasAnyWaitEvent = true;
			this.waitSoundEffect2Complete();
			break;
		case "camera":
			hasAnyWaitEvent = true;
			this.waitCameraEffectComplete(`camera.${config.camera.toLowerCase()}`, config.cameraName);
			break;
		default:
			var names = name.split(".");
			if (names.length === 2) {
				var gameObjectName = names[0];
				var propName = names[1];
				var gameObjectManager = this.parent.getGameObjectManager(void 0, gameObjectName);
				if (!gameObjectManager) continue;
				if (propName === "destroy") return this.waitGameObjectDestroy(void 0, gameObjectName);
				if (typeof gameObjectManager.getProperty(gameObjectName, propName) === "number") {
					hasAnyWaitEvent = true;
					this.waitGameObjectTweenComplete(void 0, gameObjectName, propName);
					continue;
				}
				var dataKey = propName;
				var matchFalseFlag = dataKey.startsWith("!");
				if (matchFalseFlag) dataKey = dataKey.substring(1);
				if (gameObjectManager.hasData(gameObjectName, propName)) {
					hasAnyWaitEvent = true;
					this.waitGameObjectDataFlag(void 0, gameObjectName, dataKey, !matchFalseFlag);
					continue;
				}
				this.waitEvent(gameObject, propName);
				continue;
			} else if (names.length === 1) {}
	}
	if (!hasAnyWaitEvent) this.waitTime(0);
	return this.parent;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/managers/waiteventmanager/Methods.js
var methods$37 = { waitAny: WaitAny$1 };
Object.assign(methods$37, WaitTimeMethods_default, WaitInputMethods_default, WaitGameObjectMethods_default, WaitCameraMethods_default, WaitMusicMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/managers/waiteventmanager/WaitEventManager.js
var WaitEventManager = class extends WaitEvent$1 {
	constructor(parent, config) {
		super(parent);
		this.waitCompleteEventName = GetValue$279(config, "completeEventName", this.waitCompleteEventName);
		this.setClickTarget(GetValue$279(config, "clickTarget", this.scene));
		this.setClickShortcutKeys(GetValue$279(config, "clickShortcutKeys", void 0));
		this.setCameraTarget(GetValue$279(config, "camera", this.scene.cameras.main));
	}
	get clickTarget() {
		return this.parent.clickTarget;
	}
	set clickTarget(value) {
		this.parent.clickTarget = value;
	}
	get clickShortcutKeys() {
		return this.parent.clickShortcutKeys;
	}
	set clickShortcutKeys(value) {
		this.parent.clickShortcutKeys = value;
	}
	get cameraTarget() {
		return this.parent.cameraTarget;
	}
	set cameraTarget(value) {
		this.parent.cameraTarget = value;
	}
	destroy() {
		this.clearClickTarget();
		this.clearClickShortcutKeys();
		this.clearCameraTarget();
		super.destroy();
	}
	get scene() {
		return this.parent.managersScene;
	}
};
Object.assign(WaitEventManager.prototype, methods$37);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/managers/InitManagers.js
var GetValue$238 = __webpack_exports__Utils.Objects.GetValue;
var InitManagers = function(scene, config) {
	this.clickTarget = void 0;
	this.clickShortcutKeys = void 0;
	this.cameraTarget = void 0;
	this.managersScene = scene;
	this.gameObjectManagers = {};
	var layerNames = GetValue$238(config, "layers", false);
	if (layerNames !== false) {
		var layerManager = new LayerManager(scene, {
			name: "LAYER",
			layers: layerNames,
			rootLayer: GetValue$238(config, "rootLayer", void 0),
			depth: GetValue$238(config, "layerDepth", void 0)
		});
		this.addGameObjectManager(layerManager);
		this.layerManager = layerManager;
	}
	var soundManagerConfig = GetValue$238(config, "sounds");
	if (soundManagerConfig !== false) this.soundManager = new SoundManager(scene, soundManagerConfig);
	this.timeline = new Timeline(this);
	this.waitEventManager = new WaitEventManager(this, config);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/managers/SetTimeScale.js
var SetTimeScale = function(value) {
	this.timeline.timeScale = value;
	for (var name in this.gameObjectManagers) this.gameObjectManagers[name].setTimeScale(value);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/managers/GetTimeScale.js
var GetTimeScale = function() {
	return this.timeline.timeScale;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/managers/DestroyManagers.js
var DestroyManagers = function(fromScene) {
	this.waitEventManager.destroy();
	this.waitEventManager = void 0;
	delete this.gameObjectManagers.layer;
	for (var name in this.gameObjectManagers) {
		this.gameObjectManagers[name].destroy(fromScene);
		delete this.gameObjectManagers[name];
	}
	if (this.layerManager) {
		this.layerManager.destroy(fromScene);
		this.layerManager = void 0;
	}
	if (this.soundManager) {
		this.soundManager.destroy();
		this.soundManager = void 0;
	}
	if (this.timeline) {
		this.timeline.destroy();
		this.timeline = void 0;
	}
	this.clickTarget = void 0;
	this.clickShortcutKeys = void 0;
	this.cameraTarget = void 0;
	this.managersScene = void 0;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/managers/GameObjectManagerMethods.js
var GameObjectManagerMethods_default$1 = {
	addGameObjectManager(config, GameObjectManagerClass) {
		var gameobjectManager;
		if (config instanceof GOManager) gameobjectManager = config;
		else if (typeof config === "string") {
			gameobjectManager = GameObjectManagerClass;
			gameobjectManager.name = config;
		} else {
			if (config === void 0) config = {};
			if (GameObjectManagerClass === void 0) GameObjectManagerClass = GOManager;
			if (!config.createGameObjectScope) config.createGameObjectScope = this;
			gameobjectManager = new GameObjectManagerClass(this.managersScene, config);
		}
		this.gameObjectManagers[gameobjectManager.name] = gameobjectManager;
		return this;
	},
	getGameObjectManager(managerName, gameObjectName) {
		if (managerName) {
			var manager = this.gameObjectManagers[managerName];
			return manager;
		} else {
			if (gameObjectName && gameObjectName.charAt(0) === "!") gameObjectName = gameObjectName.substring(1);
			for (var managerName in this.gameObjectManagers) {
				var manager = this.gameObjectManagers[managerName];
				if (manager.has(gameObjectName)) return manager;
			}
		}
	},
	getGameObjectManagerNames() {
		var names = [];
		for (var name in this.gameObjectManagers) names.push(name);
		return names;
	},
	getGameObjectManagerName(gameObjectName) {
		var gameObjectManager = this.getGameObjectManager(void 0, gameObjectName);
		if (!gameObjectManager) return;
		return gameObjectManager.name;
	},
	hasGameObjectMananger(managerName) {
		return managerName in this.gameObjectManagers;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/managers/GameObjectMethods.js
var GameObjectMethods_default = {
	createGameObject(goType, name, ...params) {
		this.getGameObjectManager(goType, name).add(name, ...params);
		return this;
	},
	destroyGameObject(goType, name) {
		var gameObjectManager = this.getGameObjectManager(goType, name);
		if (name === void 0) gameObjectManager.removeAll();
		else gameObjectManager.remove(name);
		return this;
	},
	hasGameObject(goType, name) {
		return !!this.getGameObjectManager(goType, name);
	},
	callGameObjectMethod(goType, name, methodName, ...params) {
		this.getGameObjectManager(goType, name).call(name, methodName, ...params);
		return this;
	},
	setGameObjectProperty(goType, name, prop, value) {
		this.getGameObjectManager(goType, name).setProperty(name, prop, value);
		return this;
	},
	easeGameObjectProperty(goType, name, config) {
		this.getGameObjectManager(goType, name).easeProperty(name, config);
		return this;
	},
	getGameObjectTweenTask(goType, name, property) {
		return this.getGameObjectManager(goType, name).getTweenTask(name, property);
	},
	getGameObject(goType, name, out) {
		var gameobjectManager = this.getGameObjectManager(goType, name);
		if (!gameobjectManager) return out;
		if (typeof name === "string") return gameobjectManager.getGO(name);
		else {
			if (out === void 0) out = [];
			var names = name;
			if (names === void 0) names = Object.keys(gameobjectManager.bobs);
			var isArrayOutput = Array.isArray(out);
			for (var i = 0, cnt = names.length; i < cnt; i++) {
				name = names[i];
				var gameObject = gameobjectManager.getGO(name);
				if (!gameObject) continue;
				if (isArrayOutput) out.push(gameObject);
				else out[name] = gameObject;
			}
			return out;
		}
	},
	addGameObject(goType, name, gameObject) {
		var gameobjectManager = this.getGameObjectManager(goType, name);
		if (typeof name === "string") gameobjectManager.addGO(name, gameObject);
		else {
			var names = name;
			for (name in names) gameobjectManager.addGO(name, names[name]);
		}
		return this;
	},
	drawGameObjectsBounds(goTypes, graphics, config) {
		if (goTypes instanceof __webpack_exports__GameObjects.Graphics) {
			config = graphics;
			graphics = goTypes;
			goTypes = void 0;
		}
		if (goTypes === void 0) goTypes = this.getGameObjectManagerNames();
		if (!Array.isArray(goTypes)) goTypes = [goTypes];
		for (var i = 0, cnt = goTypes.length; i < cnt; i++) this.getGameObjectManager(goTypes[i]).drawGameObjectsBounds(graphics, config);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/managers/Extend.js
var Extend = function(BaseClass) {
	class Managers extends BaseClass {}
	var Methods = {
		initManagers: InitManagers,
		setTimeScale: SetTimeScale,
		getTimeScale: GetTimeScale,
		destroyManagers: DestroyManagers
	};
	Object.assign(Managers.prototype, Methods, GameObjectManagerMethods_default$1, GameObjectMethods_default);
	return Managers;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/eventemitter/EventEmitter.js
var import_eventemitter3 = /* @__PURE__ */ __toESM((/* @__PURE__ */ __commonJSMin(((exports, module) => {
	var has = Object.prototype.hasOwnProperty;
	var prefix = "~";
	/**
	* Constructor to create a storage for our `EE` objects.
	* An `Events` instance is a plain object whose properties are event names.
	*
	* @constructor
	* @private
	*/
	function Events() {}
	if (Object.create) {
		Events.prototype = Object.create(null);
		if (!new Events().__proto__) prefix = false;
	}
	/**
	* Representation of a single event listener.
	*
	* @param {Function} fn The listener function.
	* @param {*} context The context to invoke the listener with.
	* @param {Boolean} [once=false] Specify if the listener is a one-time listener.
	* @constructor
	* @private
	*/
	function EE(fn, context, once) {
		this.fn = fn;
		this.context = context;
		this.once = once || false;
	}
	/**
	* Add a listener for a given event.
	*
	* @param {EventEmitter} emitter Reference to the `EventEmitter` instance.
	* @param {(String|Symbol)} event The event name.
	* @param {Function} fn The listener function.
	* @param {*} context The context to invoke the listener with.
	* @param {Boolean} once Specify if the listener is a one-time listener.
	* @returns {EventEmitter}
	* @private
	*/
	function addListener(emitter, event, fn, context, once) {
		if (typeof fn !== "function") throw new TypeError("The listener must be a function");
		var listener = new EE(fn, context || emitter, once), evt = prefix ? prefix + event : event;
		if (!emitter._events[evt]) emitter._events[evt] = listener, emitter._eventsCount++;
		else if (!emitter._events[evt].fn) emitter._events[evt].push(listener);
		else emitter._events[evt] = [emitter._events[evt], listener];
		return emitter;
	}
	/**
	* Clear event by name.
	*
	* @param {EventEmitter} emitter Reference to the `EventEmitter` instance.
	* @param {(String|Symbol)} evt The Event name.
	* @private
	*/
	function clearEvent(emitter, evt) {
		if (--emitter._eventsCount === 0) emitter._events = new Events();
		else delete emitter._events[evt];
	}
	/**
	* Minimal `EventEmitter` interface that is molded against the Node.js
	* `EventEmitter` interface.
	*
	* @constructor
	* @public
	*/
	function EventEmitter() {
		this._events = new Events();
		this._eventsCount = 0;
	}
	/**
	* Return an array listing the events for which the emitter has registered
	* listeners.
	*
	* @returns {Array}
	* @public
	*/
	EventEmitter.prototype.eventNames = function eventNames() {
		var names = [], events, name;
		if (this._eventsCount === 0) return names;
		for (name in events = this._events) if (has.call(events, name)) names.push(prefix ? name.slice(1) : name);
		if (Object.getOwnPropertySymbols) return names.concat(Object.getOwnPropertySymbols(events));
		return names;
	};
	/**
	* Return the listeners registered for a given event.
	*
	* @param {(String|Symbol)} event The event name.
	* @returns {Array} The registered listeners.
	* @public
	*/
	EventEmitter.prototype.listeners = function listeners(event) {
		var evt = prefix ? prefix + event : event, handlers = this._events[evt];
		if (!handlers) return [];
		if (handlers.fn) return [handlers.fn];
		for (var i = 0, l = handlers.length, ee = new Array(l); i < l; i++) ee[i] = handlers[i].fn;
		return ee;
	};
	/**
	* Return the number of listeners listening to a given event.
	*
	* @param {(String|Symbol)} event The event name.
	* @returns {Number} The number of listeners.
	* @public
	*/
	EventEmitter.prototype.listenerCount = function listenerCount(event) {
		var evt = prefix ? prefix + event : event, listeners = this._events[evt];
		if (!listeners) return 0;
		if (listeners.fn) return 1;
		return listeners.length;
	};
	/**
	* Calls each of the listeners registered for a given event.
	*
	* @param {(String|Symbol)} event The event name.
	* @returns {Boolean} `true` if the event had listeners, else `false`.
	* @public
	*/
	EventEmitter.prototype.emit = function emit(event, a1, a2, a3, a4, a5) {
		var evt = prefix ? prefix + event : event;
		if (!this._events[evt]) return false;
		var listeners = this._events[evt], len = arguments.length, args, i;
		if (listeners.fn) {
			if (listeners.once) this.removeListener(event, listeners.fn, void 0, true);
			switch (len) {
				case 1: return listeners.fn.call(listeners.context), true;
				case 2: return listeners.fn.call(listeners.context, a1), true;
				case 3: return listeners.fn.call(listeners.context, a1, a2), true;
				case 4: return listeners.fn.call(listeners.context, a1, a2, a3), true;
				case 5: return listeners.fn.call(listeners.context, a1, a2, a3, a4), true;
				case 6: return listeners.fn.call(listeners.context, a1, a2, a3, a4, a5), true;
			}
			for (i = 1, args = new Array(len - 1); i < len; i++) args[i - 1] = arguments[i];
			listeners.fn.apply(listeners.context, args);
		} else {
			var length = listeners.length, j;
			for (i = 0; i < length; i++) {
				if (listeners[i].once) this.removeListener(event, listeners[i].fn, void 0, true);
				switch (len) {
					case 1:
						listeners[i].fn.call(listeners[i].context);
						break;
					case 2:
						listeners[i].fn.call(listeners[i].context, a1);
						break;
					case 3:
						listeners[i].fn.call(listeners[i].context, a1, a2);
						break;
					case 4:
						listeners[i].fn.call(listeners[i].context, a1, a2, a3);
						break;
					default:
						if (!args) for (j = 1, args = new Array(len - 1); j < len; j++) args[j - 1] = arguments[j];
						listeners[i].fn.apply(listeners[i].context, args);
				}
			}
		}
		return true;
	};
	/**
	* Add a listener for a given event.
	*
	* @param {(String|Symbol)} event The event name.
	* @param {Function} fn The listener function.
	* @param {*} [context=this] The context to invoke the listener with.
	* @returns {EventEmitter} `this`.
	* @public
	*/
	EventEmitter.prototype.on = function on(event, fn, context) {
		return addListener(this, event, fn, context, false);
	};
	/**
	* Add a one-time listener for a given event.
	*
	* @param {(String|Symbol)} event The event name.
	* @param {Function} fn The listener function.
	* @param {*} [context=this] The context to invoke the listener with.
	* @returns {EventEmitter} `this`.
	* @public
	*/
	EventEmitter.prototype.once = function once(event, fn, context) {
		return addListener(this, event, fn, context, true);
	};
	/**
	* Remove the listeners of a given event.
	*
	* @param {(String|Symbol)} event The event name.
	* @param {Function} fn Only remove the listeners that match this function.
	* @param {*} context Only remove the listeners that have this context.
	* @param {Boolean} once Only remove one-time listeners.
	* @returns {EventEmitter} `this`.
	* @public
	*/
	EventEmitter.prototype.removeListener = function removeListener(event, fn, context, once) {
		var evt = prefix ? prefix + event : event;
		if (!this._events[evt]) return this;
		if (!fn) {
			clearEvent(this, evt);
			return this;
		}
		var listeners = this._events[evt];
		if (listeners.fn) {
			if (listeners.fn === fn && (!once || listeners.once) && (!context || listeners.context === context)) clearEvent(this, evt);
		} else {
			for (var i = 0, events = [], length = listeners.length; i < length; i++) if (listeners[i].fn !== fn || once && !listeners[i].once || context && listeners[i].context !== context) events.push(listeners[i]);
			if (events.length) this._events[evt] = events.length === 1 ? events[0] : events;
			else clearEvent(this, evt);
		}
		return this;
	};
	/**
	* Remove all listeners, or those of the specified event.
	*
	* @param {(String|Symbol)} [event] The event name.
	* @returns {EventEmitter} `this`.
	* @public
	*/
	EventEmitter.prototype.removeAllListeners = function removeAllListeners(event) {
		var evt;
		if (event) {
			evt = prefix ? prefix + event : event;
			if (this._events[evt]) clearEvent(this, evt);
		} else {
			this._events = new Events();
			this._eventsCount = 0;
		}
		return this;
	};
	EventEmitter.prototype.off = EventEmitter.prototype.removeListener;
	EventEmitter.prototype.addListener = EventEmitter.prototype.on;
	EventEmitter.prefixed = prefix;
	EventEmitter.EventEmitter = EventEmitter;
	if ("undefined" !== typeof module) module.exports = EventEmitter;
})))());
var EventEmitter = class extends import_eventemitter3.default {
	shutdown() {
		this.removeAllListeners();
	}
	destroy() {
		this.removeAllListeners();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/eventemitter/StandAloneEventEmitterMethods.js
var StandAloneEventEmitterMethods_default = {
	setEventEmitter(eventEmitter, EventEmitterClass) {
		if (EventEmitterClass === void 0) EventEmitterClass = EventEmitter;
		this._privateEE = eventEmitter === true || eventEmitter === void 0;
		this._eventEmitter = this._privateEE ? new EventEmitterClass() : eventEmitter;
		return this;
	},
	destroyEventEmitter() {
		if (this._eventEmitter && this._privateEE) this._eventEmitter.shutdown();
		return this;
	},
	getEventEmitter() {
		return this._eventEmitter;
	},
	on: function() {
		if (this._eventEmitter) this._eventEmitter.on.apply(this._eventEmitter, arguments);
		return this;
	},
	once: function() {
		if (this._eventEmitter) this._eventEmitter.once.apply(this._eventEmitter, arguments);
		return this;
	},
	off: function() {
		if (this._eventEmitter) this._eventEmitter.off.apply(this._eventEmitter, arguments);
		return this;
	},
	emit: function(event) {
		if (this._eventEmitter && event) this._eventEmitter.emit.apply(this._eventEmitter, arguments);
		return this;
	},
	addListener: function() {
		if (this._eventEmitter) this._eventEmitter.addListener.apply(this._eventEmitter, arguments);
		return this;
	},
	removeListener: function() {
		if (this._eventEmitter) this._eventEmitter.removeListener.apply(this._eventEmitter, arguments);
		return this;
	},
	removeAllListeners: function() {
		if (this._eventEmitter) this._eventEmitter.removeAllListeners.apply(this._eventEmitter, arguments);
		return this;
	},
	listenerCount: function() {
		if (this._eventEmitter) return this._eventEmitter.listenerCount.apply(this._eventEmitter, arguments);
		return 0;
	},
	listeners: function() {
		if (this._eventEmitter) return this._eventEmitter.listeners.apply(this._eventEmitter, arguments);
		return [];
	},
	eventNames: function() {
		if (this._eventEmitter) return this._eventEmitter.eventNames.apply(this._eventEmitter, arguments);
		return [];
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/string/TypeConvert.js
var FLOAT = /^\s*-?(\d*\.?\d+|\d+\.?\d*)(e[-+]?\d+)?\s*$/i;
var HEX = /^0x[0-9A-F]+$/i;
var TypeConvert = function(s) {
	if (typeof s !== "string") return s;
	if (s === "") s = null;
	else if (FLOAT.test(s)) s = parseFloat(s);
	else if (HEX.test(s)) s = parseInt(s, 16);
	else switch (s) {
		case "false":
			s = false;
			break;
		case "true":
			s = true;
			break;
		case "null":
			s = null;
			break;
		case "undefined": s = void 0;
	}
	return s;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/logic/bracketparser/bracketparserbase/BracketParser.js
var BracketParser$1 = class {
	constructor(config) {
		this.setEventEmitter(GetValue$279(config, "eventEmitter", void 0));
		this.setValueConverter(GetValue$279(config, "valueConvert", true));
		this.setLoopEnable(GetValue$279(config, "loop", false));
		this.setMultipleLinesTagEnable(GetValue$279(config, "multipleLinesTag", false));
		var delimiters = GetValue$279(config, "delimiters", "<>");
		this.setDelimiters(delimiters[0], delimiters[1]);
		this.setTranslateTagNameCallback(GetValue$279(config, "translateTagNameCallback"));
		this.isRunning = false;
		this.isPaused = false;
		this.skipEventFlag = false;
		this.justCompleted = false;
		this.lastTagStart = null;
		this.lastTagEnd = null;
		this.lastContent = null;
	}
	shutdown() {
		this.destroyEventEmitter();
	}
	destroy() {
		this.shutdown();
	}
	setMultipleLinesTagEnable(enable) {
		if (enable === void 0) enable = true;
		this.multipleLinesTagEnable = enable;
		return this;
	}
	setDelimiters(delimiterLeft, delimiterRight) {
		if (delimiterRight === void 0) {
			delimiterRight = delimiterLeft[1];
			delimiterLeft = delimiterLeft[0];
		}
		this.delimiterLeft = delimiterLeft;
		this.delimiterRight = delimiterRight;
		delimiterLeft = EscapeRegex(this.delimiterLeft);
		delimiterRight = EscapeRegex(this.delimiterRight);
		var flag = this.multipleLinesTagEnable ? "gs" : "gi";
		this.reSplit = RegExp(`${delimiterLeft}(.+?)${delimiterRight}`, flag);
		return this;
	}
	setTranslateTagNameCallback(callback) {
		this.translateTagNameCallback = callback;
		return this;
	}
	setValueConverter(converter) {
		if (converter === true) converter = TypeConvert;
		else if (!converter) converter = BypassValueConverter;
		this.valueConverter = converter;
		return this;
	}
	setLoopEnable(enable) {
		if (enable === void 0) enable = true;
		this.loopEnable = enable;
		return this;
	}
	setSource(source) {
		this.source = source;
		return this;
	}
	resetIndex(index) {
		if (index === void 0) index = 0;
		this.progressIndex = index;
		this.reSplit.lastIndex = index;
		this.lastTagStart = null;
		this.lastTagEnd = null;
		this.lastContent = null;
		this.justCompleted = false;
		this.isRunning = false;
		return this;
	}
	start(source) {
		this.setSource(source).restart();
		return this;
	}
	restart() {
		this.resetIndex().next();
	}
	next() {
		if (this.isPaused) this.onResume();
		if (this.isRunning) return this;
		this.isRunning = true;
		if (this.justCompleted) {
			this.isRunning = false;
			return this;
		}
		if (this.reSplit.lastIndex === 0) this.onStart();
		var text = this.source, lastIndex = text.length;
		this.reSplit.lastIndex = this.progressIndex;
		while (true) {
			var regexResult = this.reSplit.exec(text);
			if (!regexResult) {
				if (this.progressIndex < lastIndex) {
					this.onContent(text.substring(this.progressIndex, lastIndex));
					if (this.isPaused) {
						this.progressIndex = lastIndex;
						break;
					}
				}
				this.onComplete();
				this.isRunning = false;
				return;
			}
			var matchEnd = this.reSplit.lastIndex;
			var matchStart = matchEnd - regexResult[0].length;
			if (this.progressIndex < matchStart) {
				this.onContent(text.substring(this.progressIndex, matchStart));
				if (this.isPaused) {
					this.progressIndex = matchStart;
					break;
				}
			}
			this.lastTagSource = regexResult[0];
			this.onTag(regexResult[1]);
			this.lastTagSource = void 0;
			this.progressIndex = matchEnd;
			if (this.isPaused) break;
		}
		this.isRunning = false;
		return this;
	}
	skipEvent() {
		this.skipEventFlag = true;
		return this;
	}
	pause() {
		if (!this.isPaused) this.onPause();
		return this;
	}
	pauseUntilEvent(eventEmitter, eventName) {
		if (this.isPaused) return this;
		this.pause();
		eventEmitter.once(eventName, function() {
			this.next();
		}, this);
		return this;
	}
	onContent(content) {
		this.skipEventFlag = false;
		this.emit("content", content);
		this.lastContent = content;
	}
	onTag(tagContent) {}
	onStart() {
		this.isRunning = true;
		this.emit("start", this);
	}
	onComplete() {
		this.isRunning = false;
		this.justCompleted = true;
		this.emit("complete", this);
		if (this.loopEnable) this.resetIndex();
	}
	onPause() {
		this.isPaused = true;
		this.emit("pause", this);
	}
	onResume() {
		this.isPaused = false;
		this.emit("resume", this);
	}
};
var BypassValueConverter = function(s) {
	return s;
};
Object.assign(BracketParser$1.prototype, StandAloneEventEmitterMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/string/StringToValues.js
var StringToValues = function(text, valueConverter, delimiter) {
	if (text == null) return [];
	if (valueConverter === void 0) valueConverter = TypeConvert;
	if (delimiter === void 0) delimiter = ",";
	var values = text.split(delimiter);
	for (var i = 0, cnt = values.length; i < cnt; i++) values[i] = valueConverter(values[i]);
	return values;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/logic/bracketparser/bracketparser/BracketParser.js
var BracketParser = class extends BracketParser$1 {
	constructor(config) {
		if (config === void 0) config = {};
		if (!config.hasOwnProperty("multipleLinesTag")) config.multipleLinesTag = false;
		super(config);
		this.setTagExpression(GetValue$279(config, "regex.tag", void 0));
		this.setValueExpression(GetValue$279(config, "regex.value", void 0));
		var delimiters = GetValue$279(config, "delimiters", "<>");
		this.setDelimiters(delimiters[0], delimiters[1]);
	}
	setTagExpression(express) {
		if (!express) express = DefaultTokenExpression;
		this.tagExpression = express;
		return this;
	}
	setValueExpression(express) {
		if (!express) express = DefaultTokenExpression;
		this.valueExpression = express;
		return this;
	}
	setDelimiters(delimiterLeft, delimiterRight) {
		super.setDelimiters(delimiterLeft, delimiterRight);
		var tag = `(${this.tagExpression})(=(${this.valueExpression}))?`;
		this.reTag = RegExp(tag, "i");
		if (this.tagExpression !== DefaultTokenExpression || this.valueExpression !== DefaultTokenExpression) {
			var startTagExpression = `${this.tagExpression}(=${this.valueExpression})?`;
			var endTagExpression = `/${this.tagExpression}`;
			delimiterLeft = EscapeRegex(this.delimiterLeft);
			delimiterRight = EscapeRegex(this.delimiterRight);
			var flag = this.multipleLinesTagEnable ? "gs" : "gi";
			this.reSplit = RegExp(`${delimiterLeft}((${startTagExpression})|(${endTagExpression}))${delimiterRight}`, flag);
		}
		return this;
	}
	onTag(tagContent) {
		var regexResult = tagContent.match(this.reTag);
		var tagName = regexResult[1];
		var isEndTag = tagName.charAt(0) === "/";
		if (isEndTag) tagName = tagName.substring(1, tagName.length);
		if (this.translateTagNameCallback) tagName = this.translateTagNameCallback(tagName);
		this.skipEventFlag = false;
		if (!isEndTag) {
			var values = StringToValues(regexResult[3], this.valueConverter);
			this.emit(`+${tagName}`, ...values);
			if (!this.skipEventFlag) this.emit("+", tagName, ...values);
			this.lastTagStart = tagName;
		} else {
			this.emit(`-${tagName}`);
			if (!this.skipEventFlag) this.emit("-", tagName);
			this.lastTagEnd = tagName;
		}
	}
};
var DefaultTokenExpression = `[^=]+`;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/bracketparser.js
var bracketparser_default = BracketParser;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/textstyle/OnParseColorTag.js
var OnParseColorTag = function(textPlayer, parser, config) {
	var tagName = "color";
	var defaultColor;
	parser.on("start", function() {
		defaultColor = textPlayer.textStyle.color;
	}).on(`+${tagName}`, function(color) {
		textPlayer.textStyle.setColor(color);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		textPlayer.textStyle.setColor(defaultColor);
		parser.skipEvent();
	}).on("complete", function() {
		textPlayer.textStyle.setColor(defaultColor);
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/textstyle/OnParseStrokeColorTag.js
var OnParseStrokeColorTag = function(textPlayer, parser, config) {
	var tagName = "stroke";
	var defaultStroke;
	parser.on("start", function() {
		defaultStroke = textPlayer.textStyle.stroke;
		textPlayer.textStyle.setStrokeStyle(null);
	}).on(`+${tagName}`, function(color) {
		if (color === void 0) color = defaultStroke;
		textPlayer.textStyle.setStrokeStyle(color);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		textPlayer.textStyle.setStrokeStyle(null);
		parser.skipEvent();
	}).on("complete", function() {
		textPlayer.textStyle.setStrokeStyle(defaultStroke);
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/textstyle/OnParseBoldTag.js
var OnParseBoldTag = function(textPlayer, parser, config) {
	var tagName = "b";
	parser.on("start", function() {
		textPlayer.textStyle.setBold(false);
	}).on(`+${tagName}`, function() {
		textPlayer.textStyle.setBold(true);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		textPlayer.textStyle.setBold(false);
		parser.skipEvent();
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/textstyle/OnParseItalicTag.js
var OnParseItalicTag = function(textPlayer, parser, config) {
	var tagName = "i";
	parser.on("start", function() {
		textPlayer.textStyle.setItalic(false);
	}).on(`+${tagName}`, function() {
		textPlayer.textStyle.setItalic(true);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		textPlayer.textStyle.setItalic(false);
		parser.skipEvent();
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/textstyle/OnParseFontSizeTag.js
var OnParseFontSizeTag = function(textPlayer, parser, config) {
	var tagName = "size";
	var defaultFontSize;
	parser.on("start", function() {
		defaultFontSize = textPlayer.textStyle.fontSize;
	}).on(`+${tagName}`, function(fontSize) {
		textPlayer.textStyle.setFontSize(fontSize);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		textPlayer.textStyle.setFontSize(defaultFontSize);
		parser.skipEvent();
	}).on("complete", function() {
		textPlayer.textStyle.setFontSize(defaultFontSize);
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/textstyle/OnParseOffsetYTag.js
var OnParseOffsetYTag = function(textPlayer, parser, config) {
	var tagName = "y";
	var defaultOffsetY;
	parser.on("start", function() {
		defaultOffsetY = textPlayer.textStyle.offsetY;
		textPlayer.textStyle.setOffsetY(0);
	}).on(`+${tagName}`, function(y) {
		if (y === void 0) y = defaultOffsetY;
		textPlayer.textStyle.setOffsetY(y);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		textPlayer.textStyle.setOffsetY(0);
		parser.skipEvent();
	}).on("complete", function() {
		textPlayer.textStyle.setOffsetY(0);
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/textstyle/OnParseOffsetXTag.js
var OnParseOffsetXTag = function(textPlayer, parser, config) {
	var tagName = "x";
	var defaultOffsetX;
	parser.on("start", function() {
		defaultOffsetX = textPlayer.textStyle.offsetY;
		textPlayer.textStyle.setOffsetX(0);
	}).on(`+${tagName}`, function(y) {
		if (y === void 0) y = defaultOffsetX;
		textPlayer.textStyle.setOffsetX(y);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		textPlayer.textStyle.setOffsetX(0);
		parser.skipEvent();
	}).on("complete", function() {
		textPlayer.textStyle.setOffsetX(0);
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/textstyle/OnParseLeftSpaceTag.js
var OnParseLeftSpaceTag = function(textPlayer, parser, config) {
	var tagName = "left";
	var defaultLeftSpace;
	parser.on("start", function() {
		defaultLeftSpace = textPlayer.textStyle.leftSpace;
		textPlayer.textStyle.setLeftSpace(0);
	}).on(`+${tagName}`, function(space) {
		if (space === void 0) space = defaultLeftSpace;
		textPlayer.textStyle.setLeftSpace(space);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		textPlayer.textStyle.setLeftSpace(0);
		parser.skipEvent();
	}).on("complete", function() {
		textPlayer.textStyle.setLeftSpace(0);
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/textstyle/OnParseRightSpaceTag.js
var OnParseRightSpaceTag = function(textPlayer, parser, config) {
	var tagName = "right";
	var defaultRightSpace;
	parser.on("start", function() {
		defaultRightSpace = textPlayer.textStyle.rightSpace;
		textPlayer.textStyle.setRightSpace(0);
	}).on(`+${tagName}`, function(space) {
		if (space === void 0) space = defaultRightSpace;
		textPlayer.textStyle.setRightSpace(space);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		textPlayer.textStyle.setRightSpace(0);
		parser.skipEvent();
	}).on("complete", function() {
		textPlayer.textStyle.setRightSpace(0);
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/textstyle/OnParseShadowColorTag.js
var OnParseShadowColorTag = function(textPlayer, parser, config) {
	var tagName = "shadow";
	var defaultShadowColor;
	parser.on("start", function() {
		defaultShadowColor = textPlayer.textStyle.shadowColor;
		textPlayer.textStyle.setShadowColor(null);
	}).on(`+${tagName}`, function(color) {
		if (color === void 0) color = defaultShadowColor;
		textPlayer.textStyle.setShadowColor(color);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		textPlayer.textStyle.setShadowColor(null);
		parser.skipEvent();
	}).on("complete", function() {
		textPlayer.textStyle.setShadowColor(defaultShadowColor);
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/textstyle/OnParseAlignTag.js
var OnParseAlignTag = function(textPlayer, parser, config) {
	var tagName = "align";
	parser.on(`+${tagName}`, function(align) {
		textPlayer.textStyle.setAlign(align);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		textPlayer.textStyle.setAlign();
		parser.skipEvent();
	}).on("complete", function() {
		textPlayer.textStyle.setAlign();
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/image/OnParseImageTag.js
var OnParseImageTag$1 = function(textPlayer, parser, config) {
	var tagName = "img";
	parser.on(`+${tagName}`, function(name) {
		var imgData = textPlayer.imageManager.get(name);
		if (imgData) {
			var tintMode = imgData.tintMode;
			AppendImage.call(textPlayer, imgData.key, imgData.frame, {
				width: imgData.width,
				hieght: imgData.height,
				leftSpace: imgData.left,
				rightSpace: imgData.right,
				color: tintMode !== void 0 ? textPlayer.textStyle.color : void 0,
				tintMode
			});
		}
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/space/OnParseSpaceTag.js
var OnParseImageTag = function(textPlayer, parser, config) {
	var tagName = "space";
	parser.on(`+${tagName}`, function(width) {
		AppendSpace.call(textPlayer, width);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/typing/OnParseTypingSpeedTag.js
var OnParseTypingSpeedTag = function(textPlayer, parser, config) {
	var tagName = "speed";
	parser.on(`+${tagName}`, function(speed) {
		AppendCommand$2(textPlayer, speed);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		AppendCommand$2(textPlayer, void 0);
		parser.skipEvent();
	});
};
var SetTypingSpeed = function(speed) {
	this.typeWriter.setTypingSpeed(speed);
};
var AppendCommand$2 = function(textPlayer, speed) {
	AppendCommand$3.call(textPlayer, "speed", SetTypingSpeed, speed, textPlayer);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/soundeffect/OnParsePlaySoundEffectTag.js
var OnParsePlaySoundEffectTag = function(textPlayer, parser, config) {
	var tagName = "se";
	parser.on(`+${tagName}`, function(name, fadeInTime) {
		AppendCommand$3.call(textPlayer, tagName, PlaySoundEffect, [name, fadeInTime], textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
	var tagName = "se2";
	parser.on(`+${tagName}`, function(name, fadeInTime) {
		AppendCommand$3.call(textPlayer, tagName, PlaySoundEffect2, [name, fadeInTime], textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
};
var PlaySoundEffect = function(params) {
	if (this.skipSoundEffect) return;
	var name = params[0];
	var fadeInTime = params[1];
	this.soundManager.playSoundEffect(name);
	if (fadeInTime) this.soundManager.fadeInSoundEffect(fadeInTime);
};
var PlaySoundEffect2 = function(params) {
	if (this.skipSoundEffect) return;
	var name = params[0];
	var fadeInTime = params[1];
	this.soundManager.playSoundEffect2(name);
	if (fadeInTime) this.soundManager.fadeInSoundEffect2(fadeInTime);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/soundeffect/OnParseFadeInSoundEffectTag.js
var OnParseFadeInSoundEffectTag = function(textPlayer, parser, config) {
	var tagName = "se.fadein";
	parser.on(`+${tagName}`, function(time) {
		AppendCommand$3.call(textPlayer, tagName, FadeInSoundEffect, time, textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
	var tagName = "se2.fadein";
	parser.on(`+${tagName}`, function(time) {
		AppendCommand$3.call(textPlayer, tagName, FadeInSoundEffect2, time, textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
};
var FadeInSoundEffect = function(time) {
	this.soundManager.fadeInSoundEffect(time);
};
var FadeInSoundEffect2 = function(time) {
	this.soundManager.fadeInSoundEffect2(time);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/soundeffect/OnParseFadeOutSoundEffectTag.js
var OnParseFadeOutSoundEffectTag = function(textPlayer, parser, config) {
	var tagName = "se.fadeout";
	parser.on(`+${tagName}`, function(time, isStopped) {
		isStopped = isStopped === "stop";
		AppendCommand$3.call(textPlayer, tagName, FadeOutSoundEffect, [time, isStopped], textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
	var tagName = "se2.fadeout";
	parser.on(`+${tagName}`, function(time, isStopped) {
		isStopped = isStopped === "stop";
		AppendCommand$3.call(textPlayer, tagName, FadeOutSoundEffect2, [time, isStopped], textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
};
var FadeOutSoundEffect = function(params) {
	this.soundManager.fadeOutSoundEffect(...params);
};
var FadeOutSoundEffect2 = function(params) {
	this.soundManager.fadeOutSoundEffect2(...params);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/soundeffect/OnParseSetSoundEffectVolumeTag.js
var OnParseSetSoundEffectVolumeTag = function(textPlayer, parser, config) {
	var tagName = "se.volume";
	parser.on(`+${tagName}`, function(volume) {
		AppendCommand$3.call(textPlayer, tagName, SetSoundEffectVolume, volume, textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
	var tagName = "se2.volume";
	parser.on(`+${tagName}`, function(volume) {
		AppendCommand$3.call(textPlayer, tagName, SetSoundEffectVolume2, volume, textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
};
var SetSoundEffectVolume = function(volume) {
	this.soundManager.setSoundEffectVolume(volume, true);
};
var SetSoundEffectVolume2 = function(volume) {
	this.soundManager.setSoundEffectVolume2(volume, true);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/soundeffect/OnParseSetSoundEffectMuteTag.js
var OnParseSetSoundEffectMuteTag = function(textPlayer, parser, config) {
	var tagName = "se.mute";
	parser.on(`+${tagName}`, function() {
		AppendCommand$3.call(textPlayer, tagName, SetSoundEffectMute, void 0, textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
	var tagName = "se2.mute";
	parser.on(`+${tagName}`, function() {
		AppendCommand$3.call(textPlayer, tagName, SetSoundEffect2Mute, void 0, textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
	var tagName = "se.unmute";
	parser.on(`+${tagName}`, function() {
		AppendCommand$3.call(textPlayer, tagName, SetSoundEffectUnMute, void 0, textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
	var tagName = "se2.unmute";
	parser.on(`+${tagName}`, function() {
		AppendCommand$3.call(textPlayer, tagName, SetSoundEffect2UnMute, void 0, textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
};
var SetSoundEffectMute = function() {
	this.soundManager.setSoundEffectMute(true);
};
var SetSoundEffect2Mute = function() {
	this.soundManager.setSoundEffect2Mute(true);
};
var SetSoundEffectUnMute = function() {
	this.soundManager.setSoundEffectMute(false);
};
var SetSoundEffect2UnMute = function() {
	this.soundManager.setSoundEffect2Mute(false);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/backgroundmusic/OnParsePlayBackgroundMusicTag.js
var OnParsePlayBackgroundMusicTag = function(textPlayer, parser, config) {
	var tagName = "bgm";
	parser.on(`+${tagName}`, function(name, fadeInTime) {
		AppendCommand$3.call(textPlayer, tagName, PlayBackgroundMusic, [name, fadeInTime], textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		AppendCommand$3.call(textPlayer, "bgm.stop", StopBackgroundMusic, void 0, textPlayer);
		parser.skipEvent();
	});
	var tagName = "bgm2";
	parser.on(`+${tagName}`, function(name, fadeInTime) {
		AppendCommand$3.call(textPlayer, tagName, PlayBackgroundMusic2, [name, fadeInTime], textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		AppendCommand$3.call(textPlayer, "bgm2.stop", StopBackgroundMusic2, void 0, textPlayer);
		parser.skipEvent();
	});
};
var PlayBackgroundMusic = function(params) {
	var name = params[0];
	var fadeInTime = params[1];
	this.soundManager.playBackgroundMusic(name);
	if (fadeInTime) this.soundManager.fadeInBackgroundMusic(fadeInTime);
};
var StopBackgroundMusic = function() {
	this.soundManager.stopBackgroundMusic();
};
var PlayBackgroundMusic2 = function(params) {
	var name = params[0];
	var fadeInTime = params[1];
	this.soundManager.playBackgroundMusic2(name);
	if (fadeInTime) this.soundManager.fadeInBackgroundMusic2(fadeInTime);
};
var StopBackgroundMusic2 = function() {
	this.soundManager.stopBackgroundMusic2();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/backgroundmusic/OnParseFadeInBackgroundMusicTag.js
var OnParseFadeInBackgroundMusicTag = function(textPlayer, parser, config) {
	var tagName = "bgm.fadein";
	parser.on(`+${tagName}`, function(time) {
		AppendCommand$3.call(textPlayer, tagName, FadeInBackgroundMusic, time, textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
	var tagName = "bgm2.fadein";
	parser.on(`+${tagName}`, function(time) {
		AppendCommand$3.call(textPlayer, tagName, FadeInBackgroundMusic2, time, textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
};
var FadeInBackgroundMusic = function(time) {
	this.soundManager.fadeInBackgroundMusic(time);
};
var FadeInBackgroundMusic2 = function(time) {
	this.soundManager.fadeInBackgroundMusic2(time);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/backgroundmusic/OnParseFadeOutBackgroundMusicTag.js
var OnParseFadeOutBackgroundMusicTag = function(textPlayer, parser, config) {
	var tagName = "bgm.fadeout";
	parser.on(`+${tagName}`, function(time, isStopped) {
		isStopped = isStopped === "stop";
		AppendCommand$3.call(textPlayer, tagName, FadeOutBackgroundMusic, [time, isStopped], textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
	var tagName = "bgm2.fadeout";
	parser.on(`+${tagName}`, function(time, isStopped) {
		isStopped = isStopped === "stop";
		AppendCommand$3.call(textPlayer, tagName, FadeOutBackgroundMusic2, [time, isStopped], textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
};
var FadeOutBackgroundMusic = function(params) {
	this.soundManager.fadeOutBackgroundMusic(...params);
};
var FadeOutBackgroundMusic2 = function(params) {
	this.soundManager.fadeOutBackgroundMusic2(...params);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/backgroundmusic/OnParseCrossFadeBackgroundMusicTag.js
var OnParseCrossFadeBackgroundMusicTag = function(textPlayer, parser, config) {
	var tagName = "bgm.cross";
	parser.on(`+${tagName}`, function(name, fadeTime) {
		AppendCommand$3.call(textPlayer, tagName, CrossFadeBackgroundMusic, [name, fadeTime], textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
	var tagName = "bgm2.cross";
	parser.on(`+${tagName}`, function(name, fadeTime) {
		AppendCommand$3.call(textPlayer, tagName, CrossFadeBackgroundMusic2, [name, fadeTime], textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
};
var CrossFadeBackgroundMusic = function(params) {
	this.soundManager.crossFadeBackgroundMusic(...params);
};
var CrossFadeBackgroundMusic2 = function(params) {
	this.soundManager.crossFadeBackgroundMusic2(...params);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/backgroundmusic/OnParsePauseBackgroundMusicTag.js
var OnParsePauseBackgroundMusicTag = function(textPlayer, parser, config) {
	var tagName = "bgm.pause";
	parser.on(`+${tagName}`, function() {
		AppendCommand$3.call(textPlayer, tagName, PauseBackgroundMusic, void 0, textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		AppendCommand$3.call(textPlayer, "bgm.resume", ResumeBackgroundMusic, void 0, textPlayer);
		parser.skipEvent();
	});
	var tagName = "bgm2.pause";
	parser.on(`+${tagName}`, function() {
		AppendCommand$3.call(textPlayer, tagName, PauseBackgroundMusic2, void 0, textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		AppendCommand$3.call(textPlayer, "bgm2.resume", ResumeBackgroundMusic2, void 0, textPlayer);
		parser.skipEvent();
	});
};
var PauseBackgroundMusic = function() {
	this.soundManager.pauseBackgroundMusic();
};
var ResumeBackgroundMusic = function() {
	this.soundManager.resumeBackgroundMusic();
};
var PauseBackgroundMusic2 = function() {
	this.soundManager.pauseBackgroundMusic2();
};
var ResumeBackgroundMusic2 = function() {
	this.soundManager.resumeBackgroundMusic2();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/backgroundmusic/OnParseSetBackgroundMusicVolumeTag.js
var OnParseSetBackgroundMusicVolumeTag = function(textPlayer, parser, config) {
	var tagName = "bgm.volume";
	parser.on(`+${tagName}`, function(volume) {
		AppendCommand$3.call(textPlayer, tagName, SetBackgroundMusicVolume, volume, textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
	var tagName = "bgm2.volume";
	parser.on(`+${tagName}`, function(volume) {
		AppendCommand$3.call(textPlayer, tagName, SetBackgroundMusicVolume2, volume, textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
};
var SetBackgroundMusicVolume = function(volume) {
	this.soundManager.setBackgroundMusicVolume(volume);
};
var SetBackgroundMusicVolume2 = function(volume) {
	this.soundManager.setBackgroundMusicVolume2(volume);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/backgroundmusic/OnParseSetBackgroundMusicMuteTag.js
var OnParseSetBackgroundMusicMuteTag = function(textPlayer, parser, config) {
	var tagName = "bgm.mute";
	parser.on(`+${tagName}`, function() {
		AppendCommand$3.call(textPlayer, tagName, SetBackgroundMusicMute, void 0, textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
	var tagName = "bgm2.mute";
	parser.on(`+${tagName}`, function() {
		AppendCommand$3.call(textPlayer, tagName, SetBackgroundMusic2Mute, void 0, textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
	var tagName = "bgm.unmute";
	parser.on(`+${tagName}`, function() {
		AppendCommand$3.call(textPlayer, tagName, SetBackgroundMusicUnMute, void 0, textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
	var tagName = "bgm2.unmute";
	parser.on(`+${tagName}`, function() {
		AppendCommand$3.call(textPlayer, tagName, SetBackgroundMusic2UnMute, void 0, textPlayer);
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
};
var SetBackgroundMusicMute = function() {
	this.soundManager.setBackgroundMusicMute(true);
};
var SetBackgroundMusic2Mute = function() {
	this.soundManager.setBackgroundMusic2Mute(true);
};
var SetBackgroundMusicUnMute = function() {
	this.soundManager.setBackgroundMusicMute(false);
};
var SetBackgroundMusic2UnMute = function() {
	this.soundManager.setBackgroundMusic2Mute(false);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/camera/OnParseFadeInCameraTag.js
var OnParseFadeInCameraTag = function(textPlayer, parser, config) {
	var tagName = "camera.fadein";
	parser.on(`+${tagName}`, function(duration, red, green, blue) {
		AppendCommand$3.call(textPlayer, tagName, PlayFadeInEffect, [
			duration,
			red,
			green,
			blue
		], textPlayer);
		parser.skipEvent();
	});
};
var PlayFadeInEffect = function(params) {
	this.cameraTarget.fadeIn(...params);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/camera/OnParseFadeOutCameraTag.js
var OnParseFadeOutCameraTag = function(textPlayer, parser, config) {
	var tagName = "camera.fadeout";
	parser.on(`+${tagName}`, function(duration, red, green, blue) {
		AppendCommand$3.call(textPlayer, tagName, PlayFadeOutEffect, [
			duration,
			red,
			green,
			blue
		], textPlayer);
		parser.skipEvent();
	});
};
var PlayFadeOutEffect = function(params) {
	this.cameraTarget.fadeOut(...params);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/camera/OnParseShakeCameraTag.js
var OnParseShakeCameraTag = function(textPlayer, parser, config) {
	var tagName = "camera.shake";
	parser.on(`+${tagName}`, function(duration, intensity) {
		AppendCommand$3.call(textPlayer, tagName, PlayShakeEffect, [duration, intensity], textPlayer);
		parser.skipEvent();
	});
};
var PlayShakeEffect = function(params) {
	this.cameraTarget.shake(...params);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/camera/OnParseFlashCameraTag.js
var OnParseFlashCameraTag = function(textPlayer, parser, config) {
	var tagName = "camera.flash";
	parser.on(`+${tagName}`, function(duration, red, green, blue) {
		AppendCommand$3.call(textPlayer, tagName, PlayFlashEffect, [
			duration,
			red,
			green,
			blue
		], textPlayer);
		parser.skipEvent();
	});
};
var PlayFlashEffect = function(params) {
	this.cameraTarget.flash(...params);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/camera/OnParseZoomCameraTag.js
var OnParseZoomCameraTag = function(textPlayer, parser, config) {
	var tagName = "camera.zoom";
	parser.on(`+${tagName}`, function(value) {
		AppendCommand$3.call(textPlayer, tagName, Zoom, value, textPlayer);
		parser.skipEvent();
	}).on(`+${tagName}.to`, function(value, duration, ease) {
		AppendCommand$3.call(textPlayer, "camera.zoom.to", ZoomTo, [
			value,
			duration,
			ease
		], textPlayer);
		parser.skipEvent();
	});
};
var Zoom = function(value) {
	this.cameraTarget.setZoom(value);
};
var ZoomTo = function(params) {
	this.cameraTarget.zoomTo(...params);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/camera/OnParseRotateCameraTag.js
var DegToRad$13 = __webpack_exports__Math.DegToRad;
var OnParseRotateCameraTag = function(textPlayer, parser, config) {
	var tagName = "camera.rotate";
	parser.on(`+${tagName}`, function(value) {
		value = DegToRad$13(value);
		AppendCommand$3.call(textPlayer, tagName, Rotate$2, value, textPlayer);
		parser.skipEvent();
	}).on(`+${tagName}.to`, function(value, duration, ease) {
		value = DegToRad$13(value);
		AppendCommand$3.call(textPlayer, "camera.rotate.to", RotateTo, [
			value,
			duration,
			ease
		], textPlayer);
		parser.skipEvent();
	});
};
var Rotate$2 = function(value) {
	this.cameraTarget.setRotation(value);
};
var RotateTo = function(params) {
	var value = params[0];
	var duration = params[1];
	var ease = params[2];
	this.cameraTarget.rotateTo(value, false, duration, ease);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/camera/OnParseScrollCameraTag.js
var OnParseScrollCameraTag = function(textPlayer, parser, config) {
	var tagName = "camera.scroll";
	parser.on(`+${tagName}`, function(x, y) {
		AppendCommand$3.call(textPlayer, tagName, Scroll, [x, y], textPlayer);
		parser.skipEvent();
	}).on(`+${tagName}.to`, function(x, y, duration, ease) {
		AppendCommand$3.call(textPlayer, "camera.scroll.to", ScrollTo, [
			x,
			y,
			duration,
			ease
		], textPlayer);
		parser.skipEvent();
	});
};
var Scroll = function(params) {
	this.cameraTarget.setScroll(...params);
};
var ScrollTo = function(params) {
	var x = params[0];
	var y = params[1];
	var duration = params[2];
	var ease = params[3];
	var camera = this.cameraTarget;
	var xSave = camera.scrollX;
	var ySave = camera.scrollY;
	camera.setScroll(x, y);
	x += camera.centerX;
	y += camera.centerY;
	camera.setScroll(xSave, ySave);
	camera.pan(x, y, duration, ease);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/wait/OnParseWaitTag.js
var OnParseWaitTag = function(textPlayer, parser, config) {
	var tagWait = "wait";
	var tagClick = "click";
	parser.on(`+${tagWait}`, function(name) {
		AppendCommand$1(textPlayer, name);
		parser.skipEvent();
	}).on(`-${tagWait}`, function() {
		parser.skipEvent();
	}).on(`+${tagClick}`, function() {
		AppendCommand$1(textPlayer, "click");
		parser.skipEvent();
	}).on(`-${tagClick}`, function() {
		parser.skipEvent();
	});
};
var Wait$2 = function(name) {
	this.typeWriter.wait(name);
};
var AppendCommand$1 = function(textPlayer, name) {
	AppendCommand$3.call(textPlayer, "wait", Wait$2, name, textPlayer);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/content/OnParseNewLineTag.js
var OnParseNewLineTag = function(textPlayer, parser, config) {
	var tagName = "r";
	parser.on(`+${tagName}`, function() {
		AppendText.call(textPlayer, "\n");
		parser.skipEvent();
	}).on(`-${tagName}`, function() {
		parser.skipEvent();
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/content/OnParsePageBreakTag.js
var OnParsePageBreakTag = function(textPlayer, parser, config) {
	var tagNames = ["pagebreak", "pb"];
	for (var i = 0, cnt = tagNames.length; i < cnt; i++) {
		var tagName = tagNames[i];
		parser.on(`+${tagName}`, function() {
			AppendText.call(textPlayer, "\f");
			parser.skipEvent();
		}).on(`-${tagName}`, function() {
			parser.skipEvent();
		});
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/content/OnParseContentOff.js
var OnParseContentOff = function(textPlayer, parser, config) {
	parser.on(`+content.off`, function() {
		parser.setContentOutputEnable(false);
		parser.skipEvent();
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/content/OnParseContentOn.js
var OnParseContentOn = function(textPlayer, parser, config) {
	parser.on(`+content.on`, function() {
		parser.setContentOutputEnable();
		parser.skipEvent();
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/content/OnParseContent.js
var OnParseContent = function(textPlayer, parser, config) {
	parser.on("content", function(content) {
		if (parser.contentOutputEnable) AppendText.call(textPlayer, content);
		else {
			var startTag = `+${parser.lastTagStart}`;
			textPlayer.emit(`parser.${startTag}#content`, parser, content);
		}
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/custom/OnParseCustomTag.js
var OnParseCustomTag = function(textPlayer, parser, config) {
	parser.on("start", function() {
		textPlayer.emit("parser.start", parser);
	}).on("+", function(tagName, ...value) {
		if (parser.skipEventFlag) return;
		var startTag = `+${tagName}`;
		var param = value;
		textPlayer.emit(`parser.${startTag}`, parser, ...value, param);
		AppendCommand(textPlayer, startTag, param);
	}).on("-", function(tagName) {
		if (parser.skipEventFlag) return;
		var endTag = `-${tagName}`;
		var param = [];
		textPlayer.emit(`parser.${endTag}`, parser, param);
		AppendCommand(textPlayer, endTag, param);
	}).on("complete", function() {
		textPlayer.emit("parser.complete", parser);
	});
};
var FireEvent$1 = function(param, tagName) {
	var eventName = `tag.${tagName}`;
	if (param == null) this.emit(eventName);
	else this.emit(eventName, ...param);
};
var AppendCommand = function(textPlayer, name, param) {
	AppendCommand$3.call(textPlayer, name, FireEvent$1, param, textPlayer);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/AddParseCallbacks.js
var ParseCallbacks$2 = [
	OnParseColorTag,
	OnParseStrokeColorTag,
	OnParseBoldTag,
	OnParseItalicTag,
	OnParseFontSizeTag,
	OnParseShadowColorTag,
	OnParseAlignTag,
	OnParseOffsetYTag,
	OnParseOffsetXTag,
	OnParseLeftSpaceTag,
	OnParseRightSpaceTag,
	OnParseImageTag$1,
	OnParseImageTag,
	OnParseTypingSpeedTag,
	OnParsePlaySoundEffectTag,
	OnParseFadeInSoundEffectTag,
	OnParseFadeOutSoundEffectTag,
	OnParseSetSoundEffectVolumeTag,
	OnParseSetSoundEffectMuteTag,
	OnParsePlayBackgroundMusicTag,
	OnParseFadeInBackgroundMusicTag,
	OnParseFadeOutBackgroundMusicTag,
	OnParseCrossFadeBackgroundMusicTag,
	OnParsePauseBackgroundMusicTag,
	OnParseSetBackgroundMusicVolumeTag,
	OnParseSetBackgroundMusicMuteTag,
	OnParseFadeInCameraTag,
	OnParseFadeOutCameraTag,
	OnParseShakeCameraTag,
	OnParseFlashCameraTag,
	OnParseZoomCameraTag,
	OnParseRotateCameraTag,
	OnParseScrollCameraTag,
	OnParseWaitTag,
	OnParseNewLineTag,
	OnParsePageBreakTag,
	OnParseContentOff,
	OnParseContentOn,
	OnParseContent,
	OnParseCustomTag
];
var AddParseCallbacks = function(textPlayer, parser, config) {
	for (var i = 0, cnt = ParseCallbacks$2.length; i < cnt; i++) ParseCallbacks$2[i](textPlayer, parser, config);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/PreProcessSource.js
var PreProcess = function(parser, source) {
	var comentLineStart = parser.commentLineStart;
	var lines = source.split("\n");
	for (var i = 0, cnt = lines.length; i < cnt; i++) {
		var line = lines[i];
		if (line === "") {} else if (line.trim().length === 0) lines[i] = "";
		else if (comentLineStart && line.startsWith(comentLineStart)) lines[i] = "";
	}
	return lines.join("");
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/parser/Parser.js
var GetValue$237 = __webpack_exports__Utils.Objects.GetValue;
var Parser = class extends bracketparser_default {
	constructor(textPlayer, config) {
		if (config === void 0) config = {};
		if (!config.hasOwnProperty("delimiters")) config.delimiters = "[]";
		super(config);
		AddParseCallbacks(textPlayer, this, config);
		this.setCommentLineStartSymbol(GetValue$237(config, "comment", "//"));
		this.setContentOutputEnable();
	}
	setCommentLineStartSymbol(symbol) {
		this.commentLineStart = symbol;
		return this;
	}
	setContentOutputEnable(enable) {
		if (enable === void 0) enable = true;
		this.contentOutputEnable = enable;
		return this;
	}
	start(source) {
		super.start(PreProcess(this, source));
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/typewriter/TypingSpeedMethods.js
var TypingSpeedMethods_default$1 = {
	setDefaultTypingSpeed(speed) {
		this.defaultSpeed = speed;
		return this;
	},
	setTypingSpeed(speed) {
		if (speed === void 0) speed = this.defaultSpeed;
		this.speed = speed;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/utils/Events.js
var PageFadeOutCompleteEvent = "page.fadeout";
var StopPlayEvent = "_remove.play";
var ClearEvents$1 = [PageFadeOutCompleteEvent, StopPlayEvent];
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/typewriter/FadeOutPage.js
var FadeOutPage = function() {
	if (!this.fadeOutPageCallback || !this.children) {
		this.emit(PageFadeOutCompleteEvent);
		return this;
	}
	var renderableChildren = this.children.filter(function(child) {
		return child.renderable;
	});
	var waitObject = this.fadeOutPageCallback(renderableChildren, this.fadeOutPageDuration);
	if (!waitObject) this.emit(PageFadeOutCompleteEvent);
	else if (waitObject.once) waitObject.once("complete", function() {
		this.emit(PageFadeOutCompleteEvent);
	}, this);
	else if (waitObject.then) {
		var self = this;
		waitObject.then(function() {
			self.emit(PageFadeOutCompleteEvent);
		});
	} else this.emit(PageFadeOutCompleteEvent);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/promise/WaitEvent.js
var WaitEvent = function(eventEmitter, eventName) {
	return new Promise(function(resolve, reject) {
		eventEmitter.once(eventName, function() {
			resolve();
		});
	});
};
var WaitComplete = function(eventEmitter) {
	return WaitEvent(eventEmitter, "complete");
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/typewriter/Start.js
var Start$1 = function(children) {
	this.children = children;
	this.index = 0;
	this.isPageTyping = true;
	if (this.onTypeStart) this.onTypeStart(children);
	this.typing();
	return WaitComplete(this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/typewriter/TimerTypes.js
var TypingDelayTimerType = "delay";
var TypingAnimationTimerType = "anim";
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/typewriter/Typing.js
var Typing = function(offsetTime) {
	if (offsetTime === void 0) offsetTime = 0;
	var delay = 0;
	this.inTypingProcessLoop = true;
	while (this.inTypingProcessLoop) {
		var child = this.getNextChild();
		if (!child) {
			if (this.timeline.isRunning) this.timeline.once("complete", function() {
				this.isPageTyping = false;
				this.emit("complete");
			}, this);
			else {
				this.isPageTyping = false;
				this.emit("complete");
			}
			break;
		}
		if (child.renderable) {
			var animationConfig = this.animationConfig;
			if (animationConfig.duration > 0) {
				var animationTimer = this.timeline.addTimer({
					name: TypingAnimationTimerType,
					target: child,
					duration: animationConfig.duration,
					yoyo: animationConfig.yoyo,
					onStart: animationConfig.onStart,
					onProgress: animationConfig.onProgress,
					onComplete: animationConfig.onComplete
				});
				if (this.skipTypingAnimation) animationTimer.seek(1);
			} else if (animationConfig.onStart) animationConfig.onStart(child, 0);
			if (this.minSizeEnable) this.textPlayer.setToMinSize();
			this.textPlayer.emit("typing", child);
			var nextChild = this.nextChild;
			if (nextChild) {
				if (this.skipSpaceEnable && IsSpaceChar(nextChild)) {} else {
					delay += this.speed + offsetTime;
					offsetTime = 0;
					if (delay > 0) {
						this.typingTimer = this.timeline.addTimer({
							name: TypingDelayTimerType,
							target: this,
							duration: delay,
							onComplete: function(target, t, timer) {
								target.typingTimer = void 0;
								Typing.call(target, timer.remainder);
							}
						});
						break;
					}
				}
			}
		} else if (IsCommand(child)) child.exec();
	}
	if (this.minSizeEnable) this.textPlayer.setToMinSize();
	this.inTypingProcessLoop = false;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/typewriter/Pause.js
var Pause$1 = function() {
	this.timeline.pause();
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/typewriter/Resume.js
var Resume$1 = function() {
	this.timeline.resume();
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/typewriter/PauseTyping.js
var PauseTyping$1 = function() {
	if (this.isTypingPaused) return this;
	if (this.typingTimer) {
		this.typingTimer.pause();
		this.isTypingPaused = true;
	} else if (this.inTypingProcessLoop) {
		this.inTypingProcessLoop = false;
		this.isTypingPaused = true;
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/typewriter/ResumeTyping.js
var ResumeTyping$1 = function(offsetTime) {
	if (!this.isTypingPaused) return this;
	if (offsetTime === void 0) offsetTime = 0;
	if (this.typingTimer) {
		this.isTypingPaused = false;
		this.typingTimer.resume();
		this.typingTimer.remainder += offsetTime;
	} else if (this.isTypingPaused) {
		this.isTypingPaused = false;
		this.typing(offsetTime);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/utils/wait/WaitCameraEffect.js
var IsWaitCameraEffect = function(name) {
	switch (name) {
		case "camera.fadein":
		case "camera.fadeout":
		case "camera.flash":
		case "camera.shake":
		case "camera.zoom":
		case "camera.rotate":
		case "camera.scroll": return true;
		default: return false;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/utils/wait/WaitGameObject.js
var IsWaitGameObject = function(textPlayer, name) {
	var names = name.split(".");
	return textPlayer.gameObjectManagers.hasOwnProperty(names[0]);
};
var WaitGameObject = function(textPlayer, tag, callback, scope) {
	var waitEventManager = textPlayer.waitEventManager;
	var tags = tag.split(".");
	var goType = tags[0];
	var gameObjectManager = textPlayer.getGameObjectManager(goType);
	var waitEventName = `wait.${goType}`;
	switch (tags.length) {
		case 1:
			waitEventManager.waitGameObjectManagerEmpty(goType);
			textPlayer.emit(waitEventName);
			return;
		case 2:
			var name = tags[1];
			waitEventManager.waitGameObjectDestroy(goType, name);
			textPlayer.emit(waitEventName, name);
			return;
		case 3:
			var name = tags[1], prop = tags[2];
			if (typeof gameObjectManager.getProperty(name, prop) === "number") {
				waitEventManager.waitGameObjectTweenComplete(goType, name, prop);
				textPlayer.emit(waitEventName, name, prop);
				return;
			}
			var dataKey = prop;
			var matchFalseFlag = dataKey.startsWith("!");
			if (matchFalseFlag) dataKey = dataKey.substring(1);
			if (gameObjectManager.hasData(name, dataKey)) {
				waitEventManager.waitGameObjectDataFlag(goType, name, dataKey, !matchFalseFlag);
				textPlayer.emit(waitEventName, name, dataKey);
				return;
			} else {
				waitEventManager.waitTime(0);
				return;
			}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/utils/wait/WaitAny.js
var KeyCodes = __webpack_exports__Input.Keyboard.KeyCodes;
var WaitAny = function(textPlayer, names, callback, scope) {
	var waitEventManager = textPlayer.waitEventManager;
	waitEventManager.clearWaitCompleteCallbacks().addWaitCompleteCallback(callback, scope);
	if (typeof names === "string" && names.length > 1 && names.indexOf("|") !== -1) names = names.split("|");
	else names = [names];
	for (var i = 0, cnt = names.length; i < cnt; i++) {
		var name = names[i];
		if (name == null || name === "wait") {
			var waitCompleteTriggerCallback = textPlayer.waitEventManager.getWaitCompleteTriggerCallback();
			textPlayer.emit("wait", waitCompleteTriggerCallback);
		} else if (typeof name === "number" || !isNaN(name)) {
			var time = parseFloat(name);
			waitEventManager.waitTime(time);
			textPlayer.emit("wait.time", time);
		} else if (name === "click") {
			waitEventManager.waitClick();
			textPlayer.emit("wait.click");
		} else if (name === "se") {
			waitEventManager.waitSoundEffectComplete();
			var music = textPlayer.soundManager.getLastSoundEffect();
			textPlayer.emit("wait.music", music);
		} else if (name === "se2") {
			waitEventManager.waitSoundEffect2Complete();
			var music = textPlayer.soundManager.getLastSoundEffect2();
			textPlayer.emit("wait.music", music);
		} else if (name === "bgm") {
			waitEventManager.waitBackgroundMusicComplete();
			var music = textPlayer.soundManager.getBackgroundMusic();
			textPlayer.emit("wait.music", music);
		} else if (name === "bgm2") {
			waitEventManager.waitBackgroundMusic2Complete();
			var music = textPlayer.soundManager.getBackgroundMusic2();
			textPlayer.emit("wait.music", music);
		} else if (KeyCodes.hasOwnProperty(name.toUpperCase())) {
			waitEventManager.waitKeyDown(name);
			textPlayer.emit("wait.keydown", name);
		} else if (IsWaitCameraEffect(name)) {
			waitEventManager.waitCameraEffectComplete(name);
			textPlayer.emit("wait.camera", name);
		} else if (IsWaitGameObject(textPlayer, name)) WaitGameObject(textPlayer, name, callback, scope);
		else {
			var waitCompleteTriggerCallback = textPlayer.waitEventManager.getWaitCompleteTriggerCallback();
			textPlayer.emit(`wait.${name}`, waitCompleteTriggerCallback);
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/typewriter/Wait.js
var Wait$1 = function(name) {
	if (this.ignoreWait) return this;
	this.pauseTyping();
	WaitAny(this.textPlayer, name, this.resumeTyping, this);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/typewriter/SetIgnoreWait.js
var SetIgnoreWait$1 = function(value) {
	if (value === void 0) value = true;
	this.ignoreWait = value;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/typewriter/SetSkipSpaceEnable.js
var SetSkipSpaceEnable = function(enable) {
	if (enable === void 0) enable = true;
	this.skipSpaceEnable = enable;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/typewriter/SetSkipTypingAnimation.js
var SetSkipTypingAnimation = function(value) {
	if (value === void 0) value = true;
	this.skipTypingAnimation = value;
	if (value) {
		var timers = this.timeline.getTimers(TypingAnimationTimerType);
		for (var i = 0, cnt = timers.length; i < cnt; i++) timers[i].seek(1);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/typewriter/SetSkipSoundEffect.js
var SetSkipSoundEffect = function(value) {
	if (value === void 0) value = true;
	this.skipSoundEffect = value;
	if (value) {
		var soundManager = this.textPlayer._soundManager;
		if (soundManager) soundManager.fadeOutAllSoundEffects(100, true);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/typewriter/SkipCurrentTypingDelay.js
var SkipCurrentTypingDelay = function() {
	if (this.typingTimer) this.typingTimer.seek(1);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/typewriter/Methods.js
var Methods$19 = {
	fadeOutPage: FadeOutPage,
	start: Start$1,
	typing: Typing,
	pause: Pause$1,
	resume: Resume$1,
	pauseTyping: PauseTyping$1,
	resumeTyping: ResumeTyping$1,
	wait: Wait$1,
	setIgnoreWait: SetIgnoreWait$1,
	setSkipSpaceEnable: SetSkipSpaceEnable,
	setSkipTypingAnimation: SetSkipTypingAnimation,
	setSkipSoundEffect: SetSkipSoundEffect,
	skipCurrentTypingDelay: SkipCurrentTypingDelay
};
Object.assign(Methods$19, TypingSpeedMethods_default$1);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/typewriter/TypeWriter.js
var GetValue$236 = __webpack_exports__Utils.Objects.GetValue;
var TypeWriter = class {
	constructor(textPlayer, config) {
		this.setEventEmitter();
		this.textPlayer = textPlayer;
		this.isPageTyping = false;
		this.typingTimer = void 0;
		this.pauseTypingTimer = void 0;
		this.inTypingProcessLoop = false;
		this.isTypingPaused = false;
		this.setIgnoreWait(false);
		this.setSkipTypingAnimation(false);
		this.setTypingStartCallback(GetValue$236(config, "onTypingStart", SetChildrenInvisible));
		this.setDefaultTypingSpeed(GetValue$236(config, "speed", 250));
		this.setTypingSpeed();
		this.setSkipSpaceEnable(GetValue$236(config, "skipSpace", false));
		this.setAnimationConfig(GetValue$236(config, "animation", void 0));
		this.setMinSizeEnable(GetValue$236(config, "minSizeEnable", false));
		this.setFadeOutPageCallback(GetValue$236(config, "fadeOutPage"));
	}
	destroy() {
		this.destroyEventEmitter();
		this.textPlayer = void 0;
		this.typingTimer = void 0;
		this.pauseTypingTimer = void 0;
		this.onTypeStart = void 0;
		this.animationConfig = void 0;
	}
	get timeline() {
		return this.textPlayer.timeline;
	}
	setTypingStartCallback(callback) {
		this.onTypeStart = callback;
		return this;
	}
	setAnimationConfig(config) {
		if (!config) config = {};
		if (!config.hasOwnProperty("duration")) config.duration = 0;
		if (!config.hasOwnProperty("onStart")) config.onStart = SetChildVisible;
		this.animationConfig = config;
		return this;
	}
	setFadeOutPageCallback(callback) {
		this.fadeOutPageCallback = callback;
		return this;
	}
	setMinSizeEnable(enable) {
		if (enable === void 0) enable = true;
		this.minSizeEnable = enable;
		return this;
	}
	getNextChild() {
		var child = this.nextChild;
		this.index = Math.min(this.index + 1, this.children.length);
		this._nextChild = void 0;
		return child;
	}
	get nextChild() {
		if (!this._nextChild) this._nextChild = this.children[this.index];
		return this._nextChild;
	}
};
var SetChildVisible = function(child) {
	if (child.setVisible) child.setVisible();
};
var SetChildrenInvisible = function(children) {
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		if (child.setVisible) child.setVisible(false);
	}
};
Object.assign(TypeWriter.prototype, EventEmitterMethods_default, Methods$19);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/sprite/spritemanager/SpriteBob.js
var SpriteBob = class extends BobBase {
	playAnimation(key) {
		this.gameObject.anims.timeScale = this.timeScale;
		this.gameObject.play(key);
		return this;
	}
	stopAnimation() {
		this.gameObject.stop();
		return this;
	}
	chainAnimation(keys) {
		this.gameObject.chain(keys);
		return this;
	}
	pauseAnimation() {
		this.gameObject.anims.pause();
		return this;
	}
	setTimeScale(timeScale) {
		super.setTimeScale(timeScale);
		if (this.gameObject.anims) this.gameObject.anims.timeScale = timeScale;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/sprite/spritemanager/methods/AnimationMethods.js
var AnimationMethods_default = {
	playAnimation(name, key) {
		if (!this.has(name)) this.add(name);
		this.get(name).playAnimation(key);
		return this;
	},
	stopAnimation(name) {
		if (!this.has(name)) return this;
		this.get(name).stopAnimation();
		return this;
	},
	chainAnimation(name, keys) {
		if (!this.has(name)) return this;
		this.get(name).chainAnimation(keys);
		return this;
	},
	pauseAnimation(name) {
		if (!this.has(name)) return this;
		this.get(name).pauseAnimation();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/sprite/spritemanager/methods/Methods.js
var Methods$18 = {};
Object.assign(Methods$18, AnimationMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/sprite/spritemanager/methods/GetCreateGameObjectCallback.js
var GetCreateGameObjectCallback = function(callback) {
	if (!callback || callback === "sprite") callback = CreateSprite;
	else if (callback === "image") callback = CreateImage$1;
	return callback;
};
var CreateSprite = function(scene, textureKey, frameName) {
	if (typeof frameName !== "string" && typeof frameName !== "number") frameName = void 0;
	return scene.add.sprite(0, 0, textureKey, frameName);
};
var CreateImage$1 = function(scene, textureKey, frameName) {
	if (typeof frameName !== "string" && typeof frameName !== "number") frameName = void 0;
	return scene.add.image(0, 0, textureKey, frameName);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/sprite/spritemanager/SpriteManager.js
var SpriteManager = class extends GOManager {
	constructor(scene, config) {
		if (config === void 0) config = {};
		config.BobClass = SpriteBob;
		super(scene, config);
	}
	setCreateGameObjectCallback(callback, scope) {
		callback = GetCreateGameObjectCallback(callback);
		super.setCreateGameObjectCallback(callback, scope);
		return this;
	}
};
Object.assign(SpriteManager.prototype, Methods$18);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/spritemanager/OnParsePlayAnimationTag.js
var IsPlayAnimationTag = function(tags, goType) {
	return tags.length === 3 && tags[0] === goType && tags[2] === "play";
};
var IsStopAnimationTag = function(tags, goType) {
	return tags.length === 3 && tags[0] === goType && tags[2] === "stop";
};
var OnParsePlayAnimationTag = function(textPlayer, parser, config) {
	var goType = config.name;
	parser.on("+", function(tag, ...keys) {
		if (parser.skipEventFlag) return;
		var tags = tag.split(".");
		var name;
		if (IsPlayAnimationTag(tags, goType)) name = tags[1];
		else return;
		AppendCommand$3.call(textPlayer, `${goType}.play`, PlayAnimation, [
			goType,
			name,
			keys
		], textPlayer);
		parser.skipEvent();
	}).on("+", function(tag) {
		if (parser.skipEventFlag) return;
		var tags = tag.split(".");
		var name;
		if (IsStopAnimationTag(tags, goType)) name = tags[1];
		else return;
		AppendCommand$3.call(textPlayer, `${goType}.stop`, StopAnimation, [goType, name], textPlayer);
		parser.skipEvent();
	}).on("-", function(tag) {
		if (parser.skipEventFlag) return;
		var tags = tag.split(".");
		var name;
		if (IsPlayAnimationTag(tags, goType)) name = tags[1];
		else return;
		AppendCommand$3.call(textPlayer, `${goType}.stop`, StopAnimation, [goType, name], textPlayer);
		parser.skipEvent();
	});
};
var PlayAnimation = function(params) {
	var goType, name, keys;
	[goType, name, keys] = params;
	var key = keys.shift();
	var gameObjectManager = this.getGameObjectManager(goType);
	gameObjectManager.playAnimation(name, key);
	if (keys.length > 0) gameObjectManager.chainAnimation(name, keys);
};
var StopAnimation = function(params) {
	var goType, args;
	[goType, ...args] = params;
	this.getGameObjectManager(goType).stopAnimation(...args);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/spritemanager/OnParsePauseAnimationTag.js
var IsPauseAnimationTag = function(tags, goType) {
	return tags.length === 3 && tags[0] === goType && tags[2] === "pause";
};
var OnParsePauseAnimationTag = function(textPlayer, parser, config) {
	var goType = config.name;
	parser.on("+", function(tag) {
		if (parser.skipEventFlag) return;
		var tags = tag.split(".");
		var name;
		if (IsPauseAnimationTag(tags, goType)) name = tags[1];
		else return;
		AppendCommand$3.call(textPlayer, `${goType}.pause`, PauseAnimation, [goType, name], textPlayer);
		parser.skipEvent();
	});
};
var PauseAnimation = function(params) {
	var goType, args;
	[goType, ...args] = params;
	this.getGameObjectManager(goType).pauseAnimation(...args);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/spritemanager/OnParseChainAnimationTag.js
var IsChainAnimationTag = function(tags, goType) {
	return tags.length === 3 && tags[0] === goType && tags[2] === "chain";
};
var OnParseChainAnimationTag = function(textPlayer, parser, config) {
	var goType = config.name;
	parser.on("+", function(tag) {
		if (parser.skipEventFlag) return;
		var tags = tag.split(".");
		var name;
		if (IsChainAnimationTag(tags, goType)) name = tags[1];
		else return;
		var keys = Array.prototype.slice.call(arguments, 1);
		AppendCommand$3.call(textPlayer, `${goType}.chain`, ChainAnimation, [
			goType,
			name,
			keys
		], textPlayer);
		parser.skipEvent();
	});
};
var ChainAnimation = function(params) {
	var goType, args;
	[goType, ...args] = params;
	this.getGameObjectManager(goType).chainAnimation(...args);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/spritemanager/AddSpriteManager.js
var ParseCallbacks$1 = [
	OnParsePlayAnimationTag,
	OnParsePauseAnimationTag,
	OnParseChainAnimationTag
];
var AddSpriteManager = function(config) {
	if (config === void 0) config = {};
	config.name = "sprite";
	config.parseCallbacks = ParseCallbacks$1;
	config.createGameObject = GetCreateGameObjectCallback(config.createGameObject);
	this.addGameObjectManager(config, SpriteManager);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/gameobjectmanager/OnParseAddGameObjectTag.js
var IsAddGameObjectTag = function(tags, goType) {
	return tags.length === 2 && tags[0] === goType;
};
var OnParseAddGameObjectTag = function(textPlayer, parser, config) {
	var goType = config.name;
	parser.on("+", function(tag, ...args) {
		if (parser.skipEventFlag) return;
		var tags = tag.split(".");
		var name;
		if (IsAddGameObjectTag(tags, goType)) name = tags[1];
		else return;
		AppendCommand$3.call(textPlayer, `${goType}.add`, AddGameObject, [
			goType,
			name,
			...args
		], textPlayer);
		parser.skipEvent();
	}).on("-", function(tag) {
		if (parser.skipEventFlag) return;
		var tags = tag.split(".");
		var name;
		if (IsAddGameObjectTag(tags, goType)) name = tags[1];
		else return;
		AppendCommand$3.call(textPlayer, `${goType}.remove`, RemoveGameObject, [goType, name], textPlayer);
		parser.skipEvent();
	});
};
var AddGameObject = function(params) {
	var goType, args;
	[goType, ...args] = params;
	this.getGameObjectManager(goType).add(...args);
};
var RemoveGameObject = function(params) {
	var goType, args;
	[goType, ...args] = params;
	this.getGameObjectManager(goType).remove(...args);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/gameobjectmanager/OnParseRemoveAllGameObjectsTag.js
var OnParseRemoveAllGameObjectsTag = function(textPlayer, parser, config) {
	var goType = config.name;
	parser.on("-", function(tag) {
		if (parser.skipEventFlag) return;
		if (tag === goType) {} else return;
		AppendCommand$3.call(textPlayer, `${goType}.removeall`, RemoveAllSprites, goType, textPlayer);
		parser.skipEvent();
	});
};
var RemoveAllSprites = function(goType) {
	this.getGameObjectManager(goType).removeAll();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/gameobjectmanager/OnParseCallGameObjectMethodTag.js
var IsPropTag = function(tags, goType) {
	return tags.length === 3 && tags[0] === goType;
};
var OnParseCallGameObjectMethodTag = function(textPlayer, parser, config) {
	var goType = config.name;
	parser.on(`+`, function(tag, ...parameters) {
		if (parser.skipEventFlag) return;
		var tags = tag.split(".");
		var name, prop;
		if (IsPropTag(tags, goType)) {
			name = tags[1];
			prop = tags[2];
		} else return;
		AppendCommand$3.call(textPlayer, `${goType}.call`, CallMethod, [
			goType,
			name,
			prop,
			...parameters
		], textPlayer);
		parser.skipEvent();
	});
};
var CallMethod = function(params) {
	var goType, name, prop, args;
	[goType, name, prop, ...args] = params;
	var eventName = `${goType}.${prop}`;
	this.emit(eventName, name, ...args);
	if (this.listenerCount(eventName) > 0) return;
	var gameObjectManager = this.getGameObjectManager(goType);
	if (gameObjectManager.hasMethod(name, prop)) gameObjectManager.call(name, prop, ...args);
	else gameObjectManager.setProperty(name, prop, args[0]);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/gameobjectmanager/OnParseEaseGameObjectPropertyTag.js
var EaseMode = {
	to: true,
	yoyo: true,
	from: true,
	toLeft: true,
	toRight: true,
	toUp: true,
	toDown: true,
	yoyoLeft: true,
	yoyoRight: true,
	yoyoUp: true,
	yoyoDown: true,
	fromLeft: true,
	fromRight: true,
	fromUp: true,
	fromDown: true
};
var IsEasePropertyTag = function(tags, goType) {
	return tags.length === 4 && tags[0] === goType && EaseMode[tags[3]];
};
var OnParseEaseGameObjectPropertyTag = function(textPlayer, parser, config) {
	var goType = config.name;
	textPlayer.getGameObjectManager(goType);
	parser.on(`+`, function(tag, value, duration, ease, repeat) {
		if (parser.skipEventFlag) return;
		var tags = tag.split(".");
		var name, property, easeMode;
		if (IsEasePropertyTag(tags, goType)) {
			name = tags[1];
			property = tags[2];
			easeMode = tags[3];
		} else return;
		if (typeof ease === "number") {
			repeat = ease;
			ease = void 0;
		}
		AppendCommand$3.call(textPlayer, `${goType}.ease`, EaseProperty, [
			goType,
			name,
			property,
			value,
			duration,
			ease,
			repeat,
			easeMode
		], textPlayer);
		parser.skipEvent();
	});
};
var EaseProperty = function(params) {
	var goType, name, property, value, duration, ease, repeat, easeMode;
	[goType, name, property, value, duration, ease, repeat, easeMode] = params;
	var gameObjectManager = this.getGameObjectManager(goType);
	var currentValue = gameObjectManager.getProperty(name, property);
	if (typeof currentValue !== "number") return;
	if (easeMode.endsWith("Left") || easeMode.endsWith("Up")) {
		if (easeMode.startsWith("to") || easeMode.startsWith("yoyo")) value = currentValue - value;
		else if (easeMode.startsWith("from")) {
			gameObjectManager.setProperty(name, property, currentValue - value);
			value = currentValue;
		}
	} else if (easeMode.endsWith("Right") || easeMode.endsWith("Down")) {
		if (easeMode.startsWith("to") || easeMode.startsWith("yoyo")) value = currentValue + value;
		else if (easeMode.startsWith("from")) {
			gameObjectManager.setProperty(name, property, currentValue + value);
			value = currentValue;
		}
	} else if (easeMode === "from") {
		gameObjectManager.setProperty(name, property, value);
		value = currentValue;
	}
	var isYoyo = easeMode.startsWith("yoyo");
	gameObjectManager.easeProperty(name, {
		property,
		value,
		duration,
		ease,
		repeat,
		yoyo: isYoyo
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/gameobjectmanager/GameObjectManagerMethods.js
var ParseCallbacks = [
	OnParseAddGameObjectTag,
	OnParseRemoveAllGameObjectsTag,
	OnParseCallGameObjectMethodTag,
	OnParseEaseGameObjectPropertyTag
];
var AddGameObjectManager = GameObjectManagerMethods_default$1.addGameObjectManager;
var GameObjectManagerMethods_default = { addGameObjectManager(config, GameObjectManagerClass) {
	config = config ? Clone$2(config) : {};
	if (!config.name) console.warn(`[TextPlayer] Parameter 'name' is required in addGameObjectManager(config) method`);
	var defaultLayer = config.defaultLayer;
	var createGameObject = config.createGameObject;
	var layerManager = this.layerManager;
	config.createGameObject = function(scene, ...args) {
		var gameObject = createGameObject.call(this, scene, ...args);
		if (defaultLayer && layerManager) layerManager.addToLayer(defaultLayer, gameObject);
		return gameObject;
	};
	AddGameObjectManager.call(this, config, GameObjectManagerClass);
	var customParseCallbacks = config.parseCallbacks;
	if (!customParseCallbacks) customParseCallbacks = ParseCallbacks;
	else customParseCallbacks = [...customParseCallbacks, ...ParseCallbacks];
	for (var i = 0, cnt = customParseCallbacks.length; i < cnt; i++) customParseCallbacks[i](this, this.parser, config);
	return this;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/SetClickTarget.js
var SetClickTarget = function(target) {
	this.waitEventManager.setClickTarget(target);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/SetCameraTarget.js
var SetCameraTarget = function(camera) {
	this.waitEventManager.setCameraTarget(camera);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/SetNextPageInput.js
var SetNextPageInput = function(input) {
	var textPlayer = this;
	if (!input) this.nextPageInput = null;
	else if (typeof input === "function") this.nextPageInput = function(callback, scope) {
		var waitEventManager = textPlayer.waitEventManager;
		waitEventManager.clearWaitCompleteCallbacks().addWaitCompleteCallback(callback, scope);
		var waitCompleteTriggerCallback = waitEventManager.getWaitCompleteTriggerCallback();
		input.call(textPlayer, waitCompleteTriggerCallback);
	};
	else this.nextPageInput = function(callback, scope) {
		WaitAny(textPlayer, input, callback, scope);
	};
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/AddImage.js
var AddImage = function(key, config) {
	this.imageManager.add(key, config);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/PlayMethods.js
var PlayMethods_default = {
	play(content) {
		if (this.isPlaying) return this;
		this.removeChildren();
		this.parser.start(content);
		this.isPlaying = true;
		this.once("complete", function() {
			this.isPlaying = false;
		}, this);
		this.lastWrapResult = void 0;
		this.typingNextPage();
		return this;
	},
	playPromise(content) {
		var promise = WaitComplete(this);
		this.play(content);
		return promise;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/TypingNextPage.js
var TypingNextPage = function() {
	if (!this.isPlaying || this.isPageTyping) return this;
	this.typeWriter.once(PageFadeOutCompleteEvent, _TypingNextPage, this).fadeOutPage();
	return this;
};
var _TypingNextPage = function() {
	var result = this.runWrap(this.lastWrapResult);
	this.lastWrapResult = result;
	this.emit("page.start");
	var OnTypingPageComplete = function() {
		this.emit(StopPlayEvent);
		if (result.isLastPage) this.emit("complete");
		else {
			this.emit("page.complete");
			if (this.ignoreNextPageInput) TypingNextPage.call(this);
			else if (this.nextPageInput) this.nextPageInput(TypingNextPage, this);
		}
	};
	this.once(StopPlayEvent, function() {
		this.typeWriter.off("complete", OnTypingPageComplete, this);
	}, this);
	this.typeWriter.once("complete", OnTypingPageComplete, this).start(result.children);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/PauseMethods.js
var PauseMethods_default = {
	pauseTyping() {
		this.typeWriter.pauseTyping();
		return this;
	},
	pause() {
		this.pauseTyping();
		this.timeline.pause();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/ResumeMethods.js
var ResumeMethods_default = {
	resumeTyping(offsetTime) {
		this.typeWriter.resumeTyping(offsetTime);
		return this;
	},
	resume() {
		this.resumeTyping();
		this.timeline.resume();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/Wait.js
var Wait = function(name) {
	this.typeWriter.wait(name);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/TypingSpeedMethods.js
var TypingSpeedMethods_default = {
	setDefaultTypingSpeed(speed) {
		this.defaultTypingSpeed = speed;
		return this;
	},
	setTypingSpeed(speed) {
		this.typingSpeed = speed;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/SetIgnoreWait.js
var SetIgnoreWait = function(value) {
	this.typeWriter.setIgnoreWait(value);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/SetIgnoreNextPageInput.js
var SetIgnoreNextPageInput = function(enable) {
	if (enable === void 0) enable = true;
	this.ignoreNextPageInput = enable;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/ShowPage.js
var ShowPage = function() {
	if (!this.isPlaying || !this.isPageTyping) return this;
	var typingSpeedSave = this.typeWriter.speed;
	var ignoreWaitSave = this.typeWriter.ignoreWait;
	var skipTypingAnimationSave = this.typeWriter.skipTypingAnimation;
	var skipSoundEffectSave = this.typeWriter.skipSoundEffect;
	this.typeWriter.once("complete", function() {
		this.typeWriter.setTypingSpeed(typingSpeedSave).setIgnoreWait(ignoreWaitSave).setSkipTypingAnimation(skipTypingAnimationSave).setSkipSoundEffect(skipSoundEffectSave);
	}, this).setTypingSpeed(0).skipCurrentTypingDelay().setIgnoreWait(true).setSkipTypingAnimation(true).setSkipSoundEffect(true);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/spritemanager/SpriteMethods.js
var SpriteMethods_default = {
	getSprite(name) {
		return this.getGameObject("sprite", name);
	},
	addSprite(name, gameObject) {
		this.addGameObject("sprite", name, gameObject);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/ContentMethods.js
var ContentMethods_default = {
	setContentOutputEnable(enable) {
		this.parser.setContentOutputEnable(enable);
		return this;
	},
	setContentCallback(callback, scope) {
		this.contentCallback = callback;
		this.contentCallbackScope = scope;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/Methods.js
var Methods$17 = {
	setClickTarget: SetClickTarget,
	setCameraTarget: SetCameraTarget,
	setNextPageInput: SetNextPageInput,
	addImage: AddImage,
	typingNextPage: TypingNextPage,
	wait: Wait,
	setIgnoreWait: SetIgnoreWait,
	setIgnoreNextPageInput: SetIgnoreNextPageInput,
	showPage: ShowPage
};
Object.assign(Methods$17, GameObjectManagerMethods_default, PlayMethods_default, PauseMethods_default, ResumeMethods_default, TypingSpeedMethods_default, SpriteMethods_default, ContentMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/methods/utils/ClearEvents.js
var ClearEvents = function(textPlayer) {
	for (var i = 0, cnt = ClearEvents$1.length; i < cnt; i++) textPlayer.emit(ClearEvents$1[i]);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/textplayer/TextPlayer.js
var IsPlainObject$51 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$235 = __webpack_exports__Utils.Objects.GetValue;
var TextPlayer = class extends Extend(DynamicText) {
	constructor(scene, x, y, fixedWidth, fixedHeight, config) {
		if (IsPlainObject$51(x)) config = x;
		else if (IsPlainObject$51(fixedWidth)) config = fixedWidth;
		if (config === void 0) config = {};
		var content = config.text;
		delete config.text;
		super(scene, x, y, fixedWidth, fixedHeight, config);
		this.type = "rexTextPlayer";
		this.parser = new Parser(this, GetValue$235(config, "parser", void 0));
		this.initManagers(scene, config);
		this.typeWriter = new TypeWriter(this, GetValue$235(config, "typing", void 0));
		this._imageManager = void 0;
		var imageData = GetValue$235(config, "images", void 0);
		if (imageData) this.addImage(imageData);
		var spriteManagerConfig = GetValue$235(config, "sprites");
		if (spriteManagerConfig !== false && spriteManagerConfig !== null) AddSpriteManager.call(this, spriteManagerConfig);
		this.setIgnoreNextPageInput(GetValue$235(config, "ignoreNextPageInput", false));
		this.setNextPageInput(GetValue$235(config, "nextPageInput", null));
		this.isPlaying = false;
		if (content) this.play(content);
	}
	get imageManager() {
		if (this._imageManager === void 0) this._imageManager = new ImageManager(this.scene);
		return this._imageManager;
	}
	get spriteManager() {
		return this.getGameObjectManager("sprite");
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		ClearEvents(this);
		this.parser.destroy();
		this.parser = void 0;
		this.typeWriter.destroy(fromScene);
		this.typeWriter = void 0;
		if (this._imageManager) this._imageManager.destroy(fromScene);
		this._imageManager = void 0;
		this.destroyManagers(fromScene);
		super.destroy(fromScene);
	}
	get isPageTyping() {
		return this.typeWriter.isPageTyping;
	}
	set defaultTypingSpeed(speed) {
		this.typeWriter.setDefaultTypingSpeed(speed);
	}
	get defaultTypingSpeed() {
		return this.typeWriter.defaultTypingSpeed;
	}
	set typingSpeed(speed) {
		this.typeWriter.setTypingSpeed(speed);
	}
	get typingSpeed() {
		return this.typeWriter.speed;
	}
	set timeScale(value) {
		this.setTimeScale(value);
	}
	get timeScale() {
		return this.getTimeScale();
	}
};
Object.assign(TextPlayer.prototype, Methods$17);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textplayer/TextPlayer.js
var TextPlayer_default = TextPlayer;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textplayer/Factory.js
ObjectFactory.register("textPlayer", function(x, y, width, height, config) {
	var gameObject = new TextPlayer_default(this.scene, x, y, width, height, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.TextPlayer", TextPlayer_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/hiddentextedit/methods/InputTextProperties.js
var ElementProperties$1 = {
	maxLength: ["maxLength", void 0],
	minLength: ["minLength", void 0],
	readOnly: ["readOnly", false]
};
var StyleProperties$1 = { direction: ["direction", void 0] };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/object/CopyProperty.js
var CopyProperty = function(from, to, key) {
	if (typeof key === "string") {
		if (from.hasOwnProperty(key)) to[key] = from[key];
	} else {
		var keys = key;
		if (Array.isArray(keys)) for (var i = 0, cnt = keys.length; i < cnt; i++) CopyProperty(from, to, keys[i]);
		else for (var key in keys) CopyProperty(from, to, key);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/hiddentextedit/methods/CopyElementConfig.js
var CopyElementConfig = function(from) {
	if (from === void 0) from = {};
	var to = {};
	CopyProperty(from, to, "inputType");
	CopyProperty(from, to, "type");
	CopyProperty(from, to, "style");
	CopyProperty(from, to, StyleProperties$1);
	CopyProperty(from, to, ElementProperties$1);
	return to;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/input/IsPointerInHitArea.js
var IsPointerInHitArea = function(gameObject, pointer, preTest, postTest, returnFirstPointer) {
	if (pointer) {
		if (preTest && !preTest(gameObject, pointer)) return false;
		if (!HitTest(gameObject, pointer)) return false;
		if (postTest && !postTest(gameObject, pointer)) return false;
		return true;
	} else {
		if (returnFirstPointer === void 0) returnFirstPointer = false;
		var inputManager = gameObject.scene.input.manager;
		var pointersTotal = inputManager.pointersTotal;
		var pointers = inputManager.pointers, pointer;
		for (var i = 0; i < pointersTotal; i++) {
			pointer = pointers[i];
			if (preTest && !preTest(gameObject, pointer)) continue;
			if (!HitTest(gameObject, pointer)) continue;
			if (postTest && !postTest(gameObject, pointer)) continue;
			if (returnFirstPointer) return pointer;
			return true;
		}
		return false;
	}
};
var HitTest = function(gameObject, pointer) {
	var scene = gameObject.scene;
	var cameras = scene.input.cameras.getCamerasBelowPointer(pointer);
	var inputManager = scene.input.manager;
	var gameObjects = [gameObject];
	for (var i = 0, len = cameras.length; i < len; i++) {
		inputManager.hitTest(pointer, gameObjects, cameras[i], HitTestResult);
		if (HitTestResult.length > 0) {
			HitTestResult.length = 0;
			return true;
		}
	}
	HitTestResult.length = 0;
	return false;
};
var HitTestResult = [];
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/hiddentextedit/methods/LastOpenedEditor.js
var LastOpenedEditor$1 = void 0;
var SetLastOpenedEditor$1 = function(editor) {
	if (editor === LastOpenedEditor$1) return;
	if (LastOpenedEditor$1 !== void 0) LastOpenedEditor$1.close();
	LastOpenedEditor$1 = editor;
};
var CloseLastOpenEditor$1 = function(editor) {
	if (editor !== LastOpenedEditor$1) return;
	LastOpenedEditor$1 = void 0;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dom/utils/SetProperties.js
var GetValue$234 = __webpack_exports__Utils.Objects.GetValue;
var SetProperties = function(properties, config, out) {
	if (out === void 0) out = {};
	var property, value;
	for (var key in properties) {
		property = properties[key];
		value = GetValue$234(config, key, property[1]);
		if (value !== void 0) out[property[0]] = value;
	}
	return out;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dom/utils/StopPropagationTouchEvents.js
var StopPropagationTouchEvents = function(element) {
	element.addEventListener("touchstart", callback, false);
	element.addEventListener("touchmove", callback, false);
	element.addEventListener("touchend", callback, false);
	element.addEventListener("mousedown", callback, false);
	element.addEventListener("mouseup", callback, false);
	element.addEventListener("mousemove", callback, false);
};
var callback = function(e) {
	e.stopPropagation();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/hiddentextedit/methods/EnterClose.js
var EnterClose = function() {
	this.close();
	this.emit("keydown-ENTER", this.parent, this);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/hiddentextedit/methods/OnOpen.js
var OnOpen = function() {
	this.isOpened = true;
	this.initText();
	if (this.enterCloseEnable) this.scene.input.keyboard.once("keydown-ENTER", EnterClose, this);
	this.scene.sys.events.on("postupdate", this.updateText, this);
	if (this.clickOutSideTarget) {
		MoveMyDepthAbove.call(this.clickOutSideTarget, this.parent);
		MoveMyDepthBelow.call(this.clickOutSideTarget, this.parent);
		this.clickOutSideTarget.setInteractive().on("pointerdown", this.onClickOutside, this);
	} else this.scene.input.on("pointerdown", this.onClickOutside, this);
	if (this.onOpenCallback) this.onOpenCallback(this.parent, this);
	this.emit("open", this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/hiddentextedit/methods/RemoveElement.js
var RemoveElement = function(element) {
	if (!element) return;
	var parentElement = element.parentElement;
	if (parentElement) parentElement.removeChild(element);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/hiddentextedit/methods/OnClose.js
var OnClose = function() {
	this.isOpened = false;
	this.updateText();
	if (this.enterCloseEnable) this.scene.input.keyboard.off("keydown-ENTER", EnterClose, this);
	this.scene.sys.events.off("postupdate", this.updateText, this);
	if (this.clickOutSideTarget) this.clickOutSideTarget.disableInteractive().off("pointerdown", this.onClickOutside, this);
	else this.scene.input.off("pointerdown", this.onClickOutside, this);
	if (this.onCloseCallback) this.onCloseCallback(this.parent, this);
	RemoveElement(this.node);
	this.node = void 0;
	this.emit("close", this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/hiddentextedit/methods/CreateElement.js
var GetValue$233 = __webpack_exports__Utils.Objects.GetValue;
var CreateElement = function(parent, config) {
	var element;
	var textType = GetValue$233(config, "inputType", void 0);
	if (textType === void 0) textType = GetValue$233(config, "type", "text");
	if (textType === "textarea") {
		element = document.createElement("textarea");
		element.style.resize = "none";
	} else {
		element = document.createElement("input");
		element.type = textType;
	}
	var style = GetValue$233(config, "style", void 0);
	var elementStyle = element.style;
	SetProperties(StyleProperties$1, style, elementStyle);
	elementStyle.position = "absolute";
	elementStyle.opacity = 0;
	elementStyle.pointerEvents = "none";
	elementStyle.zIndex = 0;
	elementStyle.transform = "scale(0)";
	SetProperties(ElementProperties$1, config, element);
	StopPropagationTouchEvents(element);
	var scaleManager = parent.scene.sys.scale;
	(scaleManager.isFullscreen ? scaleManager.fullscreenTarget : document.body).appendChild(element);
	element.addEventListener("focus", function(e) {
		OnOpen.call(parent);
	});
	element.addEventListener("blur", function(e) {
		OnClose.call(parent);
	});
	return element;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/hiddentextedit/methods/Open.js
var Open$2 = function() {
	if (this.isOpened) return this;
	if (this.readOnly) return this;
	SetLastOpenedEditor$1(this);
	if (!this.node) this.node = CreateElement(this, this.nodeConfig);
	this.setFocus();
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/hiddentextedit/methods/Close.js
var Close$1 = function() {
	if (!this.isOpened) return this;
	CloseLastOpenEditor$1(this);
	this.setBlur();
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/hiddentextedit/methods/Methods.js
var Methods$16 = {
	open: Open$2,
	close: Close$1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/hiddentextedit/HiddenTextEditBase.js
var GetValue$232 = __webpack_exports__Utils.Objects.GetValue;
var HiddenTextEditBase = class extends ComponentBase {
	constructor(gameObject, config) {
		super(gameObject);
		var textType = GetValue$232(config, "inputType", void 0);
		if (textType === void 0) textType = GetValue$232(config, "type", "text");
		this.setEnterCloseEnable(GetValue$232(config, "enterClose", textType !== "textarea"));
		var onOpen = GetValue$232(config, "onOpen", void 0);
		if (!onOpen) onOpen = GetValue$232(config, "onFocus", void 0);
		this.onOpenCallback = onOpen;
		this.clickOutSideTarget = GetValue$232(config, "clickOutSideTarget", void 0);
		var onClose = GetValue$232(config, "onClose", void 0);
		if (!onClose) onClose = GetValue$232(config, "onBlur", void 0);
		this.onCloseCallback = onClose;
		this.onUpdateCallback = GetValue$232(config, "onUpdate", void 0);
		this.isOpened = false;
		gameObject.on("pointerdown", function() {
			this.open();
		}, this).setInteractive();
		this.nodeConfig = CopyElementConfig(config);
		this.node = void 0;
	}
	destroy() {
		this.close();
		if (this.clickOutSideTarget) this.clickOutSideTarget.destroy();
		super.destroy();
	}
	onClickOutside(pointer) {
		if (!IsPointerInHitArea(this.parent, pointer)) this.close();
	}
	setEnterCloseEnable(enable) {
		if (enable === void 0) enable = true;
		this.enterCloseEnable = enable;
		return this;
	}
	initText() {}
	updateText() {}
	get text() {
		if (!this.node) return "";
		return this.node.value;
	}
	set text(value) {
		if (!this.node) return;
		this.node.value = value;
	}
	setText(value) {
		this.text = value;
		return this;
	}
	get maxLength() {
		return this.nodeConfig.maxLength;
	}
	set maxLength(value) {
		this.nodeConfig.maxLength = value;
		if (this.node) this.node.maxLength = value;
	}
	setMaxLength(value) {
		this.maxLength = value;
		return this;
	}
	get minLength() {
		return this.nodeConfig.minLength;
	}
	set minLength(value) {
		this.nodeConfig.minLength = value;
		if (this.node) this.node.minLength = value;
	}
	setMinLength(value) {
		this.minLength = value;
		return this;
	}
	get placeholder() {
		return this.node.placeholder;
	}
	set placeholder(value) {
		if (!this.node) return;
		this.node.placeholder = value;
	}
	setPlaceholder(value) {
		this.placeholder = value;
		return this;
	}
	selectText(selectionStart, selectionEnd) {
		if (!this.node) return this;
		if (selectionStart === void 0) this.node.select();
		else this.node.setSelectionRange(selectionStart, selectionEnd);
		return this;
	}
	selectAll() {
		this.selectText();
		return this;
	}
	get selectionStart() {
		if (!this.node) return 0;
		return this.node.selectionStart;
	}
	get selectionEnd() {
		if (!this.node) return 0;
		return this.node.selectionEnd;
	}
	get selectedText() {
		if (!this.node) return "";
		var node = this.node;
		return node.value.substring(node.selectionStart, node.selectionEnd);
	}
	get cursorPosition() {
		if (!this.node) return 0;
		return this.node.selectionStart;
	}
	set cursorPosition(value) {
		if (!this.node) return;
		this.node.setSelectionRange(value, value);
	}
	setCursorPosition(value) {
		if (value === void 0) value = this.text.length;
		else if (value < 0) value = this.text.length + value;
		this.cursorPosition = value;
		return this;
	}
	get tooltip() {
		if (!this.node) return "";
		return this.node.title;
	}
	set tooltip(value) {
		if (!this.node) return this;
		this.node.title = value;
	}
	setTooltip(value) {
		this.tooltip = value;
		return this;
	}
	setTextChangedCallback(callback) {
		this.onTextChanged = callback;
		return this;
	}
	get readOnly() {
		return this.nodeConfig.readOnly;
	}
	set readOnly(value) {
		this.nodeConfig.readOnly = value;
		if (this.node) this.node.readOnly = value;
	}
	setReadOnly(value) {
		if (value === void 0) value = true;
		this.readOnly = value;
		return this;
	}
	get spellCheck() {
		if (!this.node) return "";
		return this.node.spellcheck;
	}
	set spellCheck(value) {
		if (!this.node) return;
		this.node.spellcheck = value;
	}
	setSpellCheck(value) {
		this.spellCheck = value;
		return this;
	}
	get fontColor() {
		if (!this.node) return;
		return this.node.style.color;
	}
	set fontColor(value) {
		if (!this.node) return;
		this.node.style.color = value;
	}
	setFontColor(value) {
		this.fontColor = value;
		return this;
	}
	setStyle(key, value) {
		if (!this.node) return this;
		this.node.style[key] = value;
		return this;
	}
	getStyle(key) {
		if (!this.node) return;
		return this.node.style[key];
	}
	scrollToBottom() {
		if (!this.node) return this;
		this.node.scrollTop = this.node.scrollHeight;
		return this;
	}
	setEnabled(enabled) {
		if (!this.node) return this;
		if (enabled === void 0) enabled = true;
		this.node.disabled = !enabled;
		return this;
	}
	setBlur() {
		if (!this.node) return this;
		this.node.blur();
		return this;
	}
	setFocus() {
		if (!this.node) return this;
		this.node.focus();
		return this;
	}
	get isFocused() {
		return this.isOpened;
	}
};
Object.assign(HiddenTextEditBase.prototype, Methods$16);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/hiddentextedit/defaultcallbacks/NumberInputUpdateCallback.js
var NumberInputUpdateCallback = function(text, textObject, hiddenInputText) {
	text = text.replace(" ", "");
	var previousText = hiddenInputText.previousText;
	if (text === previousText) return text;
	if (isNaN(text)) {
		hiddenInputText.emit("nan", text, hiddenInputText);
		text = previousText;
		var cursorPosition = hiddenInputText.cursorPosition - 1;
		hiddenInputText.setText(text);
		hiddenInputText.setCursorPosition(cursorPosition);
	} else hiddenInputText.previousText = text;
	return text;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/canvasinput/textedit/SelectRange.js
var SelectRange = function(hiddenTextEdit) {
	var textObject = hiddenTextEdit.parent;
	var selectionStart = hiddenTextEdit.isOpened ? hiddenTextEdit.selectionStart : null;
	var selectionEnd = hiddenTextEdit.isOpened ? hiddenTextEdit.selectionEnd : null;
	var prevSelectionStart = hiddenTextEdit.prevSelectionStart;
	var prevSelectionEnd = hiddenTextEdit.prevSelectionEnd;
	if (prevSelectionStart === selectionStart && prevSelectionEnd === selectionEnd) return;
	var min, max;
	if (prevSelectionStart === null) {
		min = selectionStart;
		max = selectionEnd;
	} else if (selectionStart === null) {
		min = prevSelectionStart;
		max = prevSelectionEnd;
	} else {
		min = Math.min(prevSelectionStart, selectionStart);
		max = Math.max(prevSelectionEnd, selectionEnd);
	}
	for (var i = min; i < max; i++) {
		var inPrevSelectionRange;
		if (prevSelectionStart === null) inPrevSelectionRange = false;
		else inPrevSelectionRange = i >= prevSelectionStart && i < prevSelectionEnd;
		var inSelectionRange;
		if (selectionStart === null) inSelectionRange = false;
		else inSelectionRange = i >= selectionStart && i < selectionEnd;
		if (inPrevSelectionRange === inSelectionRange) continue;
		var child = textObject.getCharChild(i);
		if (child) {
			var eventName = inPrevSelectionRange ? "rangeout" : "rangein";
			textObject.emit(eventName, child, i, textObject);
		}
	}
	hiddenTextEdit.prevSelectionStart = selectionStart;
	hiddenTextEdit.prevSelectionEnd = selectionEnd;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/dynamictext/bob/utils/ScrollToBob.js
var ScrollToBob = function(bob) {
	var textObject = bob.parent;
	var textObjectLeftX = 0, textObjectRightX = textObject.width, textObjectTopY = 0, textObjectBottomY = textObject.height;
	var childX = bob.drawX, childY = bob.drawY;
	var childLeftX = childX + bob.drawTLX, childRightX = childX + bob.drawTRX, childTopY = childY + bob.drawTLY, childBottomY = childY + bob.drawBLY;
	var dx;
	if (childLeftX < textObjectLeftX) dx = textObjectLeftX - childLeftX;
	else if (childRightX > textObjectRightX) dx = textObjectRightX - childRightX;
	else dx = 0;
	var dy;
	if (childTopY < textObjectTopY) dy = textObjectTopY - childTopY;
	else if (childBottomY > textObjectBottomY) dy = textObjectBottomY - childBottomY;
	else dy = 0;
	textObject._textOX += dx;
	textObject._textOY += dy;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/canvasinput/textedit/MoveCursor.js
var MoveCursor = function(hiddenTextEdit) {
	var textObject = hiddenTextEdit.parent;
	var text = textObject.text;
	if (hiddenTextEdit.requestCursorPosition !== null) {
		hiddenTextEdit.setCursorPosition(hiddenTextEdit.requestCursorPosition);
		hiddenTextEdit.requestCursorPosition = null;
	}
	var cursorPosition = hiddenTextEdit.cursorPosition;
	if (hiddenTextEdit.prevCursorPosition === cursorPosition) return;
	if (hiddenTextEdit.prevCursorPosition !== null) {
		if (hiddenTextEdit.prevCursorPosition > text.length) hiddenTextEdit.prevCursorPosition = null;
	}
	if (hiddenTextEdit.prevCursorPosition !== null) {
		var child = textObject.getCharChild(hiddenTextEdit.prevCursorPosition);
		if (child) {
			if (child.text === "\n") child.clearTextSize();
			textObject.emit("cursorout", child, hiddenTextEdit.prevCursorPosition, textObject);
		}
	}
	if (cursorPosition != null) {
		var child = textObject.getCharChild(cursorPosition);
		if (child) {
			if (child.text === "\n") child.copyTextSize(textObject.lastInsertCursor);
			ScrollToBob(child);
			textObject.emit("cursorin", child, cursorPosition, textObject);
		}
	}
	textObject.emit("movecursor", cursorPosition, hiddenTextEdit.prevCursorPosition, textObject);
	hiddenTextEdit.prevCursorPosition = cursorPosition;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/canvasinput/textedit/ClearSelectRange.js
var ClearSelectRange = function(hiddenTextEdit) {
	var prevSelectionStart = hiddenTextEdit.prevSelectionStart;
	if (prevSelectionStart === null) return;
	var prevSelectionEnd = hiddenTextEdit.prevSelectionEnd;
	var textObject = hiddenTextEdit.parent;
	for (var i = prevSelectionStart; i < prevSelectionEnd; i++) {
		var child = textObject.getCharChild(i);
		if (child) textObject.emit("cursorout", child, i, textObject);
	}
	hiddenTextEdit.prevSelectionStart = null;
	hiddenTextEdit.prevSelectionEnd = null;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/canvasinput/textedit/ClearCursor.js
var ClearCursor = function(hiddenTextEdit) {
	var prevCursorPosition = hiddenTextEdit.prevCursorPosition;
	if (prevCursorPosition === null) return;
	var textObject = hiddenTextEdit.parent;
	var child = textObject.getCharChild(prevCursorPosition);
	if (child) textObject.emit("cursorout", child, prevCursorPosition, textObject);
	hiddenTextEdit.prevCursorPosition = null;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/canvasinput/textedit/HiddenTextEdit.js
var GetValue$231 = __webpack_exports__Utils.Objects.GetValue;
var HiddenTextEdit$1 = class extends HiddenTextEditBase {
	constructor(gameObject, config) {
		super(gameObject, config);
		this.setSelectAllWhenFocusEnable(GetValue$231(config, "selectAll", false));
		this.cursorMoveStartIndex = null;
		this.prevCursorPosition = null;
		this.prevSelectionStart = null;
		this.prevSelectionEnd = null;
		this.firstClickAfterOpen = false;
		this.requestCursorPosition = null;
		gameObject.on("pointerdown", function(pointer, localX, localY, event) {
			var child = gameObject.getNearestChild(localX, localY);
			var charIndex = gameObject.getCharIndex(child);
			if (!this.selectAllWhenFocus || !this.firstClickAfterOpen) this.setCursorPosition(charIndex);
			this.cursorMoveStartIndex = charIndex;
			this.firstClickAfterOpen = false;
		}, this).on("pointermove", function(pointer, localX, localY, event) {
			if (!pointer.isDown) return;
			var child = gameObject.getNearestChild(localX, localY);
			var charIndex = gameObject.getCharIndex(child);
			if (this.cursorMoveStartIndex < charIndex) this.selectText(this.cursorMoveStartIndex, charIndex + 1);
			else this.selectText(charIndex, this.cursorMoveStartIndex + 1);
		}, this);
		this.on("open", function() {
			if (this.selectAllWhenFocus) this.selectAll();
			this.firstClickAfterOpen = true;
			gameObject.emit("open");
		}, this).on("close", function() {
			gameObject.emit("close");
		}).on("keydown-ENTER", function() {
			gameObject.emit("keydown-ENTER");
		});
	}
	initText() {
		var textObject = this.parent;
		this.prevCursorPosition = null;
		this.setText(textObject.rawText);
		return this;
	}
	updateText() {
		this.updateRawText();
		this.updateDisplayText();
		this.updateCursor();
		return this;
	}
	updateRawText() {
		this.parent.updateRawText(this.text);
		return this;
	}
	updateDisplayText(text) {
		var textObject = this.parent;
		if (!text) text = this.text;
		if (this.onUpdateCallback) {
			var newText = this.onUpdateCallback(text, textObject, this);
			if (newText != null) text = newText;
		}
		if (textObject.text !== text) textObject.setText(text);
		return this;
	}
	updateCursor() {
		if (this.isOpened) {
			if (this.selectionStart !== this.selectionEnd) {
				ClearCursor(this);
				SelectRange(this);
			} else {
				ClearSelectRange(this);
				MoveCursor(this);
			}
		} else {
			ClearSelectRange(this);
			ClearCursor(this);
		}
		return this;
	}
	setNumberInput() {
		this.onUpdateCallback = NumberInputUpdateCallback;
		return this;
	}
	setSelectAllWhenFocusEnable(enable) {
		if (enable === void 0) enable = true;
		this.selectAllWhenFocus = enable;
		return this;
	}
	setRequestCursorPosition(value) {
		if (!this.isOpened) return this;
		this.requestCursorPosition = value;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/canvasinput/textedit/CreateHiddenTextEdit.js
var GetValue$230 = __webpack_exports__Utils.Objects.GetValue;
var PropertiesList = [
	"inputType",
	"onOpen",
	"clickOutSideTarget",
	"onFocus",
	"onClose",
	"onBlur",
	"onUpdate",
	"enterClose",
	"readOnly",
	"maxLength",
	"minLength",
	"selectAll"
];
var CreateHiddenTextEdit = function(parent, parentConfig) {
	var config = GetValue$230(parentConfig, "edit");
	if (config === void 0) config = {};
	CopyProperty(parentConfig, config, PropertiesList);
	return new HiddenTextEdit$1(parent, config);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/object/HasValue.js
var HasValue = function(source, key) {
	if (!source || typeof source === "number") return false;
	else if (source.hasOwnProperty(key)) return true;
	else if (key.indexOf(".") !== -1) {
		var keys = key.split(".");
		var parent = source;
		for (var i = 0; i < keys.length; i++) if (parent.hasOwnProperty(keys[i])) parent = parent[keys[i]];
		else return false;
		return true;
	} else return false;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/system/GetRootGameObject.js
var GetRootGameObject = function(gameObject) {
	if (gameObject.parentContainer) return GetRootGameObject(gameObject.parentContainer);
	var layer = GetLayer(gameObject);
	if (layer) return GetRootGameObject(layer);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/camera/GetFirstRenderCamera.js
var GetFirstRenderCamera = function(gameObject) {
	var cameraFilter = GetRootGameObject(gameObject).cameraFilter;
	var cameras = gameObject.scene.sys.cameras.cameras;
	var camera, isCameraIgnore;
	for (var i = 0, cnt = cameras.length; i < cnt; i++) {
		camera = cameras[i];
		isCameraIgnore = (cameraFilter & camera.id) > 0;
		if (!isCameraIgnore) return camera;
	}
	return null;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/fullwindow/FullWindow.js
var FullWindow = class extends ComponentBase {
	constructor(gameObject, config) {
		super(gameObject);
		this.targetCamera = void 0;
		this.boot();
	}
	boot() {
		this.scene.sys.events.on("prerender", this.resize, this);
	}
	destroy() {
		if (!this.scene) return;
		this.scene.sys.events.off("prerender", this.resize, this);
		super.destroy();
	}
	resize() {
		var scene = this.scene;
		var gameObject = this.parent;
		var camera = GetFirstRenderCamera(gameObject);
		if (!camera) return;
		gameObject.setScrollFactor(0).setOrigin(.5);
		var gameSize = scene.sys.scale.gameSize;
		var gameWidth = gameSize.width, gameHeight = gameSize.height, scale = 1 / camera.zoom;
		var x = gameWidth / 2, y = gameHeight / 2;
		var width = gameWidth * scale, height = gameHeight * scale;
		if (gameObject.x !== x || gameObject.y !== y) gameObject.setPosition(x, y);
		if (gameObject.width !== width || gameObject.height !== height) gameObject.setSize(width, height);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/fullwindow/fullwindowzone/FullWindowZone.js
var Zone$2 = __webpack_exports__GameObjects.Zone;
var FullWindowRectangle$1 = class extends Zone$2 {
	constructor(scene) {
		super(scene, 0, 0, 2, 2);
		this.fullWindow = new FullWindow(this);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/canvasinput/methods/InjectDefaultConfig.js
var InjectDefaultConfig = function(scene, config) {
	var isSingleLineMode = !config.textArea;
	if (!HasValue(config, "wrap.vAlign")) {
		var defaultValue = isSingleLineMode ? "center" : "top";
		SetValue(config, "wrap.vAlign", defaultValue);
	}
	if (!HasValue(config, "wrap.wrapMode")) SetValue(config, "wrap.wrapMode", "char");
	if (!HasValue(config, "wrap.maxLines")) {
		var defaultValue = isSingleLineMode ? 1 : void 0;
		SetValue(config, "wrap.maxLines", defaultValue);
	}
	if (isSingleLineMode) SetValue(config, "wrap.wrapWidth", Infinity);
	if (!HasValue(config, "wrap.useDefaultTextHeight")) SetValue(config, "wrap.useDefaultTextHeight", true);
	if (!config.edit) config.edit = {};
	if (!HasValue(config.edit, "inputType")) {
		var defaultValue = isSingleLineMode ? "text" : "textarea";
		SetValue(config.edit, "inputType", defaultValue);
	}
	if (config.clickOutSideTarget === true) {
		var clickOutSideTarget = new FullWindowRectangle$1(scene);
		scene.add.existing(clickOutSideTarget);
		config.clickOutSideTarget = clickOutSideTarget;
	}
	return config;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/object/ExtractByPrefix.js
var ExtractByPrefix = function(obj, prefix, delimiter, out) {
	if (delimiter === void 0) delimiter = ".";
	if (out === void 0) out = {};
	if (!obj) return out;
	if (prefix in obj) return Object.assign(out, obj[prefix]);
	prefix += delimiter;
	for (var key in obj) {
		if (!key.startsWith(prefix)) continue;
		out[key.replace(prefix, "")] = obj[key];
	}
	return out;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/canvasinput/methods/RegisterArrowKeysEvent.js
var RegisterArrowKeysEvent = function() {
	var keyEventManager = this.scene.input.keyboard;
	this.textEdit.on("open", function() {
		keyEventManager.on("keydown-UP", this.cursorMoveUp, this).on("keydown-DOWN", this.cursorMoveDown, this);
	}, this).on("close", function() {
		keyEventManager.off("keydown-UP", this.cursorMoveUp, this).off("keydown-DOWN", this.cursorMoveDown, this);
	}, this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/object/GetPartialData.js
var GetPartialData = function(obj, keys, out) {
	if (out === void 0) out = {};
	if (Array.isArray(keys)) {
		var key;
		for (var i = 0, cnt = keys.length; i < cnt; i++) {
			key = keys[i];
			out[key] = obj[key];
		}
	} else for (var key in keys) out[key] = obj[key];
	return out;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/object/IsKeyValueEqual.js
var IsKeyValueEqual = function(objA, objB) {
	for (var key in objA) {
		if (!(key in objB)) return false;
		if (objA[key] !== objB[key]) return false;
	}
	for (var key in objB) if (!(key in objA)) return false;
	return true;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/canvasinput/methods/RegisterCursorStyle.js
var RegisterCursorStyle = function(cursorStyle) {
	if (IsEmpty(cursorStyle)) return;
	this.setCursorStyle(cursorStyle).on("cursorin", function(child) {
		var cursorStyle = this.cursorStyle;
		var styleSave = GetPartialData(child.style, cursorStyle);
		if (IsKeyValueEqual(cursorStyle, styleSave)) return;
		child.styleSave = styleSave;
		child.modifyStyle(cursorStyle);
	}, this).on("cursorout", function(child) {
		if (!child.styleSave) return;
		child.modifyStyle(child.styleSave);
		child.styleSave = void 0;
	}, this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/canvasinput/methods/RegisterRangeStyle.js
var RegisterRangeStyle = function(rangeStyle) {
	if (IsEmpty(rangeStyle)) return;
	this.setRangeStyle(rangeStyle).on("rangein", function(child) {
		var rangeStyle = this.rangeStyle;
		var styleSave = GetPartialData(child.style, rangeStyle);
		if (IsKeyValueEqual(rangeStyle, styleSave)) return;
		child.styleSave = styleSave;
		child.modifyStyle(rangeStyle);
	}, this).on("rangeout", function(child) {
		if (!child.styleSave) return;
		child.modifyStyle(child.styleSave);
		child.styleSave = void 0;
	}, this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/canvasinput/methods/RegisterFocusStyle.js
var RegisterFocusStyle = function(focusStyle) {
	if (IsEmpty(focusStyle)) return;
	this.setFocusStyle(focusStyle).on("open", function() {
		var child = this.background;
		var focusStyle = this.focusStyle;
		var styleSave = GetPartialData(child, focusStyle);
		if (IsKeyValueEqual(focusStyle, styleSave)) return;
		child.styleSave = styleSave;
		child.modifyStyle(focusStyle);
	}, this).on("close", function() {
		var child = this.background;
		if (!child.styleSave) return;
		child.modifyStyle(child.styleSave);
		child.styleSave = void 0;
	}, this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/canvasinput/methods/CreateInsertCursorChild.js
var CreateInsertCursorChild = function(textObject) {
	var child = textObject.createCharChild("|");
	child.text = "";
	return child;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/jsdiff/diff/base.js
function Diff() {}
Diff.prototype = {
	diff(oldString, newString, options = {}) {
		let callback = options.callback;
		if (typeof options === "function") {
			callback = options;
			options = {};
		}
		this.options = options;
		let self = this;
		function done(value) {
			if (callback) {
				setTimeout(function() {
					callback(void 0, value);
				}, 0);
				return true;
			} else return value;
		}
		oldString = this.castInput(oldString);
		newString = this.castInput(newString);
		oldString = this.removeEmpty(this.tokenize(oldString));
		newString = this.removeEmpty(this.tokenize(newString));
		let newLen = newString.length, oldLen = oldString.length;
		let editLength = 1;
		let maxEditLength = newLen + oldLen;
		if (options.maxEditLength) maxEditLength = Math.min(maxEditLength, options.maxEditLength);
		let bestPath = [{
			newPos: -1,
			components: []
		}];
		let oldPos = this.extractCommon(bestPath[0], newString, oldString, 0);
		if (bestPath[0].newPos + 1 >= newLen && oldPos + 1 >= oldLen) return done([{
			value: this.join(newString),
			count: newString.length
		}]);
		function execEditLength() {
			for (let diagonalPath = -1 * editLength; diagonalPath <= editLength; diagonalPath += 2) {
				let basePath;
				let addPath = bestPath[diagonalPath - 1], removePath = bestPath[diagonalPath + 1], oldPos = (removePath ? removePath.newPos : 0) - diagonalPath;
				if (addPath) bestPath[diagonalPath - 1] = void 0;
				let canAdd = addPath && addPath.newPos + 1 < newLen, canRemove = removePath && 0 <= oldPos && oldPos < oldLen;
				if (!canAdd && !canRemove) {
					bestPath[diagonalPath] = void 0;
					continue;
				}
				if (!canAdd || canRemove && addPath.newPos < removePath.newPos) {
					basePath = clonePath(removePath);
					self.pushComponent(basePath.components, void 0, true);
				} else {
					basePath = addPath;
					basePath.newPos++;
					self.pushComponent(basePath.components, true, void 0);
				}
				oldPos = self.extractCommon(basePath, newString, oldString, diagonalPath);
				if (basePath.newPos + 1 >= newLen && oldPos + 1 >= oldLen) return done(buildValues(self, basePath.components, newString, oldString, self.useLongestToken));
				else bestPath[diagonalPath] = basePath;
			}
			editLength++;
		}
		if (callback) (function exec() {
			setTimeout(function() {
				if (editLength > maxEditLength) return callback();
				if (!execEditLength()) exec();
			}, 0);
		})();
		else while (editLength <= maxEditLength) {
			let ret = execEditLength();
			if (ret) return ret;
		}
	},
	pushComponent(components, added, removed) {
		let last = components[components.length - 1];
		if (last && last.added === added && last.removed === removed) components[components.length - 1] = {
			count: last.count + 1,
			added,
			removed
		};
		else components.push({
			count: 1,
			added,
			removed
		});
	},
	extractCommon(basePath, newString, oldString, diagonalPath) {
		let newLen = newString.length, oldLen = oldString.length, newPos = basePath.newPos, oldPos = newPos - diagonalPath, commonCount = 0;
		while (newPos + 1 < newLen && oldPos + 1 < oldLen && this.equals(newString[newPos + 1], oldString[oldPos + 1])) {
			newPos++;
			oldPos++;
			commonCount++;
		}
		if (commonCount) basePath.components.push({ count: commonCount });
		basePath.newPos = newPos;
		return oldPos;
	},
	equals(left, right) {
		if (this.options.comparator) return this.options.comparator(left, right);
		else return left === right || this.options.ignoreCase && left.toLowerCase() === right.toLowerCase();
	},
	removeEmpty(array) {
		let ret = [];
		for (let i = 0; i < array.length; i++) if (array[i]) ret.push(array[i]);
		return ret;
	},
	castInput(value) {
		return value;
	},
	tokenize(value) {
		return value.split("");
	},
	join(chars) {
		return chars.join("");
	}
};
function buildValues(diff, components, newString, oldString, useLongestToken) {
	let componentPos = 0, componentLen = components.length, newPos = 0, oldPos = 0;
	for (; componentPos < componentLen; componentPos++) {
		let component = components[componentPos];
		if (!component.removed) {
			if (!component.added && useLongestToken) {
				let value = newString.slice(newPos, newPos + component.count);
				value = value.map(function(value, i) {
					let oldValue = oldString[oldPos + i];
					return oldValue.length > value.length ? oldValue : value;
				});
				component.value = diff.join(value);
			} else component.value = diff.join(newString.slice(newPos, newPos + component.count));
			newPos += component.count;
			if (!component.added) oldPos += component.count;
		} else {
			component.value = diff.join(oldString.slice(oldPos, oldPos + component.count));
			oldPos += component.count;
			if (componentPos && components[componentPos - 1].added) {
				let tmp = components[componentPos - 1];
				components[componentPos - 1] = components[componentPos];
				components[componentPos] = tmp;
			}
		}
	}
	let lastComponent = components[componentLen - 1];
	if (componentLen > 1 && typeof lastComponent.value === "string" && (lastComponent.added || lastComponent.removed) && diff.equals("", lastComponent.value)) {
		components[componentLen - 2].value += lastComponent.value;
		components.pop();
	}
	return components;
}
function clonePath(path) {
	return {
		newPos: path.newPos,
		components: path.components.slice(0)
	};
}
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/jsdiff/diff/character.js
var characterDiff = new Diff();
function diffChars(oldStr, newStr, options) {
	return characterDiff.diff(oldStr, newStr, options);
}
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/jsdiff/diff/word.js
var extendedWordChars = /^[a-zA-Z\u{C0}-\u{FF}\u{D8}-\u{F6}\u{F8}-\u{2C6}\u{2C8}-\u{2D7}\u{2DE}-\u{2FF}\u{1E00}-\u{1EFF}]+$/u;
var reWhitespace = /\S/;
var wordDiff = new Diff();
wordDiff.equals = function(left, right) {
	if (this.options.ignoreCase) {
		left = left.toLowerCase();
		right = right.toLowerCase();
	}
	return left === right || this.options.ignoreWhitespace && !reWhitespace.test(left) && !reWhitespace.test(right);
};
wordDiff.tokenize = function(value) {
	let tokens = value.split(/([^\S\r\n]+|[()[\]{}'"\r\n]|\b)/);
	for (let i = 0; i < tokens.length - 1; i++) if (!tokens[i + 1] && tokens[i + 2] && extendedWordChars.test(tokens[i]) && extendedWordChars.test(tokens[i + 2])) {
		tokens[i] += tokens[i + 2];
		tokens.splice(i + 1, 2);
		i--;
	}
	return tokens;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/jsdiff/diff/line.js
var lineDiff = new Diff();
lineDiff.tokenize = function(value) {
	let retLines = [], linesAndNewlines = value.split(/(\n|\r\n)/);
	if (!linesAndNewlines[linesAndNewlines.length - 1]) linesAndNewlines.pop();
	for (let i = 0; i < linesAndNewlines.length; i++) {
		let line = linesAndNewlines[i];
		if (i % 2 && !this.options.newlineIsToken) retLines[retLines.length - 1] += line;
		else {
			if (this.options.ignoreWhitespace) line = line.trim();
			retLines.push(line);
		}
	}
	return retLines;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/jsdiff/diff/sentence.js
var sentenceDiff = new Diff();
sentenceDiff.tokenize = function(value) {
	return value.split(/(\S.+?[.!?])(?=\s+|$)/);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/jsdiff/diff/css.js
var cssDiff = new Diff();
cssDiff.tokenize = function(value) {
	return value.split(/([{}:;,]|\s+)/);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/jsdiff/diff/json.js
var objectPrototypeToString = Object.prototype.toString;
var jsonDiff = new Diff();
jsonDiff.useLongestToken = true;
jsonDiff.tokenize = lineDiff.tokenize;
jsonDiff.castInput = function(value) {
	const { undefinedReplacement, stringifyReplacer = (k, v) => typeof v === "undefined" ? undefinedReplacement : v } = this.options;
	return typeof value === "string" ? value : JSON.stringify(canonicalize(value, null, null, stringifyReplacer), stringifyReplacer, "  ");
};
jsonDiff.equals = function(left, right) {
	return Diff.prototype.equals.call(jsonDiff, left.replace(/,([\r\n])/g, "$1"), right.replace(/,([\r\n])/g, "$1"));
};
function canonicalize(obj, stack, replacementStack, replacer, key) {
	stack = stack || [];
	replacementStack = replacementStack || [];
	if (replacer) obj = replacer(key, obj);
	let i = 0;
	for (; i < stack.length; i += 1) if (stack[i] === obj) return replacementStack[i];
	let canonicalizedObj;
	if ("[object Array]" === objectPrototypeToString.call(obj)) {
		stack.push(obj);
		canonicalizedObj = new Array(obj.length);
		replacementStack.push(canonicalizedObj);
		for (i = 0; i < obj.length; i += 1) canonicalizedObj[i] = canonicalize(obj[i], stack, replacementStack, replacer, key);
		stack.pop();
		replacementStack.pop();
		return canonicalizedObj;
	}
	if (obj && obj.toJSON) obj = obj.toJSON();
	if (typeof obj === "object" && obj !== null) {
		stack.push(obj);
		canonicalizedObj = {};
		replacementStack.push(canonicalizedObj);
		let sortedKeys = [], key;
		for (key in obj)
 /* istanbul ignore else */
		if (obj.hasOwnProperty(key)) sortedKeys.push(key);
		sortedKeys.sort();
		for (i = 0; i < sortedKeys.length; i += 1) {
			key = sortedKeys[i];
			canonicalizedObj[key] = canonicalize(obj[key], stack, replacementStack, replacer, key);
		}
		stack.pop();
		replacementStack.pop();
	} else canonicalizedObj = obj;
	return canonicalizedObj;
}
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/jsdiff/diff/array.js
var arrayDiff = new Diff();
arrayDiff.tokenize = function(value) {
	return value.slice();
};
arrayDiff.join = arrayDiff.removeEmpty = function(value) {
	return value;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/canvasinput/methods/SetText.js
var RemoveItem$8 = __webpack_exports__Utils.Array.Remove;
var SetText = function(textObject, newText) {
	var text = textObject.text;
	if (newText === text) return;
	if (text == null) text = "";
	RemoveItem$8(textObject.children, textObject.lastInsertCursor);
	if (newText === "") textObject.removeChildren();
	else {
		var results = diffChars(text, newText);
		var charIndex = 0;
		for (var i = 0, cnt = results.length; i < cnt; i++) {
			var result = results[i];
			if (result.removed) textObject.removeText(charIndex, result.count);
			else if (result.added) {
				textObject.insertText(charIndex, result.value);
				charIndex += result.count;
			} else charIndex += result.count;
		}
	}
	textObject.children.push(textObject.lastInsertCursor);
	textObject.runWrap();
	if (textObject.isOpened && !textObject.isDisplayTextSeparated) textObject.textEdit.setText(newText);
	textObject.emit("textchange", newText, textObject);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/canvasinput/methods/RawTextMethods.js
var RawTextMethods_default = {
	setRawText(value) {
		if (value == null) {
			this.clearRawText();
			return this;
		}
		value = value.toString();
		this._rawText = value;
		this.isDisplayTextSeparated = true;
		if (this.isOpened) this.textEdit.setText(value).updateText();
		return this;
	},
	clearRawText() {
		this._rawText = void 0;
		this.isDisplayTextSeparated = false;
		if (this.isOpened) this.textEdit.setText(this.text).updateText();
		return this;
	},
	getRawText() {
		return this.isDisplayTextSeparated ? this._rawText : this.text;
	},
	updateRawText(value) {
		if (!this.isDisplayTextSeparated) return this;
		value = value.toString();
		this._rawText = value;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/canvasinput/methods/SetTextOXYMethods.js
var SetTextOXYMethods_default = {
	setTextOYByPercentage(percentage) {
		this.setTextOY(-this.textVisibleHeight * percentage);
		return this;
	},
	getTextOYPercentage() {
		var textVisibleHeight = this.textVisibleHeight;
		if (textVisibleHeight === 0) return 0;
		return this._textOY / -textVisibleHeight;
	},
	setTextOXByPercentage(percentage) {
		this.setTextOX(-this.textVisibleWidth * percentage);
		return this;
	},
	getTextOXPercentage() {
		var textVisibleWidth = this.textVisibleWidth;
		if (textVisibleWidth === 0) return 0;
		return this._textOX / -textVisibleWidth;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/canvasinput/methods/MoveCursorMethods.js
var Clamp$14 = __webpack_exports__Math.Clamp;
var GetIndex = function(characterCountOfLines, position) {
	var result = {
		lineIndex: 0,
		position: 0
	};
	if (position < 0) return result;
	for (var li = 0, lcnt = characterCountOfLines.length; li < lcnt; li++) {
		var characterCount = characterCountOfLines[li];
		if (position <= characterCount) {
			result.lineIndex = li;
			break;
		}
		position -= characterCount;
	}
	result.position = position;
	return result;
};
var GetPosition = function(characterCountOfLines, index) {
	var position = 0;
	var lineIndex = index.lineIndex;
	if (lineIndex < 0) return position;
	for (var li = 0, lcnt = characterCountOfLines.length; li < lcnt; li++) {
		var characterCount = characterCountOfLines[li];
		if (lineIndex > li) position += characterCount;
		else if (lineIndex === li) position += Math.min(index.position, characterCount);
		else break;
	}
	return position;
};
var MoveCursorMethods_default = {
	cursorMoveLeft() {
		if (!this.isOpened) return this;
		var position = Clamp$14(this.cursorPosition - 1, 0, this.inputText.length);
		this.setCursorPosition(position);
		return this;
	},
	cursorMoveRight() {
		if (!this.isOpened) return this;
		var position = Clamp$14(this.cursorPosition + 1, 0, this.inputText.length);
		this.setCursorPosition(position);
		return this;
	},
	cursorMoveUp() {
		if (!this.isOpened) return this;
		var result = GetIndex(this.characterCountOfLines, this.cursorPosition);
		result.lineIndex -= 1;
		var position = Clamp$14(GetPosition(this.characterCountOfLines, result), 0, this.inputText.length);
		this.setCursorPosition(position);
		return this;
	},
	cursorMoveDown() {
		if (!this.isOpened) return this;
		var result = GetIndex(this.characterCountOfLines, this.cursorPosition);
		result.lineIndex += 1;
		var position = Clamp$14(GetPosition(this.characterCountOfLines, result), 0, this.inputText.length);
		this.setCursorPosition(position);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dynamictext/canvasinput/CanvasInput.js
var IsPlainObject$50 = __webpack_exports__Utils.Objects.IsPlainObject;
__webpack_exports__Utils.Objects.GetValue;
var CanvasInput = class extends DynamicText {
	constructor(scene, x, y, fixedWidth, fixedHeight, config) {
		if (IsPlainObject$50(x)) config = x;
		else if (IsPlainObject$50(fixedWidth)) config = fixedWidth;
		if (config === void 0) config = {};
		InjectDefaultConfig(scene, config);
		var text = config.text;
		if (text) delete config.text;
		var rawText = config.rawText;
		if (rawText) delete config.rawText;
		var focusStyle = ExtractByPrefix(config.background, "focus");
		var cursorStyle = ExtractByPrefix(config.style, "cursor");
		var rangeStyle = ExtractByPrefix(config.style, "range");
		super(scene, x, y, fixedWidth, fixedHeight, config);
		this.type = "rexCanvasInput";
		this.contentWidth = void 0;
		this.contentHeight = void 0;
		this.lineHeight = void 0;
		this.linesCount = void 0;
		this.characterCountOfLines = [];
		this._text;
		this.isDisplayTextSeparated = false;
		this._rawText = void 0;
		this.textEdit = CreateHiddenTextEdit(this, config);
		RegisterArrowKeysEvent.call(this);
		if (config.focusStyle) Object.assign(focusStyle, config.focusStyle);
		RegisterFocusStyle.call(this, focusStyle);
		if (config.cursorStyle) Object.assign(cursorStyle, config.cursorStyle);
		RegisterCursorStyle.call(this, cursorStyle);
		if (config.rangeStyle) Object.assign(rangeStyle, config.rangeStyle);
		if (IsEmpty(rangeStyle)) Object.assign(rangeStyle, cursorStyle);
		RegisterRangeStyle.call(this, rangeStyle);
		var addCharCallback = config.onAddChar;
		if (addCharCallback) this.on("addchar", addCharCallback);
		var cursorInCallback = config.onCursorIn;
		if (cursorInCallback) this.on("cursorin", cursorInCallback);
		var cursorOutCallback = config.onCursorOut;
		if (cursorOutCallback) this.on("cursorout", cursorOutCallback);
		var useCursorCallback = !config.onRangeIn && !config.onRangeOut;
		var rangeInCallback = !useCursorCallback ? config.onRangeIn : config.onCursorIn;
		if (rangeInCallback) this.on("rangein", rangeInCallback);
		var rangeOutCallback = !useCursorCallback ? config.onRangeOut : config.onCursorOut;
		if (rangeOutCallback) this.on("rangeout", rangeOutCallback);
		var moveCursorCallback = config.onMoveCursor;
		if (moveCursorCallback) this.on("movecursor", moveCursorCallback);
		this.setParseTextCallback(config.parseTextCallback);
		this.lastInsertCursor = CreateInsertCursorChild(this);
		this.setRawText(rawText);
		if (!text) text = "";
		this.setText(text);
	}
	addChild(child, index) {
		super.addChild(child, index);
		if (Array.isArray(child)) {
			var children = child;
			for (var i = 0, cnt = children.length; i < cnt; i++) {
				var child = children[i];
				if (IsChar(child)) this.emit("addchar", child, index + i, this);
			}
		} else if (IsChar(child)) this.emit("addchar", child, index, this);
		return this;
	}
	get text() {
		return this._text;
	}
	set text(value) {
		if (value == null) value = "";
		else value = value.toString();
		if (this._text === value) return;
		SetText(this, value);
		this._text = value;
	}
	get rawText() {
		return this.getRawText();
	}
	setText(text) {
		this.text = text;
		return this;
	}
	appendText(text) {
		this.setText(this.text + text);
		return this;
	}
	runWrap(config) {
		var result = super.runWrap(config);
		this.contentWidth = result.maxLineWidth;
		this.contentHeight = result.linesHeight;
		this.lineHeight = result.lineHeight;
		this.linesCount = result.lines.length;
		this.characterCountOfLines.length = 0;
		var wrapLines = result.lines;
		for (var li = 0, lcnt = wrapLines.length; li < lcnt; li++) {
			var line = wrapLines[li].children;
			var characterCount = 0;
			for (var ci = 0, ccnt = line.length; ci < ccnt; ci++) {
				var child = line[ci];
				if (child.active && !child.removed && IsChar(child)) characterCount++;
			}
			this.characterCountOfLines.push(characterCount);
		}
		return result;
	}
	setSize(width, height) {
		if (this.width === width && this.height === height) return this;
		super.setSize(width, height);
		this.runWrap();
		return this;
	}
	get displayText() {
		return this.text;
	}
	set displayText(value) {
		this.text = value;
	}
	setDisplayText(value) {
		this.displayText = value;
		return this;
	}
	get inputText() {
		return this.textEdit.text;
	}
	set inputText(value) {
		this.textEdit.text = value;
	}
	setInputText(value) {
		this.inputText = value;
		return this;
	}
	setParseTextCallback(callback) {
		if (!callback) callback = DefaultParseTextCallback;
		this.parseTextCallback = callback;
		return this;
	}
	get value() {
		return this.parseTextCallback(this.text);
	}
	set value(value) {
		this.setText(value);
	}
	getValue() {
		return this.value;
	}
	setValue(value) {
		this.value = value;
		return this;
	}
	updateEditor() {
		this.textEdit.updateText();
		return this;
	}
	get readOnly() {
		return this.textEdit.readOnly;
	}
	set readOnly(value) {
		this.textEdit.readOnly = value;
	}
	setReadOnly(value) {
		this.textEdit.setReadOnly(value);
		return this;
	}
	open(onCloseCallback) {
		if (onCloseCallback) this.textEdit.once("close", onCloseCallback);
		this.textEdit.open();
		return this;
	}
	close() {
		this.textEdit.close();
		return this;
	}
	get isOpened() {
		return this.textEdit.isOpened;
	}
	setFocusStyle(style) {
		this.focusStyle = style;
		return this;
	}
	setCursorStyle(style) {
		this.cursorStyle = style;
		return this;
	}
	setRangeStyle(style) {
		this.rangeStyle = style;
		return this;
	}
	setNumberInput() {
		this.textEdit.setNumberInput().setSelectAllWhenFocusEnable();
		this.parseTextCallback = Number;
		return this;
	}
	get maxLength() {
		return this.textEdit.maxLength;
	}
	set maxLength(value) {
		this.textEdit.maxLength = value;
	}
	setMaxLength(value) {
		this.maxLength = value;
		return this;
	}
	get minLength() {
		return this.textEdit.minLength;
	}
	set minLength(value) {
		this.textEdit.minLength = value;
	}
	setMinLength(value) {
		this.minLength = value;
		return this;
	}
	get cursorPosition() {
		return this.textEdit.cursorPosition;
	}
	set cursorPosition(value) {
		if (!this.isOpened) return;
		this.textEdit.cursorPosition = value;
		this.textEdit.requestCursorPosition = value;
	}
	setCursorPosition(value) {
		this.cursorPosition = value;
		return this;
	}
	get topTextOY() {
		return 0;
	}
	get bottomTextOY() {
		return -this.textVisibleHeight;
	}
	get leftTextOX() {
		return 0;
	}
	get rightTextOX() {
		return -this.textVisibleWidth;
	}
	get textVisibleHeight() {
		var h = this.contentHeight - this.height;
		if (h < 0) h = 0;
		return h;
	}
	get textVisibleWidth() {
		var w = this.contentWidth - this.width;
		if (w < 0) w = 0;
		return w;
	}
	set t(value) {
		this.setTextOYByPercentage(value).updateTexture();
	}
	get t() {
		return this.getTextOYPercentage();
	}
	set s(value) {
		this.setTextOXByPercentage(value).updateTexture();
	}
	get s() {
		return this.getTextOXPercentage();
	}
	updateFromEditor() {
		this.textEdit.updateDisplayText(this.rawText).updateCursor();
		return this;
	}
};
var DefaultParseTextCallback = function(text) {
	return text;
};
Object.assign(CanvasInput.prototype, SetTextOXYMethods_default, RawTextMethods_default, MoveCursorMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/canvasinput/CanvasInput.js
var CanvasInput_default = CanvasInput;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/canvasinput/Factory.js
ObjectFactory.register("canvasInput", function(x, y, fixedWidth, fixedHeight, config) {
	var gameObject = new CanvasInput_default(this.scene, x, y, fixedWidth, fixedHeight, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.CanvasInput", CanvasInput_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/hiddentextedit/HiddenTextEdit.js
var GetValue$229 = __webpack_exports__Utils.Objects.GetValue;
var Wrap$3 = __webpack_exports__Math.Wrap;
var HiddenTextEdit = class extends HiddenTextEditBase {
	constructor(gameObject, config) {
		if (config === void 0) config = {};
		if (config.onUpdate === "number") config.onUpdate = NumberInputUpdateCallback;
		super(gameObject, config);
		this.setCursor(GetValue$229(config, "cursor", "|"));
		this.setCursorFlashDuration(GetValue$229(config, "cursorFlashDuration", 1e3));
		this.cursorFlashTimer = 0;
	}
	initText() {
		this.cursorFlashTimer = 0;
		this.prevCursorPosition = void 0;
		this.setText(this.parent.text);
		this.setCursorPosition();
		return this;
	}
	updateText() {
		var textObject = this.parent;
		var text = this.text;
		if (this.onUpdateCallback) {
			var newText = this.onUpdateCallback(text, textObject, this);
			if (newText != null) text = newText;
		}
		if (this.isOpened && this.hasCursor) {
			var cursorPosition = this.cursorPosition;
			text = text.substring(0, cursorPosition) + this.cursor + text.substring(cursorPosition);
			if (this.prevCursorPosition !== cursorPosition) this.prevCursorPosition = cursorPosition;
		}
		if (textObject.text !== text) {
			textObject.setText(text);
			this.emit("textchange", text, textObject, this);
		}
		return this;
	}
	setCursor(s) {
		this._cursor = s;
		this.hasCursor = s && s !== "";
		return s;
	}
	setCursorFlashDuration(duration) {
		this.cursorFlashDuration = duration;
		return this;
	}
	get cursor() {
		if (!this._isFocused) return this._cursor;
		var cursor;
		if (this.cursorFlashTimer < this.cursorFlashDuration / 2) cursor = this._cursor;
		else cursor = " ";
		var timerValue = this.cursorFlashTimer + GetTickDelta(this.scene);
		this.cursorFlashTimer = Wrap$3(timerValue, 0, this.cursorFlashDuration);
		return cursor;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/hiddenedit/HiddenEdit.js
var HiddenEdit_default = HiddenTextEdit;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/hiddenedit/Factory.js
ObjectFactory.register("hiddenEdit", function(textObject, config) {
	return new HiddenEdit_default(textObject, config);
});
SetValue(window, "RexPlugins.UI.HiddenEdit", HiddenEdit_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/shapes/render/WebGLRenderer.js
var GetCalcMatrix$1 = __webpack_exports__GameObjects.GetCalcMatrix;
var WebGLRenderer$2 = function(renderer, src, drawingContext, parentMatrix) {
	src.updateData();
	var camera = drawingContext.camera;
	camera.addToRenderList(src);
	var calcMatrix = GetCalcMatrix$1(src, camera, parentMatrix, !drawingContext.useCanvas).calc;
	var dx = src._displayOriginX;
	var dy = src._displayOriginY;
	var alpha = src.alpha;
	var submitter = src.customRenderNodes.Submitter || src.defaultRenderNodes.Submitter;
	var shapes = src.geom, shape;
	for (var i = 0, cnt = shapes.length; i < cnt; i++) {
		shape = shapes[i];
		if (shape.visible) shape.webglRender(drawingContext, submitter, calcMatrix, src, alpha, dx, dy);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/shapes/render/CanvasRenderer.js
var SetTransform = __webpack_exports__Renderer.Canvas.SetTransform;
var CanvasRenderer$1 = function(renderer, src, camera, parentMatrix) {
	src.updateData();
	camera.addToRenderList(src);
	var ctx = renderer.currentContext;
	if (SetTransform(renderer, ctx, src, camera, parentMatrix)) {
		var dx = src._displayOriginX;
		var dy = src._displayOriginY;
		var shapes = src.geom, shape;
		for (var i = 0, cnt = shapes.length; i < cnt; i++) {
			shape = shapes[i];
			if (shape.visible) shape.canvasRender(ctx, dx, dy);
		}
		ctx.restore();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/shapes/render/Render.js
var Render_default$1 = {
	renderWebGL: WebGLRenderer$2,
	renderCanvas: CanvasRenderer$1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/shapes/BaseShapes.js
var Shape = __webpack_exports__GameObjects.Shape;
var RemoveItem$7 = __webpack_exports__Utils.Array.Remove;
var BaseShapes = class extends Shape {
	constructor(scene, x, y, width, height) {
		if (x === void 0) x = 0;
		if (y === void 0) y = 0;
		if (width === void 0) width = 2;
		if (height === void 0) height = width;
		super(scene, "rexShapes", []);
		this._width = -1;
		this._height = -1;
		this.dirty = true;
		this.isSizeChanged = true;
		this.shapes = {};
		this.setPosition(x, y);
		this.setSize(width, height);
		this.updateDisplayOrigin();
	}
	get width() {
		return this._width;
	}
	set width(value) {
		this.setSize(value, this._height);
	}
	get height() {
		return this._height;
	}
	set height(value) {
		this.setSize(this._width, value);
	}
	setDirty(value) {
		if (value === void 0) value = true;
		this.dirty = value;
		return this;
	}
	setSize(width, height) {
		this.isSizeChanged = this.isSizeChanged || this._width !== width || this._height !== height;
		this.dirty = this.dirty || this.isSizeChanged;
		this._width = width;
		this._height = height;
		this.updateDisplayOrigin();
		var input = this.input;
		if (input && !input.customHitArea) {
			input.hitArea.width = width;
			input.hitArea.height = height;
		}
		return this;
	}
	resize(width, height) {
		this.setSize(width, height);
		return this;
	}
	get fillColor() {
		return this._fillColor;
	}
	set fillColor(value) {
		this.setFillStyle(value, this._fillAlpha);
	}
	get fillAlpha() {
		return this._fillAlpha;
	}
	set fillAlpha(value) {
		this.setFillStyle(this._fillColor, value);
	}
	setFillStyle(color, alpha) {
		if (alpha === void 0) alpha = 1;
		this.dirty = this.dirty || this.fillColor !== color || this.fillAlpha !== alpha;
		this._fillColor = color;
		this._fillAlpha = alpha;
		return this;
	}
	get lineWidth() {
		return this._lineWidth;
	}
	set lineWidth(value) {
		this.setStrokeStyle(value, this._strokeColor, this._strokeAlpha);
	}
	get strokeColor() {
		return this._strokeColor;
	}
	set strokeColor(value) {
		this.setStrokeStyle(this._lineWidth, value, this._strokeAlpha);
	}
	get strokeAlpha() {
		return this._strokeAlpha;
	}
	set strokeAlpha(value) {
		this.setStrokeStyle(this._lineWidth, this._strokeColor, value);
	}
	setStrokeStyle(lineWidth, color, alpha) {
		if (alpha === void 0) alpha = 1;
		this.dirty = this.dirty || this.lineWidth !== lineWidth || this.strokeColor !== color || this.strokeAlpha !== alpha;
		this._lineWidth = lineWidth;
		this._strokeColor = color;
		this._strokeAlpha = alpha;
		return this;
	}
	updateShapes() {}
	updateData() {
		if (!this.dirty) return this;
		this.updateShapes();
		var shapes = this.geom;
		for (var i = 0, cnt = shapes.length; i < cnt; i++) {
			var shape = shapes[i];
			if (shape.dirty) shape.updateData();
		}
		this.isSizeChanged = false;
		this.dirty = false;
		return this;
	}
	clear() {
		this.geom.length = 0;
		Clear$1(this.shapes);
		this.dirty = true;
		return this;
	}
	getShape(name) {
		return this.shapes[name];
	}
	getShapes() {
		return this.geom;
	}
	addShape(shape) {
		this.geom.push(shape);
		var name = shape.name;
		if (name) this.shapes[name] = shape;
		this.dirty = true;
		return this;
	}
	deleteShape(name) {
		var shape = this.getShape(name);
		if (shape) {
			delete this.shapes[name];
			RemoveItem$7(this.geom, shape);
		}
		return this;
	}
};
Object.assign(BaseShapes.prototype, Render_default$1);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/checkbox/methods/StyleMethods.js
var StyleMethods_default$2 = {
	setBoxFillStyle(color, alpha) {
		if (alpha === void 0) alpha = 1;
		this.dirty = this.dirty || this.boxFillColor !== color || this.boxFillAlpha !== alpha;
		this.boxFillColor = color;
		this.boxFillAlpha = alpha;
		return this;
	},
	setUncheckedBoxFillStyle(color, alpha) {
		if (alpha === void 0) alpha = 1;
		this.dirty = this.dirty || this.uncheckedBoxFillColor !== color || this.uncheckedBoxFillAlpha !== alpha;
		this.uncheckedBoxFillColor = color;
		this.uncheckedBoxFillAlpha = alpha;
		return this;
	},
	setBoxStrokeStyle(lineWidth, color, alpha) {
		if (alpha === void 0) alpha = 1;
		this.dirty = this.dirty || this.boxLineWidth !== lineWidth || this.boxStrokeColor !== color || this.boxStrokeAlpha !== alpha;
		this.boxLineWidth = lineWidth;
		this.boxStrokeColor = color;
		this.boxStrokeAlpha = alpha;
		return this;
	},
	setUncheckedBoxStrokeStyle(lineWidth, color, alpha) {
		if (alpha === void 0) alpha = 1;
		this.dirty = this.dirty || this.uncheckedBoxLineWidth !== lineWidth || this.uncheckedBoxStrokeColor !== color || this.uncheckedBoxStrokeAlpha !== alpha;
		this.uncheckedBoxLineWidth = lineWidth;
		this.uncheckedBoxStrokeColor = color;
		this.uncheckedBoxStrokeAlpha = alpha;
		return this;
	},
	setCheckerStyle(color, alpha) {
		if (alpha === void 0) alpha = 1;
		this.dirty = this.dirty || this.checkerColor !== color || this.checkAlpha !== alpha;
		this.checkerColor = color;
		this.checkAlpha = alpha;
		return this;
	},
	setBoxShape(isCircleShape) {
		if (isCircleShape === void 0) isCircleShape = false;
		if (this.isCircleShape === isCircleShape) return this;
		this.isCircleShape = isCircleShape;
		this.isSizeChanged = true;
		this.dirty = true;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/checkbox/methods/SizeMethods.js
var SizeMethods_default$1 = {
	setBoxSize(size) {
		this.dirty = this.dirty || this.boxSize !== size;
		this.boxSize = size;
		return this;
	},
	setCheckerSize(size) {
		this.dirty = this.dirty || this.checkerSize !== size;
		this.checkerSize = size;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/shapes/geoms/base/StyleMethods.js
var FillStyle = function(color, alpha) {
	if (color == null) this.isFilled = false;
	else {
		if (alpha === void 0) alpha = 1;
		this.isFilled = true;
		this.fillColor = color;
		this.fillAlpha = alpha;
	}
	return this;
};
var LineStyle = function(lineWidth, color, alpha) {
	if (lineWidth == null || color == null) this.isStroked = false;
	else {
		if (alpha === void 0) alpha = 1;
		this.isStroked = true;
		this.lineWidth = lineWidth;
		this.strokeColor = color;
		this.strokeAlpha = alpha;
	}
	return this;
};
var StyleMethods_default$1 = {
	fillStyle: FillStyle,
	lineStyle: LineStyle
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/shapes/geoms/base/BaseGeom.js
var BaseGeom = class {
	constructor() {
		this.name = void 0;
		this.dirty = true;
		this.visible = true;
		this.data = void 0;
		this.isFilled = false;
		this.fillColor = void 0;
		this.fillAlpha = 1;
		this.isStroked = false;
		this.lineWidth = 1;
		this.strokeColor = void 0;
		this.strokeAlpha = 1;
	}
	setName(name) {
		this.name = name;
		return this;
	}
	setVisible(visible) {
		if (visible === void 0) visible = true;
		this.visible = visible;
		return this;
	}
	reset() {
		this.setVisible().fillStyle().lineStyle();
		return this;
	}
	webglRender(drawingContext, submitter, gameObject, calcMatrix, alpha, dx, dy) {}
	canvasRender(ctx, dx, dy) {}
	updateData() {
		this.dirty = false;
	}
};
Object.assign(BaseGeom.prototype, StyleMethods_default$1, DataMethods_default$2);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/shapes/geoms/lines/PathBase.js
var Earcut$1 = __webpack_exports__Geom.Polygon.Earcut;
var PathBase = class extends BaseGeom {
	constructor() {
		super();
		this.pathData = [];
		this.isDashed = false;
		this.strokePathData = void 0;
		this.strokePathMask = void 0;
		this.dashPattern = void 0;
		this.dashOffset = 0;
		this.pathIndexes = [];
		this.closePath = false;
	}
	updateData() {
		this.pathIndexes = Earcut$1(this.pathData);
		super.updateData();
		this.buildStrokePath();
		return this;
	}
	webglRender(drawingContext, submitter, calcMatrix, gameObject, alpha, dx, dy) {
		if (this.isFilled) FillPathWebGL(drawingContext, submitter, calcMatrix, gameObject, this, alpha, dx, dy);
		if (this.isStroked) StrokePathWebGL(drawingContext, submitter, calcMatrix, gameObject, this, alpha, dx, dy);
	}
	canvasRender(ctx, dx, dy) {
		if (this.isFilled) FillPathCanvas(ctx, this, dx, dy);
		if (this.isStroked) StrokePathCanvas(ctx, this, dx, dy);
	}
};
Object.assign(PathBase.prototype, Methods$24);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/shapes/geoms/lines/arc/Arc.js
__webpack_exports__Math.DegToRad;
var Arc = class extends PathBase {
	constructor(x, y, radiusX, radiusY, startAngle, endAngle, anticlockwise, pie) {
		if (x === void 0) x = 0;
		if (y === void 0) y = 0;
		if (radiusX === void 0) radiusX = 0;
		if (radiusY === void 0) radiusY = 0;
		if (startAngle === void 0) startAngle = 0;
		if (endAngle === void 0) endAngle = 360;
		if (anticlockwise === void 0) anticlockwise = false;
		if (pie === void 0) pie = false;
		super();
		this.setCenterPosition(x, y);
		this.setRadius(radiusX, radiusY);
		this.setAngle(startAngle, endAngle, anticlockwise);
		this.setPie(pie);
		this.setIterations(32);
	}
	get x() {
		return this._x;
	}
	set x(value) {
		this.dirty = this.dirty || this._x !== value;
		this._x = value;
	}
	get y() {
		return this._y;
	}
	set y(value) {
		this.dirty = this.dirty || this._y !== value;
		this._y = value;
	}
	setCenterPosition(x, y) {
		if (y === void 0) y = x;
		this.x = x;
		this.y = y;
		return this;
	}
	get radiusX() {
		return this._radiusX;
	}
	set radiusX(value) {
		this.dirty = this.dirty || this._radiusX !== value;
		this._radiusX = value;
	}
	get radiusY() {
		return this._radiusY;
	}
	set radiusY(value) {
		this.dirty = this.dirty || this._radiusY !== value;
		this._radiusY = value;
	}
	setRadius(radiusX, radiusY) {
		if (radiusY === void 0) radiusY = radiusX;
		this.radiusX = radiusX;
		this.radiusY = radiusY;
		return this;
	}
	get startAngle() {
		return this._startAngle;
	}
	set startAngle(value) {
		this.dirty = this.dirty || this._startAngle !== value;
		this._startAngle = value;
	}
	get endAngle() {
		return this._endAngle;
	}
	set endAngle(value) {
		this.dirty = this.dirty || this._endAngle !== value;
		this._endAngle = value;
	}
	get anticlockwise() {
		return this._anticlockwise;
	}
	set anticlockwise(value) {
		this.dirty = this.dirty || this._anticlockwise !== value;
		this._anticlockwise = value;
	}
	setAngle(startAngle, endAngle, anticlockwise) {
		if (anticlockwise === void 0) anticlockwise = false;
		this.startAngle = startAngle;
		this.endAngle = endAngle;
		this.anticlockwise = anticlockwise;
		return this;
	}
	get pie() {
		return this._pie;
	}
	set pie(value) {
		this.dirty = this.dirty || this._pie !== value;
		this._pie = value;
	}
	setPie(pie) {
		if (pie === void 0) pie = true;
		this.pie = pie;
		return this;
	}
	get iterations() {
		return this._iterations;
	}
	set iterations(value) {
		this.dirty = this.dirty || this._iterations !== value;
		this._iterations = value;
	}
	setIterations(iterations) {
		this.iterations = iterations;
		return this;
	}
	updateData() {
		this.pathData.length = 0;
		if (this.pie) this.pathData.push(this.x, this.y);
		ArcTo$1(this.x, this.y, this.radiusX, this.radiusY, this.startAngle, this.endAngle, this.anticlockwise, this.iterations, this.pathData);
		if (this.pie) this.pathData.push(this.x, this.y);
		this.pathData.push(this.pathData[0], this.pathData[1]);
		super.updateData();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/shapes/geoms/lines/arc/Circle.js
var Circle = class extends Arc {
	constructor(x, y, radius) {
		super(x, y, radius, radius, 0, 360);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/shapes/geoms/lines/Curve.js
var Curve = class extends PathBase {
	constructor(curve) {
		super();
		this.setCurve(curve);
		this.setIterations(32);
	}
	get curve() {
		return this._curve;
	}
	set curve(value) {
		this.dirty = this.dirty || this._curve !== value;
		this._curve = value;
	}
	setCurve(curve) {
		this.curve = curve;
		return this;
	}
	get iterations() {
		return this._iterations;
	}
	set iterations(value) {
		this.dirty = this.dirty || this._iterations !== value;
		this._iterations = value;
	}
	setIterations(iterations) {
		this.iterations = iterations;
		return this;
	}
	updateData() {
		this.pathData.length = 0;
		var points = this.curve.getPoints(this.iterations);
		for (var i = 0, cnt = points.length; i < cnt; i++) this.pathData.push(points[i].x, points[i].y);
		this.pathData.push(points[0].x, points[0].y);
		super.updateData();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/shapes/geoms/lines/arc/Ellipse.js
var Ellipse = class extends Arc {
	constructor(x, y, radiusX, radiusY) {
		super(x, y, radiusX, radiusY, 0, 360);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/shapes/geoms/lines/Line.js
var Line = class extends PathBase {
	constructor(x0, y0, x1, y1) {
		if (x0 === void 0) x0 = 0;
		if (y0 === void 0) y0 = 0;
		if (x1 === void 0) x1 = 0;
		if (y1 === void 0) y1 = 0;
		super();
		this.setP0(x0, y0);
		this.setP1(x1, y1);
	}
	get x0() {
		return this._x0;
	}
	set x0(value) {
		this.dirty = this.dirty || this._x0 !== value;
		this._x0 = value;
	}
	get y0() {
		return this._y0;
	}
	set y0(value) {
		this.dirty = this.dirty || this._y0 !== value;
		this._y0 = value;
	}
	setP0(x, y) {
		this.x0 = x;
		this.y0 = y;
		return this;
	}
	get x1() {
		return this._x1;
	}
	set x1(value) {
		this.dirty = this.dirty || this._x1 !== value;
		this._x1 = value;
	}
	get y1() {
		return this._y1;
	}
	set y1(value) {
		this.dirty = this.dirty || this._y1 !== value;
		this._y1 = value;
	}
	setP1(x, y) {
		this.x1 = x;
		this.y1 = y;
		return this;
	}
	updateData() {
		this.pathData.length = 0;
		this.pathData.push(this.x0, this.y0);
		this.pathData.push(this.x1, this.y1);
		this.pathData.push(this.x0, this.y0);
		super.updateData();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/geom/pathdata/StartAt.js
var StartAt = function(x, y, pathData) {
	pathData.length = 0;
	if (x != null) pathData.push(x, y);
	return pathData;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/geom/pathdata/QuadraticBezierTo.js
var QuadraticBezierInterpolation = __webpack_exports__Math.Interpolation.QuadraticBezier;
var QuadraticBezierTo = function(cx, cy, x, y, iterations, pathData) {
	var pathDataCnt = pathData.length;
	var p0x = pathData[pathDataCnt - 2];
	var p0y = pathData[pathDataCnt - 1];
	for (var i = 1, last = iterations - 1; i <= last; i++) {
		var t = i / last;
		pathData.push(QuadraticBezierInterpolation(t, p0x, cx, x), QuadraticBezierInterpolation(t, p0y, cy, y));
	}
	return pathData;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/geom/pathdata/CubicBezierCurveTo.js
var CubicBezierInterpolation = __webpack_exports__Math.Interpolation.CubicBezier;
var CubicBezierCurveTo = function(cx0, cy0, cx1, cy1, x, y, iterations, pathData) {
	var pathDataCnt = pathData.length;
	var p0x = pathData[pathDataCnt - 2];
	var p0y = pathData[pathDataCnt - 1];
	for (var i = 1, last = iterations - 1; i <= last; i++) {
		var t = i / last;
		pathData.push(CubicBezierInterpolation(t, p0x, cx0, cx1, x), CubicBezierInterpolation(t, p0y, cy0, cy1, y));
	}
	return pathData;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/geom/pathdata/CatmullRomTo.js
var CatmullRomInterpolation = __webpack_exports__Math.Interpolation.CatmullRom;
var CatmullRomTo = function(points, iterations, pathData) {
	var pathDataCnt = pathData.length;
	var p0x = pathData[pathDataCnt - 2];
	var p0y = pathData[pathDataCnt - 1];
	var xList = [p0x];
	var yList = [p0y];
	for (var i = 0, cnt = points.length; i < cnt; i += 2) {
		xList.push(points[i]);
		yList.push(points[i + 1]);
	}
	for (var i = 1, last = iterations - 1; i <= last; i++) {
		var t = i / last;
		pathData.push(CatmullRomInterpolation(xList, t), CatmullRomInterpolation(yList, t));
	}
	return pathData;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/geom/pathdata/DuplicateLast.js
var DuplicateLast = function(pathData) {
	var len = pathData.length;
	if (len < 2) return pathData;
	var lastX = pathData[len - 2];
	var lastY = pathData[len - 1];
	pathData.push(lastX);
	pathData.push(lastY);
	return pathData;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/geom/pathdata/PathDataBuilder/AddPathMethods.js
var ControlTypeQuadratic = "quadratic";
var ControlTypeCubic = "cubic";
var WarnPathTypeMismatch = function(methodName, expectedControlType) {
	if (!this.pathTypeMismatchWarningEnable) return;
	if (typeof console === "undefined" || !console.warn) return;
	console.warn(methodName + " path type mismatch: expected previous control type to be " + expectedControlType + ", got " + (this.lastControlType || "none") + ". Falling back to current point as control point.");
};
var AddPathMethods_default = {
	clear() {
		this.start();
		return this;
	},
	start() {
		this.startAt();
		return this;
	},
	startAt(x, y) {
		this.restorePathData();
		this.accumulationLengths = void 0;
		StartAt(x, y, this.pathData);
		this.firstPointX = x;
		this.firstPointY = y;
		this.lastPointX = x;
		this.lastPointY = y;
		this.resetControlPoint();
		return this;
	},
	lineTo(x, y, relative) {
		if (relative === void 0) relative = false;
		if (relative) {
			x += this.lastPointX;
			y += this.lastPointY;
		}
		LineTo(x, y, this.pathData);
		this.lastPointX = x;
		this.lastPointY = y;
		this.resetControlPoint();
		return this;
	},
	verticalLineTo(x, relative) {
		this.lineTo(x, this.lastPointY, relative);
		return this;
	},
	horizontalLineTo(y, relative) {
		this.lineTo(this.lastPointX, y, relative);
		return this;
	},
	ellipticalArc(centerX, centerY, radiusX, radiusY, startAngle, endAngle, anticlockwise) {
		if (anticlockwise === void 0) anticlockwise = false;
		ArcTo$1(centerX, centerY, radiusX, radiusY, startAngle, endAngle, anticlockwise, this.iterations, this.pathData);
		this.lastPointX = this.pathData[this.pathData.length - 2];
		this.lastPointY = this.pathData[this.pathData.length - 1];
		this.resetControlPoint();
		return this;
	},
	arc(centerX, centerY, radius, startAngle, endAngle, anticlockwise) {
		this.ellipticalArc(centerX, centerY, radius, radius, startAngle, endAngle, anticlockwise);
		return this;
	},
	quadraticBezierTo(cx, cy, x, y) {
		QuadraticBezierTo(cx, cy, x, y, this.iterations, this.pathData);
		this.lastPointX = x;
		this.lastPointY = y;
		this.lastCX = cx;
		this.lastCY = cy;
		this.lastControlType = ControlTypeQuadratic;
		return this;
	},
	smoothQuadraticBezierTo(x, y) {
		var cx, cy;
		if (this.lastControlType === ControlTypeQuadratic) {
			cx = this.lastPointX * 2 - this.lastCX;
			cy = this.lastPointY * 2 - this.lastCY;
		} else {
			WarnPathTypeMismatch.call(this, "smoothQuadraticBezierTo()", ControlTypeQuadratic);
			cx = this.lastPointX;
			cy = this.lastPointY;
		}
		this.quadraticBezierTo(cx, cy, x, y);
		return this;
	},
	cubicBezierTo(cx0, cy0, cx1, cy1, x, y) {
		CubicBezierCurveTo(cx0, cy0, cx1, cy1, x, y, this.iterations, this.pathData);
		this.lastPointX = x;
		this.lastPointY = y;
		this.lastCX = cx1;
		this.lastCY = cy1;
		this.lastControlType = ControlTypeCubic;
		return this;
	},
	smoothCubicBezierTo(cx1, cy1, x, y) {
		var cx0, cy0;
		if (this.lastControlType === ControlTypeCubic) {
			cx0 = this.lastPointX * 2 - this.lastCX;
			cy0 = this.lastPointY * 2 - this.lastCY;
		} else {
			WarnPathTypeMismatch.call(this, "smoothCubicBezierTo()", ControlTypeCubic);
			cx0 = this.lastPointX;
			cy0 = this.lastPointY;
		}
		this.cubicBezierTo(cx0, cy0, cx1, cy1, x, y);
		return this;
	},
	catmullRomTo(...points) {
		CatmullRomTo(points, this.iterations, this.pathData);
		this.lastPointX = points[points.length - 2];
		this.lastPointY = points[points.length - 1];
		this.resetControlPoint();
		return this;
	},
	close() {
		var startX = this.pathData[0], startY = this.pathData[1];
		if (startX !== this.lastPointX || startY !== this.lastPointY) this.lineTo(startX, startY);
		this.closePath = true;
		this.resetControlPoint();
		return this;
	},
	end() {
		DuplicateLast(this.pathData);
		this.resetControlPoint();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/geom/pathdata/RotateAround.js
var PointRotateAround$1 = __webpack_exports__Math.RotateAround;
var RotateAround$5 = function(centerX, centerY, angle, pathData) {
	var point = {
		x: 0,
		y: 0
	};
	for (var i = 0, cnt = pathData.length - 1; i < cnt; i += 2) {
		point.x = pathData[i];
		point.y = pathData[i + 1];
		PointRotateAround$1(point, centerX, centerY, angle);
		pathData[i] = point.x;
		pathData[i + 1] = point.y;
	}
	return pathData;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/geom/pathdata/Scale.js
var Scale$1 = function(centerX, centerY, scaleX, scaleY, pathData) {
	for (var i = 0, cnt = pathData.length - 1; i < cnt; i += 2) {
		var x = pathData[i] - centerX;
		var y = pathData[i + 1] - centerY;
		x *= scaleX;
		y *= scaleY;
		pathData[i] = x + centerX;
		pathData[i + 1] = y + centerY;
	}
	return pathData;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/geom/pathdata/Offset.js
var Offset = function(x, y, pathData) {
	for (var i = 0, cnt = pathData.length - 1; i < cnt; i += 2) {
		pathData[i] += x;
		pathData[i + 1] += y;
	}
	return pathData;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/geom/pathdata/PathDataBuilder/TransformPointsMethods.js
var DegToRad$12 = __webpack_exports__Math.DegToRad;
var PointRotateAround = __webpack_exports__Math.RotateAround;
var TransformPointsMethods_default = {
	rotateAround(centerX, centerY, angle) {
		if (this.pathData.length === 0) return this;
		angle = DegToRad$12(angle);
		RotateAround$5(centerX, centerY, angle, this.pathData);
		var pathDataCnt = this.pathData.length;
		this.lastPointX = this.pathData[pathDataCnt - 2];
		this.lastPointY = this.pathData[pathDataCnt - 1];
		if (this.lastCX !== void 0) {
			var point = {
				x: this.lastCX,
				y: this.lastCY
			};
			PointRotateAround(point, centerX, centerY, angle);
			this.lastCX = point.x;
			this.lastCY = point.y;
		}
		return this;
	},
	scale(centerX, centerY, scaleX, scaleY) {
		if (this.pathData.length === 0) return this;
		Scale$1(centerX, centerY, scaleX, scaleY, this.pathData);
		var pathDataCnt = this.pathData.length;
		this.lastPointX = this.pathData[pathDataCnt - 2];
		this.lastPointY = this.pathData[pathDataCnt - 1];
		if (this.lastCX !== void 0) {
			this.lastCX = (this.lastCX - centerX) * scaleX + centerX;
			this.lastCY = (this.lastCY - centerY) * scaleY + centerY;
		}
		return this;
	},
	offset(x, y) {
		if (this.pathData.length === 0) return this;
		Offset(x, y, this.pathData);
		var pathDataCnt = this.pathData.length;
		this.lastPointX = this.pathData[pathDataCnt - 2];
		this.lastPointY = this.pathData[pathDataCnt - 1];
		if (this.lastCX !== void 0) {
			this.lastCX += x;
			this.lastCY += y;
		}
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/array/Copy.js
var Copy = function(dest, src, startIdx, endIdx) {
	if (startIdx === void 0) startIdx = 0;
	if (endIdx === void 0) endIdx = src.length;
	dest.length = endIdx - startIdx;
	for (var i = 0, len = dest.length; i < len; i++) dest[i] = src[i + startIdx];
	return dest;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/geom/pathdata/PathDataBuilder/SavePathDataMethods.js
var SavePathDataMethods_default = {
	savePathData() {
		if (this.pathDataSaved) return this;
		this.pathDataSave = [...this.pathData];
		this.pathData.length = 0;
		this.pathDataSaved = true;
		return this;
	},
	restorePathData() {
		if (!this.pathDataSaved) return this;
		Copy(this.pathData, this.pathDataSave);
		this.pathDataSave = void 0;
		this.pathDataSaved = false;
		this.firstPointX = this.pathData[0];
		this.firstPointY = this.pathData[1];
		this.lastPointX = this.pathData[this.pathData.length - 2];
		this.lastPointY = this.pathData[this.pathData.length - 1];
		this.resetControlPoint();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/geom/pathdata/PathDataBuilder/PathSegmentMethods.js
var DistanceBetween$7 = __webpack_exports__Math.Distance.Between;
var Wrap$2 = __webpack_exports__Math.Wrap;
var Linear$25 = __webpack_exports__Math.Linear;
var AppendFromPathSegment = function(srcPathData, accumulationLengths, startT, endT, destPathData) {
	if (endT === void 0) {
		endT = startT;
		startT = 0;
	}
	startT = WrapT(startT);
	endT = WrapT(endT);
	if (startT === endT) return;
	var totalPathLength = accumulationLengths[accumulationLengths.length - 1];
	var startL = totalPathLength * startT;
	var endL = totalPathLength * endT;
	if (startT < endT) AddPathSegment(srcPathData, accumulationLengths, startL, endL, destPathData);
	else {
		AddPathSegment(srcPathData, accumulationLengths, startL, totalPathLength, destPathData);
		AddPathSegment(srcPathData, accumulationLengths, 0, endL, destPathData);
	}
	DuplicateLast(destPathData);
};
var AddPathSegment = function(srcPathData, accumulationLengths, startL, endL, destPathData) {
	var skipState = startL > 0;
	for (var i = 0, cnt = accumulationLengths.length; i < cnt; i++) {
		var pIdx = i * 2;
		var d = accumulationLengths[i];
		if (skipState) {
			if (d < startL) continue;
			else if (d == startL) skipState = false;
			else {
				var deltaD = d - accumulationLengths[i - 1];
				var t = 1 - (d - startL) / deltaD;
				destPathData.push(GetInterpolation(srcPathData, pIdx - 2, pIdx, t));
				destPathData.push(GetInterpolation(srcPathData, pIdx - 1, pIdx + 1, t));
				skipState = false;
			}
		}
		if (d <= endL) {
			destPathData.push(srcPathData[pIdx]);
			destPathData.push(srcPathData[pIdx + 1]);
			if (d === endL) break;
		} else {
			var deltaD = d - accumulationLengths[i - 1];
			var t = 1 - (d - endL) / deltaD;
			destPathData.push(GetInterpolation(srcPathData, pIdx - 2, pIdx, t));
			destPathData.push(GetInterpolation(srcPathData, pIdx - 1, pIdx + 1, t));
			break;
		}
	}
};
var GetInterpolation = function(pathData, i0, i1, t) {
	var p0 = pathData[i0], p1 = pathData[i1];
	return Linear$25(p0, p1, t);
};
var WrapT = function(t) {
	if (t === 0) return 0;
	else if (t % 1 === 0) return 1;
	return Wrap$2(t, 0, 1);
};
var PathSegmentMethods_default = {
	updateAccumulationLengths() {
		if (this.accumulationLengths == null) this.accumulationLengths = [];
		else if (this.accumulationLengths.length === this.pathData.length / 2) return this;
		var accumulationLengths = this.accumulationLengths;
		var pathData = this.pathData;
		var prevX, prevY, x, y;
		var d, accumulationLength = 0;
		for (var i = 0, cnt = pathData.length; i < cnt; i += 2) {
			x = pathData[i];
			y = pathData[i + 1];
			d = prevX === void 0 ? 0 : DistanceBetween$7(prevX, prevY, x, y);
			accumulationLength += d;
			accumulationLengths.push(accumulationLength);
			prevX = x;
			prevY = y;
		}
		this.totalPathLength = accumulationLength;
		return this;
	},
	setDisplayPathSegment(startT, endT) {
		if (!this.pathDataSaved) {
			this.updateAccumulationLengths();
			this.savePathData();
		}
		this.pathData.length = 0;
		AppendFromPathSegment(this.pathDataSave, this.accumulationLengths, startT, endT, this.pathData);
		this.firstPointX = this.pathData[0];
		this.firstPointY = this.pathData[1];
		this.lastPointX = this.pathData[this.pathData.length - 2];
		this.lastPointY = this.pathData[this.pathData.length - 1];
		this.resetControlPoint();
		return this;
	},
	appendFromPathSegment(src, startT, endT) {
		if (startT === void 0) this.pathData.push(...src.pathData);
		else {
			src.updateAccumulationLengths();
			AppendFromPathSegment(src.pathData, src.accumulationLengths, startT, endT, this.pathData);
		}
		this.firstPointX = this.pathData[0];
		this.firstPointY = this.pathData[1];
		this.lastPointX = this.pathData[this.pathData.length - 2];
		this.lastPointY = this.pathData[this.pathData.length - 1];
		this.resetControlPoint();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/geom/pathdata/PathDataBuilder/GraphicsMethods.js
var GraphicsMethods_default = { draw(graphics, isFill, isStroke) {
	var points = this.toPoints();
	if (isFill) graphics.fillPoints(points, this.closePath, this.closePath);
	if (isStroke) graphics.strokePoints(points, this.closePath, this.closePath);
	return this;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/geom/pathdata/ToPoints.js
var ToPoints = function(pathData, points) {
	if (points === void 0) points = [];
	for (var i = 0, cnt = pathData.length - 1; i < cnt; i += 2) points.push({
		x: pathData[i],
		y: pathData[i + 1]
	});
	return points;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/geom/pathdata/ToPolygon.js
var Polygon = __webpack_exports__Geom.Polygon;
var ToPolygon = function(pathData, polygon) {
	if (polygon === void 0) polygon = new Polygon();
	polygon.setTo(pathData);
	return polygon;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/geom/pathdata/PathDataBuilder/PathDataBuilder.js
var PathDataBuilder = class {
	constructor(pathData) {
		if (pathData === void 0) pathData = [];
		this.pathData = pathData;
		this.closePath = false;
		this.setIterations(32);
		this.firstPointX = void 0;
		this.firstPointY = void 0;
		this.lastPointX = void 0;
		this.lastPointY = void 0;
		this.lastCX = void 0;
		this.lastCY = void 0;
		this.lastControlType = void 0;
		this.pathTypeMismatchWarningEnable = true;
		this.accumulationLengths = void 0;
	}
	setIterations(iterations) {
		this.iterations = iterations;
		return this;
	}
	setPathTypeMismatchWarningEnable(enable) {
		if (enable === void 0) enable = true;
		this.pathTypeMismatchWarningEnable = enable;
		return this;
	}
	resetControlPoint() {
		this.lastCX = this.lastPointX;
		this.lastCY = this.lastPointY;
		this.lastControlType = void 0;
		return this;
	}
	toPoints() {
		return ToPoints(this.pathData);
	}
	toPolygon(polygon) {
		return ToPolygon(this.pathData, polygon);
	}
};
Object.assign(PathDataBuilder.prototype, AddPathMethods_default, TransformPointsMethods_default, SavePathDataMethods_default, PathSegmentMethods_default, GraphicsMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/shapes/geoms/lines/Lines.js
var Lines = class extends PathBase {
	constructor() {
		super();
		this.builder = new PathDataBuilder(this.pathData);
	}
	get iterations() {
		return this.builder.iterations;
	}
	set iterations(value) {
		this.dirty = this.dirty || this.builder.iterations !== value;
		this.builder.setIterations(value);
	}
	setIterations(iterations) {
		this.iterations = iterations;
		return this;
	}
	setPathTypeMismatchWarningEnable(enable) {
		this.builder.setPathTypeMismatchWarningEnable(enable);
		return this;
	}
	get lastPointX() {
		return this.builder.lastPointX;
	}
	get lastPointY() {
		return this.builder.lastPointY;
	}
	start() {
		this.builder.start();
		this.dirty = true;
		return this;
	}
	startAt(x, y) {
		this.builder.startAt(x, y);
		this.dirty = true;
		return this;
	}
	lineTo(x, y, relative) {
		this.builder.lineTo(x, y, relative);
		this.dirty = true;
		return this;
	}
	verticalLineTo(x, relative) {
		this.builder.verticalLineTo(x, relative);
		this.dirty = true;
		return this;
	}
	horizontalLineTo(y, relative) {
		this.builder.horizontalLineTo(y, relative);
		this.dirty = true;
		return this;
	}
	ellipticalArc(centerX, centerY, radiusX, radiusY, startAngle, endAngle, anticlockwise) {
		this.builder.ellipticalArc(centerX, centerY, radiusX, radiusY, startAngle, endAngle, anticlockwise);
		this.dirty = true;
		return this;
	}
	arc(centerX, centerY, radius, startAngle, endAngle, anticlockwise) {
		this.builder.arc(centerX, centerY, radius, startAngle, endAngle, anticlockwise);
		this.dirty = true;
		return this;
	}
	quadraticBezierTo(cx, cy, x, y) {
		this.builder.quadraticBezierTo(cx, cy, x, y);
		this.dirty = true;
		return this;
	}
	smoothQuadraticBezierTo(x, y) {
		this.builder.smoothQuadraticBezierTo(x, y);
		this.dirty = true;
		return this;
	}
	cubicBezierTo(cx0, cy0, cx1, cy1, x, y) {
		this.builder.cubicBezierTo(cx0, cy0, cx1, cy1, x, y);
		this.dirty = true;
		return this;
	}
	smoothCubicBezierTo(cx1, cy1, x, y) {
		this.builder.smoothCubicBezierTo(cx1, cy1, x, y);
		this.dirty = true;
		return this;
	}
	catmullRomTo(...points) {
		this.builder.catmullRomTo(...points);
		this.dirty = true;
		return this;
	}
	close() {
		this.builder.close();
		this.closePath = this.builder.closePath;
		this.dirty = true;
		return this;
	}
	end() {
		this.builder.end();
		this.dirty = true;
		return this;
	}
	rotateAround(centerX, centerY, angle) {
		this.builder.rotateAround(centerX, centerY, angle);
		this.dirty = true;
		return this;
	}
	scale(centerX, centerY, scaleX, scaleY) {
		this.builder.scale(centerX, centerY, scaleX, scaleY);
		this.dirty = true;
		return this;
	}
	offset(x, y) {
		this.builder.offset(x, y);
		this.dirty = true;
		return this;
	}
	toPolygon(polygon) {
		return this.builder.toPolygon(polygon);
	}
	appendPathFrom(src, startT, endT) {
		this.builder.appendFromPathSegment(src.builder, startT, endT);
		return this;
	}
	copyPathFrom(src, startT, endT) {
		this.builder.clear().appendFromPathSegment(src.builder, startT, endT);
		return this;
	}
	setDisplayPathSegment(startT, endT) {
		this.builder.setDisplayPathSegment(startT, endT);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/shapes/geoms/rectangle/Rectangle.js
var Utils = __webpack_exports__Renderer.WebGL.Utils;
var Rectangle$2 = class extends BaseGeom {
	constructor(x, y, width, height) {
		if (x === void 0) x = 0;
		if (y === void 0) y = 0;
		if (width === void 0) width = 0;
		if (height === void 0) height = width;
		super();
		this.pathData = [];
		this.isDashed = false;
		this.strokePathData = void 0;
		this.strokePathMask = void 0;
		this.dashPattern = void 0;
		this.dashOffset = 0;
		this.closePath = true;
		this.setTopLeftPosition(x, y);
		this.setSize(width, height);
	}
	get x() {
		return this._x;
	}
	set x(value) {
		this.dirty = this.dirty || this._x !== value;
		this._x = value;
	}
	get y() {
		return this._y;
	}
	set y(value) {
		this.dirty = this.dirty || this._y !== value;
		this._y = value;
	}
	setTopLeftPosition(x, y) {
		this.x = x;
		this.y = y;
		return this;
	}
	get width() {
		return this._width;
	}
	set width(value) {
		this.dirty = this.dirty || this._width !== value;
		this._width = value;
	}
	get height() {
		return this._height;
	}
	set height(value) {
		this.dirty = this.dirty || this._height !== value;
		this._height = value;
	}
	setSize(width, height) {
		this.width = width;
		this.height = height;
		return this;
	}
	get centerX() {
		return this.x + this.width / 2;
	}
	set centerX(value) {
		this.x = value - this.width / 2;
	}
	get centerY() {
		return this.y + this.height / 2;
	}
	set centerY(value) {
		this.y = value - this.height / 2;
	}
	setCenterPosition(x, y) {
		this.centerX = x;
		this.centerY = y;
		return this;
	}
	updateData() {
		this.pathData.length = 0;
		var x0 = this.x, x1 = x0 + this.width, y0 = this.y, y1 = y0 + this.height;
		this.pathData.push(x0, y0);
		this.pathData.push(x1, y0);
		this.pathData.push(x1, y1);
		this.pathData.push(x0, y1);
		this.pathData.push(x0, y0);
		super.updateData();
		this.buildStrokePath();
		return this;
	}
	webglRender(drawingContext, submitter, calcMatrix, gameObject, alpha, dx, dy) {
		if (this.isFilled) {
			var fillTintColor = Utils.getTintAppendFloatAlpha(this.fillColor, this.fillAlpha * alpha);
			(gameObject.customRenderNodes.FillRect || gameObject.defaultRenderNodes.FillRect).run(drawingContext, calcMatrix, submitter, -dx + this.x, -dy + this.y, this.width, this.height, fillTintColor, fillTintColor, fillTintColor, fillTintColor);
		}
		if (this.isStroked) StrokePathWebGL(drawingContext, submitter, calcMatrix, gameObject, this, alpha, dx, dy);
	}
	canvasRender(ctx, dx, dy) {
		if (this.isFilled) {
			FillStyleCanvas(ctx, this);
			ctx.fillRect(-dx, -dy, this.width, this.height);
		}
		if (this.isStroked) StrokePathCanvas(ctx, this, dx, dy);
	}
};
Object.assign(Rectangle$2.prototype, Methods$24);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/shapes/geoms/lines/roundrectangle/RoundRectangle.js
var GetValue$228 = __webpack_exports__Utils.Objects.GetValue;
var RoundRectangle = class extends PathBase {
	constructor(x, y, width, height, radius, iterations) {
		if (x === void 0) x = 0;
		if (y === void 0) y = 0;
		if (width === void 0) width = 0;
		if (height === void 0) height = width;
		if (radius === void 0) radius = 0;
		if (iterations === void 0) iterations = 6;
		super();
		this.setTopLeftPosition(x, y);
		this.setSize(width, height);
		this.setRadius(radius);
		this.setIterations(iterations);
		this.closePath = true;
	}
	get x() {
		return this._x;
	}
	set x(value) {
		this.dirty = this.dirty || this._x !== value;
		this._x = value;
	}
	get y() {
		return this._y;
	}
	set y(value) {
		this.dirty = this.dirty || this._y !== value;
		this._y = value;
	}
	setTopLeftPosition(x, y) {
		this.x = x;
		this.y = y;
		return this;
	}
	get width() {
		return this._width;
	}
	set width(value) {
		this.dirty = this.dirty || this._width !== value;
		this._width = value;
	}
	get height() {
		return this._height;
	}
	set height(value) {
		this.dirty = this.dirty || this._height !== value;
		this._height = value;
	}
	setSize(width, height) {
		this.width = width;
		this.height = height;
		return this;
	}
	get centerX() {
		return this.x + this.width / 2;
	}
	set centerX(value) {
		this.x = value - this.width / 2;
	}
	get centerY() {
		return this.y + this.height / 2;
	}
	set centerY(value) {
		this.y = value - this.height / 2;
	}
	setCenterPosition(x, y) {
		this.centerX = x;
		this.centerY = y;
		return this;
	}
	get radiusTL() {
		return this._radiusTL;
	}
	set radiusTL(value) {
		var isConvex = value > 0;
		this.dirty = this.dirty || this._radiusTL !== value || this._convexTL !== isConvex;
		this._convexTL = isConvex;
		this._radiusTL = Math.abs(value);
	}
	get radiusTR() {
		return this._radiusTR;
	}
	set radiusTR(value) {
		var isConvex = value > 0;
		this.dirty = this.dirty || this._radiusTR !== value || this._convexTR !== isConvex;
		this._convexTR = isConvex;
		this._radiusTR = Math.abs(value);
	}
	get radiusBL() {
		return this._radiusBL;
	}
	set radiusBL(value) {
		var isConvex = value > 0;
		this.dirty = this.dirty || this._radiusBL !== value || this._convexBL !== isConvex;
		this._convexBL = isConvex;
		this._radiusBL = Math.abs(value);
	}
	get radiusBR() {
		return this._radiusBR;
	}
	set radiusBR(value) {
		var isConvex = value > 0;
		this.dirty = this.dirty || this._radiusBR !== value || this._convexBR !== isConvex;
		this._convexBR = isConvex;
		this._radiusBR = Math.abs(value);
	}
	get radius() {
		return Math.max(this.radiusTL, this.radiusTR, this.radiusBL, this.radiusBR);
	}
	set radius(value) {
		if (typeof value === "number") {
			this.radiusTL = value;
			this.radiusTR = value;
			this.radiusBL = value;
			this.radiusBR = value;
		} else {
			this.radiusTL = GetValue$228(value, "tl", 0);
			this.radiusTR = GetValue$228(value, "tr", 0);
			this.radiusBL = GetValue$228(value, "bl", 0);
			this.radiusBR = GetValue$228(value, "br", 0);
		}
	}
	setRadius(radius) {
		if (radius === void 0) radius = 0;
		this.radius = radius;
		return this;
	}
	get iterations() {
		return this._iterations;
	}
	set iterations(value) {
		this.dirty = this.dirty || this._iterations !== value;
		this._iterations = value;
	}
	setIterations(iterations) {
		this.iterations = iterations;
		return this;
	}
	updateData() {
		var pathData = this.pathData;
		pathData.length = 0;
		var width = this.width, height = this.height, radius, iterations = this.iterations + 1;
		radius = this.radiusTL;
		if (radius > 0) {
			if (this._convexTL) {
				var centerX = radius;
				var centerY = radius;
				ArcTo$1(centerX, centerY, radius, radius, 180, 270, false, iterations, pathData);
			} else {
				var centerX = 0;
				var centerY = 0;
				ArcTo$1(centerX, centerY, radius, radius, 90, 0, true, iterations, pathData);
			}
		} else LineTo(0, 0, pathData);
		radius = this.radiusTR;
		if (radius > 0) {
			if (this._convexTR) {
				var centerX = width - radius;
				var centerY = radius;
				ArcTo$1(centerX, centerY, radius, radius, 270, 360, false, iterations, pathData);
			} else {
				var centerX = width;
				var centerY = 0;
				ArcTo$1(centerX, centerY, radius, radius, 180, 90, true, iterations, pathData);
			}
		} else LineTo(width, 0, pathData);
		radius = this.radiusBR;
		if (radius > 0) {
			if (this._convexBR) {
				var centerX = width - radius;
				var centerY = height - radius;
				ArcTo$1(centerX, centerY, radius, radius, 0, 90, false, iterations, pathData);
			} else {
				var centerX = width;
				var centerY = height;
				ArcTo$1(centerX, centerY, radius, radius, 270, 180, true, iterations, pathData);
			}
		} else LineTo(width, height, pathData);
		radius = this.radiusBL;
		if (radius > 0) {
			if (this._convexBL) {
				var centerX = radius;
				var centerY = height - radius;
				ArcTo$1(centerX, centerY, radius, radius, 90, 180, false, iterations, pathData);
			} else {
				var centerX = 0;
				var centerY = height;
				ArcTo$1(centerX, centerY, radius, radius, 360, 270, true, iterations, pathData);
			}
		} else LineTo(0, height, pathData);
		pathData.push(pathData[0], pathData[1]);
		Offset(this.x, this.y, pathData);
		super.updateData();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/shapes/geoms/triangle/Triangle.js
var Earcut = __webpack_exports__Geom.Polygon.Earcut;
var Triangle$1 = class extends BaseGeom {
	constructor(x0, y0, x1, y1, x2, y2) {
		if (x0 === void 0) x0 = 0;
		if (y0 === void 0) y0 = 0;
		if (x1 === void 0) x1 = 0;
		if (y1 === void 0) y1 = 0;
		if (x2 === void 0) x2 = 0;
		if (y2 === void 0) y2 = 0;
		super();
		this.pathData = [];
		this.isDashed = false;
		this.strokePathData = void 0;
		this.strokePathMask = void 0;
		this.dashPattern = void 0;
		this.dashOffset = 0;
		this.pathIndexes = [];
		this.closePath = true;
		this.setP0(x0, y0);
		this.setP1(x1, y1);
		this.setP2(x2, y2);
	}
	get x0() {
		return this._x0;
	}
	set x0(value) {
		this.dirty = this.dirty || this._x0 !== value;
		this._x0 = value;
	}
	get y0() {
		return this._y0;
	}
	set y0(value) {
		this.dirty = this.dirty || this._y0 !== value;
		this._y0 = value;
	}
	setP0(x, y) {
		this.x0 = x;
		this.y0 = y;
		return this;
	}
	get x1() {
		return this._x1;
	}
	set x1(value) {
		this.dirty = this.dirty || this._x1 !== value;
		this._x1 = value;
	}
	get y1() {
		return this._y1;
	}
	set y1(value) {
		this.dirty = this.dirty || this._y1 !== value;
		this._y1 = value;
	}
	setP1(x, y) {
		this.x1 = x;
		this.y1 = y;
		return this;
	}
	get x2() {
		return this._x2;
	}
	set x2(value) {
		this.dirty = this.dirty || this._x2 !== value;
		this._x2 = value;
	}
	get y2() {
		return this._y2;
	}
	set y2(value) {
		this.dirty = this.dirty || this._y2 !== value;
		this._y2 = value;
	}
	setP2(x, y) {
		this.dirty = this.dirty || this.x2 !== x || this.y2 !== y;
		this.x2 = x;
		this.y2 = y;
		return this;
	}
	updateData() {
		this.pathData.length = 0;
		this.pathData.push(this.x0, this.y0);
		this.pathData.push(this.x1, this.y1);
		this.pathData.push(this.x2, this.y2);
		this.pathData.push(this.x0, this.y0);
		this.pathIndexes = Earcut(this.pathData);
		super.updateData();
		this.buildStrokePath();
		return this;
	}
	webglRender(drawingContext, submitter, calcMatrix, gameObject, alpha, dx, dy) {
		if (this.isFilled) FillPathWebGL(drawingContext, submitter, calcMatrix, gameObject, this, alpha, dx, dy);
		if (this.isStroked) StrokePathWebGL(drawingContext, submitter, calcMatrix, gameObject, this, alpha, dx, dy);
	}
	canvasRender(ctx, dx, dy) {
		if (this.isFilled) FillPathCanvas(ctx, this, dx, dy);
		if (this.isStroked) StrokePathCanvas(ctx, this, dx, dy);
	}
};
Object.assign(Triangle$1.prototype, Methods$24);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/checkbox/methods/ShapesUpdateMethods.js
var ShapesUpdateMethods_default$4 = {
	buildShapes() {
		this.addShape(new RoundRectangle().setName("box")).addShape(new Lines().setName("checker"));
	},
	updateShapes() {
		var centerX = this.width / 2, centerY = this.height / 2, radius = Math.min(centerX, centerY);
		var width = radius * 2;
		var x = centerX - radius, y = centerY - radius;
		var boxLineWidth = this.boxLineWidth;
		var checkLineWidth = Math.max(width / 10, 2);
		var boxShape = this.getShape("box");
		var checkerShape = this.getShape("checker");
		if (this.isSizeChanged) {
			var posOffset = width * (1 - this.boxSize) / 2;
			var halfBoxLineWidth = boxLineWidth / 2;
			var boxInnerWidth = width * this.boxSize - boxLineWidth;
			boxShape.setTopLeftPosition(x + halfBoxLineWidth + posOffset, y + halfBoxLineWidth + posOffset).setSize(boxInnerWidth, boxInnerWidth);
			if (this.isCircleShape) boxShape.setRadius(boxInnerWidth / 2);
			else boxShape.setRadius(0);
			var posOffset = width * (1 - this.checkerSize) / 2;
			var unit = width * this.checkerSize / 4, u1 = unit * 1, u2 = unit * 2, u3 = unit * 3;
			checkerShape.startAt(u1, u2).lineTo(u2, u3).lineTo(u3, u1).offset(x + posOffset, y + posOffset).end();
		}
		if (this.checked) {
			boxShape.fillStyle(this.boxFillColor, this.boxFillAlpha).lineStyle(boxLineWidth, this.boxStrokeColor, this.boxStrokeAlpha);
			checkerShape.lineStyle(checkLineWidth, this.checkerColor);
		} else {
			boxShape.fillStyle(this.uncheckedBoxFillColor, this.uncheckedBoxFillAlpha).lineStyle(boxLineWidth, this.uncheckedBoxStrokeColor, this.uncheckedBoxStrokeAlpha);
			checkerShape.lineStyle();
		}
		if (this.checked) checkerShape.setDisplayPathSegment(this.checkerAnimProgress);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/ease/EaseValueTask.js
var GetValue$227 = __webpack_exports__Utils.Objects.GetValue;
var Linear$24 = __webpack_exports__Math.Linear;
var EaseValueTask = class extends EaseValueTaskBase {
	constructor(gameObject, config) {
		super(gameObject, config);
		this.resetFromJSON();
		this.boot();
	}
	start(config) {
		if (this.timer.isRunning) return this;
		var target = this.target;
		this.propertyKey = GetValue$227(config, "key", "value");
		var currentValue = target[this.propertyKey];
		this.fromValue = GetValue$227(config, "from", currentValue);
		this.toValue = GetValue$227(config, "to", currentValue);
		this.setEase(GetValue$227(config, "ease", this.ease));
		this.setDuration(GetValue$227(config, "duration", this.duration));
		this.setRepeat(GetValue$227(config, "repeat", 0));
		this.setDelay(GetValue$227(config, "delay", 0));
		this.setRepeatDelay(GetValue$227(config, "repeatDelay", 0));
		this.timer.setDuration(this.duration).setRepeat(this.repeat).setDelay(this.delay).setRepeatDelay(this.repeatDelay);
		target[this.propertyKey] = this.fromValue;
		super.start();
		return this;
	}
	updateTarget(target, timer) {
		var t = timer.t;
		t = this.easeFn(t);
		target[this.propertyKey] = Linear$24(this.fromValue, this.toValue, t);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/checkbox/methods/CheckerAnimationMethods.js
var CheckerAnimationMethods_default = {
	setCheckerAnimationDuration(duration) {
		if (duration === void 0) duration = 0;
		this.checkerAnimDuration = duration;
		return this;
	},
	playCheckerAnimation() {
		if (this.checkerAnimProgressTask === void 0) this.checkerAnimProgressTask = new EaseValueTask(this, { eventEmitter: null });
		this.checkerAnimProgressTask.restart({
			key: "checkerAnimProgress",
			from: 0,
			to: 1,
			duration: this.checkerAnimDuration
		});
		return this;
	},
	stopCheckerAnimation() {
		if (this.checkerAnimProgressTask === void 0) return this;
		this.checkerAnimProgressTask.stop();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/checkbox/methods/Methods.js
var methods$36 = {};
Object.assign(methods$36, StyleMethods_default$2, SizeMethods_default$1, ShapesUpdateMethods_default$4, CheckerAnimationMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/checkbox/Const.js
var DefaultBoxFillColor = 23730;
var DefaultCheckerColor = 16777215;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/checkbox/CheckboxShape.js
var GetValue$226 = __webpack_exports__Utils.Objects.GetValue;
var IsPlainObject$49 = __webpack_exports__Utils.Objects.IsPlainObject;
var CheckboxShape = class extends BaseShapes {
	constructor(scene, x, y, width, height, color, config) {
		if (IsPlainObject$49(x)) {
			config = x;
			x = GetValue$226(config, "x", 0);
			y = GetValue$226(config, "y", 0);
			width = GetValue$226(config, "width", 2);
			height = GetValue$226(config, "height", 2);
			color = GetValue$226(config, "color", DefaultBoxFillColor);
		} else if (IsPlainObject$49(color)) {
			config = color;
			color = GetValue$226(config, "color", DefaultBoxFillColor);
		}
		super(scene, x, y, width, height);
		this.type = "rexCheckbox";
		if (color === void 0) color = DefaultBoxFillColor;
		this.setBoxShape(GetValue$226(config, "circleBox", false));
		this.setBoxFillStyle(color, GetValue$226(config, "boxFillAlpha", 1));
		this.setUncheckedBoxFillStyle(GetValue$226(config, "uncheckedColor", null), GetValue$226(config, "uncheckedBoxFillAlpha", 1));
		this.setBoxStrokeStyle(GetValue$226(config, "boxLineWidth", 4), GetValue$226(config, "boxStrokeColor", color), GetValue$226(config, "boxStrokeAlpha", 1));
		this.setUncheckedBoxStrokeStyle(this.boxLineWidth, GetValue$226(config, "uncheckedBoxStrokeColor", this.boxStrokeColor), GetValue$226(config, "uncheckedBoxStrokeAlpha", this.boxStrokeAlpha));
		this.setCheckerStyle(GetValue$226(config, "checkerColor", DefaultCheckerColor), GetValue$226(config, "checkerAlpha", 1));
		this.setBoxSize(GetValue$226(config, "boxSize", 1));
		this.setCheckerSize(GetValue$226(config, "checkerSize", 1));
		this.setCheckerAnimationDuration(GetValue$226(config, "animationDuration", 150));
		this.buildShapes();
		var value = GetValue$226(config, "checked");
		if (value === void 0) value = GetValue$226(config, "value", false);
		this.setValue(value);
	}
	get value() {
		return this._value;
	}
	set value(value) {
		value = !!value;
		if (this._value === value) return;
		this.dirty = true;
		this._value = value;
		if (value) this.playCheckerAnimation();
		else this.stopCheckerAnimation();
		this.emit("valuechange", value);
	}
	setValue(value) {
		this.value = value;
		return this;
	}
	toggleValue() {
		this.setValue(!this.value);
		return this;
	}
	get checked() {
		return this.value;
	}
	set checked(value) {
		this.value = value;
	}
	setChecked(checked) {
		if (checked === void 0) checked = true;
		this.setValue(checked);
		return this;
	}
	toggleChecked() {
		this.toggleValue();
		return this;
	}
	get checkerAnimProgress() {
		return this._checkerAnimProgress;
	}
	set checkerAnimProgress(value) {
		if (this._checkerAnimProgress === value) return;
		this._checkerAnimProgress = value;
		this.dirty = true;
	}
};
Object.assign(CheckboxShape.prototype, methods$36);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/input/button/Button.js
var GetValue$225 = __webpack_exports__Utils.Objects.GetValue;
var Button = class extends ComponentBase {
	constructor(gameObject, config) {
		super(gameObject, config);
		this._enable = void 0;
		gameObject.setInteractive(GetValue$225(config, "inputConfig", void 0));
		this.resetFromJSON(config);
		this.boot();
	}
	resetFromJSON(o) {
		this.pointer = void 0;
		this.lastClickTime = void 0;
		this.isDown = false;
		this.isOver = false;
		this.setEnable(GetValue$225(o, "enable", true));
		this.setMode(GetValue$225(o, "mode", 1));
		this.setClickInterval(GetValue$225(o, "clickInterval", 100));
		this.setDragThreshold(GetValue$225(o, "threshold", void 0));
		return this;
	}
	boot() {
		var gameObject = this.parent;
		gameObject.on("pointerdown", this.onPress, this);
		gameObject.on("pointerup", this.onRelease, this);
		gameObject.on("pointerout", this.onPointOut, this);
		gameObject.on("pointermove", this.onMove, this);
		gameObject.on("pointerover", this.onOver, this);
		gameObject.on("pointerout", this.onOut, this);
	}
	shutdown(fromScene) {
		if (this.isShutdown) return;
		this.pointer = null;
		super.shutdown(fromScene);
	}
	get enable() {
		return this._enable;
	}
	set enable(e) {
		if (this._enable === e) return;
		if (!e) this.cancel();
		this._enable = e;
		var eventName = e ? "enable" : "disable";
		this.emit(eventName, this, this.parent);
	}
	setEnable(e) {
		if (e === void 0) e = true;
		this.enable = e;
		return this;
	}
	toggleEnable() {
		this.setEnable(!this.enable);
		return this;
	}
	setMode(m) {
		if (typeof m === "string") m = CLICKMODE$1[m];
		this.mode = m;
		return this;
	}
	setClickInterval(interval) {
		this.clickInterval = interval;
		return this;
	}
	setDragThreshold(distance) {
		this.dragThreshold = distance;
		return this;
	}
	onPress(pointer, localX, localY, event) {
		if (this.pointer !== void 0) return;
		this.pointer = pointer;
		this.isDown = true;
		this.emit("down", this, this.parent, pointer, event);
		if (this.mode === 0) this.click(pointer.downTime, pointer, event);
	}
	onRelease(pointer, localX, localY, event) {
		if (this.pointer !== pointer) return;
		this.isDown = false;
		this.emit("up", this, this.parent, pointer, event);
		if (this.mode === 1) this.click(pointer.upTime, pointer, event);
		this.pointer = void 0;
	}
	onPointOut(pointer, event) {
		if (this.pointer !== pointer) return;
		this.cancel();
	}
	onMove(pointer, localX, localY, event) {
		if (this.pointer !== pointer) return;
		if (this.dragThreshold === void 0) return;
		if (this.mode === 1) {
			if (pointer.getDistance() >= this.dragThreshold) this.cancel();
		}
	}
	onOver(pointer, localX, localY, event) {
		if (!this.enable) return this;
		this.isOver = true;
		this.emit("over", this, this.parent, pointer, event);
		return this;
	}
	onOut(pointer, event) {
		if (!this.enable) return this;
		this.isOver = false;
		this.emit("out", this, this.parent, pointer, event);
		return this;
	}
	click(nowTime, pointer, event) {
		if (!this.enable) return this;
		if (nowTime === void 0) {
			this.emit("click", this, this.parent, pointer, event);
			return this;
		}
		this.pointer = void 0;
		var lastClickTime = this.lastClickTime;
		if (lastClickTime !== void 0 && nowTime - lastClickTime <= this.clickInterval) return this;
		this.lastClickTime = nowTime;
		this.emit("click", this, this.parent, pointer, event);
		return this;
	}
	cancel() {
		this.pointer = void 0;
		return this;
	}
};
var CLICKMODE$1 = {
	press: 0,
	pointerdown: 0,
	release: 1,
	pointerup: 1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/checkbox/Checkbox.js
var GetValue$224 = __webpack_exports__Utils.Objects.GetValue;
var IsPlainObject$48 = __webpack_exports__Utils.Objects.IsPlainObject;
var Checkbox = class extends CheckboxShape {
	constructor(scene, x, y, width, height, color, config) {
		if (IsPlainObject$48(x)) {
			config = x;
			x = GetValue$224(config, "x", 0);
			y = GetValue$224(config, "y", 0);
			width = GetValue$224(config, "width", 2);
			height = GetValue$224(config, "height", 2);
			color = GetValue$224(config, "color", DefaultBoxFillColor);
		} else if (IsPlainObject$48(color)) {
			config = color;
			color = GetValue$224(config, "color", DefaultBoxFillColor);
		}
		super(scene, x, y, width, height, color, config);
		this._click = new Button(this, GetValue$224(config, "click"));
		this._click.on("click", function() {
			this.toggleValue();
		}, this);
		this.setReadOnly(GetValue$224(config, "readOnly", false));
	}
	get readOnly() {
		return !this._click.enable;
	}
	set readOnly(value) {
		this._click.enable = !value;
	}
	setReadOnly(enable) {
		if (enable === void 0) enable = true;
		this.readOnly = enable;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/checkbox/Checkbox.js
var Checkbox_default = Checkbox;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/checkbox/Factory.js
ObjectFactory.register("checkbox", function(x, y, width, height, color, config) {
	var gameObject = new Checkbox_default(this.scene, x, y, width, height, color, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.Checkbox", Checkbox_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/toggleswitch/methods/StyleMethods.js
var StyleMethods_default = {
	setTrackFillStyle(color, alpha) {
		if (alpha === void 0) alpha = 1;
		this.dirty = this.dirty || this.trackFillColor !== color || this.trackFillAlpha !== alpha;
		this.trackFillColor = color;
		this.trackFillAlpha = alpha;
		return this;
	},
	setFalseValueTrackFillStyle(color, alpha) {
		if (alpha === void 0) alpha = 1;
		this.dirty = this.dirty || this.falseValueTrackColor !== color || this.uncheckedTrackFillAlpha !== alpha;
		this.falseValueTrackColor = color;
		this.falseValueTrackFillAlpha = alpha;
		return this;
	},
	setThumbStyle(color, alpha) {
		if (alpha === void 0) alpha = 1;
		this.dirty = this.dirty || this.thumbColor !== color || this.checkAlpha !== alpha;
		this.thumbColor = color;
		this.thumbAlpha = alpha;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/toggleswitch/methods/SizeMethods.js
var SizeMethods_default = {
	setTrackSize(width, height) {
		this.dirty = this.dirty || this.trackWidth !== width || this.trackHeight !== height;
		this.trackWidth = width;
		this.trackHeight = height;
		return this;
	},
	setTrackRadius(radius) {
		this.dirty = this.dirty || this.trackRadius !== radius;
		this.trackRadius = radius;
		return this;
	},
	setThumbSize(width, height) {
		if (height === void 0) height = width;
		this.dirty = this.dirty || this.thumbWidth !== width || this.thumbHeight !== height;
		this.thumbWidth = width;
		this.thumbHeight = height;
		return this;
	},
	setThumbRadius(radius) {
		this.dirty = this.dirty || this.thumbRadius !== radius;
		this.thumbRadius = radius;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/toggleswitch/methods/PositionMethods.js
var PositionMethods_default = {
	setThumbPosition(left, right) {
		if (right === void 0) right = 1 - left;
		this.thumbLeftX = left;
		this.thumbRightX = right;
		return this;
	},
	setRTL(rtl) {
		if (rtl === void 0) rtl = true;
		this.rtl = rtl;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/math/Linear.js
/**
* @author       Richard Davey <rich@photonstorm.com>
* @copyright    2019 Photon Storm Ltd.
* @license      {@link https://github.com/photonstorm/phaser/blob/master/license.txt|MIT License}
*/
/**
* Calculates a linear (interpolation) value over t.
*
* @function Phaser.Math.Linear
* @since 3.0.0
*
* @param {number} p0 - The first point.
* @param {number} p1 - The second point.
* @param {number} t - The percentage between p0 and p1 to return, represented as a number between 0 and 1.
*
* @return {number} The step t% of the way between p0 and p1.
*/
var Linear$23 = function(p0, p1, t) {
	return (p1 - p0) * t + p0;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/color/MixColor.js
var MixColor = function(color0, color1, t) {
	var r = Linear$23(GetR(color0), GetR(color1), t);
	var g = Linear$23(GetG(color0), GetG(color1), t);
	var b = Linear$23(GetB(color0), GetB(color1), t);
	return (r & 255) << 16 | (g & 255) << 8 | b & 255;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/toggleswitch/methods/ShapesUpdateMethods.js
var Linear$22 = __webpack_exports__Math.Linear;
var ShapesUpdateMethods_default$3 = {
	buildShapes() {
		this.addShape(new RoundRectangle().setName("track")).addShape(new RoundRectangle().setName("thumb"));
	},
	updateShapes() {
		var width = this.width, height = this.height;
		var toggleAnimProgress = this.value ? this.toggleAnimProgress : 1 - this.toggleAnimProgress;
		var trackShape = this.getShape("track");
		if (this.isSizeChanged) {
			var trackWidth = width * this.trackWidth, trackHeight = height * this.trackHeight, trackX = (width - trackWidth) / 2, trackY = (height - trackHeight) / 2, trackRadius = height * this.trackRadius;
			trackShape.setTopLeftPosition(trackX, trackY).setSize(trackWidth, trackHeight).setRadius(trackRadius);
		}
		var trackFillColor = MixColor(this.falseValueTrackColor, this.trackFillColor, toggleAnimProgress);
		var trackFillAlpha = Linear$22(this.falseValueTrackFillAlpha, this.trackFillAlpha, toggleAnimProgress);
		trackShape.fillStyle(trackFillColor, trackFillAlpha);
		var thumbShape = this.getShape("thumb");
		if (this.isSizeChanged) {
			var thumbWidth = width * this.thumbWidth, thumbHeight = height * this.thumbHeight, thumbRadius = height * this.thumbRadius;
			thumbShape.setSize(thumbWidth, thumbHeight).setRadius(thumbRadius);
		}
		var thumbX = Linear$22(this.thumbLeftX, this.thumbRightX, toggleAnimProgress) * width;
		if (this.rtl) thumbX = width - thumbX;
		var thumbY = height / 2;
		thumbShape.setCenterPosition(thumbX, thumbY);
		thumbShape.fillStyle(this.thumbColor, this.thumbAlpha);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/toggleswitch/methods/ToggleAnimationMethods.js
var ToggleAnimationMethods_default = {
	setToggleAnimationDuration(duration) {
		if (duration === void 0) duration = 0;
		this.toggleAnimDuration = duration;
		return this;
	},
	playToggleAnimation() {
		if (this.toggleAnimProgressTask === void 0) this.toggleAnimProgressTask = new EaseValueTask(this, { eventEmitter: null });
		this.toggleAnimProgressTask.restart({
			key: "toggleAnimProgress",
			from: 0,
			to: 1,
			duration: this.toggleAnimDuration
		});
		return this;
	},
	stopToggleAnimation() {
		if (this.toggleAnimProgressTask === void 0) return this;
		this.toggleAnimProgressTask.stop();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/toggleswitch/methods/Methods.js
var methods$35 = {};
Object.assign(methods$35, StyleMethods_default, SizeMethods_default, PositionMethods_default, ShapesUpdateMethods_default$3, ToggleAnimationMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/color/GrayScale.js
var GrayScale = function(color) {
	var shade = .3 * GetR(color) + .59 * GetG(color) + .11 * GetB(color);
	return (shade & 255) << 16 | (shade & 255) << 8 | shade & 255;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/toggleswitch/ToggleSwitchShape.js
var GetValue$223 = __webpack_exports__Utils.Objects.GetValue;
var IsPlainObject$47 = __webpack_exports__Utils.Objects.IsPlainObject;
var DefaultTrackFillColor = 23730;
var DefaultThumbFillColor = 16777215;
var ToggleSwitchShape = class extends BaseShapes {
	constructor(scene, x, y, width, height, color, config) {
		if (IsPlainObject$47(x)) {
			config = x;
			x = GetValue$223(config, "x", 0);
			y = GetValue$223(config, "y", 0);
			width = GetValue$223(config, "width", 2);
			height = GetValue$223(config, "height", 2);
			color = GetValue$223(config, "color", DefaultTrackFillColor);
		} else if (IsPlainObject$47(color)) {
			config = color;
			color = GetValue$223(config, "color", DefaultTrackFillColor);
		}
		super(scene, x, y, width, height);
		this.type = "rexToggleSwitch";
		if (color === void 0) color = DefaultTrackFillColor;
		this.setTrackFillStyle(color, GetValue$223(config, "trackFillAlpha", 1));
		this.setFalseValueTrackFillStyle(GetValue$223(config, "falseValueTrackColor", GrayScale(color)), GetValue$223(config, "falseValueTrackFillAlpha", 1));
		this.setThumbStyle(GetValue$223(config, "thumbColor", DefaultThumbFillColor), GetValue$223(config, "thumbAlpha", 1));
		this.setTrackSize(GetValue$223(config, "trackWidth", .9), GetValue$223(config, "trackHeight", .5));
		this.setTrackRadius(GetValue$223(config, "trackRadius", this.trackHeight * .5));
		var thumbHeight = GetValue$223(config, "thumbHeight", void 0);
		var thumbWidth = GetValue$223(config, "thumbWidth", thumbHeight);
		if (thumbWidth === void 0) thumbWidth = this.trackHeight * .9;
		this.setThumbSize(thumbWidth, thumbHeight);
		this.setThumbRadius(GetValue$223(config, "thumbRadius", this.thumbHeight * .5));
		this.setThumbPosition(GetValue$223(config, "thumbLeft", .3), GetValue$223(config, "thumbRight", void 0));
		this.setRTL(GetValue$223(config, "rtl", false));
		this.setToggleAnimationDuration(GetValue$223(config, "animationDuration", 150));
		this.buildShapes();
		this.setValue(GetValue$223(config, "value", false), 0);
	}
	get value() {
		return this._value;
	}
	set value(value) {
		value = !!value;
		if (this._value === value) return;
		this.dirty = true;
		this._value = value;
		this.playToggleAnimation();
		this.emit("valuechange", value);
	}
	setValue(value, duration) {
		if (duration === void 0) duration = this.toggleAnimDuration;
		var durationSave = this.toggleAnimDuration;
		this.toggleAnimDuration = duration;
		this.value = value;
		this.toggleAnimDuration = durationSave;
		return this;
	}
	toggleValue(duration) {
		this.setValue(!this.value, duration);
		return this;
	}
	get toggleAnimProgress() {
		return this._toggleAnimProgress;
	}
	set toggleAnimProgress(value) {
		if (this._toggleAnimProgress === value) return;
		this._toggleAnimProgress = value;
		this.dirty = true;
	}
};
Object.assign(ToggleSwitchShape.prototype, methods$35);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/toggleswitch/ToggleSwitch.js
var GetValue$222 = __webpack_exports__Utils.Objects.GetValue;
var ToggleSwitch = class extends ToggleSwitchShape {
	constructor(scene, x, y, width, height, color, config) {
		super(scene, x, y, width, height, color, config);
		this._click = new Button(this, GetValue$222(config, "click"));
		this._click.on("click", function() {
			this.toggleValue();
		}, this);
		this.setReadOnly(GetValue$222(config, "readOnly", false));
	}
	get readOnly() {
		return !this._click.enable;
	}
	set readOnly(value) {
		this._click.enable = !value;
	}
	setReadOnly(enable) {
		if (enable === void 0) enable = true;
		this.readOnly = enable;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/toggleswitch/ToggleSwitch.js
var ToggleSwitch_default = ToggleSwitch;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/toggleswitch/Factory.js
ObjectFactory.register("toggleSwitch", function(x, y, width, height, color, config) {
	var gameObject = new ToggleSwitch_default(this.scene, x, y, width, height, color, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.ToggleSwitch", ToggleSwitch_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/canvas/canvas/LoadImageMethods.js
var LoadImageMethods_default = {
	loadFromURL(url, callback) {
		var self = this;
		var img = new Image();
		img.onload = function() {
			if (self.width !== img.width || self.height !== img.height) self.resize(img.width, img.height);
			else self.clear();
			self.context.drawImage(img, 0, 0);
			self.updateTexture();
			if (callback) callback();
			img.onload = null;
			img.src = "";
			img.remove();
		};
		img.src = url;
		return this;
	},
	loadFromURLPromise(url) {
		var self = this;
		return new Promise(function(resolve, reject) {
			self.loadFromURL(url, resolve);
		});
	},
	loadFromFile(file, callback) {
		var url = URL.createObjectURL(file);
		this.loadFromURL(url, function() {
			URL.revokeObjectURL(url);
			if (callback) callback();
		});
		return this;
	},
	loadFromFilePromise(file) {
		var self = this;
		return new Promise(function(resolve, reject) {
			self.loadFromFile(file, resolve);
		});
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/canvas/canvas/Canvas.js
var Canvas = class extends Canvas$1 {};
Object.assign(Canvas.prototype, LoadImageMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/canvas/Canvas.js
var Canvas_default = Canvas;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/canvas/Factory.js
ObjectFactory.register("canvas", function(x, y, width, height) {
	var gameObject = new Canvas_default(this.scene, x, y, width, height);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.Canvas", Canvas_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/canvas/circlemaskimage/CircleMaskImage.js
var GetValue$221 = __webpack_exports__Utils.Objects.GetValue;
var CircleMaskImage = class extends Canvas$1 {
	constructor(scene, x, y, key, frame, config) {
		super(scene, x, y);
		this.type = "rexCircleMaskImage";
		this.setTexture(key, frame, config);
	}
	setTexture(key, frame, config) {
		if (typeof frame === "object") {
			config = frame;
			frame = void 0;
		}
		if (typeof config === "string") config = { maskType: config };
		var maskType = GetValue$221(config, "maskType", 0);
		var backgroundColor = GetValue$221(config, "backgroundColor", void 0);
		var strokeColor = GetValue$221(config, "strokeColor", void 0);
		var strokeWidth = GetValue$221(config, "strokeWidth", strokeColor != null ? 10 : 0);
		if (maskType === void 0) maskType = 0;
		else if (typeof maskType === "string") maskType = MASKTYPE[maskType];
		this._textureKey = key;
		this._frameName = frame;
		if (maskType === null) {
			this.loadTexture(key, frame);
			this.dirty = true;
			return this;
		}
		var textureFrame = this.scene.sys.textures.getFrame(key, frame);
		if (!textureFrame) return this;
		if (textureFrame.cutWidth !== this.width || textureFrame.cutHeight !== this.height) this.setCanvasSize(textureFrame.cutWidth, textureFrame.cutHeight);
		else this.clear();
		var canvas = this.canvas, ctx = this.context;
		var width = canvas.width, height = canvas.height;
		if (backgroundColor != null) {
			ctx.fillStyle = backgroundColor;
			ctx.fillRect(0, 0, width, height);
		}
		ctx.save();
		ctx.beginPath();
		var halfStrokeLineWidth = strokeWidth / 2;
		switch (maskType) {
			case 1:
				var centerX = Math.floor(width / 2);
				var centerY = Math.floor(height / 2);
				var radiusX = centerX - halfStrokeLineWidth;
				var radiusY = centerY - halfStrokeLineWidth;
				ctx.ellipse(centerX, centerY, radiusX, radiusY, 0, 0, 2 * Math.PI);
				break;
			case 2:
				var radiusConfig = GetValue$221(config, "radius", 0);
				var iteration = GetValue$221(config, "iteration", void 0);
				AddRoundRectanglePath(ctx, halfStrokeLineWidth, halfStrokeLineWidth, width - strokeWidth, height - strokeWidth, radiusConfig, iteration);
				break;
			default:
				var centerX = Math.floor(width / 2);
				var centerY = Math.floor(height / 2);
				var radius = Math.min(centerX, centerY) - halfStrokeLineWidth;
				ctx.arc(centerX, centerY, radius, 0, 2 * Math.PI);
		}
		if (strokeColor != null) {
			ctx.strokeStyle = strokeColor;
			ctx.lineWidth = strokeWidth;
			ctx.stroke();
		}
		ctx.clip();
		this.loadTexture(key, frame);
		ctx.restore();
		this.dirty = true;
		return this;
	}
	resize(width, height) {
		this.setDisplaySize(width, height);
		return this;
	}
};
var MASKTYPE = {
	circle: 0,
	ellipse: 1,
	roundRectangle: 2
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/circlemaskimage/CircleMaskImage.js
var CircleMaskImage_default = CircleMaskImage;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/circlemaskimage/Factory.js
ObjectFactory.register("circleMaskImage", function(x, y, key, frame, config) {
	var gameObject = new CircleMaskImage_default(this.scene, x, y, key, frame, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.CircleMaskImage", CircleMaskImage_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/canvas/alphamaskimage/AlphaMaskImage.js
var GetValue$220 = __webpack_exports__Utils.Objects.GetValue;
var AlphaMaskImage = class extends Canvas$1 {
	constructor(scene, x, y, key, frame, config) {
		super(scene, x, y);
		this.type = "rexAlphaMaskImage";
		this.maskFrame = null;
		this.setTexture(key, frame, config);
	}
	setTexture(key, frame, config) {
		if (typeof frame === "object") {
			config = frame;
			frame = void 0;
		}
		if (typeof config === "string") config = { mask: { key: config } };
		var maskKey = GetValue$220(config, "mask.key");
		var maskFrame = GetValue$220(config, "mask.frame");
		var invertMaskAlpha = GetValue$220(config, "mask.invertAlpha", false);
		var maskScale = GetValue$220(config, "mask.scale");
		var backgroundColor = GetValue$220(config, "backgroundColor");
		if (maskKey) {
			this._maskKey = maskKey;
			this._maskFrame = maskFrame;
			this._maskScale = maskScale;
			var texture = maskKey ? this.scene.sys.textures.get(maskKey) : null;
			this.maskFrame = texture ? texture.get(maskFrame) : null;
		}
		this._textureKey = key;
		this._frameName = frame;
		var maskTextureFrame = this.maskFrame;
		if (maskTextureFrame === null) {
			this.loadTexture(key, frame);
			this.dirty = true;
			return this;
		}
		var hasBackgroundColor = backgroundColor != null;
		this.loadTexture(key, frame);
		var canvas = this.canvas, ctx = this.context;
		var width = canvas.width, height = canvas.height;
		ctx.save();
		ctx.globalCompositeOperation = invertMaskAlpha ? "destination-out" : "destination-in";
		var maskWidth, maskHeight;
		if (this._maskScale != null) {
			maskWidth = maskTextureFrame.cutWidth * this._maskScale;
			maskHeight = maskTextureFrame.cutHeight * this._maskScale;
		} else {
			maskWidth = width;
			maskHeight = height;
		}
		var maskX = (width - maskWidth) / 2;
		var maskY = (height - maskHeight) / 2;
		this.drawFrame(this._maskKey, this._maskFrame, maskX, maskY, maskWidth, maskHeight);
		ctx.restore();
		if (hasBackgroundColor) {
			ctx.save();
			ctx.globalCompositeOperation = "destination-over";
			ctx.fillStyle = backgroundColor;
			ctx.fillRect(0, 0, width, height);
			ctx.restore();
		}
		this.dirty = true;
		return this;
	}
	resize(width, height) {
		this.setDisplaySize(width, height);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/alphamaskimage/AlphaMaskImage.js
var AlphaMaskImage_default = AlphaMaskImage;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/alphamaskimage/Factory.js
ObjectFactory.register("alphaMaskImage", function(x, y, key, frame, config) {
	var gameObject = new AlphaMaskImage_default(this.scene, x, y, key, frame, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.AlphaMaskImage", AlphaMaskImage_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/progressvalue/ProgressValueMethods.js
var Linear$21 = __webpack_exports__Math.Linear;
var Percent$5 = __webpack_exports__Math.Percent;
var ProgressValueMethods_default = {
	setValue(value, min, max) {
		if (value === void 0 || value === null) return this;
		if (min !== void 0) value = Percent$5(value, min, max);
		this.value = value;
		return this;
	},
	addValue(inc, min, max) {
		if (min !== void 0) inc = Percent$5(inc, min, max);
		this.value += inc;
		return this;
	},
	getValue(min, max) {
		var value = this.value;
		if (min !== void 0 && max !== void 0) value = Linear$21(min, max, value);
		return value;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/ease/EaseValueMethods.js
var Percent$4 = __webpack_exports__Math.Percent;
var EaseValueMethods_default$1 = {
	setEaseValuePropName(name) {
		this.easeValuePropName = name;
		return this;
	},
	setEaseValueDuration(duration) {
		this.easeValueDuration = duration;
		return this;
	},
	setEaseValueFunction(ease) {
		this.easeFunction = ease;
		return this;
	},
	stopEaseValue() {
		if (this.easeValueTask) this.easeValueTask.stop();
		return this;
	},
	easeValueTo(value, min, max) {
		if (value === void 0 || value === null) return this;
		if (min !== void 0) value = Percent$4(value, min, max);
		if (this.easeValueTask === void 0) this.easeValueTask = new EaseValueTask(this, { eventEmitter: null });
		this.easeValueTask.restart({
			key: this.easeValuePropName,
			to: value,
			duration: this.easeValueDuration,
			ease: this.easeFunction
		});
		return this;
	},
	easeValueRepeat(from, to, repeat, repeatDelay) {
		if (repeat === void 0) repeat = -1;
		if (repeatDelay === void 0) repeatDelay = 0;
		if (this.easeValueTask === void 0) this.easeValueTask = new EaseValueTask(this, { eventEmitter: null });
		this.easeValueTask.restart({
			key: this.easeValuePropName,
			from,
			to,
			duration: this.easeValueDuration,
			ease: this.easeFunction,
			repeat,
			repeatDelay
		});
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/progressbase/ProgressBase.js
var GetValue$219 = __webpack_exports__Utils.Objects.GetValue;
var Clamp$13 = __webpack_exports__Math.Clamp;
function ProgressBase_default(BaseClass) {
	class ProgressBase extends BaseClass {
		bootProgressBase(config) {
			this.eventEmitter = GetValue$219(config, "eventEmitter", this);
			var callback = GetValue$219(config, "valuechangeCallback", null);
			if (callback !== null) {
				var scope = GetValue$219(config, "valuechangeCallbackScope", void 0);
				this.eventEmitter.on("valuechange", callback, scope);
			}
			this.setEaseValuePropName("value").setEaseValueDuration(GetValue$219(config, "easeValue.duration", 0)).setEaseValueFunction(GetValue$219(config, "easeValue.ease", "Linear"));
			return this;
		}
		get value() {
			return this._value;
		}
		set value(value) {
			value = Clamp$13(value, 0, 1);
			var oldValue = this._value;
			var valueChanged = oldValue != value;
			this.dirty = this.dirty || valueChanged;
			this._value = value;
			if (valueChanged) this.eventEmitter.emit("valuechange", this._value, oldValue, this.eventEmitter);
		}
	}
	Object.assign(ProgressBase.prototype, ProgressValueMethods_default, EaseValueMethods_default$1);
	return ProgressBase;
}
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/circularprogress/ShapesUpdateMethods.js
var RadToDeg$12 = __webpack_exports__Math.RadToDeg;
var DegToRad$11 = __webpack_exports__Math.DegToRad;
var FillArc = function(shape, x, y, outerRadius, innerRadius, startAngle, endAngle, anticlockwise) {
	var isCircle = Math.abs(endAngle - startAngle) === 360;
	var radStartAngle = DegToRad$11(startAngle), radEndAngle = DegToRad$11(endAngle);
	var cosStartAngle = Math.cos(radStartAngle), sinStartAngle = Math.sin(radStartAngle), cosEndAngle = Math.cos(radEndAngle), sinEndAngle = Math.sin(radEndAngle);
	shape.startAt(x + cosStartAngle * outerRadius, y + sinStartAngle * outerRadius);
	shape.arc(x, y, outerRadius, startAngle, endAngle, anticlockwise);
	if (isCircle && innerRadius === 0) {} else {
		shape.lineTo(x + cosEndAngle * innerRadius, y + sinEndAngle * innerRadius);
		if (innerRadius > 0) shape.arc(x, y, innerRadius, endAngle, startAngle, !anticlockwise);
	}
	shape.close();
	return shape;
};
var ShapesUpdateMethods_default$2 = {
	buildShapes() {
		var iterations = this.iterations;
		this.addShape(new Lines().setIterations(iterations).setName("track")).addShape(new Lines().setIterations(iterations).setName("bar")).addShape(new Circle().setIterations(iterations).setName("center"));
	},
	updateShapes() {
		var x = this.radius;
		var barWidth = this.thickness * this.radius;
		var barOuterRadius = this.radius;
		var barInnerRadius = barOuterRadius - barWidth;
		var trackShape = this.getShape("track");
		if (this.trackColor != null && this.thickness > 0) {
			trackShape.fillStyle(this.trackColor, this.trackAlpha);
			FillArc(trackShape, x, x, barOuterRadius, barInnerRadius, 0, 360, false);
		} else trackShape.reset();
		var barShape = this.getShape("bar");
		if (this.barColor != null && this.thickness > 0) {
			var anticlockwise, startAngle, endAngle;
			if (this.value === 1) {
				anticlockwise = false;
				startAngle = 0;
				endAngle = 360;
			} else {
				anticlockwise = this.anticlockwise;
				startAngle = RadToDeg$12(this.startAngle);
				endAngle = 360 * (anticlockwise ? 1 - this.value : this.value) + startAngle;
			}
			barShape.fillStyle(this.barColor, this.barAlpha);
			FillArc(barShape, x, x, barOuterRadius + 1, barInnerRadius - 1, startAngle, endAngle, false);
		} else barShape.reset();
		var centerShape = this.getShape("center");
		if (this.centerColor && barInnerRadius > 0) centerShape.setCenterPosition(x, x).setRadius(barInnerRadius).fillStyle(this.centerColor, this.centerAlpha);
		else centerShape.reset();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/circularprogress/CircularProgress.js
var GetValue$218 = __webpack_exports__Utils.Objects.GetValue;
var IsPlainObject$46 = __webpack_exports__Utils.Objects.IsPlainObject;
var Clamp$12 = __webpack_exports__Math.Clamp;
var DefaultStartAngle$1 = __webpack_exports__Math.DegToRad(270);
var CircularProgress$1 = class extends ProgressBase_default(BaseShapes) {
	constructor(scene, x, y, radius, barColor, value, config) {
		var barAlpha;
		var trackColor, trackAlpha;
		var centerColor, centerAlpha;
		if (IsPlainObject$46(x)) {
			config = x;
			x = GetValue$218(config, "x", 0);
			y = GetValue$218(config, "y", 0);
			radius = GetValue$218(config, "radius", 1);
			barColor = GetValue$218(config, "barColor", void 0);
			value = GetValue$218(config, "value", 0);
		}
		barAlpha = GetValue$218(config, "barAlpha", 1);
		trackColor = GetValue$218(config, "trackColor", void 0);
		trackAlpha = GetValue$218(config, "trackAlpha", 1);
		centerColor = GetValue$218(config, "centerColor", void 0);
		centerAlpha = GetValue$218(config, "centerAlpha", 1);
		if (radius === void 0) radius = 1;
		var width = radius * 2;
		super(scene, x, y, width, width);
		this.type = "rexCircularProgress";
		this.bootProgressBase(config);
		this.setRadius(radius);
		this.setTrackColor(trackColor, trackAlpha);
		this.setBarColor(barColor, barAlpha);
		this.setCenterColor(centerColor, centerAlpha);
		this.setThickness(GetValue$218(config, "thickness", .2));
		this.setStartAngle(GetValue$218(config, "startAngle", DefaultStartAngle$1));
		this.setAnticlockwise(GetValue$218(config, "anticlockwise", false));
		this.iterations = GetValue$218(config, "iterations", 128);
		this.buildShapes();
		this.setValue(value);
	}
	resize(width, height) {
		width = Math.floor(Math.min(width, height));
		if (width === this.width) return this;
		super.resize(width, width);
		this.setRadius(width / 2);
		return this;
	}
	get radius() {
		return this._radius;
	}
	set radius(value) {
		this.dirty = this.dirty || this._radius != value;
		this._radius = value;
		var width = value * 2;
		this.resize(width, width);
	}
	setRadius(radius) {
		this.radius = radius;
		return this;
	}
	get trackColor() {
		return this._trackColor;
	}
	set trackColor(value) {
		this.dirty = this.dirty || this._trackColor != value;
		this._trackColor = value;
	}
	get trackAlpha() {
		return this._trackColor;
	}
	set trackAlpha(value) {
		this.dirty = this.dirty || this._trackAlpha != value;
		this._trackAlpha = value;
	}
	setTrackColor(color, alpha) {
		if (alpha === void 0) alpha = 1;
		this.trackColor = color;
		this.trackAlpha = alpha;
		return this;
	}
	get barColor() {
		return this._barColor;
	}
	set barColor(value) {
		this.dirty = this.dirty || this._barColor != value;
		this._barColor = value;
	}
	get barAlpha() {
		return this._barAlpha;
	}
	set barAlpha(value) {
		this.dirty = this.dirty || this._barAlpha != value;
		this._barAlpha = value;
	}
	setBarColor(color, alpha) {
		if (alpha === void 0) alpha = 1;
		this.barColor = color;
		this.barAlpha = alpha;
		return this;
	}
	get startAngle() {
		return this._startAngle;
	}
	set startAngle(value) {
		this.dirty = this.dirty || this._startAngle != value;
		this._startAngle = value;
	}
	setStartAngle(angle) {
		this.startAngle = angle;
		return this;
	}
	get anticlockwise() {
		return this._anticlockwise;
	}
	set anticlockwise(value) {
		this.dirty = this.dirty || this._anticlockwise != value;
		this._anticlockwise = value;
	}
	setAnticlockwise(anticlockwise) {
		if (anticlockwise === void 0) anticlockwise = true;
		this.anticlockwise = anticlockwise;
		return this;
	}
	get thickness() {
		return this._thickness;
	}
	set thickness(value) {
		value = Clamp$12(value, 0, 1);
		this.dirty = this.dirty || this._thickness != value;
		this._thickness = value;
	}
	setThickness(thickness) {
		this.thickness = thickness;
		return this;
	}
	get centerColor() {
		return this._centerColor;
	}
	set centerColor(value) {
		this.dirty = this.dirty || this._centerColor != value;
		this._centerColor = value;
	}
	get centerAlpha() {
		return this._centerAlpha;
	}
	set centerAlpha(value) {
		this.dirty = this.dirty || this._centerAlpha != value;
		this._centerAlpha = value;
	}
	setCenterColor(color, alpha) {
		if (alpha === void 0) alpha = 1;
		this.centerColor = color;
		this.centerAlpha = alpha;
		return this;
	}
};
Object.assign(CircularProgress$1.prototype, ShapesUpdateMethods_default$2);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/circularprogress/CircularProgress.js
var CircularProgress_default = CircularProgress$1;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/circularprogress/Factory.js
ObjectFactory.register("circularProgress", function(x, y, radius, barColor, value, config) {
	var gameObject = new CircularProgress_default(this.scene, x, y, radius, barColor, value, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.CircularProgress", CircularProgress_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/canvas/DrawCircle.js
var DrawCircle = function(canvas, context, x, y, rx, ry, fillStyle, strokeStyle, lineWidth, startAngle, endAngle, anticlockwise) {
	if (startAngle === void 0) startAngle = 0;
	if (endAngle === void 0) endAngle = 2 * Math.PI;
	if (anticlockwise === void 0) anticlockwise = false;
	context.beginPath();
	context.ellipse(x, y, rx, ry, 0, startAngle, endAngle, anticlockwise);
	if (fillStyle != null) {
		context.fillStyle = fillStyle;
		context.fill();
	}
	if (strokeStyle != null) {
		context.strokeStyle = strokeStyle;
		context.lineWidth = lineWidth;
		context.stroke();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/color/ColorNameToInteger.js
var ColorNames = [
	"AliceBlue",
	"AntiqueWhite",
	"Aqua",
	"Aquamarine",
	"Azure",
	"Beige",
	"Bisque",
	"Black",
	"BlanchedAlmond",
	"Blue",
	"BlueViolet",
	"Brown",
	"BurlyWood",
	"CadetBlue",
	"Chartreuse",
	"Chocolate",
	"Coral",
	"CornflowerBlue",
	"Cornsilk",
	"Crimson",
	"Cyan",
	"DarkBlue",
	"DarkCyan",
	"DarkGoldenRod",
	"DarkGray",
	"DarkGrey",
	"DarkGreen",
	"DarkKhaki",
	"DarkMagenta",
	"DarkOliveGreen",
	"DarkOrange",
	"DarkOrchid",
	"DarkRed",
	"DarkSalmon",
	"DarkSeaGreen",
	"DarkSlateBlue",
	"DarkSlateGray",
	"DarkSlateGrey",
	"DarkTurquoise",
	"DarkViolet",
	"DeepPink",
	"DeepSkyBlue",
	"DimGray",
	"DimGrey",
	"DodgerBlue",
	"FireBrick",
	"FloralWhite",
	"ForestGreen",
	"Fuchsia",
	"Gainsboro",
	"GhostWhite",
	"Gold",
	"GoldenRod",
	"Gray",
	"Grey",
	"Green",
	"GreenYellow",
	"HoneyDew",
	"HotPink",
	"IndianRed",
	"Indigo",
	"Ivory",
	"Khaki",
	"Lavender",
	"LavenderBlush",
	"LawnGreen",
	"LemonChiffon",
	"LightBlue",
	"LightCoral",
	"LightCyan",
	"LightGoldenRodYellow",
	"LightGray",
	"LightGrey",
	"LightGreen",
	"LightPink",
	"LightSalmon",
	"LightSeaGreen",
	"LightSkyBlue",
	"LightSlateGray",
	"LightSlateGrey",
	"LightSteelBlue",
	"LightYellow",
	"Lime",
	"LimeGreen",
	"Linen",
	"Magenta",
	"Maroon",
	"MediumAquaMarine",
	"MediumBlue",
	"MediumOrchid",
	"MediumPurple",
	"MediumSeaGreen",
	"MediumSlateBlue",
	"MediumSpringGreen",
	"MediumTurquoise",
	"MediumVioletRed",
	"MidnightBlue",
	"MintCream",
	"MistyRose",
	"Moccasin",
	"NavajoWhite",
	"Navy",
	"OldLace",
	"Olive",
	"OliveDrab",
	"Orange",
	"OrangeRed",
	"Orchid",
	"PaleGoldenRod",
	"PaleGreen",
	"PaleTurquoise",
	"PaleVioletRed",
	"PapayaWhip",
	"PeachPuff",
	"Peru",
	"Pink",
	"Plum",
	"PowderBlue",
	"Purple",
	"RebeccaPurple",
	"Red",
	"RosyBrown",
	"RoyalBlue",
	"SaddleBrown",
	"Salmon",
	"SandyBrown",
	"SeaGreen",
	"SeaShell",
	"Sienna",
	"Silver",
	"SkyBlue",
	"SlateBlue",
	"SlateGray",
	"SlateGrey",
	"Snow",
	"SpringGreen",
	"SteelBlue",
	"Tan",
	"Teal",
	"Thistle",
	"Tomato",
	"Turquoise",
	"Violet",
	"Wheat",
	"White",
	"WhiteSmoke",
	"Yellow",
	"YellowGreen"
];
var ColorValues = [
	15792383,
	16444375,
	65535,
	8388564,
	15794175,
	16119260,
	16770244,
	0,
	16772045,
	255,
	9055202,
	10824234,
	14596231,
	6266528,
	8388352,
	13789470,
	16744272,
	6591981,
	16775388,
	14423100,
	65535,
	139,
	35723,
	12092939,
	11119017,
	11119017,
	25600,
	12433259,
	9109643,
	5597999,
	16747520,
	10040012,
	9109504,
	15308410,
	9419919,
	4734347,
	3100495,
	3100495,
	52945,
	9699539,
	16716947,
	49151,
	6908265,
	6908265,
	2003199,
	11674146,
	16775920,
	2263842,
	16711935,
	14474460,
	16316671,
	16766720,
	14329120,
	8421504,
	8421504,
	32768,
	11403055,
	15794160,
	16738740,
	13458524,
	4915330,
	16777200,
	15787660,
	15132410,
	16773365,
	8190976,
	16775885,
	11393254,
	15761536,
	14745599,
	16448210,
	13882323,
	13882323,
	9498256,
	16758465,
	16752762,
	2142890,
	8900346,
	7833753,
	7833753,
	11584734,
	16777184,
	65280,
	3329330,
	16445670,
	16711935,
	8388608,
	6737322,
	205,
	12211667,
	9662683,
	3978097,
	8087790,
	64154,
	4772300,
	13047173,
	1644912,
	16121850,
	16770273,
	16770229,
	16768685,
	128,
	16643558,
	8421376,
	7048739,
	16753920,
	16729344,
	14315734,
	15657130,
	10025880,
	11529966,
	14381203,
	16773077,
	16767673,
	13468991,
	16761035,
	14524637,
	11591910,
	8388736,
	6697881,
	16711680,
	12357519,
	4286945,
	9127187,
	16416882,
	16032864,
	3050327,
	16774638,
	10506797,
	12632256,
	8900331,
	6970061,
	7372944,
	7372944,
	16775930,
	65407,
	4620980,
	13808780,
	32896,
	14204888,
	16737095,
	4251856,
	15631086,
	16113331,
	16777215,
	16119285,
	16776960,
	10145074
];
var ColorNameToIntegerDict = {};
var name$1;
for (var i = 0, cnt = ColorNames.length; i < cnt; i++) {
	name$1 = ColorNames[i].toLowerCase();
	ColorNameToIntegerDict[name$1] = ColorValues[i];
}
var ColorNameToInteger = function(colorName) {
	colorName = colorName.toLowerCase();
	if (ColorNameToIntegerDict.hasOwnProperty(colorName)) return ColorNameToIntegerDict[colorName];
	else return null;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/color/ColorStringToInteger.js
var ColorStringToInteger = function(value) {
	if (typeof value !== "string") return value;
	if (value.startsWith("#")) value = parseInt(value.substring(1), 16);
	else if (value.startsWith("0x")) value = parseInt(value.substring(2), 16);
	else value = ColorNameToInteger(value);
	return value;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/canvas/DrawGradientArc.js
var DegToRad$10 = __webpack_exports__Math.DegToRad;
var Linear$20 = __webpack_exports__Math.Linear;
var DrawGradientArc = function(canvas, context, x, y, rx, ry, startColor, endColor, lineWidth, startAngle, endAngle, anticlockwise, AngleStepDeg) {
	if (startAngle === void 0) startAngle = 0;
	if (endAngle === void 0) endAngle = 2 * Math.PI;
	if (anticlockwise === void 0) anticlockwise = false;
	if (AngleStepDeg === void 0) AngleStepDeg = 2;
	startColor = ColorStringToInteger(startColor);
	endColor = ColorStringToInteger(endColor);
	StartRGB = GetRGB(startColor, StartRGB);
	EndRGB = GetRGB(endColor, EndRGB);
	var angleStep = DegToRad$10(AngleStepDeg);
	var totalAngle;
	if (anticlockwise) totalAngle = startAngle - endAngle;
	else totalAngle = endAngle - startAngle;
	if (totalAngle < 0) totalAngle += 2 * Math.PI;
	var segments = Math.ceil(totalAngle / angleStep);
	var deltaAngle = totalAngle / segments;
	if (anticlockwise) deltaAngle = -deltaAngle;
	for (var i = 0; i < segments; i++) {
		var t = i / segments;
		var r = Math.round(Linear$20(StartRGB.r, EndRGB.r, t));
		var g = Math.round(Linear$20(StartRGB.g, EndRGB.g, t));
		var b = Math.round(Linear$20(StartRGB.b, EndRGB.b, t));
		var segmentStartAngle = startAngle + i * deltaAngle;
		var segmentEndAngle = segmentStartAngle + deltaAngle;
		if (i < segments - 1) segmentEndAngle += deltaAngle / 2;
		context.beginPath();
		context.ellipse(x, y, rx, ry, 0, segmentStartAngle, segmentEndAngle, anticlockwise);
		context.strokeStyle = `rgb(${r},${g},${b})`;
		context.lineWidth = lineWidth;
		context.stroke();
	}
};
var StartRGB;
var EndRGB;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/canvas/DrawText.js
var DrawText = function(canvas, context, x, y, text, font, fillStyle, strokeStyle, lineWidth, textAlign, textBaseline) {
	if (lineWidth === void 0 && strokeStyle != null) lineWidth = 2;
	if (textAlign === void 0) textAlign = "start";
	if (textBaseline === void 0) textBaseline = "alphabetic";
	context.font = font;
	context.textAlign = textAlign;
	context.textBaseline = textBaseline;
	context.fillStyle = fillStyle;
	context.strokeStyle = strokeStyle;
	context.lineWidth = lineWidth;
	context.lineCap = "round";
	context.lineJoin = "round";
	if (strokeStyle != null && strokeStyle !== "none" && lineWidth > 0) context.strokeText(text, x, y);
	if (fillStyle != null && fillStyle !== "none") context.fillText(text, x, y);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/canvas/circularprogress/DrawContent.js
var PI2$1 = Math.PI * 2;
var DrawContent$1 = function() {
	var x = this.radius;
	var lineWidth = this.thickness * this.radius;
	var barRadius = this.radius - lineWidth / 2;
	var centerRadius = this.radius - lineWidth;
	var canvas = this.canvas, context = this.context;
	var anticlockwise = this.anticlockwise, startAngle = this.startAngle, endAngle = this.endAngle, deltaAngle = this._deltaAngle;
	if (this.trackColor && lineWidth > 0) {
		context.save();
		DrawCircle(canvas, context, x, x, barRadius, barRadius, void 0, this.trackColor, lineWidth, startAngle, endAngle, anticlockwise);
		context.restore();
	}
	if (this.barColor && barRadius > 0) {
		var barEndAngle;
		if (this.value >= 1) barEndAngle = endAngle;
		else if (anticlockwise) barEndAngle = (startAngle - deltaAngle * this.value + PI2$1) % PI2$1;
		else barEndAngle = (startAngle + deltaAngle * this.value) % PI2$1;
		context.save();
		if (this.barColor2) DrawGradientArc(canvas, context, x, x, barRadius, barRadius, this.barColor2, this.barColor, lineWidth, startAngle, barEndAngle, anticlockwise);
		else DrawCircle(canvas, context, x, x, barRadius, barRadius, void 0, this.barColor, lineWidth, startAngle, barEndAngle, anticlockwise);
		context.restore();
	}
	if (this.centerColor && centerRadius > 0) {
		var fillStyle;
		if (this.centerColor2) {
			fillStyle = this.context.createRadialGradient(x, x, 0, x, x, centerRadius);
			fillStyle.addColorStop(0, this.centerColor);
			fillStyle.addColorStop(1, this.centerColor2);
		} else fillStyle = this.centerColor;
		context.save();
		DrawCircle(canvas, context, x, x, centerRadius, centerRadius, fillStyle);
		context.restore();
	}
	if (this.textFormatCallback && (this.textColor || this.textStrokeColor)) {
		context.save();
		DrawText(canvas, context, x, x, this.getFormatText(), this.textFont, this.textColor, this.textStrokeColor, this.textStrokeThickness, "center", "middle");
		context.restore();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/canvas/circularprogress/CircularProgress.js
var GetValue$217 = __webpack_exports__Utils.Objects.GetValue;
var IsPlainObject$45 = __webpack_exports__Utils.Objects.IsPlainObject;
var Clamp$11 = __webpack_exports__Math.Clamp;
var DefaultStartAngle = __webpack_exports__Math.DegToRad(270);
var PI2 = Math.PI * 2;
var CircularProgress = class extends ProgressBase_default(Canvas$1) {
	constructor(scene, x, y, radius, barColor, value, config) {
		if (IsPlainObject$45(x)) {
			config = x;
			x = GetValue$217(config, "x", 0);
			y = GetValue$217(config, "y", 0);
			radius = GetValue$217(config, "radius", 1);
			barColor = GetValue$217(config, "barColor", void 0);
			value = GetValue$217(config, "value", 0);
		}
		var width = radius * 2;
		var resolution = GetValue$217(config, "resolution", 1);
		super(scene, x, y, width, width, resolution);
		this.type = "rexCircularProgressCanvas";
		this.bootProgressBase(config);
		this.setRadius(radius);
		this.setTrackColor(GetValue$217(config, "trackColor", void 0));
		this.setBarColor(barColor);
		this.setBarColor2(GetValue$217(config, "barColor2", void 0));
		this.setCenterColor(GetValue$217(config, "centerColor", void 0));
		this.setThickness(GetValue$217(config, "thickness", .2));
		this.setAnticlockwise(GetValue$217(config, "anticlockwise", false));
		this.setStartAngle(GetValue$217(config, "startAngle", DefaultStartAngle));
		var endAngle = GetValue$217(config, "endAngle");
		if (endAngle === void 0) {
			if (this.anticlockwise) endAngle = this.startAngle - PI2;
			else endAngle = this.startAngle + PI2;
		}
		this.setEndAngle(GetValue$217(config, "endAngle", endAngle));
		this.setTextColor(GetValue$217(config, "textColor", void 0));
		this.setTextStrokeColor(GetValue$217(config, "textStrokeColor", void 0), GetValue$217(config, "textStrokeThickness", void 0));
		var textFont = GetValue$217(config, "textFont", void 0);
		if (textFont) this.setTextFont(textFont);
		else this.setTextFont(GetValue$217(config, "textSize", "16px"), GetValue$217(config, "textFamily", "Courier"), GetValue$217(config, "textStyle", ""));
		this.setTextFormatCallback(GetValue$217(config, "textFormatCallback", void 0), GetValue$217(config, "textFormatCallbackScope", void 0));
		this.setValue(value);
	}
	resize(width, height) {
		width = Math.floor(Math.min(width, height));
		if (width === this.width) return this;
		super.resize(width, width);
		this.setRadius(width / 2);
		return this;
	}
	get radius() {
		return this._radius;
	}
	set radius(value) {
		this.dirty = this.dirty || this._radius != value;
		this._radius = value;
		var width = value * 2;
		this.resize(width, width);
	}
	setRadius(radius) {
		this.radius = radius;
		return this;
	}
	get trackColor() {
		return this._trackColor;
	}
	set trackColor(value) {
		value = GetStyle(value, this.canvas, this.context);
		this.dirty = this.dirty || this._trackColor != value;
		this._trackColor = value;
	}
	setTrackColor(color) {
		this.trackColor = color;
		return this;
	}
	get barColor() {
		return this._barColor;
	}
	set barColor(value) {
		value = GetStyle(value, this.canvas, this.context);
		this.dirty = this.dirty || this._barColor != value;
		this._barColor = value;
	}
	setBarColor(color) {
		this.barColor = color;
		return this;
	}
	get barColor2() {
		return this._barColor2;
	}
	set barColor2(value) {
		value = GetStyle(value, this.canvas, this.context);
		this.dirty = this.dirty || this._barColor2 != value;
		this._barColor2 = value;
	}
	setBarColor2(color) {
		this.barColor2 = color;
		return this;
	}
	get startAngle() {
		return this._startAngle;
	}
	set startAngle(value) {
		this.dirty = this.dirty || this._startAngle != value;
		this._startAngle = value;
		this._deltaAngle = GetDeltaAngle(this._startAngle, this._endAngle, this._anticlockwise);
	}
	setStartAngle(angle) {
		this.startAngle = angle;
		return this;
	}
	get endAngle() {
		return this._endAngle;
	}
	set endAngle(value) {
		this.dirty = this.dirty || this._endAngle != value;
		this._endAngle = value;
		this._deltaAngle = GetDeltaAngle(this._startAngle, this._endAngle, this._anticlockwise);
	}
	setEndAngle(angle) {
		this.endAngle = angle;
		return this;
	}
	get anticlockwise() {
		return this._anticlockwise;
	}
	set anticlockwise(value) {
		this.dirty = this.dirty || this._anticlockwise != value;
		this._anticlockwise = value;
		this._deltaAngle = GetDeltaAngle(this._startAngle, this._endAngle, this._anticlockwise);
	}
	setAnticlockwise(anticlockwise) {
		if (anticlockwise === void 0) anticlockwise = true;
		this.anticlockwise = anticlockwise;
		return this;
	}
	get thickness() {
		return this._thickness;
	}
	set thickness(value) {
		value = Clamp$11(value, 0, 1);
		this.dirty = this.dirty || this._thickness != value;
		this._thickness = value;
	}
	setThickness(thickness) {
		this.thickness = thickness;
		return this;
	}
	get centerColor() {
		return this._centerColor;
	}
	set centerColor(value) {
		value = GetStyle(value, this.canvas, this.context);
		this.dirty = this.dirty || this._centerColor != value;
		this._centerColor = value;
	}
	get centerColor2() {
		return this._centerColor2;
	}
	set centerColor2(value) {
		value = GetStyle(value, this.canvas, this.context);
		this.dirty = this.dirty || this._centerColor2 != value;
		this._centerColor2 = value;
	}
	setCenterColor(color, color2) {
		this.centerColor = color;
		this.centerColor2 = color2;
		return this;
	}
	get textColor() {
		return this._textColor;
	}
	set textColor(value) {
		value = GetStyle(value, this.canvas, this.context);
		this.dirty = this.dirty || this._textColor != value;
		this._textColor = value;
	}
	setTextColor(color) {
		this.textColor = color;
		return this;
	}
	get textStrokeColor() {
		return this._textStrokeColor;
	}
	set textStrokeColor(value) {
		value = GetStyle(value, this.canvas, this.context);
		this.dirty = this.dirty || this._textStrokeColor != value;
		this._textStrokeColor = value;
	}
	get textStrokeThickness() {
		return this._textStrokeThickness;
	}
	set textStrokeThickness(value) {
		this.dirty = this.dirty || this._textStrokeThickness != value;
		this._textStrokeThickness = value;
	}
	setTextStrokeColor(color, thickness) {
		if (thickness === void 0) thickness = 2;
		this.textStrokeColor = color;
		this.textStrokeThickness = thickness;
		return this;
	}
	get textFont() {
		return this._textFont;
	}
	set textFont(value) {
		this.dirty = this.dirty || this._textFont != value;
		this._textFont = value;
	}
	setTextFont(fontSize, fontFamily, fontStyle) {
		var font;
		if (fontFamily === void 0) font = fontSize;
		else font = fontStyle + " " + fontSize + " " + fontFamily;
		this.textFont = font;
		return this;
	}
	setTextFormatCallback(callback, scope) {
		this.textFormatCallback = callback;
		this.textFormatCallbackScope = scope;
		return this;
	}
	updateTexture() {
		super.updateTexture(function() {
			this.clear();
			DrawContent$1.call(this);
		}, this);
		return this;
	}
	getFormatText(value) {
		if (value === void 0) value = this.value;
		var text;
		if (this.textFormatCallbackScope) text = this.textFormatCallback(value);
		else text = this.textFormatCallback.call(this.textFormatCallbackScope, value);
		return text;
	}
};
var GetDeltaAngle = function(startAngle, endAngle, anticlockwise) {
	if (anticlockwise) {
		if (startAngle <= endAngle) return PI2 + startAngle - endAngle;
		else return startAngle - endAngle;
	} else if (startAngle >= endAngle) return PI2 + endAngle - startAngle;
	else return endAngle - startAngle;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/circularprogresscanvas/CircularProgressCanvas.js
var CircularProgressCanvas_default = CircularProgress;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/circularprogresscanvas/Factory.js
ObjectFactory.register("circularProgressCanvas", function(x, y, radius, barColor, value, config) {
	var gameObject = new CircularProgressCanvas_default(this.scene, x, y, radius, barColor, value, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.CircularProgressCanvas", CircularProgressCanvas_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/lineprogress/UpdateShapes.js
var UpdateShapes$1 = function() {
	var skewX = this.skewX;
	var width = this.width - Math.abs(skewX);
	var height = this.height;
	var trackFill = this.getShape("trackFill");
	trackFill.fillStyle(this.trackColor);
	if (trackFill.isFilled) BuildRectangle(trackFill, 0, 0, width, height, skewX);
	var bar = this.getShape("bar");
	bar.fillStyle(this.barColor);
	if (bar.isFilled) {
		var barX0, barX1;
		if (!this.rtl) {
			barX0 = 0;
			barX1 = width * this.value;
		} else {
			barX0 = width * (1 - this.value);
			barX1 = width;
		}
		BuildRectangle(bar, barX0, 0, barX1, height, skewX);
	}
	var trackStroke = this.getShape("trackStroke");
	trackStroke.lineStyle(this.trackStrokeThickness, this.trackStrokeColor);
	if (trackStroke.isStroked) BuildRectangle(trackStroke, 0, 0, width, height, skewX);
};
var BuildRectangle = function(lines, x0, y0, x1, y1, skewX) {
	var startX = (x0 + x1) / 2;
	if (skewX >= 0) lines.startAt(startX + skewX, y0).lineTo(x1 + skewX, y0).lineTo(x1, y1).lineTo(x0, y1).lineTo(x0 + skewX, y0).lineTo(startX + skewX, y0);
	else lines.startAt(startX, y0).lineTo(x1, y0).lineTo(x1 - skewX, y1).lineTo(x0 - skewX, y1).lineTo(x0, y0).lineTo(startX, y0);
	lines.close();
	return lines;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/lineprogress/LineProgress.js
var GetValue$216 = __webpack_exports__Utils.Objects.GetValue;
var IsPlainObject$44 = __webpack_exports__Utils.Objects.IsPlainObject;
var LineProgress$1 = class extends ProgressBase_default(BaseShapes) {
	constructor(scene, x, y, width, height, barColor, value, config) {
		if (IsPlainObject$44(x)) {
			config = x;
			x = config.x;
			y = config.y;
			width = config.width;
			height = config.height;
			barColor = config.barColor;
			value = config.value;
		} else if (IsPlainObject$44(width)) {
			config = width;
			width = config.width;
			height = config.height;
			barColor = config.barColor;
			value = config.value;
		} else if (IsPlainObject$44(barColor)) {
			config = barColor;
			barColor = config.barColor;
			value = config.value;
		}
		if (x === void 0) x = 0;
		if (y === void 0) y = 0;
		if (width === void 0) width = 2;
		if (height === void 0) height = width;
		if (value === void 0) value = 0;
		super(scene, x, y, width, height, config);
		this.type = "rexLineProgress";
		this.bootProgressBase(config);
		this.addShape(new Lines().setName("trackFill")).addShape(new Lines().setName("bar")).addShape(new Lines().setName("trackStroke"));
		this.setTrackColor(GetValue$216(config, "trackColor", void 0));
		this.setBarColor(barColor);
		this.setTrackStroke(GetValue$216(config, "trackStrokeThickness", 2), GetValue$216(config, "trackStrokeColor", void 0));
		this.setSkewX(GetValue$216(config, "skewX", 0));
		this.setRTL(GetValue$216(config, "rtl", false));
		this.setValue(value);
	}
	get trackColor() {
		return this._trackColor;
	}
	set trackColor(value) {
		this.dirty = this.dirty || this._trackColor != value;
		this._trackColor = value;
	}
	setTrackColor(color) {
		this.trackColor = color;
		return this;
	}
	get trackStrokeColor() {
		return this._trackStrokeColor;
	}
	set trackStrokeColor(value) {
		this.dirty = this.dirty || this._trackStrokeColor != value;
		this._trackStrokeColor = value;
	}
	get trackStrokeThickness() {
		return this._trackStrokeThickness;
	}
	set trackStrokeThickness(value) {
		this.dirty = this.dirty || this._trackStrokeThickness != value;
		this._trackStrokeThickness = value;
	}
	setTrackStroke(lineWidth, color) {
		this.trackStrokeThickness = lineWidth;
		this.trackStrokeColor = color;
		return this;
	}
	get barColor() {
		return this._barColor;
	}
	set barColor(value) {
		this.dirty = this.dirty || this._barColor != value;
		this._barColor = value;
	}
	setBarColor(color) {
		this.barColor = color;
		return this;
	}
	get skewX() {
		return this._skewX;
	}
	set skewX(value) {
		this.dirty = this.dirty || this._skewX != value;
		this._skewX = value;
	}
	setSkewX(value) {
		this.skewX = value;
		return this;
	}
	get rtl() {
		return this._rtl;
	}
	set rtl(value) {
		value = !!value;
		this.dirty = this.dirty || this._rtl != value;
		this._rtl = value;
	}
	setRTL(enable) {
		if (enable === void 0) enable = true;
		this.rtl = enable;
		return this;
	}
};
var Methods$15 = { updateShapes: UpdateShapes$1 };
Object.assign(LineProgress$1.prototype, Methods$15);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/lineprogress/LineProgress.js
var LineProgress_default = LineProgress$1;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/lineprogress/Factory.js
ObjectFactory.register("lineProgress", function(x, y, width, height, barColor, value, config) {
	var gameObject = new LineProgress_default(this.scene, x, y, width, height, barColor, value, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.LineProgress", LineProgress_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/roundrectangleprogress/methods/BuildRoundRectangle.js
var BuildRoundRectangle = function(lines, width, height, cornerRadius, iteration) {
	lines.setIterations(iteration).start();
	var radius = cornerRadius.tl;
	if (IsArcCorner$1(radius)) {
		if (radius.convex) {
			var centerX = radius.x;
			var centerY = radius.y;
			lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 180, 270, false);
		} else {
			var centerX = 0;
			var centerY = 0;
			lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 90, 0, true);
		}
	} else lines.lineTo(0, 0);
	var radius = cornerRadius.tr;
	if (IsArcCorner$1(radius)) {
		if (radius.convex) {
			var centerX = width - radius.x;
			var centerY = radius.y;
			lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 270, 360, false);
		} else {
			var centerX = width;
			var centerY = 0;
			lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 180, 90, true);
		}
	} else lines.lineTo(width, 0);
	var radius = cornerRadius.br;
	if (IsArcCorner$1(radius)) {
		if (radius.convex) {
			var centerX = width - radius.x;
			var centerY = height - radius.y;
			lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 0, 90, false);
		} else {
			var centerX = width;
			var centerY = height;
			lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 270, 180, true);
		}
	} else lines.lineTo(width, height);
	var radius = cornerRadius.bl;
	if (IsArcCorner$1(radius)) {
		if (radius.convex) {
			var centerX = radius.x;
			var centerY = height - radius.y;
			lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 90, 180, false);
		} else {
			var centerX = 0;
			var centerY = height;
			lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 360, 270, true);
		}
	} else lines.lineTo(0, height);
	lines.close();
	return lines;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/roundrectangleprogress/methods/BuildRoundRectangleBarDirection0.js
var RadToDeg$11 = __webpack_exports__Math.RadToDeg;
var BuildRoundRectangleBarDirection0 = function(lines, width, height, cornerRadius, value) {
	var barWidth = width * value;
	var radius = cornerRadius.tl;
	if (IsArcCorner$1(radius)) {
		var theta;
		if (barWidth > radius.x) theta = 90;
		else theta = RadToDeg$11(Math.acos((radius.x - barWidth) / radius.x));
		var centerX = radius.x;
		var centerY = radius.y;
		lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 180, 180 + theta, false);
	} else lines.lineTo(0, 0);
	var radius = cornerRadius.tr;
	if (IsArcCorner$1(radius) && barWidth > width - radius.x) {
		var theta = 90 - RadToDeg$11(Math.acos((barWidth - (width - radius.x)) / radius.x));
		var centerX = width - radius.x;
		var centerY = radius.y;
		lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 270, 270 + theta, false);
	} else lines.lineTo(barWidth, 0);
	var radius = cornerRadius.br;
	if (IsArcCorner$1(radius) && barWidth > width - radius.x) {
		var theta = 90 - RadToDeg$11(Math.acos((barWidth - (width - radius.x)) / radius.x));
		var centerX = width - radius.x;
		var centerY = height - radius.y;
		lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 90 - theta, 90, false);
	} else lines.lineTo(barWidth, height);
	var radius = cornerRadius.bl;
	if (IsArcCorner$1(radius)) {
		var theta;
		if (barWidth > radius.x) theta = 90;
		else theta = RadToDeg$11(Math.acos((radius.x - barWidth) / radius.x));
		var centerX = radius.x;
		var centerY = height - radius.y;
		lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 180 - theta, 180, false);
	} else lines.lineTo(0, height);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/roundrectangleprogress/methods/BuildRoundRectangleBarDirection1.js
var RadToDeg$10 = __webpack_exports__Math.RadToDeg;
var BuildRoundRectangleBarDirection1 = function(lines, width, height, cornerRadius, value) {
	var barHeight = height * value;
	var radius = cornerRadius.tl;
	if (IsArcCorner$1(radius)) {
		var theta;
		if (barHeight > radius.y) theta = 90;
		else theta = RadToDeg$10(Math.acos((radius.y - barHeight) / radius.y));
		var centerX = radius.x;
		var centerY = radius.y;
		lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 270 - theta, 270, false);
	} else lines.lineTo(0, 0);
	var radius = cornerRadius.tr;
	if (IsArcCorner$1(radius)) {
		var theta;
		if (barHeight > radius.y) theta = 90;
		else theta = RadToDeg$10(Math.acos((radius.y - barHeight) / radius.y));
		var centerX = width - radius.x;
		var centerY = radius.y;
		lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 270, 270 + theta, false);
	} else lines.lineTo(width, 0);
	var radius = cornerRadius.br;
	if (IsArcCorner$1(radius) && barHeight > height - radius.y) {
		var theta = 90 - RadToDeg$10(Math.acos((barHeight - (height - radius.y)) / radius.y));
		var centerX = width - radius.x;
		var centerY = height - radius.y;
		lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 0, 0 + theta, false);
	} else lines.lineTo(width, barHeight);
	var radius = cornerRadius.bl;
	if (IsArcCorner$1(radius) && barHeight > height - radius.y) {
		var theta = 90 - RadToDeg$10(Math.acos((barHeight - (height - radius.y)) / radius.y));
		var centerX = radius.x;
		var centerY = height - radius.y;
		lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 180 - theta, 180, false);
	} else lines.lineTo(0, barHeight);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/roundrectangleprogress/methods/BuildRoundRectangleBarDirection2.js
var RadToDeg$9 = __webpack_exports__Math.RadToDeg;
var BuildRoundRectangleBarDirection2 = function(lines, width, height, cornerRadius, value) {
	var barWidth = width * value;
	var radius = cornerRadius.tr;
	if (IsArcCorner$1(radius)) {
		var theta;
		if (barWidth > radius.x) theta = 90;
		else theta = RadToDeg$9(Math.acos((radius.x - barWidth) / radius.x));
		var centerX = width - radius.x;
		var centerY = radius.y;
		lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 360 - theta, 360, false);
	} else lines.lineTo(width, 0);
	var radius = cornerRadius.br;
	if (IsArcCorner$1(radius)) {
		var theta;
		if (barWidth > radius.x) theta = 90;
		else theta = RadToDeg$9(Math.acos((radius.x - barWidth) / radius.x));
		var centerX = width - radius.x;
		var centerY = height - radius.y;
		lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 0, 0 + theta, false);
	} else lines.lineTo(width, height);
	var radius = cornerRadius.bl;
	if (IsArcCorner$1(radius) && barWidth > width - radius.x) {
		var theta = 90 - RadToDeg$9(Math.acos((barWidth - (width - radius.x)) / radius.x));
		var centerX = radius.x;
		var centerY = height - radius.y;
		lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 90, 90 + theta, false);
	} else lines.lineTo(width - barWidth, height);
	var radius = cornerRadius.tl;
	if (IsArcCorner$1(radius) && barWidth > width - radius.x) {
		var theta = 90 - RadToDeg$9(Math.acos((barWidth - (width - radius.x)) / radius.x));
		var centerX = radius.x;
		var centerY = radius.y;
		lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 270 - theta, 270, false);
	} else lines.lineTo(width - barWidth, 0);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/roundrectangleprogress/methods/BuildRoundRectangleBarDirection3.js
var RadToDeg$8 = __webpack_exports__Math.RadToDeg;
var BuildRoundRectangleBarDirection3 = function(lines, width, height, cornerRadius, value) {
	var barHeight = height * value;
	var radius = cornerRadius.br;
	if (IsArcCorner$1(radius)) {
		if (barHeight > radius.y) theta = 90;
		else theta = RadToDeg$8(Math.acos((radius.y - barHeight) / radius.y));
		var centerX = width - radius.x;
		var centerY = height - radius.y;
		lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 90 - theta, 90, false);
	} else lines.lineTo(width, height);
	var radius = cornerRadius.bl;
	if (IsArcCorner$1(radius)) {
		if (barHeight > radius.y) theta = 90;
		else theta = RadToDeg$8(Math.acos((radius.y - barHeight) / radius.y));
		var centerX = radius.x;
		var centerY = height - radius.y;
		lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 90, 90 + theta, false);
	} else lines.lineTo(0, height);
	var radius = cornerRadius.tl;
	if (IsArcCorner$1(radius) && barHeight > height - radius.y) {
		var theta = 90 - RadToDeg$8(Math.acos((barHeight - (height - radius.y)) / radius.y));
		var centerX = radius.x;
		var centerY = radius.y;
		lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 180, 180 + theta, false);
	} else lines.lineTo(0, height - barHeight);
	var radius = cornerRadius.tr;
	if (IsArcCorner$1(radius) && barHeight > height - radius.y) {
		var theta = 90 - RadToDeg$8(Math.acos((barHeight - (height - radius.y)) / radius.y));
		var centerX = width - radius.x;
		var centerY = radius.y;
		lines.ellipticalArc(centerX, centerY, radius.x, radius.y, 360 - theta, 360, false);
	} else lines.lineTo(width, height - barHeight);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/roundrectangleprogress/methods/BuildRoundRectangleBar.js
var BuildRoundRectangleBar = function(lines, width, height, cornerRadius, value, orientation, rtl, iteration) {
	lines.setIterations(iteration).start();
	if (value === 0) return lines;
	else if (value === 1) return BuildRoundRectangle(lines, width, height, cornerRadius, iteration);
	var callback;
	if (orientation === 0) callback = rtl ? BuildRoundRectangleBarDirection2 : BuildRoundRectangleBarDirection0;
	else callback = rtl ? BuildRoundRectangleBarDirection3 : BuildRoundRectangleBarDirection1;
	callback(lines, width, height, cornerRadius, value);
	lines.close();
	return lines;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/roundrectangleprogress/methods/UpdateShapes.js
var UpdateShapes = function() {
	var width = this.width;
	var height = this.height;
	var cornerRadius = this.rrGeom.cornerRadius;
	var value = this.value;
	var orientation = this.orientation;
	var rtl = this.rtl;
	var iteration = this.iteration + 1;
	var trackFill = this.getShape("trackFill");
	trackFill.fillStyle(this.trackColor);
	if (trackFill.isFilled) BuildRoundRectangle(trackFill, width, height, cornerRadius, iteration);
	var bar = this.getShape("bar");
	bar.fillStyle(this.barColor);
	if (bar.isFilled) BuildRoundRectangleBar(bar, width, height, cornerRadius, value, orientation, rtl, iteration);
	var trackStroke = this.getShape("trackStroke");
	trackStroke.lineStyle(this.trackStrokeThickness, this.trackStrokeColor);
	if (trackStroke.isStroked) BuildRoundRectangle(trackStroke, width, height, cornerRadius, iteration);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/orientation/GetOrientationMode.js
var OrientationMode = {
	x: 0,
	h: 0,
	horizontal: 0,
	"left-to-right": 0,
	y: 1,
	v: 1,
	vertical: 1,
	"top-to-bottom": 1
};
var GetOrientationMode = function(orientation) {
	if (typeof orientation === "string") orientation = OrientationMode[orientation];
	return orientation;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/roundrectangleprogress/RoundRectangleProgress.js
var GetValue$215 = __webpack_exports__Utils.Objects.GetValue;
var IsPlainObject$43 = __webpack_exports__Utils.Objects.IsPlainObject;
var RoundRectangleProgress = class extends ProgressBase_default(BaseShapes) {
	constructor(scene, x, y, width, height, radiusConfig, barColor, value, config) {
		if (IsPlainObject$43(x)) {
			config = x;
			x = config.x;
			y = config.y;
			width = config.width;
			height = config.height;
			radiusConfig = config.radius;
			barColor = config.barColor;
			value = config.value;
		} else if (IsPlainObject$43(width)) {
			config = width;
			width = config.width;
			height = config.height;
			radiusConfig = config.radius;
			barColor = config.barColor;
			value = config.value;
		} else if (IsPlainObject$43(radiusConfig)) {
			config = radiusConfig;
			radiusConfig = config.radius;
			barColor = config.barColor;
			value = config.value;
		}
		if (x === void 0) x = 0;
		if (y === void 0) y = 0;
		if (width === void 0) width = 1;
		if (height === void 0) height = width;
		if (radiusConfig === void 0) radiusConfig = 0;
		if (value === void 0) value = 0;
		super(scene, x, y, width, height, config);
		this.type = "rexRoundRectangleProgress";
		this.rrGeom = new RoundRectangle$3();
		this.bootProgressBase(config);
		this.addShape(new Lines().setName("trackFill")).addShape(new Lines().setName("bar")).addShape(new Lines().setName("trackStroke"));
		this.setTrackColor(GetValue$215(config, "trackColor", void 0));
		this.setBarColor(barColor);
		this.setTrackStroke(GetValue$215(config, "trackStrokeThickness", 2), GetValue$215(config, "trackStrokeColor", void 0));
		this.setOrientation(GetValue$215(config, "orientation", 0));
		this.setRTL(GetValue$215(config, "rtl", false));
		this.setRadius(radiusConfig);
		this.setIteration(GetValue$215(radiusConfig, "iteration", void 0));
		this.setValue(value);
	}
	get trackColor() {
		return this._trackColor;
	}
	set trackColor(value) {
		this.dirty = this.dirty || this._trackColor != value;
		this._trackColor = value;
	}
	setTrackColor(color) {
		this.trackColor = color;
		return this;
	}
	get trackStrokeColor() {
		return this._trackStrokeColor;
	}
	set trackStrokeColor(value) {
		this.dirty = this.dirty || this._trackStrokeColor != value;
		this._trackStrokeColor = value;
	}
	get trackStrokeThickness() {
		return this._trackStrokeThickness;
	}
	set trackStrokeThickness(value) {
		this.dirty = this.dirty || this._trackStrokeThickness != value;
		this._trackStrokeThickness = value;
	}
	setTrackStroke(lineWidth, color) {
		this.trackStrokeThickness = lineWidth;
		this.trackStrokeColor = color;
		return this;
	}
	get barColor() {
		return this._barColor;
	}
	set barColor(value) {
		this.dirty = this.dirty || this._barColor != value;
		this._barColor = value;
	}
	setBarColor(color) {
		this.barColor = color;
		return this;
	}
	get orientation() {
		return this._orientation;
	}
	set orientation(value) {
		value = GetOrientationMode(value);
		this.dirty = this.dirty || this._orientation != value;
		this._orientation = value;
	}
	setOrientation(value) {
		this.orientation = value;
		return this;
	}
	get rtl() {
		return this._rtl;
	}
	set rtl(value) {
		value = !!value;
		this.dirty = this.dirty || this._rtl != value;
		this._rtl = value;
	}
	setRTL(enable) {
		if (enable === void 0) enable = true;
		this.rtl = enable;
		return this;
	}
	get radius() {
		return this.rrGeom.radius;
	}
	set radius(value) {
		this.rrGeom.setRadius(value);
		this.dirty = true;
	}
	get radiusTL() {
		return this.rrGeom.radiusTL;
	}
	set radiusTL(value) {
		this.rrGeom.radiusTL = value;
		this.dirty = true;
	}
	get radiusTR() {
		return this.rrGeom.radiusTR;
	}
	set radiusTR(value) {
		this.rrGeom.radiusTR = value;
		this.dirty = true;
	}
	get radiusBL() {
		return this.rrGeom.radiusBL;
	}
	set radiusBL(value) {
		this.rrGeom.radiusBL = value;
		this.dirty = true;
	}
	get radiusBR() {
		return this.rrGeom.radiusBR;
	}
	set radiusBR(value) {
		this.rrGeom.radiusBR = value;
		this.dirty = true;
	}
	setRadius(value) {
		if (value === void 0) value = 0;
		this.radius = value;
		return this;
	}
	setRadiusTL(value) {
		if (value === void 0) value = 0;
		this.radiusTL = value;
		return this;
	}
	setRadiusTR(value) {
		if (value === void 0) value = 0;
		this.radiusTR = value;
		return this;
	}
	setRadiusBL(value) {
		if (value === void 0) value = 0;
		this.radiusBL = value;
		return this;
	}
	setRadiusBR(value) {
		if (value === void 0) value = 0;
		this.radiusBR = value;
		return this;
	}
	get cornerRadius() {
		return this.rrGeom.cornerRadius;
	}
	set cornerRadius(value) {
		this.radius = value;
	}
	setCornerRadius(value) {
		return this.setRadius(value);
	}
	get iteration() {
		return this._iteration;
	}
	set iteration(value) {
		if (this._iteration === void 0) {
			this._iteration = value;
			return;
		}
		if (this._iteration === value) return;
		this._iteration = value;
		this.dirty = true;
	}
	setIteration(iteration) {
		if (iteration === void 0) iteration = 6;
		this.iteration = iteration;
		return this;
	}
};
var Methods$14 = { updateShapes: UpdateShapes };
Object.assign(RoundRectangleProgress.prototype, Methods$14);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/roundrectangleprogress/RoundRectangleProgress.js
var RoundRectangleProgress_default = RoundRectangleProgress;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/roundrectangleprogress/Factory.js
ObjectFactory.register("roundRectangleProgress", function(x, y, width, height, radiusConfig, barColor, value, config) {
	var gameObject = new RoundRectangleProgress_default(this.scene, x, y, width, height, radiusConfig, barColor, value, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.RoundRectangleProgress", RoundRectangleProgress_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/canvas/AddPolygonPath.js
var AddPolygonPath = function(context, points) {
	context.save();
	context.beginPath();
	var point = points[0];
	context.moveTo(point.x, point.y);
	for (var i = 1, cnt = points.length; i < cnt; i++) {
		point = points[i];
		context.lineTo(point.x, point.y);
	}
	context.closePath();
	context.restore();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/canvas/DrawPolygon.js
var DrawPolygon = function(canvas, context, points, fillStyle, strokeStyle, lineWidth, lineJoin) {
	if (lineJoin === void 0) lineJoin = "round";
	AddPolygonPath(context, points);
	context.lineJoin = lineJoin;
	if (fillStyle != null) {
		context.fillStyle = fillStyle;
		context.fill();
	}
	if (strokeStyle != null) {
		context.strokeStyle = strokeStyle;
		context.lineWidth = lineWidth;
		context.stroke();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/canvas/lineprogress/DrawContent.js
var DrawContent = function() {
	var skewX = this.skewX;
	var width = this.width - Math.abs(skewX);
	var height = this.height;
	var canvas = this.canvas, context = this.context;
	if (this.trackColor || this.trackStrokeColor) BuildPolygon(0, 0, width, height, skewX, this.trackPoints);
	var barX0, barX1;
	if (this.barColor) {
		if (!this.rtl) {
			barX0 = 0;
			barX1 = width * this.value;
		} else {
			barX0 = width * (1 - this.value);
			barX1 = width;
		}
		BuildPolygon(barX0, 0, barX1, height, skewX, this.barPoints);
	}
	if (this.trackColor) {
		context.save();
		DrawPolygon(canvas, context, this.trackPoints, this.trackColor);
		context.restore();
	}
	if (this.barColor) {
		context.save();
		var style;
		if (this.barColor2) {
			var grd;
			if (this.isHorizontalGradient) {
				var helfHeight = height / 2;
				grd = context.createLinearGradient(barX0, helfHeight, barX1, helfHeight);
			} else {
				var helfWidth = width / 2;
				grd = context.createLinearGradient(helfWidth, 0, helfWidth, height);
			}
			grd.addColorStop(0, this.rtl ? this.barColor : this.barColor2);
			grd.addColorStop(1, this.rtl ? this.barColor2 : this.barColor);
			style = grd;
		} else style = this.barColor;
		DrawPolygon(canvas, context, this.barPoints, style);
		context.restore();
	}
	if (this.trackStrokeColor && this.trackStrokeThickness > 0) {
		context.save();
		DrawPolygon(canvas, context, this.trackPoints, void 0, this.trackStrokeColor, this.trackStrokeThickness);
		context.restore();
	}
};
var BuildPolygon = function(x0, y0, x1, y1, skewX, out) {
	if (out === void 0) out = [];
	out.length = 4;
	for (var i = 0; i < 4; i++) if (!out[i]) out[i] = {};
	var p;
	if (skewX >= 0) {
		p = out[0];
		p.x = x0 + skewX;
		p.y = y0;
		p = out[1];
		p.x = x1 + skewX;
		p.y = y0;
		p = out[2];
		p.x = x1;
		p.y = y1;
		p = out[3];
		p.x = x0;
		p.y = y1;
	} else {
		p = out[0];
		p.x = x0;
		p.y = y0;
		p = out[1];
		p.x = x1;
		p.y = y0;
		p = out[2];
		p.x = x1 - skewX;
		p.y = y1;
		p = out[3];
		p.x = x0 - skewX;
		p.y = y1;
	}
	return out;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/canvas/lineprogress/LineProgress.js
var GetValue$214 = __webpack_exports__Utils.Objects.GetValue;
var IsPlainObject$42 = __webpack_exports__Utils.Objects.IsPlainObject;
var LineProgress = class extends ProgressBase_default(Canvas$1) {
	constructor(scene, x, y, width, height, barColor, value, config) {
		if (IsPlainObject$42(x)) {
			config = x;
			x = GetValue$214(config, "x", 0);
			y = GetValue$214(config, "y", 0);
			width = GetValue$214(config, "width", 2);
			height = GetValue$214(config, "height", 2);
			barColor = GetValue$214(config, "barColor", void 0);
			value = GetValue$214(config, "value", 0);
		} else if (IsPlainObject$42(width)) {
			config = width;
			width = GetValue$214(config, "width", 2);
			height = GetValue$214(config, "height", 2);
			barColor = GetValue$214(config, "barColor", void 0);
			value = GetValue$214(config, "value", 0);
		} else if (IsPlainObject$42(barColor)) {
			config = barColor;
			barColor = GetValue$214(config, "barColor", void 0);
			value = GetValue$214(config, "value", 0);
		}
		var resolution = GetValue$214(config, "resolution", 1);
		super(scene, x, y, width, height, resolution);
		this.type = "rexLineProgressCanvas";
		this.trackPoints = [];
		this.barPoints = [];
		this.bootProgressBase(config);
		this.setTrackColor(GetValue$214(config, "trackColor", void 0));
		this.setBarColor(barColor, GetValue$214(config, "barColor2", void 0), GetValue$214(config, "isHorizontalGradient", void 0));
		this.setTrackStroke(GetValue$214(config, "trackStrokeThickness", 2), GetValue$214(config, "trackStrokeColor", void 0));
		this.setSkewX(GetValue$214(config, "skewX", 0));
		this.setRTL(GetValue$214(config, "rtl", false));
		this.setValue(value);
	}
	get trackColor() {
		return this._trackColor;
	}
	set trackColor(value) {
		value = GetStyle(value, this.canvas, this.context);
		this.dirty = this.dirty || this._trackColor != value;
		this._trackColor = value;
	}
	setTrackColor(color) {
		this.trackColor = color;
		return this;
	}
	get trackStrokeColor() {
		return this._trackStrokeColor;
	}
	set trackStrokeColor(value) {
		value = GetStyle(value, this.canvas, this.context);
		this.dirty = this.dirty || this._trackStrokeColor != value;
		this._trackStrokeColor = value;
	}
	get trackStrokeThickness() {
		return this._trackStrokeThickness;
	}
	set trackStrokeThickness(value) {
		this.dirty = this.dirty || this._trackStrokeThickness != value;
		this._trackStrokeThickness = value;
	}
	setTrackStroke(lineWidth, color) {
		this.trackStrokeThickness = lineWidth;
		this.trackStrokeColor = color;
		return this;
	}
	get barColor() {
		return this._barColor;
	}
	set barColor(value) {
		value = GetStyle(value, this.canvas, this.context);
		this.dirty = this.dirty || this._barColor != value;
		this._barColor = value;
	}
	get barColor2() {
		return this._barColor2;
	}
	set barColor2(value) {
		value = GetStyle(value, this.canvas, this.context);
		this.dirty = this.dirty || this._barColor2 != value;
		this._barColor2 = value;
	}
	get isHorizontalGradient() {
		return this._isHorizontalGradient;
	}
	set isHorizontalGradient(value) {
		this.dirty |= this._isHorizontalGradient != value;
		this._isHorizontalGradient = value;
	}
	setBarColor(color, color2, isHorizontalGradient) {
		if (isHorizontalGradient === void 0) isHorizontalGradient = true;
		this.barColor = color;
		this.barColor2 = color2;
		this.isHorizontalGradient = isHorizontalGradient;
		return this;
	}
	get skewX() {
		return this._skewX;
	}
	set skewX(value) {
		this.dirty = this.dirty || this._skewX != value;
		this._skewX = value;
	}
	setSkewX(value) {
		this.skewX = value;
		return this;
	}
	get rtl() {
		return this._rtl;
	}
	set rtl(value) {
		value = !!value;
		this.dirty = this.dirty || this._rtl != value;
		this._rtl = value;
	}
	setRTL(enable) {
		if (enable === void 0) enable = true;
		this.rtl = enable;
		return this;
	}
	updateTexture() {
		super.updateTexture(function() {
			this.clear();
			DrawContent.call(this);
		}, this);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/lineprogresscanvas/LineProgressCanvas.js
var LineProgressCanvas_default = LineProgress;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/lineprogresscanvas/Factory.js
ObjectFactory.register("lineProgressCanvas", function(x, y, width, height, barColor, value, config) {
	var gameObject = new LineProgressCanvas_default(this.scene, x, y, width, height, barColor, value, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.LineProgressCanvas", LineProgressCanvas_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/triangle/methods/DrawFitTriangle.js
__webpack_exports__Math.Wrap;
var Linear$19 = __webpack_exports__Math.Linear;
var DrawFitTriangle = function() {
	var triangle = this.getShape("triangle");
	var padding = this.padding;
	var right = this.width - padding.right;
	var left = 0 + padding.left;
	var bottom = this.height - padding.bottom;
	var top = 0 + padding.top;
	var centerX = (left + right) / 2;
	var centerY = (top + bottom) / 2;
	var points = {
		0: {
			a: {
				x: left,
				y: top
			},
			b: {
				x: right,
				y: centerY
			},
			c: {
				x: left,
				y: bottom
			}
		},
		1: {
			a: {
				x: left,
				y: top
			},
			b: {
				x: centerX,
				y: bottom
			},
			c: {
				x: right,
				y: top
			}
		},
		2: {
			a: {
				x: right,
				y: top
			},
			b: {
				x: left,
				y: centerY
			},
			c: {
				x: right,
				y: bottom
			}
		},
		3: {
			a: {
				x: left,
				y: bottom
			},
			b: {
				x: centerX,
				y: top
			},
			c: {
				x: right,
				y: bottom
			}
		}
	};
	var pax, pay, pbx, pby, pcx, pcy;
	if (this.previousDirection === void 0) {
		var currentTrianglePoints = points[this.direction];
		var pa = currentTrianglePoints.a, pb = currentTrianglePoints.b, pc = currentTrianglePoints.c;
		pax = pa.x;
		pay = pa.y;
		pbx = pb.x;
		pby = pb.y;
		pcx = pc.x;
		pcy = pc.y;
	} else {
		var p0 = points[this.previousDirection];
		var p1 = points[this.direction];
		var t = this.easeDirectionProgress;
		pax = Linear$19(p0.a.x, p1.a.x, t);
		pay = Linear$19(p0.a.y, p1.a.y, t);
		pbx = Linear$19(p0.b.x, p1.b.x, t);
		pby = Linear$19(p0.b.y, p1.b.y, t);
		pcx = Linear$19(p0.c.x, p1.c.x, t);
		pcy = Linear$19(p0.c.y, p1.c.y, t);
	}
	triangle.startAt(pax, pay).lineTo(pbx, pby).lineTo(pcx, pcy);
	if (!this.arrowOnly) triangle.close();
	else triangle.end();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/triangle/methods/DrawCircleVerticesTriangle.js
var DegToRad$9 = __webpack_exports__Math.DegToRad;
var Rad120 = DegToRad$9(120);
var DrawCircleVerticesTriangle = function(triangle) {
	var triangle = this.getShape("triangle");
	var centerX = this.width / 2, centerY = this.height / 2;
	var radius = Math.min(centerX, centerY) * this.radius, verticeRotation = this.verticeRotation;
	triangle.startAt(centerX + radius * Math.cos(verticeRotation + Rad120), centerY + radius * Math.sin(verticeRotation + Rad120)).lineTo(centerX + radius * Math.cos(verticeRotation), centerY + radius * Math.sin(verticeRotation)).lineTo(centerX + radius * Math.cos(verticeRotation - Rad120), centerY + radius * Math.sin(verticeRotation - Rad120));
	if (!this.arrowOnly) triangle.close();
	else triangle.end();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/triangle/methods/ShapesUpdateMethods.js
var ShapesUpdateMethods_default$1 = {
	buildShapes() {
		this.addShape(new Lines().setName("triangle"));
	},
	updateShapes() {
		var triangle = this.getShape("triangle");
		if (!this.arrowOnly) triangle.fillStyle(this.fillColor, this.fillAlpha).lineStyle(this.lineWidth, this.strokeColor, this.strokeAlpha);
		else triangle.fillStyle().lineStyle(this.lineWidth, this.strokeColor, this.strokeAlpha);
		if (this.shapeMode === 0) DrawFitTriangle.call(this);
		else DrawCircleVerticesTriangle.call(this);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/triangle/methods/EaseDirectionMethods.js
var EaseDirectionMethods_default = {
	setEaseDuration(duration) {
		if (duration === void 0) duration = 0;
		this.easeDuration = duration;
		return this;
	},
	playEaseDirectionation() {
		if (this.easeDirectionProgressTask === void 0) this.easeDirectionProgressTask = new EaseValueTask(this, { eventEmitter: null });
		this.easeDirectionProgressTask.restart({
			key: "easeDirectionProgress",
			from: 0,
			to: 1,
			duration: this.easeDuration
		});
		return this;
	},
	stopEaseDirection() {
		if (this.easeDirectionProgressTask === void 0) return this;
		this.easeDirectionProgressTask.stop();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/triangle/Triangle.js
var GetValue$213 = __webpack_exports__Utils.Objects.GetValue;
var IsPlainObject$41 = __webpack_exports__Utils.Objects.IsPlainObject;
var DegToRad$8 = __webpack_exports__Math.DegToRad;
var RadToDeg$7 = __webpack_exports__Math.RadToDeg;
var Triangle = class extends BaseShapes {
	constructor(scene, x, y, width, height, fillColor, fillAlpha) {
		var strokeColor, strokeAlpha, strokeWidth, arrowOnly;
		var direction, easeDuration, padding;
		var radius;
		if (IsPlainObject$41(x)) {
			var config = x;
			x = config.x;
			y = config.y;
			width = config.width;
			height = config.height;
			fillColor = config.color;
			fillAlpha = config.alpha;
			strokeColor = config.strokeColor;
			strokeAlpha = config.strokeAlpha;
			strokeWidth = config.strokeWidth;
			arrowOnly = config.arrowOnly;
			direction = config.direction;
			easeDuration = config.easeDuration;
			padding = config.padding;
			radius = config.radius;
		}
		if (x === void 0) x = 0;
		if (y === void 0) y = 0;
		if (width === void 0) width = 1;
		if (height === void 0) height = width;
		if (arrowOnly === void 0) arrowOnly = false;
		if (direction === void 0) direction = 0;
		if (easeDuration === void 0) easeDuration = 0;
		if (padding === void 0) padding = 0;
		if (radius === void 0) radius = void 0;
		super(scene, x, y, width, height);
		this.type = "rexTriangle";
		this.setFillStyle(fillColor, fillAlpha);
		if (strokeColor !== void 0 && strokeWidth === void 0) strokeWidth = 2;
		this.setStrokeStyle(strokeWidth, strokeColor, strokeAlpha);
		this.setArrowOnly(arrowOnly);
		this.setDirection(direction, easeDuration);
		this.setPadding(padding);
		this.setRadius(radius);
		this.buildShapes();
	}
	get arrowOnly() {
		return this._arrowOnly;
	}
	set arrowOnly(value) {
		this.dirty = this.dirty || this._arrowOnly != value;
		this._arrowOnly = value;
	}
	setArrowOnly(enable) {
		if (enable === void 0) enable = true;
		this.arrowOnly = enable;
		return this;
	}
	get direction() {
		return this._direction;
	}
	set direction(value) {
		value = ParseDirection(value);
		if (this._direction === value) return;
		if (this.easeDuration > 0 && this._direction !== void 0) this.previousDirection = this._direction;
		else this.previousDirection = void 0;
		this._direction = value;
		this.verticeAngle = value * 90;
		this.dirty = true;
		if (this.previousDirection !== void 0) this.playEaseDirectionation();
		else this.stopEaseDirection();
	}
	setDirection(direction, easeDuration) {
		if (easeDuration !== void 0) this.setEaseDuration(easeDuration);
		this.direction = direction;
		return this;
	}
	toggleDirection(easeDuration) {
		this.setDirection(this.direction + 2, easeDuration);
		return this;
	}
	get easeDirectionProgress() {
		return this._easeDirectionProgress;
	}
	set easeDirectionProgress(value) {
		if (this._easeDirectionProgress === value) return;
		this._easeDirectionProgress = value;
		this.dirty = true;
	}
	setPadding(left, top, right, bottom) {
		if (typeof left === "object") {
			var config = left;
			var x = GetValue$213(config, "x", null);
			if (x !== null) {
				left = x;
				right = x;
			} else {
				left = GetValue$213(config, "left", 0);
				right = GetValue$213(config, "right", left);
			}
			var y = GetValue$213(config, "y", null);
			if (y !== null) {
				top = y;
				bottom = y;
			} else {
				top = GetValue$213(config, "top", 0);
				bottom = GetValue$213(config, "bottom", top);
			}
		} else {
			if (left === void 0) left = 0;
			if (top === void 0) top = left;
			if (right === void 0) right = left;
			if (bottom === void 0) bottom = top;
		}
		if (this.padding === void 0) this.padding = {};
		this.dirty = this.dirty || this.padding.left != left || this.padding.top != top || this.padding.right != right || this.padding.bottom != bottom;
		this.padding.left = left;
		this.padding.top = top;
		this.padding.right = right;
		this.padding.bottom = bottom;
		this.setRadius();
		return this;
	}
	get radius() {
		return this._radius;
	}
	set radius(value) {
		this.dirty = this.dirty || this._radius != value;
		this._radius = value;
	}
	setRadius(radius) {
		this.radius = radius;
		this.shapeMode = radius == null ? 0 : 1;
		return this;
	}
	get verticeRotation() {
		return this._verticeRotation;
	}
	set verticeRotation(value) {
		this.dirty = this.dirty || this._verticeRotation != value;
		this._verticeRotation = value;
	}
	setVerticeRotation(rotation) {
		this.verticeRotation = rotation;
		return this;
	}
	get verticeAngle() {
		return RadToDeg$7(this.verticeRotation);
	}
	set verticeAngle(value) {
		this.verticeRotation = DegToRad$8(value);
	}
	setVerticeAngle(angle) {
		this.verticeAngle = angle;
		return this;
	}
};
var DirectionNameMap = {
	right: 0,
	down: 1,
	left: 2,
	up: 3
};
var ParseDirection = function(direction) {
	if (typeof direction === "string") direction = DirectionNameMap[direction];
	direction = direction % 4;
	return direction;
};
Object.assign(Triangle.prototype, ShapesUpdateMethods_default$1, EaseDirectionMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/triangle/Triangle.js
var Triangle_default = Triangle;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/triangle/Factory.js
ObjectFactory.register("triangle", function(x, y, width, height, fillColor, fillAlpha) {
	var gameObject = new Triangle_default(this.scene, x, y, width, height, fillColor, fillAlpha);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.Triangle", Triangle_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/renderer/WebGLRenderer.js
var SKIP_CHECK_BLEND_MODE$1 = __webpack_exports__BlendModes.SKIP_CHECK;
var WebGLRenderer$1 = function(renderer, container, drawingContext, parentMatrix, renderStep, displayList, displayListIndex) {
	var camera = drawingContext.camera;
	camera.addToRenderList(container);
	if (!container.layerRendererEnable) return;
	var rendererLayer = container.rendererLayer;
	if (!rendererLayer) return;
	var children = rendererLayer.list;
	var childCount = children.length;
	if (childCount === 0) return;
	var currentContext = drawingContext;
	rendererLayer.depthSort();
	var layerHasBlendMode = container.blendMode !== SKIP_CHECK_BLEND_MODE$1;
	var useStencilChildrenMask = container.childrenMaskGameObject && container.useStencilChildrenMask;
	if (!layerHasBlendMode && currentContext.blendMode !== 0) {
		currentContext = currentContext.getClone();
		currentContext.setBlendMode(0);
		currentContext.use();
	}
	if (useStencilChildrenMask) PushStencilMask(renderer, container.childrenMaskGameObject, currentContext);
	for (var i = 0; i < childCount; i++) {
		var child = children[i];
		if (!child.willRender(camera)) continue;
		if (!layerHasBlendMode && child.blendMode !== currentContext.blendMode && child.blendMode !== SKIP_CHECK_BLEND_MODE$1) {
			currentContext = currentContext.getClone();
			currentContext.setBlendMode(child.blendMode);
			currentContext.use();
		}
		child.renderWebGLStep(renderer, child, currentContext, void 0, void 0, children, i);
	}
	if (useStencilChildrenMask) PopStencilMask(renderer, container.childrenMaskGameObject, currentContext);
	if (currentContext !== drawingContext) currentContext.release();
};
var PushStencilMask = function(renderer, maskGameObject, drawingContext) {
	RenderStencilMask(renderer, maskGameObject, drawingContext, true);
};
var PopStencilMask = function(renderer, maskGameObject, drawingContext) {
	RenderStencilMask(renderer, maskGameObject, drawingContext, false);
};
var RenderStencilMask = function(renderer, maskGameObject, drawingContext, push) {
	var gl = renderer.gl;
	var opIncr = gl.INCR_WRAP;
	var opDecr = gl.DECR_WRAP;
	var fillOp = push ? opIncr : opDecr;
	var maskOp = push ? opDecr : opIncr;
	var currentContext = drawingContext.getClone();
	currentContext.setAlphaStrategy(renderer.config.stencilAlphaStrategy);
	currentContext.setColorWritemask(false, false, false, false);
	currentContext.setStencil(true, gl.ALWAYS, 0, 255, fillOp, fillOp, fillOp, 0, 255);
	currentContext.use();
	renderer.renderNodes.getNode("FillCamera").run(currentContext, 4278190080, drawingContext.useCanvas);
	currentContext = currentContext.getClone();
	currentContext.use();
	currentContext.setStencil(true, gl.ALWAYS, 0, 255, maskOp, maskOp, maskOp, 0, 255);
	maskGameObject.renderWebGLStep(renderer, maskGameObject, currentContext);
	currentContext.release();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/renderer/CanvasRenderer.js
var CanvasRenderer = function(renderer, container, camera) {
	camera.addToRenderList(container);
	if (!container.layerRendererEnable) return;
	var rendererLayer = container.rendererLayer;
	if (!rendererLayer) return;
	var children = rendererLayer.list;
	if (children.length === 0) return;
	rendererLayer.depthSort();
	var layerHasBlendMode = container.blendMode !== -1;
	if (!layerHasBlendMode) renderer.setBlendMode(0);
	if (container.mask) container.mask.preRenderCanvas(renderer, null, camera);
	for (var i = 0; i < children.length; i++) {
		var child = children[i];
		if (!child.willRender(camera)) continue;
		if (!layerHasBlendMode && child.blendMode !== renderer.currentBlendMode) renderer.setBlendMode(child.blendMode);
		child.renderCanvas(renderer, child, camera);
	}
	if (container.mask) container.mask.postRenderCanvas(renderer);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/renderer/Renderer.js
var Renderer_default = {
	renderWebGL: WebGLRenderer$1,
	renderCanvas: CanvasRenderer
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/renderer/ChildrenDisplayList.js
var List = __webpack_exports__Structs.List;
var StableSort = __webpack_exports__Utils.Array.StableSort;
var GameObjectEvents = __webpack_exports__GameObjects.Events;
var SceneEvents = __webpack_exports__Scenes.Events;
var ChildrenDisplayList = class extends List {
	constructor(parent) {
		super(parent.scene);
		this.parent = parent;
		this.scene = parent.scene;
		this.events = this.scene.sys.events;
		this.active = false;
		this.isRexContainerLiteLayer = true;
		this.sortChildrenFlag = false;
		this.addCallback = this.addChildCallback;
		this.removeCallback = this.removeChildCallback;
	}
	addChildCallback(gameObject) {
		var displayList = gameObject.displayList;
		if (displayList && displayList !== this) gameObject.removeFromDisplayList();
		if (gameObject.parentContainer) gameObject.parentContainer.remove(gameObject);
		if (!gameObject.displayList) {
			this.queueDepthSort();
			gameObject.displayList = this;
			gameObject.emit(GameObjectEvents.ADDED_TO_SCENE, gameObject, this.scene);
			this.events.emit(SceneEvents.ADDED_TO_SCENE, gameObject, this.scene);
		}
	}
	removeChildCallback(gameObject) {
		this.queueDepthSort();
		if (gameObject.displayList === this) gameObject.displayList = null;
		gameObject.emit(GameObjectEvents.REMOVED_FROM_SCENE, gameObject, this.scene);
		this.events.emit(SceneEvents.REMOVED_FROM_SCENE, gameObject, this.scene);
	}
	queueDepthSort() {
		this.sortChildrenFlag = true;
		return this;
	}
	depthSort() {
		if (!this.sortChildrenFlag || this.list.length < 2) {
			this.sortChildrenFlag = false;
			return this;
		}
		StableSort(this.list, this.sortByDepth);
		this.sortChildrenFlag = false;
		return this;
	}
	sortByDepth(childA, childB) {
		return childA._depth - childB._depth;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/utils/GetLocalState.js
var DegToRad$7 = __webpack_exports__Math.DegToRad;
var RadToDeg$6 = __webpack_exports__Math.RadToDeg;
var GetLocalState = function(gameObject) {
	if (!gameObject.hasOwnProperty("rexContainer")) {
		var rexContainer = {
			parent: null,
			self: null,
			layer: null,
			x: 0,
			y: 0,
			syncPosition: true,
			rotation: 0,
			syncRotation: true,
			scaleX: 0,
			scaleY: 0,
			syncScale: true,
			alpha: 0,
			syncAlpha: true,
			syncScrollFactor: true,
			syncCameraFilter: true,
			syncDisplayList: true,
			visible: true,
			active: true
		};
		Object.defineProperty(rexContainer, "angle", {
			get: function() {
				return RadToDeg$6(this.rotation);
			},
			set: function(value) {
				this.rotation = DegToRad$7(value);
			}
		});
		Object.defineProperty(rexContainer, "displayWidth", {
			get: function() {
				return gameObject.width * this.scaleX;
			},
			set: function(width) {
				this.scaleX = width / gameObject.width;
			}
		});
		Object.defineProperty(rexContainer, "displayHeight", {
			get: function() {
				return gameObject.height * this.scaleY;
			},
			set: function(height) {
				this.scaleY = height / gameObject.height;
			}
		});
		gameObject.rexContainer = rexContainer;
	}
	return gameObject.rexContainer;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/p3container/GetValidChildren.js
var GetSourceList = function(gameObject) {
	if (gameObject.displayList) return gameObject.displayList;
	else if (gameObject.parentContainer) return gameObject.parentContainer.list;
	return null;
};
var GetValidChildren = function(parent, includeParent) {
	if (includeParent === void 0) includeParent = true;
	var children = parent.getAllChildren(includeParent ? [parent] : void 0);
	var targetList = includeParent ? GetSourceList(parent) : null;
	children = children.filter(function(gameObject) {
		var sourceList = GetSourceList(gameObject);
		if (!sourceList) return false;
		if (!targetList) targetList = sourceList;
		return sourceList === targetList;
	});
	return children;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/p3container/AddToContainer.js
var SetLayerState = function(gameObjects, layer) {
	for (var i = 0, cnt = gameObjects.length; i < cnt; i++) GetLocalState(gameObjects[i]).layer = layer;
};
var AddToContainer = function(p3Container, config) {
	if (config === void 0) config = {};
	var { includeParent = false, setLayerState = false, clearDepthSort = false } = config;
	var gameObjects = GetValidChildren(this, includeParent);
	SortGameObjectsByDepth(gameObjects);
	p3Container.add(gameObjects);
	if (setLayerState) SetLayerState(gameObjects, p3Container);
	if (clearDepthSort) p3Container.sortChildrenFlag = false;
	return gameObjects;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/Base.js
CheckPhaserVersion();
var Zone$1 = __webpack_exports__GameObjects.Zone;
var AddItem = __webpack_exports__Utils.Array.Add;
var RemoveItem$6 = __webpack_exports__Utils.Array.Remove;
var SKIP_CHECK_BLEND_MODE = __webpack_exports__BlendModes.SKIP_CHECK;
var Base$4 = class extends Zone$1 {
	constructor(scene, x, y, width, height) {
		if (x === void 0) x = 0;
		if (y === void 0) y = 0;
		if (width === void 0) width = 1;
		if (height === void 0) height = 1;
		super(scene, x, y, width, height);
		this.children = [];
		this.layerRendererEnable = false;
		this.rendererLayer = void 0;
		this.setBlendMode(SKIP_CHECK_BLEND_MODE);
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		if (fromScene) {
			var child;
			for (var i = this.children.length - 1; i >= 0; i--) {
				child = this.children[i];
				if (!child.parentContainer && (!child.displayList || child.displayList === this.rendererLayer)) child.destroy(fromScene);
			}
		}
		this.clear(!fromScene);
		super.destroy(fromScene);
		this.rendererLayer = void 0;
	}
	contains(gameObject) {
		return this.children.indexOf(gameObject) !== -1;
	}
	add(gameObjects) {
		AddItem(this.children, gameObjects, 0, function(gameObject) {
			gameObject.once("destroy", this.onChildDestroy, this);
			this.addChildCallback(gameObject);
		}, this);
		return this;
	}
	remove(gameObjects, destroyChild) {
		RemoveItem$6(this.children, gameObjects, function(gameObject) {
			gameObject.off("destroy", this.onChildDestroy, this);
			this.removeChildCallback(gameObject, destroyChild);
			if (destroyChild) gameObject.destroy();
		}, this);
		return this;
	}
	addChildCallback(gameObject) {
		var layer = this.rendererLayer;
		if (layer) layer.add(gameObject);
	}
	removeChildCallback(gameObject, destroyChild) {
		var layer = this.rendererLayer;
		if (layer) layer.remove(gameObject);
	}
	onChildDestroy(child, fromScene) {
		this.remove(child, false);
	}
	clear(destroyChild) {
		var gameObject;
		var children = this.children.slice();
		for (var i = 0, cnt = children.length; i < cnt; i++) {
			gameObject = children[i];
			if (!gameObject) continue;
			gameObject.off("destroy", this.onChildDestroy, this);
			this.removeChildCallback(gameObject, destroyChild);
			if (destroyChild) gameObject.destroy();
		}
		this.children.length = 0;
		return this;
	}
	enableLayerRenderer() {
		if (this.layerRendererEnable) return this;
		this.layerRendererEnable = true;
		var rendererLayer = new ChildrenDisplayList(this);
		this.rendererLayer = rendererLayer;
		AddToContainer.call(this, rendererLayer, {
			includeParent: false,
			setLayerState: true,
			clearDepthSort: true
		});
		return this;
	}
	enableFilters() {
		if (this.filterCamera || !this.scene.renderer.gl) return this;
		this.enableLayerRenderer();
		this.setFiltersFocusContext(true);
		super.enableFilters();
		return this;
	}
};
var Components$1 = __webpack_exports__GameObjects.Components;
__webpack_exports__Class.mixin(Base$4, [Components$1.Alpha, Components$1.Flip]);
Object.assign(Base$4.prototype, Renderer_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/GetParent.js
var GetParent$1 = function(gameObject, name) {
	var parent;
	if (name === void 0) {
		if (gameObject.hasOwnProperty("rexContainer")) parent = gameObject.rexContainer.parent;
	} else {
		parent = GetParent$1(gameObject);
		while (parent) {
			if (parent.name === name) break;
			parent = GetParent$1(parent);
		}
	}
	return parent;
};
var GetTopmostParent$1 = function(gameObject) {
	var parent = GetParent$1(gameObject);
	while (parent) {
		gameObject = parent;
		parent = GetParent$1(parent);
	}
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/Parent.js
var Parent_default = {
	setParent(gameObject, parent) {
		if (parent === void 0) parent = this;
		var localState = GetLocalState(gameObject);
		if (parent) {
			localState.parent = parent;
			localState.self = gameObject;
		} else {
			localState.parent = null;
			localState.self = null;
		}
		return this;
	},
	getParent(gameObject, name) {
		if (typeof gameObject === "string") {
			name = gameObject;
			gameObject = void 0;
		}
		if (gameObject === void 0) gameObject = this;
		return GetParent$1(gameObject, name);
	},
	getTopmostParent(gameObject) {
		if (gameObject === void 0) gameObject = this;
		return GetTopmostParent$1(gameObject);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/AddChild.js
var GetValue$212 = __webpack_exports__Utils.Objects.GetValue;
var BaseAdd = Base$4.prototype.add;
var Add$8 = function(gameObject, config) {
	this.setParent(gameObject);
	var state = GetLocalState(gameObject);
	SetupSyncFlags(state, config);
	this.resetChildState(gameObject).updateChildVisible(gameObject).updateChildActive(gameObject).updateChildScrollFactor(gameObject).updateChildMask(gameObject).updateCameraFilter(gameObject);
	BaseAdd.call(this, gameObject);
	SyncDisplayList$1.call(this, gameObject, state);
	return this;
};
var AddLocal = function(gameObject, config) {
	this.setParent(gameObject);
	var state = GetLocalState(gameObject);
	SetupSyncFlags(state, config);
	state.x = gameObject.x;
	state.y = gameObject.y;
	state.rotation = gameObject.rotation;
	state.scaleX = gameObject.scaleX;
	state.scaleY = gameObject.scaleY;
	state.alpha = gameObject.alpha;
	state.visible = gameObject.visible;
	state.active = gameObject.active;
	this.updateChildPosition(gameObject).updateChildAlpha(gameObject).updateChildVisible(gameObject).updateChildActive(gameObject).updateChildScrollFactor(gameObject).updateChildMask(gameObject);
	BaseAdd.call(this, gameObject);
	SyncDisplayList$1.call(this, gameObject, state);
	return this;
};
var SetupSyncFlags = function(state, config) {
	if (config === void 0) config = true;
	if (typeof config === "boolean") {
		state.syncPosition = config;
		state.syncRotation = config;
		state.syncScale = config;
		state.syncAlpha = config;
		state.syncScrollFactor = config;
		state.syncCameraFilter = config;
		state.syncDisplayList = config;
	} else {
		state.syncPosition = GetValue$212(config, "syncPosition", true);
		state.syncRotation = GetValue$212(config, "syncRotation", true);
		state.syncScale = GetValue$212(config, "syncScale", true);
		state.syncAlpha = GetValue$212(config, "syncAlpha", true);
		state.syncScrollFactor = GetValue$212(config, "syncScrollFactor", true);
		state.syncCameraFilter = GetValue$212(config, "syncCameraFilter", true);
		state.syncDisplayList = GetValue$212(config, "syncDisplayList", true);
	}
};
var SyncDisplayList$1 = function(gameObject, state) {
	this.addToParentContainer(gameObject);
	if (state.syncDisplayList) this.addToPatentLayer(gameObject);
};
var AddChild_default = {
	add(gameObject) {
		if (Array.isArray(gameObject)) this.addMultiple(gameObject);
		else Add$8.call(this, gameObject);
		return this;
	},
	pin(gameObject, config) {
		if (Array.isArray(gameObject)) this.addMultiple(gameObject, config);
		else Add$8.call(this, gameObject, config);
		return this;
	},
	addMultiple(gameObjects) {
		var args = Array.from(arguments);
		for (var i = 0, cnt = gameObjects.length; i < cnt; i++) {
			args[0] = gameObjects[i];
			this.add.apply(this, args);
		}
		return this;
	},
	addLocal(gameObject) {
		if (Array.isArray(gameObject)) this.addMultiple(gameObject);
		else AddLocal.call(this, gameObject);
		return this;
	},
	pinLocal(gameObject, config) {
		if (Array.isArray(gameObject)) this.addMultiple(gameObject, config);
		else AddLocal.call(this, gameObject, config);
		return this;
	},
	addLocalMultiple(gameObjects) {
		for (var i = 0, cnt = gameObjects.length; i < cnt; i++) AddLocal.call(this, gameObjects[i]);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/RemoveChild.js
var BaseRemove = Base$4.prototype.remove;
var BaseClear = Base$4.prototype.clear;
var RemoveChild_default = {
	remove(gameObject, destroyChild) {
		if (GetParent$1(gameObject) !== this) return this;
		this.setParent(gameObject, null);
		BaseRemove.call(this, gameObject, destroyChild);
		return this;
	},
	unpin(gameObject, destroyChild) {
		if (GetParent$1(gameObject) !== this) return this;
		this.setParent(gameObject, null);
		BaseRemove.call(this, gameObject, destroyChild);
		return this;
	},
	clear(destroyChild) {
		var children = this.children;
		for (var i = 0, cnt = children.length; i < cnt; i++) {
			var child = children[i];
			this.setParent(child, null);
		}
		BaseClear.call(this, destroyChild);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/ChildState.js
var ChildState_default = {
	getLocalState(gameObject) {
		return GetLocalState(gameObject);
	},
	resetChildState(gameObject) {
		this.resetChildPositionState(gameObject).resetChildVisibleState(gameObject).resetChildAlphaState(gameObject).resetChildActiveState(gameObject);
		return this;
	},
	resetChildrenState(gameObjects) {
		for (var i = 0, cnt = gameObjects.length; i < cnt; i++) this.resetChildState(gameObjects[i]);
		return this;
	},
	syncProperties() {
		this.syncPosition().syncVisible().syncAlpha().syncActive().syncScrollFactor().syncMask();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/Transform.js
var Transform_default = {
	worldToLocal(point) {
		point.x -= this.x;
		point.y -= this.y;
		var c = Math.cos(-this.rotation);
		var s = Math.sin(-this.rotation);
		var tx = point.x;
		var ty = point.y;
		point.x = tx * c - ty * s;
		point.y = tx * s + ty * c;
		point.x /= this.scaleX;
		point.y /= this.scaleY;
		return point;
	},
	localToWorld(point) {
		point.x *= this.scaleX;
		point.y *= this.scaleY;
		var c = Math.cos(this.rotation);
		var s = Math.sin(this.rotation);
		var tx = point.x;
		var ty = point.y;
		point.x = tx * c - ty * s;
		point.y = tx * s + ty * c;
		point.x += this.x;
		point.y += this.y;
		return point;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/utils/GetScale.js
var GetScale = function(a, b) {
	if (a === b) return 1;
	else return a / b;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/Position.js
var Position_default = {
	updateChildPosition(child) {
		if (child.isRexContainerLite) child.syncChildrenEnable = false;
		var localState = GetLocalState(child);
		var parent = localState.parent;
		if (localState.syncPosition) {
			child.x = localState.x;
			child.y = localState.y;
			parent.localToWorld(child);
		}
		if (localState.syncRotation) child.rotation = localState.rotation + parent.rotation;
		if (localState.syncScale) {
			child.scaleX = localState.scaleX * parent.scaleX;
			child.scaleY = localState.scaleY * parent.scaleY;
		}
		if (child.isRexContainerLite) {
			child.syncChildrenEnable = true;
			child.syncPosition();
		}
		return this;
	},
	syncPosition() {
		if (this.syncChildrenEnable) this.children.forEach(this.updateChildPosition, this);
		return this;
	},
	resetChildPositionState(child) {
		var localState = GetLocalState(child);
		var parent = localState.parent;
		localState.x = child.x;
		localState.y = child.y;
		parent.worldToLocal(localState);
		localState.scaleX = GetScale(child.scaleX, parent.scaleX);
		localState.scaleY = GetScale(child.scaleY, parent.scaleY);
		localState.rotation = child.rotation - parent.rotation;
		return this;
	},
	setChildPosition(child, x, y) {
		child.x = x;
		child.y = y;
		this.resetChildPositionState(child);
		return this;
	},
	setChildLocalPosition(child, x, y) {
		var localState = GetLocalState(child);
		localState.x = x;
		localState.y = y;
		this.updateChildPosition(child);
		return this;
	},
	resetLocalPositionState() {
		var parent = GetLocalState(this).parent;
		if (parent) parent.resetChildPositionState(this);
		return this;
	},
	getChildLocalX(child) {
		return GetLocalState(child).x;
	},
	getChildLocalY(child) {
		return GetLocalState(child).y;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/Rotation.js
var DegToRad$6 = __webpack_exports__Math.DegToRad;
var Rotation_default = {
	updateChildRotation(child) {
		var localState = GetLocalState(child);
		var parent = localState.parent;
		if (localState.syncRotation) child.rotation = parent.rotation + localState.rotation;
		return this;
	},
	syncRotation() {
		if (this.syncChildrenEnable) this.children.forEach(this.updateChildRotation, this);
		return this;
	},
	resetChildRotationState(child) {
		var localState = GetLocalState(child);
		var parent = localState.parent;
		localState.rotation = child.rotation - parent.rotation;
		return this;
	},
	setChildRotation(child, rotation) {
		child.rotation = rotation;
		this.resetChildRotationState(child);
		return this;
	},
	setChildAngle(child, angle) {
		child.angle = angle;
		this.resetChildRotationState(child);
		return this;
	},
	setChildLocalRotation(child, rotation) {
		var localState = GetLocalState(child);
		localState.rotation = rotation;
		this.updateChildRotation(child);
		return this;
	},
	setChildLocalAngle(child, angle) {
		var localState = GetLocalState(child);
		localState.rotation = DegToRad$6(angle);
		this.updateChildRotation(child);
		return this;
	},
	resetLocalRotationState() {
		var parent = GetLocalState(this).parent;
		if (parent) parent.resetChildRotationState(this);
		return this;
	},
	getChildLocalRotation(child) {
		return GetLocalState(child).rotation;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/Scale.js
var Scale_default = {
	updateChildScale(child) {
		var state = GetLocalState(child);
		var parent = state.parent;
		if (state.syncScale) {
			child.scaleX = parent.scaleX * state.scaleX;
			child.scaleY = parent.scaleY * state.scaleY;
		}
		return this;
	},
	syncScale() {
		if (this.syncChildrenEnable) this.children.forEach(this.updateChildScale, this);
		return this;
	},
	resetChildScaleState(child) {
		var state = GetLocalState(child);
		var parent = state.parent;
		state.scaleX = GetScale(child.scaleX, parent.scaleX);
		state.scaleY = GetScale(child.scaleY, parent.scaleY);
		return this;
	},
	setChildScale(child, scaleX, scaleY) {
		if (scaleY === void 0) scaleY = scaleX;
		child.scaleX = scaleX;
		child.scaleY = scaleY;
		this.resetChildScaleState(child);
		return this;
	},
	setChildScaleX(child, scaleX) {
		child.scaleX = scaleX;
		this.resetChildScaleState(child);
		return this;
	},
	setChildScaleY(child, scaleY) {
		child.scaleY = scaleY;
		this.resetChildScaleState(child);
		return this;
	},
	setChildLocalScale(child, scaleX, scaleY) {
		if (scaleY === void 0) scaleY = scaleX;
		var state = GetLocalState(child);
		state.scaleX = scaleX;
		state.scaleY = scaleY;
		this.updateChildScale(child);
		return this;
	},
	setChildLocalScaleX(child, scaleX) {
		var state = GetLocalState(child);
		state.scaleX = scaleX;
		this.updateChildScale(child);
		return this;
	},
	setChildLocalScaleY(child, scaleY) {
		var state = GetLocalState(child);
		state.scaleY = scaleY;
		this.updateChildScale(child);
		return this;
	},
	setChildDisplaySize(child, width, height) {
		child.setDisplaySize(width, height);
		this.resetChildScaleState(child);
		return this;
	},
	resetLocalScaleState() {
		var parent = GetLocalState(this).parent;
		if (parent) parent.resetChildScaleState(this);
		return this;
	},
	getChildLocalScaleX(child) {
		return GetLocalState(child).scaleX;
	},
	getChildLocalScaleY(child) {
		return GetLocalState(child).scaleY;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/Visible.js
var Visible_default$1 = {
	updateChildVisible(child) {
		var localState = GetLocalState(child);
		var parent = localState.parent;
		var maskVisible = localState.hasOwnProperty("maskVisible") ? localState.maskVisible : true;
		child.visible = (parent ? parent.visible : true) && localState.visible && maskVisible;
		return this;
	},
	syncVisible() {
		if (this.syncChildrenEnable) this.children.forEach(this.updateChildVisible, this);
		return this;
	},
	resetChildVisibleState(child) {
		var localState = GetLocalState(child);
		if (localState.hasOwnProperty("maskVisible")) delete localState.maskVisible;
		localState.visible = child.visible;
		return this;
	},
	setChildVisible(child, visible) {
		this.setChildLocalVisible(child, visible);
		return this;
	},
	setChildLocalVisible(child, visible) {
		if (visible === void 0) visible = true;
		var localState = GetLocalState(child);
		localState.visible = visible;
		this.updateChildVisible(child);
		return this;
	},
	setChildMaskVisible(child, visible) {
		if (visible === void 0) visible = true;
		var localState = GetLocalState(child);
		localState.maskVisible = visible;
		this.updateChildVisible(child);
		return this;
	},
	resetLocalVisibleState() {
		var parent = GetLocalState(this).parent;
		if (parent) parent.resetChildVisibleState(this);
		return this;
	},
	getChildLocalVisible(child) {
		return GetLocalState(child).visible;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/Alpha.js
var Alpha_default = {
	updateChildAlpha(child) {
		var state = GetLocalState(child);
		var parent = state.parent;
		if (state.syncAlpha) child.alpha = parent.alpha * state.alpha;
		return this;
	},
	syncAlpha() {
		if (this.syncChildrenEnable) this.children.forEach(this.updateChildAlpha, this);
		return this;
	},
	resetChildAlphaState(child) {
		var state = GetLocalState(child);
		var parent = state.parent;
		state.alpha = GetScale(child.alpha, parent.alpha);
		return this;
	},
	setChildAlpha(child, alpha) {
		child.alpha = alpha;
		this.resetChildAlphaState(child);
		return this;
	},
	setChildLocalAlpha(child, alpha) {
		var state = GetLocalState(child);
		state.alpha = alpha;
		this.updateChildAlpha(child);
		return this;
	},
	resetLocalAlphaState() {
		var parent = GetLocalState(this).parent;
		if (parent) parent.resetChildAlphaState(this);
		return this;
	},
	getChildLocalAlpha(child) {
		return GetLocalState(child).alpha;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/Active.js
var Active_default = {
	updateChildActive(child) {
		var localState = GetLocalState(child);
		child.active = localState.parent.active && localState.active;
		return this;
	},
	syncActive() {
		if (this.syncChildrenEnable) this.children.forEach(this.updateChildActive, this);
		return this;
	},
	resetChildActiveState(child) {
		var localState = GetLocalState(child);
		localState.active = child.active;
		return this;
	},
	setChildActive(child, active) {
		child.active = active;
		this.resetChildActiveState(child);
		return this;
	},
	setChildLocalActive(child, active) {
		if (active === void 0) active = true;
		var localState = GetLocalState(child);
		localState.active = active;
		this.updateChildActive(child);
		return this;
	},
	resetLocalActiveState() {
		var parent = GetLocalState(this).parent;
		if (parent) parent.resetChildActiveState(this);
		return this;
	},
	getChildLocalActive(child) {
		return GetLocalState(child).active;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/ScrollFactor.js
var ScrollFactor_default = {
	updateChildScrollFactor(child) {
		var localState = GetLocalState(child);
		var parent = localState.parent;
		if (localState.syncScrollFactor) {
			child.scrollFactorX = parent.scrollFactorX;
			child.scrollFactorY = parent.scrollFactorY;
		}
		return this;
	},
	syncScrollFactor() {
		if (this.syncChildrenEnable) this.children.forEach(this.updateChildScrollFactor, this);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/CameraFilter.js
var CameraFilter_default = {
	updateCameraFilter(child) {
		var state = GetLocalState(child);
		var parent = state.parent;
		if (state.syncCameraFilter) child.cameraFilter = parent.cameraFilter;
		return this;
	},
	syncCameraFilter() {
		if (this.syncChildrenEnable) this.children.forEach(this.updateCameraFilter, this);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/Mask.js
var Mask_default = {
	updateChildMask(child) {
		if (this.mask == null) return this;
		if ((this.mask.hasOwnProperty("geometryMask") ? this.mask.geometryMask : this.mask.bitmapMask) !== child) child.mask = this.mask;
		return this;
	},
	syncMask() {
		if (this.syncChildrenEnable) this.children.forEach(this.updateChildMask, this);
		return this;
	},
	setMask(mask) {
		this.mask = mask;
		return this;
	},
	clearChildrenMask() {
		var children = this.children;
		for (var i = 0, cnt = children.length; i < cnt; i++) {
			var child = children[i];
			if (child.clearMask) child.clearMask(false);
			if (!child.hasOwnProperty("isRexContainerLite")) this.setChildMaskVisible(child);
		}
		return this;
	},
	clearMask(destroyMask) {
		if (destroyMask === void 0) destroyMask = false;
		if (destroyMask && this.mask) mask.destroy();
		this._mask = null;
		this.setChildMaskVisible(this);
		this.clearChildrenMask();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/system/FilterDisplayGameObjects.js
var FilterDisplayGameObjects = function(gameObjects, referanceGameObject) {
	var targetDisplayList = referanceGameObject ? referanceGameObject.displayList : void 0;
	var targetParentContainer = referanceGameObject ? referanceGameObject.parentContainer : void 0;
	return gameObjects.filter(function(gameObject) {
		var displayList = gameObject.displayList;
		var parentContainer = gameObject.parentContainer;
		if (!displayList && !parentContainer) return false;
		if (!referanceGameObject) return true;
		if (displayList) return displayList === targetDisplayList;
		if (parentContainer) return parentContainer === targetParentContainer;
		return false;
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/Depth.js
var Depth_default = {
	setDepth(value, containerOnly) {
		this.depth = value;
		if (!this.layerRendererEnable) {
			if (!containerOnly && this.children) {
				var children = this.getAllChildren();
				for (var i = 0, cnt = children.length; i < cnt; i++) children[i].depth = value;
			}
		}
		return this;
	},
	swapDepth(containerB) {
		var depthA = this.depth;
		var depthB = containerB.depth;
		this.setDepth(depthB);
		containerB.setDepth(depthA);
		return this;
	},
	incDepth(inc) {
		this.depth += inc;
		if (!this.layerRendererEnable) {
			if (this.children) {
				var children = this.getAllChildren();
				for (var i = 0, cnt = children.length; i < cnt; i++) children[i].depth += inc;
			}
		}
		return this;
	},
	bringToTop() {
		var displayList = this.displayList;
		if (!displayList) return this;
		if (!this.layerRendererEnable) {
			var children = this.getAllChildren([this]);
			SortGameObjectsByDepth(children, false);
			for (var i = 0, cnt = children.length; i < cnt; i++) {
				var child = children[i];
				if (displayList.exists(child)) displayList.bringToTop(child);
			}
		} else if (displayList.exists(this)) displayList.bringToTop(this);
		return this;
	},
	bringMeToTop() {
		return this.bringToTop();
	},
	sendToBack() {
		var displayList = this.displayList;
		if (!displayList) return this;
		if (!this.layerRendererEnable) {
			var children = this.getAllChildren([this]);
			SortGameObjectsByDepth(children, true);
			for (var i = 0, cnt = children.length; i < cnt; i++) {
				var child = children[i];
				if (displayList.exists(child)) displayList.sendToBack(child);
			}
		} else if (displayList.exists(this)) displayList.sendToBack(this);
		return this;
	},
	sendMeToBack() {
		return this.sendToBack();
	},
	moveDepthBelow(gameObject) {
		var displayList = this.displayList;
		if (!displayList) return this;
		if (gameObject.displayList !== displayList) return this;
		if (!this.layerRendererEnable) {
			var children = this.getAllChildren([this]);
			SortGameObjectsByDepth(children, false);
			for (var i = 0, cnt = children.length; i < cnt; i++) {
				var child = children[i];
				if (displayList.exists(child)) {
					displayList.moveBelow(gameObject, child);
					break;
				}
			}
		} else if (displayList.exists(this)) displayList.moveBelow(gameObject, this);
		return this;
	},
	moveMyDepthBelow(gameObject) {
		return this.moveDepthBelow(gameObject);
	},
	moveDepthAbove(gameObject) {
		var displayList = this.displayList;
		if (!displayList) return this;
		if (gameObject.displayList !== displayList) return this;
		if (!this.layerRendererEnable) {
			var children = this.getAllChildren([this]);
			SortGameObjectsByDepth(children, true);
			for (var i = 0, cnt = children.length; i < cnt; i++) {
				var child = children[i];
				if (displayList.exists(child)) {
					displayList.moveAbove(gameObject, child);
					break;
				}
			}
		} else if (displayList.exists(this)) displayList.moveAbove(gameObject, this);
		return this;
	},
	moveMyDepthAbove(gameObject) {
		return this.moveDepthAbove(gameObject);
	},
	bringChildToTop(child) {
		if (child === this && this.layerRendererEnable) return this;
		var gameObjects;
		if (child !== this && child.isRexContainerLite && !child.layerRendererEnable) {
			gameObjects = child.getAllChildren([child]);
			gameObjects = FilterDisplayGameObjects(gameObjects, child);
			gameObjects = SortGameObjectsByDepth(gameObjects, false);
		} else gameObjects = [child];
		var topChild;
		if (!this.layerRendererEnable) {
			var children = this.getAllChildren([this]);
			children = FilterDisplayGameObjects(children, child);
			children = SortGameObjectsByDepth(children, false);
			topChild = children[children.length - 1];
		} else topChild = this;
		for (var i = 0, cnt = gameObjects.length; i < cnt; i++) {
			var gameObject = gameObjects[i];
			if (topChild === gameObject) continue;
			if (gameObject !== this && topChild.displayList !== gameObject.displayList) continue;
			topChild.displayList.moveAbove(gameObject, topChild);
			topChild = gameObject;
		}
		return this;
	},
	sendChildToBack(child) {
		if (child === this && this.layerRendererEnable) return this;
		var gameObjects;
		if (child !== this && child.isRexContainerLite && !child.layerRendererEnable) {
			gameObjects = child.getAllChildren([child]);
			gameObjects = FilterDisplayGameObjects(gameObjects, child);
			gameObjects = SortGameObjectsByDepth(gameObjects, false);
		} else gameObjects = [child];
		var bottomChild;
		if (!this.layerRendererEnable) {
			var children = this.getAllChildren([this]);
			children = FilterDisplayGameObjects(children, child);
			children = SortGameObjectsByDepth(children, false);
			bottomChild = children[0];
		} else bottomChild = this;
		for (var i = gameObjects.length - 1; i >= 0; i--) {
			var gameObject = gameObjects[i];
			if (bottomChild === gameObject) continue;
			if (gameObject !== this && bottomChild.displayList !== gameObject.displayList) continue;
			bottomChild.displayList.moveBelow(gameObject, bottomChild);
			bottomChild = gameObject;
		}
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/utils/Traversal.js
var DepthFirstSearch = function(root, callback) {
	if (!callback(root) && root.isRexContainerLite) {
		var children = root.children;
		for (var i = 0, cnt = children.length; i < cnt; i++) DepthFirstSearch(children[i], callback);
	}
};
var BreadthFirstSearch = function(root, callback) {
	var queue = [root];
	while (queue.length > 0) {
		var current = queue.shift();
		if (!callback(current) && current.isRexContainerLite) queue.push(...current.children);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/Children.js
var ArrayUtils = __webpack_exports__Utils.Array;
var Children_default = {
	getChildren(out) {
		if (!out) out = this.children;
		else for (var i = 0, cnt = this.children.length; i < cnt; i++) out.push(this.children[i]);
		return out;
	},
	getAllChildren(out) {
		if (out === void 0) out = [];
		var root = this;
		BreadthFirstSearch(root, function(child) {
			if (child === root) return;
			out.push(child);
		});
		return out;
	},
	getAllVisibleChildren(out) {
		if (out === void 0) out = [];
		var root = this;
		BreadthFirstSearch(root, function(child) {
			if (child === root) return;
			if (!child.visible) return true;
			out.push(child);
		});
		return out;
	},
	bfs(callback, root) {
		if (root === void 0) root = this;
		BreadthFirstSearch(root, callback);
		return this;
	},
	dfs(callback, root) {
		if (root === void 0) root = this;
		DepthFirstSearch(root, callback);
		return this;
	},
	contains(gameObject) {
		var parent = GetParent$1(gameObject);
		if (!parent) return false;
		else if (parent === this) return true;
		else return this.contains(parent);
	},
	getByName(name, recursive) {
		if (!recursive) return ArrayUtils.GetFirst(this.children, "name", name);
		else {
			var queue = [this];
			var parent, child;
			while (queue.length) {
				parent = queue.shift();
				for (var i = 0, cnt = parent.children.length; i < cnt; i++) {
					child = parent.children[i];
					if (child.name === name) return child;
					else if (child.isRexContainerLite) queue.push(child);
				}
			}
			return null;
		}
	},
	getRandom(startIndex, length) {
		return ArrayUtils.GetRandom(this.children, startIndex, length);
	},
	getFirst(property, value, startIndex, endIndex) {
		return ArrayUtils.GetFirstElement(this.children, property, value, startIndex, endIndex);
	},
	getAll(property, value, startIndex, endIndex) {
		return ArrayUtils.GetAll(this.children, property, value, startIndex, endIndex);
	},
	count(property, value, startIndex, endIndex) {
		return ArrayUtils.CountAllMatching(this.children, property, value, startIndex, endIndex);
	},
	swap(child1, child2) {
		ArrayUtils.Swap(this.children, child1, child2);
		return this;
	},
	setAll(property, value, startIndex, endIndex) {
		ArrayUtils.SetAll(this.children, property, value, startIndex, endIndex);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/Tween.js
var GetLocalStates = function(gameObjects) {
	var localStates = [];
	for (var i = 0, cnt = gameObjects.length; i < cnt; i++) {
		var gameObject = gameObjects[i];
		if (!gameObject.hasOwnProperty("rexContainer")) continue;
		localStates.push(gameObject.rexContainer);
	}
	return localStates;
};
var GetScene = function(gameObjects) {
	for (var i = 0, cnt = gameObjects.length; i < cnt; i++) {
		var scene = gameObjects[i].scene;
		if (scene) return scene;
	}
	return null;
};
var UpdateChild = function(tween, key, target) {
	if (!target.parent) {
		tween.remove();
		return;
	}
	var parent = target.parent;
	var child = target.self;
	switch (key) {
		case "x":
		case "y":
			parent.updateChildPosition(child);
			break;
		case "angle":
		case "rotation":
			parent.updateChildRotation(child);
			break;
		case "scaleX":
		case "scaleY":
		case "displayWidth":
		case "displayHeight":
			parent.updateChildScale(child);
			break;
		case "alpha":
			parent.updateChildAlpha(child);
			break;
		default:
			parent.updateChildPosition(child);
			parent.updateChildRotation(child);
			parent.updateChildScale(child);
			parent.updateChildAlpha(child);
	}
};
var Tween_default = {
	tweenChild(tweenConfig) {
		var targets = tweenConfig.targets;
		if (!Array.isArray(targets)) targets = [targets];
		var scene = this.scene || GetScene(targets);
		if (!scene) return;
		tweenConfig.targets = GetLocalStates(targets);
		var tween = scene.tweens.add(tweenConfig);
		tween.on("update", UpdateChild);
		return tween;
	},
	tweenSelf(tweenConfig) {
		tweenConfig.targets = [this];
		return this.tweenChild(tweenConfig);
	},
	createTweenChildConfig(tweenConfig) {
		var targets = tweenConfig.targets;
		if (targets) {
			if (!Array.isArray(targets)) targets = [targets];
			tweenConfig.targets = GetLocalStates(targets);
		}
		var onUpdate = tweenConfig.onUpdate;
		tweenConfig.onUpdate = function(tween, target) {
			if (onUpdate) onUpdate(tween, target);
			UpdateChild(tween, void 0, target);
		};
		return tweenConfig;
	},
	tween(tweenConfig) {
		var scene = this.scene;
		if (!tweenConfig.targets) tweenConfig.targets = this;
		return scene.tweens.add(tweenConfig);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/p3container/RemoveFromContainer.js
var ClearLayerState = function(gameObjects, layer) {
	for (var i = 0, cnt = gameObjects.length; i < cnt; i++) {
		var gameObject = gameObjects[i];
		if (!gameObject.hasOwnProperty("rexContainer")) continue;
		var state = GetLocalState(gameObject);
		if (state.layer === layer) state.layer = null;
	}
};
var RemoveFromContainer = function(p3Container, descending, addToScene) {
	if (!this.scene) return;
	var gameObjects = GetValidChildren(this);
	SortGameObjectsByDepth(gameObjects, descending);
	p3Container.remove(gameObjects);
	ClearLayerState(gameObjects, p3Container);
	if (addToScene) gameObjects.forEach(function(gameObject) {
		gameObject.addToDisplayList();
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/P3Container.js
var IsLayer = function(gameObject) {
	return gameObject && (IsLayerGameObject(gameObject) || gameObject.isRexContainerLiteLayer);
};
var P3Container_default = {
	addToContainer(p3Container) {
		this._setParentContainerFlag = true;
		AddToContainer.call(this, p3Container, {
			includeParent: true,
			setLayerState: false,
			clearDepthSort: false
		});
		this._setParentContainerFlag = false;
		return this;
	},
	addToLayer(layer) {
		AddToContainer.call(this, layer, {
			includeParent: true,
			setLayerState: false,
			clearDepthSort: false
		});
		return this;
	},
	removeFromContainer() {
		if (!this.parentContainer) return this;
		this._setParentContainerFlag = true;
		RemoveFromContainer.call(this, this.parentContainer, true, false);
		this._setParentContainerFlag = false;
		return this;
	},
	removeFromLayer(addToScene) {
		if (addToScene === void 0) addToScene = true;
		if (!IsLayer(this.displayList)) return this;
		RemoveFromContainer.call(this, this.displayList, false, addToScene);
		return this;
	},
	getParentContainer() {
		if (this.parentContainer) return this.parentContainer;
		var parent = this.getParent();
		while (parent) {
			var p3Container = parent.parentContainer;
			if (p3Container) return p3Container;
			parent = parent.getParent();
		}
		return null;
	},
	addToParentContainer(gameObject) {
		if (!gameObject.displayList) return this;
		var p3Container = this.getParentContainer();
		if (!p3Container) return this;
		if (gameObject.isRexContainerLite) gameObject.addToContainer(p3Container);
		else p3Container.add(gameObject);
		return this;
	},
	addToPatentLayer(gameObject) {
		if (!gameObject.displayList) return this;
		var parentLayer = this.displayList;
		if (parentLayer === gameObject.displayList) return this;
		if (IsLayer(parentLayer)) {
			if (gameObject.isRexContainerLite) gameObject.addToLayer(parentLayer);
			else parentLayer.add(gameObject);
		}
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/RendererLayer.js
var GetRendererLayer = function() {
	if (this.rendererLayer) return this.rendererLayer;
	var parent = this.getParent();
	while (parent) {
		if (parent.rendererLayer) return parent.rendererLayer;
		parent = parent.getParent();
	}
	return null;
};
var RendererLayer_default = {
	hasLayer() {
		return this.layerRendererEnable;
	},
	enableLayer() {
		this.enableLayerRenderer();
		return this;
	},
	getLayer() {
		this.enableLayerRenderer();
		return this;
	},
	addChildCallback(gameObject) {
		if (!gameObject.displayList) return;
		var layer = GetRendererLayer.call(this);
		if (!layer) return;
		if (layer === gameObject.displayList) return;
		if (gameObject.isRexContainerLite) AddToContainer.call(gameObject, layer, {
			includeParent: true,
			setLayerState: true,
			clearDepthSort: false
		});
		else layer.add(gameObject);
		var state = GetLocalState(gameObject);
		state.layer = layer;
	},
	removeChildCallback(gameObject, destroyChild) {
		var state = GetLocalState(gameObject);
		var layer = state.layer;
		if (!layer) return;
		if (gameObject.isRexContainerLite) gameObject.removeFromLayer(true);
		else layer.remove(gameObject);
		state.layer = null;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/bounds/GetBoundsOfGameObjects.js
var Rectangle$1 = __webpack_exports__Geom.Rectangle;
var Union = __webpack_exports__Geom.Rectangle.Union;
var GetBoundsOfGameObjects = function(gameObjects, out) {
	if (out === void 0) out = new Rectangle$1();
	else if (out === true) {
		if (GlobRect$1 === void 0) GlobRect$1 = new Rectangle$1();
		out = GlobRect$1;
	}
	out.setTo(0, 0, 0, 0);
	var gameObject;
	var firstClone = true;
	for (var i = 0, cnt = gameObjects.length; i < cnt; i++) {
		gameObject = gameObjects[i];
		if (!gameObject.getBounds) continue;
		var boundsRect = GetBounds(gameObject, true);
		if (firstClone) {
			out.setTo(boundsRect.x, boundsRect.y, boundsRect.width, boundsRect.height);
			firstClone = false;
		} else Union(boundsRect, out, out);
	}
	return out;
};
var GlobRect$1;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/rendertexture/Snapshot.js
var GetValue$211 = __webpack_exports__Utils.Objects.GetValue;
var Snapshot = function(config) {
	if (!config) return;
	var gameObjects = config.gameObjects;
	if (!Array.isArray(gameObjects)) gameObjects = [gameObjects];
	var renderTexture = config.renderTexture;
	var saveTexture = config.saveTexture;
	var x = GetValue$211(config, "x", void 0);
	var y = GetValue$211(config, "y", void 0);
	var width = GetValue$211(config, "width", void 0);
	var height = GetValue$211(config, "height", void 0);
	var originX = GetValue$211(config, "originX", 0);
	var originY = GetValue$211(config, "originY", 0);
	var padding = GetValue$211(config, "padding", 0);
	var scrollX, scrollY;
	if (width === void 0 || height === void 0 || x === void 0 || y === void 0) {
		var bounds = GetBoundsOfGameObjects(gameObjects, true);
		if (x !== void 0 && y !== void 0) {
			width = Math.max(x - bounds.left, bounds.right - x) * 2;
			height = Math.max(y - bounds.top, bounds.bottom - y) * 2;
			originX = .5;
			originY = .5;
		} else {
			x = bounds.x;
			y = bounds.y;
			width = bounds.width;
			height = bounds.height;
			originX = 0;
			originY = 0;
		}
		scrollX = bounds.x;
		scrollY = bounds.y;
	} else {
		scrollX = x + (0 - originX) * width;
		scrollY = y + (0 - originY) * height;
	}
	scrollX -= padding;
	scrollY -= padding;
	width += padding * 2;
	height += padding * 2;
	var scene = gameObjects[0].scene;
	var textureManager = scene.sys.textures;
	if (saveTexture && !renderTexture) renderTexture = textureManager.addDynamicTexture(saveTexture, width, height);
	if (!renderTexture) renderTexture = scene.add.renderTexture(0, 0, width, height);
	if (renderTexture.setPosition) renderTexture.setPosition(x, y);
	if (renderTexture.width !== width || renderTexture.height !== height) renderTexture.setSize(width, height);
	if (renderTexture.setOrigin) renderTexture.setOrigin(originX, originY);
	renderTexture.camera.setScroll(scrollX, scrollY);
	gameObjects = SortGameObjectsByDepth(Clone$2(gameObjects));
	renderTexture.draw(gameObjects).render();
	if (saveTexture) {
		if (IsGameObject(renderTexture)) renderTexture.saveTexture(saveTexture);
		else if (renderTexture.key !== saveTexture) textureManager.renameTexture(renderTexture.key, key);
	}
	return renderTexture;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/RenderTexture.js
var RenderTexture_default = { snapshot(config) {
	var scaleXSave = this.scaleX;
	var scaleYSave = this.scaleY;
	var scale1 = scaleXSave === 1 && scaleYSave === 1;
	if (!scale1) this.setScale(1);
	if (config === void 0) config = {};
	config.gameObjects = this.getAllVisibleChildren();
	config.x = this.x;
	config.y = this.y;
	config.originX = this.originX;
	config.originY = this.originY;
	var rt = Snapshot(config);
	var isValidRT = !!rt.scene;
	if (!scale1) {
		this.setScale(scaleXSave, scaleYSave);
		if (isValidRT) rt.setScale(scaleXSave, scaleYSave);
	}
	return isValidRT ? rt : this;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/DrawBounds.js
var GetValue$210 = __webpack_exports__Utils.Objects.GetValue;
var DrawBounds$1 = function(graphics, config) {
	var drawContainer = GetValue$210(config, "drawContainer", true);
	var gameObjects = GetValue$210(config, "children");
	if (gameObjects === void 0) gameObjects = this.getAllVisibleChildren([this]);
	if (!drawContainer) gameObjects = gameObjects.filter(function(gameObject) {
		return !gameObject.isRexContainerLite;
	});
	DrawBounds$2(gameObjects, graphics, config);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/origin/ChangeOrigin.js
var RotateAround$4 = __webpack_exports__Math.RotateAround;
var ChangeOrigin$1 = function(gameObject, originX, originY) {
	if (originY === void 0) originY = originX;
	var deltaXY = {
		x: (originX - gameObject.originX) * gameObject.displayWidth,
		y: (originY - gameObject.originY) * gameObject.displayHeight
	};
	RotateAround$4(deltaXY, 0, 0, gameObject.rotation);
	gameObject.originX = originX;
	gameObject.originY = originY;
	gameObject.x = gameObject.x + deltaXY.x;
	gameObject.y = gameObject.y + deltaXY.y;
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/ChangeOrigin.js
var ChangeOrigin = function(originX, originY) {
	this.syncChildrenEnable = false;
	ChangeOrigin$1(this, originX, originY);
	this.syncChildrenEnable = true;
	var children = this.getAllChildren();
	for (var i = 0, cnt = children.length; i < cnt; i++) this.resetChildPositionState(children[i]);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/Methods.js
var methods$34 = {
	changeOrigin: ChangeOrigin,
	drawBounds: DrawBounds$1
};
Object.assign(methods$34, Parent_default, AddChild_default, RemoveChild_default, ChildState_default, Transform_default, Position_default, Rotation_default, Scale_default, Visible_default$1, Alpha_default, Active_default, ScrollFactor_default, CameraFilter_default, Mask_default, Depth_default, Children_default, Tween_default, P3Container_default, RendererLayer_default, RenderTexture_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/ContainerLite.js
var ContainerLite = class extends Base$4 {
	constructor(scene, x, y, width, height, children) {
		if (Array.isArray(width)) {
			children = width;
			width = void 0;
			height = void 0;
		}
		super(scene, x, y, width, height);
		this.type = "rexContainerLite";
		this.isRexContainerLite = true;
		this.syncChildrenEnable = true;
		this._active = true;
		this._mask = null;
		this._scrollFactorX = 1;
		this._scrollFactorY = 1;
		this._cameraFilter = 0;
		if (children) this.add(children);
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		this.syncChildrenEnable = false;
		super.destroy(fromScene);
	}
	resize(width, height) {
		this.setSize(width, height);
		return this;
	}
	get x() {
		return this._x;
	}
	set x(value) {
		if (this._x === value) return;
		this._x = value;
		this.syncPosition();
	}
	get y() {
		return this._y;
	}
	set y(value) {
		if (this._y === value) return;
		this._y = value;
		this.syncPosition();
	}
	get rotation() {
		return super.rotation;
	}
	set rotation(value) {
		if (this.rotation === value) return;
		super.rotation = value;
		this.syncPosition();
	}
	get scaleX() {
		return super.scaleX;
	}
	set scaleX(value) {
		if (this.scaleX === value) return;
		super.scaleX = value;
		this.syncPosition();
	}
	get scaleY() {
		return super.scaleY;
	}
	set scaleY(value) {
		if (this.scaleY === value) return;
		super.scaleY = value;
		this.syncPosition();
	}
	get scale() {
		return super.scale;
	}
	set scale(value) {
		if (this.scale === value) return;
		super.scale = value;
		this.syncPosition();
	}
	get visible() {
		return super.visible;
	}
	set visible(value) {
		if (super.visible === value) return;
		super.visible = value;
		this.syncVisible();
	}
	get alpha() {
		return super.alpha;
	}
	set alpha(value) {
		if (super.alpha === value) return;
		super.alpha = value;
		this.syncAlpha();
	}
	get active() {
		return this._active;
	}
	set active(value) {
		if (this._active === value) return;
		this._active = value;
		this.syncActive();
	}
	get mask() {
		return this._mask;
	}
	set mask(mask) {
		if (this._mask === mask) return;
		this._mask = mask;
		if (!this.layerRendererEnable) this.syncMask();
	}
	get scrollFactorX() {
		return this._scrollFactorX;
	}
	set scrollFactorX(value) {
		if (this._scrollFactorX === value) return;
		this._scrollFactorX = value;
		this.syncScrollFactor();
	}
	get scrollFactorY() {
		return this._scrollFactorY;
	}
	set scrollFactorY(value) {
		if (this._scrollFactorY === value) return;
		this._scrollFactorY = value;
		this.syncScrollFactor();
	}
	get cameraFilter() {
		return this._cameraFilter;
	}
	set cameraFilter(value) {
		if (this._cameraFilter === value) return;
		this._cameraFilter = value;
		this.syncCameraFilter();
	}
	get list() {
		return this.children;
	}
	static GetParent(child) {
		return GetParent$1(child);
	}
	get parentContainer() {
		return this._parentContainer;
	}
	set parentContainer(value) {
		if (!this._parentContainer && !value) {
			this._parentContainer = value;
			return;
		}
		if (this.setParentContainerFlag) {
			this._parentContainer = value;
			return;
		}
		if (this._parentContainer && !value) {
			this.removeFromContainer();
			this._parentContainer = value;
		} else if (value) {
			this._parentContainer = value;
			this.addToContainer(value);
		} else this._parentContainer = value;
	}
	get setParentContainerFlag() {
		if (this._setParentContainerFlag) return true;
		var parent = GetParent$1(this);
		return parent ? parent.setParentContainerFlag : false;
	}
};
Object.assign(ContainerLite.prototype, methods$34);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/container/Container.js
var Container_default = ContainerLite;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/GetSizerConfig.js
var GetSizerConfig = function(gameObject) {
	if (!gameObject.hasOwnProperty("rexSizer")) gameObject.rexSizer = {};
	return gameObject.rexSizer;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/GetSizerConfig.js
function GetSizerConfig_default(gameObject) {
	if (gameObject === void 0) gameObject = this;
	return GetSizerConfig(gameObject);
}
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/GetChildPrevState.js
var GetChildPrevState = function(child) {
	var childConfig = GetSizerConfig(child);
	if (!childConfig.hasOwnProperty("prevState")) childConfig.prevState = {};
	return childConfig.prevState;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/PushIntoBounds.js
var PushIntoBounds = function(bounds) {
	if (bounds === void 0) bounds = GetViewport(this.scene);
	this.left = Math.max(this.left, bounds.left);
	this.right = Math.min(this.right, bounds.right);
	this.top = Math.max(this.top, bounds.top);
	this.bottom = Math.min(this.bottom, bounds.bottom);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/actions/AlignConst.js
var ALIGN$1 = __webpack_exports__Display.Align;
var AlignConst_default$1 = {
	center: ALIGN$1.CENTER,
	left: ALIGN$1.LEFT_CENTER,
	right: ALIGN$1.RIGHT_CENTER,
	top: ALIGN$1.TOP_CENTER,
	bottom: ALIGN$1.BOTTOM_CENTER,
	"left-top": ALIGN$1.TOP_LEFT,
	"top-left": ALIGN$1.TOP_LEFT,
	"left-center": ALIGN$1.LEFT_CENTER,
	"center-left": ALIGN$1.LEFT_CENTER,
	"left-bottom": ALIGN$1.BOTTOM_LEFT,
	"bottom-left": ALIGN$1.BOTTOM_LEFT,
	"center-top": ALIGN$1.TOP_CENTER,
	"top-center": ALIGN$1.TOP_CENTER,
	"center-center": ALIGN$1.CENTER,
	"center-bottom": ALIGN$1.BOTTOM_CENTER,
	"bottom-center": ALIGN$1.BOTTOM_CENTER,
	"right-top": ALIGN$1.TOP_RIGHT,
	"top-right": ALIGN$1.TOP_RIGHT,
	"right-center": ALIGN$1.RIGHT_CENTER,
	"center-right": ALIGN$1.RIGHT_CENTER,
	"right-bottom": ALIGN$1.BOTTOM_RIGHT,
	"bottom-right": ALIGN$1.BOTTOM_RIGHT
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/AlignConst.js
var AlignConst_default = AlignConst_default$1;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/actions/GlobZone.js
var globZone = new __webpack_exports__GameObjects.Zone({ sys: {
	queueDepthSort: NOOP,
	events: { once: NOOP }
} }, 0, 0, 1, 1);
globZone.setOrigin(0);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/align/const.js
var ALIGN_CONST = {
	/**
	* A constant representing a top-left alignment or position.
	* @constant
	* @name Phaser.Display.Align.TOP_LEFT
	* @since 3.0.0
	* @type {integer}
	*/
	TOP_LEFT: 0,
	/**
	* A constant representing a top-center alignment or position.
	* @constant
	* @name Phaser.Display.Align.TOP_CENTER
	* @since 3.0.0
	* @type {integer}
	*/
	TOP_CENTER: 1,
	/**
	* A constant representing a top-right alignment or position.
	* @constant
	* @name Phaser.Display.Align.TOP_RIGHT
	* @since 3.0.0
	* @type {integer}
	*/
	TOP_RIGHT: 2,
	/**
	* A constant representing a left-top alignment or position.
	* @constant
	* @name Phaser.Display.Align.LEFT_TOP
	* @since 3.0.0
	* @type {integer}
	*/
	LEFT_TOP: 3,
	/**
	* A constant representing a left-center alignment or position.
	* @constant
	* @name Phaser.Display.Align.LEFT_CENTER
	* @since 3.0.0
	* @type {integer}
	*/
	LEFT_CENTER: 4,
	/**
	* A constant representing a left-bottom alignment or position.
	* @constant
	* @name Phaser.Display.Align.LEFT_BOTTOM
	* @since 3.0.0
	* @type {integer}
	*/
	LEFT_BOTTOM: 5,
	/**
	* A constant representing a center alignment or position.
	* @constant
	* @name Phaser.Display.Align.CENTER
	* @since 3.0.0
	* @type {integer}
	*/
	CENTER: 6,
	/**
	* A constant representing a right-top alignment or position.
	* @constant
	* @name Phaser.Display.Align.RIGHT_TOP
	* @since 3.0.0
	* @type {integer}
	*/
	RIGHT_TOP: 7,
	/**
	* A constant representing a right-center alignment or position.
	* @constant
	* @name Phaser.Display.Align.RIGHT_CENTER
	* @since 3.0.0
	* @type {integer}
	*/
	RIGHT_CENTER: 8,
	/**
	* A constant representing a right-bottom alignment or position.
	* @constant
	* @name Phaser.Display.Align.RIGHT_BOTTOM
	* @since 3.0.0
	* @type {integer}
	*/
	RIGHT_BOTTOM: 9,
	/**
	* A constant representing a bottom-left alignment or position.
	* @constant
	* @name Phaser.Display.Align.BOTTOM_LEFT
	* @since 3.0.0
	* @type {integer}
	*/
	BOTTOM_LEFT: 10,
	/**
	* A constant representing a bottom-center alignment or position.
	* @constant
	* @name Phaser.Display.Align.BOTTOM_CENTER
	* @since 3.0.0
	* @type {integer}
	*/
	BOTTOM_CENTER: 11,
	/**
	* A constant representing a bottom-right alignment or position.
	* @constant
	* @name Phaser.Display.Align.BOTTOM_RIGHT
	* @since 3.0.0
	* @type {integer}
	*/
	BOTTOM_RIGHT: 12
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/bounds/GetBottom.js
var GetBottom = function(gameObject) {
	var height = GetDisplayHeight(gameObject);
	return gameObject.y + height - height * gameObject.originY;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/bounds/GetCenterX.js
var GetCenterX = function(gameObject) {
	var width = GetDisplayWidth(gameObject);
	return gameObject.x - width * gameObject.originX + width * .5;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/bounds/SetBottom.js
var SetBottom = function(gameObject, value) {
	var height = GetDisplayHeight(gameObject);
	gameObject.y = value - height + height * gameObject.originY;
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/bounds/SetCenterX.js
var SetCenterX = function(gameObject, x) {
	var width = GetDisplayWidth(gameObject);
	gameObject.x = x + width * gameObject.originX - width * .5;
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/align/in/BottomCenter.js
var BottomCenter = function(gameObject, alignIn, offsetX, offsetY) {
	if (offsetX === void 0) offsetX = 0;
	if (offsetY === void 0) offsetY = 0;
	SetCenterX(gameObject, GetCenterX(alignIn) + offsetX);
	SetBottom(gameObject, GetBottom(alignIn) + offsetY);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/bounds/GetLeft.js
var GetLeft = function(gameObject) {
	var width = GetDisplayWidth(gameObject);
	return gameObject.x - width * gameObject.originX;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/bounds/SetLeft.js
var SetLeft = function(gameObject, value) {
	gameObject.x = value + GetDisplayWidth(gameObject) * gameObject.originX;
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/align/in/BottomLeft.js
var BottomLeft = function(gameObject, alignIn, offsetX, offsetY) {
	if (offsetX === void 0) offsetX = 0;
	if (offsetY === void 0) offsetY = 0;
	SetLeft(gameObject, GetLeft(alignIn) - offsetX);
	SetBottom(gameObject, GetBottom(alignIn) + offsetY);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/bounds/GetRight.js
var GetRight = function(gameObject) {
	var width = GetDisplayWidth(gameObject);
	return gameObject.x + width - width * gameObject.originX;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/bounds/SetRight.js
var SetRight = function(gameObject, value) {
	var width = GetDisplayWidth(gameObject);
	gameObject.x = value - width + width * gameObject.originX;
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/align/in/BottomRight.js
var BottomRight = function(gameObject, alignIn, offsetX, offsetY) {
	if (offsetX === void 0) offsetX = 0;
	if (offsetY === void 0) offsetY = 0;
	SetRight(gameObject, GetRight(alignIn) + offsetX);
	SetBottom(gameObject, GetBottom(alignIn) + offsetY);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/bounds/SetCenterY.js
var SetCenterY = function(gameObject, y) {
	var height = GetDisplayHeight(gameObject);
	gameObject.y = y + height * gameObject.originY - height * .5;
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/bounds/CenterOn.js
var CenterOn = function(gameObject, x, y) {
	SetCenterX(gameObject, x);
	return SetCenterY(gameObject, y);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/bounds/GetCenterY.js
var GetCenterY = function(gameObject) {
	var height = GetDisplayHeight(gameObject);
	return gameObject.y - height * gameObject.originY + height * .5;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/align/in/Center.js
var Center = function(gameObject, alignIn, offsetX, offsetY) {
	if (offsetX === void 0) offsetX = 0;
	if (offsetY === void 0) offsetY = 0;
	CenterOn(gameObject, GetCenterX(alignIn) + offsetX, GetCenterY(alignIn) + offsetY);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/align/in/LeftCenter.js
var LeftCenter = function(gameObject, alignIn, offsetX, offsetY) {
	if (offsetX === void 0) offsetX = 0;
	if (offsetY === void 0) offsetY = 0;
	SetLeft(gameObject, GetLeft(alignIn) - offsetX);
	SetCenterY(gameObject, GetCenterY(alignIn) + offsetY);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/align/in/RightCenter.js
var RightCenter = function(gameObject, alignIn, offsetX, offsetY) {
	if (offsetX === void 0) offsetX = 0;
	if (offsetY === void 0) offsetY = 0;
	SetRight(gameObject, GetRight(alignIn) + offsetX);
	SetCenterY(gameObject, GetCenterY(alignIn) + offsetY);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/bounds/GetTop.js
var GetTop = function(gameObject) {
	var height = GetDisplayHeight(gameObject);
	return gameObject.y - height * gameObject.originY;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/bounds/SetTop.js
var SetTop = function(gameObject, value) {
	gameObject.y = value + GetDisplayHeight(gameObject) * gameObject.originY;
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/align/in/TopCenter.js
var TopCenter = function(gameObject, alignIn, offsetX, offsetY) {
	if (offsetX === void 0) offsetX = 0;
	if (offsetY === void 0) offsetY = 0;
	SetCenterX(gameObject, GetCenterX(alignIn) + offsetX);
	SetTop(gameObject, GetTop(alignIn) - offsetY);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/align/in/TopLeft.js
var TopLeft = function(gameObject, alignIn, offsetX, offsetY) {
	if (offsetX === void 0) offsetX = 0;
	if (offsetY === void 0) offsetY = 0;
	SetLeft(gameObject, GetLeft(alignIn) - offsetX);
	SetTop(gameObject, GetTop(alignIn) - offsetY);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/align/in/TopRight.js
var TopRight = function(gameObject, alignIn, offsetX, offsetY) {
	if (offsetX === void 0) offsetX = 0;
	if (offsetY === void 0) offsetY = 0;
	SetRight(gameObject, GetRight(alignIn) + offsetX);
	SetTop(gameObject, GetTop(alignIn) - offsetY);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/align/align/in/QuickSet.js
var AlignInMap = [];
AlignInMap[ALIGN_CONST.BOTTOM_CENTER] = BottomCenter;
AlignInMap[ALIGN_CONST.BOTTOM_LEFT] = BottomLeft;
AlignInMap[ALIGN_CONST.BOTTOM_RIGHT] = BottomRight;
AlignInMap[ALIGN_CONST.CENTER] = Center;
AlignInMap[ALIGN_CONST.LEFT_CENTER] = LeftCenter;
AlignInMap[ALIGN_CONST.RIGHT_CENTER] = RightCenter;
AlignInMap[ALIGN_CONST.TOP_CENTER] = TopCenter;
AlignInMap[ALIGN_CONST.TOP_LEFT] = TopLeft;
AlignInMap[ALIGN_CONST.TOP_RIGHT] = TopRight;
var QuickSet = function(child, alignIn, position, offsetX, offsetY) {
	return AlignInMap[position](child, alignIn, offsetX, offsetY);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/actions/AlignIn.js
var AlignIn = function(child, x, y, width, height, align) {
	globZone.setPosition(x, y).setSize(width, height);
	QuickSet(child, globZone, align);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/DrawBounds.js
var GetValue$209 = __webpack_exports__Utils.Objects.GetValue;
var Group$1 = __webpack_exports__GameObjects.Group;
var P3Container = __webpack_exports__GameObjects.Container;
var DrawBounds = function(graphics, config) {
	var scene = graphics.scene;
	var color, lineWidth;
	var createTextCallback, createTextCallbackScope, textAlign;
	if (typeof config === "number") color = config;
	else {
		color = GetValue$209(config, "color");
		lineWidth = GetValue$209(config, "lineWidth");
		var nameTextConfig = GetValue$209(config, "name", false);
		if (nameTextConfig) {
			createTextCallback = GetValue$209(nameTextConfig, "createTextCallback", DefaultCreateTextCallback);
			createTextCallbackScope = GetValue$209(nameTextConfig, "createTextCallbackScope", void 0);
			textAlign = GetValue$209(nameTextConfig, "align", "left-top");
			if (typeof textAlign === "string") textAlign = AlignConst_default[textAlign];
		}
	}
	if (color === void 0) color = 16777215;
	if (lineWidth === void 0) lineWidth = 1;
	if (createTextCallback && !graphics.children) {
		graphics.children = new Group$1(scene);
		graphics.once("destroy", function(graphics, fromScene) {
			graphics.children.destroy(!fromScene);
			graphics.children = void 0;
		});
		var graphicsClear = graphics.clear.bind(graphics);
		graphics.clear = function() {
			graphicsClear();
			graphics.children.clear(false, true);
		};
	}
	var children = this.getAllShownChildren([this]);
	GetP3ContainerChildren(children, children);
	var child;
	var nameText;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		child = children[i];
		if (child.getBounds || child.width !== void 0 && child.height !== void 0) GlobRect = GetBounds(child, GlobRect);
		else continue;
		if (color != null) graphics.lineStyle(lineWidth, color).strokeRectShape(GlobRect);
		if (child.name && createTextCallback) {
			if (createTextCallbackScope) nameText = createTextCallback.call(createTextCallbackScope, scene);
			else nameText = createTextCallback(scene);
			if (nameText) {
				nameText.setText(child.name);
				graphics.children.add(nameText);
				AlignIn(nameText, GlobRect.x, GlobRect.y, GlobRect.width, GlobRect.height, textAlign);
			}
		}
	}
	return this;
};
var DefaultCreateTextCallback = function(scene, child, childBoundsRect) {
	return scene.add.text(0, 0, "");
};
var GetP3ContainerChildren = function(gameObjects, output) {
	if (!Array.isArray(gameObjects)) gameObjects = [gameObjects];
	if (output === void 0) output = [];
	for (var i = 0, cnt = gameObjects.length; i < cnt; i++) {
		var gameObject = gameObjects[i];
		if (gameObject instanceof P3Container) {
			output.push(...gameObject.list);
			GetP3ContainerChildren(gameObject.list, output);
		}
	}
	return output;
};
var GlobRect = void 0;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/bounds/GetBoundsConfig.js
var GetValue$208 = __webpack_exports__Utils.Objects.GetValue;
var GetBoundsConfig = function(config, out) {
	if (config === void 0) config = 0;
	if (out === void 0) out = {};
	if (typeof config === "number") {
		out.left = config;
		out.right = config;
		out.top = config;
		out.bottom = config;
	} else {
		out.left = GetValue$208(config, "left", 0);
		out.right = GetValue$208(config, "right", 0);
		out.top = GetValue$208(config, "top", 0);
		out.bottom = GetValue$208(config, "bottom", 0);
	}
	return out;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/GetBoundsConfig.js
var GetBoundsConfig_default = GetBoundsConfig;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/utils/AddChild.js
var ContainerAdd = Container_default.prototype.add;
var AddChild$1 = function(gameObject) {
	ContainerAdd.call(this, gameObject);
	if (this.sizerEventsEnable) {
		gameObject.emit("sizer.add", gameObject, this);
		this.emit("add", gameObject, this);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/AddChildMethods.js
var AddChildMethods_default$8 = {
	addBackground(gameObject, paddingConfig, childKey) {
		if (this.backgroundChildren === void 0) this.backgroundChildren = [];
		if (typeof paddingConfig === "string") {
			childKey = paddingConfig;
			paddingConfig = void 0;
		}
		if (paddingConfig === void 0) paddingConfig = 0;
		AddChild$1.call(this, gameObject);
		this.backgroundChildren.push(gameObject);
		var config = this.getSizerConfig(gameObject);
		config.padding = GetBoundsConfig_default(paddingConfig);
		if (childKey !== void 0) this.addChildrenMap(childKey, gameObject);
		return this;
	},
	isBackground(gameObject) {
		if (this.backgroundChildren === void 0) return false;
		return this.backgroundChildren.indexOf(gameObject) !== -1;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/GetParentSizerMethods.js
var GetParent = function(gameObject, name) {
	var parent = null;
	if (name === void 0) {
		if (gameObject.hasOwnProperty("rexContainer")) {
			parent = gameObject.rexContainer.parent;
			if (parent) {
				if (!parent.isRexSizer) parent = GetParent(parent);
			} else parent = null;
		}
	} else {
		parent = GetParent(gameObject);
		while (parent) {
			if (parent.name === name) break;
			parent = GetParent(parent);
		}
	}
	return parent;
};
var GetTopmostParent = function(gameObject) {
	var parent = GetParent(gameObject);
	while (parent) {
		gameObject = parent;
		parent = GetParent(parent);
	}
	return gameObject;
};
var GetParentSizerMethods_default = {
	getParentSizer(gameObject, name) {
		if (typeof gameObject === "string") {
			name = gameObject;
			gameObject = void 0;
		}
		if (gameObject === void 0) gameObject = this;
		return GetParent(gameObject, name);
	},
	getTopmostSizer(gameObject) {
		if (gameObject === void 0) gameObject = this;
		return GetTopmostParent(gameObject);
	},
	hasParentSizer(parentGameObject, gameObject) {
		if (gameObject === void 0) gameObject = this;
		var parent = GetParent(gameObject);
		while (parent) {
			if (parent === parentGameObject) return true;
			parent = GetParent(parent);
		}
		return false;
	},
	hasChild(child, gameObject) {
		if (gameObject === void 0) gameObject = this;
		return this.hasParentSizer(gameObject, child);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/utils/RemoveChild.js
var RemoveItem$5 = __webpack_exports__Utils.Array.Remove;
var ContainerRemove = Container_default.prototype.remove;
var GetParentSizer$1 = GetParentSizerMethods_default.getParentSizer;
var RemoveChild = function(gameObject, destroyChild) {
	var parent = GetParentSizer$1(gameObject);
	while (parent) {
		if (parent.removeChildCallback) parent.removeChildCallback(gameObject, destroyChild);
		parent = GetParentSizer$1(parent);
	}
	if (this.isBackground(gameObject)) RemoveItem$5(this.backgroundChildren, gameObject);
	ContainerRemove.call(this, gameObject, destroyChild);
	if (!destroyChild && this.sizerEventsEnable) {
		gameObject.emit("sizer.remove", gameObject, this);
		this.emit("remove", gameObject, this);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/RemoveChildMethods.js
var RemoveItem$4 = __webpack_exports__Utils.Array.Remove;
var GetParentSizer = GetParentSizerMethods_default.getParentSizer;
var RemoveChildMethods_default$7 = {
	removeFromParentSizer() {
		var parent = GetParentSizer(gameObject);
		if (parent) parent.remove(this);
		return this;
	},
	removeBackground(gameObject, destroyChild) {
		if (this.backgroundChildren === void 0) return this;
		if (this.getParentSizer(gameObject) !== this) return this;
		RemoveItem$4(this.backgroundChildren, gameObject);
		RemoveChild.call(this, gameObject, destroyChild);
		return this;
	},
	removeAllBackgrounds(destroyChild) {
		if (this.backgroundChildren === void 0) return this;
		for (var i = this.backgroundChildren.length - 1; i >= 0; i--) this.remove(this.backgroundChildren[i], destroyChild);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/AddChildrenMap.js
var AddChildrenMap = function(key, gameObject) {
	if (typeof key === "string") this.childrenMap[key] = gameObject;
	else {
		var config = key;
		for (key in config) this.childrenMap[key] = config[key];
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/RemoveChildrenMap.js
var RemoveChildrenMap = function(key) {
	if (typeof key === "object") {
		var gameObject = key;
		for (var key in this.childrenMap) if (this.childrenMap[key] === gameObject) {
			delete this.childrenMap[key];
			return this;
		}
	}
	delete this.childrenMap[key];
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/GetElement.js
var GetElement = function(mapNameList, recursive) {
	if (typeof mapNameList === "string") mapNameList = mapNameList.split(".");
	if (mapNameList.length === 0) return;
	if (recursive === void 0) recursive = false;
	var name = mapNameList.shift(), element = null;
	if (name.charAt(0) === "#") {
		name = name.substring(1);
		element = this.getByName(name, recursive);
	} else if (mapNameList.length === 0 && recursive) {
		var childrenMap = this.childrenMap;
		if (childrenMap) {
			var queue = [childrenMap];
			var child;
			while (queue.length) {
				childrenMap = queue.shift();
				for (var key in childrenMap) {
					child = childrenMap[key];
					if (key === name) {
						element = child;
						break;
					} else if (child && typeof child === "object" && child.childrenMap) queue.push(child.childrenMap);
				}
				if (element) break;
			}
		}
	} else if (name.indexOf("[") === -1) {
		if (this.childrenMap) element = this.childrenMap[name];
	} else {
		var innerMatch = name.match(RE_OBJ);
		if (innerMatch != null) {
			if (this.childrenMap) {
				var elements = this.childrenMap[innerMatch[1]];
				if (elements) element = elements[innerMatch[2]];
			}
		}
	}
	if (mapNameList.length === 0) return element;
	else if (element && element.childrenMap) return element.getElement(mapNameList);
	else return null;
};
var RE_OBJ = /(\S+)\[(\d+)\]/i;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/GetChildIndex.js
var GetChildIndex = function(child) {
	if (Array.isArray(this.sizerChildren)) {
		var index = this.sizerChildren.indexOf(child);
		if (index === -1) index = null;
		return index;
	} else {
		if (this.getParentSizer(child) !== this) return null;
		for (var key in this.sizerChildren) if (this.sizerChildre[key] === child) return key;
		return null;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/PaddingMethods.js
var PaddingMethods_default = {
	getInnerPadding(key) {
		return GetPadding$1(this.space, key);
	},
	setInnerPadding(key, value) {
		SetPadding$1(this.space, key, value);
		return this;
	},
	getOuterPadding(key) {
		return GetPadding$1(this.getSizerConfig(this).padding, key);
	},
	setOuterPadding(key, value) {
		SetPadding$1(this.getSizerConfig(this).padding, key, value);
		return this;
	},
	getChildOuterPadding(child, key) {
		if (typeof child === "string") child = this.getElement(child);
		return GetPadding$1(this.getSizerConfig(child).padding, key);
	},
	setChildOuterPadding(child, key, value) {
		if (typeof child === "string") child = this.getElement(child);
		SetPadding$1(this.getSizerConfig(child).padding, key, value);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/ResolveWidth.js
var ResolveWidth$3 = function(width) {
	var childrenWidth = this.childrenWidth;
	if (childrenWidth === void 0) return;
	var minWidth = this.minWidth !== void 0 ? this.minWidth * this.scaleX : 0;
	if (width === void 0) {
		width = Math.max(minWidth, childrenWidth);
		if (this.layoutWarnEnable) {
			if (minWidth > 0 && childrenWidth > minWidth) console.warn(`Layout width warn: ${this.constructor.name}'s minWidth (${minWidth}) < childrenWidth (${childrenWidth})`);
		}
	} else if (this.layoutWarnEnable) {
		if (minWidth > width || childrenWidth > width) console.warn(`Layout width warn: ${this.constructor.name}'s minWidth (${minWidth}) or childrenWidth (${childrenWidth} > targetWidth ${width})`);
	}
	return width;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/HasWidthWrap.js
var HasWidthWrap$2 = function() {
	var child;
	for (var i in this.sizerChildren) {
		child = this.sizerChildren[i];
		if (!child || child.isRexSizer && child.ignoreLayout || !child.runWidthWrap) continue;
		if (!child.hasWidthWrap || child.hasWidthWrap()) return true;
	}
	return false;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/ResolveChildrenWidth.js
var ResolveChildrenWidth$1 = function(parentWidth) {
	var child, expandedChildWidth, childWidth;
	for (var i in this.sizerChildren) {
		child = this.sizerChildren[i];
		if (child && child.isRexSizer && !child.ignoreLayout) {
			if (parentWidth !== void 0) {
				expandedChildWidth = this.getExpandedChildWidth(child, parentWidth);
				childWidth = child.resolveWidth(expandedChildWidth);
				if (childWidth === void 0) childWidth = expandedChildWidth;
				child.resolveChildrenWidth(childWidth);
			} else if (child.minWidth > 0) child.resolveChildrenWidth(child.minWidth);
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/RunWidthWrap.js
var RunWidthWrap$3 = function(parentWidth) {
	var child, expandedChildWidth, childWidth;
	for (var i in this.sizerChildren) {
		child = this.sizerChildren[i];
		if (!child || child.isRexSizer && child.ignoreLayout || !child.runWidthWrap) continue;
		if (parentWidth !== void 0) {
			expandedChildWidth = this.getExpandedChildWidth(child, parentWidth);
			if (child.isRexSizer) {
				childWidth = child.resolveWidth(expandedChildWidth);
				if (childWidth === void 0) childWidth = expandedChildWidth;
			} else childWidth = expandedChildWidth;
			child.runWidthWrap(childWidth);
		} else if (child.minWidth > 0) child.runWidthWrap(child.minWidth);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/ResolveHeight.js
var ResolveHeight$3 = function(height) {
	var childrenHeight = this.childrenHeight;
	if (childrenHeight === void 0) return;
	var minHeight = this.minHeight !== void 0 ? this.minHeight * this.scaleY : 0;
	if (height === void 0) {
		height = Math.max(minHeight, childrenHeight);
		if (this.layoutWarnEnable) {
			if (minHeight > 0 && childrenHeight > minHeight) console.warn(`Layout height warn: ${this.constructor.name}'s minHeight (${minHeight}) < childrenHeight (${childrenHeight})`);
		}
	} else if (this.layoutWarnEnable) {
		if (minHeight > height || childrenHeight > height) console.warn(`Layout height warn: ${this.constructor.name}'s minHeight (${minHeight}) or childrenHeight (${childrenHeight}) > targetHeight (${height})`);
	}
	return height;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/HasHeightWrap.js
var HasHeightWrap$2 = function() {
	var child;
	for (var i in this.sizerChildren) {
		child = this.sizerChildren[i];
		if (!child || child.isRexSizer && child.ignoreLayout || !child.runHeightWrap) continue;
		if (!child.hasHeightWrap || child.hasHeightWrap()) return true;
	}
	return false;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/ResolveChildrenHeight.js
var ResolveChildrenHeight$1 = function(parentHeight) {
	var child, expandedChildHeight, childHeight;
	for (var i in this.sizerChildren) {
		child = this.sizerChildren[i];
		if (child && child.isRexSizer && !child.ignoreLayout) {
			if (parentHeight !== void 0) {
				expandedChildHeight = this.getExpandedChildHeight(child, parentHeight);
				childHeight = child.resolveHeight(expandedChildHeight);
				if (childHeight === void 0) childHeight = expandedChildHeight;
				child.resolveChildrenHeight(childHeight);
			} else if (child.minHeight > 0) child.resolveChildrenHeight(child.minHeight);
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/RunHeightWrap.js
var RunHeightWrap$3 = function(parentHeight) {
	var child, expandedChildHeight, childHeight;
	for (var i in this.sizerChildren) {
		child = this.sizerChildren[i];
		if (!child || child.isRexSizer && child.ignoreLayout || !child.runHeightWrap) continue;
		if (parentHeight !== void 0) {
			expandedChildHeight = this.getExpandedChildHeight(child, parentHeight);
			if (child.isRexSizer) {
				childHeight = child.resolveHeight(expandedChildHeight);
				if (childHeight === void 0) childHeight = expandedChildHeight;
			} else childHeight = expandedChildHeight;
			child.runHeightWrap(childHeight);
		} else if (child.minHeight > 0) child.runHeightWrap(child.minHeight);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/GetChildWidth.js
var GetChildWidth = function(child) {
	var childWidth;
	if (child.isRexSizer) {
		var childrenWidth = child.childrenWidth;
		if (childrenWidth == void 0) return;
		var childMinWidth = child.minWidth * child.scaleX;
		childWidth = Math.max(childMinWidth, childrenWidth);
	} else if (child.minWidth !== void 0) childWidth = child.minWidth;
	else if (child._minWidth !== void 0) childWidth = child._minWidth;
	else childWidth = GetDisplayWidth(child);
	return childWidth;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/GetChildHeight.js
var GetChildHeight = function(child) {
	var childHeight;
	if (child.isRexSizer) {
		var childrenHeight = child.childrenHeight;
		if (childrenHeight === void 0) return;
		var childMinHeight = child.minHeight * child.scaleY;
		childHeight = Math.max(childMinHeight, childrenHeight);
	} else if (child.minHeight !== void 0) childHeight = child.minHeight;
	else if (child._minHeight !== void 0) childHeight = child._minHeight;
	else childHeight = GetDisplayHeight(child);
	return childHeight;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/GetExpandedChildWidth.js
var GetExpandedChildWidth$4 = function(child, parentWidth) {
	return parentWidth;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/GetExpandedChildHeight.js
var GetExpandedChildHeight$4 = function(child, parentHeight) {
	return parentHeight;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/GetChildrenWidth.js
var GetChildrenWidth$5 = function() {
	return 0;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/GetChildrenHeight.js
var GetChildrenHeight$5 = function() {
	return 0;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/GetAllChildrenSizers.js
var GetAllChildrenSizers = function(out) {
	if (out === void 0) out = [];
	var startIdx = out.length;
	var children = this.getChildrenSizers(out);
	var endIdx = out.length;
	for (var i = startIdx; i < endIdx; i++) children[i].getAllChildrenSizers(out);
	return out;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/GetChildrenSizers.js
var GetChildrenSizers$5 = function(out) {
	if (out === void 0) out = [];
	return out;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/GetShownChildrenMethods.js
var GetShownChildrenMethods_default = {
	getShownChildren(out) {
		if (out === void 0) out = [];
		var children = this.children, child;
		for (var i = 0, cnt = children.length; i < cnt; i++) {
			child = children[i];
			if (child.rexSizer && child.rexSizer.hidden) continue;
			out.push(child);
		}
		return out;
	},
	getAllShownChildren(out) {
		if (out === void 0) out = [];
		var queue = [this];
		while (queue.length > 0) {
			var current = queue.shift();
			if (current.rexSizer && current.rexSizer.hidden) continue;
			if (current !== this) out.push(current);
			if (current.isRexContainerLite) queue.push(...current.children);
		}
		return out;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/PreLayout.js
var PreLayout$3 = function() {
	this._childrenWidth = void 0;
	this._childrenHeight = void 0;
	var children = this.getChildrenSizers(), child;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		child = children[i];
		if (child.ignoreLayout) continue;
		child.preLayout();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/Layout.js
var Layout = function() {
	this.runLayout();
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/size/HasResizeMethod.js
var HasResizeMethod = function(gameObject) {
	if (gameObject.resize) return true;
	if (!gameObject.setSize) return false;
	for (var i = 0, cnt = ExcludeClassList$1.length; i < cnt; i++) {
		var excludeClass = ExcludeClassList$1[i];
		if (excludeClass && gameObject instanceof excludeClass) return false;
	}
	return true;
};
var ExcludeClassList$1 = [
	__webpack_exports__GameObjects.Image,
	__webpack_exports__GameObjects.Sprite,
	__webpack_exports__GameObjects.Mesh,
	__webpack_exports__GameObjects.Shader,
	__webpack_exports__GameObjects.Video
];
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/size/CanSetDisplaySize.js
var CanSetDisplaySize = function(gameObject) {
	if (gameObject.displayWidth === void 0) return false;
	for (var i = 0, cnt = ExcludeClassList.length; i < cnt; i++) {
		var excludeClass = ExcludeClassList[i];
		if (excludeClass && gameObject instanceof excludeClass) return false;
	}
	return true;
};
var ExcludeClassList = [__webpack_exports__GameObjects.BitmapText];
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/size/ResizeGameObject.js
var ResizeGameObject = function(gameObject, newDisplayWidth, newDisplayHeight) {
	if (!gameObject || newDisplayWidth === void 0 && newDisplayHeight === void 0) return;
	if (HasResizeMethod(gameObject)) {
		var newWidth, newHeight;
		if (newDisplayWidth === void 0) newWidth = gameObject.width;
		else newWidth = newDisplayWidth / gameObject.scaleX;
		if (newDisplayHeight === void 0) newHeight = gameObject.height;
		else newHeight = newDisplayHeight / gameObject.scaleY;
		if (gameObject.resize) gameObject.resize(newWidth, newHeight);
		else gameObject.setSize(newWidth, newHeight);
	} else {
		var canSetDisplaySize = CanSetDisplaySize(gameObject);
		if (newDisplayWidth !== void 0) {
			if (canSetDisplaySize) gameObject.displayWidth = newDisplayWidth;
			else gameObject.scaleX = newDisplayWidth / gameObject.width;
		}
		if (newDisplayHeight !== void 0) {
			if (canSetDisplaySize) gameObject.displayHeight = newDisplayHeight;
			else gameObject.scaleY = newDisplayHeight / gameObject.height;
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/RunLayout.js
var RunLayout$1 = function(parent, newWidth, newHeight) {
	if (this.ignoreLayout) return this;
	var isTopmostParent = !parent;
	if (isTopmostParent) this.preLayout();
	var runWidthWrap, runHeightWrap;
	if (isTopmostParent || parent.runChildrenWrapFlag) {
		runWidthWrap = this.hasWidthWrap();
		runHeightWrap = this.hasHeightWrap();
	} else {
		runWidthWrap = false;
		runHeightWrap = false;
	}
	var size = ResolveSize(this, newWidth, newHeight, runWidthWrap, runHeightWrap);
	if (!size) console.error("Can't resolve size of ", this);
	var width = size.width;
	var height = size.height;
	ResizeGameObject(this, width, height);
	if (this.sizerEventsEnable) {
		if (this.layoutedChildren === void 0) this.layoutedChildren = [];
	}
	this.layoutChildren();
	this.layoutBackgrounds();
	if (this.sizerEventsEnable) {
		this.emit("postlayout", this.layoutedChildren, this);
		this.layoutedChildren.length = 0;
	}
	this.postLayout(parent, width, height);
	if (isTopmostParent) {
		if (this._anchor) this._anchor.updatePosition();
	}
	return this;
};
var ResolveSize = function(self, width, height, runWidthWrap, runHeightWrap) {
	var newWidth = ResolveWidth$2(self, width, runWidthWrap);
	var newHeight = ResolveHeight$2(self, height, runHeightWrap);
	if (newWidth === void 0) newWidth = ResolveWidth$2(self, width, runWidthWrap);
	if (newWidth !== void 0 && newHeight !== void 0) return {
		width: newWidth,
		height: newHeight
	};
	return false;
};
var ResolveWidth$2 = function(self, width, runWidthWrap) {
	var width = self.resolveWidth(width);
	if (runWidthWrap) {
		self.resolveChildrenWidth(width);
		self.runWidthWrap(width);
	}
	return width;
};
var ResolveHeight$2 = function(self, height, runHeightWrap) {
	var height = self.resolveHeight(height);
	if (runHeightWrap) {
		self.resolveChildrenHeight(height);
		self.runHeightWrap(height);
	}
	return height;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/LayoutChildren.js
var LayoutChildren$5 = function() {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/PostLayout.js
var PostLayout = function(parent, newWidth, newHeight) {
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/anchor/DefaultResizeCallback.js
var DefaultResizeCallback = function(width, height, gameObject, anchor) {
	ResizeGameObject(gameObject, width, height);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/anchor/Anchor.js
var GetValue$207 = __webpack_exports__Utils.Objects.GetValue;
var Anchor = class extends ComponentBase {
	constructor(gameObject, config) {
		super(gameObject, { eventEmitter: false });
		this.viewport = void 0;
		this.resetFromJSON(config);
	}
	shutdown(fromScene) {
		if (this.isShutdown) return;
		this.autoAnchor(false);
		this.viewport = void 0;
		this.onUpdateViewportCallback = void 0;
		this.onUpdateViewportCallbackScope = void 0;
		this.onResizeCallback = void 0;
		this.onResizeCallbackScope = void 0;
		super.shutdown(fromScene);
	}
	resetFromJSON(o) {
		if (o === void 0) o = {};
		var gameObject = this.parent;
		var alignX, configX;
		if (o.x !== void 0) {
			alignX = null;
			configX = o.x;
		} else if (o.left !== void 0) {
			alignX = 0;
			configX = o.left;
		} else if (o.right !== void 0) {
			alignX = 1;
			configX = o.right;
		} else if (o.centerX !== void 0) {
			alignX = .5;
			configX = o.centerX;
		}
		var alignY, configY;
		if (o.y !== void 0) {
			alignY = null;
			configY = o.y;
		} else if (o.top !== void 0) {
			alignY = 0;
			configY = o.top;
		} else if (o.bottom !== void 0) {
			alignY = 1;
			configY = o.bottom;
		} else if (o.centerY !== void 0) {
			alignY = .5;
			configY = o.centerY;
		}
		var percentageX, offsetX;
		if (configX !== void 0) {
			configX = configX.replace("left", "0%").replace("right", "100%").replace("center", "50%").split("%");
			percentageX = parseFloat(configX[0]) / 100;
			offsetX = configX[1] === "" ? 0 : parseFloat(configX[1]);
		}
		var percentageY, offsetY;
		if (configY !== void 0) {
			configY = configY.replace("top", "0%").replace("bottom", "100%").replace("center", "50%").split("%");
			percentageY = parseFloat(configY[0]) / 100;
			offsetY = configY[1] === "" ? 0 : parseFloat(configY[1]);
		}
		var configWidth = o.width;
		var percentageWidth, paddingWidth;
		if (configWidth !== void 0) {
			configWidth = configWidth.split("%");
			percentageWidth = parseFloat(configWidth[0]) / 100;
			paddingWidth = configWidth[1] === "" ? 0 : parseFloat(configWidth[1]);
		}
		var configHeight = o.height;
		var percentageHeight, paddingHeight;
		if (configHeight !== void 0) {
			configHeight = configHeight.split("%");
			percentageHeight = parseFloat(configHeight[0]) / 100;
			paddingHeight = configHeight[1] === "" ? 0 : parseFloat(configHeight[1]);
		}
		var aspectRatio = o.aspectRatio;
		if (aspectRatio === true) aspectRatio = gameObject.displayWidth / gameObject.displayHeight;
		this.setAlign(alignX, alignY);
		this.setPercentage(percentageX, percentageY);
		this.setOffset(offsetX, offsetY);
		this.setSizePercentage(percentageWidth, percentageHeight);
		this.setSizePadding(paddingWidth, paddingHeight);
		this.setAspectRatio(aspectRatio);
		var onResizeCallback = GetValue$207(o, "onResizeCallback", DefaultResizeCallback);
		var onResizeCallbackScope = GetValue$207(o, "onResizeCallbackScope");
		this.setResizeCallback(onResizeCallback, onResizeCallbackScope);
		var onUpdateViewportCallback = GetValue$207(o, "onUpdateViewportCallback");
		var onUpdateViewportCallbackScope = GetValue$207(o, "onUpdateViewportCallbackScope");
		this.setUpdateViewportCallback(onUpdateViewportCallback, onUpdateViewportCallbackScope);
		this.autoAnchor(o.enable);
		return this;
	}
	autoAnchor(enable) {
		if (enable === void 0) enable = true;
		enable = !!enable;
		if (this.autoAnchorEnable === enable) return this;
		if (enable) {
			this.scene.sys.scale.on("resize", this.anchor, this);
			this.anchor();
		} else this.scene.sys.scale.off("resize", this.anchor, this);
		this.autoAnchorEnable = enable;
		return this;
	}
	setAlign(x, y) {
		this.alignX = x;
		this.alignY = y;
		return this;
	}
	setPercentage(x, y) {
		this.percentageX = x;
		this.percentageY = y;
		return this;
	}
	setOffset(x, y) {
		this.offsetX = x;
		this.offsetY = y;
		return this;
	}
	setSizePercentage(width, height) {
		this.percentageWidth = width;
		this.percentageHeight = height;
		return this;
	}
	setSizePadding(width, height) {
		this.paddingWidth = width;
		this.paddingHeight = height;
		return this;
	}
	setAspectRatio(aspectRatio) {
		this.aspectRatio = aspectRatio;
		return this;
	}
	setResizeCallback(callback, scope) {
		this.onResizeCallback = callback;
		this.onResizeCallbackScope = scope;
		return this;
	}
	setUpdateViewportCallback(callback, scope) {
		this.onUpdateViewportCallback = callback;
		this.onUpdateViewportCallbackScope = scope;
		return this;
	}
	anchor() {
		this.updateViewport();
		this.updateSize();
		this.updatePosition();
		return this;
	}
	updateSize() {
		var callback = this.onResizeCallback, scope = this.onResizeCallbackScope;
		var newWidth = this.anchorWidth, newHeight = this.anchorHeight;
		if (newWidth === void 0 && newHeight === void 0 || !callback) return;
		var gameObject = this.parent;
		if (newWidth === void 0) {
			if (this.aspectRatio && newHeight) newWidth = this.aspectRatio * newHeight;
			else newWidth = gameObject.width;
		}
		if (newHeight === void 0) {
			if (this.aspectRatio && newWidth) newHeight = newWidth / this.aspectRatio;
			else newHeight = gameObject.height;
		}
		if (scope) callback.call(scope, newWidth, newHeight, gameObject, this);
		else callback(newWidth, newHeight, gameObject, this);
	}
	updatePosition() {
		var gameObject = this.parent;
		if (this.alignX === null) gameObject.x = this.anchorX;
		else if (this.alignX !== void 0) gameObject.x = this.anchorX + gameObject.displayWidth * (gameObject.originX - this.alignX);
		if (this.alignY === null) gameObject.y = this.anchorY;
		else if (this.alignY !== void 0) gameObject.y = this.anchorY + gameObject.displayHeight * (gameObject.originY - this.alignY);
		return this;
	}
	get anchorX() {
		return this.viewport.x + this.viewport.width * this.percentageX + this.offsetX;
	}
	get anchorY() {
		return this.viewport.y + this.viewport.height * this.percentageY + this.offsetY;
	}
	get anchorWidth() {
		if (this.percentageWidth === void 0) return;
		return this.viewport.width * this.percentageWidth + this.paddingWidth;
	}
	get anchorHeight() {
		if (this.percentageHeight === void 0) return;
		return this.viewport.height * this.percentageHeight + this.paddingHeight;
	}
	updateViewport() {
		var camera = this.parent.scene.cameras.main;
		this.viewport = GetViewport(this.scene, camera, this.viewport);
		var viewport = this.viewport;
		var callback = this.onUpdateViewportCallback, scope = this.onUpdateViewportCallbackScope;
		if (callback) {
			if (scope) callback.call(scope, viewport, this.parent, this);
			else callback(viewport, this.parent, this);
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/anchor/Anchor.js
var Anchor_default = Anchor;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/SetAnchor.js
var SetAnchor = function(config) {
	if (config === void 0) config = {};
	var hasMinWidth = config.width !== void 0;
	var hasMinHeight = config.height !== void 0;
	if ((hasMinWidth || hasMinHeight) && !config.onResizeCallback) {
		var hasAspectRatio = config.aspectRatio === true || config.aspectRatio > 0;
		var resizeWidth = hasMinWidth || hasMinHeight && hasAspectRatio;
		var resizeHeight = hasMinHeight || hasMinWidth && hasAspectRatio;
		config.onResizeCallback = function(width, height, sizer) {
			if (resizeWidth) sizer.setMinWidth(width);
			if (resizeHeight) sizer.setMinHeight(height);
			sizer.layout();
		};
	}
	if (this._anchor === void 0) this._anchor = new Anchor_default(this, config);
	else this._anchor.resetFromJSON(config);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/scale/Scale.js
var GetValue$206 = __webpack_exports__Utils.Objects.GetValue;
var GetAdvancedValue$3 = __webpack_exports__Utils.Objects.GetAdvancedValue;
var Linear$18 = __webpack_exports__Math.Linear;
var Scale = class extends EaseValueTaskBase {
	constructor(gameObject, config) {
		super(gameObject, config);
		this.scaleStart = {};
		this.scaleEnd = {};
		this.resetFromJSON(config);
		this.boot();
	}
	resetFromJSON(o) {
		super.resetFromJSON(o);
		this.setMode(GetValue$206(o, "mode", 0));
		this.setScaleRange(GetAdvancedValue$3(o, "start", void 0), GetAdvancedValue$3(o, "end", 0));
		return this;
	}
	setMode(m) {
		if (typeof m === "string") m = MODE$3[m];
		this.mode = m;
		return this;
	}
	setScaleRange(start, end) {
		if (typeof start === "number") {
			this.startX = start;
			this.startY = start;
		} else {
			this.startX = GetAdvancedValue$3(start, "x", this.parent.scaleX);
			this.startY = GetAdvancedValue$3(start, "y", this.parent.scaleY);
		}
		if (typeof end === "number") {
			this.endX = end;
			this.endY = end;
		} else {
			this.endX = GetAdvancedValue$3(end, "x", void 0);
			this.endY = GetAdvancedValue$3(end, "y", void 0);
		}
		this.hasScaleX = this.startX !== void 0 && this.endX !== void 0;
		this.hasScaleY = this.startY !== void 0 && this.endY !== void 0;
		return this;
	}
	start() {
		if (this.timer.isRunning) return this;
		var gameObject = this.parent;
		if (this.hasScaleX) gameObject.scaleX = this.startX;
		if (this.hasScaleY) gameObject.scaleY = this.startY;
		var repeat = this.repeat;
		if (this.mode === 2) {
			if (repeat !== -1) repeat = (repeat + 1) * 2 - 1;
		}
		this.timer.setDelay(this.delay).setDuration(this.duration).setRepeat(repeat);
		super.start();
		return this;
	}
	updateTarget(gameObject, timer) {
		var t = timer.t;
		if (timer.isOddIteration) t = 1 - t;
		t = this.easeFn(t);
		if (this.hasScaleX) gameObject.scaleX = Linear$18(this.startX, this.endX, t);
		if (this.hasScaleY) gameObject.scaleY = Linear$18(this.startY, this.endY, t);
	}
	complete() {
		super.complete();
		if (this.mode === 1) this.parent.destroy();
		return this;
	}
};
var MODE$3 = {
	stop: 0,
	destroy: 1,
	yoyo: 2
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/scale/PopUp.js
var PopUp$1 = function(gameObject, duration, orientation, ease, scale) {
	if (ease === void 0) ease = "Cubic";
	var start, end;
	switch (orientation) {
		case 0:
		case "x":
			start = { x: 0 };
			end = { x: gameObject.scaleX };
			break;
		case 1:
		case "y":
			start = { y: 0 };
			end = { y: gameObject.scaleY };
			break;
		default:
			start = 0;
			end = gameObject.scale;
	}
	var config = {
		mode: 0,
		start,
		end,
		duration,
		ease
	};
	if (scale === void 0) scale = new Scale(gameObject, config);
	else scale.resetFromJSON(config);
	scale.restart();
	return scale;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/popup.js
var popup_default = PopUp$1;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/scale/ScaleDownDestroy.js
var ScaleDownDestroy = function(gameObject, duration, orientation, ease, destroyMode, scale) {
	if (ease === void 0) ease = "Linear";
	if (destroyMode instanceof Scale) {
		scale = destroyMode;
		destroyMode = void 0;
	}
	if (destroyMode === void 0) destroyMode = true;
	var config = {};
	config.mode = destroyMode ? 1 : 0;
	switch (orientation) {
		case 0:
		case "x":
			config.end = { x: 0 };
			break;
		case 1:
		case "y":
			config.end = { y: 0 };
			break;
		default: config.end = 0;
	}
	config.duration = duration;
	config.ease = ease;
	if (scale === void 0) scale = new Scale(gameObject, config);
	else scale.resetFromJSON(config);
	scale.restart();
	return scale;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/scale-down-destroy.js
var scale_down_destroy_default = ScaleDownDestroy;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/scale/Yoyo.js
var Yoyo = function(gameObject, duration, peakValue, repeat, orientation, ease, scale) {
	if (peakValue === void 0) peakValue = 1.2;
	if (repeat === void 0) repeat = 0;
	if (ease === void 0) ease = "Cubic";
	var start, end;
	switch (orientation) {
		case 0:
		case "x":
			start = { x: gameObject.scaleX };
			end = { x: peakValue };
			break;
		case 1:
		case "y":
			start = { y: gameObject.scaleX };
			end = { y: peakValue };
			break;
		default:
			start = gameObject.scaleX;
			end = peakValue;
	}
	var config = {
		mode: 2,
		start,
		end,
		duration: duration / 2,
		ease,
		repeat
	};
	if (scale === void 0) scale = new Scale(gameObject, config);
	else scale.resetFromJSON(config);
	scale.restart();
	return scale;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/scale/ScaleMethods.js
var IsPlainObject$40 = __webpack_exports__Utils.Objects.IsPlainObject;
var ScaleMethods_default = {
	onInitScale() {
		var gameObject = this;
		var scale = this._scaleBehavior;
		scale.completeEventName = void 0;
		scale.on("complete", function() {
			if (scale.completeEventName) {
				gameObject.emit(scale.completeEventName, gameObject);
				scale.completeEventName = void 0;
			}
		});
	},
	popUp(duration, orientation, ease) {
		if (IsPlainObject$40(duration)) {
			var config = duration;
			duration = config.duration;
			orientation = config.orientation;
			ease = config.ease;
		}
		var isInit = this._scaleBehavior === void 0;
		this._scaleBehavior = PopUp$1(this, duration, orientation, ease, this._scaleBehavior);
		if (isInit) this.onInitScale();
		this._scaleBehavior.completeEventName = "popup.complete";
		return this;
	},
	popUpPromise(duration, orientation, ease) {
		this.popUp(duration, orientation, ease);
		return WaitComplete(this._scaleBehavior);
	},
	isRunningPopUp() {
		return this._scaleBehavior && this._scaleBehavior.completeEventName === "popup.complete";
	},
	scaleDownDestroy(duration, orientation, ease, destroyMode) {
		if (IsPlainObject$40(duration)) {
			var config = duration;
			duration = config.duration;
			orientation = config.orientation;
			ease = config.ease;
			destroyMode = config.destroy;
		}
		var isInit = this._scaleBehavior === void 0;
		this._scaleBehavior = ScaleDownDestroy(this, duration, orientation, ease, destroyMode, this._scaleBehavior);
		if (isInit) this.onInitScale();
		this._scaleBehavior.completeEventName = "scaledown.complete";
		return this;
	},
	scaleDownDestroyPromise(duration, orientation, ease, destroyMode) {
		this.scaleDownDestroy(duration, orientation, ease, destroyMode);
		return WaitComplete(this._scaleBehavior);
	},
	scaleDown(duration, orientation, ease) {
		this.scaleDownDestroy(duration, orientation, ease, false);
		return this;
	},
	scaleDownPromise(duration, orientation, ease) {
		this.scaleDown(duration, orientation, ease);
		return WaitComplete(this._scaleBehavior);
	},
	isRunningScaleDown() {
		return this._scaleBehavior && this._scaleBehavior.completeEventName === "scaledown.complete";
	},
	scaleYoyo(duration, peakValue, repeat, orientation, ease) {
		if (IsPlainObject$40(duration)) {
			var config = duration;
			duration = config.duration;
			peakValue = config.peakValue;
			repeat = config.repeat;
			orientation = config.orientation;
			ease = config.ease;
		}
		var isInit = this._scaleBehavior === void 0;
		this._scaleBehavior = Yoyo(this, duration, peakValue, repeat, orientation, ease, this._scaleBehavior);
		if (isInit) this.onInitScale();
		this._scaleBehavior.completeEventName = "scaleyoyo.complete";
		return this;
	},
	scaleYoyoPromise(duration, peakValue, repeat, orientation, ease) {
		this.scaleYoyo(duration, peakValue, repeat, orientation, ease);
		return WaitComplete(this._scaleBehavior);
	},
	isRunningScaleYoyo() {
		return this._scaleBehavior && (this._scaleBehavior.completeEventName = "scaleyoyo.complete");
	},
	isRunningEaseScale() {
		return this.isRunningPopUp() || this.isRunningScaleDown() || this.isRunningScaleYoyo();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/ScaleMethods.js
var methods$33 = {};
Object.assign(methods$33, ScaleMethods_default);
methods$33.onInitScale = function() {
	ScaleMethods_default.onInitScale.call(this);
	var gameObject = this;
	this._scaleBehavior.on("update", function() {
		var parent = GetParentSizerMethods_default.getParentSizer(gameObject);
		if (parent) parent.resetChildPositionState(gameObject);
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/fade/Fade.js
var GetValue$205 = __webpack_exports__Utils.Objects.GetValue;
var GetAdvancedValue$2 = __webpack_exports__Utils.Objects.GetAdvancedValue;
var Linear$17 = __webpack_exports__Math.Linear;
var Fade = class extends EaseValueTaskBase {
	constructor(gameObject, config) {
		super(gameObject, config);
		this.resetFromJSON(config);
		this.boot();
	}
	resetFromJSON(o) {
		super.resetFromJSON(o);
		this.setMode(GetValue$205(o, "mode", 0));
		this.setAlphaRange(GetAdvancedValue$2(o, "start", this.parent.alpha), GetAdvancedValue$2(o, "end", 0));
		return this;
	}
	setMode(m) {
		if (typeof m === "string") m = MODE$2[m];
		this.mode = m;
		return this;
	}
	setAlphaRange(start, end) {
		this.alphaStart = start;
		this.alphaEnd = end;
		return this;
	}
	start() {
		if (this.timer.isRunning) return this;
		this.parent.setAlpha(this.alphaStart);
		this.timer.setDelay(this.delay).setDuration(this.duration).setRepeat(this.mode === 2 ? -1 : 0);
		super.start();
		return this;
	}
	updateTarget(gameObject, timer) {
		var t = timer.t;
		if (timer.isOddIteration) t = 1 - t;
		gameObject.alpha = Linear$17(this.alphaStart, this.alphaEnd, t);
	}
	complete() {
		super.complete();
		if (this.mode === 1) this.parent.destroy();
		return this;
	}
};
var MODE$2 = {
	stop: 0,
	destroy: 1,
	yoyo: 2
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/fade/FadeIn.js
var IsPlainObject$39 = __webpack_exports__Utils.Objects.IsPlainObject;
var FadeIn = function(gameObject, duration, alpha, fade) {
	var startAlpha, endAlpha;
	if (IsPlainObject$39(alpha)) {
		startAlpha = alpha.start;
		endAlpha = alpha.end;
	} else endAlpha = alpha;
	if (startAlpha === void 0) startAlpha = 0;
	if (endAlpha === void 0) endAlpha = 1;
	var config = {
		mode: 0,
		start: startAlpha,
		end: endAlpha,
		duration
	};
	if (fade === void 0) fade = new Fade(gameObject, config);
	else fade.resetFromJSON(config);
	fade.restart();
	return fade;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/fade-in.js
var fade_in_default = FadeIn;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/fade/FadeOutDestroy.js
var FadeOutDestroy = function(gameObject, duration, destroyMode, fade) {
	if (destroyMode instanceof Fade) {
		fade = destroyMode;
		destroyMode = void 0;
	}
	if (destroyMode === void 0) destroyMode = true;
	var config = {
		mode: destroyMode ? 1 : 0,
		end: 0,
		duration
	};
	if (fade === void 0) fade = new Fade(gameObject, config);
	else fade.resetFromJSON(config);
	fade.restart();
	return fade;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/fade-out-destroy.js
var fade_out_destroy_default = FadeOutDestroy;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/fade/FadeMethods.js
var IsPlainObject$38 = __webpack_exports__Utils.Objects.IsPlainObject;
var FadeMethods_default = {
	onInitFade() {
		var gameObject = this;
		var fade = this._fade;
		fade.completeEventName = void 0;
		fade.on("complete", function() {
			if (fade.completeEventName) {
				gameObject.emit(fade.completeEventName, gameObject);
				fade.completeEventName = void 0;
			}
		});
	},
	fadeIn(duration, alpha) {
		if (IsPlainObject$38(duration)) {
			var config = duration;
			duration = config.duration;
			alpha = config.alpha;
		}
		var isInit = this._fade === void 0;
		this._fade = FadeIn(this, duration, alpha, this._fade);
		if (isInit) this.onInitFade();
		this._fade.completeEventName = "fadein.complete";
		return this;
	},
	fadeInPromise(duration, alpha) {
		this.fadeIn(duration, alpha);
		return WaitComplete(this._fade);
	},
	isRunningFadeIn() {
		return this._fade && this._fade.completeEventName === "fadein.complete";
	},
	fadeOutDestroy(duration, destroyMode) {
		if (IsPlainObject$38(duration)) {
			var config = duration;
			duration = config.duration;
			destroyMode = config.destroy;
		}
		var isInit = this._fade === void 0;
		this._fade = FadeOutDestroy(this, duration, destroyMode, this._fade);
		if (isInit) this.onInitFade();
		this._fade.completeEventName = "fadeout.complete";
		return this;
	},
	fadeOutDestroyPromise(duration, destroyMode) {
		this.fadeOutDestroy(duration, destroyMode);
		return WaitComplete(this._fade);
	},
	fadeOut(duration) {
		this.fadeOutDestroy(duration, false);
		return this;
	},
	fadeOutPromise(duration) {
		this.fadeOut(duration);
		return WaitComplete(this._fade);
	},
	isRunningFadeOut() {
		return this._fade && this._fade.completeEventName === "fadeout.complete";
	},
	isRunningEaseFade() {
		return this.isRunningFadeIn() || this.isRunningFadeOut();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/FadeMethods.js
var methods$32 = {};
Object.assign(methods$32, FadeMethods_default);
methods$32.onInitFade = function() {
	FadeMethods_default.onInitFade.call(this);
	var gameObject = this;
	this._fade.on("update", function() {
		var parent = GetParentSizerMethods_default.getParentSizer(gameObject);
		if (parent) parent.resetChildAlphaState(gameObject);
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/easemove/EaseMove.js
var GetValue$204 = __webpack_exports__Utils.Objects.GetValue;
var GetAdvancedValue$1 = __webpack_exports__Utils.Objects.GetAdvancedValue;
var Linear$16 = __webpack_exports__Math.Linear;
var EaseMove = class extends EaseValueTaskBase {
	constructor(gameObject, config) {
		super(gameObject, config);
		this.resetFromJSON(config);
		this.boot();
	}
	resetFromJSON(o) {
		super.resetFromJSON(o);
		this.setMode(GetValue$204(o, "mode", 0));
		if (o && (o.hasOwnProperty("x") || o.hasOwnProperty("y"))) {
			var endX = GetAdvancedValue$1(o, "x", void 0);
			var endY = GetAdvancedValue$1(o, "y", void 0);
			this.setTargetPosition(endX, endY);
		} else this.setTargetPosition(o);
		return this;
	}
	setMode(m) {
		if (typeof m === "string") m = MODE$1[m];
		this.mode = m;
		return this;
	}
	setTargetPosition(x, y) {
		if (typeof x === "number" || typeof y === "number") {
			this.startX = this.parent.x;
			this.startY = this.parent.y;
			this.endX = x;
			this.endY = y;
		} else {
			var config = x;
			this.startX = GetAdvancedValue$1(config, "startX", void 0);
			this.startY = GetAdvancedValue$1(config, "startY", void 0);
			this.endX = GetAdvancedValue$1(config, "endX", void 0);
			this.endY = GetAdvancedValue$1(config, "endY", void 0);
		}
		this.hasMoveX = this.startX !== void 0 && this.endX !== void 0;
		this.hasMoveY = this.startY !== void 0 && this.endY !== void 0;
		return this;
	}
	start() {
		if (this.timer.isRunning) return this;
		var gameObject = this.parent;
		if (this.hasMoveX) gameObject.x = this.startX;
		if (this.hasMoveY) gameObject.y = this.startY;
		this.timer.setDelay(this.delay).setDuration(this.duration).setRepeat(this.mode === 2 ? -1 : 0);
		super.start();
		return this;
	}
	updateTarget(gameObject, timer) {
		var t = timer.t;
		if (timer.isOddIteration) t = 1 - t;
		t = this.easeFn(t);
		if (this.hasMoveX) gameObject.x = Linear$16(this.startX, this.endX, t);
		if (this.hasMoveY) gameObject.y = Linear$16(this.startY, this.endY, t);
	}
	complete() {
		super.complete();
		if (this.mode === 1) this.parent.destroy();
		return this;
	}
};
var MODE$1 = {
	stop: 0,
	destroy: 1,
	yoyo: 2
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/easemove/ParseValue.js
var ParseValue = function(propertyValue, startValue) {
	if (typeof propertyValue === "number") return propertyValue;
	else {
		var op = propertyValue[0];
		var num = parseFloat(propertyValue.substr(2));
		switch (op) {
			case "+": return startValue + num;
			case "-": return startValue - num;
			case "*": return startValue * num;
			case "/": return startValue / num;
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/easemove/EaseMoveTo.js
var EaseMoveTo = function(gameObject, duration, endX, endY, ease, destroyMode, easeMove) {
	if (destroyMode instanceof EaseMove) {
		easeMove = destroyMode;
		destroyMode = void 0;
	}
	if (destroyMode === void 0) destroyMode = false;
	var config = {};
	config.mode = destroyMode ? 1 : 0;
	if (endX !== void 0) {
		config.startX = gameObject.x;
		config.endX = ParseValue(endX, gameObject.x);
	}
	if (endY !== void 0) {
		config.startY = gameObject.y;
		config.endY = ParseValue(endY, gameObject.y);
	}
	config.duration = duration;
	config.ease = ease === void 0 ? "Linear" : ease;
	if (easeMove === void 0) easeMove = new EaseMove(gameObject, config);
	else easeMove.resetFromJSON(config);
	easeMove.restart();
	return easeMove;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/easemove/EaseMoveFrom.js
var EaseMoveFrom = function(gameObject, duration, startX, startY, ease, destroyMode, easeMove) {
	if (destroyMode instanceof EaseMove) {
		easeMove = destroyMode;
		destroyMode = void 0;
	}
	if (destroyMode === void 0) destroyMode = false;
	var config = {};
	config.mode = destroyMode ? 1 : 0;
	if (startX !== void 0) {
		config.startX = ParseValue(startX, gameObject.x);
		config.endX = gameObject.x;
	}
	if (startY !== void 0) {
		config.startY = ParseValue(startY, gameObject.y);
		config.endY = gameObject.y;
	}
	config.duration = duration;
	config.ease = ease === void 0 ? "Linear" : ease;
	if (easeMove === void 0) easeMove = new EaseMove(gameObject, config);
	else easeMove.resetFromJSON(config);
	easeMove.restart();
	return easeMove;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/easemove/EaseMoveMethods.js
var IsPlainObject$37 = __webpack_exports__Utils.Objects.IsPlainObject;
var DistanceBetween$6 = __webpack_exports__Math.Distance.Between;
var EaseMoveMethods_default = {
	onInitEaseMove() {
		var gameObject = this;
		var easeMove = this._easeMove;
		easeMove.completeEventName = void 0;
		easeMove.on("complete", function() {
			if (easeMove.completeEventName) {
				gameObject.emit(easeMove.completeEventName, gameObject);
				easeMove.completeEventName = void 0;
			}
		});
	},
	moveFrom(duration, x, y, ease, destroyMode) {
		if (IsPlainObject$37(duration)) {
			var config = duration;
			x = config.x;
			y = config.y;
			if (config.hasOwnProperty("speed")) duration = DistanceBetween$6(x, y, this.x, this.y) * 1e3 / config.speed;
			else duration = config.duration;
			ease = config.ease;
		}
		var isInit = this._easeMove === void 0;
		this._easeMove = EaseMoveFrom(this, duration, x, y, ease, destroyMode, this._easeMove);
		if (isInit) this.onInitEaseMove();
		this._easeMove.completeEventName = "movefrom.complete";
		return this;
	},
	moveFromPromise(duration, x, y, ease, destroyMode) {
		this.moveFrom(duration, x, y, ease, destroyMode);
		return WaitComplete(this._easeMove);
	},
	moveFromDestroy(duration, x, y, ease) {
		this.moveFrom(duration, x, y, ease, true);
		return this;
	},
	moveFromDestroyPromise(duration, x, y, ease) {
		this.moveFromDestroy(duration, x, y, ease);
		return WaitComplete(this._easeMove);
	},
	isRunningMoveFrom() {
		return this._easeMove && (this._easeMove.completeEventName = "movefrom.complete");
	},
	moveTo(duration, x, y, ease, destroyMode) {
		if (IsPlainObject$37(duration)) {
			var config = duration;
			x = config.x;
			y = config.y;
			if (config.hasOwnProperty("speed")) duration = DistanceBetween$6(x, y, this.x, this.y) * 1e3 / config.speed;
			else duration = config.duration;
			ease = config.ease;
		}
		var isInit = this._easeMove === void 0;
		this._easeMove = EaseMoveTo(this, duration, x, y, ease, destroyMode, this._easeMove);
		if (isInit) this.onInitEaseMove();
		this._easeMove.completeEventName;
		return this;
	},
	moveToPromise(duration, x, y, ease, destroyMode) {
		this.moveTo(duration, x, y, ease, destroyMode);
		return WaitComplete(this._easeMove);
	},
	moveToDestroy(duration, x, y, ease) {
		this.moveTo(duration, x, y, ease, true);
		return this;
	},
	moveToDestroyPromise(duration, x, y, ease) {
		this.moveToDestroy(duration, x, y, ease, true);
		return WaitComplete(this._easeMove);
	},
	isRunningMoveTo() {
		return this._easeMove && this._easeMove.completeEventName === "moveto.complete";
	},
	isRunningEaseMove() {
		return this.isRunningMoveFrom() || this.isRunningMoveTo();
	},
	moveStop(toEnd) {
		if (!this._easeMove) return this;
		this._easeMove.stop(toEnd);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/EaseMoveMethods.js
var methods$31 = {};
Object.assign(methods$31, EaseMoveMethods_default);
methods$31.onInitEaseMove = function() {
	EaseMoveMethods_default.onInitEaseMove.call(this);
	var gameObject = this;
	this._easeMove.on("update", function() {
		var parent = GetParentSizerMethods_default.getParentSizer(gameObject);
		if (parent) parent.resetChildPositionState(gameObject);
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/shake/ShakePosition.js
var GetValue$203 = __webpack_exports__Utils.Objects.GetValue;
var ShakePosition = class extends TickTask {
	constructor(gameObject, config) {
		super(gameObject, config);
		this.timer = new Timer$1();
		this.resetFromJSON(config);
		this.boot();
	}
	resetFromJSON(o) {
		this.timer.resetFromJSON(GetValue$203(o, "timer"));
		this.setEnable(GetValue$203(o, "enable", true));
		this.setMode(GetValue$203(o, "mode", 1));
		this.isRunning = GetValue$203(o, "isRunning", false);
		this.setMagnitudeMode(GetValue$203(o, "magnitudeMode", 1));
		this.setAxisMode(GetValue$203(o, "axis", 0));
		this.setDuration(GetValue$203(o, "duration", 500));
		this.setMagnitude(GetValue$203(o, "magnitude", 10));
		this.ox = GetValue$203(o, "ox", void 0);
		this.oy = GetValue$203(o, "oy", void 0);
		return this;
	}
	toJSON() {
		return {
			timer: this.timer.toJSON(),
			enable: this.enable,
			mode: this.mode,
			isRunning: this.isRunning,
			magnitudeMode,
			duration: this.duration,
			magnitude: this.magnitude,
			ox: this.ox,
			oy: this.oy
		};
	}
	shutdown(fromScene) {
		if (this.isShutdown) return;
		super.shutdown(fromScene);
		this.timer.destroy();
		this.timer = void 0;
	}
	startTicking() {
		super.startTicking();
		if (this.mode === 0) {
			this.scene.game.events.on("poststep", this.update, this);
			this.scene.game.events.on("prestep", this.backToOrigin, this);
		} else this.scene.sys.events.on("preupdate", this.update, this);
	}
	stopTicking() {
		super.stopTicking();
		if (this.scene) {
			if (this.mode === 0) {
				this.scene.game.events.off("poststep", this.update, this);
				this.scene.game.events.off("prestep", this.backToOrigin, this);
			} else this.scene.sys.events.off("preupdate", this.update, this);
		}
	}
	setEnable(e) {
		if (e == void 0) e = true;
		this.enable = e;
		return this;
	}
	setMode(mode) {
		if (typeof mode === "string") mode = MODE[mode];
		this.mode = mode;
		return this;
	}
	setMagnitudeMode(magnitudeMode) {
		if (typeof magnitudeMode === "string") magnitudeMode = MANITUDEMODE[magnitudeMode];
		this.magnitudeMode = magnitudeMode;
		return this;
	}
	setAxisMode(m) {
		if (typeof m === "string") m = DIRECTIONNODE$1[m];
		this.axisMode = m;
		return this;
	}
	setDuration(duration) {
		this.duration = duration;
		return this;
	}
	setMagnitude(magnitude) {
		this.magnitude = magnitude;
		return this;
	}
	start(duration, magnitude) {
		if (typeof duration !== "number") {
			var config = duration;
			magnitude = GetValue$203(config, "magnitude", void 0);
			duration = GetValue$203(config, "duration", void 0);
		}
		if (magnitude !== void 0) this.setMagnitude(magnitude);
		if (duration !== void 0) this.setDuration(duration);
		this.timer.setDuration(this.duration).start();
		super.start();
		return this;
	}
	shake(duration, magnitude) {
		this.start(duration, magnitude);
		return this;
	}
	update(time, delta) {
		if (!this.isRunning || !this.enable) return this;
		var gameObject = this.parent;
		if (!gameObject.active) return this;
		this.timer.update(time, delta);
		if (this.timer.isDone) {
			this.backToOrigin();
			this.complete();
		} else {
			if (this.ox === void 0) {
				this.ox = gameObject.x;
				this.oy = gameObject.y;
			}
			var magnitude = this.magnitude;
			if (this.magnitudeMode === 1) magnitude *= 1 - this.timer.t;
			var a = Math.random() * Math.PI * 2;
			var x = this.ox + Math.cos(a) * magnitude;
			var y = this.oy + Math.sin(a) * magnitude;
			switch (this.axisMode) {
				case 1:
					gameObject.x = x;
					break;
				case 2:
					gameObject.y = y;
					break;
				default:
					gameObject.x = x;
					gameObject.y = y;
			}
		}
		return this;
	}
	backToOrigin() {
		if (!this.isRunning || !this.enable) return this;
		if (this.ox === void 0) return this;
		var gameObject = this.parent;
		switch (this.axisMode) {
			case 1:
				gameObject.x = this.ox;
				break;
			case 2:
				gameObject.y = this.oy;
				break;
			default:
				gameObject.x = this.ox;
				gameObject.y = this.oy;
		}
		this.ox = void 0;
		this.oy = void 0;
		return this;
	}
};
var MODE = {
	effect: 0,
	behavior: 1
};
var DIRECTIONNODE$1 = {
	"both": 0,
	"h&v": 0,
	"x&y": 0,
	"horizontal": 1,
	"h": 1,
	"x": 1,
	"vertical": 2,
	"v": 2,
	"y": 2
};
var MANITUDEMODE = {
	constant: 0,
	decay: 1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/shake/Shake.js
var Shake_default = ShakePosition;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/ShakeMethods.js
var IsPlainObject$36 = __webpack_exports__Utils.Objects.IsPlainObject;
var OnInitShake = function(gameObject, shake) {
	shake.on("complete", function() {
		gameObject.emit("shake.complete", gameObject);
	});
};
var ShakeMethods_default = {
	shake(duration, magnitude, magnitudeMode) {
		if (IsPlainObject$36(duration)) {
			var config = duration;
			duration = config.duration;
			magnitude = config.magnitude;
			magnitudeMode = config.magnitudeMode;
		}
		if (this._shake === void 0) {
			this._shake = new Shake_default(this, {
				mode: 0,
				magnitudeMode: 1
			});
			OnInitShake(this, this._shake);
		}
		if (duration !== void 0) this._shake.setDuration(duration);
		if (magnitude !== void 0) this._shake.setMagnitude(magnitude);
		if (magnitudeMode !== void 0) this._shake.setMagnitudeMode(magnitudeMode);
		this._shake.shake();
		return this;
	},
	shakePromise(duration, alpha) {
		this.shake(duration, alpha);
		return WaitComplete(this._shake);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/easedata/EaseData.js
var IsPlainObject$35 = __webpack_exports__Utils.Objects.IsPlainObject;
var EaseData = class extends ComponentBase {
	constructor(parent, config) {
		super(parent, config);
		this.parent.setDataEnabled();
		this.easeTasks = {};
	}
	complete(key) {
		this.emit(`complete-${key}`, this.parent, this);
		this.emit("complete", key, this.parent, this);
	}
	getEaseTask(key) {
		var easeTask = this.easeTasks[key];
		if (easeTask === void 0) {
			easeTask = new EaseValueTask(this.parent);
			this.easeTasks[key] = easeTask;
			easeTask.setTarget(this.parent.data.values).on("complete", function() {
				this.complete(key);
			}, this);
		}
		return easeTask;
	}
	easeTo(key, value, duration, ease) {
		if (IsPlainObject$35(key)) {
			var config = key;
			key = config.key;
			value = config.value;
			duration = config.duration;
			ease = config.ease;
			var speed = config.speed;
			if (duration === void 0 && speed !== void 0) duration = Math.abs(value - this.parent.data.values[key]) / speed * 1e3;
		}
		if (duration === void 0) duration = 1e3;
		if (ease === void 0) ease = "Linear";
		this.getEaseTask(key).restart({
			key,
			to: value,
			duration,
			ease
		});
		return this;
	}
	easeFrom(key, value, duration, ease) {
		if (IsPlainObject$35(key)) {
			var config = key;
			key = config.key;
			value = config.value;
			duration = config.duration;
			ease = config.ease;
			var speed = config.speed;
			if (duration === void 0 && speed !== void 0) duration = Math.abs(value - this.parent.data.values[key]) / speed * 1e3;
		}
		if (duration === void 0) duration = 1e3;
		if (ease === void 0) ease = "Linear";
		this.getEaseTask(key).restart({
			key,
			from: value,
			duration,
			ease
		});
		return this;
	}
	stopEase(key, toEnd) {
		if (toEnd === void 0) toEnd = true;
		var easeTask = this.easeTasks[key];
		if (easeTask) easeTask.stop(toEnd);
		return this;
	}
	stopAll(toEnd) {
		if (toEnd === void 0) toEnd = true;
		for (var key in this.easeTasks) this.stopEase(key, toEnd);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/EaseDataMethods.js
var OnInitEaseData = function(gameObject, easeData) {
	easeData.on("complete", function(key) {
		gameObject.emit(`easedata.${key}.complete`, gameObject);
		gameObject.emit("easedata.complete", key, gameObject);
	});
};
var EaseDataMethods_default = {
	easeDataTo(key, value, duration, ease) {
		if (!this._easeData) {
			this._easeData = new EaseData(this);
			OnInitEaseData(this, this._easeData);
		}
		this._easeData.easeTo(key, value, duration, ease);
		return this;
	},
	easeDataToPromise(key, value, duration, ease) {
		this.easeDataTo(key, value, duration, ease);
		return WaitEvent(this._easeData, `complete-${key}`);
	},
	stopEaseData(key, toEnd) {
		if (!this._easeData) return this;
		this._easeData.stopEase(key, toEnd);
		return this;
	},
	stopAllEaseData(toEnd) {
		if (!this._easeData) return this;
		this._easeData.stopAll(toEnd);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/DelayCallMethods.js
var RemoveItem$3 = __webpack_exports__Utils.Array.Remove;
var OnInitDelayCallTimers = function(gameObject) {
	gameObject._delayCallTimers = [];
	gameObject.once("destroy", function() {
		var timers = gameObject._delayCallTimers;
		for (var i = 0, cnt = timers.length; i < cnt; i++) timers[i].remove();
		gameObject._delayCallTimers = void 0;
	});
};
var DelayCallMethods_default$2 = { delayCall(delay, callback, scope) {
	if (this._delayCallTimers === void 0) OnInitDelayCallTimers(this);
	var timer;
	var self = this;
	var OnTimeOut = function() {
		RemoveItem$3(self._delayCallTimers, timer);
		if (scope) callback.call(scope);
		else callback();
	};
	timer = this.scene.time.delayedCall(delay, OnTimeOut);
	this._delayCallTimers.push(timer);
	return this;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/Hide.js
var Show = function(gameObject) {
	_hide(gameObject, false);
};
var Hide = function(gameObject) {
	_hide(gameObject, true);
};
var IsShown = function(gameObject) {
	if (!gameObject) return false;
	return !GetSizerConfig(gameObject).hidden;
};
var _hide = function(gameObject, hidden) {
	if (!gameObject) return;
	var config = GetSizerConfig(gameObject);
	config.hidden = hidden;
	var parent = GetParent$1(gameObject);
	if (parent) parent.setChildVisible(gameObject, !hidden);
	else gameObject.setVisible(!hidden);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/HideMethods.js
var HideMethods_default = {
	show(gameObject) {
		if (gameObject === void 0) gameObject = this;
		Show(gameObject, false);
		return this;
	},
	hide(gameObject) {
		if (gameObject === void 0) gameObject = this;
		Hide(gameObject, true);
		return this;
	},
	isShow(gameObject) {
		if (gameObject === void 0) gameObject = this;
		return IsShown(gameObject);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/logic/fsm/FSMBase.js
var StateProperties$1 = [
	"next",
	"exit",
	"enter"
];
var FSM$1 = class {
	constructor(config) {
		var states = GetValue$279(config, "states", void 0);
		if (states) this.addStates(states);
		var extend = GetValue$279(config, "extend", void 0);
		if (extend) {
			for (var name in extend) if (!this.hasOwnProperty(name) || this[name] === void 0) this[name] = extend[name];
		}
		var eventEmitter = GetValue$279(config, "eventEmitter", void 0);
		var EventEmitterClass = GetValue$279(config, "EventEmitterClass", void 0);
		this.setEventEmitter(eventEmitter, EventEmitterClass);
		this._stateLock = false;
		this.resetFromJSON(config);
	}
	shutdown() {
		this.destroyEventEmitter();
	}
	destroy() {
		this.shutdown();
	}
	resetFromJSON(o) {
		this.setEnable(GetValue$279(o, "enable", true));
		this.start(GetValue$279(o, "start", void 0));
		var init = GetValue$279(o, "init", void 0);
		if (init) init.call(this);
		return this;
	}
	toJSON() {
		return {
			curState: this.state,
			prevState: this.prevState,
			enable: this.enable,
			start: this._start
		};
	}
	setEnable(e) {
		if (e === void 0) e = true;
		this.enable = e;
		return this;
	}
	toggleEnable() {
		this.setEnable(!this.enable);
		return this;
	}
	set state(newState) {
		if (!this.enable || this._stateLock) return;
		if (this._state === newState) return;
		this._prevState = this._state;
		this._state = newState;
		this._stateLock = true;
		this.emit("statechange", this);
		if (this._prevState != null) {
			var exitEventName = "exit_" + this._prevState;
			var exitCallback = this[exitEventName];
			if (exitCallback) exitCallback.call(this);
			this.emit(exitEventName, this);
		}
		this._stateLock = false;
		if (this._state != null) {
			var enterEventName = "enter_" + this._state;
			var enterCallback = this[enterEventName];
			if (enterCallback) enterCallback.call(this);
			this.emit(enterEventName, this);
		}
	}
	get state() {
		return this._state;
	}
	get prevState() {
		return this._prevState;
	}
	start(state) {
		this._start = state;
		this._prevState = void 0;
		this._state = state;
		return this;
	}
	goto(nextState) {
		if (nextState != null) this.state = nextState;
		return this;
	}
	next() {
		var nextState;
		var getNextState = this["next_" + this.state];
		if (getNextState) {
			if (typeof getNextState === "string") nextState = getNextState;
			else nextState = getNextState.call(this);
		}
		this.goto(nextState);
		return this;
	}
	get stateProperties() {
		return StateProperties$1;
	}
	addState(name, state) {
		if (typeof name !== "string") {
			state = name;
			name = state.name;
		}
		var stateProperties = this.stateProperties;
		for (var i = 0, cnt = stateProperties.length; i < cnt; i++) {
			var propertyName = stateProperties[i];
			var propertyValue = state[propertyName];
			if (propertyValue) this[`${propertyName}_${name}`] = propertyValue;
		}
		return this;
	}
	addStates(states) {
		if (Array.isArray(states)) for (var i = 0, cnt = states.length; i < cnt; i++) this.addState(states[i]);
		else for (var name in states) this.addState(name, states[name]);
		return this;
	}
	runMethod(methodName, a1, a2, a3, a4, a5) {
		var fn = this[methodName + "_" + this.state];
		if (!fn) return;
		var len = arguments.length;
		switch (len) {
			case 1: return fn.call(this);
			case 2: return fn.call(this, a1);
			case 3: return fn.call(this, a1, a2);
			case 4: return fn.call(this, a1, a2, a3);
			case 5: return fn.call(this, a1, a2, a3, a4);
			case 6: return fn.call(this, a1, a2, a3, a4, a5);
		}
		var args = new Array(len - 1);
		for (var i = 1; i < len; i++) args[i - 1] = arguments[i];
		return fn.apply(this, args);
	}
};
Object.assign(FSM$1.prototype, StandAloneEventEmitterMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/eventemitter/HasListener.js
var HasListener = function(eventEmitter, eventName, fn, context, once) {
	if (once === void 0) once = false;
	var listeners = eventEmitter._events[eventName];
	if (!listeners) return false;
	for (var i = 0, cnt = listeners.length; i < cnt; i++) {
		var listener = listeners[i];
		if (listener.fn === fn && listener.context === context && listener.once === once) return true;
	}
	return false;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/logic/fsm/FSM.js
var StateProperties = [
	"next",
	"exit",
	"enter",
	"update",
	"preupdate",
	"postupdate"
];
var FSM = class extends FSM$1 {
	shutdown() {
		this.stopUpdate();
		this.stopPreUpdate();
		this.stopPostUpdate();
		this._scene = void 0;
		super.shutdown();
	}
	resetFromJSON(o) {
		super.resetFromJSON(o);
		this._scene = GetValue$279(o, "scene", void 0);
		return this;
	}
	get stateProperties() {
		return StateProperties;
	}
	update(time, delta) {
		this.runMethod("update", time, delta);
	}
	preupdate(time, delta) {
		this.runMethod("preupdate", time, delta);
	}
	postupdate(time, delta) {
		this.runMethod("postupdate", time, delta);
	}
	startUpdate(scene) {
		if (!scene) scene = this._scene;
		var eventEmitter = scene.sys.events;
		if (HasListener(eventEmitter, "update", this.update, this)) return this;
		this._scene = scene;
		eventEmitter.on("update", this.update, this);
		return this;
	}
	stopUpdate() {
		if (!this._scene) return this;
		this._scene.sys.events.off("update", this.update, this);
		return this;
	}
	startPreUpdate(scene) {
		if (!scene) scene = this._scene;
		var eventEmitter = scene.sys.events;
		if (HasListener(eventEmitter, "preupdate", this.preupdate, this)) return this;
		this._scene = scene;
		eventEmitter.on("preupdate", this.preupdate, this);
		return this;
	}
	stopPreUpdate() {
		if (!this._scene) return this;
		this._scene.sys.events.off("preupdate", this.preupdate, this);
		return this;
	}
	startPostUpdate(scene) {
		if (!scene) scene = this._scene;
		var eventEmitter = scene.sys.events;
		if (HasListener(eventEmitter, "postupdate", this.postupdate, this)) return this;
		this._scene = scene;
		eventEmitter.on("postupdate", this.postupdate, this);
		return this;
	}
	stopPostUpdate() {
		if (!this._scene) return this;
		this._scene.sys.events.off("postupdate", this.postupdate, this);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/fsm.js
var fsm_default = FSM;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/openclosetransition/State.js
var State$1 = class extends fsm_default {
	constructor(parent, config) {
		super(config);
		this.parent = parent;
		var initState = config.initState || "IDLE";
		this.start(initState);
	}
	init() {
		this.start("IDLE");
	}
	next_IDLE() {
		return "TRANS_OPNE";
	}
	next_TRANS_OPNE() {
		return "OPEN";
	}
	enter_TRANS_OPNE() {
		var transitionBehavior = this.parent;
		if (transitionBehavior.transitInTime > 0) {
			var delay = transitionBehavior.runTransitionInCallback();
			transitionBehavior.delayCall(delay, this.next, this);
		} else this.next();
	}
	exit_TRANS_OPNE() {
		this.parent.removeDelayCall();
	}
	next_OPEN() {
		return "TRANS_CLOSE";
	}
	enter_OPEN() {
		this.parent.onOpen();
	}
	exit_OPEN() {
		this.parent.removeDelayCall();
	}
	next_TRANS_CLOSE() {
		return "CLOSE";
	}
	enter_TRANS_CLOSE() {
		var transitionBehavior = this.parent;
		if (transitionBehavior.transitOutTime > 0) {
			var delay = transitionBehavior.runTransitionOutCallback();
			transitionBehavior.delayCall(delay, this.next, this);
		} else this.next();
	}
	exit_TRANS_CLOSE() {
		this.parent.removeDelayCall();
	}
	next_CLOSE() {
		return "TRANS_OPNE";
	}
	enter_CLOSE() {
		this.parent.onClose();
	}
	exit_CLOSE() {}
	canOpen() {
		return this.state === "IDLE" || this.state === "CLOSE";
	}
	canClose() {
		return this.state === "IDLE" || this.state === "OPEN";
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/time/PostStepDelayCall.js
var PostStepDelayCall = function(gameObject, delay, callback, scope, args) {
	var scene = GetSceneObject(gameObject);
	return scene.time.delayedCall(delay, function() {
		scene.game.events.once("poststep", function() {
			callback.call(scope, args);
		});
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/openclosetransition/methods/DelayCallMethods.js
var DelayCallMethods_default$1 = {
	delayCall(delay, callback, scope) {
		this.delayCallTimer = PostStepDelayCall(this, delay, callback, scope);
		return this;
	},
	removeDelayCall() {
		if (this.delayCallTimer) {
			this.delayCallTimer.remove(false);
			this.delayCallTimer = void 0;
		}
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/openclosetransition/methods/ConfigurationMethods.js
var ConfigurationMethods_default$3 = {
	setTransitInTime(time) {
		this.transitInTime = time;
		return this;
	},
	setTransitOutTime(time) {
		this.transitOutTime = time;
		return this;
	},
	setTransitInCallback(callback) {
		if (!callback) callback = NOOP;
		this.transitInCallback = callback;
		return this;
	},
	setTransitOutCallback(callback) {
		if (!callback) callback = NOOP;
		this.transitOutCallback = callback;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/openclosetransition/methods/OpenMethods.js
var OpenMethods_default$1 = {
	runTransitionInCallback() {
		this.transitInCallback(this.parent, this.transitInTime);
		return this.transitInTime;
	},
	onOpen() {},
	requestOpen(openEventData, duration) {
		if (!this._state.canOpen()) return this;
		this.openEventData = arguments.length > 0 ? openEventData : this.parent;
		var transitionTimeSave = this.transitInTime;
		if (duration !== void 0) this.transitInTime = duration;
		this._state.goto("TRANS_OPNE");
		this.transitInTime = transitionTimeSave;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/openclosetransition/methods/CloseMethods.js
var CloseMethods_default = {
	runTransitionOutCallback() {
		this.transitOutCallback(this.parent, this.transitOutTime);
		return this.transitOutTime;
	},
	onClose() {
		if (this.oneShotMode) this.parent.destroy();
	},
	requestClose(closeEventData, duration) {
		if (!this._state.canClose) return this;
		this.closeEventData = arguments.length > 0 ? closeEventData : this.parent;
		var transitionTimeSave = this.transitOutTime;
		if (duration !== void 0) this.transitOutTime = duration;
		this._state.goto("TRANS_CLOSE");
		this.transitOutTime = transitionTimeSave;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/openclosetransition/methods/Methods.js
var methods$30 = {};
Object.assign(methods$30, DelayCallMethods_default$1, ConfigurationMethods_default$3, OpenMethods_default$1, CloseMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/openclosetransition/OpenCloseTransition.js
var GetValue$202 = __webpack_exports__Utils.Objects.GetValue;
var OpenCloseTransition = class extends ComponentBase {
	constructor(gameObject, config) {
		super(gameObject, config);
		this.setTransitInTime(GetValue$202(config, "duration.in", 200));
		this.setTransitOutTime(GetValue$202(config, "duration.out", 200));
		this.setTransitInCallback(GetValue$202(config, "transitIn"));
		this.setTransitOutCallback(GetValue$202(config, "transitOut"));
		this.oneShotMode = GetValue$202(config, "destroy", false);
		this.delayCallTimer = void 0;
		this._state = new State$1(this, {
			eventEmitter: false,
			initState: GetValue$202(config, "initState", "IDLE")
		});
		this.openEventData = void 0;
		this.closeEventData = void 0;
	}
	get state() {
		return this._state.state;
	}
	shutdown(fromScene) {
		if (this.isShutdown) return;
		this.transitInCallback = void 0;
		this.transitOutCallback = void 0;
		this.openEventData = void 0;
		this.closeEventData = void 0;
		this.removeDelayCall();
		super.shutdown(fromScene);
	}
};
Object.assign(OpenCloseTransition.prototype, methods$30);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/fullwindow/fullwindowrectangle/FullWindowRectangle.js
var Rectangle = __webpack_exports__GameObjects.Rectangle;
var FullWindowRectangle = class extends Rectangle {
	constructor(scene, color, alpha) {
		super(scene, 0, 0, 2, 2, color, 1);
		this.fullWindow = new FullWindow(this);
		this.setAlpha(alpha);
	}
	get tint() {
		return this.fillColor;
	}
	set tint(value) {
		this.setFillStyle(value, this.fillAlpha);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/fullwindowrectangle.js
var fullwindowrectangle_default = FullWindowRectangle;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/input/toucheventstop/TouchEventStop.js
var GetValue$201 = __webpack_exports__Utils.Objects.GetValue;
var TouchEventStop = class extends ComponentBase {
	constructor(gameObject, config) {
		super(gameObject, { eventEmitter: false });
		this.resetFromJSON(config);
		this.boot();
	}
	resetFromJSON(o) {
		this.setHitAreaMode(GetValue$201(o, "hitAreaMode", 0));
		this.setEnable(GetValue$201(o, "enable", true));
		this.setStopMode(GetValue$201(o, "stopAllLevels", true));
		return this;
	}
	boot() {
		this.parent.on("pointerdown", function(pointer, localX, localY, event) {
			if (this.stopAllLevels) event.stopPropagation();
		}, this).on("pointerup", function(pointer, localX, localY, event) {
			if (this.stopAllLevels) event.stopPropagation();
		}, this).on("pointermove", function(pointer, localX, localY, event) {
			if (this.stopAllLevels) event.stopPropagation();
		}, this).on("pointerover", function(pointer, localX, localY, event) {
			if (this.stopAllLevels) event.stopPropagation();
		}, this).on("pointerout", function(pointer, event) {
			if (this.stopAllLevels) event.stopPropagation();
		}, this);
	}
	setHitAreaMode(mode) {
		if (typeof mode === "string") mode = HitAreaMode[mode];
		var gameObject = this.parent;
		if (gameObject.input) gameObject.removeInteractive();
		if (mode === 0) gameObject.setInteractive();
		else gameObject.setInteractive({
			hitArea: {},
			hitAreaCallback: function() {
				return true;
			}
		});
		return this;
	}
	setEnable(e) {
		if (e === void 0) e = true;
		if (e) this.parent.setInteractive();
		else this.parent.disableInteractive();
		this.enable = e;
		return this;
	}
	setStopMode(allLevels) {
		if (allLevels === void 0) allLevels = true;
		this.stopAllLevels = allLevels;
		return this;
	}
	toggleEnable() {
		this.setEnable(!this.enable);
		return this;
	}
};
var HitAreaMode = {
	default: 0,
	fullWindow: 1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/toucheventstop.js
var toucheventstop_default = TouchEventStop;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/cover/Cover.js
var GetValue$200 = __webpack_exports__Utils.Objects.GetValue;
var Cover = class extends fullwindowrectangle_default {
	constructor(scene, config) {
		var fillColor = GetValue$200(config, "color", 0);
		var fillAlpha = GetValue$200(config, "alpha", .8);
		super(scene, fillColor, fillAlpha);
		this.touchEventStop = new toucheventstop_default(this, { hitAreaMode: 1 });
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/modal/CreateCover.js
var CreateCover = function(gameObject, config) {
	var scene = gameObject.scene;
	var cover = new Cover(scene, config);
	scene.add.existing(cover);
	if (gameObject.isRexContainerLite) {
		gameObject.pin(cover, {
			syncPosition: false,
			syncRotation: false,
			syncScale: false,
			syncAlpha: false,
			syncScrollFactor: false
		});
		gameObject.moveDepthBelow(cover);
	} else scene.children.moveBelow(cover, gameObject);
	return cover;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/modal/DefaultTransitCallbacks.js
var DefaultTransitCallbacks_default = {
	popUp(gameObject, duration) {
		if (gameObject._modalScaleSave !== void 0) {
			gameObject.scaleX = gameObject._modalScaleSave;
			gameObject.scaleY = gameObject._modalScaleSave;
		} else gameObject._modalScaleSave = gameObject.scaleX;
		popup_default(gameObject, duration);
	},
	scaleDown(gameObject, duration) {
		scale_down_destroy_default(gameObject, duration, void 0, void 0, false);
	},
	fadeIn(gameObject, duration) {
		if (gameObject._modalAlphaSave !== void 0) gameObject.alpha = gameObject._modalAlphaSave;
		else gameObject._modalAlphaSave = gameObject.alpha;
		fade_in_default(gameObject, duration);
	},
	fadeOut(gameObject, duration) {
		fade_out_destroy_default(gameObject, duration, false);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/modal/DefaultCoverTransitCallbacks.js
var DefaultCoverTransitInCallback = function(cover, duration) {
	if (cover._modalAlphaSave !== void 0) cover.alpha = cover._modalAlphaSave;
	else cover._modalAlphaSave = cover.alpha;
	fade_in_default(cover, duration, cover.alpha);
};
var DefaultCoverTransitOutCallback = function(cover, duration) {
	fade_out_destroy_default(cover, duration, false);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/bounds/IsPointInBounds.js
var IsPointInBounds = function(gameObject, x, y, preTest, postTest) {
	if (!gameObject) return false;
	if (preTest && !preTest(gameObject, x, y)) return false;
	if (!GetBounds(gameObject, true).contains(x, y)) return false;
	if (postTest && !postTest(gameObject, x, y)) return false;
	return true;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/modal/Modal.js
var GetValue$199 = __webpack_exports__Utils.Objects.GetValue;
var Modal$3 = class extends OpenCloseTransition {
	constructor(gameObject, config) {
		if (config === void 0) config = {};
		if (config.transitIn == null) config.transitIn = TransitionMode$1.popUp;
		if (config.transitOut == null) config.transitOut = TransitionMode$1.scaleDown;
		config.destroy = GetValue$199(config, "destroy", true);
		super(gameObject, config);
		var coverConfig = GetValue$199(config, "cover");
		this.cover = coverConfig !== false ? CreateCover(gameObject, coverConfig) : void 0;
		if (this.cover) {
			this.setCoverTransitInCallback(GetValue$199(coverConfig, "transitIn", DefaultCoverTransitInCallback));
			this.setCoverTransitOutCallback(GetValue$199(coverConfig, "transitOut", DefaultCoverTransitOutCallback));
		}
		var touchOutsideClose = GetValue$199(config, "touchOutsideClose", false);
		var timeOutDuration = GetValue$199(config, "duration.hold", -1);
		var timeOutClose = GetValue$199(config, "timeOutClose", timeOutDuration >= 0);
		var anyTouchClose = GetValue$199(config, "anyTouchClose", false);
		if (GetValue$199(config, "manualClose", false)) {
			touchOutsideClose = false;
			anyTouchClose = false;
			timeOutClose = false;
		}
		if (anyTouchClose) touchOutsideClose = false;
		if (timeOutClose) this.setDisplayTime(timeOutDuration);
		else this.setDisplayTime(-1);
		if (anyTouchClose) this.once("open", this.anyTouchClose, this);
		else if (touchOutsideClose) this.once("open", this.touchOutsideClose, this);
		if (GetValue$199(config, "openOnStart", true)) this.delayCall(0, this.requestOpen, this);
	}
	shutdown(fromScene) {
		if (this.isShutdown) return;
		if (!this.cover) this.scene.input.off("pointerup", this.touchCloseCallback, this);
		if (this.cover && !fromScene) {
			this.cover.destroy();
			this.cover = void 0;
		}
		super.shutdown(fromScene);
	}
	touchOutsideClose() {
		if (this.cover) this.cover.on("pointerup", this.touchCloseCallback, this);
		else this.scene.input.on("pointerup", this.touchCloseCallback, this);
		this.clickOutsideTest = true;
		return this;
	}
	anyTouchClose() {
		if (this.cover) this.cover.once("pointerup", this.touchCloseCallback, this);
		else this.scene.input.once("pointerup", this.touchCloseCallback, this);
		return this;
	}
	touchCloseCallback(pointer) {
		if (this.clickOutsideTest && IsPointInBounds(this.parent, pointer.worldX, pointer.worldY)) return;
		this.requestClose();
	}
	runTransitionInCallback() {
		var duration = super.runTransitionInCallback();
		var cover = this.cover;
		if (cover && this.coverTransitInCallback) this.coverTransitInCallback(cover, duration);
		return duration;
	}
	runTransitionOutCallback() {
		var duration = super.runTransitionOutCallback();
		var cover = this.cover;
		if (cover && this.coverTransitOutCallback) this.coverTransitOutCallback(cover, duration);
		return duration;
	}
	onOpen() {
		var duration = this.displayTime;
		if (duration >= 0) this.delayCall(duration, this.requestClose, this);
		this.emit("open", this.parent, this);
		super.onOpen();
	}
	onClose() {
		this.emit("close", this.closeEventData);
		super.onClose();
	}
	setDisplayTime(time) {
		this.displayTime = time;
		return this;
	}
	setTransitInCallback(callback) {
		if (typeof callback === "string") callback = TransitionMode$1[callback];
		switch (callback) {
			case TransitionMode$1.popUp:
				callback = DefaultTransitCallbacks_default.popUp;
				break;
			case TransitionMode$1.fadeIn: callback = DefaultTransitCallbacks_default.fadeIn;
		}
		super.setTransitInCallback(callback);
		return this;
	}
	setTransitOutCallback(callback) {
		if (typeof callback === "string") callback = TransitionMode$1[callback];
		switch (callback) {
			case TransitionMode$1.scaleDown:
				callback = DefaultTransitCallbacks_default.scaleDown;
				break;
			case TransitionMode$1.fadeOut: callback = DefaultTransitCallbacks_default.fadeOut;
		}
		super.setTransitOutCallback(callback);
		return this;
	}
	setCoverTransitInCallback(callback) {
		this.coverTransitInCallback = callback;
		return this;
	}
	setCoverTransitOutCallback(callback) {
		this.coverTransitOutCallback = callback;
		return this;
	}
};
var TransitionMode$1 = {
	popUp: 0,
	fadeIn: 1,
	scaleDown: 0,
	fadeOut: 1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/modal/ModalPromise.js
var Modal$2 = function(gameObject, config) {
	var modalBehavior = new Modal$3(gameObject, config);
	modalBehavior.on("open", function() {
		gameObject.emit("modal.open", modalBehavior);
	});
	modalBehavior.on("close", function(closeEventData) {
		gameObject.emit("modal.close", closeEventData, modalBehavior);
	});
	gameObject.on("modal.requestClose", modalBehavior.requestClose, modalBehavior);
	return modalBehavior;
};
var ModalPromise = function(gameObject, config) {
	var modalBehavior = Modal$2(gameObject, config);
	return new Promise(function(resolve, reject) {
		modalBehavior.once("close", function(closeEventData) {
			resolve(closeEventData);
		});
	});
};
var ModalClose = function(gameObject, closeEventData) {
	gameObject.emit("modal.requestClose", closeEventData);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/object/IsFunction.js
var IsFunction = function(obj) {
	return obj && typeof obj === "function";
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/ModalMethods.js
var ModalMethods_default$1 = {
	modal(config, onClose) {
		if (IsFunction(config)) {
			onClose = config;
			config = void 0;
		}
		if (this._modalBehavior === void 0) {
			if (this.onCreateModalBehavior) this.onCreateModalBehavior(this, config);
			this._modalBehavior = Modal$2(this, config);
		}
		if (onClose) this._modalBehavior.once("close", onClose);
		this._modalBehavior.requestOpen();
		return this;
	},
	modalPromise(config) {
		var self = this;
		return new Promise(function(resolve, reject) {
			self.modal(config, resolve);
		});
	},
	modalClose(closeEventData) {
		ModalClose(this, closeEventData);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/addevent/BindEventWithGameObject.js
var BindEventWithGameObject = function(gameObject, eventEmitter, eventName, callback, scope, once) {
	if (once === void 0) once = false;
	eventEmitter[once ? "once" : "on"](eventName, callback, scope);
	gameObject.once("destroy", function() {
		eventEmitter.off(eventName, callback, scope);
	});
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/BindEventMethods.js
var BindSceneEvent = function(eventName, gameObject, callback, scope, once) {
	if (IsFunction(gameObject)) {
		once = scope;
		scope = callback;
		callback = gameObject;
		gameObject = this;
	}
	var eventEmitter = this.scene.events;
	this.bindEvent(gameObject, eventEmitter, eventName, callback, scope, once);
	return this;
};
var BindEventMethods_default = {
	bindEvent(gameObject, eventEmitter, eventName, callback, scope, once) {
		if (typeof eventEmitter === "string") {
			once = scope;
			scope = callback;
			callback = eventName;
			eventName = eventEmitter;
			eventEmitter = gameObject;
			gameObject = this;
		}
		BindEventWithGameObject(gameObject, eventEmitter, eventName, callback, scope, once);
		return this;
	},
	bindScenePreupdateEvent(gameObject, callback, scope, once) {
		BindSceneEvent.call(this, "preupdate", gameObject, callback, scope, once);
		return this;
	},
	bindSceneUpdateEvent(gameObject, callback, scope, once) {
		BindSceneEvent.call(this, "update", gameObject, callback, scope, once);
		return this;
	},
	bindScenePostupdateEvent(gameObject, callback, scope, once) {
		BindSceneEvent.call(this, "postupdate", gameObject, callback, scope, once);
		return this;
	},
	bindSceneRenderEvent(gameObject, callback, scope, once) {
		BindSceneEvent.call(this, "render", gameObject, callback, scope, once);
		return this;
	},
	bindScenePauseEvent(gameObject, callback, scope, once) {
		BindSceneEvent.call(this, "pause", gameObject, callback, scope, once);
		return this;
	},
	bindSceneResumeEvent(gameObject, callback, scope, once) {
		BindSceneEvent.call(this, "resume", gameObject, callback, scope, once);
		return this;
	},
	bindSceneSleepEvent(gameObject, callback, scope, once) {
		BindSceneEvent.call(this, "sleep", gameObject, callback, scope, once);
		return this;
	},
	bindSceneWakeEvent(gameObject, callback, scope, once) {
		BindSceneEvent.call(this, "wake", gameObject, callback, scope, once);
		return this;
	},
	bindSceneShutdownEvent(gameObject, callback, scope, once) {
		BindSceneEvent.call(this, "shutdown", gameObject, callback, scope, once);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/input/GetPointerWorldXY.js
var GetPointerWorldXY = function(pointer, targetCamera, out) {
	var camera = pointer.camera;
	if (!camera) return null;
	if (out === void 0) out = {};
	else if (out === true) out = globalOut$1;
	if (camera === targetCamera) {
		out.x = pointer.worldX;
		out.y = pointer.worldY;
	} else camera.getWorldPoint(pointer.x, pointer.y, out);
	return out;
};
var globalOut$1 = {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/input/PointerTest.js
var PointerTest = function(gameObject, pointer, mainTest, preTest, postTest) {
	var mainCamera = gameObject.scene.sys.cameras.main, worldXY;
	var useScreenXY = gameObject.scrollFactorX === 0 && gameObject.scrollFactorY === 0;
	if (pointer) {
		if (useScreenXY) return mainTest(gameObject, pointer.x, pointer.y, preTest, postTest);
		else {
			worldXY = GetPointerWorldXY(pointer, mainCamera, true);
			if (!worldXY) return false;
			return mainTest(gameObject, worldXY.x, worldXY.y, preTest, postTest);
		}
	} else {
		var inputManager = gameObject.scene.input.manager;
		var pointersTotal = inputManager.pointersTotal;
		var pointers = inputManager.pointers;
		for (var i = 0; i < pointersTotal; i++) {
			pointer = pointers[i];
			if (useScreenXY) {
				if (mainTest(gameObject, pointer.x, pointer.y, preTest, postTest)) return true;
			} else {
				worldXY = GetPointerWorldXY(pointer, mainCamera, true);
				if (!worldXY) continue;
				if (mainTest(gameObject, worldXY.x, worldXY.y, preTest, postTest)) return true;
			}
		}
		return false;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/input/IsPointerInBounds.js
var IsPointerInBounds = function(gameObject, pointer, preTest, postTest) {
	return PointerTest(gameObject, pointer, IsPointInBounds, preTest, postTest);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/IsInTouching.js
var IsInTouching = function(pointer, gameObject) {
	if (IsGameObject(pointer) || typeof pointer === "string") {
		gameObject = pointer;
		pointer = void 0;
	}
	if (gameObject === void 0) gameObject = this;
	else if (typeof gameObject === "string") gameObject = this.getElement(gameObject);
	return IsPointerInBounds(gameObject, pointer);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/object/IsArray.js
var IsArray = function(obj) {
	return Object.prototype.toString.call(obj) === "[object Array]";
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/ContainsPoint.js
var ContainsPoint$1 = function(gameObject, x, y, preTest, postTest) {
	return IsPointInBounds(gameObject, x, y, GetPreTestCallback(preTest), postTest);
};
var IsShownSizer = function(gameObject) {
	return !(gameObject.rexSizer && gameObject.rexSizer.hidden);
};
var GetPreTestCallback = function(preTest) {
	if (!preTest) return IsShownSizer;
	return function(gameObject, x, y) {
		if (!IsShownSizer(gameObject)) return false;
		preTest(gameObject, x, y);
		return true;
	};
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/PointToChild.js
var PointToChild$2 = function(x, y, preTest, postTest, children) {
	if (!IsFunction(preTest)) {
		children = preTest;
		preTest = void 0;
		postTest = void 0;
	}
	if (children === void 0) {
		if (this.sizerChildren) children = this.sizerChildren;
		else if (this.isRexContainerLite) children = this.children;
		else children = this.list;
	}
	if (IsArray(children)) {
		var child;
		for (var i = 0, cnt = children.length; i < cnt; i++) {
			child = children[i];
			if (ContainsPoint$1(child, x, y, preTest, postTest)) return child;
		}
	} else {
		var child;
		for (var key in children) {
			child = children[key];
			if (ContainsPoint$1(child, x, y, preTest, postTest)) return child;
		}
	}
	return null;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/CopyState.js
var CopyState = function(gamObject, out) {
	if (out === void 0) out = {};
	else if (out === true) out = GlobState;
	out.x = gamObject.x;
	out.y = gamObject.y;
	out.scaleX = gamObject.scaleX;
	out.scaleY = gamObject.scaleY;
	out.width = gamObject.width;
	out.height = gamObject.height;
	out.displayWidth = gamObject.displayWidth;
	out.displayHeight = gamObject.displayHeight;
	return out;
};
var GlobState = {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/utils/PreLayoutChild.js
var PreLayoutChild = function(child) {
	if (this.sizerEventsEnable) {
		CopyState(child, this.getChildPrevState(child));
		this.layoutedChildren.push(child);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/utils/LayoutChild.js
var LayoutChild = function(child, x, y, width, height, align, offsetX, offsetY) {
	if (offsetX === void 0) offsetX = 0;
	if (offsetY === void 0) offsetY = 0;
	AlignIn(child, x, y, width, height, align);
	child.x += offsetX;
	child.y += offsetY;
	this.resetChildPositionState(child);
	if (this.sizerEventsEnable) child.emit("sizer.postlayout", child, this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/LayoutBackgrounds.js
var ALIGN_CENTER$4 = __webpack_exports__Display.Align.CENTER;
var LayoutBackgrounds = function() {
	if (this.backgroundChildren === void 0) return;
	var backgrounds = this.backgroundChildren;
	var startX = this.left, startY = this.top;
	var parentWidth = this.width * this.scaleX, parentHeight = this.height * this.scaleY;
	var child, childConfig, padding, x, y, width, height;
	for (var i = 0, cnt = backgrounds.length; i < cnt; i++) {
		child = backgrounds[i];
		childConfig = child.rexSizer;
		if (childConfig.hidden) continue;
		padding = childConfig.padding;
		PreLayoutChild.call(this, child);
		x = startX + padding.left * this.scaleX;
		y = startY + padding.top * this.scaleY;
		width = parentWidth - (padding.left + padding.right) * this.scaleX;
		height = parentHeight - (padding.top + padding.bottom) * this.scaleY;
		ResizeGameObject(child, width, height);
		LayoutChild.call(this, child, x, y, width, height, ALIGN_CENTER$4, 0, 0);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/SetDraggable.js
var IsPlainObject$34 = __webpack_exports__Utils.Objects.IsPlainObject;
var SetDraggable = function(sensor, draggable, dragTarget) {
	if (IsPlainObject$34(sensor)) {
		var config = sensor;
		sensor = config.sensor;
		dragTarget = config.target;
		draggable = config.draggable;
	} else if (typeof draggable !== "boolean") {
		dragTarget = draggable;
		draggable = void 0;
	}
	var sensorType = typeof sensor;
	if (sensorType === "string") {
		var sensorName = sensor;
		sensor = this.getElement(sensorName);
		if (!sensor) {
			console.error(`Can get element '${sensorName}'`);
			return this;
		}
	} else if (sensor === void 0 || sensorType != "object") {
		draggable = sensor;
		sensor = this;
	}
	if (draggable === void 0) draggable = true;
	if (sensor.input && sensor.input._rexUIDragSizer) sensor.input.draggable = draggable;
	else if (draggable) {
		sensor.setInteractive();
		sensor.scene.input.setDraggable(sensor);
		sensor.on("drag", function(pointer, dragX, dragY) {
			var currentDragTarget = dragTarget === void 0 ? this.getTopmostSizer() : dragTarget;
			currentDragTarget.x += dragX - sensor.x;
			currentDragTarget.y += dragY - sensor.y;
			currentDragTarget.emit("sizer.drag", pointer, dragX, dragY);
		}, this).on("dragstart", function(pointer, dragX, dragY) {
			(dragTarget === void 0 ? this.getTopmostSizer() : dragTarget).emit("sizer.dragstart", pointer, dragX, dragY);
		}, this).on("dragend", function(pointer, dragX, dragY, dropped) {
			(dragTarget === void 0 ? this.getTopmostSizer() : dragTarget).emit("sizer.dragend", pointer, dragX, dragY, dropped);
		}, this).on("drop", function(pointer, dropZone) {
			(dragTarget === void 0 ? this.getTopmostSizer() : dragTarget).emit("sizer.drop", pointer, dropZone);
		});
		sensor.input._rexUIDragSizer = true;
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/click/Click.js
var Click_default = Button;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/ClickMethods.js
var GetClickController = function(gameObject, config) {
	if (gameObject._click === void 0) gameObject._click = new Click_default(gameObject, config);
	return gameObject._click;
};
var OnClick = function(gameObject, callback, scope, config) {
	GetClickController(gameObject, config).on("click", callback, scope);
};
var ClickMethods_default = {
	getClickController(gameObject, config) {
		if (!gameObject) gameObject = this;
		return GetClickController(gameObject, config);
	},
	onClick(gameObject, callback, scope, config) {
		if (typeof gameObject === "function") {
			config = scope;
			scope = callback;
			callback = gameObject;
			gameObject = this;
		}
		OnClick(gameObject, callback, scope, config);
		return this;
	},
	offClick(gameObject, callback, scope) {
		if (typeof gameObject === "function") {
			scope = callback;
			callback = gameObject;
			gameObject = this;
		}
		if (gameObject._click === void 0) return this;
		gameObject._click.off("click", callback, scope);
		return this;
	},
	enableClick(gameObject, enabled) {
		if (typeof gameObject === "boolean") {
			enabled = gameObject;
			gameObject = void 0;
		}
		if (gameObject === void 0) gameObject = this;
		if (gameObject._click === void 0) return this;
		gameObject._click.setEnable(enabled);
		return this;
	},
	disableClick(gameObject) {
		if (gameObject === void 0) gameObject = this;
		if (gameObject._click === void 0) return this;
		gameObject._click.setEnable(false);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/input/clickoutside/ClickOutside.js
var GetValue$198 = __webpack_exports__Utils.Objects.GetValue;
var ClickOutside = class extends ComponentBase {
	constructor(gameObject, config) {
		super(gameObject, config);
		this._enable = void 0;
		var inputConfig = GetValue$198(config, "inputConfig", void 0);
		if (inputConfig) gameObject.setInteractive(inputConfig);
		this.resetFromJSON(config);
		this.boot();
	}
	resetFromJSON(o) {
		this.pointer = void 0;
		this.lastClickTime = void 0;
		this.setEnable(GetValue$198(o, "enable", true));
		this.setMode(GetValue$198(o, "mode", 1));
		this.setClickInterval(GetValue$198(o, "clickInterval", 100));
		this.setDragThreshold(GetValue$198(o, "threshold", void 0));
		return this;
	}
	boot() {
		var scene = this.parent.scene;
		scene.input.on("pointerdown", this.onPress, this);
		scene.input.on("pointerup", this.onRelease, this);
		scene.input.on("pointermove", this.onMove, this);
	}
	shutdown(fromScene) {
		if (this.isShutdown) return;
		var scene = this.parent.scene;
		scene.input.off("pointerdown", this.onPress, this);
		scene.input.off("pointerup", this.onRelease, this);
		scene.input.off("pointermove", this.onMove, this);
		this.pointer = null;
		super.shutdown(fromScene);
	}
	get enable() {
		return this._enable;
	}
	set enable(e) {
		if (this._enable === e) return;
		if (!e) this.cancel();
		this._enable = e;
		var eventName = e ? "enable" : "disable";
		this.emit(eventName, this, this.parent);
	}
	setEnable(e) {
		if (e === void 0) e = true;
		this.enable = e;
		return this;
	}
	toggleEnable() {
		this.setEnable(!this.enable);
		return this;
	}
	setMode(m) {
		if (typeof m === "string") m = CLICKMODE[m];
		this.mode = m;
		return this;
	}
	setClickInterval(interval) {
		this.clickInterval = interval;
		return this;
	}
	setDragThreshold(distance) {
		this.dragThreshold = distance;
		return this;
	}
	isPointerInside(pointer) {
		var gameObject = this.parent;
		return (gameObject.input ? IsPointerInHitArea : IsPointerInBounds)(gameObject, pointer);
	}
	onPress(pointer) {
		if (!this.parent.willRender(pointer.camera)) return;
		if (this.pointer !== void 0) return;
		if (this.isPointerInside(pointer)) return;
		this.pointer = pointer;
		if (this.mode === 0) {
			if (!this.isPointerInside(pointer)) this.click(pointer.downTime, pointer);
		}
	}
	onRelease(pointer) {
		if (!this.parent.willRender(pointer.camera)) return;
		if (this.pointer !== pointer) return;
		if (this.mode === 1) {
			if (!this.isPointerInside(pointer)) this.click(pointer.upTime, pointer);
		}
		this.pointer = void 0;
	}
	onMove(pointer, localX, localY, event) {
		if (this.pointer !== pointer) return;
		if (this.dragThreshold === void 0) return;
		if (this.mode === 1) {
			if (pointer.getDistance() >= this.dragThreshold || this.isPointerInside(pointer)) this.cancel();
		}
	}
	click(nowTime, pointer) {
		if (!this.enable) return this;
		if (nowTime === void 0) {
			this.emit("clickoutside", this, this.parent, pointer);
			return this;
		}
		this.pointer = void 0;
		var lastClickTime = this.lastClickTime;
		if (lastClickTime !== void 0 && nowTime - lastClickTime <= this.clickInterval) return this;
		this.lastClickTime = nowTime;
		this.emit("clickoutside", this, this.parent, pointer);
		return this;
	}
	cancel() {
		this.pointer = void 0;
		return this;
	}
};
var CLICKMODE = {
	press: 0,
	pointerdown: 0,
	release: 1,
	pointerup: 1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/clickoutside/ClickOutside.js
var ClickOutside_default = ClickOutside;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/ClickOutsideMethods.js
var GetClickOutsideController = function(gameObject, config) {
	if (gameObject._clickOutside === void 0) gameObject._clickOutside = new ClickOutside_default(gameObject, config);
	return gameObject._clickOutside;
};
var OnClickOutside = function(gameObject, callback, scope, config) {
	GetClickOutsideController(gameObject, config).on("clickoutside", callback, scope);
};
var ClickOutsideMethods_default = {
	getClickOutsideController(gameObject, config) {
		if (!gameObject) gameObject = this;
		return GetClickOutsideController(gameObject, config);
	},
	onClickOutside(gameObject, callback, scope, config) {
		if (typeof gameObject === "function") {
			config = scope;
			scope = callback;
			callback = gameObject;
			gameObject = this;
		}
		OnClickOutside(gameObject, callback, scope, config);
		return this;
	},
	offClickOutside(gameObject, callback, scope) {
		if (typeof gameObject === "function") {
			scope = callback;
			callback = gameObject;
			gameObject = this;
		}
		if (gameObject._clickOutside === void 0) return this;
		gameObject._clickOutside.off("clickoutside", callback, scope);
		return this;
	},
	enableClickOutside(gameObject, enabled) {
		if (typeof gameObject === "boolean") {
			enabled = gameObject;
			gameObject = void 0;
		}
		if (gameObject === void 0) gameObject = this;
		if (gameObject._clickOutside === void 0) return this;
		gameObject._clickOutside.setEnable(enabled);
		return this;
	},
	disableClickOutside(gameObject) {
		if (gameObject === void 0) gameObject = this;
		if (gameObject._clickOutside === void 0) return this;
		gameObject._clickOutside.setEnable(false);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/time/cooldown/Cooldown.js
var Cooldown = class extends FSM {
	constructor() {
		super({ eventEmitter: false });
		this.goto("IDLE");
	}
	setCooldownTime(time) {
		this.cooldownTime = time;
		this.cooldownMode = time !== void 0;
		return this;
	}
	request() {
		return this.runMethod("request");
	}
	update_IDLE() {
		this.compensationTime = 0;
	}
	request_IDLE() {
		this.next();
		return true;
	}
	next_IDLE() {
		if (this.cooldownMode) return "COOLDOWN";
	}
	enter_COOLDOWN() {
		this.remainderTime = this.cooldownTime + this.compensationTime;
	}
	update_COOLDOWN(time, delta) {
		this.remainderTime -= delta;
		if (this.remainderTime < 0) {
			this.compensationTime = this.cooldownTime > delta ? -this.remainderTime : 0;
			this.goto("IDLE");
		}
	}
	request_COOLDOWN() {
		return false;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/input/intouching/InTouching.js
var GetValue$197 = __webpack_exports__Utils.Objects.GetValue;
var InTouching = class extends ComponentBase {
	constructor(gameObject, config) {
		super(gameObject, config);
		this._enable = void 0;
		this.cooldown = new Cooldown();
		this.parent.setInteractive(GetValue$197(config, "inputConfig", void 0));
		this.resetFromJSON(config);
		this.boot();
	}
	resetFromJSON(o) {
		this.pointer = void 0;
		this.prevIsInTouch = false;
		this.isInTouching = false;
		this.setEnable(GetValue$197(o, "enable", true));
		this.setCooldown(GetValue$197(o, "cooldown", void 0));
		return this;
	}
	boot() {
		var gameObject = this.parent;
		gameObject.on("pointerdown", this.onPointIn, this);
		gameObject.on("pointerover", this.onPointIn, this);
		gameObject.on("pointerup", this.onPointOut, this);
		gameObject.on("pointerout", this.onPointOut, this);
		this.scene.sys.events.on("preupdate", this.preupdate, this);
	}
	shutdown(fromScene) {
		if (this.isShutdown) return;
		this.scene.sys.events.off("preupdate", this.preupdate, this);
		this.pointer = void 0;
		super.shutdown(fromScene);
	}
	get enable() {
		return this._enable;
	}
	set enable(e) {
		if (this._enable === e) return;
		if (!e) {
			this.prevIsInTouch = false;
			this.isInTouching = false;
			this.pointer = void 0;
		}
		this._enable = e;
		return this;
	}
	setEnable(e) {
		if (e === void 0) e = true;
		this.enable = e;
		return this;
	}
	get cooldownTime() {
		return this.cooldown.cooldownTime;
	}
	set cooldownTime(time) {
		this.cooldown.setCooldownTime(time);
	}
	setCooldown(time) {
		this.cooldownTime = time;
		return this;
	}
	toggleEnable() {
		this.setEnable(!this.enable);
		return this;
	}
	onPointIn(pointer, localX, localY) {
		if (!this.enable || !pointer.isDown || this.pointer !== void 0) return;
		this.pointer = pointer;
		this.isInTouching = true;
	}
	onPointOut(pointer) {
		if (!this.enable || this.pointer !== pointer) return;
		this.pointer = void 0;
		this.isInTouching = false;
	}
	preupdate(time, delta) {
		this.cooldown.update(time, delta);
		if (!this.prevIsInTouch && this.isInTouching) this.emit("touchstart", this, this.parent);
		if (this.isInTouching && this.cooldown.request()) this.emit("intouch", this, this.parent, this.pointer);
		if (this.prevIsInTouch && !this.isInTouching) this.emit("touchend", this, this.parent);
		this.prevIsInTouch = this.isInTouching;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/intouching/InTouching.js
var InTouching_default = InTouching;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/TouchingMethods.js
var TouchingMethods_default = {
	isPointerInBounds(target) {
		if (target === void 0) target = this;
		else if (typeof target === "string") target = this.getElement(target);
		if (!target) return false;
		return IsPointerInBounds(target);
	},
	onTouching(gameObject, callback, scope, config) {
		if (!gameObject) return this;
		if (typeof gameObject === "function") {
			config = scope;
			scope = callback;
			callback = gameObject;
			gameObject = this;
		}
		if (gameObject._inTouching === void 0) gameObject._inTouching = new InTouching_default(gameObject, config);
		gameObject._inTouching.on("intouch", callback, scope);
		return this;
	},
	offTouching(gameObject, callback, scope) {
		if (typeof gameObject === "function") {
			scope = callback;
			callback = gameObject;
			gameObject = this;
		}
		if (gameObject._inTouching === void 0) return this;
		gameObject._inTouching.off("intouch", callback, scope);
		return this;
	},
	onTouchingEnd(gameObject, callback, scope, config) {
		if (!gameObject) return this;
		if (typeof gameObject === "function") {
			config = scope;
			scope = callback;
			callback = gameObject;
			gameObject = this;
		}
		if (gameObject._inTouching === void 0) gameObject._inTouching = new InTouching_default(gameObject, config);
		gameObject._inTouching.on("touchend", callback, scope);
		return this;
	},
	offTouchingEnd(gameObject, callback, scope) {
		if (typeof gameObject === "function") {
			scope = callback;
			callback = gameObject;
			gameObject = this;
		}
		if (gameObject._inTouching === void 0) return this;
		gameObject._inTouching.off("touchend", callback, scope);
		return this;
	},
	enableTouching(gameObject, enabled) {
		if (typeof gameObject === "boolean") {
			enabled = gameObject;
			gameObject = void 0;
		}
		if (gameObject === void 0) gameObject = this;
		if (gameObject._inTouching === void 0) return this;
		gameObject._inTouching.setEnable(enabled);
		return this;
	},
	disableTouching(gameObject) {
		if (gameObject === void 0) gameObject = this;
		if (gameObject._inTouching === void 0) return this;
		gameObject._inTouching.setEnable(false);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/HoverMethods.js
var HoverMethods_default = {
	onOver(gameObject, callback, scope) {
		if (!gameObject) return this;
		if (typeof gameObject === "function") {
			scope = callback;
			callback = gameObject;
			gameObject = this;
		}
		gameObject.setInteractive().on("pointerover", callback, scope);
		return this;
	},
	onOut(gameObject, callback, scope) {
		if (!gameObject) return this;
		if (typeof gameObject === "function") {
			scope = callback;
			callback = gameObject;
			gameObject = this;
		}
		gameObject.setInteractive().on("pointerout", callback, scope);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/setchildreninteractive/ContainsPoint.js
var ContainsPoint = function(targetMode, gameObjects, x, y) {
	if (targetMode === "parent") {
		var parent;
		for (var i = 0, cnt = gameObjects.length; i < cnt; i++) {
			parent = gameObjects[i];
			if (!ContainsPoint$1(parent, x, y)) continue;
			if (parent.pointToChild) return parent.pointToChild(x, y);
			else return PointToChild$2.call(parent, x, y);
		}
	} else for (var i = 0, cnt = gameObjects.length; i < cnt; i++) {
		var target = gameObjects[i];
		if (ContainsPoint$1(target, x, y)) return target;
	}
	return null;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/setchildreninteractive/EmitChildEvent.js
var EmitChildEvent = function(eventEmitter, eventName, targets, targetMode, worldX, worldY, pointer, event) {
	var child;
	if (worldY === void 0) child = worldX;
	else {
		var firstChild = targets[0];
		if (!firstChild) return;
		var camera = pointer.camera;
		child = ContainsPoint(targetMode, targets, worldX + camera.scrollX * (firstChild.scrollFactorX - 1), worldY + camera.scrollY * (firstChild.scrollFactorY - 1));
	}
	if (!child) return;
	eventEmitter.emit(eventName, child, pointer, event);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/setchildreninteractive/DownChild.js
var GetValue$196 = __webpack_exports__Utils.Objects.GetValue;
var DownChild = function(config) {
	var downConfig = GetValue$196(config, "down", void 0);
	if (downConfig === false) return;
	else if (downConfig === true) downConfig = void 0;
	this.on("pointerdown", OnDown, this);
};
var OnDown = function(pointer, localX, localY, event) {
	var childrenInteractive = this._childrenInteractive;
	EmitChildEvent(childrenInteractive.eventEmitter, `${childrenInteractive.eventNamePrefix}down`, childrenInteractive.targetSizers, childrenInteractive.targetMode, pointer.worldX, pointer.worldY, pointer, event);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/setchildreninteractive/UpChild.js
var GetValue$195 = __webpack_exports__Utils.Objects.GetValue;
var UpChild = function(config) {
	var upConfig = GetValue$195(config, "up", void 0);
	if (upConfig === false) return;
	else if (upConfig === true) upConfig = void 0;
	this.on("pointerup", OnUp, this);
};
var OnUp = function(pointer, localX, localY, event) {
	var childrenInteractive = this._childrenInteractive;
	EmitChildEvent(childrenInteractive.eventEmitter, `${childrenInteractive.eventNamePrefix}up`, childrenInteractive.targetSizers, childrenInteractive.targetMode, pointer.worldX, pointer.worldY, pointer, event);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/setchildreninteractive/OverChild.js
var GetValue$194 = __webpack_exports__Utils.Objects.GetValue;
var OverChild = function(config) {
	var overConfig = GetValue$194(config, "over", void 0);
	if (overConfig === false) return;
	else if (overConfig === true) overConfig = void 0;
	this.on("pointermove", OnMove, this).on("pointerover", OnMove, this).on("pointerout", OnOut, this);
};
var OnMove = function(pointer, localX, localY, event) {
	var childrenInteractive = this._childrenInteractive;
	var firstChild = childrenInteractive.targetSizers[0];
	if (!firstChild) return;
	var camera = pointer.camera;
	var px = pointer.worldX + camera.scrollX * (firstChild.scrollFactorX - 1);
	var py = pointer.worldY + camera.scrollY * (firstChild.scrollFactorY - 1);
	var child = ContainsPoint(childrenInteractive.targetMode, childrenInteractive.targetSizers, px, py);
	var preChild = childrenInteractive.lastOverChild;
	if (child && preChild && child === preChild) return;
	childrenInteractive.lastOverChild = child;
	EmitChildEvent(childrenInteractive.eventEmitter, `${childrenInteractive.eventNamePrefix}out`, childrenInteractive.targetSizers, childrenInteractive.targetMode, preChild, void 0, pointer, event);
	EmitChildEvent(childrenInteractive.eventEmitter, `${childrenInteractive.eventNamePrefix}over`, childrenInteractive.targetSizers, childrenInteractive.targetMode, child, void 0, pointer, event);
};
var OnOut = function(pointer, event) {
	var childrenInteractive = this._childrenInteractive;
	var child = childrenInteractive.lastOverChild;
	childrenInteractive.lastOverChild = null;
	EmitChildEvent(childrenInteractive.eventEmitter, `${childrenInteractive.eventNamePrefix}out`, childrenInteractive.targetSizers, childrenInteractive.targetMode, child, void 0, pointer, event);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/setchildreninteractive/ClickChild.js
var GetValue$193 = __webpack_exports__Utils.Objects.GetValue;
var ClickChild = function(config) {
	var clickConfig = GetValue$193(config, "click", void 0);
	if (clickConfig === false) return;
	else if (clickConfig === true) clickConfig = void 0;
	if (clickConfig === void 0) clickConfig = {};
	if (!clickConfig.hasOwnProperty("threshold")) clickConfig.threshold = 10;
	var childrenInteractive = this._childrenInteractive;
	this._click = new Button(this, clickConfig);
	this._click.on("click", function(button, gameObject, pointer, event) {
		EmitChildEvent(childrenInteractive.eventEmitter, `${childrenInteractive.eventNamePrefix}click`, childrenInteractive.targetSizers, childrenInteractive.targetMode, pointer.worldX, pointer.worldY, pointer, event);
	}, this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/input/gestures/onepointertracer/OnePointerTracer.js
var GetValue$192 = __webpack_exports__Utils.Objects.GetValue;
var OnePointerTracer = class extends TickTask {
	constructor(gameObject, config) {
		var scene = GetSceneObject(gameObject);
		if (scene === gameObject) gameObject = void 0;
		super(scene, config);
		this.gameObject = gameObject;
		if (gameObject) gameObject.setInteractive(GetValue$192(config, "inputConfig", void 0));
		this._enable = void 0;
		this.resetFromJSON(config);
		this.boot();
	}
	resetFromJSON(o) {
		this.setEnable(GetValue$192(o, "enable", true));
		this.setDetectBounds();
		if (this.gameObject === void 0) this.setDetectBounds(GetValue$192(o, "bounds", void 0));
		else this.setDetectBounds();
		this.tracerState = TOUCH0$2;
		this.pointer = void 0;
		this.lastPointer = void 0;
		this.movedState = false;
		this.isTouchingAnyObject = false;
		return this;
	}
	boot() {
		super.boot();
		if (this.gameObject) this.gameObject.on("pointerdown", this.onPointerDown, this);
		else this.scene.input.on("pointerdown", this.onPointerDown, this);
		this.scene.input.on("pointerup", this.onPointerUp, this);
		this.scene.input.on("gameout", this.dragCancel, this);
		this.scene.input.on("pointermove", this.onPointerMove, this);
		this.scene.sys.events.once("shutdown", this.destroy, this);
	}
	shutdown(fromScene) {
		if (!this.scene) return;
		if (this.gameObject) {} else this.scene.input.off("pointerdown", this.onPointerDown, this);
		this.scene.input.off("pointerup", this.onPointerUp, this);
		this.scene.input.off("gameout", this.dragCancel, this);
		this.scene.input.off("pointermove", this.onPointerMove, this);
		this.scene.sys.events.off("shutdown", this.destroy, this);
		this.gameObject = void 0;
		this.bounds = void 0;
		this.pointer = void 0;
		this.lastPointer = void 0;
		this.movedState = false;
		super.shutdown(fromScene);
	}
	get enable() {
		return this._enable;
	}
	set enable(e) {
		if (this._enable === e) return;
		if (!e) this.dragCancel();
		this._enable = e;
		return this;
	}
	setEnable(e) {
		if (e === void 0) e = true;
		this.enable = e;
		return this;
	}
	setDetectBounds(bounds) {
		this.bounds = bounds;
		return this;
	}
	toggleEnable() {
		this.setEnable(!this.enable);
		return this;
	}
	onPointerDown(pointer, gameObjects) {
		if (!this.enable) return;
		if (this.pointer !== void 0) return;
		if (!(this.bounds ? this.bounds.contains(pointer.x, pointer.y) : true)) return;
		if (this.pointer === pointer) return;
		this.pointer = pointer;
		this.pointerCamera = pointer.camera;
		this.lastPointer = pointer;
		this.movedState = false;
		this.tracerState = TOUCH1$2;
		if (this.gameObject === void 0) this.isTouchingAnyObject = gameObjects.length > 0;
		this.onDragStart();
	}
	onPointerUp(pointer) {
		if (!this.enable) return;
		if (!(this.bounds ? this.bounds.contains(pointer.x, pointer.y) : true)) return;
		if (this.pointer !== pointer) return;
		this.pointer = void 0;
		this.pointerCamera = void 0;
		this.movedState = false;
		this.tracerState = TOUCH0$2;
		this.onDragEnd();
	}
	onPointerMove(pointer) {
		if (!this.enable) return;
		if (pointer.isDown) {
			var isInsideBounds = this.bounds ? this.bounds.contains(pointer.x, pointer.y) : true;
			var isCatchedPointer = this.pointer === pointer;
			if (!isCatchedPointer && isInsideBounds) {} else if (isCatchedPointer && !isInsideBounds) this.onPointerUp(pointer);
			else {
				if (!this.movedState) this.movedState = pointer.x !== pointer.downX || pointer.y !== pointer.downY;
				if (this.movedState) this.onDrag();
			}
		}
	}
	dragCancel() {
		if (this.tracerState === TOUCH1$2) this.onDragEnd();
		this.pointer = void 0;
		this.tracerState = TOUCH0$2;
		return this;
	}
	onDragStart() {
		this.emit("dragstart", this);
	}
	onDragEnd() {
		this.emit("dragend", this);
	}
	onDrag() {
		this.emit("drag", this);
	}
	preUpdate(time, delta) {}
	postUpdate(time, delta) {}
	startTicking() {
		super.startTicking();
		this.scene.sys.events.on("preupdate", this.preUpdate, this);
		this.scene.sys.events.on("postupdate", this.postUpdate, this);
	}
	stopTicking() {
		super.stopTicking();
		if (this.scene) {
			this.scene.sys.events.off("preupdate", this.preUpdate, this);
			this.scene.sys.events.off("postupdate", this.postUpdate, this);
		}
	}
	setRecongizedStateObject(stateObject) {
		this.recongizedState = stateObject;
		return this;
	}
	get state() {
		return this.recongizedState.state;
	}
	set state(newState) {
		this.recongizedState.state = newState;
	}
	cancel() {
		this.state = IDLE$7;
		return this;
	}
	isPointerInGameObject(gameObject, preTest, postTest) {
		var pointer = this.lastPointer;
		if (!pointer) return false;
		return IsPointerInBounds(gameObject, pointer, preTest, postTest);
	}
};
var TOUCH0$2 = 0;
var TOUCH1$2 = 1;
var IDLE$7 = "IDLE";
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/input/gestures/tap/Tap.js
var GetValue$191 = __webpack_exports__Utils.Objects.GetValue;
var DistanceBetween$5 = __webpack_exports__Math.Distance.Between;
var Tap = class extends OnePointerTracer {
	constructor(gameObject, config) {
		super(gameObject, config);
		var self = this;
		this.setRecongizedStateObject(new fsm_default({
			states: {
				IDLE: {
					enter: function() {
						self.stop();
						self.tapsCount = 0;
						self.x = 0;
						self.y = 0;
						self.worldX = 0;
						self.worldY = 0;
						self.lastPointer = void 0;
					},
					exit: function() {
						var pointer = self.lastPointer;
						self.x = pointer.x;
						self.y = pointer.y;
						self.worldX = pointer.worldX;
						self.worldY = pointer.worldY;
					}
				},
				BEGIN: { enter: function() {
					self.start();
					self.tapsCount = 0;
					self.emit("tappingstart", self, self.gameObject, self.lastPointer);
				} },
				RECOGNIZED: { enter: function() {
					self.start();
					self.emit("tap", self, self.gameObject, self.lastPointer);
					self.emit(`${self.tapsCount}tap`, self, self.gameObject, self.lastPointer);
				} }
			},
			init: function() {
				this.state = IDLE$6;
			},
			eventEmitter: false
		}));
	}
	resetFromJSON(o) {
		super.resetFromJSON(o);
		this.setHoldTime(GetValue$191(o, "time", 250));
		this.setTapInterval(GetValue$191(o, "tapInterval", 200));
		this.setDragThreshold(GetValue$191(o, "threshold", 9));
		this.setTapOffset(GetValue$191(o, "tapOffset", 10));
		var taps = GetValue$191(o, "taps", void 0);
		if (taps !== void 0) this.setTaps(taps);
		else {
			this.setMaxTaps(GetValue$191(o, "maxTaps", void 0));
			this.setMinTaps(GetValue$191(o, "minTaps", void 0));
		}
		return this;
	}
	onDragStart() {
		switch (this.state) {
			case IDLE$6:
				this.state = BEGIN$5;
				break;
			case BEGIN$5:
				var pointer = this.lastPointer;
				if (DistanceBetween$5(pointer.upX, pointer.upY, pointer.x, pointer.y) > this.tapOffset) {
					this.state = RECOGNIZED$5;
					this.state = BEGIN$5;
				}
				break;
			case RECOGNIZED$5: this.state = BEGIN$5;
		}
	}
	onDragEnd() {
		if (this.state === BEGIN$5) {
			this.tapsCount++;
			this.emit("tapping", this, this.gameObject, this.lastPointer);
			if (this.maxTaps !== void 0 && this.tapsCount === this.maxTaps) this.state = RECOGNIZED$5;
		}
	}
	onDrag() {
		if (this.state === IDLE$6) return;
		if (this.pointer.getDistance() > this.dragThreshold) this.state = IDLE$6;
	}
	preUpdate(time, delta) {
		if (!this.isRunning || !this.enable) return;
		if (this.state === BEGIN$5) {
			var pointer = this.lastPointer;
			if (pointer.isDown) {
				if (time - pointer.downTime > this.holdTime) this.state = IDLE$6;
			} else if (time - pointer.upTime > this.tapInterval) {
				if (this.minTaps === void 0 || this.tapsCount >= this.minTaps) this.state = RECOGNIZED$5;
				else this.state = IDLE$6;
			}
		}
	}
	postUpdate(time, delta) {
		if (!this.isRunning || !this.enable) return;
		if (this.state === RECOGNIZED$5) this.state = IDLE$6;
	}
	get isTapping() {
		return this.state === RECOGNIZED$5;
	}
	get isTapped() {
		return this.isTapping;
	}
	setHoldTime(time) {
		this.holdTime = time;
		return this;
	}
	setTapInterval(time) {
		this.tapInterval = time;
		return this;
	}
	setDragThreshold(distance) {
		this.dragThreshold = distance;
		return this;
	}
	setTapOffset(distance) {
		this.tapOffset = distance;
		return this;
	}
	setMaxTaps(taps) {
		this.maxTaps = taps;
		return this;
	}
	setMinTaps(taps) {
		this.minTaps = taps;
		return this;
	}
	setTaps(minTaps, maxTaps) {
		if (maxTaps === void 0) maxTaps = minTaps;
		this.setMinTaps(minTaps).setMaxTaps(maxTaps);
		return this;
	}
};
var IDLE$6 = "IDLE";
var BEGIN$5 = "BEGIN";
var RECOGNIZED$5 = "RECOGNIZED";
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/input/gestures/press/Press.js
var GetValue$190 = __webpack_exports__Utils.Objects.GetValue;
var Press = class extends OnePointerTracer {
	constructor(gameObject, config) {
		super(gameObject, config);
		var self = this;
		this.setRecongizedStateObject(new fsm_default({
			states: {
				IDLE: {
					enter: function() {
						self.x = 0;
						self.y = 0;
						self.worldX = 0;
						self.worldY = 0;
						self.lastPointer = void 0;
					},
					exit: function() {
						var pointer = self.lastPointer;
						self.x = pointer.x;
						self.y = pointer.y;
						self.worldX = pointer.worldX;
						self.worldY = pointer.worldY;
					}
				},
				BEGIN: {
					enter: function() {
						self.start();
					},
					exit: function() {
						self.stop();
					}
				},
				RECOGNIZED: {
					enter: function() {
						self.emit("pressstart", self, self.gameObject, self.lastPointer);
					},
					exit: function() {
						self.emit("pressend", self, self.gameObject, self.lastPointer);
					}
				}
			},
			init: function() {
				this.state = IDLE$5;
			},
			eventEmitter: false
		}));
	}
	resetFromJSON(o) {
		super.resetFromJSON(o);
		this.setDragThreshold(GetValue$190(o, "threshold", 9));
		this.setHoldTime(GetValue$190(o, "time", 251));
		return this;
	}
	onDragStart() {
		this.state = BEGIN$4;
		if (this.holdTime === 0) this.state = RECOGNIZED$4;
	}
	onDragEnd() {
		this.state = IDLE$5;
	}
	onDrag() {
		if (this.state === IDLE$5) return;
		if (this.pointer.getDistance() > this.dragThreshold) this.state = IDLE$5;
	}
	preUpdate(time, delta) {
		if (!this.isRunning || !this.enable) return;
		if (this.state === BEGIN$4) {
			if (time - this.pointer.downTime >= this.holdTime) this.state = RECOGNIZED$4;
		}
	}
	get isPressing() {
		return this.state === RECOGNIZED$4;
	}
	get isPressed() {
		return this.isPressing;
	}
	setHoldTime(time) {
		this.holdTime = time;
		return this;
	}
	setDragThreshold(distance) {
		this.dragThreshold = distance;
		return this;
	}
};
var IDLE$5 = "IDLE";
var BEGIN$4 = "BEGIN";
var RECOGNIZED$4 = "RECOGNIZED";
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/input/gestures/pan/Pan.js
var GetValue$189 = __webpack_exports__Utils.Objects.GetValue;
var Pan = class extends OnePointerTracer {
	constructor(gameObject, config) {
		super(gameObject, config);
		var self = this;
		this.setRecongizedStateObject(new fsm_default({
			states: {
				IDLE: {},
				BEGIN: { enter: function() {
					var pointer = self.pointer;
					self.startX = pointer.x;
					self.startY = pointer.y;
					self.startWorldX = pointer.worldX;
					self.startWorldY = pointer.worldY;
				} },
				RECOGNIZED: {
					enter: function() {
						self.emit("panstart", self, self.gameObject, self.lastPointer);
					},
					exit: function() {
						var pointer = self.lastPointer;
						self.endX = pointer.x;
						self.endY = pointer.y;
						var worldXY = GetPointerWorldXY(pointer, self.pointerCamera, true);
						self.endWorldX = worldXY.x;
						self.endWorldY = worldXY.y;
						self.emit("panend", self, self.gameObject, self.lastPointer);
					}
				}
			},
			init: function() {
				this.state = IDLE$4;
			},
			eventEmitter: false
		}));
	}
	resetFromJSON(o) {
		super.resetFromJSON(o);
		this.setDragThreshold(GetValue$189(o, "threshold", 10));
		return this;
	}
	onDragStart() {
		this.state = BEGIN$3;
		if (this.dragThreshold === 0) this.state = RECOGNIZED$3;
	}
	onDragEnd() {
		this.state = IDLE$4;
	}
	onDrag() {
		switch (this.state) {
			case BEGIN$3:
				if (this.pointer.getDistance() >= this.dragThreshold) {
					this.state = RECOGNIZED$3;
					this.dx = 0;
					this.dy = 0;
					this.dWorldX = 0;
					this.dWorldY = 0;
					var pointer = this.pointer;
					this.x = pointer.x;
					this.y = pointer.y;
					this.worldX = pointer.worldX;
					this.worldY = pointer.worldY;
				}
				break;
			case RECOGNIZED$3:
				var pointerCamera = this.pointerCamera;
				var p1 = this.pointer.position;
				var p0 = this.pointer.prevPosition;
				this.dx = p1.x - p0.x;
				this.dy = p1.y - p0.y;
				this.dWorldX = this.dx / pointerCamera.zoom;
				this.dWorldY = this.dy / pointerCamera.zoom;
				var pointer = this.pointer;
				this.x = pointer.x;
				this.y = pointer.y;
				var worldXY = GetPointerWorldXY(pointer, pointerCamera, true);
				this.worldX = worldXY.x;
				this.worldY = worldXY.y;
				this.emit("pan", this, this.gameObject, this.lastPointer);
		}
	}
	get isPanning() {
		return this.state === RECOGNIZED$3;
	}
	get isPanned() {
		return this.isPanning;
	}
	setDragThreshold(distance) {
		this.dragThreshold = distance;
		return this;
	}
};
var IDLE$4 = "IDLE";
var BEGIN$3 = "BEGIN";
var RECOGNIZED$3 = "RECOGNIZED";
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/input/gestures/swipe/VelocityMethods.js
var DistanceBetween$4 = __webpack_exports__Math.Distance.Between;
var AngleBetween$1 = __webpack_exports__Math.Angle.Between;
var VelocityMethods_default = {
	getDt: function() {
		return GetTickDelta(this.scene);
	},
	getVelocity: function() {
		var p1 = this.pointer.position;
		var p0 = this.pointer.prevPosition;
		return DistanceBetween$4(p0.x, p0.y, p1.x, p1.y) / (this.getDt() * .001);
	},
	getVelocityX: function() {
		var p1 = this.pointer.position;
		var p0 = this.pointer.prevPosition;
		return Math.abs(p1.x - p0.x) / (this.getDt() * .001);
	},
	getVelocityY: function() {
		var p1 = this.pointer.position;
		var p0 = this.pointer.prevPosition;
		return Math.abs(p1.y - p0.y) / (this.getDt() * .001);
	},
	getVelocityAngle: function() {
		var p1 = this.pointer.position;
		var p0 = this.pointer.prevPosition;
		return AngleBetween$1(p0.x, p0.y, p1.x, p1.y);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/math/angle/angletodirections/Const.js
var Const_default = {
	"up&down": 0,
	"left&right": 1,
	"4dir": 2,
	"8dir": 3
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/math/angle/angletodirections/AngleToDirections.js
var AngleToDirections = function(angle, dirMode, out) {
	if (out === void 0) out = {};
	else if (out === true) out = globOut;
	out.left = false;
	out.right = false;
	out.up = false;
	out.down = false;
	angle = (angle + 360) % 360;
	switch (dirMode) {
		case 0:
			if (angle < 180) out.down = true;
			else out.up = true;
			break;
		case 1:
			if (angle > 90 && angle <= 270) out.left = true;
			else out.right = true;
			break;
		case 2:
			if (angle > 45 && angle <= 135) out.down = true;
			else if (angle > 135 && angle <= 225) out.left = true;
			else if (angle > 225 && angle <= 315) out.up = true;
			else out.right = true;
			break;
		case 3: if (angle > 22.5 && angle <= 67.5) {
			out.down = true;
			out.right = true;
		} else if (angle > 67.5 && angle <= 112.5) out.down = true;
		else if (angle > 112.5 && angle <= 157.5) {
			out.down = true;
			out.left = true;
		} else if (angle > 157.5 && angle <= 202.5) out.left = true;
		else if (angle > 202.5 && angle <= 247.5) {
			out.left = true;
			out.up = true;
		} else if (angle > 247.5 && angle <= 292.5) out.up = true;
		else if (angle > 292.5 && angle <= 337.5) {
			out.up = true;
			out.right = true;
		} else out.right = true;
	}
	return out;
};
var globOut = {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/input/gestures/swipe/Swipe.js
var GetValue$188 = __webpack_exports__Utils.Objects.GetValue;
var RadToDeg$5 = __webpack_exports__Math.RadToDeg;
var Swipe = class extends OnePointerTracer {
	constructor(gameObject, config) {
		super(gameObject, config);
		var self = this;
		this.setRecongizedStateObject(new fsm_default({
			states: {
				IDLE: {
					enter: function() {
						self.x = 0;
						self.y = 0;
						self.worldX = 0;
						self.worldY = 0;
					},
					exit: function() {
						var pointer = self.lastPointer;
						self.x = pointer.x;
						self.y = pointer.y;
						self.worldX = pointer.worldX;
						self.worldY = pointer.worldY;
					}
				},
				BEGIN: { enter: function() {
					self.validDrag = false;
				} },
				RECOGNIZED: {
					enter: function() {
						self.start();
						self.updateDirectionStates();
						self.emit("swipe", self, self.gameObject, self.lastPointer);
					},
					exit: function() {
						self.stop();
						self.clearDirectionStates();
					}
				}
			},
			init: function() {
				this.state = IDLE$3;
			},
			eventEmitter: false
		}));
		this.clearDirectionStates();
	}
	resetFromJSON(o) {
		super.resetFromJSON(o);
		this.setDragThreshold(GetValue$188(o, "threshold", 10));
		this.setVelocityThreshold(GetValue$188(o, "velocityThreshold", 1e3));
		this.setDirectionMode(GetValue$188(o, "dir", "8dir"));
		return this;
	}
	onDragStart() {
		this.state = BEGIN$2;
	}
	onDragEnd() {
		this.state = IDLE$3;
	}
	onDrag() {
		if (this.state === BEGIN$2) {
			if (!this.validDrag) this.validDrag = this.dragThreshold === 0 || this.pointer.getDistance() >= this.dragThreshold;
			if (this.validDrag && this.dragVelocity > this.velocityThreshold) this.state = RECOGNIZED$2;
		}
	}
	postUpdate(time, delta) {
		if (!this.isRunning || !this.enable) return;
		if (this.state === RECOGNIZED$2) this.state = IDLE$3;
	}
	get isSwiping() {
		return this.state === RECOGNIZED$2;
	}
	get isSwiped() {
		return this.isSwiping;
	}
	get dragVelocity() {
		var velocity;
		switch (this.dirMode) {
			case 0:
				velocity = this.getVelocityY();
				break;
			case 1:
				velocity = this.getVelocityX();
				break;
			default: velocity = this.getVelocity();
		}
		return velocity;
	}
	setDragThreshold(distance) {
		this.dragThreshold = distance;
		return this;
	}
	setVelocityThreshold(velocity) {
		this.velocityThreshold = velocity;
		return this;
	}
	setDirectionMode(m) {
		if (typeof m === "string") m = Const_default[m];
		this.dirMode = m;
		return this;
	}
	updateDirectionStates() {
		AngleToDirections(RadToDeg$5(this.getVelocityAngle()), this.dirMode, this);
		return this;
	}
	clearDirectionStates() {
		this.left = false;
		this.right = false;
		this.up = false;
		this.down = false;
		return this;
	}
};
Object.assign(Swipe.prototype, VelocityMethods_default);
var IDLE$3 = "IDLE";
var BEGIN$2 = "BEGIN";
var RECOGNIZED$2 = "RECOGNIZED";
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/input/gestures/twopointerstracer/TwoPointersTracer.js
var GetValue$187 = __webpack_exports__Utils.Objects.GetValue;
var SpliceOne$1 = __webpack_exports__Utils.Array.SpliceOne;
var DistanceBetween$3 = __webpack_exports__Math.Distance.Between;
var AngleBetween = __webpack_exports__Math.Angle.Between;
var TwoPointersTracer = class {
	constructor(gameObject, config) {
		var scene = GetSceneObject(gameObject);
		if (scene === gameObject) gameObject = void 0;
		var amount = scene.input.manager.pointersTotal - 1;
		if (amount < 2) scene.input.addPointer(2 - amount);
		this.scene = scene;
		this.gameObject = gameObject;
		if (gameObject) gameObject.setInteractive(GetValue$187(config, "inputConfig", void 0));
		this.setEventEmitter(GetValue$187(config, "eventEmitter", void 0));
		this._enable = void 0;
		this.pointers = [];
		this.movedState = {};
		this.resetFromJSON(config);
		this.boot();
	}
	resetFromJSON(o) {
		this.setEnable(GetValue$187(o, "enable", true));
		this.bounds = GetValue$187(o, "bounds", void 0);
		this.tracerState = TOUCH0$1;
		this.pointers.length = 0;
		Clear$1(this.movedState);
		return this;
	}
	boot() {
		if (this.gameObject) this.gameObject.on("pointerdown", this.onPointerDown, this);
		else this.scene.input.on("pointerdown", this.onPointerDown, this);
		this.scene.input.on("pointerup", this.onPointerUp, this);
		this.scene.input.on("gameout", this.dragCancel, this);
		this.scene.input.on("pointermove", this.onPointerMove, this);
		this.scene.sys.events.once("shutdown", this.destroy, this);
	}
	shutdown() {
		if (!this.scene) return;
		this.destroyEventEmitter();
		this.pointers.length = 0;
		Clear$1(this.movedState);
		if (this.gameObject) {} else this.scene.input.off("pointerdown", this.onPointerDown, this);
		this.scene.input.off("pointerup", this.onPointerUp, this);
		this.scene.input.off("gameout", this.dragCancel, this);
		this.scene.input.off("pointermove", this.onPointerMove, this);
		this.scene.sys.events.off("shutdown", this.destroy, this);
		this.scene = void 0;
		this.gameObject = void 0;
	}
	destroy() {
		this.shutdown();
	}
	get enable() {
		return this._enable;
	}
	set enable(e) {
		if (this._enable === e) return;
		if (!e) this.dragCancel();
		this._enable = e;
		return this;
	}
	setEnable(e) {
		if (e === void 0) e = true;
		this.enable = e;
		return this;
	}
	toggleEnable() {
		this.setEnable(!this.enable);
		return this;
	}
	onPointerDown(pointer) {
		if (!this.enable) return;
		if (this.pointers.length === 2) return;
		if (!(this.bounds ? this.bounds.contains(pointer.x, pointer.y) : true)) return;
		if (this.pointers.indexOf(pointer) !== -1) return;
		this.movedState[pointer.id] = false;
		this.pointers.push(pointer);
		this.pointerCamera = pointer.camera;
		switch (this.tracerState) {
			case TOUCH0$1:
				this.tracerState = TOUCH1$1;
				this.onDrag1Start();
				break;
			case TOUCH1$1:
				this.tracerState = TOUCH2;
				this.onDrag2Start();
		}
	}
	onPointerUp(pointer) {
		if (!this.enable) return;
		if (!(this.bounds ? this.bounds.contains(pointer.x, pointer.y) : true)) return;
		var index = this.pointers.indexOf(pointer);
		if (index === -1) return;
		else {
			delete this.movedState[pointer.id];
			SpliceOne$1(this.pointers, index);
		}
		switch (this.tracerState) {
			case TOUCH1$1:
				this.tracerState = TOUCH0$1;
				this.onDrag1End();
				break;
			case TOUCH2:
				this.tracerState = TOUCH1$1;
				this.onDrag2End();
				this.onDrag1Start();
		}
	}
	onPointerMove(pointer) {
		if (!this.enable) return;
		if (pointer.isDown) {
			var isInsideBounds = this.bounds ? this.bounds.contains(pointer.x, pointer.y) : true;
			var isCatchedPointer = this.pointers.indexOf(pointer) !== -1;
			if (!isCatchedPointer && isInsideBounds) {} else if (isCatchedPointer && !isInsideBounds) this.onPointerUp(pointer);
			else {
				if (!this.movedState[pointer.id]) this.movedState[pointer.id] = pointer.x !== pointer.downX || pointer.y !== pointer.downY;
				if (this.movedState[pointer.id]) switch (this.tracerState) {
					case TOUCH1$1:
						this.onDrag1();
						break;
					case TOUCH2: this.onDrag2();
				}
			}
		}
	}
	dragCancel() {
		if (this.tracerState === TOUCH2) this.onDrag2End();
		this.pointers.length = 0;
		Clear$1(this.movedState);
		this.tracerState = TOUCH0$1;
		return this;
	}
	onDrag1Start() {
		this.emit("drag1start", this);
	}
	onDrag1End() {
		this.emit("drag1end", this);
	}
	onDrag1() {
		this.emit("drag1", this);
	}
	onDrag2Start() {
		this.emit("drag2start", this);
	}
	onDrag2End() {
		this.emit("drag2end", this);
	}
	onDrag2() {
		this.emit("drag2", this);
	}
	get distanceBetween() {
		if (this.tracerState !== TOUCH2) return 0;
		var p0 = this.pointers[0], p1 = this.pointers[1];
		return DistanceBetween$3(p0.x, p0.y, p1.x, p1.y);
	}
	get angleBetween() {
		if (this.tracerState !== TOUCH2) return 0;
		var p0 = this.pointers[0], p1 = this.pointers[1];
		return AngleBetween(p0.x, p0.y, p1.x, p1.y);
	}
	get drag1Vector() {
		var pointer = this.pointers[0];
		if (pointer && this.movedState[pointer.id]) {
			var p1 = pointer.position;
			var p0 = pointer.prevPosition;
			tmpDragVector.x = p1.x - p0.x;
			tmpDragVector.y = p1.y - p0.y;
		} else {
			tmpDragVector.x = 0;
			tmpDragVector.y = 0;
		}
		return tmpDragVector;
	}
	get centerX() {
		if (this.tracerState !== TOUCH2) return 0;
		var p0 = this.pointers[0].position;
		var p1 = this.pointers[1].position;
		return (p0.x + p1.x) / 2;
	}
	get centerY() {
		if (this.tracerState !== TOUCH2) return 0;
		var p0 = this.pointers[0].position;
		var p1 = this.pointers[1].position;
		return (p0.y + p1.y) / 2;
	}
	get prevCenterX() {
		if (this.tracerState !== TOUCH2) return 0;
		var preP0 = this.movedState[this.pointers[0].id] ? this.pointers[0].prevPosition : this.pointers[0].position;
		var preP1 = this.movedState[this.pointers[1].id] ? this.pointers[1].prevPosition : this.pointers[1].position;
		return (preP0.x + preP1.x) / 2;
	}
	get prevCenterY() {
		if (this.tracerState !== TOUCH2) return 0;
		var preP0 = this.movedState[this.pointers[0].id] ? this.pointers[0].prevPosition : this.pointers[0].position;
		var preP1 = this.movedState[this.pointers[1].id] ? this.pointers[1].prevPosition : this.pointers[1].position;
		return (preP0.y + preP1.y) / 2;
	}
	get movementCenterX() {
		return this.centerX - this.prevCenterX;
	}
	get movementCenterY() {
		return this.centerY - this.prevCenterY;
	}
	setRecongizedStateObject(stateObject) {
		this.recongizedState = stateObject;
		return this;
	}
	get state() {
		return this.recongizedState.state;
	}
	set state(newState) {
		this.recongizedState.state = newState;
	}
	cancel() {
		this.state = IDLE$2;
		return this;
	}
	isPointer0InGameObject(gameObject, preTest, postTest) {
		var pointer = this.pointers[0];
		if (!pointer) return false;
		return IsPointerInBounds(gameObject, pointer, preTest, postTest);
	}
	isPointer1InGameObject(gameObject, preTest, postTest) {
		var pointer = this.pointers[1];
		if (!pointer) return false;
		return IsPointerInBounds(gameObject, pointer, preTest, postTest);
	}
};
Object.assign(TwoPointersTracer.prototype, EventEmitterMethods_default);
var tmpDragVector = {};
var TOUCH0$1 = 0;
var TOUCH1$1 = 1;
var TOUCH2 = 2;
var IDLE$2 = "IDLE";
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/input/gestures/pinch/Pinch.js
var GetValue$186 = __webpack_exports__Utils.Objects.GetValue;
var Pinch = class extends TwoPointersTracer {
	constructor(gameObject, config) {
		super(gameObject, config);
		var self = this;
		this.setRecongizedStateObject(new fsm_default({
			states: {
				IDLE: { enter: function() {
					self.prevDistance = void 0;
					self.scaleFactor = 1;
				} },
				BEGIN: {},
				RECOGNIZED: {
					enter: function() {
						self.emit("pinchstart", self);
					},
					exit: function() {
						self.emit("pinchend", self);
					}
				}
			},
			init: function() {
				this.state = IDLE$1;
			},
			eventEmitter: false
		}));
	}
	resetFromJSON(o) {
		super.resetFromJSON(o);
		this.setDragThreshold(GetValue$186(o, "threshold", 0));
		return this;
	}
	onDrag2Start() {
		this.scaleFactor = 1;
		this.prevDistance = this.distanceBetween;
		this.state = BEGIN$1;
		if (this.dragThreshold === 0) this.state = RECOGNIZED$1;
	}
	onDrag2End() {
		this.state = IDLE$1;
	}
	onDrag2() {
		switch (this.state) {
			case BEGIN$1:
				if (this.pointers[0].getDistance() >= this.dragThreshold && this.pointers[1].getDistance() >= this.dragThreshold) {
					var curDistance = this.distanceBetween;
					this.scaleFactor = curDistance / this.prevDistance;
					this.prevDistance = curDistance;
					this.state = RECOGNIZED$1;
				}
				break;
			case RECOGNIZED$1:
				var curDistance = this.distanceBetween;
				this.scaleFactor = curDistance / this.prevDistance;
				this.emit("pinch", this);
				this.prevDistance = curDistance;
		}
	}
	get isPinching() {
		return this.state === RECOGNIZED$1;
	}
	get isPinched() {
		return this.isPinching;
	}
	setDragThreshold(distance) {
		this.dragThreshold = distance;
		return this;
	}
};
var IDLE$1 = "IDLE";
var BEGIN$1 = "BEGIN";
var RECOGNIZED$1 = "RECOGNIZED";
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/actions/RotateObjectAround.js
var RotateAround$3 = __webpack_exports__Math.RotateAround;
var RotateObjectAround = function(gameObject, x, y, angle) {
	RotateAround$3(gameObject, x, y, angle);
	gameObject.rotation += angle;
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/position/ScreenXYToWorldXY.js
var ScreenXYToWorldXY = function(screenX, screenY, camera, out) {
	if (out === void 0) out = {};
	else if (out === true) out = globalOut;
	camera.getWorldPoint(screenX, screenY, out);
	return out;
};
var globalOut = {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/input/gestures/rotate/SpinObject.js
var SpinObject = function(gameObject, camera) {
	if (!this.isRotation) return this;
	if (camera === void 0) camera = this.pointers[0].camera;
	var movementX = this.movementCenterX, movementY = this.movementCenterY;
	var worldXY = ScreenXYToWorldXY(this.centerX, this.centerY, camera, true);
	var centerWorldX = worldXY.x;
	var centerWorldY = worldXY.y;
	var angle = this.rotation;
	if (Array.isArray(gameObject)) {
		var gameObjects = gameObject;
		for (var i = 0, cnt = gameObjects.length; i < cnt; i++) {
			gameObject = gameObjects[i];
			gameObject.x += movementX;
			gameObject.y += movementY;
			RotateObjectAround(gameObject, centerWorldX, centerWorldY, angle);
		}
	} else {
		gameObject.x += movementX;
		gameObject.y += movementY;
		RotateObjectAround(gameObject, centerWorldX, centerWorldY, angle);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/input/gestures/rotate/Rotate.js
var GetValue$185 = __webpack_exports__Utils.Objects.GetValue;
var WrapDegrees$1 = __webpack_exports__Math.Angle.WrapDegrees;
var ShortestBetween$1 = __webpack_exports__Math.Angle.ShortestBetween;
var RadToDeg$4 = __webpack_exports__Math.RadToDeg;
var DegToRad$5 = __webpack_exports__Math.DegToRad;
var Rotate$1 = class extends TwoPointersTracer {
	constructor(gameObject, config) {
		super(gameObject, config);
		var self = this;
		this.setRecongizedStateObject(new fsm_default({
			states: {
				IDLE: { enter: function() {
					self.prevAngle = void 0;
					self.angle = 0;
				} },
				BEGIN: {},
				RECOGNIZED: {
					enter: function() {
						self.emit("rotatestart", self);
					},
					exit: function() {
						self.emit("rotateend", self);
					}
				}
			},
			init: function() {
				this.state = IDLE;
			},
			eventEmitter: false
		}));
	}
	resetFromJSON(o) {
		super.resetFromJSON(o);
		this.setDragThreshold(GetValue$185(o, "threshold", 0));
		return this;
	}
	onDrag2Start() {
		this.prevAngle = WrapDegrees$1(RadToDeg$4(this.angleBetween));
		this.state = BEGIN;
		if (this.dragThreshold === 0) this.state = RECOGNIZED;
	}
	onDrag2End() {
		this.state = IDLE;
	}
	onDrag2() {
		switch (this.state) {
			case BEGIN:
				if (this.pointers[0].getDistance() >= this.dragThreshold && this.pointers[1].getDistance() >= this.dragThreshold) {
					var curAngle = WrapDegrees$1(RadToDeg$4(this.angleBetween));
					this.angle = ShortestBetween$1(this.prevAngle, curAngle);
					this.prevAngle = curAngle;
					this.state = RECOGNIZED;
				}
				break;
			case RECOGNIZED:
				var curAngle = WrapDegrees$1(RadToDeg$4(this.angleBetween));
				this.angle = ShortestBetween$1(this.prevAngle, curAngle);
				this.prevAngle = curAngle;
				this.emit("rotate", this);
		}
	}
	get isRotating() {
		return this.state === RECOGNIZED;
	}
	get isRotated() {
		return this.isRotating;
	}
	get rotation() {
		return DegToRad$5(this.angle);
	}
	setDragThreshold(distance) {
		this.dragThreshold = distance;
		return this;
	}
};
var methods$29 = { spinObject: SpinObject };
Object.assign(Rotate$1.prototype, methods$29);
var IDLE = "IDLE";
var BEGIN = "BEGIN";
var RECOGNIZED = "RECOGNIZED";
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tap/Tap.js
var Tap_default = Tap;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/setchildreninteractive/TapChild.js
var GetValue$184 = __webpack_exports__Utils.Objects.GetValue;
var TapChild = function(config) {
	var tapConfig = GetValue$184(config, "tap", void 0);
	if (tapConfig === false) return;
	else if (tapConfig === true) tapConfig = void 0;
	var childrenInteractive = this._childrenInteractive;
	this._tap = new Tap_default(this, tapConfig);
	this._tap.on("tap", function(tap, gameObject, lastPointer) {
		EmitChildEvent(childrenInteractive.eventEmitter, `${childrenInteractive.eventNamePrefix}${tap.tapsCount}tap`, childrenInteractive.targetSizers, childrenInteractive.targetMode, tap.worldX, tap.worldY, lastPointer, tap);
		EmitChildEvent(childrenInteractive.eventEmitter, `${childrenInteractive.eventNamePrefix}tap`, childrenInteractive.targetSizers, childrenInteractive.targetMode, tap.worldX, tap.worldY, lastPointer, tap);
	}, this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/press/Press.js
var Press_default = Press;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/setchildreninteractive/PressChild.js
var GetValue$183 = __webpack_exports__Utils.Objects.GetValue;
var PressChild = function(config) {
	var pressConfig = GetValue$183(config, "press", void 0);
	if (pressConfig === false) return;
	else if (pressConfig === true) pressConfig = void 0;
	var childrenInteractive = this._childrenInteractive;
	this._press = new Press_default(this, pressConfig);
	this._press.on("pressstart", function(press, gameObject, lastPointer) {
		EmitChildEvent(childrenInteractive.eventEmitter, `${childrenInteractive.eventNamePrefix}pressstart`, childrenInteractive.targetSizers, childrenInteractive.targetMode, press.worldX, press.worldY, lastPointer, press);
	}, this).on("pressend", function(press, gameObject, lastPointer) {
		EmitChildEvent(childrenInteractive.eventEmitter, `${childrenInteractive.eventNamePrefix}pressend`, childrenInteractive.targetSizers, childrenInteractive.targetMode, press.worldX, press.worldY, lastPointer, press);
	}, this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/swipe/Swipe.js
var Swipe_default = Swipe;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/setchildreninteractive/SwipeChild.js
var GetValue$182 = __webpack_exports__Utils.Objects.GetValue;
var SwipeChild = function(config) {
	var swipeConfig = GetValue$182(config, "swipe", void 0);
	if (swipeConfig === false) return;
	else if (swipeConfig === true) swipeConfig = void 0;
	if (swipeConfig === void 0) swipeConfig = {};
	if (!swipeConfig.hasOwnProperty("dir")) swipeConfig.dir = "4dir";
	var childrenInteractive = this._childrenInteractive;
	this._swipe = new Swipe_default(this, swipeConfig);
	this._swipe.on("swipe", function(swipe, gameObject, lastPointer) {
		var dirName = swipe.left ? "left" : swipe.right ? "right" : swipe.up ? "up" : "down";
		EmitChildEvent(childrenInteractive.eventEmitter, `${childrenInteractive.eventNamePrefix}swipe${dirName}`, childrenInteractive.targetSizers, childrenInteractive.targetMode, swipe.worldX, swipe.worldY, lastPointer, swipe);
		EmitChildEvent(childrenInteractive.eventEmitter, `${childrenInteractive.eventNamePrefix}swipe`, childrenInteractive.targetSizers, childrenInteractive.targetMode, swipe.worldX, swipe.worldY, lastPointer, swipe);
	}, this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/setchildreninteractive/SetChildrenInteractive.js
var GetValue$181 = __webpack_exports__Utils.Objects.GetValue;
var SetChildrenInteractive$1 = function(gameObject, config) {
	gameObject.setInteractive();
	if (GetValue$181(config, "dropZone", false)) gameObject.input.dropZone = true;
	gameObject._childrenInteractive = {
		targetSizers: GetValue$181(config, "targets", [gameObject]),
		targetMode: GetValue$181(config, "targetMode", "parent"),
		eventEmitter: GetValue$181(config, "eventEmitter", gameObject),
		eventNamePrefix: GetValue$181(config, "inputEventPrefix", "child.")
	};
	DownChild.call(gameObject, config);
	UpChild.call(gameObject, config);
	OverChild.call(gameObject, config);
	ClickChild.call(gameObject, config);
	TapChild.call(gameObject, config);
	PressChild.call(gameObject, config);
	SwipeChild.call(gameObject, config);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/SetChildrenInteractive.js
var SetChildrenInteractiveWrap = function(config) {
	SetChildrenInteractive$1(this, config);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/BroadcastEvent.js
var BroadcastEvent = function() {
	var gameObjects = this.getAllChildren([this]);
	for (var i = 0, cnt = gameObjects.length; i < cnt; i++) {
		var gameObject = gameObjects[i];
		gameObject.emit.apply(gameObject, arguments);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/Methods.js
var methods$28 = {
	getSizerConfig: GetSizerConfig_default,
	getChildPrevState: GetChildPrevState,
	pushIntoBounds: PushIntoBounds,
	drawBounds: DrawBounds,
	resolveWidth: ResolveWidth$3,
	hasWidthWrap: HasWidthWrap$2,
	resolveChildrenWidth: ResolveChildrenWidth$1,
	runWidthWrap: RunWidthWrap$3,
	resolveHeight: ResolveHeight$3,
	hasHeightWrap: HasHeightWrap$2,
	resolveChildrenHeight: ResolveChildrenHeight$1,
	runHeightWrap: RunHeightWrap$3,
	getChildWidth: GetChildWidth,
	getChildHeight: GetChildHeight,
	getExpandedChildWidth: GetExpandedChildWidth$4,
	getExpandedChildHeight: GetExpandedChildHeight$4,
	getChildrenWidth: GetChildrenWidth$5,
	getChildrenHeight: GetChildrenHeight$5,
	addChildrenMap: AddChildrenMap,
	addElement: AddChildrenMap,
	removeChildrenMap: RemoveChildrenMap,
	getElement: GetElement,
	getChildIndex: GetChildIndex,
	getAllChildrenSizers: GetAllChildrenSizers,
	getChildrenSizers: GetChildrenSizers$5,
	preLayout: PreLayout$3,
	layout: Layout,
	runLayout: RunLayout$1,
	layoutChildren: LayoutChildren$5,
	layoutBackgrounds: LayoutBackgrounds,
	postLayout: PostLayout,
	setAnchor: SetAnchor,
	isInTouching: IsInTouching,
	pointToChild: PointToChild$2,
	setDraggable: SetDraggable,
	setChildrenInteractive: SetChildrenInteractiveWrap,
	broadcastEvent: BroadcastEvent
};
Object.assign(methods$28, PaddingMethods_default, AddChildMethods_default$8, RemoveChildMethods_default$7, GetParentSizerMethods_default, methods$33, methods$32, methods$31, ShakeMethods_default, EaseDataMethods_default, DelayCallMethods_default$2, ClickMethods_default, ClickOutsideMethods_default, TouchingMethods_default, HoverMethods_default, HideMethods_default, ModalMethods_default$1, GetShownChildrenMethods_default, BindEventMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/BaseSizer.js
var GetValue$180 = __webpack_exports__Utils.Objects.GetValue;
var Base$3 = class extends Container_default {
	constructor(scene, x, y, minWidth, minHeight, config) {
		super(scene, x, y, 1, 1);
		this.isRexSizer = true;
		var origin = GetValue$180(config, "origin", .5);
		var originX = GetValue$180(config, "originX", origin);
		var originY = GetValue$180(config, "originY", origin);
		this.setOrigin(originX, originY);
		this.setMinSize(minWidth, minHeight);
		this.setName(GetValue$180(config, "name", ""));
		this.rexSizer = {};
		this.space = {};
		this.backgroundChildren = void 0;
		this.sizerChildren = void 0;
		this.childrenMap = {};
		this.layoutedChildren = void 0;
		this.runChildrenWrapFlag = false;
		this.enableLayoutWarn(false);
		var anchorConfig = GetValue$180(config, "anchor", void 0);
		if (anchorConfig) this.setAnchor(anchorConfig);
		this.setInnerPadding(GetValue$180(config, "space", 0));
		var draggable = GetValue$180(config, "draggable", false);
		if (draggable) this.setDraggable(draggable);
		this.setSizerEventsEnable(GetValue$180(config, "sizerEvents", false));
		this.setDirty(true);
		if (GetValue$180(config, "enableLayer", false)) this.enableLayer();
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		if (fromScene) {
			var sizers = this.getAllChildrenSizers([this]);
			for (var i = 0, cnt = sizers.length; i < cnt; i++) sizers[i].sizerEventsEnable = false;
		}
		super.destroy(fromScene);
		Clear$1(this.backgroundChildren);
		Clear$1(this.sizerChildren);
		this.childrenMap = void 0;
		this.space = void 0;
		this.rexSizer = void 0;
		this.layoutedChildren = void 0;
	}
	setMinSize(minWidth, minHeight) {
		this.setMinWidth(minWidth).setMinHeight(minHeight);
		return this;
	}
	setMinWidth(minWidth) {
		if (minWidth == null) minWidth = 0;
		this.minWidth = minWidth;
		return this;
	}
	setMinHeight(minHeight) {
		if (minHeight == null) minHeight = 0;
		this.minHeight = minHeight;
		return this;
	}
	setDirty(dirty) {
		if (dirty === void 0) dirty = true;
		this.dirty = dirty;
		return this;
	}
	setSizerEventsEnable(enable) {
		if (enable === void 0) enable = true;
		this.sizerEventsEnable = enable;
		return this;
	}
	enableLayoutWarn(enable) {
		if (enable === void 0) enable = true;
		this.layoutWarnEnable = enable;
		return this;
	}
	get ignoreLayout() {
		return this.rexSizer.hidden || !this.dirty;
	}
	get childrenWidth() {
		if (this._childrenWidth === void 0) this._childrenWidth = this.getChildrenWidth();
		return this._childrenWidth;
	}
	get childrenHeight() {
		if (this._childrenHeight === void 0) this._childrenHeight = this.getChildrenHeight();
		return this._childrenHeight;
	}
	get left() {
		return this.x - GetDisplayWidth(this) * this.originX;
	}
	set left(value) {
		this.x += value - this.left;
	}
	alignLeft(value) {
		this.left = value;
		return this;
	}
	get right() {
		return this.left + GetDisplayWidth(this);
	}
	set right(value) {
		this.x += value - this.right;
	}
	alignRight(value) {
		this.right = value;
		return this;
	}
	get centerX() {
		return this.left + GetDisplayWidth(this) / 2;
	}
	set centerX(value) {
		this.x += value - this.centerX;
	}
	alignCenterX(value) {
		this.centerX = value;
		return this;
	}
	get top() {
		return this.y - GetDisplayHeight(this) * this.originY;
	}
	set top(value) {
		this.y += value - this.top;
	}
	alignTop(value) {
		this.top = value;
		return this;
	}
	get bottom() {
		return this.top + GetDisplayHeight(this);
	}
	set bottom(value) {
		this.y += value - this.bottom;
	}
	alignBottom(value) {
		this.bottom = value;
		return this;
	}
	get centerY() {
		return this.top + GetDisplayHeight(this) / 2;
	}
	set centerY(value) {
		this.y += value - this.centerY;
	}
	alignCenterY(value) {
		this.centerY = value;
		return this;
	}
	get innerLeft() {
		return this.left + this.space.left * this.scaleX;
	}
	get innerRight() {
		return this.right - this.space.right * this.scaleX;
	}
	get innerTop() {
		return this.top + this.space.top * this.scaleY;
	}
	get innerBottom() {
		return this.bottom - this.space.bottom * this.scaleY;
	}
	get innerWidth() {
		return (this.width - this.space.left - this.space.right) * this.scaleX;
	}
	get innerHeight() {
		return (this.height - this.space.top - this.space.bottom) * this.scaleY;
	}
	get minInnerWidth() {
		var result = (this.minWidth - this.space.left - this.space.right) * this.scaleX;
		return Math.max(result, 0);
	}
	get minInnerHeight() {
		var result = (this.minHeight - this.space.top - this.space.bottom) * this.scaleY;
		return Math.max(result, 0);
	}
};
Object.assign(Base$3.prototype, methods$28);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/overlapsizer/GetChildrenWidth.js
var GetChildrenWidth$4 = function() {
	if (this.rexSizer.hidden) return 0;
	var result = 0;
	var children = this.sizerChildren;
	var child, padding, childWidth;
	var hasUnknownChildWidth = false;
	for (var key in children) {
		child = children[key];
		childWidth = this.getChildWidth(child);
		if (childWidth === void 0) hasUnknownChildWidth = true;
		if (hasUnknownChildWidth) continue;
		padding = child.rexSizer.padding;
		childWidth += (padding.left + padding.right) * this.scaleX;
		result = Math.max(childWidth, result);
	}
	if (hasUnknownChildWidth) return;
	return result + (this.space.left + this.space.right) * this.scaleX;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/overlapsizer/GetChildrenHeight.js
var GetChildrenHeight$4 = function() {
	if (this.rexSizer.hidden) return 0;
	var result = 0;
	var children = this.sizerChildren;
	var child, padding, childHeight;
	var hasUnknownChildHeight = false;
	for (var key in children) {
		child = children[key];
		childHeight = this.getChildHeight(child);
		if (childHeight === void 0) hasUnknownChildHeight = true;
		if (hasUnknownChildHeight) continue;
		padding = child.rexSizer.padding;
		childHeight += (padding.top + padding.bottom) * this.scaleY;
		result = Math.max(childHeight, result);
	}
	if (hasUnknownChildHeight) return;
	return result + (this.space.top + this.space.bottom) * this.scaleY;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/overlapsizer/GetExpandedChildWidth.js
var GetExpandedChildWidth$3 = function(child, parentWidth) {
	if (parentWidth === void 0) parentWidth = this.width * this.scaleX;
	var childWidth;
	var childConfig = child.rexSizer;
	var expandWidth = childConfig.expandWidth;
	if (expandWidth) {
		var innerWidth = parentWidth - (this.space.left + this.space.right) * this.scaleX;
		var padding = childConfig.padding;
		childWidth = (innerWidth - (padding.left + padding.right) * this.scaleX) * expandWidth;
	}
	return childWidth;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/overlapsizer/GetExpandedChildHeight.js
var GetExpandedChildHeight$3 = function(child, parentHeight) {
	if (parentHeight === void 0) parentHeight = this.height;
	var childHeight;
	var childConfig = child.rexSizer;
	var expandHeight = childConfig.expandHeight;
	if (expandHeight) {
		var innerHeight = parentHeight - (this.space.top + this.space.bottom) * this.scaleY;
		var padding = childConfig.padding;
		childHeight = (innerHeight - (padding.top + padding.bottom) * this.scaleY) * expandHeight;
	}
	return childHeight;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/overlapsizer/GetChildrenSizers.js
var GetChildrenSizers$4 = function(out) {
	if (out === void 0) out = [];
	var children = this.sizerChildren, child;
	for (var key in children) {
		child = children[key];
		if (child.isRexSizer) out.push(child);
	}
	return out;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/size/FitTo.js
var FitTo = function(source, target, fitMode, out) {
	if (fitMode === void 0) fitMode = 0;
	else {
		var fitModeType = typeof fitMode;
		if (fitModeType === "boolean") {
			out = fitMode;
			fitMode = 0;
		} else if (fitModeType === "string") fitMode = FitModeMap[fitMode];
	}
	if (out === void 0) out = {};
	else if (out === true) out = globalSize;
	var scaleX = target.width / source.width;
	var scaleY = target.height / source.height;
	var scale = !fitMode ? Math.min(scaleX, scaleY) : Math.max(scaleX, scaleY);
	var newWidth = source.width * scale;
	var newHeight = source.height * scale;
	if (IsGameObject(out)) ResizeGameObject(out, newWidth, newHeight);
	else {
		out.width = newWidth;
		out.height = newHeight;
	}
	return out;
};
var FitModeMap = {
	"fit": 0,
	"FIT": 0,
	"envelop": 1,
	"ENVELOP": 1
};
var globalSize = {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/utils/CheckSize.js
var CheckSize = function(child, parent) {
	if (child.layoutWarnEnable) {
		if (child.width < child.childrenWidth) console.warn(`Layout width error: Parent=${parent.constructor.name}, Child=${child.constructor.name}`);
		if (child.height < child.childrenHeight) console.warn(`Layout height error: Parent=${parent.constructor.name}, Child=${child.constructor.name}`);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/overlapsizer/LayoutChildren.js
var LayoutChildren$4 = function() {
	var child, childConfig, padding;
	var startX = this.innerLeft, startY = this.innerTop;
	var innerWidth = this.innerWidth, innerHeight = this.innerHeight;
	var x, y, width, height, alignOffsetX, alignOffsetY;
	var childWidth, childHeight, childSize;
	var children = this.sizerChildren;
	for (var key in children) {
		child = children[key];
		if (child.rexSizer.hidden) continue;
		childConfig = child.rexSizer;
		padding = childConfig.padding;
		PreLayoutChild.call(this, child);
		childWidth = this.getExpandedChildWidth(child);
		childHeight = this.getExpandedChildHeight(child);
		if (childConfig.aspectRatio > 0) {
			sourceSize.width = childConfig.aspectRatio;
			sourceSize.height = 1;
			targetSize.width = childWidth;
			targetSize.height = childHeight;
			childSize = FitTo(sourceSize, targetSize, "FIT", true);
			childWidth = childSize.width;
			childHeight = childSize.height;
		}
		if (child.isRexSizer) {
			child.runLayout(this, childWidth, childHeight);
			CheckSize(child, this);
		} else if (!childConfig.noResize) ResizeGameObject(child, childWidth, childHeight);
		x = startX + padding.left * this.scaleX;
		width = innerWidth - (padding.left + padding.right) * this.scaleX;
		y = startY + padding.top * this.scaleY;
		height = innerHeight - (padding.top + padding.bottom) * this.scaleY;
		if (childWidth === void 0) childWidth = GetDisplayWidth(child);
		if (childHeight === void 0) childHeight = GetDisplayHeight(child);
		alignOffsetX = (childConfig.alignOffsetX + childConfig.alignOffsetOriginX * childWidth) * this.scaleX;
		alignOffsetY = (childConfig.alignOffsetY + childConfig.alignOffsetOriginY * childHeight) * this.scaleY;
		LayoutChild.call(this, child, x, y, width, height, childConfig.align, alignOffsetX, alignOffsetY);
	}
};
var sourceSize = {};
var targetSize = {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/overlapsizer/AddChildMethods.js
var IsPlainObject$33 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$179 = __webpack_exports__Utils.Objects.GetValue;
var ALIGN_CENTER$3 = __webpack_exports__Display.Align.CENTER;
var UUID$3 = __webpack_exports__Utils.String.UUID;
var NormalizeExpand = function(value) {
	var expandRatio;
	if (value === true) expandRatio = 1;
	else if (typeof value === "number" && isFinite(value) && value > 0) expandRatio = value;
	else expandRatio = 0;
	return expandRatio;
};
var Add$7 = function(gameObject, childKey, align, padding, expand, minWidth, minHeight, offsetX, offsetY, aspectRatio) {
	var offsetOriginX, offsetOriginY;
	AddChild$1.call(this, gameObject);
	if (IsPlainObject$33(childKey)) {
		var config = childKey;
		childKey = GetValue$179(config, "key", void 0);
		align = GetValue$179(config, "align", ALIGN_CENTER$3);
		padding = GetValue$179(config, "padding", 0);
		expand = GetValue$179(config, "expand", true);
		if (!gameObject.isRexSizer) {
			minWidth = GetValue$179(config, "minWidth", gameObject._minWidth);
			minHeight = GetValue$179(config, "minHeight", gameObject._minHeighted);
		}
		offsetX = GetValue$179(config, "offsetX", 0);
		offsetY = GetValue$179(config, "offsetY", 0);
		offsetOriginX = GetValue$179(config, "offsetOriginX", 0);
		offsetOriginY = GetValue$179(config, "offsetOriginY", 0);
		aspectRatio = GetValue$179(config, "aspectRatio", 0);
	}
	var hasValidKey = childKey !== void 0;
	if (!hasValidKey) childKey = UUID$3();
	if (typeof align === "string") align = AlignConst_default[align];
	if (align === void 0) align = ALIGN_CENTER$3;
	if (padding === void 0) padding = 0;
	if (expand === void 0) expand = true;
	if (!gameObject.isRexSizer) {
		if (minWidth === void 0) minWidth = gameObject._minWidth;
		if (minHeight === void 0) minHeight = gameObject._minHeight;
	}
	if (offsetX === void 0) offsetX = 0;
	if (offsetY === void 0) offsetY = 0;
	if (offsetOriginX === void 0) offsetOriginX = 0;
	if (offsetOriginY === void 0) offsetOriginY = 0;
	if (aspectRatio === void 0) aspectRatio = 0;
	else if (aspectRatio === true) aspectRatio = GetDisplayWidth(gameObject) / GetDisplayHeight(gameObject);
	if (aspectRatio > 0) {
		expand = true;
		if (minWidth === void 0) minWidth = 0;
		if (minHeight === void 0) minHeight = 0;
	}
	var config = this.getSizerConfig(gameObject);
	config.align = align;
	config.padding = GetBoundsConfig_default(padding);
	if (IsPlainObject$33(expand)) {
		config.expandWidth = NormalizeExpand(GetValue$179(expand, "width", false));
		config.expandHeight = NormalizeExpand(GetValue$179(expand, "height", false));
	} else {
		expand = NormalizeExpand(expand);
		config.expandWidth = expand;
		config.expandHeight = expand;
	}
	if (!gameObject.isRexSizer) {
		if (config.expandWidth) gameObject.minWidth = minWidth === void 0 ? GetDisplayWidth(gameObject) : minWidth;
		if (config.expandHeight) gameObject.minHeight = minHeight === void 0 ? GetDisplayHeight(gameObject) : minHeight;
	}
	config.alignOffsetX = offsetX;
	config.alignOffsetY = offsetY;
	config.alignOffsetOriginX = offsetOriginX;
	config.alignOffsetOriginY = offsetOriginY;
	config.aspectRatio = aspectRatio;
	if (this.sizerChildren.hasOwnProperty(childKey)) this.sizerChildren[childKey].destroy();
	this.sizerChildren[childKey] = gameObject;
	if (hasValidKey) this.addChildrenMap(childKey, gameObject);
	return this;
};
var AddChildMethods_default$7 = { add: Add$7 };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/utils/ClearChildren.js
var ContainerClear = Container_default.prototype.clear;
var ClearChildren = function(destroyChild) {
	if (this.backgroundChildren) this.backgroundChildren.length = 0;
	var fireRemoveEvent = !destroyChild && this.sizerEventsEnable;
	var children;
	if (fireRemoveEvent) children = this.getChildren([]);
	ContainerClear.call(this, destroyChild);
	if (fireRemoveEvent) {
		var gameObject;
		for (var i = 0, cnt = children.length; i < cnt; i++) {
			gameObject = children[i];
			gameObject.emit("sizer.remove", gameObject, this);
			this.emit("remove", gameObject, this);
		}
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/overlapsizer/RemoveChildMethods.js
var RemoveChildMethods_default$6 = {
	remove(gameObject, destroyChild) {
		var key;
		if (typeof gameObject === "string") {
			key = gameObject;
			gameObject = this.sizerChildren[key];
			if (!gameObject) return this;
		} else if (this.getParentSizer(gameObject) !== this) return this;
		else key = this.childToKey(gameObject);
		if (key) {
			delete this.sizerChildren[key];
			if (this.childrenMap.hasOwnProperty(key)) delete this.childrenMap[key];
		}
		RemoveChild.call(this, gameObject, destroyChild);
		return this;
	},
	removeAll(destroyChild) {
		for (var key in this.sizerChildren) this.remove(key, destroyChild);
		return this;
	},
	clear(destroyChild) {
		for (var key in this.sizerChildren) {
			delete this.sizerChildren[key];
			if (this.childrenMap.hasOwnProperty(key)) delete this.childrenMap[key];
		}
		ClearChildren.call(this, destroyChild);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/overlapsizer/Methods.js
var methods$27 = {
	getChildrenWidth: GetChildrenWidth$4,
	getChildrenHeight: GetChildrenHeight$4,
	getExpandedChildWidth: GetExpandedChildWidth$3,
	getExpandedChildHeight: GetExpandedChildHeight$3,
	getChildrenSizers: GetChildrenSizers$4,
	layoutChildren: LayoutChildren$4
};
Object.assign(methods$27, AddChildMethods_default$7, RemoveChildMethods_default$6);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/object/IndexOf.js
var IndexOf = function(obj, child) {
	if (Array.isArray(obj)) return obj.indexOf(child);
	else {
		for (var key in obj) if (obj[key] === child) return key;
		return null;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/overlapsizer/OverlapSizer.js
var IsPlainObject$32 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$178 = __webpack_exports__Utils.Objects.GetValue;
var OverlapSizer = class extends Base$3 {
	constructor(scene, x, y, minWidth, minHeight, config) {
		if (IsPlainObject$32(x)) {
			config = x;
			x = GetValue$178(config, "x", 0);
			y = GetValue$178(config, "y", 0);
			minWidth = GetValue$178(config, "width", void 0);
			minHeight = GetValue$178(config, "height", void 0);
		} else if (IsPlainObject$32(minWidth)) {
			config = minWidth;
			minWidth = GetValue$178(config, "width", void 0);
			minHeight = GetValue$178(config, "height", void 0);
		}
		super(scene, x, y, minWidth, minHeight, config);
		this.type = "rexOverlapSizer";
		this.sizerChildren = {};
		this.addChildrenMap("items", this.sizerChildren);
	}
	childToKey(gameObject) {
		if (typeof gameObject === "string") {
			var key = gameObject;
			if (this.sizerChildren.hasOwnPropery(key)) return key;
		} else return IndexOf(this.sizerChildren, gameObject);
		return null;
	}
};
Object.assign(OverlapSizer.prototype, methods$27);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/knob/input/IsLocalPointInKnob.js
var GetDistance = __webpack_exports__Math.Distance.Between;
var IsLocalPointInKnob = function(knob, localX, localY) {
	var centerX = knob.width / 2;
	return GetDistance(centerX, centerX, localX, localY) <= centerX;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/knob/input/OnTouchPad.js
var GetAngle$1 = __webpack_exports__Math.Angle.Between;
var NormalizeAngle = __webpack_exports__Math.Angle.Normalize;
var OnTouchPad = function(pointer, localX, localY) {
	if (!this.enable) return;
	if (!pointer.isDown) return;
	var knob = this.sizerChildren.knob;
	if (!IsLocalPointInKnob(knob, localX, localY)) return;
	var centerX = knob.width / 2;
	var startAngle = knob.startAngle;
	var endAngle = GetAngle$1(centerX, centerX, localX, localY);
	var value = NormalizeAngle(knob.anticlockwise ? startAngle - endAngle : endAngle - startAngle) / (2 * Math.PI);
	this.stopEaseValue();
	if (this.easeValueDuration === 0 || Math.abs(this.value - value) < .1) this.value = value;
	else this.easeValueTo(value);
};
var InstallEvents$1 = function() {
	this.sizerChildren.knob.on("pointerdown", OnTouchPad, this).on("pointermove", OnTouchPad, this).setInteractive();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/knob/input/OnPanPad.js
var GetAngle = __webpack_exports__Math.Angle.Between;
var WrapAngle = __webpack_exports__Math.Angle.Wrap;
var OnPointerDown = function(pointer, localX, localY) {
	if (!this.enable || this.panPointer) return;
	var knob = this.sizerChildren.knob;
	if (!IsLocalPointInKnob(knob, localX, localY)) return;
	OnPanStart.call(this, pointer);
};
var OnPointerMove = function(pointer, localX, localY) {
	if (!this.enable) return;
	if (!pointer.isDown) return;
	var knob = this.sizerChildren.knob;
	switch (this.panState) {
		case TOUCH0:
			if (IsLocalPointInKnob(knob, localX, localY)) OnPanStart.call(this, pointer);
			break;
		case TOUCH1: if (IsLocalPointInKnob(knob, localX, localY)) OnPan.call(this);
		else OnPanEnd.call(this);
	}
};
var OnPointerUp = function(pointer, localX, localY) {
	if (!this.enable || this.panPointer !== pointer) return;
	OnPanEnd.call(this);
};
var OnPanStart = function(pointer) {
	this.panPointer = pointer;
	this.panState = TOUCH1;
};
var OnPanEnd = function() {
	this.panPointer = void 0;
	this.panState = TOUCH0;
};
var OnPan = function() {
	var p0 = this.panPointer.prevPosition, p1 = this.panPointer.position;
	var knob = this.sizerChildren.knob;
	var startAngle = GetAngle(knob.x, knob.y, p0.x, p0.y), endAngle = GetAngle(knob.x, knob.y, p1.x, p1.y);
	var deltaValue = WrapAngle(knob.anticlockwise ? startAngle - endAngle : endAngle - startAngle) / (Math.PI * 2);
	this.stopEaseValue();
	this.value += deltaValue;
};
var TOUCH0 = 0;
var TOUCH1 = 1;
var InstallEvents = function() {
	this.sizerChildren.knob.on("pointerdown", OnPointerDown, this).on("pointermove", OnPointerMove, this).on("pointerup", OnPointerUp, this).setInteractive();
	this.panPointer = void 0;
	this.panState = TOUCH0;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/knob/TextObjectMethods.js
var SetTextFormatCallback = function(callback, scope) {
	this.textFormatCallback = callback;
	this.textFormatCallbackScope = scope;
	return this;
};
var GetFormatText = function(value) {
	if (value === void 0) value = this.value;
	var text;
	if (this.textFormatCallbackScope) text = this.textFormatCallback(value);
	else text = this.textFormatCallback.call(this.textFormatCallbackScope, value);
	return text;
};
var UpdateText = function(value) {
	var textObject = this.sizerChildren.text;
	if (textObject && this.textFormatCallback) {
		textObject.setText(GetFormatText.call(this, value));
		if (textObject.layout) textObject.layout();
	}
	return this;
};
var TextObjectMethods_default = {
	setTextFormatCallback: SetTextFormatCallback,
	getFormatText: GetFormatText,
	updateText: UpdateText
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/knob/Knob.js
var GetValue$177 = __webpack_exports__Utils.Objects.GetValue;
var SnapTo$3 = __webpack_exports__Math.Snap.To;
var Knob = class extends ProgressBase_default(OverlapSizer) {
	constructor(scene, config) {
		if (config === void 0) config = {};
		super(scene, config);
		this.type = "rexKnob";
		this.bootProgressBase(config);
		var background = GetValue$177(config, "background", void 0);
		var textObject = GetValue$177(config, "text", void 0);
		if (background) this.addBackground(background);
		if (textObject) {
			config.textColor = void 0;
			config.textStrokeColor = void 0;
			this.setTextFormatCallback(GetValue$177(config, "textFormatCallback", void 0), GetValue$177(config, "textFormatCallbackScope", void 0));
			config.textFormatCallback = void 0;
			config.textFormatCallbackScope = void 0;
		}
		var knob = new CircularProgressCanvas_default(scene, config);
		knob.setDepth(GetValue$177(config, "knobDepth", 0));
		knob._value = -1;
		scene.add.existing(knob);
		this.add(knob, "knob");
		if (textObject) {
			this.add(textObject, "text", "center", 0, false);
			scene.children.moveBelow(knob, textObject);
		}
		this.addChildrenMap("background", background);
		this.addChildrenMap("knob", knob);
		this.addChildrenMap("text", textObject);
		this.setEnable(GetValue$177(config, "enable", void 0));
		this.setGap(GetValue$177(config, "gap", void 0));
		this.setValue(GetValue$177(config, "value", 0), GetValue$177(config, "min", void 0), GetValue$177(config, "max", void 0));
		var inputMode = GetValue$177(config, "input", 0);
		if (typeof inputMode === "string") inputMode = INPUTMODE$1[inputMode];
		switch (inputMode) {
			case 0:
				InstallEvents.call(this);
				break;
			case 1: InstallEvents$1.call(this);
		}
	}
	setEnable(enable) {
		if (enable === void 0) enable = true;
		this.enable = enable;
		return this;
	}
	setGap(gap) {
		this.gap = gap;
		return this;
	}
	get value() {
		return this.sizerChildren.knob.value;
	}
	set value(value) {
		if (this.gap !== void 0) value = SnapTo$3(value, this.gap);
		var oldValue = this.value;
		this.sizerChildren.knob.value = value;
		var newValue = this.value;
		if (oldValue !== newValue) {
			this.updateText();
			this.eventEmitter.emit("valuechange", newValue, oldValue, this.eventEmitter);
		}
	}
};
var INPUTMODE$1 = {
	pan: 0,
	drag: 0,
	click: 1,
	none: -1
};
Object.assign(Knob.prototype, TextObjectMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/knob/Factory.js
ObjectFactory.register("knob", function(config) {
	var gameObject = new Knob(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.Knob", Knob);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/customshapes/ShapesUpdateMethods.js
var ShapeClasses = {
	arc: Arc,
	circle: Circle,
	curve: Curve,
	ellipse: Ellipse,
	line: Line,
	lines: Lines,
	rectangle: Rectangle$2,
	roundRectangle: RoundRectangle,
	triangle: Triangle$1
};
var GetValue$176 = __webpack_exports__Utils.Objects.GetValue;
var IsPlainObject$31 = __webpack_exports__Utils.Objects.IsPlainObject;
var ClearAll = function() {
	var shapes = this.getShapes();
	for (var i = 0, cnt = shapes.length; i < cnt; i++) shapes[i].lineStyle().fillStyle();
};
var ShapesUpdateMethods_default = {
	createShape(shapeType, name) {
		var ShapeClass = ShapeClasses[shapeType];
		var shape = new ShapeClass();
		if (name) shape.setName(name);
		return shape;
	},
	buildShapes(config) {
		var createCallback = GetValue$176(config, "create", void 0);
		if (IsPlainObject$31(createCallback)) {
			var shapes = createCallback;
			for (var shapeType in shapes) {
				var name = shapes[shapeType];
				switch (typeof name) {
					case "number":
						for (var i = 0; i < name; i++) this.addShape(this.createShape(shapeType));
						break;
					case "string":
						this.addShape(this.createShape(shapeType, name));
						break;
					default:
						var names = name;
						for (var i = 0, cnt = names.length; i < cnt; i++) this.addShape(this.createShape(shapeType, names[i]));
				}
			}
		} else if (Array.isArray(createCallback)) {
			var shapes = createCallback;
			for (var i = 0, cnt = shapes.length; i < cnt; i++) {
				var shape = shapes[i];
				this.addShape(this.createShape(shape.type, shape.name));
			}
		} else if (typeof createCallback === "function") createCallback.call(this);
		this.setUpdateShapesCallback(GetValue$176(config, "update"));
	},
	setUpdateShapesCallback(callback) {
		if (callback === void 0) callback = ClearAll;
		this.dirty = this.dirty || this.updateCallback !== callback;
		this.updateCallback = callback;
		return this;
	},
	updateShapes() {
		this.updateCallback.call(this);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/customshapes/CustomShapes.js
var GetValue$175 = __webpack_exports__Utils.Objects.GetValue;
var IsPlainObject$30 = __webpack_exports__Utils.Objects.IsPlainObject;
var CustomShapes = class extends BaseShapes {
	constructor(scene, x, y, width, height, config) {
		if (IsPlainObject$30(x)) {
			config = x;
			x = GetValue$175(config, "x", 0);
			y = GetValue$175(config, "y", 0);
			width = GetValue$175(config, "width", 2);
			height = GetValue$175(config, "height", 2);
		}
		super(scene, x, y, width, height);
		this.type = GetValue$175(config, "type", "rexCustomShapes");
		this.buildShapes(config);
	}
	get centerX() {
		return this.width / 2;
	}
	get centerY() {
		return this.height / 2;
	}
	worldToLocalXY(worldX, worldY, camera, out) {
		if (typeof camera === "boolean") {
			out = camera;
			camera = void 0;
		}
		return WorldXYToGameObjectLocalXY(this, worldX, worldY, camera, out);
	}
};
Object.assign(CustomShapes.prototype, ShapesUpdateMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/customshapes/CustomShapes.js
var CustomShapes_default = CustomShapes;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/customshapes/Factory.js
ObjectFactory.register("customShapes", function(x, y, width, height, config) {
	var gameObject = new CustomShapes_default(this.scene, x, y, width, height, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.CustomShapes", CustomShapes_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/shape/customprogress/CustomProgress.js
var GetValue$174 = __webpack_exports__Utils.Objects.GetValue;
var IsPlainObject$29 = __webpack_exports__Utils.Objects.IsPlainObject;
var CustomProgress = class extends ProgressBase_default(CustomShapes) {
	constructor(scene, x, y, width, height, config) {
		if (IsPlainObject$29(x)) {
			config = x;
			x = GetValue$174(config, "x", 0);
			y = GetValue$174(config, "y", 0);
			width = GetValue$174(config, "width", 2);
			height = GetValue$174(config, "height", 2);
		}
		if (config === void 0) config = {};
		if (!config.type) config.type = "rexCustomProgress";
		super(scene, x, y, width, height, config);
		this.bootProgressBase(config);
		this.setValue(GetValue$174(config, "value", 0));
	}
	get centerX() {
		return this.width / 2;
	}
	get centerY() {
		return this.height / 2;
	}
	get radius() {
		return Math.min(this.centerX, this.centerY);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/customprogress/CustomProgress.js
var CustomProgress_default = CustomProgress;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/customprogress/Factory.js
ObjectFactory.register("customProgress", function(x, y, width, height, config) {
	var gameObject = new CustomProgress_default(this.scene, x, y, width, height, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.CustomProgress", CustomProgress_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/base/EaseValueMethods.js
var Start = function(duration) {
	if (!this.easeValueTask) this.easeValueTask = new EaseValueTask(this, { eventEmitter: null });
	if (duration !== void 0) {
		this.duration = duration;
		this.easeValueTask.stop();
	}
	if (this.easeValueTask.isRunning) return this;
	this.easeValueTask.restart({
		key: "value",
		from: 0,
		to: 1,
		duration: this.duration,
		ease: this.ease,
		repeat: -1,
		delay: this.delay,
		repeatDelay: this.repeatDelay
	});
	this.setDirty();
	return this;
};
var Stop$1 = function() {
	if (!this.easeValueTask) return this;
	this.easeValueTask.stop();
	this.setDirty();
	return this;
};
var Pause = function() {
	if (!this.easeValueTask) return this;
	this.easeValueTask.pause();
	this.setDirty();
	return this;
};
var Resume = function() {
	if (!this.easeValueTask) return this;
	this.easeValueTask.pause();
	this.setDirty();
	return this;
};
var EaseValueMethods_default = {
	start: Start,
	stop: Stop$1,
	pause: Pause,
	resume: Resume
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/base/Base.js
var GetValue$173 = __webpack_exports__Utils.Objects.GetValue;
var Base$2 = class extends BaseShapes {
	constructor(scene, config) {
		var x = GetValue$173(config, "x", 0);
		var y = GetValue$173(config, "y", 0);
		var width = GetValue$173(config, "width", 64);
		var height = GetValue$173(config, "height", 64);
		super(scene, x, y, width, height);
		this.resetFromConfig(config, true);
		this.buildShapes(config);
		if (GetValue$173(config, "start", true)) this.start();
	}
	resetFromConfig(config, setDefaults) {
		if (setDefaults === void 0) setDefaults = false;
		var defaultValue = setDefaults ? 1e3 : this.duration;
		this.setDuration(GetValue$173(config, "duration", defaultValue));
		defaultValue = setDefaults ? "Linear" : this.ease;
		this.setEase(GetValue$173(config, "ease", defaultValue));
		defaultValue = setDefaults ? 0 : this.delay;
		this.setDelay(GetValue$173(config, "delay", defaultValue));
		defaultValue = setDefaults ? 0 : this.repeatDelay;
		this.setRepeatDelay(GetValue$173(config, "repeatDelay", defaultValue));
		defaultValue = setDefaults ? 16777215 : this.color;
		this.setColor(GetValue$173(config, "color", defaultValue));
		defaultValue = setDefaults ? 0 : this.value;
		this.setValue(GetValue$173(config, "value", defaultValue));
		return this;
	}
	buildShapes() {}
	get centerX() {
		return this.width / 2;
	}
	get centerY() {
		return this.height / 2;
	}
	get radius() {
		return Math.min(this.centerX, this.centerY);
	}
	get color() {
		return this._color;
	}
	set color(value) {
		this.isColorChanged = this.isColorChanged || this._color !== value;
		this.dirty = this.dirty || this.isColorChanged;
		this._color = value;
		this.setShapesColor(value);
	}
	setColor(color) {
		this.color = color;
		return this;
	}
	setShapesColor(color) {}
	get value() {
		return this._value;
	}
	set value(value) {
		value = __webpack_exports__Math.Clamp(value, 0, 1);
		this.dirty = this.dirty || this._value != value;
		this._value = value;
	}
	setValue(value) {
		this.value = value;
		return this;
	}
	setDuration(duration) {
		this.duration = duration;
		return this;
	}
	setDelay(delay) {
		this.delay = delay;
		return this;
	}
	setRepeatDelay(repeatDelay) {
		this.repeatDelay = repeatDelay;
		return this;
	}
	setEase(ease) {
		this.ease = ease;
		return this;
	}
	get isRunning() {
		return this.tweenTask ? this.tweenTask.isRunning : false;
	}
};
Object.assign(Base$2.prototype, EaseValueMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/utils/Yoyo.js
var Yoyo_default = Yoyo$1;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/arrow/UpdateShapeMethods.js
var DegToRad$4 = __webpack_exports__Math.DegToRad;
var Linear$15 = __webpack_exports__Math.Linear;
var ExpoIn$4 = __webpack_exports__Math.Easing.Expo.In;
var DIRMAP = {
	right: 0,
	down: 1,
	left: 2,
	up: 3
};
var ArrowPolygon = function(polygon, innerX, outerX, gridWidth, gridHeight, x0, y0, a, b, c, d) {
	var p0 = Transform(innerX, 0, gridWidth, gridHeight, x0, y0, a, b, c, d);
	polygon.startAt(p0.x, p0.y);
	var p1 = Transform(outerX, 0, gridWidth, gridHeight, x0, y0, a, b, c, d);
	polygon.lineTo(p1.x, p1.y);
	var p2 = Transform(outerX, outerX, gridWidth, gridHeight, x0, y0, a, b, c, d);
	polygon.lineTo(p2.x, p2.y);
	var p3 = Transform(0, outerX, gridWidth, gridHeight, x0, y0, a, b, c, d);
	polygon.lineTo(p3.x, p3.y);
	var p4 = Transform(0, innerX, gridWidth, gridHeight, x0, y0, a, b, c, d);
	polygon.lineTo(p4.x, p4.y);
	var p5 = Transform(innerX, innerX, gridWidth, gridHeight, x0, y0, a, b, c, d);
	polygon.lineTo(p5.x, p5.y);
	polygon.close();
};
var GlobPoint = {};
var Transform = function(gridX, gridY, gridWidth, gridHeight, x0, y0, a, b, c, d) {
	var x = gridX * gridWidth;
	var y = gridY * gridHeight;
	GlobPoint.x = a * x + b * y + x0;
	GlobPoint.y = c * x + d * y + y0;
	return GlobPoint;
};
var UpdateShapeMethods_default$19 = {
	setDirection(direction) {
		if (typeof direction === "string") direction = DIRMAP[direction];
		this.direction = direction;
		return this;
	},
	buildShapes() {
		for (var i = 0; i < 3; i++) this.addShape(new Lines());
	},
	updateShapes() {
		var x0, y0, a, b, c, d;
		switch (this.direction) {
			case 1:
				x0 = this.centerX;
				y0 = this.centerY - this.radius;
				var radX = DegToRad$4(315);
				a = Math.cos(radX);
				b = Math.sin(radX);
				var radY = DegToRad$4(45);
				c = Math.cos(radY);
				d = Math.sin(radY);
				break;
			case 3:
				x0 = this.centerX;
				y0 = this.centerY + this.radius;
				var radX = DegToRad$4(135);
				a = Math.cos(radX);
				b = Math.sin(radX);
				var radY = DegToRad$4(225);
				c = Math.cos(radY);
				d = Math.sin(radY);
				break;
			case 2:
				x0 = this.centerX + this.radius;
				y0 = this.centerY;
				var radX = DegToRad$4(225);
				a = Math.cos(radX);
				b = Math.sin(radX);
				var radY = DegToRad$4(315);
				c = Math.cos(radY);
				d = Math.sin(radY);
				break;
			default:
				x0 = this.centerX - this.radius;
				y0 = this.centerY;
				var radX = DegToRad$4(45);
				a = Math.cos(radX);
				b = Math.sin(radX);
				var radY = DegToRad$4(135);
				c = Math.cos(radY);
				d = Math.sin(radY);
		}
		var gridSize = this.radius / 7;
		var shapes = this.getShapes();
		for (var i = 0, cnt = shapes.length; i < cnt; i++) {
			var shape = shapes[i];
			var t = (this.value + (cnt - i) * .1) % 1;
			t = ExpoIn$4(Yoyo_default(t));
			var alpha = Linear$15(.25, 1, t);
			shape.fillStyle(this.color, alpha);
			var innerX = i * 3 + 1;
			ArrowPolygon(shape, innerX, innerX + 2, gridSize, gridSize, x0, y0, a, b, c, d);
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/audio/UpdateShapeMethods.js
var Linear$14 = __webpack_exports__Math.Linear;
var UpdateShapeMethods_default$18 = {
	buildShapes() {
		for (var i = 0; i < 4; i++) this.addShape(new Line());
		this.prevValue = void 0;
	},
	updateShapes() {
		var centerX = this.centerX;
		var centerY = this.centerY;
		var radius = this.radius;
		var leftBound = centerX - radius;
		var bottomBound = centerY + radius;
		var maxLineHeight = radius * 2;
		var shapes = this.getShapes(), cnt = shapes.length;
		var cellWidth = radius * 2 / cnt;
		var lineWidth = cellWidth * .7;
		if (this.prevValue === void 0 || this.prevValue > this.value) for (var i = 0; i < cnt; i++) {
			var line = shapes[i];
			var from = this.prevValue === void 0 ? Math.random() : line.getData("to");
			line.setData("from", from).setData("to", Math.random());
		}
		this.prevValue = this.value;
		for (var i = 0; i < cnt; i++) {
			var line = shapes[i];
			var from = line.getData("from");
			var lineHeight = Linear$14(from, line.getData("to"), this.value) * maxLineHeight;
			var x = leftBound + cellWidth * (i + .5);
			line.lineStyle(lineWidth, this.color, 1).setP0(x, bottomBound).setP1(x, bottomBound - lineHeight);
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/ball/UpdateShapeMethods.js
var Linear$13 = __webpack_exports__Math.Linear;
var UpdateShapeMethods_default$17 = {
	buildShapes() {
		for (var i = 0; i < 3; i++) this.addShape(new Circle());
	},
	updateShapes() {
		var centerX = this.centerX;
		var centerY = this.centerY;
		var radius = this.radius;
		var ballRadius = radius * .1;
		var lineWidth = Math.ceil(ballRadius * .25);
		var t = 1 - Yoyo_default(this.value);
		var trackRadius = Linear$13(.3, .9, t) * radius;
		var shapes = this.getShapes();
		for (var i = 0, cnt = shapes.length; i < cnt; i++) {
			var ball = shapes[i];
			var t = (this.value + i / cnt) % 1;
			var angle = Math.PI * 2 * t;
			ball.lineStyle(lineWidth, this.color).setRadius(ballRadius).setCenterPosition(centerX + Math.cos(angle) * trackRadius, centerY + Math.sin(angle) * trackRadius);
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/bars/UpdateShapeMethods.js
var Linear$12 = __webpack_exports__Math.Linear;
var ExpoIn$3 = __webpack_exports__Math.Easing.Expo.In;
var UpdateShapeMethods_default$16 = {
	buildShapes() {
		var cnt = 5;
		for (var i = 0; i < cnt; i++) {
			var line = new Line();
			this.addShape(line);
			var offset = Yoyo_default(i / (cnt - 1)) / 2;
			line.setData("offset", offset);
		}
	},
	updateShapes() {
		var centerX = this.centerX;
		var centerY = this.centerY;
		var radius = this.radius;
		var leftBound = centerX - radius;
		var maxLineHeight = radius * 2;
		var shapes = this.getShapes(), cnt = shapes.length;
		var cellWidth = radius * 2 / cnt;
		var lineWidth = cellWidth * .7;
		for (var i = 0; i < cnt; i++) {
			var line = shapes[i];
			var t = (this.value + line.getData("offset")) % 1;
			t = ExpoIn$3(Yoyo_default(t));
			var lineHeight = Linear$12(.4, 1, t) * maxLineHeight;
			var x = leftBound + cellWidth * (i + .5);
			line.lineStyle(lineWidth, this.color, 1).setP0(x, centerY - lineHeight / 2).setP1(x, centerY + lineHeight / 2);
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/box/UpdateShapeMethods.js
var UpdateShapeMethods_default$15 = {
	buildShapes() {
		this.addShape(new Lines().setName("border"));
		this.addShape(new Lines().setName("fill"));
	},
	updateShapes() {
		var centerX = this.centerX;
		var centerY = this.centerY;
		var halfWidth = this.radius * .7;
		var left = centerX - halfWidth, top = centerY - halfWidth, width = halfWidth * 2;
		this.getShape("border").lineStyle(2, this.color, 1).startAt(left, top).lineTo(width, 0, true).lineTo(0, width, true).lineTo(-width, 0, true).lineTo(0, -width, true).close();
		if (this.value < .5) {
			var t = (.5 - this.value) * 2;
			var height = width * t;
			this.getShape("fill").fillStyle(this.color, 1).startAt(left, top).lineTo(width, 0, true).lineTo(0, height, true).lineTo(-width, 0, true).lineTo(0, -height, true).close();
		} else {
			var t = (this.value - .5) * 2;
			var angle = 180 * t;
			this.getShape("border").rotateAround(centerX, centerY, angle);
			this.getShape("fill").fillStyle().lineStyle();
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/clock/UpdateShapeMethods.js
var RadToDeg$3 = __webpack_exports__Math.RadToDeg;
var WrapDegrees = __webpack_exports__Math.Angle.WrapDegrees;
var WrapRad = __webpack_exports__Math.Angle.Wrap;
var ShortestBetween = __webpack_exports__Math.Angle.ShortestBetween;
var DegToRad$3 = __webpack_exports__Math.DegToRad;
var Rad270 = __webpack_exports__Math.DegToRad(270);
var UpdateShapeMethods_default$14 = {
	buildShapes() {
		this.addShape(new Circle().setName("border"));
		this.addShape(new Line().setName("minuteHand"));
		this.addShape(new Line().setName("hourHand"));
		this.minuteHandAngle = 0;
		this.hourHandAngle = 0;
	},
	updateShapes() {
		var centerX = this.centerX;
		var centerY = this.centerY;
		var radius = this.radius;
		var lineWidth = Math.ceil(radius / 25);
		var borderRadius = radius - lineWidth / 2;
		var minuteHandLength = radius * .8;
		var hourHandLength = radius * .5;
		var prevMinuteHandAngle = this.minuteHandAngle;
		this.minuteHandAngle = Math.PI * 2 * this.value;
		var deltaAngle = ShortestBetween(WrapDegrees(RadToDeg$3(prevMinuteHandAngle)), WrapDegrees(RadToDeg$3(this.minuteHandAngle)));
		this.hourHandAngle = WrapRad(this.hourHandAngle + DegToRad$3(deltaAngle) / 12);
		this.getShape("border").lineStyle(lineWidth, this.color).setRadius(borderRadius).setCenterPosition(centerX, centerY);
		var angle = this.minuteHandAngle + Rad270;
		this.getShape("minuteHand").lineStyle(lineWidth, this.color).setP0(centerX, centerY).setP1(centerX + Math.cos(angle) * minuteHandLength, centerY + Math.sin(angle) * minuteHandLength);
		var angle = this.hourHandAngle + Rad270;
		this.getShape("hourHand").lineStyle(lineWidth, this.color).setP0(centerX, centerY).setP1(centerX + Math.cos(angle) * hourHandLength, centerY + Math.sin(angle) * hourHandLength);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/cube/UpdateShapeMethods.js
var Linear$11 = __webpack_exports__Math.Linear;
var ExpoIn$2 = __webpack_exports__Math.Easing.Expo.In;
var RowNum$1 = 2;
var ColNum$1 = 2;
var UpdateShapeMethods_default$13 = {
	buildShapes() {
		var cnt = 4;
		for (var i = 0; i < cnt; i++) {
			var line = new Line();
			this.addShape(line);
		}
	},
	updateShapes() {
		var centerX = this.centerX;
		var centerY = this.centerY;
		var radius = this.radius;
		var leftBound = centerX - radius;
		var topBound = centerY - radius;
		var cellWidth = radius * 2 / ColNum$1;
		var cellHeight = radius * 2 / RowNum$1;
		var shapes = this.getShapes(), cnt = shapes.length;
		for (var i = 0; i < cnt; i++) {
			var colIdx = i % ColNum$1;
			var rowIdx = Math.floor(i / RowNum$1);
			var x = leftBound + cellWidth * (colIdx + .5);
			var y = topBound + cellHeight * (rowIdx + .5);
			var line = shapes[i];
			var t = (this.value + (cnt - i) * .1) % 1;
			t = ExpoIn$2(Yoyo_default(t));
			var lineAlpha = (cnt - i) / cnt;
			var lineHeight = Linear$11(.7, 1, t) * cellHeight;
			var lineWidth = Linear$11(.7, 1, t) * cellWidth;
			line.lineStyle(lineWidth, this.color, lineAlpha).setP0(x - lineHeight / 2, y).setP1(x + lineHeight / 2, y);
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/dots/UpdateShapeMethods.js
var Linear$10 = __webpack_exports__Math.Linear;
var UpdateShapeMethods_default$12 = {
	buildShapes() {
		var cnt = 3;
		for (var i = 0; i < cnt; i++) {
			var dot = new Circle();
			this.addShape(dot);
			var offset = Yoyo_default(i / (cnt - 1)) / 2;
			dot.setData("offset", offset);
		}
	},
	updateShapes() {
		var centerX = this.centerX;
		var centerY = this.centerY;
		var radius = this.radius;
		var leftBound = centerX - radius;
		var shapes = this.getShapes(), cnt = shapes.length;
		var cellWidth = radius * 2 / cnt;
		var maxDotRadius = cellWidth / 2;
		for (var i = 0; i < cnt; i++) {
			var dot = shapes[i];
			var t = (this.value + dot.getData("offset")) % 1;
			t = Yoyo_default(t);
			var dotAlpha = Linear$10(.25, 1, t);
			var dotRadius = Linear$10(.5, 1, t) * maxDotRadius;
			dot.fillStyle(this.color, dotAlpha).setRadius(dotRadius).setCenterPosition(leftBound + cellWidth * (i + .5), centerY);
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/facebook/UpdateShapeMethods.js
var Linear$9 = __webpack_exports__Math.Linear;
var ExpoIn$1 = __webpack_exports__Math.Easing.Expo.In;
var UpdateShapeMethods_default$11 = {
	buildShapes() {
		for (var i = 0; i < 3; i++) {
			var shape = new Line();
			this.addShape(shape);
		}
	},
	updateShapes() {
		var centerX = this.centerX;
		var centerY = this.centerY;
		var radius = this.radius;
		var leftBound = centerX - radius;
		var shapes = this.getShapes(), cnt = shapes.length;
		var cellWidth = radius * 2 / cnt;
		var cellHeight = radius * 2;
		for (var i = 0; i < cnt; i++) {
			var line = shapes[i];
			var t = (this.value + (cnt - i) * .1) % 1;
			t = ExpoIn$1(Yoyo_default(t));
			var lineAlpha = (i + 1) / cnt;
			var lineHeight = Linear$9(.7, 1, t) * cellHeight;
			var lineWidth = Linear$9(.7, 1, t) * cellWidth;
			var x = leftBound + cellWidth * (i + .5);
			line.lineStyle(lineWidth, this.color, lineAlpha).setP0(x, centerY - lineHeight / 2).setP1(x, centerY + lineHeight / 2);
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/grid/UpdateShapeMethods.js
var Linear$8 = __webpack_exports__Math.Linear;
var RowNum = 3;
var ColNum = 3;
var UpdateShapeMethods_default$10 = {
	buildShapes() {
		var cnt = 9;
		for (var i = 0; i < cnt; i++) {
			var dot = new Circle();
			this.addShape(dot);
			dot.setData("offset", Math.random());
		}
		this.isInitialize = true;
	},
	updateShapes() {
		var centerX = this.centerX;
		var centerY = this.centerY;
		var radius = this.radius;
		var needLayout = this.isInitialize || this.isSizeChanged;
		var leftBound = centerX - radius;
		var topBound = centerY - radius;
		var cellWidth = radius * 2 / ColNum;
		var cellHeight = radius * 2 / RowNum;
		var maxDotRadius = Math.min(cellWidth, cellHeight) / 2 * .8;
		var shapes = this.getShapes();
		for (var i = 0, cnt = shapes.length; i < cnt; i++) {
			var colIdx = i % ColNum;
			var rowIdx = Math.floor(i / RowNum);
			var x = leftBound + cellWidth * (colIdx + .5);
			var y = topBound + cellHeight * (rowIdx + .5);
			var dot = shapes[i];
			var t = (this.value + dot.getData("offset")) % 1;
			t = Yoyo_default(t);
			dot.fillStyle(this.color, Linear$8(.25, 1, t));
			if (needLayout) dot.setRadius(maxDotRadius).setCenterPosition(x, y);
		}
		this.isInitialize = false;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/hearts/UpdateShapeMethods.js
var UpdateShapeMethods_default$9 = {
	buildShapes() {
		for (var i = 0; i < 2; i++) this.addShape(new Lines());
	},
	updateShapes() {
		var centerX = this.centerX;
		var centerY = this.centerY;
		var radius = this.radius;
		var lineWidth = Math.ceil(radius / 25);
		var maxW50 = radius - lineWidth, maxW30 = maxW50 * .6, maxW35 = maxW50 * .7, maxW60 = maxW50 * 1.2;
		var shapes = this.getShapes();
		for (var i = 0, cnt = shapes.length; i < cnt; i++) {
			var heart = shapes[i];
			var t = (this.value + i / cnt) % 1;
			var alpha = Yoyo_default(t);
			var x = centerX, y = centerY - 15 * t;
			var w50 = maxW50 * t, w30 = maxW30 * t, w35 = maxW35 * t, w60 = maxW60 * t;
			heart.lineStyle(lineWidth, this.color, alpha).startAt(x, y).cubicBezierTo(x, y - w30, x - w50, y - w30, x - w50, y).cubicBezierTo(x - w50, y + w30, x, y + w35, x, y + w60).cubicBezierTo(x, y + w35, x + w50, y + w30, x + w50, y).cubicBezierTo(x + w50, y - w30, x, y - w30, x, y).close();
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/ios/UpdateShapeMethods.js
var Linear$7 = __webpack_exports__Math.Linear;
var UpdateShapeMethods_default$8 = {
	buildShapes() {
		for (var i = 0; i < 12; i++) this.addShape(new Line());
		this.isInitialize = true;
	},
	updateShapes() {
		var centerX = this.centerX;
		var centerY = this.centerY;
		var needLayout = this.isInitialize || this.isSizeChanged;
		var radius = this.radius;
		var startRadius = radius / 2;
		var lineWidth = Math.ceil(radius / 20);
		var shapes = this.getShapes();
		for (var i = 0, cnt = shapes.length; i < cnt; i++) {
			var line = shapes[i];
			var t = i / cnt;
			var angle = Math.PI * 2 * t;
			var alpha = Linear$7(.25, 1, (1 - this.value + t) % 1);
			line.lineStyle(lineWidth, this.color, alpha);
			if (needLayout) line.setP0(centerX + Math.cos(angle) * startRadius, centerY + Math.sin(angle) * startRadius).setP1(centerX + Math.cos(angle) * radius, centerY + Math.sin(angle) * radius);
		}
		this.isInitialize = false;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/orbit/UpdateShapeMethods.js
var UpdateShapeMethods_default$7 = {
	buildShapes() {
		this.addShape(new Circle().setName("track"));
		this.addShape(new Circle().setName("thumb"));
	},
	updateShapes() {
		var centerX = this.centerX;
		var centerY = this.centerY;
		var radius = this.radius;
		var trackRadius = radius * .9;
		var trackThickness = Math.ceil(trackRadius / 25);
		var thumbRadius = radius * .1;
		var thumbAngle = Math.PI * 2 * this.value;
		this.getShape("track").lineStyle(trackThickness, this.color, .7).setRadius(trackRadius).setCenterPosition(centerX, centerY);
		this.getShape("thumb").fillStyle(this.color).setRadius(thumbRadius).setCenterPosition(centerX + Math.cos(thumbAngle) * trackRadius, centerY + Math.sin(thumbAngle) * trackRadius);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/oval/UpdateShapeMethods.js
var UpdateShapeMethods_default$6 = {
	buildShapes() {
		this.addShape(new Circle().setName("track"));
		this.addShape(new Arc().setName("arc"));
	},
	updateShapes() {
		var centerX = this.centerX;
		var centerY = this.centerY;
		var radius = this.radius;
		var lineWidth = Math.ceil(radius / 25);
		var maxRadius = radius - lineWidth / 2;
		this.getShape("track").lineStyle(lineWidth, this.color, .5).setRadius(maxRadius).setCenterPosition(centerX, centerY);
		var startAngle = this.value * 360;
		var endAngle = startAngle + 60;
		this.getShape("arc").lineStyle(lineWidth, this.color, 1).setRadius(maxRadius).setCenterPosition(centerX, centerY).setAngle(startAngle, endAngle);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/pie/UpdateShapeMethods.js
var Linear$6 = __webpack_exports__Math.Linear;
var UpdateShapeMethods_default$5 = {
	buildShapes() {
		for (var i = 0; i < 4; i++) {
			var pie = new Arc().setPie();
			this.addShape(pie);
			pie.setData("speed", Linear$6(180, 360, Math.random()));
		}
		this.prevValue = void 0;
	},
	updateShapes() {
		var centerX = this.centerX;
		var centerY = this.centerY;
		var radius = this.radius;
		var deltaValue;
		if (this.prevValue !== void 0) {
			deltaValue = this.value - this.prevValue;
			if (this.prevValue > this.value) deltaValue += 1;
		}
		var shapes = this.getShapes();
		for (var i = 0, cnt = shapes.length; i < cnt; i++) {
			var pie = shapes[i];
			var pieAlpha = (i + 1) / cnt;
			if (this.prevValue === void 0) {
				var startAngle = i / cnt * 360;
				var endAngle = startAngle + 90;
				pie.fillStyle(this.color, pieAlpha).setRadius(radius).setCenterPosition(centerX, centerY).setAngle(startAngle, endAngle).setData("angle", startAngle);
			} else {
				var startAngle = pie.getData("angle") + pie.getData("speed") * deltaValue;
				startAngle = startAngle % 360;
				var endAngle = startAngle + 90;
				pie.fillStyle(this.color, pieAlpha).setRadius(radius).setCenterPosition(centerX, centerY).setAngle(startAngle, endAngle).setData("angle", startAngle);
			}
		}
		this.prevValue = this.value;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/puff/UpdateShapeMethods.js
var UpdateShapeMethods_default$4 = {
	buildShapes() {
		this.addShape(new Circle());
	},
	updateShapes() {
		var centerX = this.centerX;
		var centerY = this.centerY;
		var radius = this.radius;
		var puffRadius = radius * this.value;
		var lineWidth = Math.ceil(radius / 25);
		var alpha = Yoyo_default(this.value);
		this.getShapes()[0].lineStyle(lineWidth, this.color, alpha).setRadius(puffRadius).setCenterPosition(centerX, centerY);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/radio/UpdateShapeMethods.js
var Linear$5 = __webpack_exports__Math.Linear;
var ExpoIn = __webpack_exports__Math.Easing.Expo.In;
var UpdateShapeMethods_default$3 = {
	buildShapes() {
		this.addShape(new Circle().setName("center"));
		this.addShape(new Lines().setName("arc0"));
		this.addShape(new Lines().setName("arc1"));
		this.isInitialize = true;
	},
	updateShapes() {
		var centerX = this.centerX;
		var centerY = this.centerY;
		var radius = this.radius;
		var needLayout = this.isInitialize || this.isSizeChanged;
		var centerRadius = radius * 2 / 6;
		var x = centerX - radius + centerRadius;
		var y = centerY + radius - centerRadius;
		var shapes = this.getShapes();
		for (var i = 0, cnt = shapes.length; i < cnt; i++) {
			var shape = shapes[i];
			var t = (this.value + (cnt - i) * .1) % 1;
			t = ExpoIn(Yoyo_default(t));
			var alpha = Linear$5(.25, 1, t);
			switch (shape.name) {
				case "center":
					shape.fillStyle(this.color, alpha);
					if (needLayout) shape.setRadius(centerRadius).setCenterPosition(x, y);
					break;
				case "arc0":
					shape.fillStyle(this.color, alpha);
					if (needLayout) {
						var radius0 = centerRadius * 2, radius1 = centerRadius * 3;
						shape.startAt(x, y - radius0).lineTo(x, y - radius1).setIterations(8).arc(x, y, radius1, 270, 360).lineTo(x + radius0, y).setIterations(6).arc(x, y, radius0, 360, 270, true).close();
					}
					break;
				case "arc1":
					shape.fillStyle(this.color, alpha);
					if (needLayout) {
						var radius0 = centerRadius * 4, radius1 = centerRadius * 5;
						shape.startAt(x, y - radius0).lineTo(x, y - radius1).setIterations(8).arc(x, y, radius1, 270, 360).lineTo(x + radius0, y).setIterations(6).arc(x, y, radius0, 360, 270, true).close();
					}
			}
		}
		this.isInitialize = false;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/rings/UpdateShapeMethods.js
var UpdateShapeMethods_default$2 = {
	buildShapes() {
		for (var i = 0; i < 2; i++) this.addShape(new Circle());
	},
	updateShapes() {
		var centerX = this.centerX;
		var centerY = this.centerY;
		var radius = this.radius;
		var lineWidth = Math.ceil(radius / 25);
		var maxRingRadius = radius - lineWidth;
		var shapes = this.getShapes();
		for (var i = 0, cnt = shapes.length; i < cnt; i++) {
			var ring = shapes[i];
			var t = (this.value + i / cnt) % 1;
			var alpha = Yoyo_default(t);
			ring.lineStyle(lineWidth, this.color, alpha).setRadius(t * maxRingRadius).setCenterPosition(centerX, centerY);
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/spinner/UpdateShapeMethods.js
var UpdateShapeMethods_default$1 = {
	buildShapes() {
		this.addShape(new Arc().setName("arc"));
	},
	updateShapes() {
		var centerX = this.centerX;
		var centerY = this.centerY;
		var radius = this.radius;
		var lineWidth = Math.ceil(radius / 10);
		var maxRadius = radius - lineWidth;
		var endAngle = this.value * 720;
		var startAngle = endAngle - Yoyo_default(this.value) * 180;
		this.getShape("arc").lineStyle(lineWidth, this.color, 1).setRadius(maxRadius).setCenterPosition(centerX, centerY).setAngle(startAngle + 315, endAngle + 315);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/aio/UpdateShapeMethods.js
__webpack_exports__Utils.Objects.GetValue;
var AnimationModeMap = {
	leftArrow: UpdateShapeMethods_default$19,
	rightArrow: UpdateShapeMethods_default$19,
	upArrow: UpdateShapeMethods_default$19,
	downArrow: UpdateShapeMethods_default$19,
	audio: UpdateShapeMethods_default$18,
	ball: UpdateShapeMethods_default$17,
	bars: UpdateShapeMethods_default$16,
	box: UpdateShapeMethods_default$15,
	clock: UpdateShapeMethods_default$14,
	cube: UpdateShapeMethods_default$13,
	dots: UpdateShapeMethods_default$12,
	facebook: UpdateShapeMethods_default$11,
	grid: UpdateShapeMethods_default$10,
	hearts: UpdateShapeMethods_default$9,
	ios: UpdateShapeMethods_default$8,
	oribit: UpdateShapeMethods_default$7,
	oval: UpdateShapeMethods_default$6,
	pie: UpdateShapeMethods_default$5,
	puff: UpdateShapeMethods_default$4,
	radio: UpdateShapeMethods_default$3,
	rings: UpdateShapeMethods_default$2,
	spinner: UpdateShapeMethods_default$1
};
var AnimationModeList = [];
for (var name in AnimationModeMap) AnimationModeList.push(name);
var GetRandomItem = __webpack_exports__Utils.Array.GetRandom;
var UpdateShapeMethods_default = {
	setAnimationMode(mode, config) {
		if (!AnimationModeMap.hasOwnProperty(mode)) mode = GetRandomItem(AnimationModeList);
		this.animationMode = mode;
		var updateMethods = AnimationModeMap[mode];
		if (config) this.resetFromConfig(config);
		switch (mode) {
			case "leftArrow":
				updateMethods.setDirection.call(this, "left");
				break;
			case "rightArrow":
				updateMethods.setDirection.call(this, "right");
				break;
			case "upArrow":
				updateMethods.setDirection.call(this, "up");
				break;
			case "downArrow": updateMethods.setDirection.call(this, "down");
		}
		this.clear();
		updateMethods.buildShapes.call(this);
		this.updateShapes = updateMethods.updateShapes.bind(this);
		this.stop().start();
		return this;
	},
	setRandomAnimationMode(config) {
		var mode = GetRandomItem(AnimationModeList);
		this.setAnimationMode(mode, config);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/spinner/aio/AIO.js
var GetValue$172 = __webpack_exports__Utils.Objects.GetValue;
var AIO = class extends Base$2 {
	constructor(scene, config) {
		super(scene, config);
		this.type = "rexSpinnerAIO";
		this.setAnimationMode(GetValue$172(config, "animationMode"));
	}
};
Object.assign(AIO.prototype, UpdateShapeMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/aiospinner/AIOSpinner.js
var AIOSpinner_default = AIO;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/aiospinner/Factory.js
ObjectFactory.register("aioSpinner", function(config) {
	var gameObject = new AIOSpinner_default(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.AIOSpinner", AIOSpinner_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/gameobject/build/CreateRectangle.js
var GetValue$171 = __webpack_exports__Utils.Objects.GetValue;
var CreateRectangle = function(scene, config) {
	var x = GetValue$171(config, "x", 0);
	var y = GetValue$171(config, "y", 0);
	var width = GetValue$171(config, "width", 1);
	var height = GetValue$171(config, "height", 1);
	var color = GetValue$171(config, "color", void 0);
	var alpha = GetValue$171(config, "alpha", 1);
	var strokeColor = GetValue$171(config, "strokeColor", void 0);
	var strokeAlpha = GetValue$171(config, "strokeAlpha", 1);
	var strokeWidth = GetValue$171(config, "strokeWidth", 2);
	var gameObject = scene.add.rectangle(x, y, width, height);
	if (color !== void 0) gameObject.setFillStyle(color, alpha);
	if (strokeColor !== void 0) gameObject.setStrokeStyle(strokeWidth, strokeColor, strokeAlpha);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/utils/FlipMethods.js
var FlipMethods_default = {
	setFlipX(value) {
		this.flipX = value;
		return this;
	},
	setFlipY(value) {
		this.flipY = value;
		return this;
	},
	toggleFlipX() {
		this.flipX = !this.flipX;
		return this;
	},
	toggleFlipY() {
		this.flipY = !this.flipY;
		return this;
	},
	setFlip(x, y) {
		this.flipX = x;
		this.flipY = y;
		return this;
	},
	resetFlip() {
		this.flipX = false;
		this.flipY = false;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/image/imagebox/ImageBoxBase.js
var IsPlainObject$28 = __webpack_exports__Utils.Objects.IsPlainObject;
var ImageBoxBase = class extends ContainerLite {
	setBackground(background) {
		if (IsPlainObject$28(background)) {
			background = CreateRectangle(this.scene, background);
			this.scene.add.existing(background);
		}
		if (background) this.add(background);
		this.background = background;
		return this;
	}
	setImage(image) {
		if (!image) image = this.scene.add.image(this.x, this.y);
		else image.setPosition(this.x, this.y).setOrigin(.5);
		this.add(image);
		this.image = image;
		this.image.setFlipX(this.flipX).setFlipY(this.flipY);
		if (this._colorTopLeft !== void 0) this.image.setTint(this._colorTopLeft, this._colorTopRight, this._colorBottomLeft, this._colorBottomRight);
		return this;
	}
	get texture() {
		return this.image.texture;
	}
	get frame() {
		return this.image.frame;
	}
	get flipX() {
		return this._flipX;
	}
	set flipX(value) {
		if (this._flipX === value) return;
		this._flipX = value;
		this.image.setFlipX(value);
	}
	get flipY() {
		return this._flipY;
	}
	set flipY(value) {
		if (this._flipY === value) return;
		this._flipY = value;
		this.image.setFlipY(value);
	}
	set tint(value) {
		this.image.tint = value;
	}
	get isTinted() {
		return this.image.isTinted;
	}
	setTint(colorTopLeft, colorTopRight, colorBottomLeft, colorBottomRight) {
		this._colorTopLeft = colorTopLeft;
		this._colorTopRight = colorTopRight;
		this._colorBottomLeft = colorBottomLeft;
		this._colorBottomRight = colorBottomRight;
		this.image.setTint(colorTopLeft, colorTopRight, colorBottomLeft, colorBottomRight);
		return this;
	}
	resizeBackground() {
		var background = this.background;
		if (!background) return this;
		background.setOrigin(this.originX, this.originY);
		background.setPosition(this.x, this.y);
		ResizeGameObject(background, this.displayWidth, this.displayHeight);
		this.resetChildScaleState(background);
		return this;
	}
	scaleImage() {
		var image = this.image;
		if (!this.scaleUp && image.width <= this.width && image.height <= this.height) return this;
		var result = FitTo(image, this, "FIT", true);
		image.setDisplaySize(result.width * this.scaleX, result.height * this.scaleY);
		this.resetChildScaleState(image);
		return this;
	}
	resize(width, height) {
		super.resize(width, height);
		this.resizeBackground();
		this.scaleImage();
		return this;
	}
	setTexture(texture, frame) {
		var image = this.image;
		image.setTexture(texture, frame);
		if (texture) {
			this.setChildVisible(image, true);
			this.scaleImage();
		} else this.setChildVisible(image, false);
		return this;
	}
};
Object.assign(ImageBoxBase.prototype, FlipMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/image/imagebox/ImageBox.js
var IsPlainObject$27 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$170 = __webpack_exports__Utils.Objects.GetValue;
var ImageBox = class extends ImageBoxBase {
	constructor(scene, x, y, texture, frame, config) {
		if (IsPlainObject$27(x)) {
			config = x;
			x = GetValue$170(config, "x", 0);
			y = GetValue$170(config, "y", 0);
			texture = GetValue$170(config, "key", void 0);
			frame = GetValue$170(config, "frame", void 0);
		} else if (IsPlainObject$27(frame)) {
			config = frame;
			frame = void 0;
		}
		super(scene, x, y, 1, 1);
		this.type = "rexImageBox";
		var background = GetValue$170(config, "background");
		this.setBackground(background);
		var image = GetValue$170(config, "image");
		this.setImage(image);
		image = this.image;
		if (texture === void 0) this.setChildVisible(image, false);
		else image.setTexture(texture, frame);
		this.scaleUp = GetValue$170(config, "scaleUp", false);
		var width = GetValue$170(config, "width", image.width);
		var height = GetValue$170(config, "height", image.height);
		this.resize(width, height);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/imagebox/ImageBox.js
var ImageBox_default = ImageBox;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/imagebox/Factory.js
ObjectFactory.register("imageBox", function(x, y, texture, frame, config) {
	var gameObject = new ImageBox_default(this.scene, x, y, texture, frame, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.ImageBox", ImageBox_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/lazyloadimagebox/SpinnerMethods.js
var GetValue$169 = __webpack_exports__Utils.Objects.GetValue;
var SpinnerMethods_default = {
	setSpinner(spinner) {
		if (spinner === null) {
			this.spinner = spinner;
			return this;
		}
		var sizeRatio;
		if (!IsGameObject(spinner)) {
			var scene = this.scene;
			var animationMode = GetValue$169(spinner, "animationMode", "ios");
			sizeRatio = GetValue$169(spinner, "sizeRatio", .6);
			var size = Math.min(this.displayWidth, this.displayHeight) * sizeRatio;
			spinner = new AIO(scene, {
				width: size,
				height: size,
				animationMode
			});
			scene.add.existing(spinner);
		} else sizeRatio = spinner.width / Math.min(this.width, this.height);
		this.spinnerSizeRatio = sizeRatio;
		spinner.setPosition(this.x, this.y).setOrigin(.5);
		this.add(spinner);
		this.spinner = spinner;
		this.stopSpinner();
		return this;
	},
	startSpinner() {
		var spinner = this.spinner;
		if (!spinner) return this;
		spinner.start();
		this.setChildVisible(spinner, true);
		return this;
	},
	stopSpinner() {
		var spinner = this.spinner;
		if (!spinner) return this;
		spinner.stop();
		this.setChildVisible(spinner, false);
		return this;
	},
	resizeSpinner() {
		var spinner = this.spinner;
		if (!spinner) return this;
		var size = Math.min(this.displayWidth, this.displayHeight) * this.spinnerSizeRatio;
		spinner.setSize(size, size);
		this.resetChildScaleState(spinner);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/lazyloadimagebox/LazyLoadImageBox.js
var GetValue$168 = __webpack_exports__Utils.Objects.GetValue;
var LazyLoadImageBox = class extends ImageBoxBase {
	constructor(scene, config) {
		var x = GetValue$168(config, "x", 0);
		var y = GetValue$168(config, "y", 0);
		var width = GetValue$168(config, "width", 0);
		var height = GetValue$168(config, "height", 0);
		super(scene, x, y, width, height);
		this.type = "rexLazyLoadImageBox";
		this.scaleUp = GetValue$168(config, "scaleUp", false);
		var background = GetValue$168(config, "background");
		this.setBackground(background);
		var image = GetValue$168(config, "image");
		this.setImage(image);
		var texture = GetValue$168(config, "key", void 0);
		var frame = GetValue$168(config, "frame", void 0);
		var url = GetValue$168(config, "url", void 0);
		this.setTexture(texture, frame, url);
		var spinner = GetValue$168(config, "spinner");
		this.setSpinner(spinner);
		this.resize(width, height);
	}
	setTexture(texture, frame, url) {
		this._textureKey = texture;
		this._frameName = frame;
		var scene = this.scene;
		if (!scene.sys.textures.exists(texture) && !!texture && !!url) {
			super.setTexture();
			this.startSpinner();
			var self = this;
			var callback = super.setTexture;
			scene.load.image(texture, url).once(`filecomplete-image-${texture}`, function(key) {
				if (!!self.scene && key === self._textureKey) {
					callback.call(self, texture, frame);
					self.stopSpinner();
				}
			}).start();
		} else {
			super.setTexture(texture, frame);
			this.stopSpinner();
		}
		return this;
	}
	resize(width, height) {
		super.resize(width, height);
		this.resizeSpinner();
		return this;
	}
};
Object.assign(LazyLoadImageBox.prototype, SpinnerMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fullwindowrectangle/FullWindowRectangle.js
var FullWindowRectangle_default = fullwindowrectangle_default;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fullwindowrectangle/Factory.js
ObjectFactory.register("fullWindowRectangle", function(fillColor, fillAlpha) {
	var gameObject = new FullWindowRectangle_default(this.scene, fillColor, fillAlpha);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.FullWindowRectangle", FullWindowRectangle_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fullwindowzone/FullWindowZone.js
var FullWindowZone_default = FullWindowRectangle$1;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fullwindowzone/Factory.js
ObjectFactory.register("fullWindowZone", function() {
	var gameObject = new FullWindowZone_default(this.scene);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.FullWindowZone", FullWindowZone_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/cover/Cover.js
var Cover_default = Cover;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/cover/Factory.js
ObjectFactory.register("cover", function(config) {
	var gameObject = new Cover_default(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.Cover", Cover_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dom/utils/Resize.js
var Resize$1 = function(width, height) {
	if (this.scene.sys.scale.autoRound) {
		width = Math.floor(width);
		height = Math.floor(height);
	}
	if (this.width === width && this.height === height) return this;
	var style = this.node.style;
	style.width = `${width}px`;
	style.height = `${height}px`;
	this.updateSize();
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dom/inputtext/InputTextProperties.js
var ElementProperties = {
	id: ["id", void 0],
	text: ["value", void 0],
	maxLength: ["maxLength", void 0],
	minLength: ["minLength", void 0],
	placeholder: ["placeholder", void 0],
	tooltip: ["title", void 0],
	readOnly: ["readOnly", false],
	spellCheck: ["spellcheck", false],
	autoComplete: ["autocomplete", "off"],
	autoCapitalize: ["autocapitalize", "off"]
};
var StyleProperties = {
	align: ["textAlign", void 0],
	paddingLeft: ["padding-left", void 0],
	paddingRight: ["padding-right", void 0],
	paddingTop: ["padding-top", void 0],
	paddingBottom: ["padding-bottom", void 0],
	fontFamily: ["fontFamily", void 0],
	fontSize: ["font-size", void 0],
	color: ["color", "#ffffff"],
	backgroundColor: ["backgroundColor", "transparent"],
	border: ["border", 0],
	borderColor: ["borderColor", "transparent"],
	borderRadius: ["border-radius", void 0],
	outline: ["outline", "none"],
	direction: ["direction", void 0]
};
var ElementEvents = {
	input: "textchange",
	click: "click",
	dblclick: "dblclick",
	mousedown: "pointerdown",
	mousemove: "pointermove",
	mouseup: "pointerup",
	touchstart: "pointerdown",
	touchmove: "pointermove",
	touchend: "pointerup",
	keydown: "keydown",
	keyup: "keyup",
	keypress: "keypress",
	compositionstart: "compositionStart",
	compositionend: "compositionEnd",
	compositionupdate: "compositionUpdate",
	focus: "focus",
	blur: "blur",
	select: "select"
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dom/utils/RouteEvents.js
var GetValue$167 = __webpack_exports__Utils.Objects.GetValue;
var RouteEvents = function(gameObject, element, elementEvents, config) {
	var preventDefault = GetValue$167(config, "preventDefault", false);
	var preTest = GetValue$167(config, "preTest");
	for (let elementEventName in elementEvents) element.addEventListener(elementEventName, function(e) {
		if (!preTest || preTest(gameObject, elementEventName)) gameObject.emit(elementEvents[elementEventName], gameObject, e);
		if (preventDefault) e.preventDefault();
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dom/inputtext/InputText.js
var DOMElement$2 = __webpack_exports__GameObjects.DOMElement;
var IsPlainObject$26 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$166 = __webpack_exports__Utils.Objects.GetValue;
var InputText = class extends DOMElement$2 {
	constructor(scene, x, y, width, height, config) {
		if (IsPlainObject$26(x)) {
			config = x;
			x = GetValue$166(config, "x", 0);
			y = GetValue$166(config, "y", 0);
			width = GetValue$166(config, "width", 0);
			height = GetValue$166(config, "height", 0);
		} else if (IsPlainObject$26(width)) {
			config = width;
			width = GetValue$166(config, "width", 0);
			height = GetValue$166(config, "height", 0);
		}
		if (config === void 0) config = {};
		var element;
		var textType = GetValue$166(config, "inputType", void 0);
		if (textType === void 0) textType = GetValue$166(config, "type", "text");
		if (textType === "textarea") {
			element = document.createElement("textarea");
			element.style.resize = "none";
		} else {
			element = document.createElement("input");
			element.type = textType;
		}
		SetProperties(ElementProperties, config, element);
		var style = GetValue$166(config, "style", void 0);
		style = SetProperties(StyleProperties, config, style);
		var elementStyle = element.style;
		for (var key in config) if (key in ElementProperties || key in StyleProperties) continue;
		else if (key in elementStyle) style[key] = config[key];
		else if (key in element) {
			if (key === "type") continue;
			element[key] = config[key];
		}
		style["box-sizing"] = "border-box";
		super(scene, x, y, element, style);
		this.type = "rexInputText";
		this.resize(width, height);
		RouteEvents(this, element, ElementEvents);
		StopPropagationTouchEvents(element);
		if (GetValue$166(config, "selectAll", false)) this.selectAll();
		this._isFocused = false;
		this.on("focus", function() {
			this._isFocused = true;
		}, this).on("blur", function() {
			this._isFocused = false;
		}, this);
	}
	get inputType() {
		if (this.node.tagName.toLowerCase() === "textarea") return "textarea";
		else return this.node.type;
	}
	get text() {
		return this.node.value;
	}
	set text(value) {
		this.node.value = value;
	}
	setText(value) {
		this.text = value;
		return this;
	}
	get maxLength() {
		return this.node.maxLength;
	}
	set maxLength(value) {
		this.node.maxLength = value;
	}
	setMaxLength(value) {
		this.maxLength = value;
		return this;
	}
	get minLength() {
		return this.node.minLength;
	}
	set minLength(value) {
		this.node.minLength = value;
	}
	setMinLength(value) {
		this.minLength = value;
		return this;
	}
	get placeholder() {
		return this.node.placeholder;
	}
	set placeholder(value) {
		this.node.placeholder = value;
	}
	setPlaceholder(value) {
		this.placeholder = value;
		return this;
	}
	selectText(selectionStart, selectionEnd) {
		if (selectionStart === void 0) this.node.select();
		else this.node.setSelectionRange(selectionStart, selectionEnd);
		return this;
	}
	selectAll() {
		this.selectText();
		return this;
	}
	get selectionStart() {
		return this.node.selectionStart;
	}
	get selectionEnd() {
		return this.node.selectionEnd;
	}
	get selectedText() {
		var node = this.node;
		return node.value.substring(node.selectionStart, node.selectionEnd);
	}
	get cursorPosition() {
		return this.node.selectionStart;
	}
	set cursorPosition(value) {
		this.node.setSelectionRange(value, value);
	}
	setCursorPosition(value) {
		if (value === void 0) value = this.text.length;
		else if (value < 0) value = this.text.length + value;
		this.cursorPosition = value;
		return this;
	}
	get tooltip() {
		return this.node.title;
	}
	set tooltip(value) {
		this.node.title = value;
	}
	setTooltip(value) {
		this.tooltip = value;
		return this;
	}
	setTextChangedCallback(callback) {
		this.onTextChanged = callback;
		return this;
	}
	get readOnly() {
		return this.node.readOnly;
	}
	set readOnly(value) {
		this.node.readOnly = value;
	}
	setReadOnly(value) {
		if (value === void 0) value = true;
		this.readOnly = value;
		return this;
	}
	get spellCheck() {
		return this.node.spellcheck;
	}
	set spellCheck(value) {
		this.node.spellcheck = value;
	}
	setSpellCheck(value) {
		this.spellCheck = value;
		return this;
	}
	get fontColor() {
		return this.node.style.color;
	}
	set fontColor(value) {
		this.node.style.color = value;
	}
	setFontColor(value) {
		this.fontColor = value;
		return this;
	}
	setStyle(key, value) {
		this.node.style[key] = value;
		return this;
	}
	getStyle(key) {
		return this.node.style[key];
	}
	scrollToBottom() {
		this.node.scrollTop = this.node.scrollHeight;
		return this;
	}
	setEnabled(enabled) {
		if (enabled === void 0) enabled = true;
		this.node.disabled = !enabled;
		return this;
	}
	setBlur() {
		this.node.blur();
		return this;
	}
	setFocus() {
		this.node.focus();
		return this;
	}
	get isFocused() {
		return this._isFocused;
	}
};
var methods$26 = { resize: Resize$1 };
Object.assign(InputText.prototype, methods$26);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/inputtext/InputText.js
var InputText_default = InputText;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/inputtext/Factory.js
ObjectFactory.register("inputText", function(config) {
	var gameObject = new InputText_default(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.InputText", InputText_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/filechooser/CreateFileInput.js
var GetValue$165 = __webpack_exports__Utils.Objects.GetValue;
var CreateFileInput = function(game, config) {
	var fileInput = document.createElement("input");
	fileInput.type = "file";
	var accept = GetValue$165(config, "accept", "");
	var multiple = GetValue$165(config, "multiple", false);
	fileInput.setAttribute("accept", accept);
	if (multiple) fileInput.setAttribute("multiple", "");
	else fileInput.removeAttribute("multiple");
	return fileInput;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/promise/Delay.js
var Delay = function(time, result) {
	if (time === void 0) time = 0;
	return new Promise(function(resolve, reject) {
		setTimeout(function() {
			resolve(result);
		}, time);
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dom/filechooser/ClickPromise.js
var ClickPromise = function({ game, fileInput, closeDelay }) {
	return WaitEvent(GetGame(game).events, "focus").then(function() {
		return Delay(closeDelay);
	}).then(function() {
		var result = { files: fileInput.files };
		return Promise.resolve(result);
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/filechooser/Open.js
var GetValue$164 = __webpack_exports__Utils.Objects.GetValue;
var RemoveFromDOM = __webpack_exports__DOM.RemoveFromDOM;
var Open$1 = function(game, config) {
	var closeDelay = GetValue$164(config, "closeDelay", 200);
	var fileInput = CreateFileInput(game, config);
	fileInput.click();
	return ClickPromise({
		game,
		fileInput,
		closeDelay
	}).then(function(result) {
		RemoveFromDOM(fileInput);
		fileInput.remove();
		return Promise.resolve(result);
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dom/utils/SyncTo.js
var SyncTo = function(gameObject) {
	this.setOrigin(gameObject.originX, gameObject.originY);
	this.setPosition(gameObject.x, gameObject.y);
	this.resize(gameObject.displayWidth, gameObject.displayHeight);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/system/GetCache.js
var GetCache = function(game, loaderType, cacheType) {
	if (cacheType === void 0) switch (loaderType) {
		case "image":
		case "svg":
			cacheType = "textures";
			break;
		case "animation":
			cacheType = "json";
			break;
		case "tilemapTiledJSON":
		case "tilemapCSV":
			cacheType = "tilemap";
			break;
		case "glsl":
			cacheType = "shader";
			break;
		default: cacheType = loaderType;
	}
	game = GetGame(game);
	var cache;
	if (cacheType === "textures") cache = game.textures;
	else cache = game.cache[cacheType];
	return cache;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/loader/FileObjectToCache.js
var FileObjectToCache = function(scene, file, loaderType, key, cacheType, onComplete) {
	if (cacheType === null || cacheType === false) {} else if (IsFunction(cacheType)) cacheType();
	else {
		var cache = GetCache(scene, loaderType, cacheType);
		if (cache.exists(key)) cache.remove(key);
	}
	var loader = scene.load;
	if (onComplete) loader.once(`filecomplete-${loaderType}-${key}`, function(key, type, data) {
		onComplete(data);
	});
	if (IsFunction(file)) file();
	else {
		var url = window.URL.createObjectURL(file);
		loader[loaderType](key, url);
	}
	loader.start();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dom/utils/LoadFileMethods.js
var LoadFile = function(file, loaderType, key, cacheType, onComplete) {
	var scene = this.scene;
	FileObjectToCache(scene, file, loaderType, key, cacheType, onComplete);
	return this;
};
var LoadFilePromise = function(file, loaderType, key, cacheType) {
	var scene = this.scene;
	return new Promise(function(resolve, reject) {
		var onComplete = function(data) {
			resolve(data);
		};
		FileObjectToCache(scene, file, loaderType, key, cacheType, onComplete);
	});
};
var LoadFileMethods_default = {
	loadFile: LoadFile,
	loadFilePromise: LoadFilePromise
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dom/filechooser/FileChooser.js
var DOMElement$1 = __webpack_exports__GameObjects.DOMElement;
var IsPlainObject$25 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$163 = __webpack_exports__Utils.Objects.GetValue;
var FileChooser = class extends DOMElement$1 {
	constructor(scene, x, y, width, height, config) {
		if (IsPlainObject$25(x)) {
			config = x;
			x = GetValue$163(config, "x", 0);
			y = GetValue$163(config, "y", 0);
			width = GetValue$163(config, "width", 0);
			height = GetValue$163(config, "height", 0);
		} else if (IsPlainObject$25(width)) {
			config = width;
			width = GetValue$163(config, "width", 0);
			height = GetValue$163(config, "height", 0);
		}
		var inputElement = document.createElement("input");
		inputElement.type = "file";
		var inputStyle = inputElement.style;
		inputStyle.display = "none";
		var labelElement = document.createElement("label");
		labelElement.appendChild(inputElement);
		var style = GetValue$163(config, "style", void 0);
		super(scene, x, y, labelElement, style);
		this.type = "rexFileChooser";
		this.resetFromJSON(config);
		this.resize(width, height);
		var self = this;
		inputElement.onchange = function() {
			self.emit("change", self);
		};
		this.setCloseDelay(GetValue$163(config, "closeDelay", 200));
		inputElement.onclick = function() {
			ClickPromise({
				game: scene,
				fileInput: inputElement,
				closeDelay: self.closeDelay
			}).then(function() {
				self.emit("select", self);
			});
		};
	}
	resetFromJSON(config) {
		this.setAccept(GetValue$163(config, "accept", ""));
		this.setMultiple(GetValue$163(config, "multiple", false));
		return this;
	}
	setAccept(accept) {
		if (accept === void 0) accept = "";
		this.fileInput.setAttribute("accept", accept);
		return this;
	}
	setMultiple(enabled) {
		if (enabled === void 0) enabled = true;
		if (enabled) this.fileInput.setAttribute("multiple", "");
		else this.fileInput.removeAttribute("multiple");
		return this;
	}
	setCloseDelay(delay) {
		if (delay === void 0) delay = 200;
		this.closeDelay = delay;
		return this;
	}
	get fileInput() {
		return this.node.children[0];
	}
	open() {
		this.fileInput.click();
		return this;
	}
	get files() {
		return this.fileInput.files;
	}
	setOpenEnable(enable) {
		if (enable === void 0) enable = true;
		this.fileInput.disabled = !enable;
		return this;
	}
};
var methods$25 = {
	resize: Resize$1,
	syncTo: SyncTo
};
Object.assign(FileChooser.prototype, methods$25, LoadFileMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/filechooser/Factory.js
ObjectFactory.register("fileChooser", function(config) {
	var gameObject = new FileChooser(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.FileChooser", FileChooser);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dom/filedropzone/methods/DropEnableMethods.js
var DropEnableMethods_default = {
	setDropEnable(enable) {
		if (enable === void 0) enable = true;
		this.dropEnable = enable;
		return this;
	},
	toggleDropEnable() {
		this.dropEnable = !this.dropEnable;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dom/filedropzone/methods/FilterMethods.js
var FilterMethods_default = {
	addFilter(name, callback) {
		if (!this.filters) this.filters = {};
		this.filters[name] = callback;
		return this;
	},
	addFilters(fileTypeFilters) {
		if (!this.fileTypeFilters) this.fileTypeFilters = {};
		for (var name in fileTypeFilters) this.fileTypeFilters[name] = fileTypeFilters[name];
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dom/filedropzone/methods/Methods.js
var Methods$13 = {
	resize: Resize$1,
	syncTo: SyncTo
};
Object.assign(Methods$13, DropEnableMethods_default, FilterMethods_default, LoadFileMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dom/filedropzone/FileDropZoneProperties.js
var DragDropEvents = {
	dragenter: "dragenter",
	dragleave: "dragleave",
	dragover: "dragover",
	drop: "drop"
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/dom/filedropzone/FileDropZone.js
var DOMElement = __webpack_exports__GameObjects.DOMElement;
var IsPlainObject$24 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$162 = __webpack_exports__Utils.Objects.GetValue;
var FileDropZone = class extends DOMElement {
	constructor(scene, x, y, width, height, config) {
		if (IsPlainObject$24(x)) {
			config = x;
			x = GetValue$162(config, "x", 0);
			y = GetValue$162(config, "y", 0);
			width = GetValue$162(config, "width", 0);
			height = GetValue$162(config, "height", 0);
		} else if (IsPlainObject$24(width)) {
			config = width;
			width = GetValue$162(config, "width", 0);
			height = GetValue$162(config, "height", 0);
		}
		if (config === void 0) config = {};
		var element = document.createElement("div");
		var style = GetValue$162(config, "style", void 0);
		super(scene, x, y, element, style);
		this.type = "rexFileDropZone";
		this.resize(width, height);
		this._files = [];
		this.setDropEnable(GetValue$162(config, "dropEnable", true));
		var filters = GetValue$162(config, "filters");
		if (filters) this.addFilters(filters);
		RouteEvents(this, element, DragDropEvents, {
			preventDefault: true,
			preTest(gameObject) {
				return gameObject.dropEnable;
			}
		});
		this.on("drop", function(gameObject, e) {
			this._files = e.dataTransfer.files;
			var files = this._files;
			if (files && this.fileTypeFilters) for (var filterType in this.fileTypeFilters) {
				var filterCallback = this.fileTypeFilters[filterType];
				var filteredFiles = [];
				for (var i = 0, cnt = files.length; i < cnt; i++) {
					var file = files[i];
					if (filterCallback(file, files)) filteredFiles.push(file);
				}
				if (filteredFiles.length > 0) this.emit(`drop.${filterType}`, filteredFiles);
			}
		}, this);
	}
	get files() {
		return this._files;
	}
};
Object.assign(FileDropZone.prototype, Methods$13);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/filedropzone/FileDropZone.js
var FileDropZone_default = FileDropZone;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/filedropzone/Factory.js
ObjectFactory.register("fileDropZone", function(config) {
	var gameObject = new FileDropZone_default(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.FileDropZone", FileDropZone_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/GetChildrenWidth.js
var GetChildrenWidth$3 = function(minimumMode) {
	if (this.rexSizer.hidden) return 0;
	if (minimumMode === void 0) minimumMode = true;
	var result = 0;
	var children = this.sizerChildren;
	var child, sizerConfig, proportion, padding, childWidth;
	var hasUnknownChildWidth = false;
	this.childrenProportion;
	if (this.orientation === 0) {
		var isFirstChild = true;
		for (var i = 0, cnt = children.length; i < cnt; i++) {
			child = children[i];
			if (!child.hasOwnProperty("rexSizer")) continue;
			sizerConfig = child.rexSizer;
			if (sizerConfig.hidden) continue;
			proportion = sizerConfig.proportion;
			if (proportion === 0 || minimumMode) {
				childWidth = this.getChildWidth(child);
				if (sizerConfig.fitRatio > 0 && !sizerConfig.resolved) childWidth = void 0;
				if (childWidth === void 0) {
					if (proportion !== 0 && !this.hasProportion0Child) childWidth = 0;
					else hasUnknownChildWidth = true;
				}
			} else childWidth = 0;
			if (hasUnknownChildWidth) continue;
			padding = child.rexSizer.padding;
			childWidth += (padding.left + padding.right) * this.scaleX;
			if (isFirstChild) isFirstChild = false;
			else childWidth += this.space.item * this.scaleX;
			result += childWidth;
		}
	} else for (var i = 0, cnt = children.length; i < cnt; i++) {
		child = children[i];
		if (!child.hasOwnProperty("rexSizer")) continue;
		sizerConfig = child.rexSizer;
		if (sizerConfig.hidden) continue;
		childWidth = this.getChildWidth(child);
		if (childWidth === void 0) hasUnknownChildWidth = true;
		if (hasUnknownChildWidth) continue;
		padding = sizerConfig.padding;
		childWidth += (padding.left + padding.right) * this.scaleX;
		result = Math.max(childWidth, result);
	}
	if (hasUnknownChildWidth) return;
	return result + (this.space.left + this.space.right) * this.scaleX;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/GetChildrenHeight.js
var GetChildrenHeight$3 = function(minimumMode) {
	if (this.rexSizer.hidden) return 0;
	if (minimumMode === void 0) minimumMode = true;
	var result = 0;
	var children = this.sizerChildren;
	var child, sizerConfig, proportion, padding, childHeight;
	var hasUnknownChildHeight = false;
	this.childrenProportion;
	if (this.orientation === 0) for (var i = 0, cnt = children.length; i < cnt; i++) {
		child = children[i];
		if (!child.hasOwnProperty("rexSizer")) continue;
		sizerConfig = child.rexSizer;
		if (sizerConfig.hidden) continue;
		childHeight = this.getChildHeight(child);
		if (childHeight === void 0) hasUnknownChildHeight = true;
		if (hasUnknownChildHeight) continue;
		padding = sizerConfig.padding;
		childHeight += (padding.top + padding.bottom) * this.scaleY;
		result = Math.max(childHeight, result);
	}
	else {
		var isFirstChild = true;
		for (var i = 0, cnt = children.length; i < cnt; i++) {
			child = children[i];
			if (!child.hasOwnProperty("rexSizer")) continue;
			sizerConfig = child.rexSizer;
			if (sizerConfig.hidden) continue;
			proportion = sizerConfig.proportion;
			if (proportion === 0 || minimumMode) {
				childHeight = this.getChildHeight(child);
				if (sizerConfig.fitRatio > 0 && !sizerConfig.resolved) childHeight = void 0;
				if (childHeight === void 0) {
					if (proportion !== 0 && !this.hasProportion0Child) childHeight = 0;
					else hasUnknownChildHeight = true;
				}
			} else childHeight = 0;
			if (hasUnknownChildHeight) continue;
			padding = sizerConfig.padding;
			childHeight += (padding.top + padding.bottom) * this.scaleY;
			if (isFirstChild) isFirstChild = false;
			else childHeight += this.space.item * this.scaleY;
			result += childHeight;
		}
	}
	if (hasUnknownChildHeight) return;
	return result + (this.space.top + this.space.bottom) * this.scaleY;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/GetExpandedChildWidth.js
var GetExpandedChildWidth$2 = function(child, parentWidth) {
	if (parentWidth === void 0) parentWidth = this.width * this.scaleX;
	var childWidth;
	var sizerConfig = child.rexSizer;
	if (this.orientation === 0) {
		if (sizerConfig.proportion > 0 && this.proportionLength > 0) childWidth = sizerConfig.proportion * this.proportionLength;
	} else if (sizerConfig.expand) {
		var space = this.space;
		var innerWidth = parentWidth - (space.left + space.right) * this.scaleX;
		var padding = sizerConfig.padding;
		childWidth = innerWidth - (padding.left + padding.right) * this.scaleX;
	}
	return childWidth;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/GetExpandedChildHeight.js
var GetExpandedChildHeight$2 = function(child, parentHeight) {
	if (parentHeight === void 0) parentHeight = this.height;
	var childHeight;
	var sizerConfig = child.rexSizer;
	if (this.orientation === 0) {
		if (sizerConfig.expand) {
			var space = this.space;
			var innerHeight = parentHeight - (space.top + space.bottom) * this.scaleY;
			var padding = sizerConfig.padding;
			childHeight = innerHeight - (padding.top + padding.bottom) * this.scaleY;
		}
	} else if (sizerConfig.proportion > 0 && this.proportionLength > 0) childHeight = sizerConfig.proportion * this.proportionLength;
	return childHeight;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/GetChildrenSizers.js
var GetChildrenSizers$3 = function(out) {
	if (out === void 0) out = [];
	var children = this.sizerChildren, child;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		child = children[i];
		if (child.isRexSizer) out.push(child);
	}
	return out;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/PreLayout.js
var PreLayout$2 = function() {
	this.hasRatioFitChild = false;
	var child, sizerConfig;
	var children = this.sizerChildren;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		child = children[i];
		sizerConfig = child.rexSizer;
		if (sizerConfig.hidden) continue;
		if (sizerConfig.fitRatio > 0) {
			ResizeGameObject(child, 0, 0);
			sizerConfig.resolved = false;
			this.hasRatioFitChild = true;
		}
	}
	this._childrenProportion = void 0;
	this.hasProportion0Child = false;
	this.proportionLength = void 0;
	PreLayout$3.call(this);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/LayoutChildren.js
var Wrap$1 = __webpack_exports__Math.Wrap;
var LayoutChildren$3 = function() {
	var children = this.sizerChildren;
	var child, childConfig, padding;
	var startX = this.innerLeft, startY = this.innerTop;
	var innerWidth = this.innerWidth;
	var innerHeight = this.innerHeight;
	var itemX = startX, itemY = startY;
	var x, y, width, height, alignOffsetX, alignOffsetY;
	var childWidth, childHeight;
	var childIndex, startChildIndex = this.startChildIndex;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		if (startChildIndex === 0) childIndex = i;
		else childIndex = Wrap$1(i + startChildIndex, 0, cnt);
		if (this.rtl) childIndex = cnt - childIndex - 1;
		child = children[childIndex];
		if (child.rexSizer.hidden) continue;
		childConfig = child.rexSizer;
		padding = childConfig.padding;
		PreLayoutChild.call(this, child);
		if (child.isRexSpace) {
			childWidth = 0;
			childHeight = 0;
		} else {
			childWidth = this.getExpandedChildWidth(child);
			childHeight = this.getExpandedChildHeight(child);
		}
		if (child.isRexSizer) {
			child.runLayout(this, childWidth, childHeight);
			CheckSize(child, this);
		} else if (!childConfig.noResize) ResizeGameObject(child, childWidth, childHeight);
		if (childWidth === void 0) childWidth = GetDisplayWidth(child);
		if (childHeight === void 0) childHeight = GetDisplayHeight(child);
		if (this.orientation === 0) {
			x = itemX + padding.left * this.scaleX;
			if (childConfig.proportion === 0 || this.proportionLength === 0) width = childWidth;
			else width = childConfig.proportion * this.proportionLength;
			y = itemY + padding.top * this.scaleY;
			height = innerHeight - (padding.top + padding.bottom) * this.scaleY;
		} else {
			x = itemX + padding.left * this.scaleX;
			width = innerWidth - (padding.left + padding.right) * this.scaleX;
			y = itemY + padding.top * this.scaleY;
			if (childConfig.proportion === 0 || this.proportionLength === 0) height = childHeight;
			else height = childConfig.proportion * this.proportionLength;
		}
		if (childWidth === void 0) childWidth = GetDisplayWidth(child);
		if (childHeight === void 0) childHeight = GetDisplayHeight(child);
		alignOffsetX = (childConfig.alignOffsetX + childConfig.alignOffsetOriginX * childWidth) * this.scaleX;
		alignOffsetY = (childConfig.alignOffsetY + childConfig.alignOffsetOriginY * childHeight) * this.scaleY;
		LayoutChild.call(this, child, x, y, width, height, childConfig.align, alignOffsetX, alignOffsetY);
		if (this.orientation === 0) itemX += width + (padding.left + padding.right) * this.scaleX + this.space.item * this.scaleX;
		else itemY += height + (padding.top + padding.bottom) * this.scaleY + this.space.item * this.scaleY;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/ResolveWidth.js
var ResolveWidth$1 = function(width) {
	var width = ResolveWidth$3.call(this, width);
	if (width !== void 0 && this.orientation === 0 && this.proportionLength === void 0) {
		var remainder = width - this.childrenWidth;
		if (remainder > 0) {
			remainder = width - this.getChildrenWidth(false);
			this.proportionLength = remainder / this.childrenProportion;
		} else {
			this.proportionLength = 0;
			if (remainder < 0) {}
		}
	}
	return width;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/ResolveHeight.js
var ResolveHeight$1 = function(height) {
	var height = ResolveHeight$3.call(this, height);
	if (height !== void 0 && this.orientation === 1 && this.proportionLength === void 0) {
		var remainder = height - this.childrenHeight;
		if (remainder > 0) {
			remainder = height - this.getChildrenHeight(false);
			this.proportionLength = remainder / this.childrenProportion;
		} else {
			this.proportionLength = 0;
			if (remainder < 0) {}
		}
	}
	return height;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/HasWidthWrap.js
var HasWidthWrap$1 = function() {
	if (this.hasRatioFitChild && this.orientation === 1) return true;
	return HasWidthWrap$2.call(this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/ExpandFitRatioChildren.js
var ExpandFitRatioChildren = function(width, height) {
	if (!this.hasRatioFitChild) return;
	var innerHeight;
	if (this.orientation === 0) innerHeight = height - (this.getInnerPadding("top") + this.getInnerPadding("bottom")) * this.scaleY;
	else width - (this.getInnerPadding("left") + this.getInnerPadding("right")) * this.scaleX;
	var child, childConfig;
	var childWidth, childHeight;
	var children = this.sizerChildren;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		var childConfig = child.rexSizer;
		if (childConfig.hidden) continue;
		var fitRatio = childConfig.fitRatio;
		if (!fitRatio) continue;
		if (this.orientation === 0) {
			childHeight = innerHeight - (this.getChildOuterPadding(child, "top") + this.getChildOuterPadding(child, "bottom")) * this.scaleY;
			childWidth = childHeight * fitRatio;
		} else {
			childWidth = innerHeight - (this.getChildOuterPadding(child, "top") + this.getChildOuterPadding(child, "bottom")) * this.scaleX;
			childHeight = childWidth / fitRatio;
		}
		if (!childConfig.noResize) ResizeGameObject(child, childWidth, childHeight);
		if (child.isRexSizer) child.setMinSize(childWidth, childHeight);
		childConfig.resolved = true;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/RunWidthWrap.js
var RunWidthWrap$2 = function(width) {
	if (this.wrapResult) return;
	if (this.orientation === 1) ExpandFitRatioChildren.call(this, width, void 0);
	RunWidthWrap$3.call(this, width);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/HasHeightWrap.js
var HasHeightWrap$1 = function() {
	if (this.hasRatioFitChild && this.orientation === 0) return true;
	return HasHeightWrap$2.call(this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/RunHeightWrap.js
var RunHeightWrap$2 = function(height) {
	if (this.wrapResult) return;
	if (this.orientation === 0) ExpandFitRatioChildren.call(this, void 0, height);
	RunHeightWrap$3.call(this, height);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/space/Space.js
var Zone = __webpack_exports__GameObjects.Zone;
var Space = class extends Zone {
	constructor(scene) {
		super(scene, 0, 0, 1, 1);
		this.isRexSpace = true;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/GetNearestChildIndex.js
var GetNearestChildIndex$1 = function(x, y) {
	var children = this.sizerChildren;
	if (children.length === 0) return -1;
	var nearestIndex = -1, minDistance = Infinity;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		var distance;
		if (this.orientation === 0) distance = Math.abs(child.left - x);
		else distance = Math.abs(child.top - y);
		if (minDistance > distance) {
			minDistance = distance;
			nearestIndex = i;
		}
	}
	var child = children[children.length - 1];
	var distance;
	if (this.orientation === 0) distance = Math.abs(child.right - x);
	else distance = Math.abs(child.bottom - y);
	if (minDistance > distance) {
		minDistance = distance;
		nearestIndex = i + 1;
	}
	return nearestIndex;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/AddChildMethods.js
var IsPlainObject$23 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$161 = __webpack_exports__Utils.Objects.GetValue;
var ALIGN_CENTER$2 = __webpack_exports__Display.Align.CENTER;
var PROPORTIONMODE = {
	min: 0,
	full: -1
};
var Add$6 = function(gameObject, proportion, align, paddingConfig, expand, childKey, index, minWidth, minHeight, fitRatio) {
	var offsetX, offsetY;
	var offsetOriginX, offsetOriginY;
	AddChild$1.call(this, gameObject);
	var isRexSpace = gameObject.isRexSpace;
	var proportionType = typeof proportion;
	if (proportion === null) return this;
	else if (proportionType === "number") {} else if (proportionType === "string") proportion = PROPORTIONMODE[proportion];
	else if (IsPlainObject$23(proportion)) {
		var config = proportion;
		proportion = GetValue$161(config, "proportion", void 0);
		align = GetValue$161(config, "align", ALIGN_CENTER$2);
		paddingConfig = GetValue$161(config, "padding", 0);
		expand = GetValue$161(config, "expand", false);
		childKey = GetValue$161(config, "key", void 0);
		index = GetValue$161(config, "index", void 0);
		if (!gameObject.isRexSizer) {
			minWidth = GetValue$161(config, "minWidth", void 0);
			minHeight = GetValue$161(config, "minHeight", void 0);
		}
		fitRatio = GetValue$161(config, "fitRatio", 0);
		offsetX = GetValue$161(config, "offsetX", 0);
		offsetY = GetValue$161(config, "offsetY", 0);
		offsetOriginX = GetValue$161(config, "offsetOriginX", 0);
		offsetOriginY = GetValue$161(config, "offsetOriginY", 0);
	}
	if (typeof align === "string") align = AlignConst_default[align];
	if (proportion === void 0) proportion = isRexSpace ? 1 : 0;
	if (align === void 0) align = ALIGN_CENTER$2;
	if (paddingConfig === void 0) paddingConfig = 0;
	if (expand === void 0) expand = false;
	if (minWidth === void 0) {
		if (isRexSpace) minWidth = 0;
		else if (!gameObject.isRexSizer) minWidth = gameObject._minWidth;
	}
	if (minHeight === void 0) {
		if (isRexSpace) minHeight = 0;
		else if (!gameObject.isRexSizer) minHeight = gameObject._minHeight;
	}
	if (fitRatio === void 0 || fitRatio === false) fitRatio = 0;
	else if (fitRatio === true) fitRatio = GetDisplayWidth(gameObject) / GetDisplayHeight(gameObject);
	if (offsetX === void 0) offsetX = 0;
	if (offsetY === void 0) offsetY = 0;
	if (offsetOriginX === void 0) offsetOriginX = 0;
	if (offsetOriginY === void 0) offsetOriginY = 0;
	var config = this.getSizerConfig(gameObject);
	config.proportion = proportion;
	config.align = align;
	config.padding = GetBoundsConfig_default(paddingConfig);
	config.expand = expand;
	config.fitRatio = proportion === 0 ? fitRatio : 0;
	config.alignOffsetX = offsetX;
	config.alignOffsetY = offsetY;
	config.alignOffsetOriginX = offsetOriginX;
	config.alignOffsetOriginY = offsetOriginY;
	if (index === void 0 || index >= this.sizerChildren.length) this.sizerChildren.push(gameObject);
	else this.sizerChildren.splice(index, 0, gameObject);
	if (!gameObject.isRexSizer) {
		if (proportion > 0) {
			if (this.orientation === 0) gameObject.minWidth = minWidth === void 0 ? GetDisplayWidth(gameObject) : minWidth;
			else gameObject.minHeight = minHeight === void 0 ? GetDisplayHeight(gameObject) : minHeight;
		}
		if (expand) {
			if (this.orientation === 0) gameObject.minHeight = minHeight;
			else gameObject.minWidth = minWidth;
		}
	}
	if (childKey !== void 0) this.addChildrenMap(childKey, gameObject);
	return this;
};
var AddChildMethods_default$6 = {
	add: Add$6,
	addSpace(proportion) {
		this.insertSpace(void 0, proportion);
		return this;
	},
	insertSpace(index, proportion) {
		if (proportion === void 0) proportion = 1;
		Add$6.call(this, new Space(this.scene), {
			proportion,
			minWidth: 0,
			minHeight: 0,
			index
		});
		return this;
	},
	insert(index, gameObject, proportion, align, paddingConfig, expand, childKey, minSize) {
		if (IsPlainObject$23(proportion)) proportion.index = index;
		Add$6.call(this, gameObject, proportion, align, paddingConfig, expand, childKey, index, minSize);
		return this;
	},
	insertAtPosition(x, y, gameObject, proportion, align, paddingConfig, expand, childKey, minSize) {
		var index = GetNearestChildIndex$1.call(this, x, y);
		if (index === -1) index = void 0;
		this.insert(index, gameObject, proportion, align, paddingConfig, expand, childKey, minSize);
		return this;
	},
	insertAfter(baseGameObject, gameObject, proportion, align, paddingConfig, expand, childKey, minSize) {
		var index = this.sizerChildren.indexOf(baseGameObject);
		if (index === -1) index = this.sizerChildren.length - 1;
		this.insert(index + 1, gameObject, proportion, align, paddingConfig, expand, childKey, minSize);
		return this;
	},
	insertBefore(baseGameObject, gameObject, proportion, align, paddingConfig, expand, childKey, minSize) {
		var index = this.sizerChildren.indexOf(baseGameObject);
		if (index === -1) index = this.sizerChildren.length - 1;
		this.insert(index, gameObject, proportion, align, paddingConfig, expand, childKey, minSize);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/RemoveChildMethods.js
var RemoveItem$2 = __webpack_exports__Utils.Array.Remove;
var RemoveChildMethods_default$5 = {
	remove(gameObject, destroyChild) {
		if (this.getParentSizer(gameObject) !== this) return this;
		RemoveItem$2(this.sizerChildren, gameObject);
		RemoveChild.call(this, gameObject, destroyChild);
		return this;
	},
	removeAll(destroyChild) {
		for (var i = this.sizerChildren.length - 1; i >= 0; i--) this.remove(this.sizerChildren[i], destroyChild);
		return this;
	},
	clear(destroyChild) {
		this.sizerChildren.length = 0;
		ClearChildren.call(this, destroyChild);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/AlignMethods.js
var AlignMethods_default = {
	getChildAlign(gameObject) {
		return this.getSizerConfig(gameObject).align;
	},
	setChildAlign(gameObject, align) {
		if (typeof align === "string") align = AlignConst_default[align];
		this.getSizerConfig(gameObject).align = align;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/ProportionMethods.js
var ProportionMethods_default = {
	getChildProportion(gameObject) {
		return this.getSizerConfig(gameObject).proportion;
	},
	setChildProportion(gameObject, proportion) {
		this.getSizerConfig(gameObject).proportion = proportion;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/ExpandMethods.js
var ExpandMethods_default$1 = {
	getChildExpand(gameObject) {
		return this.getSizerConfig(gameObject).expand;
	},
	setChildExpand(gameObject, expand) {
		this.getSizerConfig(gameObject).expand = expand;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/SetChildrenAlignMode.js
var SetChildrenAlignMode = function(mode) {
	if (mode === void 0) mode = "left";
	var children = this.sizerChildren;
	var firstChild = children[0];
	var isFirstChildASpace = firstChild && firstChild.isRexSpace;
	if (mode === "right" || mode === "bottom" || mode === "center") {
		if (!isFirstChildASpace) this.insertSpace(0);
	} else if (isFirstChildASpace) this.remove(firstChild, true);
	var lastChildIndex = children.length - 1;
	var lastChild = children[lastChildIndex];
	var isLastChildASpace = lastChild && lastChild.isRexSpace;
	if (mode === "center") {
		if (!isLastChildASpace) this.insertSpace(lastChildIndex + 1);
	} else if (isLastChildASpace) this.remove(lastChild, true);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/basesizer/utils/SortChildrenMethods.js
var SortChildrenMethods_default = {
	sortChildren(callback) {
		this.sizerChildren.sort(callback);
		return this;
	},
	sortChildrenByData(key, descending) {
		this.sizerChildren.sort(function(childA, childB) {
			var valueA = childA.getData(key);
			var valueB = childB.getData(key);
			if (descending) return valueB - valueA;
			else return valueA - valueB;
		});
		return this;
	},
	sortChildrenByProperty(key, descending) {
		this.sizerChildren.sort(function(childA, childB) {
			var valueA = childA[key];
			var valueB = childB[key];
			if (descending) return valueB - valueA;
			else return valueA - valueB;
		});
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/Methods.js
var methods$24 = {
	getChildrenWidth: GetChildrenWidth$3,
	getChildrenHeight: GetChildrenHeight$3,
	getExpandedChildWidth: GetExpandedChildWidth$2,
	getExpandedChildHeight: GetExpandedChildHeight$2,
	getChildrenSizers: GetChildrenSizers$3,
	preLayout: PreLayout$2,
	layoutChildren: LayoutChildren$3,
	resolveWidth: ResolveWidth$1,
	resolveHeight: ResolveHeight$1,
	hasWidthWrap: HasWidthWrap$1,
	runWidthWrap: RunWidthWrap$2,
	hasHeightWrap: HasHeightWrap$1,
	runHeightWrap: RunHeightWrap$2,
	setChildrenAlignMode: SetChildrenAlignMode
};
Object.assign(methods$24, AddChildMethods_default$6, RemoveChildMethods_default$5, AlignMethods_default, ProportionMethods_default, ExpandMethods_default$1, SortChildrenMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/GetChildrenProportion.js
var GetChildrenProportion = function() {
	var result = 0;
	var children = this.sizerChildren;
	var child, proportion;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		child = children[i];
		if (child.rexSizer.hidden) continue;
		proportion = child.rexSizer.proportion;
		if (proportion > 0) result += proportion;
		else if (proportion === 0) this.hasProportion0Child = true;
	}
	return result;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/GetOrientationMode.js
var GetOrientationMode_default = GetOrientationMode;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/Sizer.js
var IsPlainObject$22 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$160 = __webpack_exports__Utils.Objects.GetValue;
var Sizer = class extends Base$3 {
	constructor(scene, x, y, minWidth, minHeight, orientation, config) {
		if (IsPlainObject$22(x)) {
			config = x;
			x = GetValue$160(config, "x", 0);
			y = GetValue$160(config, "y", 0);
			minWidth = GetValue$160(config, "width", void 0);
			minHeight = GetValue$160(config, "height", void 0);
			orientation = GetValue$160(config, "orientation", 0);
		} else if (IsPlainObject$22(minWidth)) {
			config = minWidth;
			minWidth = GetValue$160(config, "width", void 0);
			minHeight = GetValue$160(config, "height", void 0);
			orientation = GetValue$160(config, "orientation", 0);
		} else if (IsPlainObject$22(orientation)) {
			config = orientation;
			orientation = GetValue$160(config, "orientation", 0);
		}
		if (orientation === void 0) orientation = 0;
		super(scene, x, y, minWidth, minHeight, config);
		this.type = "rexSizer";
		this.sizerChildren = [];
		this.setOrientation(orientation);
		this.setItemSpacing(GetValue$160(config, "space.item", 0));
		this.setStartChildIndex(GetValue$160(config, "startChildIndex", 0));
		this.setRTL(GetValue$160(config, "rtl", false));
		this.addChildrenMap("items", this.sizerChildren);
	}
	setOrientation(orientation) {
		this.orientation = GetOrientationMode_default(orientation);
		return this;
	}
	setItemSpacing(space) {
		this.space.item = space;
		return this;
	}
	setStartChildIndex(index) {
		this.startChildIndex = index;
		return this;
	}
	setRTL(enable) {
		if (enable === void 0) enable = true;
		this.rtl = enable;
		return this;
	}
	get childrenProportion() {
		if (this._childrenProportion === void 0) this._childrenProportion = GetChildrenProportion.call(this);
		return this._childrenProportion;
	}
};
Object.assign(Sizer.prototype, methods$24);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/size/SetDisplaySize.js
var SetDisplaySize = function(gameObject, width, height) {
	if (!gameObject) return;
	var unknownWidth = width == null;
	var unknownHeight = height == null;
	if (unknownWidth && unknownHeight) return gameObject;
	if (!unknownWidth) gameObject.displayWidth = width;
	if (!unknownHeight) gameObject.displayHeight = height;
	if (unknownWidth) gameObject.scaleX = gameObject.scaleY;
	if (unknownHeight) gameObject.scaleY = gameObject.scaleX;
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/label/methods/ResetDisplayContent.js
var ResetDisplayContent$2 = function(config) {
	if (config === void 0) config = {};
	else if (typeof config === "string") config = { text: config };
	var text = config.text || "";
	this.setText(text);
	var iconGameObjct = this.childrenMap.icon;
	if (iconGameObjct) {
		if (!config.icon) this.hide(iconGameObjct);
		else this.show(iconGameObjct);
		var iconSize = config.iconSize;
		if (iconSize) {
			this.setChildDisplaySize(iconGameObjct, iconSize, iconSize);
			if (this.iconWidth !== void 0) this.setIconSize(iconSize);
		}
		if (config.icon !== true) this.setIconTexture(config.icon, config.iconFrame);
	}
	var actionGameObjct = this.childrenMap.action;
	if (actionGameObjct) {
		if (!config.action) this.hide(actionGameObjct);
		else this.show(actionGameObjct);
		var actionSize = config.actionSize;
		if (actionSize) {
			this.setChildDisplaySize(actionGameObjct, actionSize, actionSize);
			if (this.actionWidth !== void 0) this.setActionSize(actionSize);
		}
		if (config.action !== true) this.setActionTexture(config.action, config.actionFrame);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/label/methods/Methods.js
var methods$23 = {
	appendText: AppendText$1,
	resetDisplayContent: ResetDisplayContent$2
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/label/Base.js
var LabelBase = class extends Sizer {
	get text() {
		var textObject = this.childrenMap.text;
		if (!textObject) return "";
		return textObject.text;
	}
	set text(value) {
		var textObject = this.childrenMap.text;
		if (!textObject) return;
		textObject.setText(value);
	}
	setText(value) {
		this.text = value;
		return this;
	}
	setIconTexture(key, frame) {
		var imageObject = this.childrenMap.icon;
		if (!imageObject || !imageObject.setTexture) return this;
		imageObject.setTexture(key, frame);
		if (this.iconWidth !== void 0 && this.iconHeight !== void 0) {
			SetDisplaySize(imageObject, this.iconWidth, this.iconHeight);
			this.resetChildScaleState(imageObject);
		}
		return this;
	}
	setTexture(key, frame) {
		this.setIconTexture(key, frame);
		return this;
	}
	setIconSize(width, height) {
		if (height === void 0) height = width;
		this.iconWidth = width;
		this.iconHeight = height;
		return this;
	}
	get texture() {
		var imageObject = this.childrenMap.icon;
		if (!imageObject) return;
		return imageObject.texture;
	}
	get frame() {
		var imageObject = this.childrenMap.icon;
		if (!imageObject) return;
		return imageObject.frame;
	}
	setActionTexture(key, frame) {
		var imageObject = this.childrenMap.action;
		if (!imageObject || !imageObject.setTexture) return this;
		imageObject.setTexture(key, frame);
		if (this.actionWidth !== void 0 && this.actionHeight !== void 0) {
			SetDisplaySize(imageObject, this.actionWidth, this.actionHeight);
			this.resetChildScaleState(imageObject);
		}
		return this;
	}
	get actionTexture() {
		var imageObject = this.childrenMap.action;
		if (!imageObject) return;
		return imageObject.texture;
	}
	get actionFrame() {
		var imageObject = this.childrenMap.action;
		if (!imageObject) return;
		return imageObject.frame;
	}
	setActionSize(width, height) {
		if (height === void 0) height = width;
		this.actionWidth = width;
		this.actionHeight = height;
		return this;
	}
	preLayout() {
		var icon = this.childrenMap.icon;
		if (icon && this.iconWidth !== void 0 && this.iconHeight !== void 0) SetDisplaySize(icon, this.iconWidth, this.iconHeight);
		var action = this.childrenMap.action;
		if (action && this.actionWidth !== void 0 && this.actionHeight !== void 0) SetDisplaySize(action, this.actionWidth, this.actionHeight);
		super.preLayout();
	}
	postLayout(parent, newWidth, newHeight) {
		var iconMask = this.childrenMap.iconMask;
		if (iconMask) {
			iconMask.setPosition();
			this.resetChildPositionState(iconMask);
		}
		var actionMask = this.childrenMap.actionMask;
		if (actionMask) {
			actionMask.setPosition();
			this.resetChildPositionState(actionMask);
		}
		super.postLayout(parent, newWidth, newHeight);
		return this;
	}
	resize(width, height) {
		super.resize(width, height);
		var iconMask = this.childrenMap.iconMask;
		if (iconMask) iconMask.resize();
		var actionMask = this.childrenMap.actionMask;
		if (actionMask) actionMask.resize();
		return this;
	}
};
Object.assign(LabelBase.prototype, methods$23);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/mask/defaultmaskgameobject/methods/SetPosition.js
var SetPosition$1 = function(x, y) {
	var parent = this.parent;
	if (x === void 0) x = parent.x;
	if (y === void 0) y = parent.y;
	this._maskX = x;
	this._maskY = y;
	if (this._updateMaskPosition) this._updateMaskPosition();
	else {
		this.x = x;
		this.y = y;
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/mask/defaultmaskgameobject/methods/Resize.js
var Resize = function(width, height, padding) {
	var parent = this.parent;
	if (width === void 0) width = parent.width;
	if (height === void 0) height = parent.height;
	if (padding === void 0) padding = this.padding;
	else if (typeof padding === "number") padding = GetBoundsConfig(padding);
	var isSizeChanged = this._maskWidth !== width || this._maskHeight !== height;
	var isPaddingChanged = this.padding !== padding && !IsKeyValueEqual(this.padding, padding);
	if (!isSizeChanged && !isPaddingChanged) return this;
	this._maskWidth = width;
	this._maskHeight = height;
	if (isPaddingChanged) Clone$2(padding, this.padding);
	this._maskOriginX = parent.originX;
	this._maskOriginY = parent.originY;
	this._updateMaskGeometry();
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/mask/defaultmaskgameobject/methods/SetOrigin.js
var SetOrigin = function(originX, originY) {
	if (originY === void 0) originY = originX;
	var parent = this.parent;
	if (originX === void 0) originX = parent.originX;
	if (originY === void 0) originY = parent.originY;
	if (this._maskOriginX === originX && this._maskOriginY === originY) return this;
	this._maskOriginX = originX;
	this._maskOriginY = originY;
	this._updateMaskGeometry();
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/mask/defaultmaskgameobject/methods/Contains.js
var Contains$1 = function(x, y) {
	var localPoint = this.getLocalPoint(x, y, this._maskLocalPoint);
	this._maskLocalPoint = localPoint;
	return this.geom.contains(localPoint.x, localPoint.y);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/mask/defaultmaskgameobject/methods/Methods.js
var Methods$12 = {
	setPosition: SetPosition$1,
	resize: Resize,
	setOrigin: SetOrigin,
	contains: Contains$1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/mask/GetMaskFilterViewTransformByScaleFactor.js
var TransformMatrix$1 = __webpack_exports__GameObjects.Components.TransformMatrix;
var GetMaskFilterViewTransformByScaleFactor = function(maskGameObject, scaleFactor, maskType, viewTransform) {
	if (scaleFactor === 1) return maskType;
	if (viewTransform === void 0) viewTransform = new TransformMatrix$1();
	var scale = 1 / scaleFactor;
	viewTransform.applyITRS(0, 0, 0, scale, scale);
	if (maskType === "world" && maskGameObject.parentContainer) viewTransform.multiply(maskGameObject.parentContainer.getWorldTransformMatrix(ParentTransform), viewTransform);
	return viewTransform;
};
var ParentTransform = new TransformMatrix$1();
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/system/GetRenderer.js
var GetRenderer = function(scene) {
	scene = GetSceneObject(scene);
	if (!scene) return null;
	return scene.sys.renderer;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/system/IsWebGLRenderMode.js
var IsWebGLRenderMode = function(scene) {
	var renderer = GetRenderer(scene);
	if (!renderer) return false;
	return !!renderer.gl;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/mask/defaultmaskgameobject/RectangleMask.js
var Base$1 = __webpack_exports__GameObjects.Rectangle;
var TransformMatrix = __webpack_exports__GameObjects.Components.TransformMatrix;
var MaxMaskSize = 256;
var GetAutoMaskScaleFactor = function(width, height) {
	var maxSize = Math.max(width, height);
	var scaleFactor = 1;
	while (maxSize / scaleFactor > MaxMaskSize) scaleFactor *= 2;
	return scaleFactor;
};
var RectangleMask = class extends Base$1 {
	constructor(parent, padding) {
		super(parent.scene, 0, 0, 1, 1, 16777215, 1);
		this.parent = parent;
		this.padding = GetBoundsConfig(padding);
		this.useMaskScaleFactor = IsWebGLRenderMode(parent.scene);
		this.maskScaleFactor = 1;
		this._maskFilterViewTransform = new TransformMatrix();
		this._maskFilterController = void 0;
		this._maskFilterType = void 0;
		this.setPosition().resize().setVisible(false);
		if (parent.parentContainer) parent.parentContainer.add(this);
		else parent.scene.add.existing(this);
	}
	_updateMaskGeometry() {
		this._updateMaskScaleFactor();
		this._updateMaskPosition();
		var width = this._maskWidth;
		var height = this._maskHeight;
		var padding = this.padding;
		var originX = this._maskOriginX;
		var originY = this._maskOriginY;
		var displayOriginX = width * originX + padding.left;
		var displayOriginY = height * originY + padding.top;
		this.setSize(width + padding.left + padding.right, height + padding.top + padding.bottom);
		this.originX = originX;
		this.originY = originY;
		this._displayOriginX = displayOriginX;
		this._displayOriginY = displayOriginY;
		return this;
	}
	_updateMaskPosition() {
		this.x = this._maskX;
		this.y = this._maskY;
		this.setScale(1);
		return this;
	}
	_updateMaskScaleFactor() {
		var padding = this.padding;
		if (this._maskWidth === void 0 || this._maskHeight === void 0) {
			this.maskScaleFactor = 1;
			return this;
		}
		var width = this._maskWidth + padding.left + padding.right;
		var height = this._maskHeight + padding.top + padding.bottom;
		this.maskScaleFactor = this.useMaskScaleFactor ? GetAutoMaskScaleFactor(width, height) : 1;
		this._syncMaskFilterController(this._maskFilterController, this._maskFilterType);
		return this;
	}
	getMaskFilterScaleFactor() {
		return 1 / this.maskScaleFactor;
	}
	_syncMaskFilter(maskObject, maskType) {
		if (!maskObject || !maskObject.updateDynamicTexture) return this;
		maskObject._rexDefaultMaskGameObjectType = maskType;
		this._maskFilterController = maskObject;
		this._maskFilterType = maskType;
		this._installMaskFilterSync(maskObject);
		this._syncMaskFilterController(maskObject, maskType);
		return this;
	}
	_syncMaskFilterController(maskObject, maskType) {
		if (!maskObject || maskObject.maskGameObject !== this) {
			if (this._maskFilterController === maskObject) {
				this._maskFilterController = void 0;
				this._maskFilterType = void 0;
			}
			return this;
		}
		var scaleFactor = this.getMaskFilterScaleFactor();
		if (maskObject.scaleFactor !== scaleFactor) maskObject.scaleFactor = scaleFactor;
		var normalizeMaskType = maskType === "local" || maskType === "world" ? maskType : "world";
		maskObject.viewTransform = GetMaskFilterViewTransformByScaleFactor(this, this.maskScaleFactor, normalizeMaskType, this._maskFilterViewTransform);
		maskObject.needsUpdate = true;
		return this;
	}
	_installMaskFilterSync(maskObject) {
		if (maskObject._rexDefaultMaskGameObjectUpdateDynamicTexture) return this;
		var updateDynamicTexture = maskObject.updateDynamicTexture;
		maskObject.updateDynamicTexture = function(width, height) {
			var maskGameObject = this.maskGameObject;
			if (maskGameObject && maskGameObject._syncMaskFilterController) maskGameObject._syncMaskFilterController(this, this._rexDefaultMaskGameObjectType);
			updateDynamicTexture.call(this, width, height);
		};
		maskObject._rexDefaultMaskGameObjectUpdateDynamicTexture = updateDynamicTexture;
		return this;
	}
	destroy(fromScene) {
		this.parent = void 0;
		this._maskFilterController = void 0;
		this._maskFilterType = void 0;
		this._maskFilterViewTransform.destroy();
		super.destroy(fromScene);
		return this;
	}
};
Object.assign(RectangleMask.prototype, Methods$12);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/mask/defaultmaskgameobject/CircleMask.js
var Base = __webpack_exports__GameObjects.Arc;
var CircleMask = class extends Base {
	constructor(parent, padding) {
		super(parent.scene, 0, 0, 1, 0, 360, false, 16777215, 1);
		this.parent = parent;
		this.padding = GetBoundsConfig(padding);
		this.setPosition().resize().setVisible(false);
		if (parent.parentContainer) parent.parentContainer.add(this);
		else parent.scene.add.existing(this);
	}
	_updateMaskGeometry() {
		var width = this._maskWidth;
		var height = this._maskHeight;
		var padding = this.padding;
		var originX = this._maskOriginX;
		var originY = this._maskOriginY;
		var radius = Math.min(width, height) / 2 + padding.left;
		var diameter = radius * 2;
		var displayOriginX = radius + width * (originX - .5);
		var displayOriginY = radius + height * (originY - .5);
		this.setRadius(radius);
		this.geom.setTo(radius, radius, radius);
		this.originX = diameter === 0 ? .5 : displayOriginX / diameter;
		this.originY = diameter === 0 ? .5 : displayOriginY / diameter;
		this._displayOriginX = displayOriginX;
		this._displayOriginY = displayOriginY;
		return this;
	}
	destroy(fromScene) {
		this.parent = void 0;
		super.destroy(fromScene);
		return this;
	}
};
Object.assign(CircleMask.prototype, Methods$12);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/mask/defaultmaskgameobject/CreateDefaultMaskGameObject.js
var CreateDefaultMaskGameObject = function(parent, shapeType, padding) {
	if (shapeType === void 0) shapeType = 0;
	var MaskGameObjectClass;
	switch (shapeType) {
		case 0:
		case "rectangle":
			MaskGameObjectClass = RectangleMask;
			break;
		case 1:
		case "circle":
			MaskGameObjectClass = CircleMask;
			break;
		default: MaskGameObjectClass = RectangleMask;
	}
	return new MaskGameObjectClass(parent, padding);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/mask/MaskMethods.js
var MaskController = __webpack_exports__Filters.Mask;
var SetMask = function(gameObject, maskGameObject, invert, maskType) {
	if (IsWebGLRenderMode(gameObject)) {
		if (maskType === void 0) maskType = "shared";
		maskGameObject._maskType = maskType;
		switch (maskType) {
			case "local":
			case "world":
				WebGLSetPrivateMask(gameObject, maskGameObject, invert, maskType);
				break;
			default: WebGLSetSharedMask(gameObject, maskGameObject, invert);
		}
	} else CanvasSetMask(gameObject, maskGameObject);
};
var SyncMaskFilter = function(maskGameObject, maskObject, maskType) {
	if (maskGameObject._syncMaskFilter) maskGameObject._syncMaskFilter(maskObject, maskType);
};
var WarnInvalidMaskFilterUsage = function(message) {
	if (typeof console !== "undefined" && console.warn) console.warn(message);
};
var AssignMaskObject = function(maskGameObject, maskObject, maskObjectType, maskTarget, maskType) {
	maskGameObject._maskObject = maskObject;
	maskGameObject._maskObjectType = maskObjectType;
	maskGameObject._maskTarget = maskTarget;
	if (maskObject) maskObject._maskType = maskType;
};
var ClearAssignedMaskObject = function(maskGameObject, maskObject) {
	if (maskGameObject._maskObject !== maskObject) return;
	maskGameObject._maskObject = void 0;
	maskGameObject._maskObjectType = void 0;
	maskGameObject._maskTarget = void 0;
};
var WebGLSetSharedMask = function(gameObject, maskGameObject, invert) {
	var maskObject = maskGameObject._maskObject;
	if (!maskObject) {
		maskObject = new MaskController(maskGameObject.scene.cameras.main, maskGameObject, invert);
		maskObject.ignoreDestroy = true;
		AssignMaskObject(maskGameObject, maskObject, "shared", void 0, maskGameObject._maskType);
		SyncMaskFilter(maskGameObject, maskObject, maskGameObject._maskType);
		maskGameObject.once("destroy", function() {
			maskObject.destroy();
			ClearAssignedMaskObject(maskGameObject, maskObject);
		});
	} else {
		if (maskGameObject._maskObjectType !== "shared") {
			WarnInvalidMaskFilterUsage("MaskMethods: this mask game object is already used by a private mask filter.");
			return;
		}
		if (invert !== void 0 && maskObject.invert !== void 0) maskObject.invert = invert;
		AssignMaskObject(maskGameObject, maskObject, "shared", void 0, maskGameObject._maskType);
		SyncMaskFilter(maskGameObject, maskObject, maskGameObject._maskType);
	}
	if (gameObject.mask === maskObject) return;
	if (!gameObject.filters) {
		if (!gameObject.enableFilters) return;
		gameObject.enableFilters();
	}
	var list = gameObject.filters.external.list;
	if (gameObject.mask) {
		var index = list.indexOf(gameObject.mask);
		list[index] = maskObject;
	} else list.push(maskObject);
	gameObject.mask = maskObject;
};
var WebGLSetPrivateMask = function(gameObject, maskGameObject, invert, maskType) {
	if (!gameObject.filters) {
		if (!gameObject.enableFilters) return;
		gameObject.enableFilters();
	}
	if (gameObject.mask) ClearMask(gameObject);
	var filtersList = maskType === "local" ? gameObject.filters.internal : gameObject.filters.external;
	if (maskGameObject._maskObject) {
		if (maskGameObject._maskObjectType === "shared") {
			WarnInvalidMaskFilterUsage("MaskMethods: this mask game object is already used by a shared mask filter.");
			return;
		}
		if (maskGameObject._maskTarget !== gameObject) {
			WarnInvalidMaskFilterUsage("MaskMethods: a private mask filter can only be used by one target game object.");
			return;
		}
	}
	var maskObject = filtersList.addMask(maskGameObject, invert, void 0, maskType);
	AssignMaskObject(maskGameObject, maskObject, maskType, gameObject, maskType);
	SyncMaskFilter(maskGameObject, maskObject, maskType);
	gameObject.mask = maskObject;
};
var CanvasSetMask = function(gameObject, maskGameObject) {
	var maskObject = maskGameObject._maskObject;
	if (!maskObject) {
		maskObject = maskGameObject.createGeometryMask();
		AssignMaskObject(maskGameObject, maskObject, "canvas", void 0, void 0);
		maskGameObject.once("destroy", function() {
			maskObject.destroy();
			ClearAssignedMaskObject(maskGameObject, maskObject);
		});
	}
	gameObject.mask = maskObject;
};
var ClearMask = function(gameObject) {
	if (!gameObject.mask) return;
	if (IsWebGLRenderMode(gameObject)) {
		var maskType = gameObject.mask._maskType || gameObject.mask.maskGameObject._maskType;
		switch (maskType) {
			case "local":
			case "world":
				WebGLClearPrivateMask(gameObject, maskType);
				break;
			default: WebglClearSharedMask(gameObject);
		}
	} else CanvasClearMask(gameObject);
};
var WebglClearSharedMask = function(gameObject) {
	if (!gameObject.mask) return;
	gameObject.filters.external.remove(gameObject.mask, false);
	gameObject.mask = null;
};
var WebGLClearPrivateMask = function(gameObject, maskType) {
	if (!gameObject.mask) return;
	var maskObject = gameObject.mask;
	var maskGameObject = maskObject.maskGameObject;
	var filtersList = maskType === "local" ? gameObject.filters.internal : gameObject.filters.external;
	ClearAssignedMaskObject(maskGameObject, maskObject);
	filtersList.remove(maskObject, true);
	gameObject.mask = null;
};
var CanvasClearMask = function(gameObject) {
	gameObject.mask = null;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/mask/AddChildMask.js
var AddChildMask = function(maskTarget, sizeTarget, shape, padding) {
	var maskGameObject = CreateDefaultMaskGameObject(sizeTarget, shape, padding);
	if (maskTarget && !maskTarget.isRexSizer) SetMask(maskTarget, maskGameObject);
	this.pin(maskGameObject);
	return maskGameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/AddChildCircleMask.js
var AddChildCircleMask = function(parent, child) {
	if (IsWebGLRenderMode(parent)) {
		RegisterFilter(parent, CircleFilter);
		child.enableFilters().focusFilters();
		var filterList = child.filters.internal;
		filterList.add(new CircleController(filterList.camera, { thickness: 0 }));
		child.filtersFocusContext = false;
	} else return AddChildMask.call(parent, child, child, 1);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/text/IsTextGameObject.js
var TextClass = __webpack_exports__GameObjects.Text;
var IsTextGameObject = function(gameObject) {
	return gameObject instanceof TextClass;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/bitmaptext/IsBitmapTextGameObject.js
var BitmapTextClass = __webpack_exports__GameObjects.BitmapText;
var IsBitmapTextGameObject = function(gameObject) {
	return gameObject instanceof BitmapTextClass;
};
var GetTextObjectType = function(textObject) {
	var textObjectType;
	if (IsBitmapTextGameObject(textObject)) textObjectType = 2;
	else if (IsTextGameObject(textObject)) textObjectType = 0;
	else textObjectType = 1;
	return textObjectType;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/text/TextWrapByCharCallback.js
var TextWrapByCharCallback = function(text, textObject) {
	var output = [];
	var textLines = text.split("\n");
	var style = textObject.style;
	var wrapWidth = style.wordWrapWidth;
	var wrapMode = style.hasOwnProperty("wrapMode") ? style.wrapMode : 3;
	var context = textObject.context;
	for (var i = 0, cnt = textLines.length; i < cnt; i++) WrapLine(context, textLines[i], wrapWidth, wrapMode, output);
	return output;
};
var GetTokenArray = function(text, wrapMode) {
	var tokenArray;
	if (wrapMode === 2) tokenArray = text.split("");
	else {
		tokenArray = [];
		var words = text.split(" "), word;
		for (var i = 0, wordCount = words.length; i < wordCount; i++) {
			word = words[i];
			if (i < wordCount - 1) {
				if (IsASCIIString(word)) tokenArray.push(word + " ");
				else {
					tokenArray.push(...word.split(""));
					tokenArray.push(" ");
				}
			} else if (word !== "") {
				if (IsASCIIString(word)) tokenArray.push(word);
				else tokenArray.push(...word.split(""));
			}
		}
	}
	return tokenArray;
};
var WrapLine = function(context, text, wrapWidth, wrapMode, output) {
	if (text.length <= 100) {
		if (context.measureText(text).width <= wrapWidth) {
			output.push(text);
			return output;
		}
	}
	var tokenArray = GetTokenArray(text, wrapMode);
	var token, tokenWidth;
	var line = [], remainderLineWidth = wrapWidth;
	for (var j = 0, tokenLen = tokenArray.length; j < tokenLen; j++) {
		token = tokenArray[j];
		tokenWidth = context.measureText(token).width;
		remainderLineWidth -= tokenWidth;
		if (remainderLineWidth < 0) {
			output.push(line.join(""));
			line.length = 0;
			remainderLineWidth = wrapWidth - tokenWidth;
		}
		line.push(token);
	}
	if (line.length > 0) output.push(line.join(""));
	return output;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/text/SetWrapMode.js
var SetWrapMode = function(textObject, mode) {
	switch (GetTextObjectType(textObject)) {
		case 0:
			if (typeof mode === "string") mode = WRAPMODE$1[mode] || 0;
			textObject.style.wrapMode = mode;
			switch (mode) {
				case 2:
				case 3:
					textObject.style.wordWrapCallback = TextWrapByCharCallback;
					break;
				default: textObject.style.wordWrapCallback = null;
			}
			break;
		case 1:
			if (typeof mode === "string") mode = WRAPMODE$1[mode] || 0;
			textObject.style.wrapMode = mode;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/wrapexpandtext/TextRunWidthWrap.js
var TextRunWidthWrap = function(textObject) {
	var RunWidthWrap = function(width) {
		var padding = textObject.padding;
		var wrapWidth = width - (padding.left + padding.right) * textObject.scaleX;
		var style = textObject.style;
		if (IsTextGameObject(textObject)) {
			style.wordWrapWidth = wrapWidth;
			style.maxLines = 0;
		} else {
			if (style.wrapMode === 0) style.wrapMode = 1;
			style.wrapWidth = wrapWidth;
			style.maxLines = 0;
		}
		style.fixedWidth = width;
		style.fixedHeight = 0;
		textObject.updateText();
		textObject.minHeight = textObject.height;
		return textObject;
	};
	return RunWidthWrap;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/wrapexpandtext/DynamicTextRunWidthWrap.js
var DynamicTextRunWidthWrap = function(textObject) {
	var RunWidthWrap = function(width) {
		textObject.setFixedSize(width, 0).runWordWrap();
		textObject.minHeight = textObject.height;
		return textObject;
	};
	return RunWidthWrap;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/wrapexpandtext/BitmapTextRunWidthWrap.js
var BitmapTextRunWidthWrap = function(textObject) {
	var RunWidthWrap = function(width) {
		textObject.setMaxWidth(width);
		textObject.minHeight = textObject.height;
		return textObject;
	};
	return RunWidthWrap;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/wrapexpandtext/WrapExpandText.js
var IsDynamicTextGameObject = function(gameObject) {
	return gameObject instanceof DynamicText_default;
};
var WrapExpandText = function(textObject, minWidth) {
	if (minWidth === void 0) minWidth = 0;
	textObject._minWidth = minWidth;
	if (IsDynamicTextGameObject(textObject)) textObject.runWidthWrap = DynamicTextRunWidthWrap(textObject);
	else if (IsBitmapTextGameObject(textObject)) {
		textObject.runWidthWrap = BitmapTextRunWidthWrap(textObject);
		GetSizerConfig(textObject).noResize = true;
	} else textObject.runWidthWrap = TextRunWidthWrap(textObject);
	return textObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/text/fontsizefit/FontSizeFit.js
var MaxTestCount = 65535;
var FontSizeFit = function(textObject, width, height) {
	if (width == null) return textObject;
	if (width === 0) {
		SetTextWidth(textObject, 0, height);
		return textObject;
	}
	var textLength = textObject.text.length;
	if (textLength === 0) {
		SetTextWidth(textObject, width, height);
		return textObject;
	}
	var fontSize = Math.floor(width * 1.5 / textLength);
	if (height !== void 0) {
		if (fontSize > height) fontSize = Math.floor(height);
	}
	var sizeData = {};
	var testResult = TestFontSize(textObject, fontSize, width, height, sizeData);
	for (var i = 0; i <= MaxTestCount; i++) {
		if (testResult === 0) break;
		else {
			fontSize += testResult;
			if (fontSize < 0) {
				fontSize = 0;
				break;
			}
		}
		testResult = TestFontSize(textObject, fontSize, width, height, sizeData);
	}
	if (i === MaxTestCount) console.warn(`FontSizeFit: Test count exceeds ${MaxTestCount}`);
	textObject.setFontSize(fontSize);
	SetTextWidth(textObject, width, height);
	return textObject;
};
var GetTextSize = function(textObject, fontSize, sizeData) {
	if (sizeData[fontSize] === void 0) {
		textObject.setFontSize(fontSize);
		sizeData[fontSize] = {
			width: textObject.width,
			height: textObject.height
		};
	}
	return sizeData[fontSize];
};
var TestFontSize = function(textObject, fontSize, width, height, sizeData) {
	var textSize = GetTextSize(textObject, fontSize, sizeData);
	var textSize1 = GetTextSize(textObject, fontSize + 1, sizeData);
	var deltaHeight;
	if (height !== void 0) {
		if (textSize.height <= height && textSize1.height > height) deltaHeight = 0;
		else if (textSize.height > height) return -1;
		else deltaHeight = Math.floor(height - textSize.height);
	}
	var deltaWidth;
	if (textSize.width <= width && textSize1.width > width) return 0;
	else if (textSize.width > width) return -1;
	else {
		var deltaWidth = Math.floor(width - textSize.width);
		if (deltaHeight === void 0) return deltaWidth;
		else return Math.min(deltaWidth, deltaHeight);
	}
};
var SetTextWidth = function(textObject, width, height) {
	var style = textObject.style;
	if (!style) return;
	style.fixedWidth = width;
	style.parent.width = width;
	if (height !== void 0) {
		style.fixedHeight = height;
		style.parent.height = height;
	}
	style.update(false);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/fontsizeexpandtext/FontSizeExpandText.js
var GetValue$159 = __webpack_exports__Utils.Objects.GetValue;
var FontSizeExpandText = function(textObject, config) {
	if (typeof config === "number") config = { minWidth: config };
	var minWidth = GetValue$159(config, "minWidth", 0);
	var minHeight = GetValue$159(config, "minHeight", 0);
	var fitHeight = GetValue$159(config, "fitHeight", false);
	textObject._minWidth = minWidth;
	if (!fitHeight) {
		textObject.runWidthWrap = function(width) {
			if (textObject.setFixedSize) textObject.setFixedSize(0, 0);
			FontSizeFit(textObject, width, void 0);
			textObject.minHeight = Math.max(minHeight, textObject.height);
			return textObject;
		};
		textObject.resize = function(width, height) {
			if (textObject.width === width && textObject.height === height) return textObject;
			textObject.setFixedSize(width, height);
			return textObject;
		};
	} else {
		textObject.runWidthWrap = function(width) {
			if (textObject.setFixedSize) textObject.setFixedSize(0, 0);
			textObject.setFontSize(1);
			textObject.minHeight = Math.max(minHeight, textObject.height);
			return textObject;
		};
		textObject.resize = function(width, height) {
			FontSizeFit(textObject, width, height);
			textObject.minHeight = Math.max(minHeight, textObject.height);
			return textObject;
		};
	}
	return textObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/label/Label.js
var GetValue$158 = __webpack_exports__Utils.Objects.GetValue;
var Label = class extends LabelBase {
	constructor(scene, config) {
		if (config === void 0) config = {};
		super(scene, config);
		this.type = "rexLabel";
		var background = GetValue$158(config, "background", void 0);
		var icon = GetValue$158(config, "icon", void 0);
		var iconMask = GetValue$158(config, "iconMask", void 0);
		var text = GetValue$158(config, "text", void 0);
		var action = GetValue$158(config, "action", void 0);
		var actionMask = GetValue$158(config, "actionMask", void 0);
		var align = GetValue$158(config, "align", void 0);
		if (background) this.addBackground(background);
		if (icon) {
			var padding;
			if (this.orientation === 0) {
				if (text || action) padding = {
					right: GetValue$158(config, "space.icon", 0),
					top: GetValue$158(config, "space.iconTop", 0),
					bottom: GetValue$158(config, "space.iconBottom", 0),
					left: GetValue$158(config, "space.iconLeft", 0)
				};
			} else if (text || action) padding = {
				bottom: GetValue$158(config, "space.icon", 0),
				left: GetValue$158(config, "space.iconLeft", 0),
				right: GetValue$158(config, "space.iconRight", 0),
				top: GetValue$158(config, "space.iconTop", 0)
			};
			var fitRatio = GetValue$158(config, "squareFitIcon", false) ? 1 : 0;
			this.add(icon, {
				proportion: 0,
				padding,
				fitRatio
			});
			if (iconMask) iconMask = AddChildCircleMask(this, icon);
			if (!fitRatio) {
				var iconSize = GetValue$158(config, "iconSize", void 0);
				this.setIconSize(GetValue$158(config, "iconWidth", iconSize), GetValue$158(config, "iconHeight", iconSize));
			}
		}
		if (text) {
			var wrapText = GetValue$158(config, "wrapText", false);
			var adjustTextFontSize = GetValue$158(config, "adjustTextFontSize", false);
			if (wrapText) {
				if (wrapText === true) wrapText = "word";
				SetWrapMode(text, wrapText);
				config.expandTextWidth = true;
				WrapExpandText(text);
			} else if (adjustTextFontSize) {
				config.expandTextWidth = true;
				config.expandTextHeight = true;
				FontSizeExpandText(text, { fitHeight: true });
			}
			var textSpace = GetValue$158(config, "space.text", 0);
			var expandTextWidth = GetValue$158(config, "expandTextWidth", false);
			var expandTextHeight = GetValue$158(config, "expandTextHeight", false);
			var proportion, padding, expand, textAlign;
			var textAlign = wrapText && IsBitmapTextGameObject(text) ? "left" : void 0;
			if (this.orientation === 0) {
				proportion = expandTextWidth ? 1 : 0;
				if (action) padding = { right: textSpace };
				expand = expandTextHeight;
			} else {
				proportion = expandTextHeight ? 1 : 0;
				if (action) padding = { bottom: textSpace };
				expand = expandTextWidth;
			}
			this.add(text, {
				proportion,
				expand,
				padding,
				align: textAlign
			});
		}
		if (action) {
			var padding;
			if (this.orientation === 0) padding = {
				top: GetValue$158(config, "space.actionTop", 0),
				bottom: GetValue$158(config, "space.actionBottom", 0),
				right: GetValue$158(config, "space.actionRight", 0)
			};
			else padding = {
				left: GetValue$158(config, "space.actionLeft", 0),
				right: GetValue$158(config, "space.actionRight", 0),
				bottom: GetValue$158(config, "space.actionBottom", 0)
			};
			var fitRatio = GetValue$158(config, "squareFitAction", false) ? 1 : 0;
			this.add(action, {
				proportion: 0,
				padding,
				fitRatio
			});
			if (actionMask) actionMask = AddChildCircleMask(this, action);
			if (!fitRatio) {
				var actionSize = GetValue$158(config, "actionSize");
				this.setActionSize(GetValue$158(config, "actionWidth", actionSize), GetValue$158(config, "actionHeight", actionSize));
			}
		}
		this.setChildrenAlignMode(align);
		this.addChildrenMap("background", background);
		this.addChildrenMap("icon", icon);
		this.addChildrenMap("iconMask", iconMask);
		this.addChildrenMap("text", text);
		this.addChildrenMap("action", action);
		this.addChildrenMap("actionMask", actionMask);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/imageinputlabel/methods/CreateImageBox.js
var GetValue$157 = __webpack_exports__Utils.Objects.GetValue;
var CreateCanvas = function(scene, config) {
	var canvasConfig = GetValue$157(config, "canvas");
	var canvas = new Canvas_default(scene, 0, 0, GetValue$157(canvasConfig, "width", 128), GetValue$157(canvasConfig, "height", 128));
	scene.add.existing(canvas);
	var key = GetValue$157(canvasConfig, "key");
	var frame = GetValue$157(canvasConfig, "frame");
	var fillColor = GetValue$157(canvasConfig, "fill");
	if (fillColor !== void 0) canvas.fill(fillColor);
	else if (key !== void 0) canvas.loadTexture(key, frame);
	canvas.setTexture = canvas.loadTexture.bind(canvas);
	return canvas;
};
var CreateImageBox = function(scene, config) {
	var icon = new ImageBox_default(scene, {
		scaleUp: GetValue$157(config, "scaleUpIcon", false),
		background: GetValue$157(config, "iconBackground"),
		image: CreateCanvas(scene, config)
	});
	scene.add.existing(icon);
	return icon;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/imageinputlabel/methods/GetClickTarget.js
var GetValue$156 = __webpack_exports__Utils.Objects.GetValue;
var GetClickTarget = function(parent, config) {
	var clickTarget = GetValue$156(config, "clickTarget", this);
	if (typeof clickTarget === "string") clickTarget = parent.getElement(clickTarget);
	return clickTarget;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/imageinputlabel/methods/CreateClickBehavior.js
var GetValue$155 = __webpack_exports__Utils.Objects.GetValue;
var CreateClickBehavior = function(parent, config) {
	var clickTarget = GetClickTarget(parent, config);
	if (!clickTarget) return;
	var clickBehavior = new Click_default(clickTarget, GetValue$155(config, "click"));
	clickBehavior.on("click", parent.open, parent);
	return clickBehavior;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/imageinputlabel/methods/FileChooserConfig.js
var Config = {
	accept: "image/*",
	multiple: false
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/imageinputlabel/methods/OnSelectFile.js
var OnSelectFile = function(parent, files) {
	if (files.length === 0) return;
	var imageBox = parent.childrenMap.icon;
	var canvas = imageBox.image;
	var selectedFile = files[0];
	return canvas.loadFromFilePromise(selectedFile).then(function() {
		imageBox.scaleImage();
		parent.emit("select", selectedFile, parent);
		return Promise.resolve(selectedFile);
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/imageinputlabel/methods/CreateFileChooser.js
var CreateFileChooser = function(parent, config) {
	var scene = parent.scene;
	var fileChooser = new FileChooser(scene, Config);
	scene.add.existing(fileChooser);
	parent.pin(fileChooser);
	fileChooser.on("change", function() {
		OnSelectFile(parent, fileChooser.files);
	});
	return fileChooser;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/imageinputlabel/methods/OpenMethods.js
var OpenMethods_default = {
	async openPromise() {
		var self = this;
		return Open$1(this.scene.game, Config).then(function(result) {
			return OnSelectFile(self, result.files);
		});
	},
	open() {
		this.openPromise();
		return this;
	},
	setClickOpenEnable(enable) {
		if (enable === void 0) enable = true;
		if (this.clickBehavior) this.clickBehavior.setEnable(enable);
		if (this.fileChooser) this.fileChooser.setOpenEnable(enable);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/imageinputlabel/methods/GetFileName.js
var GetFileName = function(file) {
	if (!file) return null;
	var name = file.name;
	return name.substr(0, name.lastIndexOf("."));
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/imageinputlabel/methods/SaveTexture.js
var SaveTexture = function(key) {
	this.childrenMap.canvas.generateTexture(key);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/imageinputlabel/methods/Methods.js
var methods$22 = {
	getFileName: GetFileName,
	saveTexture: SaveTexture
};
Object.assign(methods$22, OpenMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/imageinputlabel/ImageInputLabel.js
var GetValue$154 = __webpack_exports__Utils.Objects.GetValue;
var ImageInputLabel = class extends Label {
	constructor(scene, config) {
		var icon = CreateImageBox(scene, config);
		config.icon = icon;
		super(scene, config);
		this.type = "rexImageFileInputLabel";
		var iconWidth = this.iconWidth, iconHeight = this.iconWidth;
		if (iconWidth !== void 0 && iconHeight !== void 0) icon.resize(iconWidth, iconHeight);
		this.clickTarget = GetClickTarget(this, config);
		if (this.clickTarget) {
			if (!GetValue$154(config, "domButton", true)) this.clickBehavior = CreateClickBehavior(this, config);
			else this.fileChooser = CreateFileChooser(this, config);
		}
		this.addChildrenMap("canvas", icon.image);
		this.addChildrenMap("iconBackground", icon.background);
		this.addChildrenMap("fileChooser", this.fileChooser);
	}
	postLayout(parent, newWidth, newHeight) {
		if (this.fileChooser) {
			this.fileChooser.syncTo(this.clickTarget);
			this.resetChildState(this.fileChooser);
		}
		super.postLayout(parent, newWidth, newHeight);
	}
};
Object.assign(ImageInputLabel.prototype, methods$22);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/imageinputlabel/Factory.js
ObjectFactory.register("imageInputLabel", function(config) {
	var gameObject = new ImageInputLabel(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.ImageInputLabel", ImageInputLabel);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/effectproperties.js
var effectproperties_default = AddEffectProperties;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/statesimage/Style.js
var Style$3 = class extends ComponentBase {
	constructor(gameObject, style) {
		super(gameObject);
		return new Proxy(this, this);
	}
	get(target, prop) {
		if (HasProperty(target, prop)) return target[prop];
		var gameObject = target.parent;
		if (HasProperty(gameObject, prop)) return gameObject[prop];
	}
	set(target, prop, value) {
		if (HasProperty(target, prop)) target[prop] = value;
		else if (HasProperty(target.parent, prop)) target.parent[prop] = value;
		return true;
	}
	get key() {
		return this.parent.texture.key;
	}
	set key(value) {
		this.parent.setTexture(value, this.frame);
	}
	get frame() {
		return this.parent.frame.name;
	}
	set frame(value) {
		this.parent.setFrame(value);
	}
	get scale() {
		return this.parent.scaleX;
	}
	set scale(value) {
		this.parent.setScale(value);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/stylemanager/StyleManager.js
var GetValue$153 = __webpack_exports__Utils.Objects.GetValue;
var StyleManager = class extends ComponentBase {
	constructor(gameObject, config) {
		super(gameObject, config);
		this.style = GetValue$153(config, "style", this);
		var propertiesMap = GetValue$153(config, "propertiesMap");
		this.activeStyle = ExtractStyle(config, "active", propertiesMap);
		this.hoverStyle = ExtractStyle(config, "hover", propertiesMap);
		this.disableStyle = ExtractStyle(config, "disable", propertiesMap);
		this.onModifyStyle = GetValue$153(config, "onModifyStyle");
	}
	getStyle(keys) {
		return GetPartialData(this.style, keys);
	}
	modifyStyle(style) {
		for (var key in style) this.style[key] = style[key];
		if (this.onModifyStyle) this.onModifyStyle(this.parent, style);
		return this;
	}
	applyStyle(newStyle) {
		if (!newStyle) return;
		var currentStyle = this.getStyle(newStyle);
		if (!IsKeyValueEqual(currentStyle, newStyle)) {
			this.modifyStyle(newStyle);
			return currentStyle;
		} else return;
	}
	setActiveState(enable) {
		SetStateEnableMethod.call(this, "active", enable);
		return this;
	}
	setHoverState(enable) {
		SetStateEnableMethod.call(this, "hover", enable);
		return this;
	}
	setDisableState(enable) {
		SetStateEnableMethod.call(this, "disable", enable);
		return this;
	}
};
var ExtractStyle = function(config, prefix, propertiesMap) {
	var result = ExtractByPrefix(config, prefix);
	if (propertiesMap) {
		for (var name in result) if (propertiesMap.hasOwnProperty(name)) {
			result[propertiesMap[name]] = result[name];
			delete result[name];
		}
	}
	return result;
};
var SetStateEnableMethod = function(stateName, enable) {
	if (enable === void 0) enable = true;
	var stateVarName = `${stateName}State`;
	var styleVarName = `${stateName}Style`;
	var styleSaveVarName = `${stateName}StyleSave`;
	if (this[stateVarName] === enable) return;
	this[stateVarName] = enable;
	if (enable) this[styleSaveVarName] = this.applyStyle(this[styleVarName]);
	else {
		this.applyStyle(this[styleSaveVarName]);
		this[styleSaveVarName] = void 0;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/stylemanager/HelperMethods.js
var HelperMethods_default = {
	addStyleManager(config) {
		this.styleManager = new StyleManager(this, config);
		return this;
	},
	setActiveState(enable) {
		this.styleManager.setActiveState(enable);
		return this;
	},
	setHoverState(enable) {
		this.styleManager.setHoverState(enable);
		return this;
	},
	setDisableState(enable) {
		this.styleManager.setDisableState(enable);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/statesimage/StatesImage.js
var PhaserImage = __webpack_exports__GameObjects.Image;
var GetValue$152 = __webpack_exports__Utils.Objects.GetValue;
var StatesImage = class extends PhaserImage {
	constructor(scene, config) {
		if (config === void 0) config = {};
		var x = GetValue$152(config, "x", 0);
		var y = GetValue$152(config, "y", 0);
		var key = GetValue$152(config, "key", "");
		var frame = GetValue$152(config, "frame", void 0);
		super(scene, x, y, key, frame);
		this.type = "rexStatesImage";
		var effectConfig = GetValue$152(config, "effects", true);
		if (effectConfig) effectproperties_default(this, effectConfig);
		this.style = new Style$3(this, config);
		config.style = this.style;
		this.addStyleManager(config);
		delete config.style;
	}
};
Object.assign(StatesImage.prototype, HelperMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/statesimage/Factory.js
ObjectFactory.register("statesImage", function(config) {
	var gameObject = new StatesImage(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.StatesImage", StatesImage);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/statesroundrectangle/StatesRoundRectangle.js
var StatesRoundRectangle = class extends RoundRectangle_default {
	constructor(scene, config) {
		if (config === void 0) config = {};
		super(scene, config);
		this.type = "rexStatesRoundRectangleShape";
		config.style = this;
		config.propertiesMap = PropertiesMap$1;
		this.addStyleManager(config);
		delete config.style;
		delete config.propertiesMap;
	}
};
var PropertiesMap$1 = {
	color: "fillColor",
	alpha: "fillAlpha",
	strokeWidth: "lineWidth"
};
Object.assign(StatesRoundRectangle.prototype, HelperMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/statesroundrectangle/Factory.js
ObjectFactory.register("statesRoundRectangle", function(config) {
	var gameObject = new StatesRoundRectangle(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.StatesRoundRectangle", StatesRoundRectangle);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/statesnineslice/Style.js
var Style$2 = class extends ComponentBase {
	constructor(gameObject, style) {
		super(gameObject);
		return new Proxy(this, this);
	}
	get(target, prop) {
		if (HasProperty(target, prop)) return target[prop];
		var gameObject = target.parent;
		if (HasProperty(gameObject, prop)) return gameObject[prop];
	}
	set(target, prop, value) {
		if (HasProperty(target, prop)) target[prop] = value;
		else if (HasProperty(target.parent, prop)) target.parent[prop] = value;
		return true;
	}
	get key() {
		return this.parent.texture.key;
	}
	set key(value) {
		if (this.key === value) return;
		this.parent.setTexture(value, this.frame);
	}
	get frame() {
		return this.parent.frame.name;
	}
	set frame(value) {
		if (this.frame === value) return;
		this.parent.setFrame(value);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/statesnineslice/StatesNineSlice.js
var PhaserNineSlice = __webpack_exports__GameObjects.NineSlice;
var GetValue$151 = __webpack_exports__Utils.Objects.GetValue;
var StatesNineSlice = class extends PhaserNineSlice {
	constructor(scene, config) {
		if (config === void 0) config = {};
		var x = GetValue$151(config, "x", 0);
		var y = GetValue$151(config, "y", 0);
		var key = GetValue$151(config, "key", null);
		var frame = GetValue$151(config, "frame", null);
		var width = GetValue$151(config, "width", 0);
		var height = GetValue$151(config, "height", 0);
		var leftWidth = GetValue$151(config, "leftWidth", 0);
		var rightWidth = GetValue$151(config, "rightWidth", 0);
		var topHeight = GetValue$151(config, "topHeight", 0);
		var bottomHeight = GetValue$151(config, "bottomHeight", 0);
		super(scene, x, y, key, frame, width, height, leftWidth, rightWidth, topHeight, bottomHeight);
		this.type = "rexStatesNineSlice";
		var effectConfig = GetValue$151(config, "effects", true);
		if (effectConfig) effectproperties_default(this, effectConfig);
		this.style = new Style$2(this, config);
		config.style = this.style;
		this.addStyleManager(config);
		delete config.style;
	}
};
Object.assign(StatesNineSlice.prototype, HelperMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/statesnineslice/Factory.js
ObjectFactory.register("statesNineSlice", function(config) {
	var gameObject = new StatesNineSlice(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.StatesNineSlice", StatesNineSlice);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/statesninepatch/Style.js
var Style$1 = class extends ComponentBase {
	constructor(gameObject, style) {
		super(gameObject);
		return new Proxy(this, this);
	}
	get(target, prop) {
		if (HasProperty(target, prop)) return target[prop];
		var gameObject = target.parent;
		if (HasProperty(gameObject, prop)) return gameObject[prop];
	}
	set(target, prop, value) {
		if (HasProperty(target, prop)) target[prop] = value;
		else if (HasProperty(target.parent, prop)) target.parent[prop] = value;
		return true;
	}
	get key() {
		return this.parent.textureKey;
	}
	set key(value) {
		if (this.key === value) return;
		this.parent.setBaseTexture(value, this.baseFrameName);
	}
	get frame() {
		return this.parent.baseFrameName;
	}
	set frame(value) {
		if (this.frame === value) return;
		this.parent.setBaseTexture(this.parent.textureKey, value);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/statesninepatch/StatesNinePatch.js
var GetValue$150 = __webpack_exports__Utils.Objects.GetValue;
var StatesNinePatch = class extends NinePatch_default$1 {
	constructor(scene, config) {
		if (config === void 0) config = {};
		super(scene, config);
		this.type = "rexStatesNinePatch";
		var effectConfig = GetValue$150(config, "effects", true);
		if (effectConfig) effectproperties_default(this, effectConfig);
		this.style = new Style$1(this, config);
		config.style = this.style;
		this.addStyleManager(config);
		delete config.style;
	}
};
Object.assign(StatesNinePatch.prototype, HelperMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/statesninepatch/Factory.js
ObjectFactory.register("statesNinePatch", function(config) {
	var gameObject = new StatesNinePatch(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.StatesNinePatch", StatesNinePatch);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/statestext/StatesText.js
var PhaserText = __webpack_exports__GameObjects.Text;
var GetValue$149 = __webpack_exports__Utils.Objects.GetValue;
var StatesText = class extends PhaserText {
	constructor(scene, config) {
		if (config === void 0) config = {};
		var x = GetValue$149(config, "x", 0);
		var y = GetValue$149(config, "y", 0);
		var text = GetValue$149(config, "text", "");
		super(scene, x, y, text, config);
		this.type = "rexStatesText";
		config.style = this.style;
		config.onModifyStyle = function(gameObject, style) {
			var recalculateMetrics = style.hasOwnProperty("fontStyle") || style.hasOwnProperty("fontSize") || style.hasOwnProperty("fontFamily");
			gameObject.style.update(recalculateMetrics);
		};
		this.addStyleManager(config);
		delete config.style;
	}
};
Object.assign(StatesText.prototype, HelperMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/statestext/Factory.js
ObjectFactory.register("statesText", function(config) {
	var gameObject = new StatesText(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.StatesText", StatesText);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/statesbitmaptext/Style.js
var Style = class extends ComponentBase {
	constructor(gameObject, style) {
		super(gameObject);
		return new Proxy(this, this);
	}
	get(target, prop) {
		if (HasProperty(target, prop)) return target[prop];
		var gameObject = target.parent;
		if (HasProperty(gameObject, prop)) return gameObject[prop];
	}
	set(target, prop, value) {
		if (HasProperty(target, prop)) target[prop] = value;
		else if (HasProperty(target.parent, prop)) target.parent[prop] = value;
		return true;
	}
	get key() {
		return this.parent.texture.key;
	}
	set key(value) {
		this.parent.setTexture(value, this.frame);
	}
	get fontSize() {
		return this.parent.fontSize;
	}
	set fontSize(value) {
		this.parent.setFontSize(value);
	}
	get tint() {
		return this.parent.tintTopLeft;
	}
	set tint(value) {
		this.parent.setTint(value);
	}
	get letterSpacing() {
		return this.parent.letterSpacing;
	}
	set letterSpacing(value) {
		this.parent.setLetterSpacing(value);
	}
	get lineSpacing() {
		return this.parent.lineSpacing;
	}
	set lineSpacing(value) {
		this.parent.setLineSpacing(value);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/statesbitmaptext/StatesBitmapText.js
var PhaserBitmapText = __webpack_exports__GameObjects.BitmapText;
var GetValue$148 = __webpack_exports__Utils.Objects.GetValue;
var StatesBitmapText = class extends PhaserBitmapText {
	constructor(scene, config) {
		if (config === void 0) config = {};
		var x = GetValue$148(config, "x", 0);
		var y = GetValue$148(config, "y", 0);
		var font = GetValue$148(config, "font", "");
		var size = GetValue$148(config, "fontSize", false);
		var align = GetValue$148(config, "align", 0);
		var tint = GetValue$148(config, "tint");
		super(scene, x, y, font, "", size, align);
		this.type = "rexStatesBitmapText";
		if (tint !== void 0) this.setTint(tint);
		var effectConfig = GetValue$148(config, "effects", true);
		if (effectConfig) effectproperties_default(this, effectConfig);
		this.style = new Style(this, config);
		config.style = this.style;
		this.addStyleManager(config);
		delete config.style;
	}
};
Object.assign(StatesBitmapText.prototype, HelperMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/statesbitmaptext/Factory.js
ObjectFactory.register("statesBitmapText", function(config) {
	var gameObject = new StatesBitmapText(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.StatesBitmapText", StatesBitmapText);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/statesbarrectangle/StatesBarRectangle.js
var StatesBarRectangle = class extends LineProgress_default {
	constructor(scene, config) {
		if (config === void 0) config = {};
		if (!config.hasOwnProperty("value")) config.value = 0;
		if (!config.hasOwnProperty("hover.bar")) config["hover.bar"] = true;
		if (!config.hasOwnProperty("easeDuration")) config.easeDuration = 200;
		if (!config.hasOwnProperty("ease")) config.ease = "Quad";
		SetValue(config, "easeValue.duration", config.easeDuration);
		SetValue(config, "easeValue.ease", config.ease);
		super(scene, config);
		this.type = "rexStatesBarRectangleShape";
		this.barState = false;
		config.style = this;
		config.propertiesMap = PropertiesMap;
		this.addStyleManager(config);
		delete config.style;
		delete config.propertiesMap;
	}
	get bar() {
		return this.barState;
	}
	set bar(value) {
		value = !!value;
		if (this.barState === value) return;
		this.barState = value;
		this.easeValueTo(this.barState ? 1 : 0);
	}
};
var PropertiesMap = {
	color: "trackColor",
	strokeColor: "trackStrokeColor",
	strokeWidth: "trackStrokeThickness"
};
Object.assign(StatesBarRectangle.prototype, HelperMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/statesbarrectangle/Factory.js
ObjectFactory.register("statesBarRectangle", function(config) {
	var gameObject = new StatesBarRectangle(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.StatesBarRectangle", StatesBarRectangle);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/chart/methods/SetChart.js
var SetChart = function(config) {
	if (!window.Chart) {
		console.error(`Can not find chartjs! Load chartjs in preload stage.
scene.load.script('chartjs', 'https://cdnjs.cloudflare.com/ajax/libs/Chart.js/3.8.0/Chart.min.js');`);
		return this;
	}
	if (this.chart) this.chart.destroy();
	this.chart = new Chart(this.context, FillConfig(this, config));
	return this;
};
var FillConfig = function(canvas, config) {
	if (config === void 0) config = {};
	if (config.options === void 0) config.options = {};
	var options = config.options;
	options.responsive = false;
	options.maintainAspectRatio = false;
	if (!options.hasOwnProperty("devicePixelRatio")) options.devicePixelRatio = 1;
	var noAnimation = false;
	if (options.animation === void 0) options.animation = {};
	else if (options.animation === false) {
		noAnimation = true;
		options.animation = {};
	}
	var animationConfig = options.animation;
	if (noAnimation) animationConfig.duration = 0;
	var onProgress = animationConfig.onProgress;
	animationConfig.onProgress = function(animation) {
		if (onProgress) onProgress(animation);
		canvas.needRedraw();
	};
	var onComplete = animationConfig.onComplete;
	animationConfig.onComplete = function(animation) {
		if (onComplete) onComplete(animation);
		canvas.needRedraw();
	};
	return config;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/chart/methods/GetChartDataset.js
var GetChartDataset = function(datasetIndex) {
	if (this.chart === void 0) return;
	if (typeof datasetIndex === "string") {
		var datasets = this.chart.data.datasets, dataset;
		for (var i = 0, cnt = datasets.length; i < cnt; i++) {
			dataset = datasets[i];
			if (dataset.label === datasetIndex) return dataset;
		}
	} else return this.chart.data.datasets[datasetIndex];
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/chart/methods/GetChartData.js
var GetChartData = function(datasetIndex, dataIndex) {
	var dataset = this.getChartDataset(datasetIndex);
	if (dataset === void 0) return;
	if (typeof dataIndex === "string") {
		dataIndex = this.chart.data.labels.indexOf(dataIndex);
		if (dataIndex === -1) return;
	}
	return dataset.data[dataIndex];
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/chart/methods/SetChartData.js
var SetChartData = function(datasetIndex, dataIndex, value) {
	if (this.chart === void 0) return this;
	var dataset = this.getChartDataset(datasetIndex);
	if (typeof dataIndex === "string") {
		dataIndex = this.chart.data.labels.indexOf(dataIndex);
		if (dataIndex === -1) return this;
	}
	dataset.data[dataIndex] = value;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/chart/methods/UpdateChart.js
var UpdateChart = function() {
	if (this.chart === void 0) return this;
	this.chart.update();
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/chart/methods/Methods.js
var methods$21 = {
	setChart: SetChart,
	getChartDataset: GetChartDataset,
	getChartData: GetChartData,
	setChartData: SetChartData,
	updateChart: UpdateChart
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/chart/Chart.js
var IsChartJsV2 = function(chart) {
	return chart.resize.length === 1;
};
var Chart$1 = class extends Canvas_default {
	constructor(scene, x, y, width, height, config) {
		if (config === void 0) config = {};
		var resolution = config.resolution;
		super(scene, x, y, width, height, resolution);
		this.type = "rexChart";
		this.chart = void 0;
		if (config !== void 0) this.setChart(config);
	}
	destroy(fromScene) {
		if (!this.scene) return;
		if (this.chart) {
			this.chart.destroy();
			this.chart = void 0;
		}
		super.destroy(fromScene);
	}
	resize(width, height) {
		if (width === this.width && height === this.height) return this;
		super.resize(width, height);
		if (this.chart) {
			var targetWidth = this.canvas.width;
			var targetHeight = this.canvas.height;
			var aspectRatio = targetHeight ? targetWidth / targetHeight : null;
			var chart = this.chart;
			chart.width = targetWidth;
			chart.height = targetHeight;
			if (IsChartJsV2(chart)) chart.aspectRatio = aspectRatio;
			else chart.options.aspectRatio = aspectRatio;
			chart.update();
		}
		return this;
	}
};
Object.assign(Chart$1.prototype, methods$21);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/chart/Factory.js
ObjectFactory.register("chart", function(x, y, width, height, config) {
	var gameObject = new Chart$1(this.scene, x, y, width, height, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.Chart", Chart$1);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/container/Factory.js
ObjectFactory.register("container", function(x, y, width, height, children) {
	var gameObject = new Container_default(this.scene, x, y, width, height, children);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.Container", Container_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sizer/Factory.js
ObjectFactory.register("sizer", function(x, y, minWidth, minHeight, orientation, config) {
	var gameObject = new Sizer(this.scene, x, y, minWidth, minHeight, orientation, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.Sizer", Sizer);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/math/Sum.js
var Sum = function() {
	return Array.prototype.reduce.call(arguments, Add$5, 0);
};
var Add$5 = function(a, b) {
	return a + b;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/GetChildrenWidth.js
var GetChildrenWidth$2 = function(minimumMode) {
	if (this.rexSizer.hidden) return 0;
	if (minimumMode === void 0) minimumMode = true;
	var result = 0, columnWidth;
	var children = this.sizerChildren;
	var child, padding, childWidth, proportion;
	var hasUnknownChildWidth = false;
	this.totalColumnProportions;
	for (var i = 0; i < this.columnCount; i++) {
		proportion = this.columnProportions[i];
		columnWidth = 0;
		if (proportion === 0 || minimumMode) {
			for (var j = 0; j < this.rowCount; j++) {
				child = children[j * this.columnCount + i];
				if (!child) continue;
				if (child.rexSizer.hidden) continue;
				childWidth = this.getChildWidth(child);
				if (childWidth === void 0) {
					if (proportion !== 0 && !this.hasColumnProportion0Child) childWidth = 0;
					else hasUnknownChildWidth = true;
				}
				if (hasUnknownChildWidth) continue;
				padding = child.rexSizer.padding;
				childWidth += (padding.left + padding.right) * this.scaleX;
				columnWidth = Math.max(columnWidth, childWidth);
			}
			if (!hasUnknownChildWidth) result += columnWidth;
		}
		if (!hasUnknownChildWidth) {
			if (minimumMode) this.columnWidth[i] = columnWidth;
		}
	}
	if (hasUnknownChildWidth) return;
	var indentLeft = Math.max(this.space.indentLeftOdd, this.space.indentLeftEven);
	var totalSpace = Sum(this.space.left, indentLeft, ...this.space.column, this.space.right);
	return result + totalSpace * this.scaleX;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/GetChildrenHeight.js
var GetChildrenHeight$2 = function(minimumMode) {
	if (this.rexSizer.hidden) return 0;
	if (minimumMode === void 0) minimumMode = true;
	var result = 0, rowHeight;
	var children = this.sizerChildren;
	var child, padding, childHeight, proportion;
	var hasUnknownChildHeight = false;
	this.totalRowProportions;
	for (var i = 0; i < this.rowCount; i++) {
		proportion = this.rowProportions[i];
		rowHeight = 0;
		if (proportion === 0 || minimumMode) {
			for (var j = 0; j < this.columnCount; j++) {
				child = children[i * this.columnCount + j];
				if (!child) continue;
				if (child.rexSizer.hidden) continue;
				childHeight = this.getChildHeight(child);
				if (childHeight === void 0) {
					if (proportion !== 0 && !this.hasRowProportion0Child) childHeight = 0;
					else hasUnknownChildHeight = true;
				}
				if (hasUnknownChildHeight) continue;
				padding = child.rexSizer.padding;
				childHeight += (padding.top + padding.bottom) * this.scaleY;
				rowHeight = Math.max(rowHeight, childHeight);
			}
			if (!hasUnknownChildHeight) result += rowHeight;
		}
		if (!hasUnknownChildHeight) {
			if (minimumMode) this.rowHeight[i] = rowHeight;
		}
	}
	if (hasUnknownChildHeight) return;
	var indentTop = Math.max(this.space.indentTopOdd, this.space.indentTopEven);
	var totalSpace = Sum(this.space.top, indentTop, ...this.space.row, this.space.bottom);
	return result + totalSpace * this.scaleY;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/GetExpandedChildWidth.js
var GetExpandedChildWidth$1 = function(child, colWidth) {
	var childWidth;
	var childConfig = child.rexSizer;
	if (childConfig.expandWidth) {
		var padding = childConfig.padding;
		childWidth = colWidth - (padding.left + padding.right) * this.scaleX;
	}
	return childWidth;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/GetExpandedChildHeight.js
var GetExpandedChildHeight$1 = function(child, rowHeight) {
	var childHeight;
	var childConfig = child.rexSizer;
	if (childConfig.expandHeight) {
		var padding = childConfig.padding;
		childHeight = rowHeight - (padding.top + padding.bottom) * this.scaleY;
	}
	return childHeight;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/GetChildrenSizers.js
var GetChildrenSizers$2 = function(out) {
	if (out === void 0) out = [];
	var children = this.sizerChildren, child;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		child = children[i];
		if (child && child.isRexSizer) out.push(child);
	}
	return out;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/PreLayout.js
var PreLayout$1 = function() {
	this._totalColumnProportions = void 0;
	this._totalRowProportions = void 0;
	this.hasColumnProportion0Child = false;
	this.hasRowProportion0Child = false;
	this.proportionWidthLength = void 0;
	this.proportionHeightLength = void 0;
	PreLayout$3.call(this);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/LayoutChildren.js
var LayoutChildren$2 = function() {
	var child, childConfig, padding;
	var startX = this.innerLeft, startY = this.innerTop;
	var itemX, itemY = startY;
	var x, y, width, height, alignOffsetX, alignOffsetY;
	var childWidth, childHeight;
	var colWidth, rowHeight;
	var indentLeft, indentTop;
	for (var rowIndex = 0; rowIndex < this.rowCount; rowIndex++) {
		rowHeight = this.getRowHeight(rowIndex);
		indentLeft = rowIndex % 2 ? this.space.indentLeftEven : this.space.indentLeftOdd;
		itemX = startX + indentLeft * this.scaleX;
		for (var columnIndex = 0; columnIndex < this.columnCount; columnIndex++) {
			colWidth = this.getColumnWidth(columnIndex);
			child = this.getChildAt(columnIndex, rowIndex);
			if (!child || child.rexSizer.hidden) {
				itemX += colWidth + this.space.column[columnIndex] * this.scaleX;
				continue;
			}
			PreLayoutChild.call(this, child);
			childConfig = child.rexSizer;
			childWidth = this.getExpandedChildWidth(child, colWidth);
			childHeight = this.getExpandedChildHeight(child, rowHeight);
			if (child.isRexSizer) {
				child.runLayout(this, childWidth, childHeight);
				CheckSize(child, this);
			} else if (!childConfig.noResize) ResizeGameObject(child, childWidth, childHeight);
			padding = childConfig.padding;
			x = itemX + padding.left * this.scaleX;
			width = colWidth - (padding.left + padding.right) * this.scaleX;
			indentTop = columnIndex % 2 ? this.space.indentTopEven : this.space.indentTopOdd;
			y = itemY + indentTop * this.scaleY + padding.top * this.scaleY;
			height = rowHeight - (padding.top + padding.bottom) * this.scaleY;
			if (childWidth === void 0) childWidth = GetDisplayWidth(child);
			if (childHeight === void 0) childHeight = GetDisplayHeight(child);
			alignOffsetX = (childConfig.alignOffsetX + childConfig.alignOffsetOriginX * childWidth) * this.scaleX;
			alignOffsetY = (childConfig.alignOffsetY + childConfig.alignOffsetOriginY * childHeight) * this.scaleY;
			LayoutChild.call(this, child, x, y, width, height, childConfig.align, alignOffsetX, alignOffsetY);
			itemX += colWidth + this.space.column[columnIndex] * this.scaleX;
		}
		itemY += rowHeight + this.space.row[rowIndex] * this.scaleY;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/ResolveWidth.js
var ResolveWidth = function(width) {
	var width = ResolveWidth$3.call(this, width);
	if (width !== void 0 && this.proportionWidthLength === void 0) {
		var totalColumnProportions = this.totalColumnProportions;
		if (totalColumnProportions > 0) {
			var remainder = width - this.getChildrenWidth(false);
			if (remainder >= 0) this.proportionWidthLength = remainder / totalColumnProportions;
		} else this.proportionWidthLength = 0;
	}
	return width;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/ResolveHeight.js
var ResolveHeight = function(height) {
	var height = ResolveHeight$3.call(this, height);
	if (height !== void 0 && this.proportionHeightLength === void 0) {
		var totalRowProportions = this.totalRowProportions;
		if (totalRowProportions > 0) {
			var remainder = height - this.getChildrenHeight(false);
			if (remainder >= 0) this.proportionHeightLength = remainder / totalRowProportions;
		} else this.proportionHeightLength = 0;
	}
	return height;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/ResolveChildrenWidth.js
var ResolveChildrenWidth = function(parentWidth) {
	var child, expandedChildWidth, childWidth;
	var colWidth;
	for (var i in this.sizerChildren) {
		child = this.sizerChildren[i];
		if (child && child.isRexSizer && !child.ignoreLayout) {
			colWidth = this.getColumnWidth(parseInt(i) % this.columnCount);
			expandedChildWidth = this.getExpandedChildWidth(child, colWidth);
			childWidth = child.resolveWidth(expandedChildWidth);
			if (childWidth === void 0) childWidth = expandedChildWidth;
			child.resolveChildrenWidth(childWidth);
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/ResolveChildrenHeight.js
var ResolveChildrenHeight = function(parentHeight) {
	var child, expandedChildHeight, childHeight;
	var rowHeight;
	for (var i in this.sizerChildren) {
		child = this.sizerChildren[i];
		if (child && child.isRexSizer && !child.ignoreLayout) {
			rowHeight = this.getRowHeight(Math.floor(parseInt(i) / this.rowCount));
			expandedChildHeight = this.getExpandedChildHeight(child, rowHeight);
			childHeight = child.resolveHeight(expandedChildHeight);
			if (childHeight === void 0) childHeight = expandedChildHeight;
			child.resolveChildrenHeight(childHeight);
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/RunWidthWrap.js
var RunWidthWrap$1 = function(width) {
	var child, expandedChildWidth, childWidth;
	var colWidth;
	for (var i in this.sizerChildren) {
		child = this.sizerChildren[i];
		if (!child || child.isRexSizer && child.ignoreLayout || !child.runWidthWrap) continue;
		colWidth = this.getColumnWidth(parseInt(i) % this.columnCount);
		expandedChildWidth = this.getExpandedChildWidth(child, colWidth);
		if (child.isRexSizer) {
			childWidth = child.resolveWidth(expandedChildWidth);
			if (childWidth === void 0) childWidth = expandedChildWidth;
		}
		child.runWidthWrap(childWidth);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/RunHeightWrap.js
var RunHeightWrap$1 = function(height) {
	var child, expandedChildHeight, childHeight;
	var rowHeight;
	for (var i in this.sizerChildren) {
		child = this.sizerChildren[i];
		if (!child || child.isRexSizer && child.ignoreLayout || !child.runHeightWrap) continue;
		rowHeight = this.getRowHeight(Math.floor(parseInt(i) / this.rowCount));
		expandedChildHeight = this.getExpandedChildHeight(child, rowHeight);
		if (child.isRexSizer) {
			childHeight = child.resolveHeight(expandedChildHeight);
			if (childHeight === void 0) childHeight = expandedChildHeight;
		}
		child.runHeightWrap(childHeight);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/AddChildMethods.js
var IsPlainObject$21 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$147 = __webpack_exports__Utils.Objects.GetValue;
var ALIGN_CENTER$1 = __webpack_exports__Display.Align.CENTER;
var GetEmptyCellIndex = function(columnIndex, rowIndex, cells, columnCount, rowCount) {
	if (typeof columnIndex === "number" || typeof rowIndex === "number") {
		if (columnIndex === void 0) {
			var idx;
			for (var i = 0; i < columnCount; i++) {
				idx = rowIndex * columnCount + i;
				if (!cells[idx]) return idx;
			}
		} else if (rowIndex === void 0) {
			var idx;
			for (var i = 0; i < rowCount; i++) {
				idx = i * columnCount + columnIndex;
				if (!cells[idx]) return idx;
			}
		} else {
			var idx = rowIndex * columnCount + columnIndex;
			if (!cells[idx]) return idx;
		}
	} else if (rowIndex === true) {
		var idx;
		for (var i = 0; i < columnCount; i++) for (var j = 0; j < rowCount; j++) {
			idx = j * columnCount + i;
			if (!cells[idx]) return idx;
		}
	} else for (var i = 0, cnt = cells.length; i < cnt; i++) if (!cells[i]) return i;
	return null;
};
var Add$4 = function(gameObject, columnIndex, rowIndex, align, paddingConfig, expand, childKey) {
	var offsetX, offsetY;
	var offsetOriginX, offsetOriginY;
	AddChild$1.call(this, gameObject);
	if (IsPlainObject$21(columnIndex)) {
		var config = columnIndex;
		columnIndex = GetValue$147(config, "column", void 0);
		rowIndex = GetValue$147(config, "row", void 0);
		align = GetValue$147(config, "align", ALIGN_CENTER$1);
		paddingConfig = GetValue$147(config, "padding", 0);
		expand = GetValue$147(config, "expand", false);
		childKey = GetValue$147(config, "key", void 0);
		offsetX = GetValue$147(config, "offsetX", 0);
		offsetY = GetValue$147(config, "offsetY", 0);
		offsetOriginX = GetValue$147(config, "offsetOriginX", 0);
		offsetOriginY = GetValue$147(config, "offsetOriginY", 0);
	}
	var itemIndex = GetEmptyCellIndex(columnIndex, rowIndex, this.sizerChildren, this.columnCount, this.rowCount);
	if (itemIndex === null) {
		if (typeof columnIndex === "number" && typeof rowIndex === "number") return this;
		if (rowIndex === true || typeof rowIndex === "number") this.addEmptyColumn();
		else this.addEmptyRow();
		itemIndex = GetEmptyCellIndex(columnIndex, rowIndex, this.sizerChildren, this.columnCount, this.rowCount);
	}
	if (typeof align === "string") align = AlignConst_default[align];
	if (align === void 0) align = ALIGN_CENTER$1;
	if (paddingConfig === void 0) paddingConfig = 0;
	if (expand === void 0) expand = true;
	if (offsetX === void 0) offsetX = 0;
	if (offsetY === void 0) offsetY = 0;
	if (offsetOriginX === void 0) offsetOriginX = 0;
	if (offsetOriginY === void 0) offsetOriginY = 0;
	var config = this.getSizerConfig(gameObject);
	config.align = align;
	config.padding = GetBoundsConfig_default(paddingConfig);
	if (IsPlainObject$21(expand)) {
		config.expandWidth = GetValue$147(expand, "width", false);
		config.expandHeight = GetValue$147(expand, "height", false);
	} else {
		config.expandWidth = expand;
		config.expandHeight = expand;
	}
	config.alignOffsetX = offsetX;
	config.alignOffsetY = offsetY;
	config.alignOffsetOriginX = offsetOriginX;
	config.alignOffsetOriginY = offsetOriginY;
	this.sizerChildren[itemIndex] = gameObject;
	if (childKey !== void 0) this.addChildrenMap(childKey, gameObject);
	return this;
};
var AddChildMethods_default$5 = { add: Add$4 };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/array/Fill.js
var Fill = function(arr, value, startIdx, endIdx) {
	if (startIdx === void 0) startIdx = 0;
	if (endIdx === void 0) endIdx = arr.length - 1;
	for (var i = startIdx; i <= endIdx; i++) arr[i] = value;
	return arr;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/RemoveChildMethods.js
var RemoveChildMethods_default$4 = {
	remove(gameObject, destroyChild) {
		if (this.getParentSizer(gameObject) !== this) return this;
		var idx = this.sizerChildren.indexOf(gameObject);
		if (idx !== -1) this.sizerChildren[idx] = null;
		RemoveChild.call(this, gameObject, destroyChild);
		return this;
	},
	removeAt(columnIndex, rowIndex, destroyChild) {
		var child = this.getChildAt(columnIndex, rowIndex);
		if (child) this.remove(child, destroyChild);
		return this;
	},
	removeAll(destroyChild) {
		for (var i = this.sizerChildren.length - 1; i >= 0; i--) {
			var child = this.sizerChildren[i];
			if (!child) continue;
			this.remove(child, destroyChild);
		}
		return this;
	},
	clear(destroyChild) {
		Fill(this.sizerChildren, null);
		ClearChildren.call(this, destroyChild);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/SetSpaceMethods.js
var SetSpaceMethods_default = {
	setColumnSpace(columnSpace) {
		if (!this.space.column) this.space.column = [];
		this.space.column.length = this.columnCount - 1;
		if (typeof columnSpace === "number") {
			this.space.column0 = columnSpace;
			Fill(this.space.column, columnSpace);
		} else {
			this.space.column0 = columnSpace[0];
			for (var i = 0, cnt = this.columnCount - 1; i < cnt; i++) this.space.column[i] = columnSpace[i] || 0;
		}
		return this;
	},
	setRowSpace(rowSpace) {
		if (!this.space.row) this.space.row = [];
		this.space.row.length = this.rowCount - 1;
		if (typeof rowSpace === "number") {
			this.space.row0 = rowSpace;
			Fill(this.space.row, rowSpace);
		} else {
			this.space.row0 = rowSpace[i];
			for (var i = 0, cnt = this.rowCount - 1; i < cnt; i++) this.space.row[i] = rowSpace[i] || 0;
		}
		return this;
	},
	setIndentLeft(odd, even) {
		this.space.indentLeftOdd = odd;
		this.space.indentLeftEven = even;
		return this;
	},
	setIndentTop(odd, even) {
		this.space.indentTopOdd = odd;
		this.space.indentTopEven = even;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/ResetGrid.js
var GetValue$146 = __webpack_exports__Utils.Objects.GetValue;
var ResetGrid = function(columnCount, rowCount, columnProportions, rowProportions, space) {
	if (columnCount === void 0) columnCount = 0;
	if (rowCount === void 0) rowCount = 0;
	if (columnProportions === void 0) columnProportions = 0;
	if (rowProportions === void 0) rowProportions = 0;
	this.columnCount = columnCount;
	this.rowCount = rowCount;
	this.gridCount = columnCount * rowCount;
	this.removeAll();
	this.sizerChildren.length = columnCount * rowCount;
	Fill(this.sizerChildren, null);
	this.columnProportions = [];
	this.columnProportions.length = columnCount;
	if (typeof columnProportions === "number") Fill(this.columnProportions, columnProportions);
	else for (var i = 0; i < columnCount; i++) this.columnProportions[i] = columnProportions[i] || 0;
	this.rowProportions = [];
	this.rowProportions.length = rowCount;
	if (typeof rowProportions === "number") Fill(this.rowProportions, rowProportions);
	else for (var i = 0; i < rowCount; i++) this.rowProportions[i] = rowProportions[i] || 0;
	this.columnWidth = [];
	this.columnWidth.length = columnCount;
	this.rowHeight = [];
	this.rowHeight.length = rowCount;
	this.setColumnSpace(GetValue$146(space, "column", 0));
	this.setRowSpace(GetValue$146(space, "row", 0));
	var scene = this.scene;
	var createCellContainerCallback = this.createCellContainerCallback;
	if (createCellContainerCallback) for (var y = 0, ycnt = this.rowCount; y < ycnt; y++) for (var x = 0, xcnt = this.columnCount; x < xcnt; x++) {
		var addConfig = {
			column: x,
			row: y
		};
		var child = createCellContainerCallback(scene, x, y, addConfig);
		if (child) this.add(child, addConfig);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/InsertEmptyRow.js
var InsertEmptyRow = function(rowIndex, proportion, space) {
	if (proportion === void 0) proportion = this.rowProportions[0] || 0;
	if (space === void 0) space = this.space.row0 || 0;
	this.rowCount += 1;
	this.gridCount += this.columnCount;
	var args = [rowIndex * this.columnCount, 0];
	for (var i = 0; i < this.columnCount; i++) args.push(null);
	this.sizerChildren.splice.apply(this.sizerChildren, args);
	this.rowProportions.push(proportion);
	this.rowHeight.length += 1;
	this.space.row.splice(rowIndex, 0, space);
	return this;
};
var AddEmptyRow = function(proportion, space) {
	InsertEmptyRow.call(this, this.rowCount, proportion, space);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/InsertEmptyColumn.js
var InsertEmptyColumn = function(colIndex, proportion, space) {
	if (proportion === void 0) proportion = this.columnProportions[0] || 0;
	if (space === void 0) space = this.space.column0 || 0;
	this.columnCount += 1;
	this.gridCount += this.rowCount;
	for (var i = this.rowCount - 1; i >= 0; i--) {
		var insertIndex = i * this.columnCount + colIndex;
		this.sizerChildren.splice(insertIndex, 0, null);
	}
	this.columnProportions.push(proportion);
	this.columnWidth.length += 1;
	this.space.column.splice(colIndex, 0, space);
	return this;
};
var AddEmptyColumn = function(proportion, space) {
	InsertEmptyColumn.call(this, this.columnCount, proportion, space);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/Methods.js
var methods$20 = {
	getChildrenWidth: GetChildrenWidth$2,
	getChildrenHeight: GetChildrenHeight$2,
	getExpandedChildWidth: GetExpandedChildWidth$1,
	getExpandedChildHeight: GetExpandedChildHeight$1,
	getChildrenSizers: GetChildrenSizers$2,
	preLayout: PreLayout$1,
	layoutChildren: LayoutChildren$2,
	resolveWidth: ResolveWidth,
	resolveHeight: ResolveHeight,
	resolveChildrenWidth: ResolveChildrenWidth,
	resolveChildrenHeight: ResolveChildrenHeight,
	runWidthWrap: RunWidthWrap$1,
	runHeightWrap: RunHeightWrap$1,
	resetGrid: ResetGrid,
	insertEmptyRow: InsertEmptyRow,
	addEmptyRow: AddEmptyRow,
	insertEmptyColumn: InsertEmptyColumn,
	addEmptyColumn: AddEmptyColumn
};
Object.assign(methods$20, AddChildMethods_default$5, RemoveChildMethods_default$4, SetSpaceMethods_default, SortChildrenMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/GetTotalColumnProportions.js
var GetTotalColumnProportions = function() {
	var result = 0, proportion;
	for (var i = 0; i < this.columnCount; i++) {
		proportion = this.columnProportions[i];
		if (proportion > 0) result += proportion;
		else if (proportion === 0) this.hasColumnProportion0Child = true;
	}
	return result;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/GetTotalRowProportions.js
var GetTotalRowProportions = function() {
	var result = 0, proportion;
	for (var i = 0; i < this.rowCount; i++) {
		proportion = this.rowProportions[i];
		if (proportion > 0) result += proportion;
		else if (proportion === 0) this.hasRowProportion0Child = true;
	}
	return result;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/GridSizer.js
var IsPlainObject$20 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$145 = __webpack_exports__Utils.Objects.GetValue;
var GridSizer = class extends Base$3 {
	constructor(scene, x, y, minWidth, minHeight, columnCount, rowCount, columnProportions, rowProportions, config) {
		if (IsPlainObject$20(x)) {
			config = x;
			x = GetValue$145(config, "x", 0);
			y = GetValue$145(config, "y", 0);
			minWidth = GetValue$145(config, "width", void 0);
			minHeight = GetValue$145(config, "height", void 0);
			columnCount = GetValue$145(config, "column", config.col || 0);
			rowCount = GetValue$145(config, "row", 0);
			columnProportions = GetValue$145(config, "columnProportions", 0);
			rowProportions = GetValue$145(config, "rowProportions", 0);
		} else if (IsPlainObject$20(minWidth)) {
			config = minWidth;
			minWidth = GetValue$145(config, "width", void 0);
			minHeight = GetValue$145(config, "height", void 0);
			columnCount = GetValue$145(config, "column", config.col || 0);
			rowCount = GetValue$145(config, "row", 0);
			columnProportions = GetValue$145(config, "columnProportions", 0);
			rowProportions = GetValue$145(config, "rowProportions", 0);
		} else if (IsPlainObject$20(columnCount)) {
			config = columnCount;
			columnCount = GetValue$145(config, "column", config.col || 0);
			rowCount = GetValue$145(config, "row", 0);
			columnProportions = GetValue$145(config, "columnProportions", 0);
			rowProportions = GetValue$145(config, "rowProportions", 0);
		} else if (IsPlainObject$20(columnProportions)) {
			config = columnProportions;
			columnProportions = GetValue$145(config, "columnProportions", 0);
			rowProportions = GetValue$145(config, "rowProportions", 0);
		}
		super(scene, x, y, minWidth, minHeight, config);
		this.type = "rexGridSizer";
		this.sizerChildren = [];
		this.addChildrenMap("items", this.sizerChildren);
		this.setCreateCellContainerCallback(GetValue$145(config, "createCellContainerCallback"));
		this.setIndentLeft(GetValue$145(config, "space.indentLeftOdd", 0), GetValue$145(config, "space.indentLeftEven", 0));
		this.setIndentTop(GetValue$145(config, "space.indentTopOdd", 0), GetValue$145(config, "space.indentTopEven", 0));
		this.resetGrid(columnCount, rowCount, columnProportions, rowProportions, GetValue$145(config, "space", void 0));
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		super.destroy(fromScene);
		this.columnProportions = void 0;
		this.rowProportions = void 0;
		this.columnWidth = void 0;
		this.rowHeight = void 0;
		this.createCellContainerCallback = void 0;
	}
	setColumnProportion(columnIndex, proportion) {
		if (columnIndex >= this.columnProportions.length) return this;
		this.columnProportions[columnIndex] = proportion;
		return this;
	}
	setRowProportion(rowIndex, proportion) {
		if (rowIndex >= this.rowProportions.length) return this;
		this.rowProportions[rowIndex] = proportion;
		return this;
	}
	get totalColumnProportions() {
		if (this._totalColumnProportions === void 0) this._totalColumnProportions = GetTotalColumnProportions.call(this);
		return this._totalColumnProportions;
	}
	get totalRowProportions() {
		if (this._totalRowProportions === void 0) this._totalRowProportions = GetTotalRowProportions.call(this);
		return this._totalRowProportions;
	}
	getChildAt(columnIndex, rowIndex) {
		return this.sizerChildren[rowIndex * this.columnCount + columnIndex];
	}
	childToGridIndex(child, out) {
		if (!child) return null;
		var index = this.sizerChildren.indexOf(child);
		if (index === -1) return null;
		if (out === void 0) out = {};
		out.x = index % this.columnCount;
		out.y = Math.floor(index / this.columnCount);
		return out;
	}
	getColumnWidth(columnIndex) {
		var colProportion = this.columnProportions[columnIndex];
		return colProportion === 0 ? this.columnWidth[columnIndex] : colProportion * this.proportionWidthLength;
	}
	getRowHeight(rowIndex) {
		var rowProportion = this.rowProportions[rowIndex];
		return rowProportion === 0 ? this.rowHeight[rowIndex] : rowProportion * this.proportionHeightLength;
	}
	setCreateCellContainerCallback(callback) {
		this.createCellContainerCallback = callback;
		return this;
	}
};
Object.assign(GridSizer.prototype, methods$20);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridsizer/Factory.js
ObjectFactory.register("gridSizer", function(x, y, minWidth, minHeight, columnCount, rowCount, columnProportions, rowProportion, config) {
	var gameObject = new GridSizer(this.scene, x, y, minWidth, minHeight, columnCount, rowCount, columnProportions, rowProportion, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.GridSizer", GridSizer);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthsizer/GetChildrenWidth.js
var GetChildrenWidth$1 = function(minimumMode) {
	if (this.rexSizer.hidden) return 0;
	if (minimumMode === void 0) minimumMode = true;
	var childrenWidth;
	if (this.orientation === 0) {
		if (minimumMode) childrenWidth = this.maxChildWidth;
		else childrenWidth = this.rexSizer.resolved ? this.wrapResult.width : void 0;
	} else childrenWidth = this.rexSizer.resolved ? this.wrapResult.width : void 0;
	if (childrenWidth === void 0) return;
	return childrenWidth + (this.space.left + this.space.right) * this.scaleX;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthsizer/GetChildrenHeight.js
var GetChildrenHeight$1 = function(minimumMode) {
	if (this.rexSizer.hidden) return 0;
	if (minimumMode === void 0) minimumMode = true;
	var childrenHeight;
	if (this.orientation === 1) {
		if (minimumMode) childrenHeight = this.maxChildHeight;
		else childrenHeight = this.rexSizer.resolved ? this.wrapResult.height : void 0;
	} else childrenHeight = this.rexSizer.resolved ? this.wrapResult.height : void 0;
	if (childrenHeight === void 0) return;
	return childrenHeight + (this.space.top + this.space.bottom) * this.scaleY;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthsizer/GetChildrenSizers.js
var GetChildrenSizers$1 = function(out) {
	if (out === void 0) out = [];
	var children = this.sizerChildren, child;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		child = children[i];
		if (child === "\n") continue;
		if (child.isRexSizer) out.push(child);
	}
	return out;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthsizer/PreLayout.js
var PreLayout = function() {
	this._maxChildWidth = void 0;
	this._maxChildHeight = void 0;
	this.wrapResult = void 0;
	this.rexSizer.resolved = false;
	PreLayout$3.call(this);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthsizer/LayoutChildren.js
var LayoutChildren$1 = function() {
	var horizontalWrap = this.orientation === 0;
	var innerLineWidth = horizontalWrap ? this.innerWidth : this.innerHeight;
	var child, childConfig, padding, justifySpace = 0, indentLeft, indentTop;
	var startX = this.innerLeft, startY = this.innerTop;
	var x, y, width, height, alignOffsetX, alignOffsetY;
	var lines = this.wrapResult.lines;
	var line, lineChlidren, remainderLineWidth;
	var itemX = startX, itemY = startY;
	for (var i = 0, icnt = lines.length; i < icnt; i++) {
		line = lines[i];
		lineChlidren = line.children;
		if (this.rtl) lineChlidren.reverse();
		if (horizontalWrap) {
			indentLeft = i % 2 ? this.space.indentLeftEven : this.space.indentLeftOdd;
			itemX = startX + indentLeft * this.scaleX;
		} else {
			indentTop = i % 2 ? this.space.indentTopEven : this.space.indentTopOdd;
			itemY = startY + indentTop * this.scaleY;
		}
		remainderLineWidth = innerLineWidth - (horizontalWrap ? line.width : line.height);
		switch (this.align) {
			case 0: break;
			case 1:
				if (horizontalWrap) itemX += remainderLineWidth;
				else itemY += remainderLineWidth;
				break;
			case 2:
				if (horizontalWrap) itemX += remainderLineWidth / 2;
				else itemY += remainderLineWidth / 2;
				break;
			case 3:
				justifySpace = GetJustifySpace(innerLineWidth, remainderLineWidth, this.justifyPercentage, lineChlidren.length);
				break;
			case 4:
				justifySpace = GetJustifySpace(innerLineWidth, remainderLineWidth, this.justifyPercentage, lineChlidren.length);
				if (justifySpace === 0) {
					if (horizontalWrap) itemX += remainderLineWidth;
					else itemY += remainderLineWidth;
				}
				break;
			case 5:
				justifySpace = GetJustifySpace(innerLineWidth, remainderLineWidth, this.justifyPercentage, lineChlidren.length);
				if (justifySpace === 0) {
					if (horizontalWrap) itemX += remainderLineWidth / 2;
					else itemY += remainderLineWidth / 2;
				}
		}
		var isFirstChild = true;
		for (var j = 0, jcnt = lineChlidren.length; j < jcnt; j++) {
			child = lineChlidren[j];
			if (child.rexSizer.hidden) continue;
			childConfig = child.rexSizer;
			padding = childConfig.padding;
			PreLayoutChild.call(this, child);
			if (horizontalWrap) x = itemX + padding.left * this.scaleX;
			else y = itemY + padding.top * this.scaleY;
			if (isFirstChild) isFirstChild = false;
			else if (horizontalWrap) x += this.space.item * this.scaleX;
			else y += this.space.item * this.scaleY;
			width = GetDisplayWidth(child);
			height = GetDisplayHeight(child);
			if (horizontalWrap) {
				indentTop = j % 2 ? this.space.indentTopEven : this.space.indentTopOdd;
				y = itemY + indentTop * this.scaleY + padding.top * this.scaleY;
				itemX = x + width + padding.right * this.scaleX + justifySpace;
			} else {
				indentLeft = j % 2 ? this.space.indentLeftEven : this.space.indentLeftOdd;
				x = itemX + indentLeft * this.scaleX + padding.left * this.scaleX;
				itemY = y + height + padding.top * this.scaleY + justifySpace;
			}
			alignOffsetX = (childConfig.alignOffsetX + childConfig.alignOffsetOriginX * width) * this.scaleX;
			alignOffsetY = (childConfig.alignOffsetY + childConfig.alignOffsetOriginY * height) * this.scaleY;
			LayoutChild.call(this, child, x, y, width, height, childConfig.align, alignOffsetX, alignOffsetY);
		}
		if (horizontalWrap) itemY += line.height + this.space.line * this.scaleY;
		else itemX += line.width + this.space.line * this.scaleX;
	}
};
var GetJustifySpace = function(total, remainder, justifyPercentage, childCount) {
	return remainder / total <= justifyPercentage ? remainder / (childCount - 1) : 0;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthsizer/HasWidthWrap.js
var HasWidthWrap = function() {
	if (this.orientation === 0) return true;
	return HasWidthWrap$2.call(this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthsizer/RunChildrenWrap.js
var RunChildrenWrap = function(lineWidth) {
	var out = {
		lines: [],
		width: 0,
		height: 0
	};
	var children = this.sizerChildren;
	var child, padding, childWidth, childHeight, remainder = 0, indentLeft, indentTop;
	var lines = out.lines, lastLine = void 0, newLine;
	if (this.orientation === 0) {
		for (var i = 0, cnt = children.length; i < cnt; i++) {
			child = children[i];
			if (child === "\n") {
				child = void 0;
				childWidth = 0;
				newLine = true;
			} else {
				if (child.rexSizer.hidden) continue;
				if (child.isRexSizer) child.runLayout(this);
				childWidth = this.getChildWidth(child);
				padding = child.rexSizer.padding;
				childWidth += (padding.left + padding.right) * this.scaleX;
				newLine = remainder < childWidth || lastLine === void 0;
			}
			if (newLine) {
				if (lastLine) {
					lastLine.width = lineWidth - (remainder + this.space.item * this.scaleX);
					out.width = Math.max(out.width, lastLine.width);
					out.height += lastLine.height + this.space.line * this.scaleY;
				}
				lastLine = {
					children: [],
					width: 0,
					height: 0
				};
				lines.push(lastLine);
				indentLeft = lines.length % 2 ? this.space.indentLeftOdd : this.space.indentLeftEven;
				remainder = lineWidth - indentLeft * this.scaleX;
			}
			remainder -= childWidth + this.space.item * this.scaleX;
			if (child) {
				lastLine.children.push(child);
				childHeight = this.getChildHeight(child);
				padding = child.rexSizer.padding;
				childHeight += (padding.top + padding.bottom) * this.scaleY;
				lastLine.height = Math.max(lastLine.height, childHeight);
			}
		}
		if (lastLine) {
			lastLine.width = lineWidth - (remainder + this.space.item * this.scaleX);
			out.width = Math.max(out.width, lastLine.width);
			out.height += lastLine.height;
		}
		out.height += Math.max(this.space.indentTopOdd, this.space.indentTopEven) * this.scaleY;
	} else {
		var lineHeight = lineWidth;
		for (var i = 0, cnt = children.length; i < cnt; i++) {
			child = children[i];
			if (child === "\n") {
				child = void 0;
				childWidth = 0;
				newLine = true;
			} else {
				if (child.rexSizer.hidden) continue;
				if (child.isRexSizer) child.layout();
				childHeight = this.getChildHeight(child);
				padding = child.rexSizer.padding;
				childHeight += (padding.top + padding.bottom) * this.scaleY;
				newLine = remainder < childHeight || lastLine === void 0;
			}
			if (newLine) {
				if (lastLine) {
					lastLine.height = lineHeight - (remainder + this.space.item * this.scaleY);
					out.height = Math.max(out.height, lastLine.height);
					out.width += lastLine.width + this.space.line * this.scaleX;
				}
				lastLine = {
					children: [],
					width: 0,
					height: 0
				};
				lines.push(lastLine);
				indentTop = lines.length % 2 ? this.space.indentTopOdd : this.space.indentTopEven;
				remainder = lineHeight - indentTop * this.scaleY;
			}
			remainder -= childHeight + this.space.item * this.scaleY;
			if (child) {
				lastLine.children.push(child);
				childWidth = this.getChildWidth(child);
				padding = child.rexSizer.padding;
				childWidth += (padding.left + padding.right) * this.scaleX;
				lastLine.width = Math.max(lastLine.width, childWidth);
			}
		}
		if (lastLine) {
			lastLine.height = lineHeight - (remainder + this.space.item * this.scaleY);
			out.height = Math.max(out.height, lastLine.height);
			out.width += lastLine.width;
		}
		out.width += Math.max(this.space.indentLeftOdd, this.space.indentLeftEven) * this.scaleX;
	}
	return out;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthsizer/RunWidthWrap.js
var RunWidthWrap = function(width) {
	if (this.wrapResult) return;
	if (this.orientation === 0) {
		var innerWidth = width - (this.space.left + this.space.right) * this.scaleX;
		this.wrapResult = RunChildrenWrap.call(this, innerWidth);
		this.rexSizer.resolved = true;
		RunWidthWrap$3.call(this, width);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthsizer/HasHeightWrap.js
var HasHeightWrap = function() {
	if (this.orientation === 1) return true;
	return HasHeightWrap$2.call(this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthsizer/RunHeightWrap.js
var RunHeightWrap = function(height) {
	if (this.wrapResult) return;
	if (this.orientation === 1) {
		var innerHeight = height - (this.space.top + this.space.bottom) * this.scaleY;
		this.wrapResult = RunChildrenWrap.call(this, innerHeight);
		this.rexSizer.resolved = true;
		RunHeightWrap$3.call(this, height);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthsizer/GetExpandedChildWidth.js
var GetExpandedChildWidth = function(child, parentWidth) {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthsizer/GetExpandedChildHeight.js
var GetExpandedChildHeight = function(child, parentHeight) {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthsizer/GetNearestChildIndex.js
var DistanceBetween$2 = __webpack_exports__Math.Distance.Between;
var GetNearestChildIndex = function(x, y) {
	var children = this.sizerChildren;
	if (children.length === 0) return -1;
	var nearestIndex = -1, minDistance = Infinity;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		if (Math.abs(child.centerY - y) > child.height / 2) continue;
		var distance = DistanceBetween$2(child.left, child.centerY, x, y);
		if (minDistance > distance) {
			minDistance = distance;
			nearestIndex = i;
		}
		var nextChild = children[i + 1];
		if (nextChild && nextChild.y === child.y) continue;
		var distance = DistanceBetween$2(child.right, child.centerY, x, y);
		if (minDistance > distance) {
			minDistance = distance;
			nearestIndex = i + 1;
		}
	}
	return nearestIndex;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthsizer/AddChildMethods.js
var IsPlainObject$19 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$144 = __webpack_exports__Utils.Objects.GetValue;
var ALIGN_CENTER = __webpack_exports__Display.Align.CENTER;
var Add$3 = function(gameObject, paddingConfig, childKey, index) {
	if (gameObject === "\n") {
		this.addNewLine();
		return this;
	}
	var offsetX, offsetY;
	var offsetOriginX, offsetOriginY;
	AddChild$1.call(this, gameObject);
	if (IsPlainObject$19(paddingConfig)) {
		var config = paddingConfig;
		paddingConfig = GetValue$144(config, "padding", 0);
		childKey = GetValue$144(config, "key", void 0);
		index = GetValue$144(config, "index", void 0);
		offsetX = GetValue$144(config, "offsetX", 0);
		offsetY = GetValue$144(config, "offsetY", 0);
		offsetOriginX = GetValue$144(config, "offsetOriginX", 0);
		offsetOriginY = GetValue$144(config, "offsetOriginY", 0);
	}
	if (paddingConfig === void 0) paddingConfig = 0;
	if (offsetX === void 0) offsetX = 0;
	if (offsetY === void 0) offsetY = 0;
	if (offsetOriginX === void 0) offsetOriginX = 0;
	if (offsetOriginY === void 0) offsetOriginY = 0;
	var config = this.getSizerConfig(gameObject);
	config.align = ALIGN_CENTER;
	config.padding = GetBoundsConfig_default(paddingConfig);
	config.alignOffsetX = offsetX;
	config.alignOffsetY = offsetY;
	config.alignOffsetOriginX = offsetOriginX;
	config.alignOffsetOriginY = offsetOriginY;
	if (index === void 0 || index >= this.sizerChildren.length) this.sizerChildren.push(gameObject);
	else this.sizerChildren.splice(index, 0, gameObject);
	if (childKey !== void 0) this.addChildrenMap(childKey, gameObject);
	return this;
};
var AddChildMethods_default$4 = {
	add(gameObject, paddingConfig, childKey) {
		if (IsArray(gameObject)) {
			var gameObjects = gameObject;
			for (var i = 0, cnt = gameObjects.length; i < cnt; i++) Add$3.call(this, gameObjects[i], paddingConfig);
		} else Add$3.call(this, gameObject, paddingConfig, childKey);
		return this;
	},
	addNewLine() {
		this.sizerChildren.push("\n");
		return this;
	},
	insert(index, gameObject, paddingConfig, childKey) {
		Add$3.call(this, gameObject, paddingConfig, childKey, index);
		return this;
	},
	insertAtPosition(x, y, gameObject, paddingConfig, childKey) {
		var index = GetNearestChildIndex.call(this, x, y);
		if (index === -1) index = void 0;
		this.insert(index, gameObject, paddingConfig, childKey);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthsizer/RemoveChildMethods.js
var RemoveItem$1 = __webpack_exports__Utils.Array.Remove;
var RemoveChildMethods_default$3 = {
	remove(gameObject, destroyChild) {
		if (this.getParentSizer(gameObject) !== this) return this;
		RemoveItem$1(this.sizerChildren, gameObject);
		RemoveChild.call(this, gameObject, destroyChild);
		return this;
	},
	removeAll(destroyChild) {
		for (var i = this.sizerChildren.length - 1; i >= 0; i--) this.remove(this.sizerChildren[i], destroyChild);
		return this;
	},
	clear(destroyChild) {
		this.sizerChildren.length = 0;
		ClearChildren.call(this, destroyChild);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthsizer/Methods.js
var methods$19 = {
	getChildrenWidth: GetChildrenWidth$1,
	getChildrenHeight: GetChildrenHeight$1,
	getChildrenSizers: GetChildrenSizers$1,
	preLayout: PreLayout,
	layoutChildren: LayoutChildren$1,
	hasWidthWrap: HasWidthWrap,
	runWidthWrap: RunWidthWrap,
	hasHeightWrap: HasHeightWrap,
	runHeightWrap: RunHeightWrap,
	getExpandedChildWidth: GetExpandedChildWidth,
	getExpandedChildHeight: GetExpandedChildHeight
};
Object.assign(methods$19, AddChildMethods_default$4, RemoveChildMethods_default$3, SortChildrenMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthsizer/GetMaxChildWidth.js
var GetMaxChildWidth = function(children) {
	if (children === void 0) children = this.sizerChildren;
	var result = 0;
	var child, childWidth;
	var hasUnknownChildWidth = false;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		child = children[i];
		if (child === "\n") continue;
		childWidth = this.getChildWidth(child);
		if (childWidth === void 0) hasUnknownChildWidth = true;
		if (hasUnknownChildWidth) continue;
		result = Math.max(childWidth, result);
	}
	if (hasUnknownChildWidth) return;
	return result;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthsizer/GetMaxChildHeight.js
var GetMaxChildHeight = function(children) {
	if (children === void 0) children = this.sizerChildren;
	var result = 0;
	var child, childHeight;
	var hasUnknownChildHeight = false;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		child = children[i];
		if (child === "\n") continue;
		childHeight = this.getChildHeight(child);
		if (childHeight === void 0) hasUnknownChildHeight = true;
		if (hasUnknownChildHeight) continue;
		result = Math.max(childHeight, result);
	}
	if (hasUnknownChildHeight) return;
	return result;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthsizer/FixWidthSizer.js
var IsPlainObject$18 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$143 = __webpack_exports__Utils.Objects.GetValue;
var FixWidthSizer = class extends Base$3 {
	constructor(scene, x, y, minWidth, minHeight, config) {
		if (IsPlainObject$18(x)) {
			config = x;
			x = GetValue$143(config, "x", 0);
			y = GetValue$143(config, "y", 0);
			minWidth = GetValue$143(config, "width", void 0);
			minHeight = GetValue$143(config, "height", void 0);
		} else if (IsPlainObject$18(minWidth)) {
			config = minWidth;
			minWidth = GetValue$143(config, "width", void 0);
			minHeight = GetValue$143(config, "height", void 0);
		}
		super(scene, x, y, minWidth, minHeight, config);
		this.type = "rexFixWidthSizer";
		this.sizerChildren = [];
		this.runChildrenWrapFlag = true;
		this.setOrientation(GetValue$143(config, "orientation", 0));
		this.setItemSpacing(GetValue$143(config, "space.item", 0));
		this.setLineSpacing(GetValue$143(config, "space.line", 0));
		this.setIntentLeft(GetValue$143(config, "space.indentLeftOdd", 0), GetValue$143(config, "space.indentLeftEven", 0));
		this.setIntentTop(GetValue$143(config, "space.indentTopOdd", 0), GetValue$143(config, "space.indentTopEven", 0));
		this.setAlign(GetValue$143(config, "align", 0));
		this.setJustifyPercentage(GetValue$143(config, "justifyPercentage", .25));
		this.setRTL(GetValue$143(config, "rtl", false));
		this.wrapResult = void 0;
		this.addChildrenMap("items", this.sizerChildren);
	}
	setOrientation(orientation) {
		this.orientation = GetOrientationMode_default(orientation);
		return this;
	}
	setItemSpacing(space) {
		this.space.item = space;
		return this;
	}
	setLineSpacing(space) {
		this.space.line = space;
		return this;
	}
	setIntentLeft(odd, even) {
		this.space.indentLeftOdd = odd;
		this.space.indentLeftEven = even;
		return this;
	}
	setIntentTop(odd, even) {
		this.space.indentTopOdd = odd;
		this.space.indentTopEven = even;
		return this;
	}
	setAlign(align) {
		if (typeof align === "string") align = ALIGN[align];
		this.align = align;
		return this;
	}
	setJustifyPercentage(value) {
		this.justifyPercentage = value;
		return this;
	}
	setRTL(enabled) {
		if (enabled === void 0) enabled = true;
		this.rtl = enabled;
		return this;
	}
	get maxChildWidth() {
		if (this._maxChildWidth === void 0) this._maxChildWidth = GetMaxChildWidth.call(this);
		return this._maxChildWidth;
	}
	get maxChildHeight() {
		if (this._maxChildHeight === void 0) this._maxChildHeight = GetMaxChildHeight.call(this);
		return this._maxChildHeight;
	}
};
var ALIGN = {
	left: 0,
	top: 0,
	right: 1,
	bottom: 1,
	center: 2,
	justify: 3,
	"justify-left": 3,
	"justify-top": 3,
	"justify-right": 4,
	"justify-bottom": 4,
	"justify-center": 5
};
Object.assign(FixWidthSizer.prototype, methods$19);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthsizer/Factory.js
ObjectFactory.register("fixWidthSizer", function(x, y, minWidth, minHeight, config) {
	var gameObject = new FixWidthSizer(this.scene, x, y, minWidth, minHeight, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.FixWidthSizer", FixWidthSizer);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/overlapsizer/Factory.js
ObjectFactory.register("overlapSizer", function(x, y, minWidth, minHeight, config) {
	var gameObject = new OverlapSizer(this.scene, x, y, minWidth, minHeight, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.OverlapSizer", OverlapSizer);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/space/Factory.js
ObjectFactory.register("space", function() {
	return new Space(this.scene);
});
SetValue(window, "RexPlugins.UI.Space", Space);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/label/Factory.js
ObjectFactory.register("label", function(config) {
	var gameObject = new Label(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.Label", Label);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/build/DecorateGameObject.js
var Properties = [
	"alpha",
	"tint",
	"flipX",
	"flipY"
];
var DecorateGameObject = function(gameObject, config) {
	if (!config) return gameObject;
	for (var i = 0, cnt = Properties.length; i < cnt; i++) {
		var propertyName = Properties[i];
		if (propertyName in config && propertyName in gameObject) gameObject[propertyName] = config[propertyName];
	}
	if ("origin" in config && "originX" in gameObject) gameObject.setOrigin(config.origin);
	else {
		var originX, originY;
		if ("originX" in config && "originX" in gameObject) originX = config.originX;
		if ("originY" in config && "originY" in gameObject) originY = config.originY;
		if (originX !== void 0 && originY !== void 0) gameObject.setOrigin(originX, originY);
	}
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/build/CreateBackground.js
var CreateBackground$2 = function(scene, config) {
	var gameObjectType;
	if (config) {
		if (config.hasOwnProperty("$type")) gameObjectType = config.$type;
		else if (config.hasOwnProperty("barColor")) gameObjectType = "bar";
		else if (config.hasOwnProperty("leftWidth")) gameObjectType = "nineSlice";
		else if (config.hasOwnProperty("key")) gameObjectType = "image";
	}
	var gameObject;
	switch (gameObjectType) {
		case "bar":
			gameObject = new StatesBarRectangle(scene, config);
			break;
		case "image":
			gameObject = new StatesImage(scene, config);
			break;
		case "nineSlice":
			if (!config.hasOwnProperty("stretchMode")) gameObject = new StatesNineSlice(scene, config);
			else gameObject = new StatesNinePatch(scene, config);
			break;
		default: gameObject = new StatesRoundRectangle(scene, config);
	}
	DecorateGameObject(gameObject, config);
	scene.add.existing(gameObject);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/scrollable/methods/AddHeader.js
var AddHeader = function(header, space, align, expand) {
	var currentHeader = this.childrenMap.header;
	if (currentHeader) this.remove(currentHeader, true);
	var padding;
	if (this.orientation === 1) padding = { bottom: space };
	else padding = { right: space };
	if (expand === void 0) expand = true;
	this.insertBefore(this.childrenMap.scrollableSizer, header, {
		proportion: 0,
		align,
		padding,
		expand
	});
	this.addChildrenMap("header", header);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/scrollable/methods/AddFooter.js
var AddFooter = function(footer, space, align, expand) {
	var currentFooter = this.childrenMap.footer;
	if (currentFooter) this.remove(currentFooter, true);
	var padding;
	if (this.orientation === 1) padding = { top: space };
	else padding = { left: space };
	if (expand === void 0) expand = true;
	this.insertAfter(this.childrenMap.scrollableSizer, footer, {
		proportion: 0,
		align,
		padding,
		expand
	});
	this.addChildrenMap("footer", footer);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/scrollable/methods/ResizeController.js
var ResizeController = function() {
	switch (this.scrollMode) {
		case 0:
		case 1:
			SetControllerBounds.call(this);
			this.updateController();
			HideUnscrollableSlider.call(this);
			AdaptThumbSize.call(this);
			break;
		default:
			SetControllerBounds.call(this, "y");
			SetControllerBounds.call(this, "x");
			this.updateController();
			HideUnscrollableSlider.call(this, "y");
			HideUnscrollableSlider.call(this, "x");
			AdaptThumbSize.call(this, "y");
			AdaptThumbSize.call(this, "x");
	}
	return this;
};
var SetControllerBounds = function(axis) {
	var bound0, bound1;
	var scroller, slider;
	switch (this.scrollMode) {
		case 0:
		case 1:
			bound0 = this.topChildOY;
			bound1 = this.bottomChildOY;
			scroller = this.childrenMap.scroller;
			slider = this.childrenMap.slider;
			axis = this.scrollMode === 0 ? "Y" : "X";
			break;
		default:
			axis = axis.toUpperCase();
			if (axis === "Y") {
				bound0 = this.topChildOY;
				bound1 = this.bottomChildOY;
			} else {
				bound0 = this.leftChildOX;
				bound1 = this.rightChildOX;
			}
			scroller = this.childrenMap[`scroller${axis}`];
			slider = this.childrenMap[`slider${axis}`];
	}
	var scale = axis === "Y" ? this.scaleY : this.scaleX;
	bound1 *= scale;
	if (scroller) scroller.setBounds(bound0, bound1);
	if (slider) {
		slider.setEnable(bound0 !== bound1);
		if (slider.tickLength) slider.setTick(slider.tickLength, bound0, bound1);
	}
};
var HideUnscrollableSlider = function(axis) {
	switch (this.scrollMode) {
		case 0:
		case 1:
			var slider = this.childrenMap.slider;
			if (slider && this.hideUnscrollableSlider) this.setChildVisible(slider, this.isOverflow);
			var scroller = this.childrenMap.scroller;
			if (scroller && this.disableUnscrollableDrag) scroller.setEnable(this.isOverflow);
			break;
		default:
			axis = axis.toUpperCase();
			var isOverflow = this[`isOverflow${axis}`];
			var slider = this.childrenMap[`slider${axis}`];
			var hideUnscrollableSlider = this[`hideUnscrollableSlider${axis}`];
			if (slider && hideUnscrollableSlider) this.setChildVisible(slider, isOverflow);
			var scroller = this.childrenMap.scroller;
			var disableUnscrollableDrag = this[`disableUnscrollableDrag${axis}`];
			if (scroller && disableUnscrollableDrag) scroller.setEnable(isOverflow);
	}
};
var AdaptThumbSize = function(axis) {
	switch (this.scrollMode) {
		case 0:
		case 1:
			if (!this.adaptThumbSizeMode) return;
			var slider = this.childrenMap.slider;
			if (!slider) return;
			var ratio = Math.min(this.childVisibleHeight / this.childHeight, 1);
			var track = slider.childrenMap.track;
			var thumb = slider.childrenMap.thumb;
			var minThumbSize = this.minThumbSize;
			if (this.scrollMode === 0) {
				var newHeight = track.displayHeight * ratio;
				if (minThumbSize !== void 0 && newHeight < minThumbSize) newHeight = minThumbSize;
				ResizeGameObject(thumb, void 0, newHeight);
			} else {
				var newWidth = track.displayWidth * ratio;
				if (minThumbSize !== void 0 && newWidth < minThumbSize) newWidth = minThumbSize;
				ResizeGameObject(thumb, newWidth, void 0);
			}
			LayoutSlider(slider);
			break;
		default:
			axis = axis.toUpperCase();
			if (!this[`adaptThumb${axis}SizeMode`]) return;
			var slider = this.childrenMap[`slider${axis}`];
			if (!slider) return;
			var track = slider.childrenMap.track;
			var thumb = slider.childrenMap.thumb;
			var minThumbSize = this[`minThumb${axis}Size`];
			if (axis === "Y") {
				var ratio = Math.min(this.childVisibleHeight / this.childHeight, 1);
				var newHeight = track.displayHeight * ratio;
				if (minThumbSize !== void 0 && newHeight < minThumbSize) newHeight = minThumbSize;
				ResizeGameObject(thumb, void 0, newHeight);
			} else {
				var ratio = Math.min(this.childVisibleWidth / this.childWidth, 1);
				var newWidth = track.displayWidth * ratio;
				if (minThumbSize !== void 0 && newWidth < minThumbSize) newWidth = minThumbSize;
				ResizeGameObject(thumb, newWidth, void 0);
			}
			LayoutSlider(slider);
	}
};
var LayoutSlider = function(slider) {
	var minWidthSave = slider.minWidth;
	var minHeightSave = slider.minHeight;
	slider.minWidth = slider.width;
	slider.minHeight = slider.height;
	slider.layout();
	slider.minWidth = minWidthSave;
	slider.minHeight = minHeightSave;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/scrollable/methods/UpdateController.js
var UpdateController = function() {
	switch (this.scrollMode) {
		case 0:
		case 1:
			var scroller = this.childrenMap.scroller;
			var slider = this.childrenMap.slider;
			if (scroller) scroller.setValue(this.childOY);
			if (slider) slider.setValue(this.t);
			break;
		default:
			var scrollerY = this.childrenMap.scrollerY;
			var sliderY = this.childrenMap.sliderY;
			var scrollerX = this.childrenMap.scrollerX;
			var sliderX = this.childrenMap.sliderX;
			if (scrollerY) scrollerY.setValue(this.childOY);
			if (sliderY) sliderY.setValue(this.t);
			if (scrollerX) scrollerX.setValue(this.childOX);
			if (sliderX) sliderX.setValue(this.s);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/scrollable/methods/ChildPositionMethods.js
var Clamp$10 = __webpack_exports__Math.Clamp;
var ChildPositionMethods_default = {
	setChildOY(value, clamp) {
		if (clamp === void 0) clamp = false;
		if (clamp) value = Clamp$10(value, this.bottomChildOY, this.topChildOY);
		this.childOY = value;
		return this;
	},
	addChildOY(inc, clamp) {
		this.setChildOY(this.childOY + inc, clamp);
		return this;
	},
	setT(value, clamp) {
		if (clamp === void 0) clamp = false;
		if (clamp) value = Clamp$10(value, 0, 1);
		this.t = value;
		return this;
	},
	addT(inc, clamp) {
		this.setT(this.t + inc, clamp);
		return this;
	},
	scrollToTop() {
		this.t = 0;
		return this;
	},
	scrollToBottom() {
		this.t = 1;
		if (this.t === 0) return this;
		do
			this.t = 1;
		while (this.t !== 1);
		return this;
	},
	setChildOX(value, clamp) {
		if (clamp === void 0) clamp = false;
		if (clamp) value = Clamp$10(value, this.leftChildOX, this.rightChildOX);
		this.childOX = value;
		return this;
	},
	addChildOX(inc, clamp) {
		this.setChildOX(this.childOX + inc, clamp);
		return this;
	},
	setS(value, clamp) {
		if (clamp === void 0) clamp = false;
		if (clamp) value = Clamp$10(value, 0, 1);
		this.s = value;
		return this;
	},
	addS(inc, clamp) {
		this.setS(this.s + inc, clamp);
		return this;
	},
	scrollToLeft() {
		this.s = 0;
		return this;
	},
	scrollToRight() {
		this.s = 1;
		if (this.s === 0) return this;
		do
			this.s = 1;
		while (this.s !== 1);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/scrollable/methods/Methods.js
var Methods$11 = {
	addHeader: AddHeader,
	addFooter: AddFooter,
	resizeController: ResizeController,
	updateController: UpdateController
};
Object.assign(Methods$11, ChildPositionMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/ScrollModeConst.js
var ScrollModeConst_default = {
	v: 0,
	vertical: 0,
	y: 0,
	h: 1,
	horizontal: 1,
	x: 1,
	xy: 2,
	vh: 2
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/GetScrollMode.js
var GetScrollMode = function(config, key) {
	if (key === void 0) key = "scrollMode";
	if (!config.hasOwnProperty(key)) config[key] = GetDefaultScrollMode(config);
	var scrollMode = config[key];
	if (typeof scrollMode === "string") scrollMode = ScrollModeConst_default[scrollMode];
	return scrollMode;
};
var GetDefaultScrollMode = function(config) {
	var hasSliderY = !!config.sliderY || !!config.scrollerY;
	var hasSliderX = !!config.sliderX || !!config.scrollerX;
	var scrollMode;
	if (hasSliderY && hasSliderX) scrollMode = 2;
	else if (hasSliderY) scrollMode = 0;
	else if (hasSliderX) scrollMode = 1;
	else scrollMode = 0;
	return scrollMode;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/scrollable/methods/AddChild.js
var GetValue$142 = __webpack_exports__Utils.Objects.GetValue;
var AddChild = function(topPatent, childParent, config) {
	var childConfig = GetValue$142(config, "child");
	var child = GetValue$142(childConfig, "gameObject", void 0);
	if (child) {
		var childSpace = GetValue$142(config, "space.child", 0);
		topPatent.childMargin = {};
		var childMargin = topPatent.childMargin;
		var childPadding = {};
		if (typeof childSpace === "number") switch (topPatent.scrollMode) {
			case 0:
			case 1:
				childMargin.top = 0;
				childMargin.bottom = 0;
				childMargin.left = 0;
				childMargin.right = 0;
				break;
			default:
				childMargin.top = childSpace;
				childMargin.bottom = childSpace;
				childMargin.left = childSpace;
				childMargin.right = childSpace;
		}
		else switch (topPatent.scrollMode) {
			case 0:
				childMargin.top = GetValue$142(childSpace, "top", 0);
				childMargin.bottom = GetValue$142(childSpace, "bottom", 0);
				childPadding.left = GetValue$142(childSpace, "left", 0);
				childPadding.right = GetValue$142(childSpace, "right", 0);
				break;
			case 1:
				childMargin.top = GetValue$142(childSpace, "left", 0);
				childMargin.bottom = GetValue$142(childSpace, "right", 0);
				childPadding.top = GetValue$142(childSpace, "top", 0);
				childPadding.bottom = GetValue$142(childSpace, "bottom", 0);
				break;
			default:
				childMargin.top = GetValue$142(childSpace, "top", 0);
				childMargin.bottom = GetValue$142(childSpace, "bottom", 0);
				childMargin.left = GetValue$142(childSpace, "left", 0);
				childMargin.right = GetValue$142(childSpace, "right", 0);
		}
		childParent.add(child, {
			column: 1,
			row: 1,
			align: GetValue$142(childConfig, "align", "center"),
			padding: childPadding,
			expand: {
				width: GetValue$142(childConfig, "expandWidth", true),
				height: GetValue$142(childConfig, "expandHeight", true)
			}
		});
	}
	topPatent.addChildrenMap("child", child);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/slider/methods/PositionToPercent.js
var Percent$3 = __webpack_exports__Math.Percent;
var PositionToPercent = function(startPoint, endPoint, currentPoint) {
	var value;
	if (startPoint.y === endPoint.y) value = Percent$3(currentPoint.x, startPoint.x, endPoint.x);
	else if (startPoint.x === endPoint.x) value = Percent$3(currentPoint.y, startPoint.y, endPoint.y);
	return value;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/slider/methods/input/GetValueByPosition.js
var GetValueByPosition = function(x, y) {
	tmpPoint$3.x = x;
	tmpPoint$3.y = y;
	var startPoint, endPoint;
	if (!this.reverseAxis) {
		startPoint = this.getStartPoint();
		endPoint = this.getEndPoint();
	} else {
		startPoint = this.getEndPoint();
		endPoint = this.getStartPoint();
	}
	return PositionToPercent(startPoint, endPoint, tmpPoint$3);
};
var tmpPoint$3 = {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/slider/methods/input/OnDragThumb.js
var OnDragThumb = function(pointer, dragX, dragY) {
	if (!this.enable || !this.inputActive) return;
	this.value = GetValueByPosition.call(this, dragX, dragY);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/slider/methods/input/OnTouchTrack.js
var OnTouchTrack = function(pointer) {
	if (!this.enable || !this.inputActive) return;
	if (!pointer.isDown) return;
	var value = GetValueByPosition.call(this, pointer.worldX, pointer.worldY);
	this.stopEaseValue();
	if (this.easeValueDuration === 0 || Math.abs(this.value - value) < .1) this.value = value;
	else this.easeValueTo(value);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/slider/methods/input/RegisterInputEvents.js
var GetValue$141 = __webpack_exports__Utils.Objects.GetValue;
var RegisterInputEvents = function(config) {
	this.inputActive = false;
	var inputMode = GetValue$141(config, "input", 0);
	if (typeof inputMode === "string") inputMode = INPUTMODE[inputMode];
	var thumb = this.childrenMap.thumb;
	switch (inputMode) {
		case 0:
			if (thumb) {
				thumb.setInteractive();
				this.scene.input.setDraggable(thumb);
				thumb.on("dragstart", function(pointer) {
					this.inputActive = true;
					this.eventEmitter.emit("inputstart", pointer);
				}, this).on("dragend", function(pointer) {
					this.inputActive = false;
					this.eventEmitter.emit("inputend", pointer);
				}, this).on("drag", OnDragThumb, this);
			}
			break;
		case 1:
			this.setInteractive().on("pointerdown", function(pointer) {
				this.inputActive = true;
				this.eventEmitter.emit("inputstart", pointer);
			}, this).on("pointerdown", OnTouchTrack, this);
			var OnReleaseTrack = function(pointer) {
				if (!this.inputActive) return;
				this.inputActive = false;
				this.eventEmitter.emit("inputend", pointer);
			};
			this.scene.input.on("pointerup", OnReleaseTrack, this).on("pointermove", OnTouchTrack, this);
			this.once("destroy", function() {
				this.scene.input.on("pointerup", OnReleaseTrack, this).off("pointermove", OnTouchTrack, this);
			}, this);
	}
};
var INPUTMODE = {
	pan: 0,
	drag: 0,
	click: 1,
	none: -1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/slider/methods/GetThumbAlignPoint.js
var GetThumbAlignPoint = function(align, out) {
	if (out === void 0) out = tmpPoint$2;
	var thumb = this.childrenMap.thumb;
	var currentX = thumb.x;
	var currentY = thumb.y;
	AlignIn(thumb, this.innerLeft, this.innerTop, this.innerWidth, this.innerHeight, align);
	out.x = thumb.x;
	out.y = thumb.y;
	thumb.x = currentX;
	thumb.y = currentY;
	return out;
};
var tmpPoint$2 = {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/slider/methods/GetStartPoint.js
var AlignLeft$1 = __webpack_exports__Display.Align.LEFT_CENTER;
var AlignTop$1 = __webpack_exports__Display.Align.TOP_CENTER;
var GetStartPoint = function(out) {
	if (out === void 0) out = tmpPoint$1;
	if (this.childrenMap.thumb) {
		var align = this.orientation === 0 ? AlignLeft$1 : AlignTop$1;
		GetThumbAlignPoint.call(this, align, out);
	} else if (this.orientation === 0) {
		out.x = this.innerLeft + 1;
		out.y = this.centerY;
	} else {
		out.x = this.centerX;
		out.y = this.innerTop + 1;
	}
	return out;
};
var tmpPoint$1 = {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/slider/methods/GetEndPoint.js
var AlignRight$1 = __webpack_exports__Display.Align.RIGHT_CENTER;
var AlignBottom$1 = __webpack_exports__Display.Align.BOTTOM_CENTER;
var GetEndoint = function(out) {
	if (out === void 0) out = tmpPoint;
	if (this.childrenMap.thumb) {
		var align = this.orientation === 0 ? AlignRight$1 : AlignBottom$1;
		GetThumbAlignPoint.call(this, align, out);
	} else if (this.orientation === 0) {
		out.x = this.innerRight - 1;
		out.y = this.centerY;
	} else {
		out.x = this.centerX;
		out.y = this.innerBottom - 1;
	}
	return out;
};
var tmpPoint = {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/slider/methods/PercentToPosition.js
var Linear$4 = __webpack_exports__Math.Linear;
var PercentToPosition = function(t, startPoint, endPoint, out) {
	if (out === void 0) out = tmpOut;
	out.x = Linear$4(startPoint.x, endPoint.x, t);
	out.y = Linear$4(startPoint.y, endPoint.y, t);
	return out;
};
var tmpOut = {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/slider/methods/UpdateThumb.js
var UpdateThumb = function(t) {
	var thumb = this.childrenMap.thumb;
	if (thumb === void 0) return this;
	if (t === void 0) t = this.value;
	var startPoint, endPoint;
	if (!this.reverseAxis) {
		startPoint = this.getStartPoint();
		endPoint = this.getEndPoint();
	} else {
		startPoint = this.getEndPoint();
		endPoint = this.getStartPoint();
	}
	PercentToPosition(t, startPoint, endPoint, thumb);
	thumb.x += this.thumbOffsetX;
	thumb.y += this.thumbOffsetY;
	this.resetChildPositionState(thumb);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/slider/methods/UpdateIndicator.js
var AlignLeft = __webpack_exports__Display.Align.LEFT_CENTER;
var AlignTop = __webpack_exports__Display.Align.TOP_CENTER;
var AlignRight = __webpack_exports__Display.Align.RIGHT_CENTER;
var AlignBottom = __webpack_exports__Display.Align.BOTTOM_CENTER;
var UpdateIndicator = function(t) {
	var indicator = this.childrenMap.indicator;
	if (indicator === void 0) return this;
	if (t === void 0) t = this.value;
	var reverseAxis = this.reverseAxis;
	var newWidth, newHeight;
	var thumb = this.childrenMap.thumb;
	if (thumb) {
		if (this.orientation === 0) {
			var thumbWidth = GetDisplayWidth(thumb);
			if (!reverseAxis) {
				var thumbLeft = thumb.x - thumbWidth * thumb.originX;
				newWidth = thumbLeft + thumbWidth - this.left;
			} else {
				var thumbLeft = thumb.x - thumbWidth * thumb.originX;
				newWidth = this.right - thumbLeft;
			}
		} else {
			var thumbHeight = GetDisplayHeight(thumb);
			if (!reverseAxis) {
				var thumbTop = thumb.y - thumbHeight * thumb.originY;
				newHeight = thumbTop + thumbHeight - this.top;
			} else {
				var thumbTop = thumb.y - thumbHeight * thumb.originY;
				newHeight = this.bottom - thumbTop;
			}
		}
	} else if (this.orientation === 0) newWidth = this.width * t;
	else newHeight = this.height * t;
	ResizeGameObject(indicator, newWidth, newHeight);
	var align;
	if (!reverseAxis) align = this.orientation === 0 ? AlignLeft : AlignTop;
	else align = this.orientation === 0 ? AlignRight : AlignBottom;
	QuickSet(indicator, this, align);
	this.resetChildPositionState(indicator);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/slider/Slider.js
var GetValue$140 = __webpack_exports__Utils.Objects.GetValue;
var IsPlainObject$17 = __webpack_exports__Utils.Objects.IsPlainObject;
var Clamp$9 = __webpack_exports__Math.Clamp;
var SnapTo$2 = __webpack_exports__Math.Snap.To;
var Slider = class extends ProgressBase_default(Sizer) {
	constructor(scene, config) {
		super(scene, config);
		this.type = "rexSlider";
		this.bootProgressBase(config);
		this.reverseAxis = GetValue$140(config, "reverseAxis", false);
		var background = GetValue$140(config, "background", void 0);
		var track = GetValue$140(config, "track", void 0);
		var indicator = GetValue$140(config, "indicator", void 0);
		var thumb = GetValue$140(config, "thumb", void 0);
		if (background) {
			if (IsPlainObject$17(background)) background = CreateBackground$2(scene, background);
			this.addBackground(background);
		}
		if (track) {
			if (IsPlainObject$17(track)) track = CreateBackground$2(scene, track);
			this.add(track, {
				proportion: 1,
				expand: true,
				minWidth: this.orientation === 0 ? 0 : void 0,
				minHeight: this.orientation === 1 ? 0 : void 0
			});
		}
		if (indicator) {
			if (IsPlainObject$17(indicator)) indicator = CreateBackground$2(scene, indicator);
			this.pin(indicator);
		}
		if (thumb) {
			if (IsPlainObject$17(thumb)) thumb = CreateBackground$2(scene, thumb);
			this.pin(thumb);
			var thumbOffsetX = GetValue$140(config, "thumbOffsetX", 0);
			var thumbOffsetY = GetValue$140(config, "thumbOffsetY", 0);
			this.setThumbOffset(thumbOffsetX, thumbOffsetY);
		}
		this.addChildrenMap("background", background);
		this.addChildrenMap("track", track);
		this.addChildrenMap("indicator", indicator);
		this.addChildrenMap("thumb", thumb);
		this.setEnable(GetValue$140(config, "enable", void 0));
		var gap = GetValue$140(config, "tick", void 0);
		if (gap === void 0) gap = GetValue$140(config, "gap", void 0);
		this.setGap(gap);
		RegisterInputEvents.call(this, config);
		this.setValue(GetValue$140(config, "value", 0), GetValue$140(config, "min", void 0), GetValue$140(config, "max", void 0));
	}
	setEnable(enable) {
		if (enable === void 0) enable = true;
		this.enable = enable;
		return this;
	}
	setGap(gap, min, max) {
		if (gap && min !== void 0) gap = gap / (max - min);
		this.gap = gap;
		return this;
	}
	setTick(tick, min, max) {
		this.setGap(tick, min, max);
		return this;
	}
	get tick() {
		return this.gap;
	}
	set tick(value) {
		this.gap = value;
	}
	setThumbOffset(x, y) {
		this.thumbOffsetX = x;
		this.thumbOffsetY = y;
		return this;
	}
	get value() {
		return this._value;
	}
	set value(value) {
		if (this.gap !== void 0) value = SnapTo$2(value, this.gap);
		var oldValue = this._value;
		this._value = Clamp$9(value, 0, 1);
		if (oldValue !== this._value) {
			this.updateThumb(this._value);
			this.updateIndicator(this._value);
			this.eventEmitter.emit("valuechange", this._value, oldValue, this.eventEmitter);
		}
	}
	postLayout(parent, newWidth, newHeight) {
		this.updateThumb();
		this.updateIndicator();
		super.postLayout(parent, newWidth, newHeight);
		return this;
	}
};
var methods$18 = {
	getStartPoint: GetStartPoint,
	getEndPoint: GetEndoint,
	updateThumb: UpdateThumb,
	updateIndicator: UpdateIndicator
};
Object.assign(Slider.prototype, methods$18);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/scrollbar/ScrollBar.js
var GetValue$139 = __webpack_exports__Utils.Objects.GetValue;
var ScrollBar = class extends Sizer {
	constructor(scene, config) {
		super(scene, config);
		this.type = "rexScrollBar";
		var background = GetValue$139(config, "background", void 0);
		var buttonsConfig = GetValue$139(config, "buttons", void 0);
		var button0 = GetValue$139(buttonsConfig, "top", GetValue$139(buttonsConfig, "left", void 0));
		var button1 = GetValue$139(buttonsConfig, "bottom", GetValue$139(buttonsConfig, "right", void 0));
		var slider, sliderConfig = GetValue$139(config, "slider", void 0);
		if (background) this.addBackground(background);
		if (button0) {
			this.add(button0);
			var inTouching = new InTouching_default(button0);
			inTouching.on("intouch", function() {
				if (!this.enable) return;
				var step = !slider.reverseAxis ? -this.scrollStep : this.scrollStep;
				this.value += step;
			}, this);
		}
		if (sliderConfig) {
			sliderConfig.orientation = this.orientation;
			sliderConfig.eventEmitter = this;
			sliderConfig.value = null;
			var proportion;
			if (this.orientation === 0) proportion = GetValue$139(sliderConfig, "width", void 0) === void 0 ? 1 : 0;
			else proportion = GetValue$139(sliderConfig, "height", void 0) === void 0 ? 1 : 0;
			slider = new Slider(scene, sliderConfig);
			scene.add.existing(slider);
			this.add(slider, { proportion });
		}
		if (button1) {
			this.add(button1);
			var inTouching = new InTouching_default(button1);
			inTouching.on("intouch", function() {
				if (!this.enable) return;
				var step = !slider.reverseAxis ? this.scrollStep : -this.scrollStep;
				this.value += step;
			}, this);
		}
		var buttons = [button0, button1];
		this.addChildrenMap("background", background);
		this.addChildrenMap("slider", slider);
		this.addChildrenMap("buttons", buttons);
		var callback = GetValue$139(config, "valuechangeCallback", null);
		if (callback !== null) {
			var scope = GetValue$139(config, "valuechangeCallbackScope", void 0);
			this.on("valuechange", callback, scope);
		}
		this.setEnable(GetValue$139(config, "enable", void 0));
		this.setValue(GetValue$139(config, "value", 0));
		this.setScrollStep(GetValue$139(buttonsConfig, "step", .01));
	}
	setScrollStep(value) {
		this.scrollStep = value;
		return this;
	}
	get enable() {
		if (this.childrenMap.slider) return this.childrenMap.slider.enable;
		else return false;
	}
	set enable(value) {
		if (this.childrenMap.slider) this.childrenMap.slider.setEnable(value);
	}
	setEnable(enable) {
		if (enable === void 0) enable = true;
		this.enable = enable;
		return this;
	}
	get value() {
		if (this.childrenMap.slider) return this.childrenMap.slider.value;
		else return 0;
	}
	set value(value) {
		if (!this.childrenMap.slider) return;
		this.childrenMap.slider.value = value;
	}
	setValue(value, min, max) {
		if (this.childrenMap.slider) this.childrenMap.slider.setValue(value, min, max);
		return this;
	}
	addValue(inc, min, max) {
		if (this.childrenMap.slider) this.childrenMap.slider.addValue(inc, min, max);
		return this;
	}
	getValue(min, max) {
		if (this.childrenMap.slider) return this.childrenMap.slider.getValue(min, max);
		else return 0;
	}
	easeValueTo(value, min, max) {
		if (this.childrenMap.slider) this.childrenMap.slider.easeValueTo(value, min, max);
		return this;
	}
	stopEaseValue() {
		if (this.childrenMap.slider) this.childrenMap.slider.stopEaseValue();
		return this;
	}
	setEaseValueDuration(duration) {
		if (this.childrenMap.slider) this.childrenMap.slider.setEaseValueDuration(duration);
		return this;
	}
	setEaseValueFunction(ease) {
		if (this.childrenMap.slider) this.childrenMap.slider.setEaseValueFunction(ease);
		return this;
	}
	setGap(gap, min, max) {
		if (this.childrenMap.slider) this.childrenMap.slider.setGap(gap, min, max);
		return this;
	}
	get gap() {
		if (this.childrenMap.slider) return this.childrenMap.slider.gap;
	}
	set gap(value) {
		if (this.childrenMap.slider) this.childrenMap.slider.gap = value;
	}
	setTick(tick, min, max) {
		this.setGap(tick, min, max);
		return this;
	}
	get tick() {
		if (this.childrenMap.slider) return this.childrenMap.slider.tick;
	}
	set tick(value) {
		if (this.childrenMap.slider) this.childrenMap.slider.tick = value;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/scrollable/methods/CreateScrollbar.js
var CreateScrollbar = function(scene, config) {
	if (config === void 0) config = {};
	var sliderConfig = Clone$2(config);
	config = { slider: sliderConfig };
	config.orientation = sliderConfig.orientation;
	delete sliderConfig.orientation;
	config.background = sliderConfig.background;
	delete sliderConfig.background;
	config.buttons = sliderConfig.buttons;
	delete sliderConfig.buttons;
	config.value = null;
	var scrollBar = new ScrollBar(scene, config);
	scene.add.existing(scrollBar);
	var slider = scrollBar.childrenMap.slider;
	scrollBar.addChildrenMap("track", slider.childrenMap.track);
	scrollBar.addChildrenMap("indicator", slider.childrenMap.indicator);
	scrollBar.addChildrenMap("thumb", slider.childrenMap.thumb);
	return scrollBar;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/input/scroller/methods/StateActions.js
var SnapTo$1 = __webpack_exports__Math.Snap.To;
__webpack_exports__Math.Clamp;
var OnDragStart = function() {
	this.emit("dragstart");
};
var OnDragEnd = function() {
	this.emit("dragend");
};
var Dragging = function() {
	this.value += this.dragDelta;
};
var OnSliding = function() {
	var start = this.value;
	var speed = this.dragSpeed;
	var snapStep = this.snapStep;
	if (!!!snapStep) {
		if (speed === 0) {
			this._slowDown.stop();
			this._state.next();
		} else {
			var dec = this.slidingDeceleration;
			this._slowDown.init(start, speed > 0, speed, dec);
		}
	} else {
		var end = start;
		var dist = speed === 0 ? 0 : speed * speed / (2 * this.slidingDeceleration);
		end += speed > 0 ? dist : -dist;
		end = SnapTo$1(end, snapStep, this.minValue);
		dist = Math.abs(end - start);
		if (dist > 0) {
			var dec;
			if (speed === 0) {
				dec = this.backDeceleration;
				speed = Math.sqrt(2 * dec * dist);
			} else {
				dec = speed * speed / (2 * dist);
				dec *= .99;
			}
			this._slowDown.init(start, void 0, speed, dec, end);
		} else {
			this._slowDown.stop();
			this._state.next();
		}
	}
};
var Sliding = function(time, delta) {
	delta *= .001;
	var newValue = this._slowDown.update(delta).value;
	if (this.overMax(newValue)) {
		this.value = this.maxValue;
		this._slowDown.stop();
	} else if (this.overMin(newValue)) {
		this.value = this.minValue;
		this._slowDown.stop();
	} else this.value = newValue;
};
var OnBack = function() {
	var start = this.value;
	var end = this.outOfMinBound ? this.minValue : this.maxValue;
	var dist = Math.abs(end - start);
	var dec = this.backDeceleration;
	var speed = Math.sqrt(2 * dec * dist);
	this._slowDown.init(start, void 0, speed, dec, end);
};
var Back = function(time, delta) {
	delta *= .001;
	this.value = this._slowDown.update(delta).value;
	if (!this._slowDown.isMoving) this._state.next();
};
var Stop = function() {
	this._slowDown.stop();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/input/scroller/methods/State.js
var State = class extends fsm_default {
	constructor(parent, config) {
		super(config);
		this.parent = parent;
		this.init();
	}
	init() {
		this.start("IDLE");
	}
	next_IDLE() {
		var nextState, parent = this.parent;
		if (parent.dragState.isDown) nextState = parent.dragThreshold === 0 ? "DRAG" : "DRAGBEGIN";
		return nextState;
	}
	update_IDLE(time, delta) {
		this.next();
	}
	next_DRAGBEGIN() {
		var nextState, parent = this.parent, dragState = parent.dragState;
		if (dragState.isDown) nextState = dragState.pointer.getDistance() >= parent.dragThreshold ? "DRAG" : "DRAGBEGIN";
		else nextState = "IDLE";
		return nextState;
	}
	update_DRAGBEGIN(time, delta) {
		this.next();
	}
	next_DRAG() {
		var nextState, parent = this.parent;
		if (parent.dragState.isUp) {
			if (parent.outOfBounds) nextState = "BACK";
			else if (parent.slidingEnable) nextState = "SLIDE";
			else nextState = "IDLE";
		}
		return nextState;
	}
	update_DRAG(time, delta) {
		var parent = this.parent;
		if (parent.dragState.justMoved) Dragging.call(parent);
		this.next();
	}
	enter_DRAG() {
		OnDragStart.call(this.parent);
	}
	exit_DRAG() {
		OnDragEnd.call(this.parent);
	}
	next_SLIDE() {
		var nextState, parent = this.parent;
		if (parent.dragState.isDown) nextState = "DRAG";
		else if (!parent.isSliding) nextState = "IDLE";
		return nextState;
	}
	enter_SLIDE() {
		OnSliding.call(this.parent);
	}
	exit_SLIDE() {
		Stop.call(this.parent);
	}
	update_SLIDE(time, delta) {
		Sliding.call(this.parent, time, delta);
		this.next();
	}
	next_BACK() {
		var nextState, parent = this.parent;
		if (parent.dragState.isDown) nextState = "DRAG";
		else if (!parent.isPullBack) nextState = "IDLE";
		return nextState;
	}
	enter_BACK() {
		OnBack.call(this.parent);
	}
	exit_BACK() {
		Stop.call(this.parent);
	}
	update_BACK(time, delta) {
		Back.call(this.parent, time, delta);
		this.next();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/input/dragspeed/DragSpeed.js
var GetValue$138 = __webpack_exports__Utils.Objects.GetValue;
var DistanceBetween$1 = __webpack_exports__Math.Distance.Between;
var DragSpeed = class extends ComponentBase {
	constructor(gameObject, config) {
		super(gameObject, config);
		this._enable = void 0;
		this.rectBoundsInteractive = GetValue$138(config, "rectBoundsInteractive", false);
		if (!this.rectBoundsInteractive) gameObject.setInteractive(GetValue$138(config, "inputConfig", void 0));
		this.resetFromJSON(config);
		this.boot();
	}
	resetFromJSON(o) {
		this.pointer = void 0;
		this.isInTouched = false;
		this.holdStartTime = void 0;
		this.x = void 0;
		this.y = void 0;
		this.preX = void 0;
		this.preY = void 0;
		this.localX = void 0;
		this.localY = void 0;
		this.justMoved = false;
		this.setEnable(GetValue$138(o, "enable", true));
		this.holdThreshold = GetValue$138(o, "holdThreshold", 50);
		this.pointerOutReleaseEnable = GetValue$138(o, "pointerOutRelease", true);
		return this;
	}
	boot() {
		var scene = this.scene;
		var gameObject = this.parent;
		if (!this.rectBoundsInteractive) {
			gameObject.on("pointerdown", this.onPointIn, this);
			gameObject.on("pointerup", this.onPointOut, this);
			if (this.pointerOutReleaseEnable) gameObject.on("pointerout", this.onPointOut, this);
			gameObject.on("pointermove", this.onPointerMove, this);
		} else {
			scene.input.on("pointerdown", this.onPointIn, this);
			scene.input.on("pointerup", this.onPointOut, this);
			scene.input.on("pointermove", this.onPointerMove, this);
		}
		scene.sys.events.on("preupdate", this.preupdate, this);
	}
	shutdown(fromScene) {
		if (this.isShutdown) return;
		var scene = this.scene;
		this.parent;
		if (!this.rectBoundsInteractive) {} else {
			scene.input.off("pointerdown", this.onPointIn, this);
			scene.input.off("pointerup", this.onPointOut, this);
			scene.input.off("pointermove", this.onPointerMove, this);
		}
		scene.sys.events.off("preupdate", this.preupdate, this);
		this.pointer = void 0;
		super.shutdown(fromScene);
	}
	get enable() {
		return this._enable;
	}
	set enable(e) {
		if (this._enable === e) return;
		if (!e) {
			this.isInTouched = false;
			this.pointer = void 0;
		}
		this._enable = e;
	}
	setEnable(e) {
		if (e === void 0) e = true;
		this.enable = e;
		return this;
	}
	toggleEnable() {
		this.setEnable(!this.enable);
		return this;
	}
	setPointerOutReleaseEnable(enable) {
		if (enable === void 0) enable = true;
		this.pointerOutReleaseEnable = enable;
		return this;
	}
	get isDown() {
		return this.pointer && this.pointer.isDown;
	}
	get isUp() {
		return !this.isDown;
	}
	get dx() {
		return this.x - this.preX;
	}
	get dy() {
		return this.y - this.preY;
	}
	get dt() {
		return GetTickDelta(this.scene);
	}
	get speed() {
		if (this.x === this.preX && this.y === this.preY) return 0;
		return DistanceBetween$1(this.preX, this.preY, this.x, this.y) / (this.dt * .001);
	}
	get speedX() {
		return this.dx / (this.dt * .001);
	}
	get speedY() {
		return this.dy / (this.dt * .001);
	}
	onPointIn(pointer, localX, localY) {
		if (!this.enable || !pointer.isDown || this.pointer !== void 0) return;
		if (this.rectBoundsInteractive && !IsPointerInBounds(this.parent, pointer)) return;
		this.pointer = pointer;
		this.localX = localX;
		this.localY = localY;
	}
	onPointOut(pointer) {
		if (!this.enable || this.pointer !== pointer) return;
		this.pointer = void 0;
	}
	onPointerMove(pointer, localX, localY) {
		if (!this.enable || !pointer.isDown || this.pointer !== pointer) return;
		if (this.rectBoundsInteractive && this.pointerOutReleaseEnable && !IsPointerInBounds(this.parent, pointer)) {
			this.onPointOut(pointer);
			return;
		}
		this.localX = localX;
		this.localY = localY;
	}
	preupdate(time, delta) {
		if (!this.enable) return;
		var pointer = this.pointer;
		this.justMoved = false;
		if (pointer && !this.isInTouched) {
			this.x = pointer.worldX;
			this.y = pointer.worldY;
			this.preX = pointer.worldX;
			this.preY = pointer.worldY;
			this.isInTouched = true;
			this.holdStartTime = void 0;
			this.emit("touchstart", pointer, this.localX, this.localY);
		} else if (pointer && this.isInTouched) {
			if (this.x === pointer.x && this.y === pointer.y) {
				if (this.holdStartTime === void 0) this.holdStartTime = time;
				else if (time - this.holdStartTime > this.holdThreshold) {
					this.preX = this.x;
					this.preY = this.y;
				}
			} else {
				this.preX = this.x;
				this.preY = this.y;
				this.x = pointer.worldX;
				this.y = pointer.worldY;
				this.holdStartTime = void 0;
				this.justMoved = true;
				this.emit("touchmove", pointer, this.localX, this.localY);
			}
		} else if (!pointer && this.isInTouched) {
			this.isInTouched = false;
			this.holdStartTime = void 0;
			this.emit("touchend", pointer);
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/dragspeed.js
var dragspeed_default = DragSpeed;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/movement/Movement.js
var GetValue$137 = __webpack_exports__Utils.Objects.GetValue;
var Movement = class {
	constructor(config) {
		this.resetFromJSON(config);
	}
	resetFromJSON(o) {
		this.setValue(GetValue$137(o, "value", 0));
		this.setSpeed(GetValue$137(o, "speed", 0));
		this.setAcceleration(GetValue$137(o, "acceleration", 0));
		return this;
	}
	reset() {
		this.setValue(0);
		this.setSpeed(0);
		this.setAcceleration(0);
	}
	setValue(value) {
		this.value = value;
		return this;
	}
	setSpeed(speed) {
		this.speed = speed;
		return this;
	}
	setAcceleration(acc) {
		this.acceleration = acc;
		return this;
	}
	updateSpeed(delta) {
		if (this.acceleration !== 0) {
			this.speed += this.acceleration * delta;
			if (this.speed < 0) this.speed = 0;
		}
		return this;
	}
	getDeltaValue(delta) {
		this.updateSpeed(delta);
		if (this.speed <= 0) return 0;
		return this.speed * delta;
	}
	update(delta) {
		this.updateSpeed(delta);
		if (this.speed > 0) this.value += this.getDeltaValue(delta);
		return this;
	}
	get isMoving() {
		return this.speed > 0;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/movement/SlowDown.js
var SlowDown = class {
	constructor() {
		this.value;
		this.dir;
		this.movement = new Movement();
	}
	init(start, dir, speed, dec, end) {
		this.value = start;
		this.end = end;
		if (end !== void 0) this.dir = start < end;
		else this.dir = dir;
		speed = Math.abs(speed);
		this.movement.setSpeed(speed).setAcceleration(-dec);
		return this;
	}
	stop() {
		this.movement.reset();
	}
	update(delta) {
		var d = this.movement.getDeltaValue(delta);
		if (!this.dir) d = -d;
		if (this.end === void 0) this.value += d;
		else if (d === 0) this.value = this.end;
		else {
			this.value += d;
			if (this.dir) {
				if (this.value > this.end) this.value = this.end;
			} else if (this.value < this.end) this.value = this.end;
		}
		return this;
	}
	get isMoving() {
		return this.movement.isMoving;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/input/scroller/Scroller.js
var GetValue$136 = __webpack_exports__Utils.Objects.GetValue;
var Clamp$8 = __webpack_exports__Math.Clamp;
var Scroller = class extends ComponentBase {
	constructor(gameObject, config) {
		super(gameObject, config);
		var enable = GetValue$136(config, "enable", true);
		this._state = new State(this, {
			enable,
			eventEmitter: false
		});
		var drapSpeedConfig = {
			rectBoundsInteractive: GetValue$136(config, "rectBoundsInteractive", false),
			inputConfig: GetValue$136(config, "inputConfig", void 0),
			enable,
			pointerOutRelease: GetValue$136(config, "pointerOutRelease", true),
			eventEmitter: false
		};
		this.dragState = new dragspeed_default(gameObject, drapSpeedConfig);
		this._enable = void 0;
		this._value = void 0;
		this._slowDown = new SlowDown();
		this.setSnapStep(GetValue$136(config, "snapStep", void 0));
		var callback = GetValue$136(config, "valuechangeCallback", null);
		if (callback !== null) {
			var scope = GetValue$136(config, "valuechangeCallbackScope", void 0);
			this.on("valuechange", callback, scope);
		}
		callback = GetValue$136(config, "overmaxCallback", null);
		if (callback !== null) {
			var scope = GetValue$136(config, "overmaxCallbackScope", void 0);
			this.on("overmax", callback, scope);
		}
		callback = GetValue$136(config, "overminCallback", null);
		if (callback !== null) {
			var scope = GetValue$136(config, "overminCallbackScope", void 0);
			this.on("overmin", callback, scope);
		}
		this.resetFromJSON(config);
		this.boot();
	}
	resetFromJSON(o) {
		this.setOrientationMode(GetValue$136(o, "orientation", 0));
		this.setDragThreshold(GetValue$136(o, "threshold", 10));
		this.setSlidingDeceleration(GetValue$136(o, "slidingDeceleration", 5e3));
		this.setBackDeceleration(GetValue$136(o, "backDeceleration", 2e3));
		var dragRate = GetValue$136(o, "dragRate", 1);
		dragRate = dragRate * (GetValue$136(o, "dragReverse", false) ? -1 : 1);
		this.setDragRate(dragRate);
		var bounds = GetValue$136(o, "bounds", void 0);
		if (bounds) this.setBounds(bounds);
		else this.setBounds(GetValue$136(o, "max", 0), GetValue$136(o, "min", 0));
		this.setValue(GetValue$136(o, "value", this.maxValue || 0));
		this.setEnable(GetValue$136(o, "enable", true));
		return this;
	}
	boot() {
		this.scene.sys.events.on("preupdate", this._state.update, this._state);
	}
	shutdown(fromScene) {
		if (this.isShutdown) return;
		this.scene.sys.events.off("preupdate", this._state.update, this._state);
		this._state.destroy(fromScene);
		this.dragState.destroy(fromScene);
		this._state = void 0;
		this.dragState = void 0;
		super.shutdown(fromScene);
	}
	get enable() {
		return this._enable;
	}
	set enable(e) {
		if (this._enable === e) return;
		this._enable = e;
		this._state.setEnable(e);
		this.dragState.setEnable(e);
		return this;
	}
	setEnable(e) {
		if (e === void 0) e = true;
		this.enable = e;
		return this;
	}
	toggleEnable() {
		this.setEnable(!this.enable);
		return this;
	}
	setOrientationMode(m) {
		if (typeof m === "string") m = ORIENTATIONMODE$2[m];
		this.orientationMode = m;
		return this;
	}
	setDragThreshold(distance) {
		this.dragThreshold = distance;
		return this;
	}
	setSlidingDeceleration(dec) {
		this.slidingDeceleration = dec;
		return this;
	}
	setBackDeceleration(dec) {
		this.backDeceleration = dec;
		return this;
	}
	setDragRate(ratio) {
		this.dragRate = ratio;
		return this;
	}
	setBounds(value0, value1) {
		if (Array.isArray(value0)) {
			var bounds = value0;
			value0 = bounds[0];
			value1 = bounds[1];
		}
		if (value0 < value1) {
			this.minValue = value0;
			this.maxValue = value1;
		} else {
			this.minValue = value1;
			this.maxValue = value0;
		}
		return this;
	}
	setSnapStep(snapStep) {
		this.snapStep = snapStep;
		return this;
	}
	get value() {
		return this._value;
	}
	set value(value) {
		if (value === this._value) return;
		var oldValue = this._value;
		var isOverMax = this.overMax(value);
		var isOverMin = this.overMin(value);
		if (isOverMax) this.emit("overmax", value, oldValue);
		if (isOverMin) this.emit("overmin", value, oldValue);
		if (!this.backEnable) {
			if (isOverMax) value = this.maxValue;
			if (isOverMin) value = this.minValue;
		}
		this._value = value;
		this.emit("valuechange", value, oldValue);
	}
	setValue(value, clamp) {
		if (clamp === void 0) clamp = false;
		if (clamp) value = Clamp$8(value, this.minValue, this.maxValue);
		this.value = value;
		return this;
	}
	addValue(inc, clamp) {
		this.setValue(this.value + inc, clamp);
		return this;
	}
	get state() {
		return this._state.state;
	}
	get isDragging() {
		return this.dragState.isInTouched;
	}
	get outOfMaxBound() {
		return this.overMax(this.value);
	}
	get outOfMinBound() {
		return this.overMin(this.value);
	}
	get outOfBounds() {
		return this.outOfMinBound || this.outOfMaxBound;
	}
	overMax(value) {
		return this.maxValue != null && value > this.maxValue;
	}
	overMin(value) {
		return this.minValue != null && value < this.minValue;
	}
	get backEnable() {
		return typeof this.backDeceleration === "number";
	}
	get isPullBack() {
		return this._slowDown.isMoving;
	}
	get slidingEnable() {
		return typeof this.slidingDeceleration === "number";
	}
	get isSliding() {
		return this._slowDown.isMoving;
	}
	get dragDelta() {
		var delta;
		if (this.orientationMode === 0) delta = this.dragState.dy;
		else if (this.orientationMode === 1) delta = this.dragState.dx;
		else delta = 0;
		delta *= this.dragRate;
		return delta;
	}
	get dragSpeed() {
		var speed;
		if (this.orientationMode === 0) speed = this.dragState.speedY;
		else if (this.orientationMode === 1) speed = this.dragState.speedX;
		else speed = 0;
		speed *= this.dragRate;
		return speed;
	}
};
var ORIENTATIONMODE$2 = {
	y: 0,
	v: 0,
	vertical: 0,
	x: 1,
	h: 1,
	horizontal: 1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/scroller.js
var scroller_default = Scroller;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/input/mousewheelscroller/MouseWheelScroller.js
var GetValue$135 = __webpack_exports__Utils.Objects.GetValue;
var MouseWheelScroller = class extends ComponentBase {
	constructor(gameObject, config) {
		super(gameObject, config);
		if (this.parent !== this.scene) this.focusMode = GetValue$135(config, "focus", true);
		else this.focusMode = false;
		if (typeof this.focusMode === "boolean") this.focusMode = this.focusMode ? 1 : 0;
		this.setSpeed(GetValue$135(config, "speed", .1));
		this.setEnable(GetValue$135(config, "enable", true));
		switch (this.focusMode) {
			case 0:
			case 2:
				this.scene.input.on("wheel", this.onSceneScroll, this);
				break;
			default: gameObject.setInteractive(GetValue$135(config, "inputConfig", void 0)).on("wheel", function(pointer, dx, dy, dz, event) {
				this.tryScroll(dy);
			}, this);
		}
	}
	destroy() {
		switch (this.focusMode) {
			case 0:
			case 2: this.scene.input.off("wheel", this.onSceneScroll, this);
		}
	}
	onSceneScroll(pointer, currentlyOver, dx, dy, dz, event) {
		if (this.focusMode === 2) {
			if (!IsPointerInBounds(this.parent, pointer)) return;
		}
		this.tryScroll(dy);
	}
	setEnable(e) {
		if (e === void 0) e = true;
		this.enable = e;
		return this;
	}
	setSpeed(speed) {
		this.speed = speed;
		return this;
	}
	tryScroll(dy) {
		if (!this.enable) return;
		this.scroll(dy);
		return this;
	}
	scroll(dy) {
		dy *= this.speed;
		this.emit("scroll", dy, this.parent, this);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/scrollable/methods/AddSlider.js
var GetValue$134 = __webpack_exports__Utils.Objects.GetValue;
var SnapTo = __webpack_exports__Math.Snap.To;
var AddSlider = function(topPatent, sliderParent, axis, config) {
	axis = axis.toUpperCase();
	var isAxisY = axis === "Y";
	var isScrollXYMode = topPatent.scrollMode === 2;
	var child = topPatent.childrenMap.child;
	var snapStep;
	var snapStepKey = `snapStep${axis}`;
	if (isScrollXYMode) snapStep = GetValue$134(config, snapStepKey, void 0);
	else {
		var snapStep = GetValue$134(config, "snapStep", void 0);
		if (snapStep === void 0) snapStep = GetValue$134(config, snapStepKey, void 0);
	}
	topPatent[snapStepKey] = snapStep;
	var sliderConfig, slider;
	var sliderConfigKey = `slider${axis}`;
	if (isScrollXYMode) sliderConfig = GetValue$134(config, sliderConfigKey, void 0);
	else if (config.hasOwnProperty(sliderConfigKey)) sliderConfig = GetValue$134(config, sliderConfigKey, void 0);
	else sliderConfig = GetValue$134(config, "slider", void 0);
	if (sliderConfig) {
		if (sliderConfig === true) sliderConfig = {};
		sliderConfig.orientation = isAxisY ? 1 : 0;
		slider = CreateScrollbar(topPatent.scene, sliderConfig);
		var column, row, padding;
		var sliderPosition = GetValue$134(sliderConfig, "position", 0);
		if (typeof sliderPosition === "string") sliderPosition = SLIDER_POSITION_MAP[sliderPosition];
		var sliderPadding = GetValue$134(config, `space.slider${axis}`, void 0);
		var childPadding;
		if (sliderPadding === void 0) {
			sliderPadding = GetValue$134(config, "space.slider", void 0);
			if (sliderPadding === void 0) {
				if (isScrollXYMode) sliderPadding = 0;
				else childPadding = GetValue$134(config, "space.child", 0);
			}
		}
		var isNumberSliderPadding;
		var isNumberChildPadding;
		if (childPadding === void 0) {
			isNumberSliderPadding = typeof sliderPadding === "number";
			isNumberChildPadding = false;
		} else {
			isNumberSliderPadding = typeof childPadding === "number";
			isNumberChildPadding = isNumberSliderPadding;
		}
		if (isAxisY) {
			if (sliderPosition === 0) {
				column = 2;
				row = 1;
				if (childPadding === void 0) padding = isNumberSliderPadding ? { left: sliderPadding } : sliderPadding;
				else if (isNumberChildPadding) padding = { left: childPadding };
				else padding = { left: GetValue$134(childPadding, "right", 0) };
			} else {
				column = 0;
				row = 1;
				if (childPadding === void 0) padding = isNumberSliderPadding ? { right: sliderPadding } : sliderPadding;
				else if (isNumberChildPadding) padding = { right: childPadding };
				else padding = { right: GetValue$134(childPadding, "left", 0) };
			}
		} else if (sliderPosition === 0) {
			column = 1;
			row = 2;
			if (childPadding === void 0) padding = isNumberSliderPadding ? { top: sliderPadding } : sliderPadding;
			else if (isNumberChildPadding) padding = { top: childPadding };
			else padding = { top: GetValue$134(childPadding, "bottom", 0) };
		} else {
			column = 1;
			row = 0;
			if (childPadding === void 0) padding = isNumberSliderPadding ? { bottom: sliderPadding } : sliderPadding;
			else if (isNumberChildPadding) padding = { bottom: childPadding };
			else padding = { bottom: GetValue$134(childPadding, "top", 0) };
		}
		sliderParent.add(slider, {
			column,
			row,
			align: "center",
			padding,
			expand: true
		});
		topPatent[`hideUnscrollableSlider${axis}`] = GetValue$134(sliderConfig, "hideUnscrollableSlider", false);
		topPatent[`disableUnscrollableDrag${axis}`] = GetValue$134(sliderConfig, "disableUnscrollableDrag", false);
		topPatent[`adaptThumb${axis}SizeMode`] = GetValue$134(sliderConfig, "adaptThumbSize", false);
		topPatent[`minThumb${axis}Size`] = GetValue$134(sliderConfig, "minThumbSize", void 0);
	} else {
		topPatent[`hideUnscrollableSlider${axis}`] = false;
		topPatent[`disableUnscrollableDrag${axis}`] = false;
		topPatent[`adaptThumb${axis}SizeMode`] = false;
		topPatent[`minThumb${axis}Size`] = void 0;
	}
	var scrollDetectionMode = GetValue$134(config, "scrollDetectionMode");
	if (typeof scrollDetectionMode === "string") scrollDetectionMode = SCROLLDECTIONMODE_MAP[scrollDetectionMode];
	var scrollerConfig, scroller;
	var scrollerConfigKey = `scroller${axis}`;
	if (isScrollXYMode) scrollerConfig = GetValue$134(config, scrollerConfigKey, true);
	else if (config.hasOwnProperty(scrollerConfigKey)) scrollerConfig = GetValue$134(config, scrollerConfigKey, true);
	else scrollerConfig = GetValue$134(config, "scroller", true);
	if (scrollerConfig && child) {
		if (scrollerConfig === true) scrollerConfig = {};
		scrollerConfig.orientation = isAxisY ? 0 : 1;
		if (scrollDetectionMode !== void 0) scrollerConfig.rectBoundsInteractive = scrollDetectionMode === 1;
		scrollerConfig.snapStep = snapStep;
		scroller = new scroller_default(child, scrollerConfig);
		if (child.isRexContainerLite) {
			if (!child.layerRendererEnable) child.sendChildToBack(child);
		}
	}
	var mouseWheelScrollerConfig = GetValue$134(config, isScrollXYMode ? `mouseWheelScroller${axis}` : "mouseWheelScroller", false), mouseWheelScroller;
	if (mouseWheelScrollerConfig && child) {
		if (scrollDetectionMode !== void 0) mouseWheelScrollerConfig.focus = scrollDetectionMode === 1 ? 2 : 0;
		mouseWheelScroller = new MouseWheelScroller(child, mouseWheelScrollerConfig);
	}
	topPatent.addChildrenMap(`slider${axis}`, slider);
	topPatent.addChildrenMap(`scroller${axis}`, scroller);
	topPatent.addChildrenMap(`mouseWheelScroller${axis}`, mouseWheelScroller);
	if (!isScrollXYMode || isAxisY) {
		topPatent["hideUnscrollableSlider"] = topPatent[`hideUnscrollableSlider${axis}`];
		topPatent["disableUnscrollableDrag"] = topPatent[`disableUnscrollableDrag${axis}`];
		topPatent["adaptThumbSizeMode"] = topPatent[`adaptThumb${axis}SizeMode`];
		topPatent["minThumbSize"] = topPatent[`minThumb${axis}Size`];
		topPatent.addChildrenMap("slider", slider);
		topPatent.addChildrenMap("scroller", scroller);
		topPatent.addChildrenMap("mouseWheelScroller", mouseWheelScroller);
	}
	if (slider) {
		var keyST, eventName;
		if (isScrollXYMode) {
			keyST = isAxisY ? "t" : "s";
			eventName = `scroll${axis}`;
		} else {
			keyST = "t";
			eventName = "scroll";
		}
		slider.on("valuechange", function(newValue) {
			topPatent[keyST] = newValue;
			topPatent.emit(eventName, topPatent);
		}).on("inputend", function() {
			var snapStep = topPatent[`snapStep${axis}`];
			if (snapStep) {
				var min = topPatent[isAxisY ? `topChildOY` : `leftChildOX`];
				var max = topPatent[isAxisY ? `bottomChildOY` : `rightChildOX`];
				var valueEnd = topPatent[`childO${axis}`];
				var valueTo = SnapTo(valueEnd, snapStep, min);
				var easeDuration = 500 * (Math.abs(valueTo - valueEnd) / snapStep);
				slider.setEaseValueDuration(easeDuration).easeValueTo(valueTo, min, max);
			}
		});
	}
	if (scroller) {
		var keyChildOXY, eventName;
		if (isScrollXYMode) {
			keyChildOXY = `childO${axis}`;
			eventName = `scroll${axis}`;
		} else {
			keyChildOXY = "childOY";
			eventName = "scroll";
		}
		scroller.on("valuechange", function(newValue) {
			topPatent[keyChildOXY] = newValue;
			topPatent.emit(eventName, topPatent);
		});
	}
	if (mouseWheelScroller) {
		var methodAddChildOXY;
		if (isScrollXYMode) methodAddChildOXY = `addChildO${axis}`;
		else methodAddChildOXY = "addChildOY";
		mouseWheelScroller.on("scroll", function(incValue) {
			var snapStep = topPatent[snapStepKey];
			if (snapStep) incValue = snapStep;
			topPatent[methodAddChildOXY](-incValue, true);
		});
	}
};
var SLIDER_POSITION_MAP = {
	right: 0,
	left: 1,
	bottom: 0,
	top: 1
};
var SCROLLDECTIONMODE_MAP = {
	gameObject: 0,
	rectBounds: 1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/scrollable/methods/CreateScrollableSizer.js
var GetValue$133 = __webpack_exports__Utils.Objects.GetValue;
var CreateScrollableSizer = function(parent, config) {
	var scene = parent.scene;
	var columnProportions = [
		0,
		1,
		0
	], rowProportions = [
		0,
		1,
		0
	];
	var parentMinWidth = GetValue$133(config, "width");
	var parentMinHeight = GetValue$133(config, "height");
	if (!parentMinWidth) {
		if (!GetValue$133(config, "child.expandWidth", true)) columnProportions[1] = 0;
	}
	if (!parentMinHeight) {
		if (!GetValue$133(config, "child.expandHeight", true)) rowProportions[1] = 0;
	}
	var scrollableSizer = new GridSizer(scene, {
		column: 3,
		row: 3,
		columnProportions,
		rowProportions
	});
	AddChild(parent, scrollableSizer, config);
	switch (parent.scrollMode) {
		case 0:
			AddSlider(parent, scrollableSizer, "y", config);
			break;
		case 1:
			AddSlider(parent, scrollableSizer, "x", config);
			break;
		default:
			AddSlider(parent, scrollableSizer, "y", config);
			AddSlider(parent, scrollableSizer, "x", config);
	}
	return scrollableSizer;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/scrollable/Scrollable.js
var GetValue$132 = __webpack_exports__Utils.Objects.GetValue;
var Scrollable$1 = class extends Sizer {
	constructor(scene, config) {
		if (config === void 0) config = {};
		var scrollMode = GetScrollMode(config);
		config.orientation = !(scrollMode === 1) ? 1 : 0;
		super(scene, config);
		this.type = GetValue$132(config, "type", "rexScrollable");
		this.scrollMode = scrollMode;
		var background = GetValue$132(config, "background", void 0);
		if (background) {
			this.addBackground(background);
			this.addChildrenMap("background", background);
		}
		var scrollableSizer = CreateScrollableSizer(this, config);
		this.add(scrollableSizer, {
			proportion: 1,
			align: "center",
			padding: 0,
			expand: true
		});
		this.addChildrenMap("scrollableSizer", scrollableSizer);
		var header = GetValue$132(config, "header", void 0);
		if (header) {
			var headerSpace = GetValue$132(config, "space.header", 0);
			var headerAlign = GetValue$132(config, "align.header", "center");
			var headerExpand = GetValue$132(config, "expand.header", true);
			this.addHeader(header, headerSpace, headerAlign, headerExpand);
		}
		var footer = GetValue$132(config, "footer", void 0);
		if (footer) {
			var footerSpace = GetValue$132(config, "space.footer", 0);
			var footerAlign = GetValue$132(config, "align.footer", "center");
			var footerExpand = GetValue$132(config, "expand.footer", true);
			this.addFooter(footer, footerSpace, footerAlign, footerExpand);
		}
		this.runLayoutFlag = false;
	}
	postLayout(parent, newWidth, newHeight) {
		var s = 0, t = 0;
		if (!this.runLayoutFlag) this.runLayoutFlag = true;
		else {
			t = this.t;
			if (this.scrollMode === 2) s = this.s;
		}
		this.resizeController();
		this.setT(t);
		if (this.scrollMode === 2) this.setS(s);
		super.postLayout(parent, newWidth, newHeight);
		return this;
	}
	set t(value) {
		var childMargin = this.childMargin;
		if (childMargin.top !== 0 || childMargin.bottom !== 0) {
			var child = this.childrenMap.child;
			var innerHeight = child.topChildOY - child.bottomChildOY;
			var innerChildOY = (innerHeight + childMargin.top + childMargin.bottom) * value - childMargin.top;
			value = innerHeight !== 0 ? innerChildOY / innerHeight : 0;
		}
		this.childrenMap.child.t = value;
		this.updateController();
	}
	get t() {
		var t = this.childrenMap.child.t;
		var childMargin = this.childMargin;
		if (childMargin.top !== 0 || childMargin.bottom !== 0) {
			var child = this.childrenMap.child;
			var innerHeight = child.topChildOY - child.bottomChildOY;
			var outerHeight = innerHeight + childMargin.top + childMargin.bottom;
			var outerChildOY = innerHeight * t + childMargin.top;
			t = outerHeight !== 0 ? outerChildOY / outerHeight : 0;
		}
		return t;
	}
	set s(value) {
		var childMargin = this.childMargin;
		if (childMargin.left !== 0 || childMargin.right !== 0) {
			var child = this.childrenMap.child;
			var innerWidth = child.leftChildOX - child.rightChildOX;
			var innerChildOX = (innerWidth + childMargin.left + childMargin.right) * value - childMargin.left;
			value = innerWidth !== 0 ? innerChildOX / innerWidth : 0;
		}
		this.childrenMap.child.s = value;
		this.updateController();
	}
	get s() {
		var s = this.childrenMap.child.s;
		var childMargin = this.childMargin;
		if (childMargin.left !== 0 || childMargin.right !== 0) {
			var child = this.childrenMap.child;
			var innerWidth = child.leftChildOX - child.rightChildOX;
			var outerWidth = innerWidth + childMargin.left + childMargin.right;
			var outerChildOX = innerWidth * s + childMargin.left;
			s = outerWidth !== 0 ? outerChildOX / outerWidth : 0;
		}
		return s;
	}
	set childOY(value) {
		this.childrenMap.child.childOY = value;
		this.updateController();
	}
	get childOY() {
		return this.childrenMap.child.childOY;
	}
	set childOX(value) {
		this.childrenMap.child.childOX = value;
		this.updateController();
	}
	get childOX() {
		return this.childrenMap.child.childOX;
	}
	get topChildOY() {
		return this.childrenMap.child.topChildOY + this.childMargin.top;
	}
	get bottomChildOY() {
		return this.childrenMap.child.bottomChildOY - this.childMargin.bottom;
	}
	get leftChildOX() {
		return this.childrenMap.child.leftChildOX + this.childMargin.left;
	}
	get rightChildOX() {
		return this.childrenMap.child.rightChildOX - this.childMargin.right;
	}
	get childVisibleHeight() {
		return this.childrenMap.child.childVisibleHeight;
	}
	get childHeight() {
		return this.childrenMap.child.childHeight;
	}
	get childVisibleWidth() {
		return this.childrenMap.child.childVisibleWidth;
	}
	get childWidth() {
		return this.childrenMap.child.childWidth;
	}
	get isOverflow() {
		var child = this.childrenMap.child;
		return child.topChildOY !== child.bottomChildOY;
	}
	get isOverflowY() {
		return this.isOverflow;
	}
	get isOverflowX() {
		var child = this.childrenMap.child;
		return child.leftChildOX !== child.rightChildOX;
	}
	get sliderEnable() {
		var slider = this.childrenMap.slider;
		if (!slider) return false;
		return slider.enable;
	}
	set sliderEnable(value) {
		var slider = this.childrenMap.slider;
		if (!slider) return;
		slider.setEnable(value);
	}
	setSliderEnable(enabled) {
		if (enabled === void 0) enabled = true;
		this.sliderEnable = enabled;
		return this;
	}
	get sliderYEnable() {
		return this.sliderEnable;
	}
	set sliderYEnable(value) {
		this.sliderEnable = value;
	}
	setSliderYEnable(enabled) {
		this.setSliderEnable(enabled);
		return this;
	}
	get sliderXEnable() {
		var slider = this.childrenMap.sliderX;
		if (!slider) return false;
		return slider.enable;
	}
	set sliderXEnable(value) {
		var slider = this.childrenMap.sliderX;
		if (!slider) return;
		slider.setEnable(value);
	}
	setSliderXEnable(enabled) {
		if (enabled === void 0) enabled = true;
		this.sliderXEnable = enabled;
		return this;
	}
	get scrollerEnable() {
		var scroller = this.childrenMap.scroller;
		if (!scroller) return false;
		return scroller.enable;
	}
	set scrollerEnable(value) {
		var scroller = this.childrenMap.scroller;
		if (!scroller) return;
		scroller.setEnable(value);
	}
	setScrollerEnable(enabled) {
		if (enabled === void 0) enabled = true;
		this.scrollerEnable = enabled;
		return this;
	}
	get scrollerYEnable() {
		return this.scrollerEnable;
	}
	set scrollerYEnable(value) {
		this.scrollerEnable = value;
	}
	setScrollerYEnable(enabled) {
		this.setScrollerEnable(enabled);
		return this;
	}
	get scrollerXEnable() {
		var scroller = this.childrenMap.scrollerX;
		if (!scroller) return false;
		return scroller.enable;
	}
	set scrollerXEnable(value) {
		var scroller = this.childrenMap.scrollerX;
		if (!scroller) return;
		scroller.setEnable(value);
	}
	setScrollerXEnable(enabled) {
		if (enabled === void 0) enabled = true;
		this.scrollerXEnable = enabled;
		return this;
	}
	get scrollerXEnable() {
		var scroller = this.childrenMap.scrollerX;
		if (!scroller) return false;
		return scroller.enable;
	}
	set scrollerXEnable(value) {
		var scroller = this.childrenMap.scrollerX;
		if (!scroller) return;
		scroller.setEnable(value);
	}
	setScrollerXEnable(enabled) {
		if (enabled === void 0) enabled = true;
		this.scrollerXEnable = enabled;
		return this;
	}
	get mouseWheelScrollerEnable() {
		var mouseWheelScroller = this.childrenMap.mouseWheelScroller;
		if (!mouseWheelScroller) return false;
		return mouseWheelScroller.enable;
	}
	set mouseWheelScrollerEnable(value) {
		var mouseWheelScroller = this.childrenMap.mouseWheelScroller;
		if (!mouseWheelScroller) return;
		mouseWheelScroller.setEnable(value);
	}
	setMouseWheelScrollerEnable(enabled) {
		if (enabled === void 0) enabled = true;
		this.mouseWheelScrollerEnable = enabled;
		return this;
	}
	get mouseWheelScrollerYEnable() {
		return this.mouseWheelScrollerEnable;
	}
	set mouseWheelScrollerYEnable(value) {
		this.mouseWheelScrollerEnable = value;
	}
	setMouseWheelScrollerYEnable(enabled) {
		this.setMouseWheelScrollerEnable(enabled);
		return this;
	}
	get mouseWheelScrollerXEnable() {
		var mouseWheelScroller = this.childrenMap.mouseWheelScrollerX;
		if (!mouseWheelScroller) return false;
		return mouseWheelScroller.enable;
	}
	set mouseWheelScrollerXEnable(value) {
		var mouseWheelScroller = this.childrenMap.mouseWheelScrollerX;
		if (!mouseWheelScroller) return;
		mouseWheelScroller.setEnable(value);
	}
	setMouseWheelScrollerXEnable(enabled) {
		if (enabled === void 0) enabled = true;
		this.mouseWheelScrollerXEnable = enabled;
		return this;
	}
	setDropZoneEnable(enable) {
		if (enable === void 0) enable = true;
		var child = this.childrenMap.child;
		child.setInteractive();
		child.input.dropZone = enable;
		return this;
	}
};
Object.assign(Scrollable$1.prototype, Methods$11);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/text/TextToLines.js
var TextToLines = function(textObject, text, lines) {
	switch (GetTextObjectType(textObject)) {
		case 0:
			lines = textObject.getWrappedText(text);
			break;
		case 1:
			lines = textObject.getPenManager(text, lines);
			break;
		case 2: if (textObject.maxWidth > 0) lines = textObject.setText(text).getTextBounds().wrappedText.split("\n");
		else lines = text.split("\n");
	}
	return lines;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/text/SetNoWrapText.js
var SetNoWrapText = function(textObject, text) {
	switch (GetTextObjectType(textObject)) {
		case 0:
			var style = textObject.style;
			var wordWrapWidth = style.wordWrapWidth;
			var wordWrapCallback = style.wordWrapCallback;
			style.wordWrapWidth = 0;
			style.wordWrapCallback = void 0;
			textObject.setText(text);
			style.wordWrapWidth = wordWrapWidth;
			style.wordWrapCallback = wordWrapCallback;
			break;
		case 1:
			var style = textObject.style;
			var wrapMode = style.wrapMode;
			style.wrapMode = 0;
			textObject.setText(text);
			style.wrapMode = wrapMode;
			break;
		case 2:
			var maxWidth = textObject._maxWidth;
			textObject._maxWidth = 0;
			textObject.setText(text);
			textObject._maxWidth = maxWidth;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textarea/textblock/BaseTextBlock.js
var IsPlainObject$16 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$131 = __webpack_exports__Utils.Objects.GetValue;
var ALIGN_LEFTTOP$1 = __webpack_exports__Display.Align.TOP_LEFT;
var BaseTextBlock = class extends Base$3 {
	constructor(scene, x, y, minWidth, minHeight, config) {
		if (IsPlainObject$16(x)) {
			config = x;
			x = GetValue$131(config, "x", 0);
			y = GetValue$131(config, "y", 0);
			minWidth = GetValue$131(config, "width", void 0);
			minHeight = GetValue$131(config, "height", void 0);
		} else if (IsPlainObject$16(minWidth)) {
			config = minWidth;
			minWidth = GetValue$131(config, "width", void 0);
			minHeight = GetValue$131(config, "height", void 0);
		}
		super(scene, x, y, minWidth, minHeight, config);
		this.type = "rexBaseTextBlock";
		this.textObject = void 0;
		this.textObjectType = void 0;
		this.textMask = void 0;
		this.textObjectWidth = void 0;
		this._textObjectRealHeight = 0;
		this.linesCount = 0;
		this.text = GetValue$131(config, "content", "");
		this._textOY = 0;
		this.execeedTopState = false;
		this.execeedBottomState = false;
		this.setClampMode(GetValue$131(config, "clampTextOY", true));
		this.alwaysScrollable = GetValue$131(config, "alwaysScrollable", false);
		var background = GetValue$131(config, "background", void 0);
		var textObject = GetValue$131(config, "text", void 0);
		if (textObject === void 0) textObject = CreateDefaultTextObject(scene);
		this.textCropEnable = GetValue$131(config, "textCrop", !!textObject.setCrop);
		var textMaskEnable = GetValue$131(config, "textMask", !this.textCropEnable);
		if (background) this.addBackground(background);
		this.add(textObject);
		this.sizerChildren = [textObject];
		var sizerConfig = this.getSizerConfig(textObject);
		sizerConfig.align = ALIGN_LEFTTOP$1;
		sizerConfig.padding = GetBoundsConfig_default(0);
		sizerConfig.expand = true;
		this.textObject = textObject;
		this.textObjectType = GetTextObjectType(textObject);
		sizerConfig.preOffsetY = 0;
		sizerConfig.offsetY = 0;
		if (textMaskEnable) this.textMask = AddChildMask.call(this, this.textObject, this);
		this.addChildrenMap("background", background);
		this.addChildrenMap("text", textObject);
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		this.textObject = void 0;
		this.textMask = void 0;
		super.destroy(fromScene);
	}
	setClampMode(mode) {
		if (mode === void 0) mode = true;
		this.clampTextOY = mode;
		return this;
	}
	clearTextMetricsCache() {}
	preLayout() {
		this.clearTextMetricsCache();
		PreLayout$3.call(this);
		return this;
	}
	layoutChildren() {
		var child, childConfig, padding;
		var startX = this.left, startY = this.top;
		var x, y, width, height;
		child = this.textObject;
		if (!child.rexSizer.hidden) {
			childConfig = child.rexSizer;
			padding = childConfig.padding;
			x = startX + padding.left * this.scaleX;
			y = startY + padding.top * this.scaleY;
			width = this.width * this.scaleX - (padding.left + padding.right) * this.scaleX;
			height = this.height * this.scaleY - (padding.top + padding.bottom) * this.scaleY;
			this.resizeText(child, width, height);
			AlignIn(child, x, y, width, height, childConfig.align);
			childConfig.preOffsetY = 0;
			this.resetTextObjectPosition();
			if (this.textMask) {
				this.textMask.setPosition().resize();
				this.resetChildPositionState(this.textMask);
			}
		}
	}
	resizeText(textObject, width, height) {
		return this;
	}
	resetTextObjectPosition() {
		var config = this.textObject.rexSizer;
		this.textObject.y += config.offsetY - config.preOffsetY;
		config.preOffsetY = config.offsetY;
		this.resetChildPositionState(this.textObject);
		if (this.textCropEnable) this.cropTextObject();
	}
	cropTextObject() {
		if (!this.textObject.setCrop) return;
		var offsetY = this.textObject.rexSizer.offsetY;
		var cropY, cropHeight;
		if (offsetY <= 0) {
			cropY = -offsetY;
			cropHeight = this.height;
		} else {
			cropY = 0;
			cropHeight = this.height - offsetY;
		}
		this.textObject.setCrop(0, cropY, this.width, cropHeight);
	}
	textOYExceedTop(oy) {
		if (oy === void 0) oy = this.textOY;
		return oy > this.topTextOY;
	}
	textOYExeceedBottom(oy) {
		if (oy === void 0) oy = this.textOY;
		return oy < this.bottomTextOY;
	}
	get textOY() {
		return this._textOY;
	}
	set textOY(oy) {
		var topTextOY = this.topTextOY;
		var bottomTextOY = this.bottomTextOY;
		var textOYExceedTop = this.textOYExceedTop(oy);
		var textOYExeceedBottom = this.textOYExeceedBottom(oy);
		if (this.clampTextOY) {
			if (textOYExceedTop) oy = topTextOY;
			else if (textOYExeceedBottom) oy = bottomTextOY;
		}
		if (this._textOY !== oy) {
			this._textOY = oy;
			this.updateTextObject();
		}
		if (textOYExceedTop) {
			if (!this.execeedTopState) this.emit("execeedtop", this, oy, topTextOY);
		}
		this.execeedTopState = textOYExceedTop;
		if (textOYExeceedBottom) {
			if (!this.execeedBottomState) this.emit("execeedbottom", this, oy, bottomTextOY);
		}
		this.execeedBottomState = textOYExeceedBottom;
	}
	setTextOY(oy) {
		this.textOY = oy;
		return this;
	}
	set t(value) {
		var bottomTextOY = this.bottomTextOY;
		this.textOY = bottomTextOY === 0 ? 0 : bottomTextOY * value;
	}
	get t() {
		var bottomTextOY = this.bottomTextOY;
		if (bottomTextOY === 0) return 0;
		return this.textOY / bottomTextOY;
	}
	setTextOYByPercentage(percentage) {
		this.t = percentage;
		return this;
	}
	get topTextOY() {
		return 0;
	}
	get bottomTextOY() {
		return -this.textVisibleHeight;
	}
	get textVisibleHeight() {
		return 0;
	}
	get textHeight() {
		return 0;
	}
	get textObjectHeight() {
		return 0;
	}
};
var CreateDefaultTextObject = function(scene) {
	return scene.add.text(0, 0, "");
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textarea/textblock/TextBlock.js
var TextBlock = class extends BaseTextBlock {
	constructor(scene, x, y, minWidth, minHeight, config) {
		super(scene, x, y, minWidth, minHeight, config);
		this.type = "rexTextBlock";
		this.lines = void 0;
		this._textLineHeight = void 0;
		this._textLineSpacing = void 0;
		this._visibleLinesCount = void 0;
		this._textHeight = void 0;
		this._textVisibleHeight = void 0;
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		if (this.lines) {
			this.lines.length = 0;
			this.lines = void 0;
		}
		super.destroy(fromScene);
	}
	clearTextMetricsCache() {
		this._textLineHeight = void 0;
		this._textLineSpacing = void 0;
		this._visibleLinesCount = void 0;
		this._textHeight = void 0;
		this._textVisibleHeight = void 0;
	}
	setText(text) {
		if (text !== void 0) this.text = text;
		this.lines = TextToLines(this.textObject, this.text, this.lines);
		this.linesCount = this.lines.length;
		this._textHeight = void 0;
		this._textVisibleHeight = void 0;
		this.updateTextObject();
		return this;
	}
	updateTextObject() {
		var startLineIndex = Math.max(Math.floor(this.textHeightToLinesCount(-this.textOY)), 0);
		var textOffset = this.linesCountToTextHeight(startLineIndex) + this.textOY;
		var text = this.getLines(startLineIndex);
		SetNoWrapText(this.textObject, text);
		this.textObject.rexSizer.offsetY = textOffset;
		this.resetTextObjectPosition();
		return this;
	}
	resizeText(textObject, width, height) {
		height += this.textLineHeight + this.textLineSpacing;
		if (this.textObjectWidth === width && this._textObjectRealHeight === height) return;
		this.textObjectWidth = width;
		this._textObjectRealHeight = height;
		switch (this.textObjectType) {
			case 0:
				textObject.setFixedSize(width, height);
				var style = textObject.style;
				style.wordWrapWidth = Math.max(width, 0);
				break;
			case 2: textObject.setMaxWidth(width);
		}
		this.setText();
	}
	getLines(startLineIdx) {
		var endLineIdx = startLineIdx + this.visibleLinesCount + 1;
		return this.lines.slice(startLineIdx, endLineIdx).join("\n");
	}
	textHeightToLinesCount(height) {
		return (height - this.textLineSpacing) / (this.textLineHeight + this.textLineSpacing);
	}
	linesCountToTextHeight(linesCount) {
		var height = linesCount * (this.textLineHeight + this.textLineSpacing);
		if (linesCount > 1) height -= this.textLineSpacing;
		return height;
	}
	get textLineHeight() {
		if (this._textLineHeight === void 0) {
			var lineHeight;
			switch (this.textObjectType) {
				case 0:
					var style = this.textObject.style;
					lineHeight = style.metrics.fontSize + style.strokeThickness;
					break;
				case 2:
					var scale = this.textObject.fontSize / this.textObject.fontData.size;
					lineHeight = this.textObject.fontData.lineHeight * scale;
			}
			this._textLineHeight = lineHeight;
		}
		return this._textLineHeight;
	}
	get textLineSpacing() {
		if (this._textLineSpacing === void 0) {
			var lineSpacing;
			switch (this.textObjectType) {
				case 0:
					lineSpacing = this.textObject.lineSpacing;
					break;
				case 2: lineSpacing = 0;
			}
			this._textLineSpacing = lineSpacing;
		}
		return this._textLineSpacing;
	}
	get visibleLinesCount() {
		if (this._visibleLinesCount === void 0) this._visibleLinesCount = Math.floor(this.textHeightToLinesCount(this._textObjectRealHeight));
		return this._visibleLinesCount;
	}
	get textHeight() {
		if (this._textHeight === void 0) this._textHeight = this.linesCountToTextHeight(this.linesCount);
		return this._textHeight;
	}
	get textObjectHeight() {
		return this._textObjectRealHeight - (this.textLineHeight + this.textLineSpacing);
	}
	get textVisibleHeight() {
		if (this._textVisibleHeight === void 0) {
			var h = this.textHeight - this.textObjectHeight;
			if (!this.alwaysScrollable && h < 0) h = 0;
			this._textVisibleHeight = h;
		}
		return this._textVisibleHeight;
	}
	get textOY() {
		return this._textOY;
	}
	set textOY(oy) {
		if (this.clampTextOY && this.visibleLinesCount > this.linesCount) oy = 0;
		super.textOY = oy;
	}
	scrollToLine(lineIndex) {
		var lineHeight = this.textLineHeight + this.textLineSpacing;
		this.textOY = -lineHeight * lineIndex;
		return this;
	}
	get lineIndex() {
		var lineHeight = this.textLineHeight + this.textLineSpacing;
		return Math.floor(-this.textOY / lineHeight);
	}
	scrollToNextLine(lineCount) {
		if (lineCount === void 0) lineCount = 1;
		this.scrollToLine(this.lineIndex + lineCount);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textarea/textblock/TagTextBlock.js
var TagTextBlock = class extends BaseTextBlock {
	constructor(scene, x, y, minWidth, minHeight, config) {
		super(scene, x, y, minWidth, minHeight, config);
		this.type = "rexTagTextBlock";
	}
	setText(text) {
		if (text !== void 0) this.text = text;
		var textObject = this.textObject;
		if (textObject.text === this.text) textObject.updateText(true);
		else textObject.setText(this.text);
		this.linesCount = this.getLinesCount();
		this._textHeight = void 0;
		this._textVisibleHeight = void 0;
		this.textOY = this._textOY;
		return this;
	}
	updateTextObject() {
		this.textObject.scrollY = this._textOY;
		return this;
	}
	getLinesCount() {
		var lines = this.getLines();
		return lines ? lines.length : 0;
	}
	getLines() {
		var canvasText = this.textObject.canvasText;
		return canvasText ? canvasText.lines : null;
	}
	get textHeight() {
		return this.textObject.contentHeight;
	}
	get textObjectHeight() {
		return this.textObject.viewportHeight;
	}
	get textVisibleHeight() {
		var h = this.textHeight - this.textObjectHeight;
		if (!this.alwaysScrollable && h < 0) h = 0;
		return h;
	}
	resizeText(textObject, width, height) {
		if (this.textObjectWidth === width && this._textObjectRealHeight === height) return this;
		this.textObjectWidth = width;
		this._textObjectRealHeight = height;
		textObject.setFixedSize(width, height);
		var style = textObject.style;
		var wrapWidth = Math.max(width, 0);
		if (style.wrapMode === 0) style.wrapMode = 1;
		style.wrapWidth = wrapWidth;
		this.setText();
		return this;
	}
	resetTextObjectPosition() {
		this.resetChildPositionState(this.textObject);
	}
	scrollToLine(lineIndex) {
		var lines = this.getLines();
		if (!lines || lines.length === 0) {
			this.textOY = 0;
			return this;
		}
		if (lineIndex <= 0) {
			this.textOY = 0;
			return this;
		}
		if (lineIndex >= lines.length) {
			this.textOY = this.bottomTextOY;
			return this;
		}
		this.textOY = -lines[lineIndex].startOffset;
		return this;
	}
	get lineIndex() {
		var lines = this.getLines();
		if (!lines || lines.length === 0) return 0;
		return GetStartLineIndex(lines, -this.textOY);
	}
	scrollToNextLine(lineCount) {
		if (lineCount === void 0) lineCount = 1;
		this.scrollToLine(this.lineIndex + lineCount);
		return this;
	}
};
var GetStartLineIndex = function(lines, targetOffset) {
	var left = 0;
	var right = lines.length - 1;
	var result = lines.length;
	while (left <= right) {
		var mid = left + right >> 1;
		if (lines[mid].endOffset > targetOffset) {
			result = mid;
			right = mid - 1;
		} else left = mid + 1;
	}
	return result;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textarea/InjectProperties.js
var InjectProperties$2 = function(textBlock) {
	Object.defineProperty(textBlock, "childOY", {
		configurable: true,
		get: function() {
			return textBlock.textOY;
		},
		set: function(value) {
			textBlock.textOY = value;
		}
	});
	Object.defineProperty(textBlock, "topChildOY", { get: function() {
		return textBlock.topTextOY;
	} });
	Object.defineProperty(textBlock, "bottomChildOY", { get: function() {
		return textBlock.bottomTextOY;
	} });
	Object.defineProperty(textBlock, "childVisibleHeight", { get: function() {
		return textBlock.textObjectHeight;
	} });
	Object.defineProperty(textBlock, "childHeight", { get: function() {
		return textBlock.textHeight;
	} });
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textarea/SetTextMethods.js
var SetTextMethods_default$2 = {
	setText(text) {
		this.childrenMap.child.setText(text);
		this.resizeController();
		return this;
	},
	appendText(text) {
		this.setText(this.text + text);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textarea/ScrollMethods.js
var EaseScrollChildOY = function(y0, y1, duration, ease) {
	if (duration === void 0 || duration <= 0) return;
	if (this._easeScrollChildOY === void 0) this._easeScrollChildOY = new EaseValueTask(this);
	this._easeScrollChildOY.restart({
		key: "childOY",
		from: y0,
		to: y1,
		duration,
		ease
	});
};
var ScrollMethods_default$2 = {
	scrollToLine(lineIndex, duration, ease) {
		var y0 = this.childOY;
		this.childrenMap.child.scrollToLine(lineIndex);
		var y1 = this.childOY;
		EaseScrollChildOY.call(this, y0, y1, duration, ease);
		return this;
	},
	scrollToNextLine(lineCount, duration, ease) {
		var y0 = this.childOY;
		this.childrenMap.child.scrollToNextLine(lineCount);
		var y1 = this.childOY;
		EaseScrollChildOY.call(this, y0, y1, duration, ease);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textarea/TextArea.js
var GetValue$130 = __webpack_exports__Utils.Objects.GetValue;
var TextArea = class extends Scrollable$1 {
	constructor(scene, config) {
		if (config === void 0) config = {};
		var textObject = GetValue$130(config, "text", void 0);
		var textWidth = GetValue$130(config, "textWidth", void 0);
		var textHeight = GetValue$130(config, "textHeight", void 0);
		var textCrop = GetValue$130(config, "textCrop", !!textObject.setCrop);
		var textMask = GetValue$130(config, "textMask", !textCrop);
		var content = GetValue$130(config, "content", "");
		var textBlockConfig = {
			width: textWidth,
			height: textHeight,
			text: textObject,
			textMask,
			textCrop: textCrop && !textMask,
			content,
			clampTextOY: GetValue$130(config, "clampChildOY", false),
			alwaysScrollable: GetValue$130(config, "alwaysScrollable", false)
		};
		var textBlock;
		if (textObject && GetTextObjectType(textObject) === 1) textBlock = new TagTextBlock(scene, textBlockConfig);
		else textBlock = new TextBlock(scene, textBlockConfig);
		scene.add.existing(textBlock);
		InjectProperties$2(textBlock);
		config.scrollMode = 0;
		config.type = "rexTextArea";
		config.child = {
			gameObject: textBlock,
			expandWidth: textWidth === void 0,
			expandHeight: textHeight === void 0
		};
		var spaceConfig = GetValue$130(config, "space", void 0);
		if (spaceConfig) spaceConfig.child = GetValue$130(spaceConfig, "text", 0);
		super(scene, config);
		this.addChildrenMap("text", textObject);
	}
	get text() {
		return this.childrenMap.child.text;
	}
	get lineIndex() {
		return this.childrenMap.child.lineIndex;
	}
	get linesCount() {
		return this.childrenMap.child.linesCount;
	}
	get contentHeight() {
		return this.childrenMap.child.textHeight;
	}
};
Object.assign(TextArea.prototype, SetTextMethods_default$2, ScrollMethods_default$2);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/build/CreateTextArea.js
var GetValue$129 = __webpack_exports__Utils.Objects.GetValue;
var CreateTextArea = function(scene, config, creators) {
	config = config ? DeepClone(config) : {};
	var createBackground = GetValue$129(creators, "background", CreateBackground$2);
	var createText = GetValue$129(creators, "text", CreateText);
	var createTrack = GetValue$129(creators, "track", CreateBackground$2);
	var createThumb = GetValue$129(creators, "thumb", CreateBackground$2);
	if (createBackground) config.background = createBackground(scene, config.background);
	else delete config.background;
	if (createText) config.text = createText(scene, config.text);
	else delete config.text;
	var sliderConfig = config.slider;
	if (sliderConfig !== false && sliderConfig !== null) {
		if (sliderConfig === void 0) sliderConfig = {};
		if (createTrack) sliderConfig.track = createTrack(scene, sliderConfig.track);
		else delete sliderConfig.track;
		if (createThumb) sliderConfig.thumb = createThumb(scene, sliderConfig.thumb);
		else delete sliderConfig.thumb;
		config.slider = sliderConfig;
	}
	var gameObject = new TextArea(scene, config);
	scene.add.existing(gameObject);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/build/CreateText.js
var CreateText = function(scene, config) {
	var gameObjectType;
	if (config) {
		if (config.hasOwnProperty("$type")) gameObjectType = config.$type;
		else if (config.hasOwnProperty("key")) {
			gameObjectType = "bitmaptext";
			config.font = config.key;
		}
	}
	var gameObject;
	switch (gameObjectType) {
		case "bitmaptext":
		case "bitmap":
			gameObject = new StatesBitmapText(scene, config);
			break;
		case "bbcodetext":
		case "bbcode":
			gameObject = new BBCodeText_default(scene, 0, 0, "", config);
			break;
		case "label":
			gameObject = new SimpleLabel(scene, config);
			break;
		case "textarea":
			gameObject = CreateTextArea(scene, config);
			break;
		default: gameObject = new StatesText(scene, config);
	}
	DecorateGameObject(gameObject, config);
	scene.add.existing(gameObject);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/build/CreateImage.js
var CreateImage = function(scene, config) {
	var gameObjectType;
	if (config) {
		if (config.hasOwnProperty("$type")) gameObjectType = config.$type;
		else if (config.hasOwnProperty("leftWidth")) gameObjectType = "nineSlice";
		else if (config.hasOwnProperty("color") || config.hasOwnProperty("strokeColor")) gameObjectType = "roundRectangle";
	}
	var gameObject;
	switch (gameObjectType) {
		case "nineSlice":
			if (!config.hasOwnProperty("stretchMode")) gameObject = new StatesNineSlice(scene, config);
			else gameObject = new StatesNinePatch(scene, config);
			break;
		case "roundRectangle":
			gameObject = new StatesRoundRectangle(scene, config);
			break;
		default: gameObject = new StatesImage(scene, config);
	}
	DecorateGameObject(gameObject, config);
	scene.add.existing(gameObject);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/build/BuildLabelConfig.js
var GetValue$128 = __webpack_exports__Utils.Objects.GetValue;
var BuildLabelConfig = function(scene, config, creators) {
	config = config ? DeepClone(config) : {};
	var createBackground = GetValue$128(creators, "background", CreateBackground$2);
	var createText = GetValue$128(creators, "text", CreateText);
	var createIcon = GetValue$128(creators, "icon", CreateImage);
	var createAction = GetValue$128(creators, "action", CreateImage);
	if (config.background !== null && createBackground) config.background = createBackground(scene, config.background);
	else delete config.background;
	if (config.text !== null && createText) config.text = createText(scene, config.text);
	else delete config.text;
	if (config.icon !== null && createIcon) config.icon = createIcon(scene, config.icon);
	else delete config.icon;
	if (config.action !== null && createAction) config.action = createAction(scene, config.action);
	else delete config.action;
	return config;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/simplelabel/SimpleLabel.js
var SimpleLabel = class extends Label {
	constructor(scene, config, creators) {
		config = BuildLabelConfig(scene, config, creators);
		super(scene, config);
		this.type = "rexSimpleLabel";
	}
	setActiveState(enable) {
		RunMethod$1(this.getChildren(), "setActiveState", enable);
		return this;
	}
	setHoverState(enable) {
		RunMethod$1(this.getChildren(), "setHoverState", enable);
		return this;
	}
	setDisableState(enable) {
		RunMethod$1(this.getChildren(), "setDisableState", enable);
		return this;
	}
};
var RunMethod$1 = function(children, methodName, enable) {
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var gameObject = children[i];
		if (gameObject && gameObject[methodName]) gameObject[methodName](enable);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/simplelabel/Factory.js
ObjectFactory.register("simpleLabel", function(config, creators) {
	var gameObject = new SimpleLabel(this.scene, config, creators);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.SimpleLabel", SimpleLabel);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/titlelabel/methods/LayoutMode0.js
var GetValue$127 = __webpack_exports__Utils.Objects.GetValue;
var LayoutMode0$4 = function(config) {
	var scene = this.scene;
	var orientation = GetValue$127(config, "orientation", 0);
	this.setOrientation(orientation);
	var icon = GetValue$127(config, "icon", void 0);
	var iconMask = GetValue$127(config, "iconMask", void 0);
	var innerBackground = GetValue$127(config, "innerBackground", void 0);
	var title = GetValue$127(config, "title", void 0);
	var separator = GetValue$127(config, "separator", void 0);
	var text = GetValue$127(config, "text", void 0);
	var action = GetValue$127(config, "action", void 0);
	var actionMask = GetValue$127(config, "actionMask", void 0);
	if (icon) {
		var align = GetValue$127(config, "align.icon", "center");
		var padding;
		if (this.orientation === 0) padding = {
			right: GetValue$127(config, "space.icon", 0),
			top: GetValue$127(config, "space.iconTop", 0),
			bottom: GetValue$127(config, "space.iconBottom", 0),
			left: GetValue$127(config, "space.iconLeft", 0)
		};
		else padding = {
			bottom: GetValue$127(config, "space.icon", 0),
			left: GetValue$127(config, "space.iconLeft", 0),
			right: GetValue$127(config, "space.iconRight", 0),
			top: GetValue$127(config, "space.iconTop", 0)
		};
		var fitRatio = GetValue$127(config, "squareFitIcon", false) ? 1 : 0;
		this.add(icon, {
			proportion: 0,
			align,
			padding,
			fitRatio
		});
		if (iconMask) iconMask = AddChildCircleMask(this, icon);
		if (!fitRatio) {
			var iconSize = GetValue$127(config, "iconSize", void 0);
			this.setIconSize(GetValue$127(config, "iconWidth", iconSize), GetValue$127(config, "iconHeight", iconSize));
		}
	}
	var innerSizer = new Sizer(scene, { orientation: 1 });
	if (innerBackground) innerSizer.addBackground(innerBackground);
	var separatorSpace = GetValue$127(config, "space.separator", 0);
	if (title) {
		var align = GetValue$127(config, "align.title", "left");
		var expandTitleWidth = GetValue$127(config, "expandTitleWidth", false);
		var proportion = GetValue$127(config, "expandTitleHeight", false) ? 1 : 0, padding, expand = expandTitleWidth;
		padding = {
			bottom: !separator && text ? GetValue$127(config, "space.title", separatorSpace) : 0,
			left: GetValue$127(config, "space.titleLeft", 0),
			right: GetValue$127(config, "space.titleRight", 0)
		};
		innerSizer.add(title, {
			proportion,
			expand,
			align,
			padding
		});
	}
	if (separator) {
		var padding = {
			top: title ? separatorSpace : 0,
			bottom: text ? separatorSpace : 0,
			left: GetValue$127(config, "space.separatorLeft", 0),
			right: GetValue$127(config, "space.separatorRight", 0)
		};
		innerSizer.add(separator, {
			expand: true,
			padding
		});
	}
	if (text) {
		var align = GetValue$127(config, "align.text", "left");
		var expandTextWidth = GetValue$127(config, "expandTextWidth", false);
		var proportion = GetValue$127(config, "expandTextHeight", false) ? 1 : 0, padding, expand = expandTextWidth;
		padding = {
			left: GetValue$127(config, "space.textLeft", 0),
			right: GetValue$127(config, "space.textRight", 0)
		};
		innerSizer.add(text, {
			proportion,
			expand,
			align,
			padding
		});
	}
	var padding = void 0;
	if (action) padding = { right: GetValue$127(config, "space.text", 0) };
	this.add(innerSizer, {
		proportion: 1,
		padding
	});
	if (action) {
		var align = GetValue$127(config, "align.action", "center");
		var padding;
		if (this.orientation === 0) padding = {
			top: GetValue$127(config, "space.actionTop", 0),
			bottom: GetValue$127(config, "space.actionBottom", 0),
			right: GetValue$127(config, "space.actionRight", 0)
		};
		else padding = {
			left: GetValue$127(config, "space.actionLeft", 0),
			right: GetValue$127(config, "space.actionRight", 0),
			bottom: GetValue$127(config, "space.actionBottom", 0)
		};
		var fitRatio = GetValue$127(config, "squareFitAction", false) ? 1 : 0;
		this.add(action, {
			proportion: 0,
			align,
			padding,
			fitRatio
		});
		if (actionMask) actionMask = AddChildCircleMask(this, action);
		if (!fitRatio) {
			var actionSize = GetValue$127(config, "actionSize");
			this.setActionSize(GetValue$127(config, "actionWidth", actionSize), GetValue$127(config, "actionHeight", actionSize));
		}
	}
	this.addChildrenMap("icon", icon);
	this.addChildrenMap("iconMask", iconMask);
	this.addChildrenMap("innerSizer", innerSizer);
	this.addChildrenMap("innerBackground", innerBackground);
	this.addChildrenMap("title", title);
	this.addChildrenMap("separator", separator);
	this.addChildrenMap("text", text);
	this.addChildrenMap("action", action);
	this.addChildrenMap("actionMask", actionMask);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/titlelabel/methods/LayoutMode1.js
var GetValue$126 = __webpack_exports__Utils.Objects.GetValue;
var LayoutMode1$3 = function(config) {
	this.setOrientation(1);
	this.setRTL(false);
	var scene = this.scene;
	var title = GetValue$126(config, "title", void 0);
	var separator = GetValue$126(config, "separator", void 0);
	var innerBackground = GetValue$126(config, "innerBackground", void 0);
	var icon = GetValue$126(config, "icon", void 0);
	var iconMask = GetValue$126(config, "iconMask", void 0);
	var text = GetValue$126(config, "text", void 0);
	var action = GetValue$126(config, "action", void 0);
	var actionMask = GetValue$126(config, "actionMask", void 0);
	if (title) {
		var align = GetValue$126(config, "align.title", "left");
		var expandTitleWidth = GetValue$126(config, "expandTitleWidth", false);
		var proportion = GetValue$126(config, "expandTitleHeight", false) ? 1 : 0, padding, expand = expandTitleWidth;
		padding = {
			bottom: GetValue$126(config, "space.title", 0),
			left: GetValue$126(config, "space.titleLeft", 0),
			right: GetValue$126(config, "space.titleRight", 0)
		};
		this.add(title, {
			proportion,
			expand,
			align,
			padding
		});
	}
	if (separator) {
		var separatorSpace = GetValue$126(config, "space.separator", 0);
		var padding = {
			top: title ? separatorSpace : 0,
			bottom: text ? separatorSpace : 0,
			left: GetValue$126(config, "space.separatorLeft", 0),
			right: GetValue$126(config, "space.separatorRight", 0)
		};
		this.add(separator, {
			proportion: 0,
			expand: true,
			padding
		});
	}
	var innerSizer = new Sizer(scene, {
		orientation: GetValue$126(config, "orientation", 0),
		rtl: GetValue$126(config, "rtl", false),
		space: {
			left: GetValue$126(config, "space.innerLeft", 0),
			right: GetValue$126(config, "space.innerRight", 0),
			top: GetValue$126(config, "space.innerTop", 0),
			bottom: GetValue$126(config, "space.innerBottom", 0)
		}
	});
	if (innerBackground) innerSizer.addBackground(innerBackground);
	this.add(innerSizer, {
		proportion: 1,
		expand: true
	});
	if (icon) {
		var align = GetValue$126(config, "align.icon", "center");
		var padding;
		if (innerSizer.orientation === 0) padding = {
			right: GetValue$126(config, "space.icon", 0),
			top: GetValue$126(config, "space.iconTop", 0),
			bottom: GetValue$126(config, "space.iconBottom", 0),
			left: GetValue$126(config, "space.iconLeft", 0)
		};
		else padding = {
			bottom: GetValue$126(config, "space.icon", 0),
			left: GetValue$126(config, "space.iconLeft", 0),
			right: GetValue$126(config, "space.iconRight", 0),
			top: GetValue$126(config, "space.iconTop", 0)
		};
		var fitRatio = GetValue$126(config, "squareFitIcon", false) ? 1 : 0;
		innerSizer.add(icon, {
			proportion: 0,
			align,
			padding,
			fitRatio
		});
		if (iconMask) iconMask = AddChildCircleMask(this, icon);
		if (!fitRatio) {
			var iconSize = GetValue$126(config, "iconSize", void 0);
			this.setIconSize(GetValue$126(config, "iconWidth", iconSize), GetValue$126(config, "iconHeight", iconSize));
		}
	}
	if (text) {
		var align = GetValue$126(config, "align.text", "left");
		var textSpace = GetValue$126(config, "space.text", 0);
		var expandTextWidth = GetValue$126(config, "expandTextWidth", false);
		var expandTextHeight = GetValue$126(config, "expandTextHeight", false);
		var proportion, padding, expand;
		if (innerSizer.orientation === 0) {
			proportion = expandTextWidth ? 1 : 0;
			if (action) padding = { right: textSpace };
			expand = expandTextHeight;
		} else {
			proportion = expandTextHeight ? 1 : 0;
			if (action) padding = { bottom: textSpace };
			expand = expandTextWidth;
		}
		innerSizer.add(text, {
			proportion,
			expand,
			align,
			padding
		});
	}
	if (action) {
		var align = GetValue$126(config, "align.action", "center");
		var padding;
		if (innerSizer.orientation === 0) padding = {
			top: GetValue$126(config, "space.actionTop", 0),
			bottom: GetValue$126(config, "space.actionBottom", 0),
			right: GetValue$126(config, "space.actionRight", 0)
		};
		else padding = {
			left: GetValue$126(config, "space.actionLeft", 0),
			right: GetValue$126(config, "space.actionRight", 0),
			bottom: GetValue$126(config, "space.actionBottom", 0)
		};
		var fitRatio = GetValue$126(config, "squareFitAction", false) ? 1 : 0;
		innerSizer.add(action, {
			proportion: 0,
			align,
			padding,
			fitRatio
		});
		if (actionMask) actionMask = AddChildCircleMask(this, action);
		if (!fitRatio) {
			var actionSize = GetValue$126(config, "actionSize");
			this.setActionSize(GetValue$126(config, "actionWidth", actionSize), GetValue$126(config, "actionHeight", actionSize));
		}
	}
	this.addChildrenMap("title", title);
	this.addChildrenMap("separator", separator);
	this.addChildrenMap("innerSizer", innerSizer);
	this.addChildrenMap("innerBackground", innerBackground);
	this.addChildrenMap("icon", icon);
	this.addChildrenMap("iconMask", iconMask);
	this.addChildrenMap("text", text);
	this.addChildrenMap("action", action);
	this.addChildrenMap("actionMask", actionMask);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/titlelabel/TitleLabel.js
var GetValue$125 = __webpack_exports__Utils.Objects.GetValue;
var LayoutCallbacks$3 = [LayoutMode0$4, LayoutMode1$3];
var TitleLabel = class extends LabelBase {
	constructor(scene, config) {
		if (config === void 0) config = {};
		super(scene, config);
		this.type = "rexTitleLabel";
		var background = GetValue$125(config, "background", void 0);
		if (background) this.addBackground(background);
		var title = GetValue$125(config, "title", void 0);
		var text = GetValue$125(config, "text", void 0);
		if (title) {
			var wrapTitle = GetValue$125(config, "wrapTitle", false);
			var adjustTitleFontSize = GetValue$125(config, "adjustTitleFontSize", false);
			if (wrapTitle) {
				if (wrapTitle === true) wrapTitle = "word";
				SetWrapMode(title, wrapTitle);
				config.expandTitleWidth = true;
				WrapExpandText(title);
			} else if (adjustTitleFontSize) {
				config.expandTextWidth = true;
				config.expandTextHeight = true;
				FontSizeExpandText(title, { fitHeight: true });
			}
		}
		if (text) {
			var wrapText = GetValue$125(config, "wrapText", false);
			var adjustTextFontSize = GetValue$125(config, "adjustTextFontSize", false);
			if (wrapText) {
				if (wrapText === true) wrapText = "word";
				SetWrapMode(text, wrapText);
				config.expandTextWidth = true;
				WrapExpandText(text);
			} else if (adjustTextFontSize) {
				config.expandTextWidth = true;
				config.expandTextHeight = true;
				FontSizeExpandText(text, { fitHeight: true });
			}
		}
		(LayoutCallbacks$3[GetValue$125(config, "layoutMode", 0)] || LayoutCallbacks$3[0]).call(this, config);
		this.addChildrenMap("background", config.background);
	}
	get title() {
		var textObject = this.childrenMap.title;
		if (!textObject) return "";
		return textObject.title;
	}
	set title(value) {
		var textObject = this.childrenMap.title;
		if (!textObject) return;
		textObject.setText(value);
	}
	setTitle(value) {
		this.title = value;
		return this;
	}
	resetDisplayContent(config) {
		if (config === void 0) config = {};
		else if (typeof config === "string") config = { text: config };
		super.resetDisplayContent(config);
		var textObject = this.childrenMap.title;
		if (textObject) {
			if (config.title === void 0) {} else if (config.title) {
				this.show(textObject);
				this.setTitle(config.title);
			} else this.hide(textObject);
		}
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/titlelabel/Factory.js
ObjectFactory.register("titleLabel", function(config) {
	var gameObject = new TitleLabel(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.TitleLabel", TitleLabel);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/build/BuildTitleLabelConfig.js
var GetValue$124 = __webpack_exports__Utils.Objects.GetValue;
var BuildTitleLabelConfig = function(scene, config, creators) {
	var createInnerBackground = GetValue$124(creators, "innerBackground", CreateBackground$2);
	var createSeparator = GetValue$124(creators, "separator", CreateBackground$2);
	var createTitle = GetValue$124(creators, "title", CreateText);
	var innerBackground, separator;
	if (config.innerBackground !== null && createInnerBackground) innerBackground = createInnerBackground(scene, config.innerBackground);
	if (config.separator !== null && createSeparator) separator = createSeparator(scene, config.separator);
	config = BuildLabelConfig(scene, config, creators);
	if (innerBackground) config.innerBackground = innerBackground;
	else delete config.innerBackground;
	if (separator) config.separator = separator;
	else delete config.separator;
	if (config.title !== null && createTitle) config.title = createTitle(scene, config.title);
	else delete config.title;
	var background = config.background;
	if (background) {
		if (innerBackground) scene.children.moveBelow(background, innerBackground);
		else if (separator) scene.children.moveBelow(background, separator);
	}
	return config;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/simpletitlelabel/SimpleTitleLabel.js
var SimpleTitleLabel = class extends TitleLabel {
	constructor(scene, config, creators) {
		config = BuildTitleLabelConfig(scene, config, creators);
		super(scene, config);
		this.type = "rexSimpleTitleLabel";
	}
	setActiveState(enable) {
		RunMethod(this.getChildren(), "setActiveState", enable);
		return this;
	}
	setHoverState(enable) {
		RunMethod(this.getChildren(), "setHoverState", enable);
		return this;
	}
	setDisableState(enable) {
		RunMethod(this.getChildren(), "setDisableState", enable);
		return this;
	}
};
var RunMethod = function(children, methodName, enable) {
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var gameObject = children[i];
		if (gameObject && gameObject[methodName]) gameObject[methodName](enable);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/simpletitlelabel/Factory.js
ObjectFactory.register("simpleTitleLabel", function(config, creators) {
	var gameObject = new SimpleTitleLabel(this.scene, config, creators);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.SimpleTitleLabel", SimpleTitleLabel);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/namevaluelabel/methods/Build.js
var GetValue$123 = __webpack_exports__Utils.Objects.GetValue;
var IsPlainObject$15 = __webpack_exports__Utils.Objects.IsPlainObject;
var Build$3 = function(scene, config) {
	var background = GetValue$123(config, "background", void 0);
	var icon = GetValue$123(config, "icon", void 0);
	var iconMask = GetValue$123(config, "iconMask", void 0);
	var nameText = GetValue$123(config, "nameText", void 0);
	var valueText = GetValue$123(config, "valueText", void 0);
	var bar = GetValue$123(config, "bar", void 0);
	var action = GetValue$123(config, "action", void 0);
	var actionMask = GetValue$123(config, "actionMask", void 0);
	var isLineBar = GetValue$123(config, "barShape", "line") === "line";
	if (IsPlainObject$15(bar)) {
		bar = new (isLineBar ? LineProgressCanvas_default : CircularProgressCanvas_default)(scene, bar);
		scene.add.existing(bar);
		if (nameText) scene.children.moveBelow(bar, nameText);
		if (valueText) scene.children.moveBelow(bar, valueText);
	}
	var hasTextSizer = nameText || valueText || bar;
	if (background) this.addBackground(background);
	if (icon) {
		var padding = void 0;
		if (this.orientation === 0) {
			if (hasTextSizer || action) padding = {
				right: GetValue$123(config, "space.icon", 0),
				top: GetValue$123(config, "space.iconTop", 0),
				bottom: GetValue$123(config, "space.iconBottom", 0)
			};
		} else if (hasTextSizer || action) padding = {
			bottom: GetValue$123(config, "space.icon", 0),
			left: GetValue$123(config, "space.iconLeft", 0),
			right: GetValue$123(config, "space.iconRight", 0)
		};
		this.add(icon, {
			proportion: 0,
			padding
		});
		if (iconMask) iconMask = AddChildCircleMask(this, icon);
	}
	if (hasTextSizer) {
		var textSizer = new Sizer(scene, { orientation: 1 });
		var nameValueSizer;
		if (nameText || valueText) {
			nameValueSizer = new Sizer(scene, { orientation: 0 });
			if (nameText) {
				if (nameText.text === "") nameText.setText(" ");
				nameText.setOrigin(0, nameText.originY);
				var padding = { left: GetValue$123(config, "space.name", 0) };
				nameValueSizer.add(nameText, { padding });
			}
			if (valueText) {
				if (valueText.text === "") valueText.setText(" ");
				valueText.setOrigin(1, valueText.originY);
				nameValueSizer.addSpace();
				var padding = { right: GetValue$123(config, "space.value", 0) };
				nameValueSizer.add(valueText, { padding });
				this.setValueTextFormatCallback(GetValue$123(config, "valueTextFormatCallback", DefaultValueTextFormatCallback), GetValue$123(config, "valueTextFormatCallbackScope", void 0));
			}
			textSizer.add(nameValueSizer, { expand: true });
		}
		if (bar) {
			if (isLineBar) {
				var paddingTop = nameValueSizer ? GetValue$123(config, "space.bar") : 0;
				if (paddingTop === void 0) paddingTop = GetValue$123(config, "space.barTop", 0);
				var padding = {
					top: paddingTop,
					bottom: GetValue$123(config, "space.barBottom", 0),
					left: GetValue$123(config, "space.barLeft", 0),
					right: GetValue$123(config, "space.barRight", 0)
				};
				textSizer.add(bar, {
					expand: true,
					padding
				});
			} else {
				var padding = {
					top: GetValue$123(config, "space.barTop", 0),
					bottom: GetValue$123(config, "space.barBottom", 0),
					left: GetValue$123(config, "space.barLeft", 0),
					right: GetValue$123(config, "space.barRight", 0)
				};
				this.addBackground(bar, padding);
			}
		}
		var padding = void 0;
		if (action) padding = { right: GetValue$123(config, "space.text", 0) };
		var textAlign = GetValue$123(config, "align.text", "bottom");
		this.add(textSizer, {
			proportion: 1,
			align: textAlign,
			padding
		});
	}
	if (action) {
		var padding;
		if (this.orientation === 0) padding = {
			top: GetValue$123(config, "space.actionTop", 0),
			bottom: GetValue$123(config, "space.actionBottom", 0)
		};
		else padding = {
			left: GetValue$123(config, "space.actionLeft", 0),
			right: GetValue$123(config, "space.actionRight", 0)
		};
		this.add(action, {
			proportion: 0,
			padding
		});
		if (actionMask) actionMask = AddChildCircleMask(this, action);
	}
	this.addChildrenMap("background", background);
	this.addChildrenMap("icon", icon);
	this.addChildrenMap("iconMask", iconMask);
	this.addChildrenMap("name", nameText);
	this.addChildrenMap("value", valueText);
	this.addChildrenMap("bar", bar);
	this.addChildrenMap("action", action);
	this.addChildrenMap("actionMask", actionMask);
};
var DefaultValueTextFormatCallback = function(value, min, max) {
	return value.toString();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/namevaluelabel/methods/SetValueMethods.js
var Clamp$7 = __webpack_exports__Math.Clamp;
var SetValueMethods_default = {
	setValueTextFormatCallback(callback, scope) {
		this.valueTextFormatCallback = callback;
		this.valueTextFormatCallbackScope = scope;
		return this;
	},
	getFormatValueText(value, min, max) {
		if (value === void 0) value = this.value;
		if (min === void 0) min = this.minValue;
		if (max === void 0) max = this.maxValue;
		var text;
		if (this.valueTextFormatCallbackScope) text = this.valueTextFormatCallback(value, min, max);
		else text = this.valueTextFormatCallback.call(this.valueTextFormatCallbackScope, value, min, max);
		return text;
	},
	updateValueText(value, min, max) {
		var textObject = this.childrenMap.value;
		if (textObject && this.valueTextFormatCallback) {
			textObject.setText(this.getFormatValueText(value, min, max));
			if (textObject.layout) textObject.layout();
		}
		return this;
	},
	setValue(value, min, max) {
		if (min === void 0) min = this.minValue;
		else this.minValue = min;
		if (max === void 0) max = this.maxValue;
		else this.maxValue = max;
		value = Clamp$7(value, min, max);
		this.value = value;
		this.updateValueText(value, min, max);
		this.setBarValue(value, min, max);
		return this;
	},
	setEaseValueDuration(duration) {
		this.easeValueDuration = duration;
		return this;
	},
	easeValueTo(value, min, max) {
		if (min === void 0) min = this.minValue;
		else this.minValue = min;
		if (max === void 0) max = this.maxValue;
		else this.maxValue = max;
		if (this.easeValueTask === void 0) {
			this.easeValueTask = new EaseValueTask(this);
			this.easeValueTask.on("update", function() {
				this.setValue(this.value, this.minValue, this.maxValue);
			}, this);
		}
		this.easeValueTask.restart({
			key: "value",
			to: value,
			duration: this.easeValueDuration
		});
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/namevaluelabel/NameValueLabel.js
var NameValueLabel = class extends Sizer {
	constructor(scene, config) {
		super(scene, config);
		this.type = "rexNameValueLabel";
		Build$3.call(this, scene, config);
		this.setEaseValueDuration(1e3);
	}
	get nameText() {
		var textObject = this.childrenMap.name;
		if (textObject === void 0) return "";
		return textObject.text;
	}
	set nameText(value) {
		var textObject = this.childrenMap.name;
		if (textObject === void 0) return;
		textObject.setText(value);
	}
	setNameText(value) {
		this.nameText = value;
		return this;
	}
	get valueText() {
		var textObject = this.childrenMap.value;
		if (textObject === void 0) return "";
		return textObject.text;
	}
	set valueText(value) {
		var textObject = this.childrenMap.value;
		if (textObject === void 0) return;
		textObject.setText(value);
	}
	setValueText(value) {
		this.valueText = value;
		return this;
	}
	get barValue() {
		var bar = this.childrenMap.bar;
		if (bar === void 0) return;
		return bar.value;
	}
	set barValue(value) {
		var bar = this.childrenMap.bar;
		if (bar === void 0) return;
		bar.setValue(value);
	}
	setBarValue(value, min, max) {
		var bar = this.childrenMap.bar;
		if (bar === void 0) return this;
		bar.setValue(value, min, max);
		return this;
	}
	easeBarValueTo(value, min, max) {
		var bar = this.childrenMap.bar;
		if (bar === void 0) return this;
		bar.easeValueTo(value, min, max);
		return this;
	}
	setTexture(key, frame) {
		var imageObject = this.childrenMap.icon;
		if (imageObject === void 0) return;
		imageObject.setTexture(key, frame);
		return this;
	}
	get texture() {
		var imageObject = this.childrenMap.icon;
		if (imageObject === void 0) return;
		return imageObject.texture;
	}
	get frame() {
		var imageObject = this.childrenMap.icon;
		if (imageObject === void 0) return;
		return imageObject.frame;
	}
	postLayout(parent, newWidth, newHeight) {
		var iconMask = this.childrenMap.iconMask;
		if (iconMask) {
			iconMask.setPosition();
			this.resetChildPositionState(iconMask);
		}
		var actionMask = this.childrenMap.actionMask;
		if (actionMask) {
			actionMask.setPosition();
			this.resetChildPositionState(actionMask);
		}
		super.postLayout(parent, newWidth, newHeight);
		return this;
	}
	resize(width, height) {
		super.resize(width, height);
		var iconMask = this.childrenMap.iconMask;
		if (iconMask) iconMask.resize();
		var actionMask = this.childrenMap.actionMask;
		if (actionMask) actionMask.resize();
		return this;
	}
};
Object.assign(NameValueLabel.prototype, SetValueMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/namevaluelabel/Factory.js
ObjectFactory.register("nameValueLabel", function(config) {
	var gameObject = new NameValueLabel(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.NameValueLabel", NameValueLabel);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/logic/levelcounter/LevelCounter.js
var LevelCounter = class extends EventEmitter {
	constructor(config) {
		super();
		this.setTable(GetValue$279(config, "table"));
		this.setMaxLevel(GetValue$279(config, "maxLevel"));
		var exp = GetValue$279(config, "exp", 0);
		var level = GetValue$279(config, "level", void 0);
		if (level !== void 0 && !this.checkLevel(level, exp)) {
			console.error(`Level ${level} and Exp ${exp} are mismatch`);
			level = void 0;
		}
		this.resetExp(exp, level);
	}
	setTable(table) {
		this.levelTable = table;
		this.isLevelMapFunction = IsFunction(table);
		return this;
	}
	setMaxLevel(maxLevel) {
		if (maxLevel === void 0) {
			if (Array.isArray(this.levelTable)) maxLevel = this.levelTable.length - 1;
			else maxLevel = -1;
		}
		var maxExp;
		if (maxLevel !== -1) maxExp = this.getExp(maxLevel);
		else maxExp = -1;
		this.hasMaxLevel = maxLevel !== -1;
		this.maxLevel = maxLevel;
		this.maxExp = maxExp;
		return this;
	}
	resetExp(exp, level) {
		if (this.hasMaxLevel && exp > this.maxExp) exp = this.maxExp;
		if (level === void 0) level = this.getLevel(exp);
		this._exp = exp;
		this._level = level;
		this._requiredExp = this.getRequiredExpToNextLevel(level, exp);
		return this;
	}
	get exp() {
		return this._exp;
	}
	set exp(exp) {
		if (this.hasMaxLevel && exp > this.maxExp) exp = this.maxExp;
		if (exp < this._exp) {
			this.resetExp(exp);
			return;
		}
		if (exp === this._exp) return;
		var level = this.getLevel(exp, this._level);
		var prevLevel = this._level;
		var fromExp = this._exp, toExp;
		while (1) {
			var levelStartExp = this.getExp(prevLevel);
			var levelEndExp = this.getExp(prevLevel + 1);
			toExp = Math.min(levelEndExp, exp);
			this.emit("levelup", prevLevel, fromExp, toExp, levelStartExp, levelEndExp);
			if (prevLevel === level && toExp === exp) break;
			prevLevel++;
			fromExp = levelEndExp;
		}
		this.resetExp(exp, level);
	}
	get level() {
		return this._level;
	}
	set level(value) {
		if (this.hasMaxLevel && value > this.maxLevel) this.exp = this.maxExp;
		else this.exp = this.getExp(value);
	}
	get requiredExp() {
		return this._requiredExp;
	}
	getExp(level) {
		if (level === void 0) return this._exp;
		if (this.isLevelMapFunction) return this.levelTable(level);
		else {
			if (this.hasMaxLevel && level > this.maxLevel) level = this.maxLevel;
			return this.levelTable[level];
		}
	}
	getLevel(exp, level) {
		if (exp === void 0) return this._level;
		if (level === void 0) level = 0;
		while (1) {
			var nextLevelExp = this.getExp(level + 1);
			if (nextLevelExp > exp) break;
			level++;
			if (this.hasMaxLevel && nextLevelExp === this.maxExp) break;
		}
		return level;
	}
	getRequiredExpToNextLevel(level, exp) {
		if (level === void 0) level = this.level;
		if (exp === void 0) exp = this.exp;
		return this.getExp(level + 1) - exp;
	}
	checkLevel(level, exp) {
		return exp >= this.getExp(level) && exp < this.getExp(level + 1);
	}
	gainExp(incExp, callback, scope) {
		if (callback) this.on("levelup", callback, scope);
		this.exp += incExp;
		if (callback) this.off("levelup", callback, scope);
		return this;
	}
	setExp(exp, callback, scope) {
		if (callback) this.on("levelup", callback, scope);
		this.exp = exp;
		if (callback) this.off("levelup", callback, scope);
		return this;
	}
	setLevel(level, callback, scope) {
		if (callback) this.on("levelup", callback, scope);
		this.level = level;
		if (callback) this.off("levelup", callback, scope);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/levelcounter.js
var levelcounter_default = LevelCounter;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/clock.js
var clock_default = Clock;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/logic/runcommands/RunCommands.js
var RunCommands = function(queue, scope, config) {
	var reverse = GetValue$279(config, "reverse", false);
	var retVal;
	if (IsArray(queue[0])) {
		if (!reverse) for (var i = 0, len = queue.length; i < len; i++) retVal = RunCommands(queue[i], scope, config);
		else for (var len = queue.length, i = len - 1; i >= 0; i--) retVal = RunCommands(queue[i], scope, config);
	} else retVal = RunCommand(queue, scope, config);
	return retVal;
};
var RunCommand = function(cmd, scope, config) {
	var argsConvert = GetValue$279(config, "argsConvert", void 0);
	var argsConvertScope = GetValue$279(config, "argsConvertScope", void 0);
	var fnName = cmd[0];
	ARGS = Copy(ARGS, cmd, 1);
	if (argsConvert) {
		if (argsConvert === true) {
			argsConvert = TypeConvert;
			argsConvertScope = void 0;
		}
		for (var i = 0, len = ARGS.length; i < len; i++) if (argsConvertScope) ARGS[i] = argsConvert.call(argsConvertScope, ARGS[i], cmd);
		else ARGS[i] = argsConvert(ARGS[i], cmd);
	}
	var fn;
	if (typeof fnName === "string") {
		fn = scope[fnName];
		if (fn == null) fn = GetValue$279(scope, fnName, null);
	} else fn = fnName;
	return fn.apply(scope, ARGS);
};
var ARGS = [];
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/runcommands.js
var runcommands_default = RunCommands;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/logic/runcommands/tcrp/Player.js
var GetValue$122 = __webpack_exports__Utils.Objects.GetValue;
var Player = class extends ComponentBase {
	constructor(parent, config) {
		super(parent, config);
		var clock = GetValue$122(config, "clock", void 0);
		if (!clock) clock = new clock_default(parent);
		this.clock = clock;
		this.clock.on("update", this.update, this);
		this.commands = [];
		this.resetFromJSON(config);
	}
	resetFromJSON(o) {
		this.clock.resetFromJSON(GetValue$122(o, "clock", void 0));
		this.state = GetValue$122(o, "state", 0);
		this.commands = GetValue$122(o, "commands", []);
		this.scope = GetValue$122(o, "scope", void 0);
		this.setTimeUnit(GetValue$122(o, "timeUnit", 0));
		this.setDtMode(GetValue$122(o, "dtMode", 0));
		this.index = GetValue$122(o, "index", 0);
		this.nextTime = GetValue$122(o, "nextTime", 0);
		return this;
	}
	toJSON() {
		return {
			clock: this.clock.toJSON(),
			state: this.state,
			commands: this.commands,
			scope: this.scope,
			timeUnit: this.timeUnit,
			dtMode: this.dtMode,
			index: this.index,
			nextTime: this.nextTime
		};
	}
	shutdown(fromScene) {
		if (this.isShutdown) return;
		this.clock.shutdown(fromScene);
		this.commands = void 0;
		super.shutdown(fromScene);
	}
	load(commands, scope, config) {
		this.stop();
		var timeUnit = GetValue$122(config, "timeUnit", void 0);
		if (timeUnit !== void 0) this.setTimeUnit(timeUnit);
		var dtMode = GetValue$122(config, "dtMode", void 0);
		if (dtMode !== void 0) this.setDtMode(dtMode);
		commands = commands.filter(function(item) {
			var dt = item[0];
			return !isNaN(dt);
		}).map(function(item) {
			if (typeof item[0] === "string") item[0] = parseFloat(item[0]);
			return item;
		});
		if (this.dtMode === 0) commands.sort(function(itemA, itemB) {
			var dtA = itemA[0], dtB = itemB[0];
			return dtA > dtB ? 1 : dtA < dtB ? -1 : 0;
		});
		Copy(this.commands, commands);
		this.scope = scope;
		return this;
	}
	clear() {
		this.commands.length = 0;
		return this;
	}
	append(time, fn, ...params) {
		var command;
		if (Array.isArray(fn)) command = fn;
		else command = [fn, ...params];
		this.commands.push([time, command]);
		return this;
	}
	start(startAt) {
		if (startAt === void 0) startAt = 0;
		this.stop();
		this.index = 0;
		this.state = 1;
		this.nextTime = this.getNextDt(0);
		this.clock.start(startAt);
		this.update(startAt);
		this.emit("start", this.parent, this);
		return this;
	}
	pause() {
		this.clock.pause();
		this.emit("pause", this.parent, this);
		return this;
	}
	resume() {
		this.clock.resume();
		this.emit("resume", this.parent, this);
		return this;
	}
	stop() {
		this.clock.stop();
		this.state = 0;
		this.emit("stop", this.parent, this);
		return this;
	}
	seek(time) {
		this.clock.seek(time);
		return this;
	}
	seekToNext() {
		this.seek(this.nextTime);
		return this;
	}
	get isPlaying() {
		return this.clock.isRunning;
	}
	get completed() {
		return this.state === 2;
	}
	get timeScale() {
		return this.clock.timeScale;
	}
	set timeScale(timeScale) {
		this.clock.timeScale = timeScale;
	}
	setTimeScale(timeScale) {
		this.timeScale = timeScale;
		return this;
	}
	get now() {
		return this.clock.now;
	}
	update(now) {
		if (this.nextTime > now) return this;
		var commands = this.commands;
		while (1) {
			var item = commands[this.index];
			var command = item[1];
			if (!IsArray(command)) command = Copy(CMD, item, 1);
			runcommands_default(command, this.scope);
			this.emit("runcommand", command, this.scope);
			if (this.index >= commands.length - 1) {
				this.nextTime = 0;
				this.complete();
				return this;
			} else {
				this.index++;
				this.nextTime = this.getNextDt(this.nextTime);
				if (this.nextTime > now) return this;
			}
		}
	}
	complete() {
		this.clock.stop();
		this.state = 2;
		this.emit("complete", this.parent, this);
	}
	getNextDt(currentDt) {
		var time = this.commands[this.index][0];
		if (this.timeUnit === 1) time = time * 1e3;
		if (this.dtMode === 1) time += currentDt;
		return time;
	}
	setDtMode(dtMode) {
		if (typeof dtMode === "string") dtMode = DTMODE[dtMode];
		this.dtMode = dtMode;
		return this;
	}
	setTimeUnit(timeUnit) {
		if (typeof timeUnit === "string") timeUnit = TIMEUNITMODE[timeUnit];
		this.timeUnit = timeUnit;
		return this;
	}
};
var CMD = [];
var TIMEUNITMODE = {
	ms: 0,
	s: 1,
	sec: 1
};
var DTMODE = {
	abs: 0,
	absolute: 0,
	inc: 1,
	increment: 1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/expbar/methods/OnLevelUp.js
var OnLevelUp = function(level, fromExp, toExp, levelStartExp, levelEndExp) {
	var time = (toExp - fromExp) / (levelEndExp - levelStartExp) * this.totalEaseDuration;
	var levelEnd = toExp === levelEndExp ? level + 1 : level;
	this.player.append(0, this.setEaseValueDuration, time).append(0, this.easeValueTo, toExp, levelStartExp, levelEndExp).append(0, this.emit, "levelup.start", level, fromExp, toExp, this).append(time, NOOP).append(0, this.emit, "levelup.end", levelEnd, fromExp, toExp, this);
	if (!this.player.isPlaying) this.player.start();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/expbar/methods/ExpMethods.js
var ExpMethods_default = {
	setExpTable(table) {
		this.levelCounter.setTable(table);
		return this;
	},
	resetExp(exp) {
		this.levelCounter.resetExp(exp);
		this.setValue(this.exp, this.getExp(this.level), this.getExp(this.level + 1));
		return this;
	},
	getExp(level) {
		return this.levelCounter.getExp(level);
	},
	getLevel(exp, level) {
		return this.levelCounter.getLevel(exp, level);
	},
	getRequiredExpToNextLevel(level, exp) {
		return this.levelCounter.getRequiredExpToNextLevel(level, exp);
	},
	gainExp(exp) {
		this.levelCounter.gainExp(exp);
		return this;
	},
	setExp(exp) {
		this.levelCounter.setExp(exp);
		return this;
	},
	setLevel(level) {
		this.levelCounter.setLevel(level);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/expbar/ExpBar.js
var GetValue$121 = __webpack_exports__Utils.Objects.GetValue;
var ExpBar = class extends NameValueLabel {
	constructor(scene, config) {
		super(scene, config);
		this.type = "rexExpBar";
		this.setTotalEaseDuration(GetValue$121(config, "easeDuration", 1e3));
		this.levelCounter = new levelcounter_default(GetValue$121(config, "levelCounter"));
		this.player = new Player(this, {
			scope: this,
			dtMode: 1
		});
		this.levelCounter.on("levelup", OnLevelUp, this);
		this.player.on("complete", function() {
			this.player.clear();
			this.emit("levelup.complete", this.level, this);
		}, this);
		this.setValue(this.exp, this.getExp(this.level), this.getExp(this.level + 1));
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		this.levelCounter.destroy();
		this.levelCounter = void 0;
		this.player.destroy();
		this.player = void 0;
		super.destroy(fromScene);
	}
	get exp() {
		return this.levelCounter.exp;
	}
	set exp(value) {
		this.levelCounter.exp = value;
	}
	get level() {
		return this.levelCounter.level;
	}
	set level(value) {
		this.levelCounter.level = value;
	}
	get requiredExp() {
		return this.levelCounter.requiredExp;
	}
	setTotalEaseDuration(duration) {
		this.totalEaseDuration = duration;
		return this;
	}
};
Object.assign(ExpBar.prototype, ExpMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/expbar/Factory.js
ObjectFactory.register("expBar", function(config) {
	var gameObject = new ExpBar(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.ExpBar", ExpBar);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/buttons/AddChildMethods.js
var SizerAdd$3 = Sizer.prototype.add;
var SizerAddSpace = Sizer.prototype.addSpace;
var Add$2 = function(gameObject) {
	var isNormalGameObject = !gameObject.isRexSpace;
	var proportion = !isNormalGameObject || this.buttonsExpand ? 1 : 0;
	if (this.sizerChildren.length === 0) {
		if (isNormalGameObject) {
			if (!this.buttonsExpand && (this.buttonsAlign === "right" || this.buttonsAlign === "center" || this.buttonsAlign === "bottom")) SizerAddSpace.call(this);
			SizerAdd$3.call(this, gameObject, {
				proportion,
				expand: true
			});
			var hasTailSpace = !this.buttonsExpand && this.buttonsAlign === "center";
			if (hasTailSpace) SizerAddSpace.call(this);
			this.hasTailSpace = hasTailSpace;
		} else {
			SizerAdd$3.call(this, gameObject, {
				proportion,
				expand: true
			});
			this.hasTailSpace = false;
		}
	} else if (this.hasTailSpace) {
		var lastIndex = this.sizerChildren.length - 1;
		SizerAdd$3.call(this, gameObject, {
			index: lastIndex,
			proportion,
			expand: true
		});
	} else SizerAdd$3.call(this, gameObject, {
		proportion,
		expand: true
	});
	if (isNormalGameObject) this.buttonGroup.add(gameObject);
	return this;
};
var AddChildMethods_default$3 = {
	addButton(gameObject) {
		if (IsArray(gameObject)) {
			var gameObjects = gameObject;
			for (var i = 0, cnt = gameObjects.length; i < cnt; i++) Add$2.call(this, gameObjects[i]);
		} else Add$2.call(this, gameObject);
		return this;
	},
	addButtons(gameObjects) {
		for (var i = 0, cnt = gameObjects.length; i < cnt; i++) Add$2.call(this, gameObjects[i]);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/buttons/RemoveChildMethods.js
var SizerRmove$2 = Sizer.prototype.remove;
var SizerClear$2 = Sizer.prototype.clear;
var Remove$3 = function(gameObject, destroyChild) {
	if (this.getParentSizer(gameObject) !== this) return this;
	this.buttonGroup.remove(gameObject);
	SizerRmove$2.call(this, gameObject, destroyChild);
	return this;
};
var RemoveChildMethods_default$2 = {
	remove(gameObject, destroyChild) {
		if (IsArray(gameObject)) {
			var gameObjects = gameObject;
			for (var i = 0, cnt = gameObjects.length; i < cnt; i++) Remove$3.call(this, gameObjects[i], destroyChild);
		} else Remove$3.call(this, gameObject, destroyChild);
		return this;
	},
	clear(destroyChild) {
		var buttons = this.buttonGroup.buttons;
		buttons.length = 0;
		SizerClear$2.call(this, destroyChild);
		return this;
	},
	removeButton(gameObject, destroyChild) {
		var gameObject = this.getButton(gameObject);
		if (!gameObject) return this;
		this.remove(gameObject, destroyChild);
		return this;
	},
	clearButtons(destroyChild) {
		var buttons = this.buttonGroup.buttons;
		for (var i = buttons.length - 1; i >= 0; i--) Remove$3.call(this, buttons[i], destroyChild);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/buttongroup/OnButtonStateChange.js
var OnButtonStateChange = function(button, value, previousValue) {
	if (!button) return;
	var callback = this.setValueCallback;
	var scope = this.setValueCallbackScope;
	if (callback) {
		if (scope) callback.call(scope, button, value, previousValue);
		else callback(button, value, previousValue);
	}
	this.fireEvent("button.statechange", button, value, previousValue);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/buttongroup/InjectSelectedProperty.js
var InjectSelectedProperty = function(gameObject) {
	var self = this;
	gameObject._selected = void 0;
	Object.defineProperty(gameObject, "selected", {
		get: function() {
			return gameObject._selected;
		},
		set: function(newValue) {
			if (gameObject._selected === newValue) return;
			var previousValue = gameObject._selected;
			gameObject._selected = newValue;
			OnButtonStateChange.call(self, gameObject, newValue, previousValue);
		},
		enumerable: true,
		configurable: true
	});
	gameObject.selected = false;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/buttongroup/AddMethods.js
var AddMethods_default = {
	add(gameObject) {
		this.buttons.push(gameObject);
		if (!gameObject._click) {
			gameObject._click = new Click_default(gameObject, this.clickConfig);
			gameObject._click.on("click", function(buttonBehavior, gameObject, pointer, event) {
				this.fireEvent("button.click", gameObject, pointer, event);
			}, this).on("enable", function(buttonBehavior, gameObject) {
				this.fireEvent("button.enable", gameObject);
			}, this).on("disable", function(buttonBehavior, gameObject) {
				this.fireEvent("button.disable", gameObject);
			}, this).on("over", function(buttonBehavior, gameObject, pointer, event) {
				this.fireEvent("button.over", gameObject, pointer, event);
			}, this).on("out", function(buttonBehavior, gameObject, pointer, event) {
				this.fireEvent("button.out", gameObject, pointer, event);
			}, this).on("down", function(buttonBehavior, gameObject, pointer, event) {
				this.fireEvent("button.down", gameObject, pointer, event);
			}, this).on("up", function(buttonBehavior, gameObject, pointer, event) {
				this.fireEvent("button.up", gameObject, pointer, event);
			}, this);
			if (gameObject.isRexContainerLite) gameObject.sendChildToBack(gameObject);
		}
		if (this.buttonsType) {
			if (gameObject.name === void 0) console.error(`${this.parent.constructor.name}: Option button miss value`);
			InjectSelectedProperty.call(this, gameObject);
		}
		return this;
	},
	addMultiple(gameObjects) {
		for (var i = 0, cnt = gameObjects.length; i < cnt; i++) this.add(gameObjects[i]);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/buttongroup/RemoveMethods.js
var RemoveItem = __webpack_exports__Utils.Array.Remove;
var RemoveMethods_default = {
	remove(gameObject) {
		if (this.buttonsType) delete gameObject.selected;
		RemoveItem(this.buttons, gameObject);
		return this;
	},
	clear() {
		if (this.buttonsType) {
			var buttons = this.buttons;
			for (var i = 0, cnt = buttons.length; i < cnt; i++) delete buttons[i].selected;
		}
		this.buttons.length = 0;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/buttongroup/FireEvent.js
var FireEvent = function(eventName, button, ...args) {
	if (!this.buttons) return;
	var index;
	if (typeof button === "number") {
		index = button;
		button = this.buttons[index];
		if (!button) return;
	} else {
		index = this.buttons.indexOf(button);
		if (index === -1) return;
	}
	if (this.eventEmitter !== this.parent) this.parent.emit(eventName, button, index, ...args);
	if (this.groupName !== void 0) this.eventEmitter.emit(eventName, button, this.groupName, index, ...args);
	else this.eventEmitter.emit(eventName, button, index, ...args);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/buttongroup/ButtonsTypeMethods.js
var GetValue$120 = __webpack_exports__Utils.Objects.GetValue;
var ButtonsTypeMethods_default = {
	setButtonsType(config) {
		if (config === void 0) config = {};
		var buttonsType = GetValue$120(config, "buttonsType", config.type);
		this.buttonsType = buttonsType;
		if (!this.buttonsType) return this;
		var setValueCallback = GetValue$120(config, "setValueCallback", void 0), setValueCallbackScope = GetValue$120(config, "setValueCallbackScope", void 0);
		if (setValueCallback === void 0) {
			setValueCallback = GetValue$120(config, "setButtonStateCallback", void 0);
			setValueCallbackScope = GetValue$120(config, "setButtonStateCallbackScope", void 0);
		}
		this.setValueCallback = setValueCallback;
		this.setValueCallbackScope = setValueCallbackScope;
		switch (buttonsType) {
			case "radio":
				this.setRadioType();
				break;
			case "checkboxes": this.setCheckboxesType();
		}
		return this;
	},
	setRadioType() {
		var parent = this.parent, buttons = this.buttons;
		parent._value = void 0;
		var selectedIndex = void 0;
		Object.defineProperty(parent, "value", {
			get: function() {
				return parent._value;
			},
			set: function(newValue) {
				if (parent._value === newValue) return;
				parent._value = newValue;
				for (var i = 0, cnt = buttons.length; i < cnt; i++) {
					var button = buttons[i];
					if (button.rexSizer.hidden) continue;
					if (selectedIndex === void 0) {
						if (button.name === newValue) button.selected = true;
						else button.selected = false;
					} else if (selectedIndex === i) button.selected = true;
					else button.selected = false;
				}
			},
			enumerable: true,
			configurable: true
		});
		parent.on("button.click", function(button) {
			selectedIndex = this.buttons.indexOf(button);
			parent.value = button.name;
			selectedIndex = void 0;
		}, this);
		return this;
	},
	setCheckboxesType() {
		this.parent.on("button.click", function(button) {
			button.selected = !button.selected;
		});
		return this;
	},
	clearAllButtonsState() {
		var buttons = this.buttons;
		for (var i = 0, cnt = buttons.length; i < cnt; i++) {
			var button = buttons[i];
			button.selected = false;
		}
		return this;
	},
	getAllButtonsState() {
		var states = {};
		var buttons = this.buttons;
		for (var i = 0, cnt = buttons.length; i < cnt; i++) {
			var button = buttons[i];
			if (button.rexSizer.hidden) continue;
			states[button.name] = button.selected;
		}
		return states;
	},
	setSelectedButtonName(name) {
		this.parent.value = name;
		return this;
	},
	getSelectedButtonName() {
		return this.parent.value;
	},
	setButtonState(name, state) {
		if (state === void 0) state = true;
		var buttons = this.buttons;
		for (var i = 0, cnt = buttons.length; i < cnt; i++) {
			var button = buttons[i];
			if (button.rexSizer.hidden) continue;
			if (button.name === name) {
				button.selected = state;
				break;
			}
		}
		return this;
	},
	getButtonState(name) {
		var buttons = this.buttons;
		for (var i = 0, cnt = buttons.length; i < cnt; i++) {
			var button = buttons[i];
			if (button.rexSizer.hidden) continue;
			if (button.name === name) return button.selected;
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/system/GetGameObjectByName.js
var GetGameObjectByName = function(children, name) {
	if (!children) return null;
	else if (IsArray(children)) {
		var child;
		for (var i = 0, cnt = children.length; i < cnt; i++) {
			child = TestName(children[i], name);
			if (child) return child;
		}
	} else {
		var child;
		for (var key in children) {
			child = TestName(children[key], name);
			if (child) return child;
		}
	}
};
var TestName = function(gameObject, name) {
	if (!gameObject) return null;
	else if (gameObject.hasOwnProperty("name")) return gameObject.name === name ? gameObject : null;
	else return GetElementByName(gameObject, name);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/GetGameObjectByName.js
var GetGameObjectByName_default = GetGameObjectByName;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/buttongroup/ButtonMethods.js
var ButtonMethods_default$2 = {
	getButton(index) {
		var buttons = this.buttons, button;
		switch (typeof index) {
			case "number":
				button = buttons[index];
				break;
			case "string":
				button = GetGameObjectByName_default(buttons, index);
				break;
			default:
				button = index;
				if (buttons.indexOf(button) === -1) button = void 0;
		}
		return button;
	},
	getButtons() {
		return this.buttons;
	},
	hasAnyButton() {
		return this.buttons.length > 0;
	},
	setButtonEnable(index, enabled) {
		var buttons = this.buttons;
		if (index === void 0 || typeof index === "boolean") {
			enabled = index;
			for (var i = 0, cnt = buttons.length; i < cnt; i++) buttons[i]._click.setEnable(enabled);
		} else this.getButton(index)._click.setEnable(enabled);
		return this;
	},
	toggleButtonEnable(index) {
		var buttons = this.buttons;
		if (index === void 0 || typeof index === "boolean") for (var i = 0, cnt = buttons.length; i < cnt; i++) buttons[i]._click.toggleEnable();
		else this.getButton(index)._click.toggleEnable();
		return this;
	},
	getButtonEnable(index) {
		if (index === void 0) index = 0;
		return this.getButton(index)._click.enable;
	},
	emitButtonClick(index) {
		(this.buttonGroup ? this.buttonGroup : this).fireEvent("button.click", index);
		return this;
	},
	emitButtonOver(index) {
		var buttonGroup = this.buttonGroup ? this.buttonGroup : this;
		var buttons = this.buttons;
		for (var i = 0, cnt = buttons.length; i < cnt; i++) {
			var button = buttons[i];
			if (!button._click.isOver) continue;
			button._click.isOver = false;
			buttonGroup.fireEvent("button.out", button);
		}
		var button = this.getButton(index);
		if (button) {
			button._click.isOver = true;
			buttonGroup.fireEvent("button.over", button);
		}
		return this;
	},
	showButton(index) {
		Show(this.getButton(index));
		return this;
	},
	hideButton(index) {
		Hide(this.getButton(index));
		return this;
	},
	isButtonShown(index) {
		IsShown(this.getButton(index));
		return this;
	},
	forEachButtton(callback, scope) {
		var buttons = this.buttons;
		for (var i = 0, cnt = buttons.length; i < cnt; i++) if (scope) callback.call(scope, buttons[i], i, buttons);
		else callback(buttons[i], i, buttons);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/buttongroup/ButtonGroup.js
var ButtonGroup = class {
	constructor(config) {
		this.parent = config.parent;
		this.eventEmitter = config.eventEmitter;
		this.groupName = config.groupName;
		this.clickConfig = config.clickConfig;
		this.buttonsType = void 0;
		this.buttons = [];
	}
	destroy() {
		this.parent = void 0;
		this.eventEmitter = void 0;
		this.clickConfig = void 0;
		this.buttons = void 0;
	}
};
var methods$17 = { fireEvent: FireEvent };
Object.assign(ButtonGroup.prototype, AddMethods_default, RemoveMethods_default, ButtonsTypeMethods_default, ButtonMethods_default$2, methods$17);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/buttongroup/ButtonStateMethods.js
var ButtonStateMethods_default = {
	clearAllButtonsState() {
		this.buttonGroup.clearAllButtonsState();
		return this;
	},
	getAllButtonsState() {
		return this.buttonGroup.getAllButtonsState();
	},
	setSelectedButtonName(name) {
		this.buttonGroup.setSelectedButtonName(name);
		return this;
	},
	getSelectedButtonName() {
		return this.buttonGroup.getSelectedButtonName();
	},
	setButtonState(name, state) {
		this.buttonGroup.setButtonState(name, state);
		return this;
	},
	getButtonState(name) {
		return this.buttonGroup.getButtonState(name);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/buttons/Buttons.js
var GetValue$119 = __webpack_exports__Utils.Objects.GetValue;
var Buttons$1 = class extends Sizer {
	constructor(scene, config) {
		if (config === void 0) config = {};
		var buttonSpace = config.space;
		if (typeof buttonSpace === "number") config.space = { item: buttonSpace };
		super(scene, config);
		this.type = "rexButtons";
		this.buttonGroup = new ButtonGroup({
			parent: this,
			eventEmitter: GetValue$119(config, "eventEmitter", this),
			groupName: GetValue$119(config, "groupName", void 0),
			clickConfig: GetValue$119(config, "click", void 0)
		}).setButtonsType(config);
		var background = GetValue$119(config, "background", void 0);
		var buttons = GetValue$119(config, "buttons", void 0);
		this.buttonsExpand = GetValue$119(config, "expand", false);
		this.buttonsAlign = GetValue$119(config, "align", void 0);
		if (background) this.addBackground(background);
		if (buttons) this.addButtons(buttons);
		this.addChildrenMap("background", background);
		this.addChildrenMap("buttons", this.buttonGroup.buttons);
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		super.destroy(fromScene);
		this.buttonGroup.destroy();
		this.buttonGroup = void 0;
	}
	get buttons() {
		return this.buttonGroup.buttons;
	}
	get groupName() {
		return this.buttonGroup.groupName;
	}
	set groupName(value) {
		this.buttonGroup.groupName = value;
	}
	get eventEmitter() {
		return this.buttonGroup.eventEmitter;
	}
};
Object.assign(Buttons$1.prototype, AddChildMethods_default$3, RemoveChildMethods_default$2, ButtonMethods_default$2, ButtonStateMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/buttons/Factory.js
ObjectFactory.register("buttons", function(config) {
	var gameObject = new Buttons$1(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.Buttons", Buttons$1);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridbuttons/AddChildMethods.js
var SizerAdd$2 = GridSizer.prototype.add;
var AddChildMethods_default$2 = {
	addButton(gameObject, columnIndex, rowIndex) {
		SizerAdd$2.call(this, gameObject, columnIndex, rowIndex, void 0, 0, this.buttonsExpand);
		this.buttonGroup.add(gameObject);
		return this;
	},
	addButtons(gameObjects, rowThenColumn) {
		for (var i = 0, cnt = gameObjects; i < cnt; i++) this.addButton(gameObjects[i], void 0, rowThenColumn);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridbuttons/RemoveChildMethods.js
var SizerRmove$1 = GridSizer.prototype.remove;
var SizerClear$1 = GridSizer.prototype.clear;
var Remove$2 = function(gameObject, destroyChild) {
	var gameObject = this.getButton(gameObject);
	if (!gameObject) return this;
	this.buttonGroup.remove(gameObject);
	SizerRmove$1.call(this, gameObject, destroyChild);
	return this;
};
var RemoveChildMethods_default$1 = {
	remove(gameObject, destroyChild) {
		if (IsArray(gameObject)) {
			var gameObjects = gameObject;
			for (var i = 0, cnt = gameObjects.length; i < cnt; i++) Remove$2.call(this, gameObjects[i], destroyChild);
		} else Remove$2.call(this, gameObject, destroyChild);
		return this;
	},
	clear(destroyChild) {
		var buttons = this.buttonGroup.buttons;
		buttons.length = 0;
		SizerClear$1.call(this, destroyChild);
		return this;
	},
	removeButton(gameObject, destroyChild) {
		this.remove(gameObject, destroyChild);
		return this;
	},
	clearButtons(destroyChild) {
		var buttons = this.buttonGroup.buttons;
		for (var i = buttons.length - 1; i >= 0; i--) Remove$2.call(this, buttons[i], destroyChild);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridbuttons/GridButtons.js
var GetValue$118 = __webpack_exports__Utils.Objects.GetValue;
var GridButtons = class extends GridSizer {
	constructor(scene, config) {
		if (config === void 0) config = {};
		var rowCount = GetValue$118(config, "row", 0);
		var columnCount = GetValue$118(config, "column", config.col || 0);
		var createCellContainerCallback = GetValue$118(config, "createCellContainerCallback");
		var buttons = GetValue$118(config, "buttons", void 0);
		var buttonsExpand = GetValue$118(config, "expand", true);
		var buttonProportion = buttonsExpand ? 1 : 0;
		if (createCellContainerCallback) config.createCellContainerCallback = void 0;
		if (buttons !== void 0) {
			rowCount = Math.max(rowCount, buttons.length);
			for (var i = 0, cnt = buttons.length; i < cnt; i++) columnCount = Math.max(columnCount, buttons[i].length);
		}
		config.row = rowCount;
		config.column = columnCount;
		config.columnProportions = buttonProportion;
		config.rowProportions = buttonProportion;
		super(scene, config);
		this.type = "rexGridButtons";
		this.buttonGroup = new ButtonGroup({
			parent: this,
			eventEmitter: GetValue$118(config, "eventEmitter", this),
			groupName: GetValue$118(config, "groupName", void 0),
			clickConfig: GetValue$118(config, "click", void 0)
		}).setButtonsType(config);
		var background = GetValue$118(config, "background", void 0);
		this.buttonsExpand = buttonsExpand;
		var space = GetValue$118(config, "space", void 0);
		if (typeof space === "number") space = {
			itemX: space,
			itemY: space
		};
		if (background) this.addBackground(background);
		if (buttons) {
			var rowButtons, button;
			for (var r = 0, rcnt = buttons.length; r < rcnt; r++) {
				rowButtons = buttons[r];
				for (var c = 0, ccnt = rowButtons.length; c < ccnt; c++) {
					button = rowButtons[c];
					if (button) this.addButton(button, c, r);
				}
			}
		} else if (createCellContainerCallback) for (var y = 0; y < rowCount; y++) for (var x = 0; x < columnCount; x++) {
			var button = createCellContainerCallback(scene, x, y);
			if (button) this.addButton(button, x, y);
		}
		this.addChildrenMap("background", background);
		this.addChildrenMap("buttons", this.buttonGroup.buttons);
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		super.destroy(fromScene);
		this.buttonGroup.destroy();
		this.buttonGroup = void 0;
	}
	get buttons() {
		return this.buttonGroup.buttons;
	}
	get groupName() {
		return this.buttonGroup.groupName;
	}
	set groupName(value) {
		this.buttonGroup.groupName = value;
	}
	get eventEmitter() {
		return this.buttonGroup.eventEmitter;
	}
};
Object.assign(GridButtons.prototype, AddChildMethods_default$2, RemoveChildMethods_default$1, ButtonMethods_default$2, ButtonStateMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridbuttons/Factory.js
ObjectFactory.register("gridButtons", function(config) {
	var gameObject = new GridButtons(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.GridButtons", GridButtons);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthbuttons/AddChildMethods.js
var SizerAdd$1 = FixWidthSizer.prototype.add;
var Add$1 = function(gameObject) {
	SizerAdd$1.call(this, gameObject);
	this.buttonGroup.add(gameObject);
	return this;
};
var AddChildMethods_default$1 = {
	addButton(gameObject) {
		if (IsArray(gameObject)) {
			var gameObjects = gameObject;
			for (var i = 0, cnt = gameObjects.length; i < cnt; i++) Add$1.call(this, gameObjects[i]);
		} else Add$1.call(this, gameObject);
		return this;
	},
	addButtons(gameObjects) {
		if (IsArray(gameObjects[0])) {
			var lines = gameObjects, line;
			for (var lineIdx = 0, lastLineIdx = lines.length - 1; lineIdx <= lastLineIdx; lineIdx++) {
				line = lines[lineIdx];
				for (var i = 0, cnt = line.length; i < cnt; i++) Add$1.call(this, line[i]);
				if (lineIdx > lastLineIdx) SizerAdd$1.addNewLine(this);
			}
		} else for (var i = 0, cnt = gameObjects.length; i < cnt; i++) Add$1.call(this, gameObjects[i]);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthbuttons/RemoveChildMethods.js
var SizerRmove = FixWidthSizer.prototype.remove;
var SizerClear = FixWidthSizer.prototype.clear;
var Remove$1 = function(gameObject, destroyChild) {
	var gameObject = this.getButton(gameObject);
	if (!gameObject) return this;
	this.buttonGroup.remove(gameObject);
	SizerRmove.call(this, gameObject, destroyChild);
	return this;
};
var RemoveChildMethods_default = {
	remove(gameObject, destroyChild) {
		if (IsArray(gameObject)) {
			var gameObjects = gameObject;
			for (var i = 0, cnt = gameObjects.length; i < cnt; i++) Remove$1.call(this, gameObjects[i], destroyChild);
		} else Remove$1.call(this, gameObject, destroyChild);
		return this;
	},
	clear(destroyChild) {
		var buttons = this.buttonGroup.buttons;
		buttons.length = 0;
		SizerClear.call(this, destroyChild);
		return this;
	},
	removeButton(gameObject, destroyChild) {
		this.remove(gameObject, destroyChild);
		return this;
	},
	clearButtons(destroyChild) {
		var buttons = this.buttonGroup.buttons;
		for (var i = buttons.length - 1; i >= 0; i--) Remove$1.call(this, buttons[i], destroyChild);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthbuttons/FixWidthButtons.js
var GetValue$117 = __webpack_exports__Utils.Objects.GetValue;
var Buttons = class extends FixWidthSizer {
	constructor(scene, config) {
		if (config === void 0) config = {};
		var buttonSpace = config.space;
		if (typeof buttonSpace === "number") config.space = {
			item: buttonSpace,
			line: buttonSpace
		};
		super(scene, config);
		this.type = "rexFixWidthButtons";
		this.buttonGroup = new ButtonGroup({
			parent: this,
			eventEmitter: GetValue$117(config, "eventEmitter", this),
			groupName: GetValue$117(config, "groupName", void 0),
			clickConfig: GetValue$117(config, "click", void 0)
		}).setButtonsType(config);
		var background = GetValue$117(config, "background", void 0);
		var buttons = GetValue$117(config, "buttons", void 0);
		this.buttonsAlign = GetValue$117(config, "align", void 0);
		if (background) this.addBackground(background);
		if (buttons) this.addButtons(buttons);
		this.addChildrenMap("background", background);
		this.addChildrenMap("buttons", this.buttonGroup.buttons);
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		super.destroy(fromScene);
		this.buttonGroup.destroy();
		this.buttonGroup = void 0;
	}
	get buttons() {
		return this.buttonGroup.buttons;
	}
	get groupName() {
		return this.buttonGroup.groupName;
	}
	set groupName(value) {
		this.buttonGroup.groupName = value;
	}
	get eventEmitter() {
		return this.buttonGroup.eventEmitter;
	}
};
Object.assign(Buttons.prototype, AddChildMethods_default$1, RemoveChildMethods_default, ButtonMethods_default$2, ButtonStateMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fixwidthbuttons/Factory.js
ObjectFactory.register("fixWidthButtons", function(config) {
	var gameObject = new Buttons(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.FixWidthButtons", Buttons);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fileselectorbutton/FileChooserMethods.js
var FileChooserMethods_default = {
	setAccept(accept) {
		this.childrenMap.fileChooser.setAccept(accept);
		return this;
	},
	setMultiple(enabled) {
		this.childrenMap.fileChooser.setMultiple(enabled);
		return this;
	},
	loadFile(file, loaderType, key, cacheType, onComplete) {
		this.childrenMap.fileChooser.loadFile(file, loaderType, key, cacheType, onComplete);
		return this;
	},
	loadFilePromise(file, loaderType, key, cacheType) {
		return this.childrenMap.fileChooser.loadFilePromise(file, loaderType, key, cacheType);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fileselectorbutton/FileSelectorButton.js
var GetValue$116 = __webpack_exports__Utils.Objects.GetValue;
var FileSelectorButton = class extends Label {
	constructor(scene, config) {
		super(scene, config);
		this.type = "rexFileSelectorButton";
		var fileChooser = new FileChooser(scene);
		scene.add.existing(fileChooser);
		this.addBackground(fileChooser);
		this.addChildrenMap("fileChooser", fileChooser);
		this.setAccept(GetValue$116(config, "accept", ""));
		this.setMultiple(GetValue$116(config, "multiple", false));
		fileChooser.on("change", function(gameObject) {
			var files = gameObject.files;
			if (files.length === 0) return;
			files = Array.from(files);
			this.emit("select", files, this);
		}, this);
	}
	get files() {
		return this.childrenMap.fileChooser.files;
	}
};
Object.assign(FileSelectorButton.prototype, FileChooserMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fileselectorbutton/Factory.js
ObjectFactory.register("fileSelectorButton", function(config) {
	var gameObject = new FileSelectorButton(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.FileSelectorButton", FileSelectorButton);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/dialog/methods/ButtonMethods.js
var ButtonMethods_default$1 = {
	getChoice(index) {
		var choicesSizer = this.childrenMap.choicesSizer;
		if (choicesSizer) return choicesSizer.getButton(index);
		else return;
	},
	getAction(index) {
		return this.childrenMap.actionsSizer.getButton(index);
	},
	getToolbar(index) {
		return this.childrenMap.toolbarSizer.getButton(index);
	},
	getLeftToolbar(index) {
		return this.childrenMap.leftToolbarSizer.getButton(index);
	},
	setChoiceEnable(index, enabled) {
		var choicesSizer = this.childrenMap.choicesSizer;
		if (choicesSizer) choicesSizer.setButtonEnable(index, enabled);
		return this;
	},
	setActionEnable(index, enabled) {
		this.childrenMap.actionsSizer.setButtonEnable(index, enabled);
		return this;
	},
	setToolbarEnable(index, enabled) {
		this.childrenMap.toolbarSizer.setButtonEnable(index, enabled);
		return this;
	},
	setLeftToolbarEnable(index, enabled) {
		this.childrenMap.leftToolbarSizer.setButtonEnable(index, enabled);
		return this;
	},
	toggleChoiceEnable(index) {
		var choicesSizer = this.childrenMap.choicesSizer;
		if (choicesSizer) choicesSizer.toggleButtonEnable(index);
		return this;
	},
	toggleActionEnable(index) {
		this.childrenMap.actionsSizer.toggleButtonEnable(index);
		return this;
	},
	toggleToolbarEnable(index) {
		this.childrenMap.toolbarSizer.toggleButtonEnable(index);
		return this;
	},
	toggleLeftToolbarEnable(index) {
		this.childrenMap.leftToolbarSizer.toggleButtonEnable(index);
		return this;
	},
	getChoiceEnable(index) {
		var choicesSizer = this.childrenMap.choicesSizer;
		if (choicesSizer) return choicesSizer.getButtonEnable(index);
		else return false;
	},
	getActionEnable(index) {
		return this.childrenMap.actionsSizer.getButtonEnable(index);
	},
	getToolbarEnable(index) {
		return this.childrenMap.toolbarSizer.getButtonEnable(index);
	},
	getLeftToolbarEnable(index) {
		return this.childrenMap.leftToolbarSizer.getButtonEnable(index);
	},
	emitChoiceClick(index) {
		var choicesSizer = this.childrenMap.choicesSizer;
		if (choicesSizer) choicesSizer.emitButtonClick(index);
		return this;
	},
	emitActionClick(index) {
		this.childrenMap.actionsSizer.emitButtonClick(index);
		return this;
	},
	emitToolbarClick(index) {
		this.childrenMap.toolbarSizer.emitButtonClick(index);
		return this;
	},
	emitLeftToolbarClick(index) {
		this.childrenMap.leftToolbarSizer.emitButtonClick(index);
		return this;
	},
	showChoice(index) {
		var choicesSizer = this.childrenMap.choicesSizer;
		if (choicesSizer) choicesSizer.showButton(index);
		return this;
	},
	showAction(index) {
		this.childrenMap.actionsSizer.showButton(index);
		return this;
	},
	showToolbar(index) {
		this.childrenMap.toolbarSizer.showButton(index);
		return this;
	},
	showLeftToolbar(index) {
		this.childrenMap.leftToolbarSizer.showButton(index);
		return this;
	},
	hideChoice(index) {
		var choicesSizer = this.childrenMap.choicesSizer;
		if (choicesSizer) choicesSizer.hideButton(index);
		return this;
	},
	hideAction(index) {
		this.childrenMap.actionsSizer.hideButton(index);
		return this;
	},
	hideToolbar(index) {
		this.childrenMap.toolbarSizer.hideButton(index);
		return this;
	},
	hideLeftToolbar(index) {
		this.childrenMap.leftToolbarSizer.hideButton(index);
		return this;
	},
	addChoice(gameObject) {
		var choicesSizer = this.childrenMap.choicesSizer;
		if (choicesSizer) choicesSizer.addButton(gameObject);
		return this;
	},
	addAction(gameObject) {
		this.childrenMap.actionsSizer.addButton(gameObject);
		return this;
	},
	addToolbar(gameObject) {
		this.childrenMap.toolbarSizer.addButton(gameObject);
		return this;
	},
	addLeftToolbar(gameObject) {
		this.childrenMap.leftToolbarSizer.addButton(gameObject);
		return this;
	},
	removeChoice(index, destroyChild) {
		var choicesSizer = this.childrenMap.choicesSizer;
		if (choicesSizer) choicesSizer.removeButton(index, destroyChild);
		return this;
	},
	removeAction(index, destroyChild) {
		this.childrenMap.actionsSizer.removeButton(index, destroyChild);
		return this;
	},
	removeToolbar(index, destroyChild) {
		this.childrenMap.toolbarSizer.removeButton(index, destroyChild);
		return this;
	},
	removeLeftToolbar(index, destroyChild) {
		this.childrenMap.leftToolbarSizer.removeButton(index, destroyChild);
		return this;
	},
	clearChoices(destroyChild) {
		var choicesSizer = this.childrenMap.choicesSizer;
		if (choicesSizer) choicesSizer.clearButtons(destroyChild);
		return this;
	},
	clearActions(destroyChild) {
		this.childrenMap.actionsSizer.clearButtons(destroyChild);
		return this;
	},
	clearToolbar(destroyChild) {
		this.childrenMap.toolbarSizer.clearButtons(destroyChild);
		return this;
	},
	clearLeftToolbar(destroyChild) {
		this.childrenMap.leftToolbarSizer.clearButtons(destroyChild);
		return this;
	},
	forEachChoice(callback, scope) {
		var choicesSizer = this.childrenMap.choicesSizer;
		if (choicesSizer) choicesSizer.forEachButtton(callback, scope);
		return this;
	},
	forEachAction(callback, scope) {
		this.childrenMap.actionsSizer.forEachButtton(callback, scope);
		return this;
	},
	forEachToolbar(callback, scope) {
		this.childrenMap.toolbarSizer.forEachButtton(callback, scope);
		return this;
	},
	forEachLeftToolbar(callback, scope) {
		this.childrenMap.leftToolbarSizer.forEachButtton(callback, scope);
		return this;
	},
	setAllButtonsEnable(enabled) {
		if (enabled === void 0) enabled = true;
		if (this.childrenMap.toolbarSizer) this.setToolbarEnable(enabled);
		if (this.childrenMap.leftToolbarSizer) this.setLeftToolbarEnable(enabled);
		if (this.childrenMap.actionsSizer) this.setActionEnable(enabled);
		if (this.childrenMap.choicesSizer) this.setChoiceEnable(enabled);
		return this;
	},
	getChoicesButtonStates() {
		var choicesSizer = this.childrenMap.choicesSizer;
		if (choicesSizer) return choicesSizer.getAllButtonsState();
		else return {};
	},
	getChoicesButtonState(name) {
		var choicesSizer = this.childrenMap.choicesSizer;
		if (name === void 0) {
			if (choicesSizer) return choicesSizer.getAllButtonsState();
			else return {};
		} else if (choicesSizer) return choicesSizer.getButtonState(name);
		else return false;
	},
	setChoicesButtonState(name, state) {
		var choicesSizer = this.childrenMap.choicesSizer;
		if (choicesSizer) choicesSizer.setButtonState(name, state);
		return this;
	},
	clearChoicesButtonStates() {
		var choicesSizer = this.childrenMap.choicesSizer;
		if (choicesSizer) choicesSizer.clearAllButtonsState();
		return this;
	},
	getChoicesSelectedButtonName() {
		var choicesSizer = this.childrenMap.choicesSizer;
		if (choicesSizer) return choicesSizer.getSelectedButtonName();
		else return "";
	},
	setChoicesSelectedButtonName(name) {
		var choicesSizer = this.childrenMap.choicesSizer;
		if (choicesSizer) choicesSizer.setSelectedButtonName(name);
		return this;
	},
	hasAnyChoice() {
		var choicesSizer = this.childrenMap.choicesSizer;
		if (choicesSizer) return choicesSizer.hasAnyButton();
		return false;
	},
	hasAnyAction() {
		var actionsSizer = this.childrenMap.actionsSizer;
		if (actionsSizer) return actionsSizer.hasAnyButton();
		return false;
	},
	hasAnyToolbar() {
		var toolbarSizer = this.childrenMap.toolbarSizer;
		if (toolbarSizer) return toolbarSizer.hasAnyButton();
		return false;
	},
	hasAnyLeftToolbar() {
		var leftToolbarSizer = this.childrenMap.leftToolbarSizer;
		if (leftToolbarSizer) return leftToolbarSizer.hasAnyButton();
		return false;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/dialog/methods/ModalMethods.js
var ModalMethods_default = {
	onCreateModalBehavior(self) {
		self.on("button.click", function(button, groupName, index, pointer, event) {
			var canClose = false;
			switch (groupName) {
				case "actions":
					canClose = true;
					break;
				case "choices": if (!self.hasAnyAction()) canClose = true;
			}
			if (!canClose) return;
			var closeEventData = {
				index,
				text: button.text,
				button,
				dialog: self
			};
			switch (self.buttonsType) {
				case "radio":
					closeEventData.value = self.getChoicesSelectedButtonName();
					break;
				case "checkboxes":
					closeEventData.value = self.getChoicesButtonStates();
					break;
				default: closeEventData.value = void 0;
			}
			self.modalClose(closeEventData);
		});
	},
	modal(config, onClose) {
		if (config && config.defaultBehavior === false) this.onCreateModalBehavior = false;
		else delete this.onCreateModalBehavior;
		ModalMethods_default$1.modal.call(this, config, onClose);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/dialog/methods/Methods.js
var Methods$10 = {};
Object.assign(Methods$10, ButtonMethods_default$1, ModalMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/dialog/Dialog.js
var GetValue$115 = __webpack_exports__Utils.Objects.GetValue;
var Dialog = class extends Sizer {
	constructor(scene, config) {
		if (config === void 0) config = {};
		config.orientation = 1;
		super(scene, config);
		this.type = "rexDialog";
		this.eventEmitter = GetValue$115(config, "eventEmitter", this);
		var background = GetValue$115(config, "background", void 0);
		var title = GetValue$115(config, "title", void 0);
		var toolbar = GetValue$115(config, "toolbar", void 0);
		var toolbarBackground = GetValue$115(config, "toolbarBackground", void 0);
		var leftToolbar = GetValue$115(config, "leftToolbar", void 0);
		var leftToolbarBackground = GetValue$115(config, "leftToolbarBackground", void 0);
		var content = GetValue$115(config, "content", void 0);
		var description = GetValue$115(config, "description", void 0);
		var choicesSizer;
		var choices = GetValue$115(config, "choices", void 0);
		var choicesBackground = GetValue$115(config, "choicesBackground", void 0);
		var actionsSizer;
		var actions = GetValue$115(config, "actions", void 0);
		var actionsBackground = GetValue$115(config, "actionsBackground", void 0);
		var clickConfig = GetValue$115(config, "click", void 0);
		if (background) this.addBackground(background);
		var toolbarSizer;
		if (toolbar) {
			toolbarSizer = new Buttons$1(scene, {
				groupName: "toolbar",
				background: toolbarBackground,
				buttons: toolbar,
				orientation: 0,
				space: { item: GetValue$115(config, "space.toolbarItem", 0) },
				click: clickConfig,
				eventEmitter: this.eventEmitter
			});
			scene.add.existing(toolbarSizer);
		}
		var leftToolbarSizer;
		if (leftToolbar) {
			leftToolbarSizer = new Buttons$1(scene, {
				groupName: "leftToolbar",
				background: leftToolbarBackground,
				buttons: leftToolbar,
				orientation: 0,
				space: { item: GetValue$115(config, "space.leftToolbarItem", 0) },
				click: clickConfig,
				eventEmitter: this.eventEmitter
			});
			scene.add.existing(leftToolbarSizer);
		}
		if (title || toolbar || leftToolbar) {
			var titleExpandWidth = !!title && GetValue$115(config, "expand.title", true);
			var titleAlign = GetValue$115(config, "align.title", "center");
			var useSizer = !(title && !titleExpandWidth && titleAlign === "center" || !title && (toolbar || leftToolbar));
			var titleSizer;
			if (useSizer) titleSizer = new Sizer(scene, { orientation: 0 });
			else titleSizer = new OverlapSizer(scene);
			scene.add.existing(titleSizer);
			var titleChildExpand = useSizer ? true : { height: true };
			if (leftToolbarSizer) titleSizer.add(leftToolbarSizer, {
				align: "left",
				expand: titleChildExpand
			});
			if (title) {
				if (useSizer && !titleExpandWidth && titleAlign === "right") titleSizer.addSpace();
				var padding = {
					left: GetValue$115(config, "space.titleLeft", 0),
					right: GetValue$115(config, "space.titleRight", 0)
				};
				var proportion = titleExpandWidth ? 1 : 0;
				titleSizer.add(title, {
					align: titleAlign,
					proportion,
					expand: titleChildExpand,
					padding
				});
				if (useSizer && !titleExpandWidth && titleAlign === "left") titleSizer.addSpace();
			}
			if (toolbarSizer) {
				if (useSizer && !title) titleSizer.addSpace();
				titleSizer.add(toolbarSizer, {
					align: "right",
					expand: titleChildExpand
				});
			}
			var padding;
			if (content || description || choices || actions) padding = {
				bottom: GetValue$115(config, "space.title", 0),
				top: GetValue$115(config, "space.titleTop", 0)
			};
			var proportion = GetValue$115(config, "proportion.title", 0);
			this.add(titleSizer, {
				padding,
				proportion,
				expand: true
			});
		}
		if (content) {
			var align = GetValue$115(config, "align.content", "center");
			var contentSpace = GetValue$115(config, "space.content", 0);
			var padding = {
				left: GetValue$115(config, "space.contentLeft", 0),
				right: GetValue$115(config, "space.contentRight", 0),
				bottom: description || choices || actions ? contentSpace : 0
			};
			var proportion = GetValue$115(config, "proportion.content", 0);
			var expand = GetValue$115(config, "expand.content", true);
			this.add(content, {
				align,
				padding,
				proportion,
				expand
			});
		}
		if (description) {
			var align = GetValue$115(config, "align.description", "center");
			var descriptionSpace = GetValue$115(config, "space.description", 0);
			var padding = {
				left: GetValue$115(config, "space.descriptionLeft", 0),
				right: GetValue$115(config, "space.descriptionRight", 0),
				bottom: choices || actions ? descriptionSpace : 0
			};
			var proportion = GetValue$115(config, "proportion.description", 0);
			var expand = GetValue$115(config, "expand.description", true);
			this.add(description, {
				align,
				padding,
				proportion,
				expand
			});
		}
		if (choices) {
			var choicesType = GetValue$115(config, "choicesType", "").split("-");
			var ButtonsClass = Contains(choicesType, "wrap") ? Buttons : Contains(choicesType, "grid") ? GridButtons : Buttons$1;
			var buttonsType = Contains(choicesType, "radio") ? "radio" : Contains(choicesType, "checkboxes") ? "checkboxes" : void 0;
			var space = {
				left: GetValue$115(config, "space.choicesBackgroundLeft", 0),
				right: GetValue$115(config, "space.choicesBackgroundRight", 0),
				top: GetValue$115(config, "space.choicesBackgroundTop", 0),
				bottom: GetValue$115(config, "space.choicesBackgroundBottom", 0)
			};
			var itemSpace = GetValue$115(config, "space.choice", 0);
			if (ButtonsClass === Buttons$1) space.item = itemSpace;
			else if (ButtonsClass === Buttons) {
				space.item = itemSpace;
				space.line = GetValue$115(config, "space.choiceLine", itemSpace);
			} else {
				space.column = GetValue$115(config, "space.choiceColumn", itemSpace);
				space.row = GetValue$115(config, "space.choiceRow", itemSpace);
			}
			var choicesConfig = {
				width: GetValue$115(config, "choicesWidth", void 0),
				height: GetValue$115(config, "choicesHeight", void 0),
				groupName: "choices",
				buttonsType,
				background: choicesBackground,
				buttons: choices,
				space,
				click: clickConfig,
				eventEmitter: this.eventEmitter,
				setValueCallback: GetValue$115(config, "choicesSetValueCallback", void 0),
				setValueCallbackScope: GetValue$115(config, "choicesSetValueCallbackScope", void 0)
			};
			if (ButtonsClass === Buttons$1) choicesConfig.orientation = Contains(choicesType, "x") ? 0 : 1;
			choicesSizer = new ButtonsClass(scene, choicesConfig);
			scene.add.existing(choicesSizer);
			var choicesSpace = GetValue$115(config, "space.choices", 0);
			var padding = {
				left: GetValue$115(config, "space.choicesLeft", 0),
				right: GetValue$115(config, "space.choicesRight", 0),
				bottom: actions ? choicesSpace : 0
			};
			var align = GetValue$115(config, "align.choices", "center");
			var proportion = GetValue$115(config, "proportion.choices", 0);
			var expand = GetValue$115(config, "expand.choices", true);
			this.add(choicesSizer, {
				align,
				padding,
				proportion,
				expand
			});
			this.buttonsType = buttonsType;
		}
		if (actions) {
			actionsSizer = new Buttons$1(scene, {
				groupName: "actions",
				background: actionsBackground,
				buttons: actions,
				orientation: 0,
				space: { item: GetValue$115(config, "space.action", 0) },
				expand: GetValue$115(config, "expand.actions", false),
				align: GetValue$115(config, "align.actions", "center"),
				click: clickConfig,
				eventEmitter: this.eventEmitter
			});
			scene.add.existing(actionsSizer);
			var padding = {
				left: GetValue$115(config, "space.actionsLeft", 0),
				right: GetValue$115(config, "space.actionsRight", 0),
				bottom: GetValue$115(config, "space.actionsBottom", 0)
			};
			var proportion = GetValue$115(config, "proportion.action", 0);
			this.add(actionsSizer, {
				align: "center",
				padding,
				proportion,
				expand: true
			});
		}
		EmitButtonEvent(this, "click");
		EmitButtonEvent(this, "over");
		EmitButtonEvent(this, "out");
		EmitButtonEvent(this, "enable");
		EmitButtonEvent(this, "disable");
		this.addChildrenMap("background", background);
		this.addChildrenMap("title", title);
		this.addChildrenMap("toolbar", toolbar);
		this.addChildrenMap("leftToolbar", leftToolbar);
		this.addChildrenMap("content", content);
		this.addChildrenMap("description", description);
		this.addChildrenMap("choices", choicesSizer ? choicesSizer.buttons : void 0);
		this.addChildrenMap("actions", actionsSizer ? actionsSizer.buttons : void 0);
		this.addChildrenMap("choicesSizer", choicesSizer);
		this.addChildrenMap("actionsSizer", actionsSizer);
		this.addChildrenMap("toolbarSizer", toolbarSizer);
		this.addChildrenMap("leftToolbarSizer", leftToolbarSizer);
	}
};
var Contains = function(arr, item) {
	return arr.indexOf(item) !== -1;
};
var ButtonsGroupEventNameMap = {
	actions: "action",
	choices: "choice",
	toolbar: "toolbar",
	leftToolbar: "leftToolbar"
};
var EmitButtonEvent = function(dialog, postEventName) {
	dialog.on(`button.${postEventName}`, function(button, groupName, index, pointer, event) {
		if (!ButtonsGroupEventNameMap.hasOwnProperty(groupName)) return;
		dialog.emit(`${ButtonsGroupEventNameMap[groupName]}.${postEventName}`, button, index, pointer, event);
	});
};
Object.assign(Dialog.prototype, Methods$10);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/dialog/Factory.js
ObjectFactory.register("dialog", function(config) {
	var gameObject = new Dialog(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.Dialog", Dialog);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/build/CreateLabel.js
var CreateLabel = function(scene, config, creators) {
	var gameObject = new SimpleLabel(scene, config, creators);
	scene.add.existing(gameObject);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/confirmdialog/methods/ResetDisplayContent.js
var ResetDisplayContent$1 = function(config) {
	if (config === void 0) config = {};
	ResetTitle$1.call(this, config);
	ResetContent$1.call(this, config);
	ResetActions$1.call(this, config);
	ResetChoices.call(this, config);
	return this;
};
var ResetTitle$1 = function(config) {
	var title = this.childrenMap.title;
	config = config.title;
	if (config === null) title.hide();
	else {
		title.show();
		title.resetDisplayContent(config);
	}
};
var ResetContent$1 = function(config) {
	var content = this.childrenMap.content;
	config = config.content;
	if (config === null) content.hide();
	else {
		content.show();
		if (content.resetDisplayContent) content.resetDisplayContent(config);
		else {
			var text = config || "";
			content.setText(text);
		}
	}
};
var ResetActions$1 = function(config) {
	var actionButtons = this.childrenMap.actions;
	if (!actionButtons) return;
	var buttonContentArray = config.buttons;
	if (!buttonContentArray) {
		var buttonA = actionButtons[0];
		if (buttonA) {
			if (config.buttonA === null) buttonA.hide();
			else {
				buttonA.show();
				buttonA.resetDisplayContent(config.buttonA);
			}
		}
		var buttonB = actionButtons[1];
		if (buttonB) {
			if (config.buttonB === null) buttonB.hide();
			else {
				buttonB.show();
				buttonB.resetDisplayContent(config.buttonB);
			}
		}
	} else {
		var scene = this.scene;
		var defaultActionConfig = this.defaultActionConfig;
		var defaultActionButtonCreator = this.defaultActionButtonCreator;
		for (var i = 0, cnt = buttonContentArray.length; i < cnt; i++) {
			var buttonContent = buttonContentArray[i];
			var button = actionButtons[i];
			if (!button) {
				button = CreateLabel(scene, defaultActionConfig, defaultActionButtonCreator);
				this.addAction(button);
			}
			button.show().resetDisplayContent(buttonContent);
		}
		this.buttonMode = buttonContentArray.length;
		for (var i = buttonContentArray.length, cnt = actionButtons.length; i < cnt; i++) actionButtons[i].hide();
	}
};
var ResetChoices = function(config) {
	var choices = this.childrenMap.choices;
	if (!choices) return;
	var buttonContentArray = config.choices;
	if (!buttonContentArray) buttonContentArray = [];
	var scene = this.scene;
	var defaultChoiceConfig = this.defaultChoiceConfig;
	var defaultActionButtonCreator = this.defaultActionButtonCreator;
	for (var i = 0, cnt = buttonContentArray.length; i < cnt; i++) {
		var buttonContent = buttonContentArray[i];
		if (typeof buttonContent === "string") buttonContent = { text: buttonContent };
		var button = choices[i];
		if (!button) {
			button = CreateLabel(scene, defaultChoiceConfig, defaultActionButtonCreator);
			this.addChoice(button);
		}
		button.show().resetDisplayContent(buttonContent);
		var optionValue;
		if (buttonContent.hasOwnProperty("value")) optionValue = buttonContent.value;
		else optionValue = buttonContent.text;
		button.setName(optionValue);
	}
	for (var i = buttonContentArray.length, cnt = choices.length; i < cnt; i++) choices[i].hide();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/confirmdialog/methods/Modal.js
var Merge$4 = __webpack_exports__Utils.Objects.Merge;
var Modal$1 = function(config, onClose) {
	if (IsFunction(config)) {
		onClose = config;
		config = void 0;
	}
	if (config === void 0) config = {};
	config = Merge$4(config, this.modalStyle);
	var zeroButtonMode;
	if (this.buttonMode === 0) {
		if (this.hasAnyChoice()) zeroButtonMode = false;
		else zeroButtonMode = true;
	} else zeroButtonMode = false;
	if (!config.hasOwnProperty("anyTouchClose")) config.anyTouchClose = zeroButtonMode;
	if (!config.hasOwnProperty("manualClose")) config.manualClose = !zeroButtonMode;
	var self = this;
	var onCloseWrap = function(data) {
		var buttonIndex = data.index;
		if (buttonIndex === self.confirmButtonIndex) self.emit("confirm", data);
		else if (buttonIndex === self.cancelButtonIndex) self.emit("cancel", data);
		if (onClose) onClose(data);
	};
	ModalMethods_default$1.modal.call(this, config, onCloseWrap);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/confirmdialog/methods/SetButtonIndexMethods.js
var SetButtonIndexMethods_default = {
	setConfirmButtonIndex(index) {
		this.confirmButtonIndex = index;
		return this;
	},
	setCancelButtonIndex(index) {
		this.cancelButtonIndex = index;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/confirmdialog/methods/Methods.js
var Methods$9 = {
	resetDisplayContent: ResetDisplayContent$1,
	modal: Modal$1
};
Object.assign(Methods$9, SetButtonIndexMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/dialog/utils/RegisterSimpleLabelButtonEvents.js
var OnPointerOverCallback = function(button) {
	if (button.setHoverState) button.setHoverState(true);
};
var OnPointerOutCallback = function(button) {
	if (button.setHoverState) button.setHoverState(false);
};
var OnChoiceButtonStateChange = function(button, groupName, index, value) {
	if (button.setActiveState) button.setActiveState(value);
};
var OnButtonEnable = function(button) {
	if (button.setDisableState) button.setDisableState(false);
};
var OnButtonDisable = function(button) {
	if (button.setDisableState) button.setDisableState(true);
};
var RegisterEvents = function() {
	this.on("button.over", OnPointerOverCallback).on("button.out", OnPointerOutCallback).on("button.enable", OnButtonEnable).on("button.disable", OnButtonDisable).on("button.statechange", OnChoiceButtonStateChange);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/confirmdialog/methods/CreateContent.js
var GetValue$114 = __webpack_exports__Utils.Objects.GetValue;
var CreateContent$1 = function(scene, config, creators) {
	var type = GetValue$114(config, "$type");
	if (type === void 0) {
		if (config && (config.hasOwnProperty("slider") || config.hasOwnProperty("scroller"))) type = "textarea";
	}
	var gameObject;
	switch (type) {
		case "textarea":
			gameObject = new CreateTextArea(scene, config, creators);
			break;
		default: gameObject = new CreateLabel(scene, config, creators);
	}
	scene.add.existing(gameObject);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/confirmdialog/ConfirmDialog.js
var GetValue$113 = __webpack_exports__Utils.Objects.GetValue;
var ConfirmDialog = class extends Dialog {
	constructor(scene, config, creators) {
		config = config ? DeepClone(config) : {};
		if (creators === void 0) creators = {};
		var createBackground = GetValue$113(creators, "background", CreateBackground$2);
		config.background = createBackground(scene, config.background);
		config.title = CreateLabel(scene, config.title, creators.title);
		config.content = CreateContent$1(scene, config.content, creators.content);
		if (config.content instanceof TextArea) {
			if (HasValue(config, "height") && !HasValue(config, "proportion.content")) SetValue(config, "proportion.content", 1);
		}
		var defaultButtonConfig = config.button;
		var buttonAConfig = config.buttonA || defaultButtonConfig;
		var buttonBConfig = config.buttonB || defaultButtonConfig;
		var buttonMode = config.buttonMode;
		if (buttonMode === void 0) buttonMode = !!buttonAConfig && !!buttonBConfig ? 2 : !!buttonAConfig ? 1 : 0;
		var defaultButtonCreator = creators.button;
		var buttonACreators = creators.buttonA || defaultButtonCreator;
		var buttonBCreators = creators.buttonB || defaultButtonCreator;
		switch (buttonMode) {
			case 2:
				config.actions = [CreateLabel(scene, buttonAConfig, buttonACreators), CreateLabel(scene, buttonBConfig, buttonBCreators)];
				break;
			case 1:
				config.actions = [CreateLabel(scene, buttonAConfig, buttonACreators)];
				break;
			case 0: break;
			default: config.actions = [];
		}
		var defaultChoiceConfig = config.choice;
		if (defaultChoiceConfig) config.choices = [];
		super(scene, config);
		this.type = "rexConfirmDialog";
		this.buttonMode = buttonMode;
		this.defaultActionConfig = defaultButtonConfig;
		this.defaultActionButtonCreator = defaultButtonCreator;
		this.defaultChoiceConfig = defaultChoiceConfig;
		this.defaultChoiceCreator = creators.choice;
		var buttons = this.childrenMap.actions;
		this.addChildrenMap("buttonA", buttons ? buttons[0] : null);
		this.addChildrenMap("buttonB", buttons ? buttons[1] : null);
		RegisterEvents.call(this);
		this.setConfirmButtonIndex(GetValue$113(config, "confirmButtonIndex", 0));
		this.setCancelButtonIndex(GetValue$113(config, "cancelButtonIndex", 1));
		this.modalStyle = config.modal || {};
	}
};
Object.assign(ConfirmDialog.prototype, Methods$9);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/confirmdialog/Factory.js
ObjectFactory.register("confirmDialog", function(config, creators) {
	var gameObject = new ConfirmDialog(this.scene, config, creators);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.ConfirmDialog", ConfirmDialog);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/confirmdialog/ConfirmAction.js
var GetValue$112 = __webpack_exports__Utils.Objects.GetValue;
var ConfirmAction = function(scene, config) {
	var dialog = config.dialog;
	var newDialogMode = !dialog;
	if (newDialogMode) {
		var dialogStyle = config.style;
		if (dialogStyle.buttonMode === void 0) dialogStyle.buttonMode = 2;
		dialog = new ConfirmDialog(scene, dialogStyle, config.creators);
		scene.add.existing(dialog);
	}
	dialog.setConfirmButtonIndex(GetValue$112(config, "confirmButtonIndex", 0)).setCancelButtonIndex(GetValue$112(config, "cancelButtonIndex", 1)).resetDisplayContent(config.content).layout();
	if (newDialogMode && config.onCreateDialog) config.onCreateDialog(dialog);
	var modalConfig = config.modal;
	if (modalConfig && !modalConfig.hasOwnProperty("destroy")) modalConfig.destroy = newDialogMode;
	var confirmCallback = config.confirm;
	var cancelCallback = config.cancel;
	var confirmScope = config.confirmScope;
	var cancelScope = config.cancelScope;
	var closeCallback = config.close;
	var closeScope = config.closeScope;
	if (confirmCallback) dialog.once("confirm", confirmCallback, confirmScope);
	if (cancelCallback) dialog.once("cancel", cancelCallback, cancelScope);
	var onClose = function(data) {
		if (confirmCallback) dialog.off("confirm", confirmCallback, confirmScope);
		if (cancelCallback) dialog.off("cancel", cancelCallback, cancelScope);
		if (closeCallback) closeCallback.call(closeScope, data);
	};
	dialog.modal(modalConfig, onClose);
	return dialog;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/confirmactionbutton/ConfirmActionButton.js
var ConfirmActionButton = class extends Label {
	constructor(scene, config) {
		if (config === void 0) config = {};
		super(scene, config);
		this.type = "rexConfirmActionButton";
		this.setConfirmDialogEnable();
		this.confirmActionConfig = Clone$2(config.confirmDialog || {});
		if (config.confirm) this.setConfirmCallback(config.confirm, config.confirmScope);
		if (config.cancel) this.setCancelCallback(config.cancel, config.cancelScope);
		this.onClickCallback = function() {
			if (this.confirmDialogEnable) {
				if (this.confirmDialog) return;
				this.confirmDialog = ConfirmAction(scene, this.confirmActionConfig);
				this.confirmDialog.once("destroy", function() {
					this.confirmDialog = void 0;
				}, this);
			} else this.runConfirmCallback();
		};
		this.onClick(this.onClickCallback, this);
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		if (this.confirmDialog) this.confirmDialog.destroy();
		super.destroy(fromScene);
		this.confirmActionConfig = void 0;
		this.onClickCallback = void 0;
		this.confirmDialog = void 0;
	}
	setConfirmCallback(callback, scope) {
		this.confirmActionConfig.confirm = callback;
		this.confirmActionConfig.confirmScope = scope;
		return this;
	}
	setCancelCallback(callback, scope) {
		this.confirmActionConfig.cancel = callback;
		this.confirmActionConfig.cancelScope = scope;
		return this;
	}
	setConfirmDialogContent(content) {
		this.confirmActionConfig.content = content;
		return this;
	}
	setConfirmDialogStyle(style) {
		this.confirmActionConfig.style = style;
		return this;
	}
	setConfirmDialogModalConfig(config) {
		this.confirmActionConfig.modal = config;
		return this;
	}
	setConfirmDialogEnable(enable) {
		if (enable === void 0) enable = true;
		this.confirmDialogEnable = enable;
		return this;
	}
	runConfirmCallback() {
		var callback = this.confirmActionConfig.confirm;
		var scope = this.confirmActionConfig.confirmScope;
		if (callback) callback.call(scope);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/confirmactionbutton/Factory.js
ObjectFactory.register("confirmActionButton", function(config) {
	var gameObject = new ConfirmActionButton(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.ConfirmActionButton", ConfirmActionButton);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/nameinputdialog/methods/ResetDisplayContent.js
var ResetDisplayContent = function(config) {
	if (config === void 0) config = {};
	ResetTitle.call(this, config);
	ResetContent.call(this, config);
	ResetActions.call(this, config);
	return this;
};
var ResetTitle = function(config) {
	var title = this.childrenMap.title;
	if (config.title === null) this.hide(title);
	else {
		this.show(title);
		title.resetDisplayContent(config.title);
	}
};
var ResetContent = function(config) {
	var firstNameTitle = this.childrenMap.firstNameTitle;
	if (config.firstNameTitle === null) this.hide(firstNameTitle);
	else {
		this.show(firstNameTitle);
		var firstNameTitleConfig = config.firstNameTitle || "";
		firstNameTitle.resetDisplayContent(firstNameTitleConfig);
	}
	var firstNameInput = this.childrenMap.firstNameInput;
	if (config.firstName === null) this.hide(firstNameInput);
	else {
		this.show(firstNameInput);
		var firstName = config.firstName || "";
		firstNameInput.setText(firstName);
	}
	var lastNameTitle = this.childrenMap.lastNameTitle;
	if (config.lastNameTitle === null) this.hide(lastNameTitle);
	else {
		this.show(lastNameTitle);
		var lastNameTitleConfig = config.lastNameTitle || "";
		lastNameTitle.resetDisplayContent(lastNameTitleConfig);
	}
	var lastNameInput = this.childrenMap.lastNameInput;
	if (config.lastName === null) this.hide(lastNameInput);
	else {
		this.show(lastNameInput);
		var lastName = config.lastName || "";
		lastNameInput.setText(lastName);
	}
};
var ResetActions = function(config) {
	var action = this.childrenMap.actions[0];
	if (config.button === null) this.hide(action);
	else {
		this.show(action);
		action.resetDisplayContent(config.button);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/nameinputdialog/methods/Modal.js
var Merge$3 = __webpack_exports__Utils.Objects.Merge;
var Modal = function(config, onClose) {
	if (IsFunction(config)) {
		onClose = config;
		config = void 0;
	}
	if (config === void 0) config = {};
	config = Merge$3(config, this.modalStyle);
	config.anyTouchClose = false;
	config.manualClose = false;
	var self = this;
	var onCloseWrap = function(data) {
		data.firstName = self.firstName;
		data.lastName = self.lastName;
		self.emit("confirm", data);
		if (onClose) onClose(data);
	};
	ModalMethods_default$1.modal.call(this, config, onCloseWrap);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/nameinputdialog/methods/Methods.js
var methods$16 = {
	resetDisplayContent: ResetDisplayContent,
	modal: Modal
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/build/CreateInputText.js
var CreateInputText$1 = function(scene, config, deepCloneConfig) {
	if (deepCloneConfig === void 0) deepCloneConfig = true;
	if (deepCloneConfig) config = config ? DeepClone(config) : {};
	else if (!config) config = {};
	var inputText = new CanvasInput_default(scene, config);
	scene.add.existing(inputText);
	return inputText;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/nameinputdialog/methods/CreateContent.js
var GetValue$111 = __webpack_exports__Utils.Objects.GetValue;
var CreateContent = function(scene, config, creators) {
	var layoutMode = GetOrientationMode_default(GetValue$111(config, "layoutMode", 0));
	var isHorizontalLayout = layoutMode === 0;
	var nameSizer = new Sizer(scene, {
		orientation: layoutMode,
		space: { item: GetValue$111(config, "space.firstName", 0) }
	});
	scene.add.existing(nameSizer);
	var firstNameSizer = new Sizer(scene, { orientation: "x" });
	scene.add.existing(firstNameSizer);
	var firstNameTitle = CreateLabel(scene, GetValue$111(config, "firstNameTitle", config.nameTitle), creators.firstNameTitle || creators.nameTitle);
	firstNameSizer.add(firstNameTitle, {
		expand: true,
		proportion: GetValue$111(config, "proportion.firstNameTitle", 0),
		padding: { right: GetValue$111(config, "space.firstNameTitle", 0) }
	});
	var defaultNameInputConfig = config.nameInput || {};
	var firstNameInputConfig = GetValue$111(config, "firstNameInput", defaultNameInputConfig);
	var firstNameInput = CreateInputText$1(scene, firstNameInputConfig, creators.firstNameInput || creators.nameInput);
	var expandFirstNameSizer = !firstNameInputConfig.hasOwnProperty("width");
	firstNameSizer.add(firstNameInput, {
		expand: true,
		proportion: expandFirstNameSizer ? 1 : 0
	});
	var lastNameSizer = new Sizer(scene, { orientation: "x" });
	scene.add.existing(lastNameSizer);
	var lastNameTitle = CreateLabel(scene, GetValue$111(config, "lastNameTitle", config.nameTitle), creators.lastNameTitle || creators.nameTitle);
	lastNameSizer.add(lastNameTitle, {
		expand: true,
		proportion: GetValue$111(config, "proportion.lastNameTitle", 0),
		padding: { right: GetValue$111(config, "space.lastNameTitle", 0) }
	});
	var lastNameInputConfig = GetValue$111(config, "firstNameInput", defaultNameInputConfig);
	var lastNameInput = CreateInputText$1(scene, lastNameInputConfig, creators.lastNameInput || creators.nameInput);
	var expandLastNameSizer = !lastNameInputConfig.hasOwnProperty("width");
	lastNameSizer.add(lastNameInput, {
		expand: true,
		proportion: expandLastNameSizer ? 1 : 0
	});
	if (isHorizontalLayout) {
		var firstNameProportion = GetValue$111(config, "proportion.firstName", expandFirstNameSizer ? 1 : 0);
		nameSizer.add(firstNameSizer, {
			expand: true,
			proportion: firstNameProportion
		});
		var lastNameProportion = GetValue$111(config, "proportion.lastName", expandLastNameSizer ? 1 : 0);
		nameSizer.add(lastNameSizer, {
			expand: true,
			proportion: lastNameProportion
		});
	} else {
		nameSizer.add(firstNameSizer, {
			expand: expandFirstNameSizer,
			proportion: 0
		});
		nameSizer.add(lastNameSizer, {
			expand: expandLastNameSizer,
			proportion: 0
		});
	}
	nameSizer.addChildrenMap("firstNameTitle", firstNameTitle);
	nameSizer.addChildrenMap("firstNameInput", firstNameInput);
	nameSizer.addChildrenMap("lastNameTitle", lastNameTitle);
	nameSizer.addChildrenMap("lastNameInput", lastNameInput);
	return nameSizer;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/nameinputdialog/NameInputDialog.js
var GetValue$110 = __webpack_exports__Utils.Objects.GetValue;
var NameInputDialog = class extends Dialog {
	constructor(scene, config, creators) {
		if (config === void 0) config = {};
		if (creators === void 0) creators = {};
		config.orientation = "y";
		var spaceConfig = config.space;
		if (spaceConfig) {
			if (spaceConfig.hasOwnProperty("names")) spaceConfig.content = spaceConfig.names;
			if (spaceConfig.hasOwnProperty("namesLeft")) spaceConfig.contentLeft = spaceConfig.namesLeft;
			if (spaceConfig.hasOwnProperty("namesRight")) spaceConfig.contentRight = spaceConfig.namesRight;
		}
		var createBackground = GetValue$110(creators, "background", CreateBackground$2);
		config.background = createBackground(scene, config.background);
		config.title = CreateLabel(scene, config.title, creators.title);
		config.content = CreateContent(scene, config, creators);
		config.actions = [CreateLabel(scene, config.button, creators.button)];
		super(scene, config);
		this.type = "rexNameInputDialog";
		var namesSizerChildrenMap = config.content.childrenMap;
		this.addChildrenMap("firstNameTitle", namesSizerChildrenMap.firstNameTitle);
		this.addChildrenMap("firstNameInput", namesSizerChildrenMap.firstNameInput);
		this.addChildrenMap("lastNameTitle", namesSizerChildrenMap.lastNameTitle);
		this.addChildrenMap("lastNameInput", namesSizerChildrenMap.lastNameInput);
		this.addChildrenMap("button", config.actions[0]);
		RegisterEvents.call(this);
		this.modalStyle = config.modal || {};
	}
	get firstName() {
		return this.childrenMap.firstNameInput.text;
	}
	set firstName(value) {
		this.childrenMap.firstNameInput.setText(value);
	}
	setFirstName(value) {
		this.firstName = value;
		return this;
	}
	get lastName() {
		return this.childrenMap.lastNameInput.text;
	}
	set lastName(value) {
		this.childrenMap.lastNameInput.setText(value);
	}
	setLastName(value) {
		this.lastName = value;
		return this;
	}
};
Object.assign(NameInputDialog.prototype, methods$16);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/nameinputdialog/Factory.js
ObjectFactory.register("nameInputDialog", function(config, creators) {
	var gameObject = new NameInputDialog(this.scene, config, creators);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.NameInputDialog", NameInputDialog);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/RemoveFromParent.js
var RemoveFromParent = function(gameObject, destroyChild) {
	if (!gameObject) return;
	var parent = GetParent$1(gameObject);
	if (!parent) return;
	parent.remove(gameObject, destroyChild);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/holygrail/methods/GetAddChildConfig.js
var GetValue$109 = __webpack_exports__Utils.Objects.GetValue;
var GetAddChildConfig = function(config, key, defaultValues) {
	var proportion = GetValue$109(config, `proportion.${key}`, defaultValues.proportion);
	var align = GetValue$109(config, `align.${key}`, "center");
	var padding = GetValue$109(config, `space.${key}`, void 0);
	if (typeof padding === "number" && defaultValues.paddingKey) {
		var paddingNum = padding;
		padding = {};
		padding[defaultValues.paddingKey] = paddingNum;
	}
	var expand = GetValue$109(config, `expand.${key}`, true);
	return {
		proportion,
		align,
		padding,
		expand
	};
};
var GetAddHeaderConfig = function(config) {
	return GetAddChildConfig(config, "header", {
		proportion: 0,
		paddingKey: "bottom"
	});
};
var GetAddLeftSideConfig = function(config) {
	return GetAddChildConfig(config, "leftSide", {
		proportion: 0,
		paddingKey: "right"
	});
};
var GetAddContentConfig = function(config) {
	return GetAddChildConfig(config, "content", { proportion: 1 });
};
var GetAddRightSideConfig = function(config) {
	return GetAddChildConfig(config, "rightSide", {
		proportion: 0,
		paddingKey: "left"
	});
};
var GetAddFooterConfig = function(config) {
	return GetAddChildConfig(config, "footer", {
		proportion: 0,
		paddingKey: "top"
	});
};
var GetAddContainerConfig = function(config) {
	return {
		proportion: 1,
		align: "center",
		padding: 0,
		expand: true
	};
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/holygrail/methods/CreatExpandContainer.js
var CreatExpandContainer$1 = function(scene, orientation) {
	var container = new Sizer(scene, { orientation });
	scene.add.existing(container);
	return container;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/holygrail/methods/LayoutMode0.js
var LayoutMode0$3 = function(config) {
	var scene = this.scene;
	var header = config.header;
	if (header) this.add(header, GetAddHeaderConfig(config));
	var bodySizer = CreatExpandContainer$1(scene, 0);
	this.add(bodySizer, GetAddContainerConfig(config));
	var leftSide = config.leftSide;
	if (leftSide) bodySizer.add(leftSide, GetAddLeftSideConfig(config));
	var content = config.content;
	if (content) bodySizer.add(content, GetAddContentConfig(config));
	var rightSide = config.rightSide;
	if (rightSide) bodySizer.add(rightSide, GetAddRightSideConfig(config));
	var footer = config.footer;
	if (footer) this.add(footer, GetAddFooterConfig(config));
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/holygrail/methods/LayoutMode1.js
var LayoutMode1$2 = function(config) {
	var scene = this.scene;
	var header = config.header;
	if (header) this.add(header, GetAddHeaderConfig(config));
	var bodySizer0 = CreatExpandContainer$1(scene, 0);
	this.add(bodySizer0, GetAddContainerConfig(config));
	var leftSide = config.leftSide;
	if (leftSide) bodySizer0.add(leftSide, GetAddLeftSideConfig(config));
	var bodySizer1 = CreatExpandContainer$1(scene, 1);
	bodySizer0.add(bodySizer1, GetAddContainerConfig(config));
	var bodySizer2 = CreatExpandContainer$1(scene, 0);
	bodySizer1.add(bodySizer2, GetAddContainerConfig(config));
	var content = config.content;
	if (content) bodySizer2.add(content, GetAddContentConfig(config));
	var rightSide = config.rightSide;
	if (rightSide) bodySizer2.add(rightSide, GetAddRightSideConfig(config));
	var footer = config.footer;
	if (footer) bodySizer1.add(footer, GetAddFooterConfig(config));
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/holygrail/methods/LayoutMode2.js
var LayoutMode2 = function(config) {
	var scene = this.scene;
	var header = config.header;
	if (header) this.add(header, GetAddHeaderConfig(config));
	var bodySizer0 = CreatExpandContainer$1(scene, 0);
	this.add(bodySizer0, GetAddContainerConfig(config));
	var bodySizer1 = CreatExpandContainer$1(scene, 1);
	bodySizer0.add(bodySizer1, GetAddContainerConfig(config));
	var bodySizer2 = CreatExpandContainer$1(scene, 0);
	bodySizer1.add(bodySizer2, GetAddContainerConfig(config));
	var leftSide = config.leftSide;
	if (leftSide) bodySizer2.add(leftSide, GetAddLeftSideConfig(config));
	var content = config.content;
	if (content) bodySizer2.add(content, GetAddContentConfig(config));
	var footer = config.footer;
	if (footer) bodySizer1.add(footer, GetAddFooterConfig(config));
	var rightSide = config.rightSide;
	if (rightSide) bodySizer0.add(rightSide, GetAddRightSideConfig(config));
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/holygrail/methods/LayoutMode3.js
var LayoutMode0$2 = function(config) {
	var scene = this.scene;
	var header = config.header;
	if (header) this.add(header, GetAddHeaderConfig(config));
	var bodySizer0 = CreatExpandContainer$1(scene, 0);
	this.add(bodySizer0, GetAddContainerConfig(config));
	var leftSide = config.leftSide;
	if (leftSide) bodySizer0.add(leftSide, GetAddLeftSideConfig(config));
	var bodySizer1 = CreatExpandContainer$1(scene, 1);
	bodySizer0.add(bodySizer1, GetAddContainerConfig(config));
	var content = config.content;
	if (content) bodySizer1.add(content, GetAddContentConfig(config));
	var footer = config.footer;
	if (footer) bodySizer1.add(footer, GetAddFooterConfig(config));
	var rightSide = config.rightSide;
	if (rightSide) bodySizer0.add(rightSide, GetAddRightSideConfig(config));
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/holygrail/methods/Build.js
var GetValue$108 = __webpack_exports__Utils.Objects.GetValue;
var LayoutCallbacks$2 = [
	LayoutMode0$3,
	LayoutMode1$2,
	LayoutMode2,
	LayoutMode0$2
];
var Build$2 = function(config) {
	if (config === void 0) config = {};
	var background = config.background;
	var header = config.header;
	var leftSide = config.leftSide;
	var content = config.content;
	var rightSide = config.rightSide;
	var footer = config.footer;
	RemoveFromParent(background);
	RemoveFromParent(header);
	RemoveFromParent(leftSide);
	RemoveFromParent(content);
	RemoveFromParent(rightSide);
	RemoveFromParent(footer);
	this.clear(true);
	if (background) this.addBackground(background);
	var layoutMode = GetValue$108(config, "layoutMode", 0);
	if (typeof layoutMode === "string") layoutMode = LayoutModesMap[layoutMode.toUpperCase()];
	(LayoutCallbacks$2[layoutMode] || LayoutCallbacks$2[0]).call(this, config);
	this.addChildrenMap("background", background);
	this.addChildrenMap("header", header);
	this.addChildrenMap("leftSide", leftSide);
	this.addChildrenMap("content", content);
	this.addChildrenMap("rightSide", rightSide);
	this.addChildrenMap("footer", footer);
	return this;
};
var LayoutModesMap = {
	"FFF": 0,
	"LFF": 1,
	"FFR": 2,
	"LFR": 3
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/holygrail/HolyGrail.js
var HolyGrail = class extends Sizer {
	constructor(scene, config) {
		if (config === void 0) config = {};
		config.orientation = 1;
		super(scene, config);
		this.type = "rexHolyGrail";
		this.build(config);
	}
};
var methods$15 = { build: Build$2 };
Object.assign(HolyGrail.prototype, methods$15);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/holygrail/Factory.js
ObjectFactory.register("holyGrail", function(config) {
	var gameObject = new HolyGrail(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.HolyGrail", HolyGrail);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tabs/ButtonMethods.js
var ButtonMethods_default = {
	getButtonsSizer(groupName) {
		return this.childrenMap[`${groupName}ButtonsSizer`];
	},
	getButton(groupName, index) {
		var buttonsSizer = this.getButtonsSizer(groupName);
		return buttonsSizer ? buttonsSizer.getButton(index) : void 0;
	},
	setButtonEnable(groupName, index, enabled) {
		this.getButtonsSizer(groupName).setButtonEnable(index, enabled);
		return this;
	},
	setLeftButtonEnable(index, enabled) {
		this.childrenMap.leftButtonsSizer.setButtonEnable(index, enabled);
		return this;
	},
	setRightButtonEnable(index, enabled) {
		this.childrenMap.rightButtonsSizer.setButtonEnable(index, enabled);
		return this;
	},
	setTopButtonEnable(index, enabled) {
		this.childrenMap.topButtonsSizer.setButtonEnable(index, enabled);
		return this;
	},
	setBottomButtonEnable(index, enabled) {
		this.childrenMap.bottomButtonsSizer.setButtonEnable(index, enabled);
		return this;
	},
	toggleButtonEnable(groupName, index) {
		this.getButtonsSizer(groupName).toggleButtonEnable(index);
		return this;
	},
	toggleLeftButtonEnable(index) {
		this.childrenMap.leftButtonsSizer.toggleButtonEnable(index);
		return this;
	},
	toggleRightButtonEnable(index) {
		this.childrenMap.rightButtonsSizer.toggleButtonEnable(index);
		return this;
	},
	toggleTopButtonEnable(index) {
		this.childrenMap.topButtonsSizer.toggleButtonEnable(index);
		return this;
	},
	toggleBottomButtonEnable(index) {
		this.childrenMap.bottomButtonsSizer.toggleButtonEnable(index);
		return this;
	},
	getButtonEnable(groupName, index) {
		return this.getButtonsSizer(groupName).getButtonEnable(index);
	},
	getLeftButtonEnable(index) {
		return this.childrenMap.leftButtonsSizer.getButtonEnable(index);
	},
	getRightButtonEnable(index) {
		return this.childrenMap.rightButtonsSizer.getButtonEnable(index);
	},
	getTopButtonEnable(index) {
		return this.childrenMap.topButtonsSizer.getButtonEnable(index);
	},
	getBottomButtonEnable(index) {
		return this.childrenMap.bottomButtonsSizer.getButtonEnable(index);
	},
	emitButtonClick(groupName, index) {
		var buttonsSizer = this.getButtonsSizer(groupName);
		if (!buttonsSizer) return this;
		buttonsSizer.emitButtonClick(index);
		return this;
	},
	emitLeftButtonClick(index) {
		this.childrenMap.leftButtonsSizer.emitButtonClick(index);
		return this;
	},
	emitRightButtonClick(index) {
		this.childrenMap.rightButtonsSizer.emitButtonClick(index);
		return this;
	},
	emitTopButtonClick(index) {
		this.childrenMap.topButtonsSizer.emitButtonClick(index);
		return this;
	},
	emitBottomButtonClick(index) {
		this.childrenMap.bottomButtonsSizer.emitButtonClick(index);
		return this;
	},
	getLeftButton(index) {
		return this.childrenMap.leftButtonsSizer.getButton(index);
	},
	getRightButton(index) {
		return this.childrenMap.rightButtonsSizer.getButton(index);
	},
	getTopButton(index) {
		return this.childrenMap.topButtonsSizer.getButton(index);
	},
	getBottomButton(index) {
		return this.childrenMap.bottomButtonsSizer.getButton(index);
	},
	showButton(groupName, index) {
		Show(this.getButton(groupName, index));
		return this;
	},
	showLeftButton(index) {
		Show(this.getLeftButton(index));
		return this;
	},
	showRightButton(index) {
		Show(this.getRightButton(index));
		return this;
	},
	showTopButton(index) {
		Show(this.getTopButton(index));
		return this;
	},
	showBottomButton(index) {
		Show(this.getBottomButton(index));
		return this;
	},
	hideButton(groupName, index) {
		Hide(this.getButton(groupName, index));
		return this;
	},
	hideLeftButton(index) {
		Hide(this.getLeftButton(index));
		return this;
	},
	hideRightButton(index) {
		Hide(this.getRightButton(index));
		return this;
	},
	hideTopButton(index) {
		Hide(this.getTopButton(index));
		return this;
	},
	hideBottomButton(index) {
		Hide(this.getBottomButton(index));
		return this;
	},
	addButton(groupName, gameObject) {
		this.getButtonsSizer(groupName).addButton(gameObject);
		return this;
	},
	addLeftButton(gameObject) {
		this.addButton("left", gameObject);
		return this;
	},
	addRightButton(gameObject) {
		this.addButton("right", gameObject);
		return this;
	},
	addTopButton(gameObject) {
		this.addButton("top", gameObject);
		return this;
	},
	removeButton(groupName, index, destroyChild) {
		this.getButtonsSizer(groupName).removeButton(index, destroyChild);
		return this;
	},
	removeLeftButton(index, destroyChild) {
		this.removeButton("left", index, destroyChild);
		return this;
	},
	removeRightButton(index, destroyChild) {
		this.removeButton("right", index, destroyChild);
		return this;
	},
	removeTopButton(index, destroyChild) {
		this.removeButton("top", index, destroyChild);
		return this;
	},
	removeBottomButton(index, destroyChild) {
		this.removeButton("bottom", index, destroyChild);
		return this;
	},
	clearButtons(groupName, destroyChild) {
		this.getButtonsSizer(groupName).clearButtons(destroyChild);
		return this;
	},
	clearLeftButtons(destroyChild) {
		this.clearButtons("left", destroyChild);
		return this;
	},
	clearRightButtons(destroyChild) {
		this.clearButtons("right", destroyChild);
		return this;
	},
	clearTopButtons(destroyChild) {
		this.clearButtons("top", destroyChild);
		return this;
	},
	clearBottomButtonss(destroyChild) {
		this.clearButtons("bottom", destroyChild);
		return this;
	},
	forEachButton(groupName, callback, scope) {
		this.getButtonsSizer(groupName).forEachButtton(callback, scope);
		return this;
	},
	forEachLeftButton(callback, scope) {
		this.childrenMap.leftButtonsSizer.forEachButtton(callback, scope);
		return this;
	},
	forEachRightButton(callback, scope) {
		this.childrenMap.rightButtonsSizer.forEachButtton(callback, scope);
		return this;
	},
	forEachTopButton(callback, scope) {
		this.childrenMap.topButtonsSizer.forEachButtton(callback, scope);
		return this;
	},
	forEachBottomButton(callback, scope) {
		this.childrenMap.bottomButtonsSizer.forEachButtton(callback, scope);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tabs/Tabs.js
var GetValue$107 = __webpack_exports__Utils.Objects.GetValue;
var Tabs = class extends GridSizer {
	constructor(scene, config) {
		if (config === void 0) config = {};
		config.column = 3;
		config.row = 3;
		config.columnProportions = [
			0,
			0,
			0
		];
		config.rowProportions = [
			0,
			0,
			0
		];
		var expandPanel = GetValue$107(config, "expand.panel", false);
		if (expandPanel) {
			config.columnProportions[1] = 1;
			config.rowProportions[1] = 1;
		}
		super(scene, config);
		this.type = "rexTabs";
		this.eventEmitter = GetValue$107(config, "eventEmitter", this);
		var background = GetValue$107(config, "background", void 0);
		var panel = GetValue$107(config, "panel", void 0);
		var leftButtons = GetValue$107(config, "leftButtons", void 0);
		var leftButtonsBackground = GetValue$107(config, "leftButtonsBackground", void 0);
		var leftButtonsSizer;
		var rightButtons = GetValue$107(config, "rightButtons", void 0);
		var rightButtonsBackground = GetValue$107(config, "rightButtonsBackground", void 0);
		var rightButtonsSizer;
		var topButtons = GetValue$107(config, "topButtons", void 0);
		var topButtonsBackground = GetValue$107(config, "topButtonsBackground", void 0);
		var topButtonsSizer;
		var bottomButtons = GetValue$107(config, "bottomButtons", void 0);
		var bottomButtonsBackground = GetValue$107(config, "bottomButtonsBackground", void 0);
		var bottomButtonsSizer;
		var clickConfig = GetValue$107(config, "click", void 0);
		if (background) this.addBackground(background);
		if (panel) this.add(panel, {
			column: 1,
			row: 1,
			expand: expandPanel
		});
		if (leftButtons) {
			var leftButtonsOffset = GetValue$107(config, "space.leftButtonsOffset", 0);
			var leftButtonSpace = GetValue$107(config, "space.leftButton", 0);
			var leftButtonExpand = GetValue$107(config, "expand.leftButtons", false);
			var leftButtonsAlign = GetValue$107(config, "align.leftButtons", "top");
			leftButtonsSizer = new Buttons$1(scene, {
				groupName: "left",
				background: leftButtonsBackground,
				buttons: leftButtons,
				orientation: "y",
				space: { item: leftButtonSpace },
				expand: leftButtonExpand,
				click: clickConfig,
				eventEmitter: this.eventEmitter
			});
			this.add(leftButtonsSizer, {
				column: 0,
				row: 1,
				align: leftButtonsAlign,
				padding: { top: leftButtonsOffset },
				expand: leftButtonExpand
			});
		}
		if (rightButtons) {
			var rightButtonsOffset = GetValue$107(config, "space.rightButtonsOffset", 0);
			var rightButtonSpace = GetValue$107(config, "space.rightButton", 0);
			var rightButtonExpand = GetValue$107(config, "expand.rightButtons", false);
			var rightButtonsAlign = GetValue$107(config, "align.rightButtons", "top");
			rightButtonsSizer = new Buttons$1(scene, {
				groupName: "right",
				background: rightButtonsBackground,
				buttons: rightButtons,
				orientation: "y",
				space: { item: rightButtonSpace },
				expand: rightButtonExpand,
				click: clickConfig,
				eventEmitter: this.eventEmitter
			});
			this.add(rightButtonsSizer, {
				column: 2,
				row: 1,
				align: rightButtonsAlign,
				padding: { top: rightButtonsOffset },
				expand: rightButtonExpand
			});
		}
		if (topButtons) {
			var toptButtonsOffset = GetValue$107(config, "space.topButtonsOffset", 0);
			var topButtonSpace = GetValue$107(config, "space.topButton", 0);
			var topButtonExpand = GetValue$107(config, "expand.topButtons", false);
			var topButtonsAlign = GetValue$107(config, "align.topButtons", "left");
			topButtonsSizer = new Buttons$1(scene, {
				groupName: "top",
				background: topButtonsBackground,
				buttons: topButtons,
				orientation: "x",
				space: { item: topButtonSpace },
				expand: topButtonExpand,
				align: GetValue$107(config, "align.topButtons", void 0),
				click: clickConfig,
				eventEmitter: this.eventEmitter
			});
			this.add(topButtonsSizer, {
				column: 1,
				row: 0,
				align: topButtonsAlign,
				padding: { left: toptButtonsOffset },
				expand: topButtonExpand
			});
		}
		if (bottomButtons) {
			var bottomButtonsOffset = GetValue$107(config, "space.bottomButtonsOffset", 0);
			var bottomButtonSpace = GetValue$107(config, "space.bottomButton", 0);
			var bottomButtonExpand = GetValue$107(config, "expand.bottomButtons", false);
			var bottomButtonsAlign = GetValue$107(config, "align.bottomButtons", "left");
			bottomButtonsSizer = new Buttons$1(scene, {
				groupName: "bottom",
				background: bottomButtonsBackground,
				buttons: bottomButtons,
				orientation: "x",
				space: { item: bottomButtonSpace },
				expand: bottomButtonExpand,
				align: GetValue$107(config, "align.bottomButtons", void 0),
				click: clickConfig,
				eventEmitter: this.eventEmitter
			});
			this.add(bottomButtonsSizer, {
				column: 1,
				row: 2,
				align: bottomButtonsAlign,
				padding: { left: bottomButtonsOffset },
				expand: bottomButtonExpand
			});
		}
		this.addChildrenMap("background", background);
		this.addChildrenMap("panel", panel);
		this.addChildrenMap("leftButtons", leftButtons);
		this.addChildrenMap("rightButtons", rightButtons);
		this.addChildrenMap("topButtons", topButtons);
		this.addChildrenMap("bottomButtons", bottomButtons);
		this.addChildrenMap("leftButtonsSizer", leftButtonsSizer);
		this.addChildrenMap("rightButtonsSizer", rightButtonsSizer);
		this.addChildrenMap("topButtonsSizer", topButtonsSizer);
		this.addChildrenMap("bottomButtonsSizer", bottomButtonsSizer);
	}
};
Object.assign(Tabs.prototype, ButtonMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tabs/Factory.js
ObjectFactory.register("tabs", function(config) {
	var gameObject = new Tabs(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.Tabs", Tabs);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/slider/Factory.js
ObjectFactory.register("slider", function(config) {
	var gameObject = new Slider(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.Slider", Slider);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/table/Cell.js
var Cell = class {
	constructor(parent, config) {
		this.container = null;
		this._deltaHeight = 0;
		this.setParent(parent);
	}
	setParent(parent) {
		this.parent = parent;
		this.parentContainer = parent.getParentContainer();
	}
	destroy(fromScene) {
		if (fromScene === void 0) fromScene = false;
		if (!fromScene) this.destroyContainer();
		this.deltaHeight = 0;
		this.data = void 0;
		this.container = null;
		this.parent = void 0;
		this.parentContainer = void 0;
	}
	get table() {
		return this.parent;
	}
	get scrollMode() {
		return this.parentContainer.scrollMode;
	}
	get colIndx() {
		return this.parent.cellIndxeToColIndex(this.index);
	}
	get rowIndx() {
		return this.parent.cellIndxeToRowIndex(this.index);
	}
	getContainer() {
		return this.container;
	}
	setContainer(container) {
		if (!container) {
			this.destroyContainer();
			return this;
		}
		if (this.container) this.container.destroy();
		this.container = container;
		this.parentContainer.add(container);
		return this;
	}
	destroyContainer() {
		if (this.container) {
			this.container.destroy();
			this.container = null;
		}
		return this;
	}
	popContainer() {
		if (this.container) {
			var container = this.container;
			this.container = null;
			this.parentContainer.remove(container);
			return container;
		} else return null;
	}
	setXY(x, y) {
		if (this.container) this.parentContainer.setChildLocalPosition(this.container, x, y);
		return this;
	}
	setCellContainerAlign(align) {
		if (typeof align === "string") align = AlignConst_default$1[align];
		this.cellContainerAlign = align;
		return this;
	}
	get deltaHeight() {
		return this._deltaHeight;
	}
	set deltaHeight(deltaHeight) {
		if (deltaHeight == null) deltaHeight = 0;
		var table = this.parent;
		if (this._deltaHeight === 0 && deltaHeight !== 0) table.nonZeroDeltaHeightCount++;
		else if (this._deltaHeight !== 0 && deltaHeight === 0) table.nonZeroDeltaHeightCount--;
		var isTableHeightChanged = this._deltaHeight !== deltaHeight;
		this._deltaHeight = deltaHeight;
		if (isTableHeightChanged) {
			table.resetTotalRowsHeight();
			var eventName = this.scrollMode === 0 ? "cellheightchange" : "cellwidthchange";
			this.parentContainer.emit(eventName, this, this.container, this.parentContainer);
		}
	}
	get deltaWidth() {
		return this.deltaHeight;
	}
	set deltaWidth(deltaWidth) {
		this.deltaHeight = deltaWidth;
	}
	setDeltaHeight(deltaHeight) {
		this.deltaHeight = deltaHeight;
		return this;
	}
	setDeltaWidth(deltaWidth) {
		this.deltaHeight = deltaWidth;
		return this;
	}
	get height() {
		if (this.scrollMode === 0) return this.deltaHeight + this.parent.defaultCellHeight;
		else return this.parent.defaultCellWidth;
	}
	set height(height) {
		if (this.scrollMode === 1) return;
		this.setDeltaHeight(height - this.parent.defaultCellHeight);
	}
	setHeight(height) {
		this.height = height;
		return this;
	}
	get width() {
		if (this.scrollMode === 0) return this.parent.defaultCellWidth;
		else return this.deltaHeight + this.parent.defaultCellHeight;
	}
	set width(width) {
		if (this.scrollMode === 0) return;
		this.setDeltaHeight(width - this.parent.defaultCellHeight);
	}
	setWidth(width) {
		this.width = width;
		return this;
	}
	get scene() {
		return this.parentContainer.scene;
	}
};
Object.assign(Cell.prototype, DataMethods_default$2);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/table/Table.js
var GetValue$106 = __webpack_exports__Utils.Objects.GetValue;
var SpliceOne = __webpack_exports__Utils.Array.SpliceOne;
var DefaultCellSize = 60;
var Table = class {
	constructor(parent, config) {
		this.parent = parent;
		this.cells = [];
		this.cellPool = new pool_default();
		this.resetFromJSON(config);
	}
	resetFromJSON(o) {
		if (o === void 0) o = {};
		this.colCount = void 0;
		this.nonZeroDeltaHeightCount = 0;
		this.resetTotalRowsHeight();
		var cellHeight = o.cellHeight;
		if (cellHeight === void 0) cellHeight = DefaultCellSize;
		var cellWidth = o.cellWidth;
		if (cellWidth === void 0) cellWidth = DefaultCellSize;
		this.setDefaultCellHeight(cellHeight);
		this.setDefaultCellWidth(cellWidth);
		this.initCells(GetValue$106(o, "cellsCount", 0));
		this.setColumnCount(GetValue$106(o, "columns", 1));
		return this;
	}
	destroy(fromScene) {
		this.cellPool.destroy();
		this.cells = void 0;
		this.parent = void 0;
	}
	get defaultCellHeightMode() {
		return this.nonZeroDeltaHeightCount === 0;
	}
	setDefaultCellHeight(height) {
		this.defaultCellHeight = height;
		return this;
	}
	setDefaultCellWidth(width) {
		this.defaultCellWidth = width;
		return this;
	}
	initCells(size) {
		var cells = this.cells;
		cells.length = size;
		for (var i = 0; i < size; i++) cells[i] = null;
		return this;
	}
	insertNewCells(cellIdx, count) {
		var cells = this.cells;
		if (cellIdx === cells.length) {
			var endIdx = cellIdx + count;
			cells.legth = endIdx;
			for (var i = cellIdx; i < endIdx; i++) cells[i] = null;
		} else {
			var newCells = [];
			newCells.length = count;
			for (var i = 0; i < count; i++) newCells[i] = null;
			this.cells.splice(cellIdx, 0, ...newCells);
		}
		this.resetTotalRowsHeight();
		return this;
	}
	removeCells(cellIdx, count) {
		var endIdx = cellIdx + count;
		for (var i = cellIdx; i < endIdx; i++) this.freeCell(i);
		if (endIdx === this.cells.length) this.cells.length = cellIdx;
		else {
			if (count === 1) SpliceOne(this.cells, cellIdx);
			else this.cells.splice(cellIdx, count);
			this.buildCellIndex(cellIdx);
		}
		this.resetTotalRowsHeight();
		return this;
	}
	setColumnCount(columns) {
		this.colCount = columns;
		this.resetTotalRowsHeight();
		var parent = this.parent;
		if (parent.expandCellSize) {
			var cellWidth = (parent.scrollMode === 0 ? parent.width : parent.height) / columns;
			this.setDefaultCellWidth(cellWidth);
		}
		return this;
	}
	get rowCount() {
		return Math.ceil(this.cells.length / this.colCount);
	}
	get cellsCount() {
		return this.cells.length;
	}
	isValidCellIdx(idx) {
		return idx >= 0 && idx < this.cells.length;
	}
	heightToRowIndex(height, roundMode) {
		if (roundMode === void 0) roundMode = 0;
		if (height === 0) return 0;
		if (this.defaultCellHeightMode) {
			var rowIdx = height / this.defaultCellHeight;
			switch (roundMode) {
				case 1:
					rowIdx = Math.ceil(rowIdx);
					break;
				default: rowIdx = Math.floor(rowIdx);
			}
			return rowIdx;
		}
		var rowCount = this.rowCount;
		var remainder = height, isValidIdx, rowHeight, rowIdx = 0;
		while (1) {
			rowHeight = this.getRowHeight(rowIdx);
			remainder -= rowHeight;
			isValidIdx = rowIdx >= 0 && rowIdx < rowCount;
			if (remainder > 0 && isValidIdx) rowIdx += 1;
			else if (remainder === 0) {
				if (roundMode === 2) rowIdx += 1;
				return rowIdx;
			} else {
				if (roundMode === 1) {
					var preRowIdx = rowIdx;
					rowIdx += 1;
					isValidIdx = rowIdx >= 0 && rowIdx < rowCount;
					if (!isValidIdx) rowIdx = preRowIdx;
				}
				return rowIdx;
			}
		}
	}
	widthToColIndex(width, isCeil) {
		if (width === 0) return 0;
		var colIdx = width / this.defaultCellWidth;
		if (isCeil) colIdx = Math.ceil(colIdx);
		else colIdx = Math.floor(colIdx);
		return colIdx;
	}
	colRowToCellIndex(colIdx, rowIdx) {
		if (colIdx < 0 || colIdx >= this.colCount || rowIdx < 0 || rowIdx >= this.rowCount) return null;
		return rowIdx * this.colCount + colIdx;
	}
	rowIndexToHeight(start, end) {
		if (this.defaultCellHeightMode) return (end - start + 1) * this.defaultCellHeight;
		var h, sum = 0;
		for (var i = start; i <= end; i++) {
			h = this.getRowHeight(i);
			sum += h;
		}
		return sum;
	}
	colIndexToWidth(start, end) {
		return (end - start + 1) * this.defaultCellWidth;
	}
	getRowHeight(rowIdx) {
		var cnt = this.colCount;
		if (cnt <= 1) return this.getCellHeight(this.colRowToCellIndex(0, rowIdx));
		var maxHeight = 0, cellHeight;
		for (var i = 0; i < cnt; i++) {
			cellHeight = this.getCellHeight(this.colRowToCellIndex(i, rowIdx));
			if (maxHeight < cellHeight) maxHeight = cellHeight;
		}
		return maxHeight;
	}
	getColWidth(idx) {
		return this.defaultCellWidth;
	}
	getCellHeight(cellIdx) {
		if (!this.isValidCellIdx(cellIdx)) return 0;
		var cellHeight;
		if (this.defaultCellHeightMode) cellHeight = this.defaultCellHeight;
		else {
			var cell = this.getCell(cellIdx, false);
			var deltaHeight = cell ? cell.deltaHeight : 0;
			cellHeight = this.defaultCellHeight + deltaHeight;
		}
		return cellHeight;
	}
	resetTotalRowsHeight() {
		this._totalRowsHeight = null;
	}
	get totalRowsHeight() {
		if (this._totalRowsHeight === null) this._totalRowsHeight = this.rowIndexToHeight(0, this.rowCount - 1);
		return this._totalRowsHeight;
	}
	get totalColumnWidth() {
		return this.colCount * this.defaultCellWidth;
	}
	cellIndxeToColIndex(cellIdx) {
		return cellIdx % this.colCount;
	}
	cellIndxeToRowIndex(cellIdx) {
		return Math.floor(cellIdx / this.colCount);
	}
	getCell(cellIdx, createNewCell) {
		if (!this.isValidCellIdx(cellIdx)) return null;
		if (createNewCell === void 0) createNewCell = true;
		if (this.cells[cellIdx] === null && createNewCell) {
			var cell = this.newCell(cellIdx);
			this.cells[cellIdx] = cell;
		}
		return this.cells[cellIdx];
	}
	newCell(cellIdx) {
		var cell = this.cellPool.pop();
		if (cell === null) cell = new Cell(this);
		else cell.setParent(this);
		cell.index = cellIdx;
		return cell;
	}
	buildCellIndex(startIdx) {
		if (startIdx === void 0) startIdx = 0;
		var cells = this.cells, cell;
		for (var i = startIdx, len = cells.length; i < len; i++) {
			cell = cells[i];
			if (cell) cell.index = i;
		}
		return this;
	}
	getParentContainer() {
		return this.parent;
	}
	freeCell(cell) {
		if (typeof cell === "number") cell = this.cells[cell];
		if (!cell) return this;
		cell.destroy();
		this.cellPool.push(cell);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/methods/SetTableOY.js
var SetTableOY = function(oy) {
	var table = this.table;
	var topTableOY = this.topTableOY;
	var bottomTableOY = this.bottomTableOY;
	var tableOYExceedTop = oy > topTableOY;
	var tableOYExeceedBottom = oy < bottomTableOY;
	if (this.clampTableOXY) {
		if (table.rowCount < table.heightToRowIndex(this.instHeight, 1)) oy = 0;
		else if (tableOYExceedTop) oy = topTableOY;
		else if (tableOYExeceedBottom) oy = bottomTableOY;
	}
	if (this._tableOY !== oy) this._tableOY = oy;
	if (tableOYExceedTop) {
		if (!this.execeedTopState) this.emit("execeedtop", this, oy, topTableOY);
	}
	this.execeedTopState = tableOYExceedTop;
	if (tableOYExeceedBottom) {
		if (!this.execeedBottomState) this.emit("execeedbottom", this, oy, bottomTableOY);
	}
	this.execeedBottomState = tableOYExeceedBottom;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/methods/SetTableOX.js
var SetTableOX = function(ox) {
	var table = this.table;
	var leftTableOX = this.leftTableOX;
	var rightTableOX = this.rightTableOX;
	var tableOXExeceedLeft = ox > this.leftTableOX;
	var tableOXExeceedRight = ox < this.rightTableOX;
	if (this.clampTableOXY) {
		if (table.colCount < table.widthToColIndex(this.instWidth, true)) ox = 0;
		else if (tableOXExeceedLeft) ox = leftTableOX;
		else if (tableOXExeceedRight) ox = rightTableOX;
	}
	if (this._tableOX !== ox) this._tableOX = ox;
	if (tableOXExeceedLeft) {
		if (!this.execeedLeftState) this.emit("execeedleft", this, ox, leftTableOX);
	}
	this.execeedLeftState = tableOXExeceedLeft;
	if (tableOXExeceedRight) {
		if (!this.execeedRightState) this.emit("execeedright", this, ox, rightTableOX);
	}
	this.execeedRightState = tableOXExeceedRight;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/mask/MaskChildren.js
var Intersects = __webpack_exports__Geom.Intersects.RectangleToRectangle;
var Overlaps = __webpack_exports__Geom.Rectangle.Overlaps;
var MaskChildren = function({ maskType, parent, maskGameObject, children, onVisible, onInvisible, scope }) {
	if (!maskGameObject) return;
	var handlers;
	switch (maskType) {
		case "stencil":
		case "layer":
			handlers = VisibilityOnlyHandlers;
			break;
		default: handlers = DefaultHandlers;
	}
	if (children === void 0) children = parent.getAllChildren();
	var hasAnyVisibleCallback = !!onVisible || !!onInvisible;
	var parentBounds = parent.getBounds();
	var child, childBounds, visiblePointsNumber;
	var isChildVisible;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		child = children[i];
		if (child === maskGameObject) continue;
		if (!IsVisible(child)) continue;
		isChildVisible = child.visible;
		if (child.getBounds) {
			childBounds = child.getBounds(childBounds);
			visiblePointsNumber = ContainsPoints(parentBounds, childBounds);
			switch (visiblePointsNumber) {
				case 4:
					handlers.visible(parent, child, maskGameObject);
					break;
				case 0:
					if (Intersects(parentBounds, childBounds) || Overlaps(parentBounds, childBounds)) handlers.partial(parent, child, maskGameObject);
					else handlers.invisible(parent, child, maskGameObject);
					break;
				default: handlers.partial(parent, child, maskGameObject);
			}
		} else handlers.partial(parent, child, maskGameObject);
		if (hasAnyVisibleCallback && child.visible !== isChildVisible) {
			var callback = child.visible ? onVisible : onInvisible;
			if (callback) {
				if (scope) callback.call(scope, child, parent);
				else callback(child, parent);
			}
		}
	}
};
var IsVisible = function(gameObject) {
	if (!gameObject.displayList && !gameObject.parentContainer) return false;
	while (1) {
		var localState = gameObject.rexContainer;
		if (!localState) return gameObject.visible;
		else if (localState.visible) {
			var parent = localState.parent;
			if (parent) {
				gameObject = parent;
				continue;
			} else return true;
		} else return false;
	}
};
var ContainsPoints = function(rectA, rectB) {
	var top = rectB.top, bottom = rectB.bottom, left = rectB.left, right = rectB.right;
	var result = 0;
	result += rectA.contains(left, top) ? 1 : 0;
	result += rectA.contains(left, bottom) ? 1 : 0;
	result += rectA.contains(right, top) ? 1 : 0;
	result += rectA.contains(right, bottom) ? 1 : 0;
	return result;
};
var SetVisible = function(parent, child, maskGameObject) {
	if (!child.hasOwnProperty("isRexContainerLite")) {
		ClearMask(child);
		parent.setChildMaskVisible(child, true);
	} else {
		child.syncChildrenEnable = false;
		parent.setChildMaskVisible(child, true);
		child.syncChildrenEnable = true;
	}
};
var SetPartiallyVisible = function(parent, child, maskGameObject) {
	if (!child.hasOwnProperty("isRexContainerLite")) {
		SetMask(child, maskGameObject);
		parent.setChildMaskVisible(child, true);
	} else {
		child.syncChildrenEnable = false;
		parent.setChildMaskVisible(child, true);
		child.syncChildrenEnable = true;
	}
};
var SetInvisible = function(parent, child, maskGameObject) {
	if (!child.hasOwnProperty("isRexContainerLite")) {
		ClearMask(child);
		parent.setChildMaskVisible(child, false);
	} else {
		child.syncChildrenEnable = false;
		parent.setChildMaskVisible(child, false);
		child.syncChildrenEnable = true;
	}
};
var DefaultHandlers = {
	visible: SetVisible,
	partial: SetPartiallyVisible,
	invisible: SetInvisible
};
var VisibilityOnlyHandlers = {
	visible: SetVisible,
	partial: SetVisible,
	invisible: SetInvisible
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/mask/ChildrenMaskMethods.js
var GetValue$105 = __webpack_exports__Utils.Objects.GetValue;
var MASKUPDATEMODE = {
	update: 0,
	everyTick: 1
};
var ChildrenMaskMethods_default = {
	setupChildrenMask(config) {
		if (config === false) return this;
		var maskType = GetValue$105(config, "maskType");
		if (!maskType) {
			if (this.layerRendererEnable) maskType = "layer";
			else maskType = "children";
		}
		switch (maskType) {
			case "stencil":
				this.setStencilChildrenMaskEanble(true);
				break;
			case "layer": this.enableLayer();
		}
		this.maskType = maskType;
		this.setMaskUpdateMode(GetValue$105(config, "updateMode", 0));
		this.enableChildrenMask(GetValue$105(config, "padding", 0));
		this.onMaskGameObjectVisible = GetValue$105(config, "onVisible");
		this.onMaskGameObjectInvisible = GetValue$105(config, "onInvisible");
		this.maskGameObjectCallbackScope = GetValue$105(config, "scope");
		this.startMaskUpdate();
		return this;
	},
	destroyChildrenMask() {
		if (!this.childrenMaskGameObject) return this;
		this.stopMaskUpdate();
		this.childrenMaskGameObject.destroy();
		this.childrenMaskGameObject = void 0;
		this.onMaskGameObjectVisible = null;
		this.onMaskGameObjectInvisible = null;
		this.maskGameObjectCallbackScope = null;
		return this;
	},
	setMaskUpdateMode(mode) {
		if (typeof mode === "string") mode = MASKUPDATEMODE[mode];
		this.maskUpdateMode = mode;
		return this;
	},
	startMaskUpdate() {
		this.scene.game.events.on("poststep", this.maskChildren, this);
	},
	stopMaskUpdate() {
		this.scene.game.events.off("poststep", this.maskChildren, this);
	},
	enableChildrenMask(maskPadding) {
		this.childrenMaskGameObject = AddChildMask.call(this, null, this, 0, maskPadding);
		return this;
	},
	setMaskChildrenFlag(value) {
		if (value === void 0) value = true;
		this.maskChildrenFlag = value;
		return this;
	},
	maskChildren() {
		if (!this.childrenMaskGameObject || !this.maskChildrenFlag || this.alpha === 0 || !this.visible) return this;
		switch (this.maskType) {
			case "stencil": break;
			case "layer": SetMask(this, this.childrenMaskGameObject);
		}
		MaskChildren({
			maskType: this.maskType,
			parent: this,
			maskGameObject: this.childrenMaskGameObject,
			onVisible: this.onMaskGameObjectVisible,
			onInvisible: this.onMaskGameObjectInvisible,
			scope: this.maskGameObjectCallbackScope
		});
		if (this.maskUpdateMode === 0) this.maskChildrenFlag = false;
		return this;
	},
	layoutChildrenMask() {
		if (!this.childrenMaskGameObject) return this;
		var maskGameObject = this.childrenMaskGameObject;
		maskGameObject.setPosition().resize();
		this.resetChildPositionState(maskGameObject);
		return this;
	},
	setStencilChildrenMaskEanble(enable) {
		if (enable === void 0) enable = true;
		this.useStencilChildrenMask = enable;
		if (enable) this.enableLayer();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/methods/updatetable/ShowCells.js
var ShowCells = function() {
	if (this.cellsCount === 0) return;
	var scaleXSave = this.scaleX;
	var scaleYSave = this.scaleY;
	var scale1 = scaleXSave === 1 && scaleYSave === 1;
	if (!scale1) this.setScale(1);
	var table = this.table;
	var tableOYOffset = this.tableOYOffset;
	var tableOY = this.tableOY + tableOYOffset, tableOX = this.tableOX;
	this.startRowIndex = Math.max(table.heightToRowIndex(-tableOY, 2), 0);
	var rowIndex = this.startRowIndex;
	var startColumnIndex = Math.max(table.widthToColIndex(-tableOX), 0);
	var columnIndex = startColumnIndex;
	var cellIdx = table.colRowToCellIndex(columnIndex, rowIndex);
	var bottomBound = this.bottomBound;
	var rightBound = this.rightBound;
	var lastIdx = table.cellsCount - 1;
	var lastColIdx = table.colCount - 1;
	var startCellTLX = this.getCellTLX(columnIndex), cellTLX = startCellTLX;
	var cellTLY = this.getCellTLY(rowIndex) + tableOYOffset;
	while (cellTLY < bottomBound && cellIdx !== null && cellIdx <= lastIdx) {
		var cell = table.getCell(cellIdx, true);
		this.visibleCells.add(cell);
		if (!this.preVisibleCells.has(cell)) this.showCell(cell);
		var x, y;
		if (this.scrollMode === 0) {
			x = cellTLX;
			y = cellTLY;
		} else {
			x = cellTLY;
			y = cellTLX;
		}
		if (cell.cellContainerAlign == null) cell.setXY(x, y);
		else {
			var cellContainer = cell.getContainer();
			AlignIn(cellContainer, x, y, cell.width, cell.height, cell.cellContainerAlign);
			cell.setXY(cellContainer.x, cellContainer.y);
		}
		if (cellTLX < rightBound && columnIndex < lastColIdx) {
			cellTLX += table.getColWidth(columnIndex);
			columnIndex += 1;
		} else {
			cellTLX = startCellTLX;
			cellTLY += table.getRowHeight(rowIndex);
			columnIndex = startColumnIndex;
			rowIndex += 1;
		}
		cellIdx = table.colRowToCellIndex(columnIndex, rowIndex);
	}
	if (!scale1) this.setScale(scaleXSave, scaleYSave);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/methods/updatetable/ShowCell.js
var ShowCell = function(cell) {
	var reusedCellContainer = null;
	var cellContainer = cell.getContainer();
	if (cellContainer) {
		reusedCellContainer = cellContainer;
		cell.popContainer();
	} else if (this.cellContainersPool) {
		reusedCellContainer = this.cellContainersPool.getFirstDead();
		if (reusedCellContainer !== null) reusedCellContainer.setActive(true).setVisible(true);
	}
	this.emit("cellvisible", cell, reusedCellContainer, this);
	if (this.cellContainersPool) {
		var cellContainer = cell.getContainer();
		if (cellContainer) {
			if (reusedCellContainer === null) this.cellContainersPool.add(cellContainer);
			else if (reusedCellContainer !== cellContainer) {
				this.cellContainersPool.add(cellContainer);
				this.cellContainersPool.killAndHide(reusedCellContainer);
			}
		} else if (reusedCellContainer !== null) this.cellContainersPool.killAndHide(reusedCellContainer);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/methods/updatetable/GetCellTLX.js
var GetCellTLX = function(colIdx) {
	var ox = this.scrollMode === 0 ? this.topLeftX : this.topLeftY;
	return this.tableOX + this.table.colIndexToWidth(0, colIdx - 1) + ox;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/methods/updatetable/GetCellTLY.js
var GetCellTLY = function(rowIdx) {
	var oy = this.scrollMode === 0 ? this.topLeftY : this.topLeftX;
	return this.tableOY + this.table.rowIndexToHeight(0, rowIdx - 1) + oy;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/methods/updatetable/HideCells.js
var HideCells = function() {
	var preList = this.preVisibleCells;
	var curList = this.visibleCells;
	var self = this;
	preList.forEach(function(cell) {
		if (!curList.has(cell)) self.hideCell(cell);
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/methods/updatetable/HideCell.js
var HideCell = function(cell) {
	this.emit("cellinvisible", cell);
	if (this.cellContainersPool) {
		var cellContainer = cell.popContainer();
		if (cellContainer) {
			cellContainer.setScale(1).setAlpha(1);
			this.cellContainersPool.killAndHide(cellContainer);
		}
	}
	cell.destroyContainer();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/methods/updatetable/UpdateTable.js
var UpdateTable = function(refresh, maskChildren) {
	if (refresh === void 0) refresh = false;
	if (maskChildren === void 0) maskChildren = false;
	if (refresh) {
		ClearVisibleCellIndexes.call(this);
		this.hideCells();
	}
	ClearVisibleCellIndexes.call(this);
	this.showCells();
	this.hideCells();
	this.setMaskChildrenFlag();
	if (maskChildren) {
		this.layoutChildrenMask();
		this.maskChildren();
	}
	return this;
};
var ClearVisibleCellIndexes = function() {
	var tmp = this.preVisibleCells;
	this.preVisibleCells = this.visibleCells;
	this.visibleCells = tmp;
	this.visibleCells.clear();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/methods/IsCellVisible.js
var IsCellVisible = function(cellIdx) {
	var cell = this.table.getCell(cellIdx, false);
	return cell && this.visibleCells.has(cell);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/methods/PointToCell.js
var PointToCellIndex = function(x, y) {
	y -= this.y + this.topLeftY;
	x -= this.x + this.topLeftX;
	if (x < 0 || x >= this.displayWidth || y < 0 || y >= this.displayHeight) return null;
	var offsetTableOY = this.tableOY - (this.scrollMode === 0 ? y : x);
	var offsetTableOX = this.tableOX - (this.scrollMode === 0 ? x : y);
	var table = this.table;
	var rowIdx = table.heightToRowIndex(-offsetTableOY, 0);
	var colIdx = table.widthToColIndex(-offsetTableOX);
	var cellIdx = table.colRowToCellIndex(colIdx, rowIdx);
	if (cellIdx === null) return null;
	if (!this.isCellVisible(cellIdx)) return null;
	return cellIdx;
};
var PointToCellContainer = function(x, y) {
	var cellIdx = PointToCellIndex.call(this, x, y);
	if (cellIdx === null) return;
	return this.getCellContainer(cellIdx);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/set/Each.js
var Each = function(set, callback, scope) {
	set = Array.from(set);
	for (var i = 0, cnt = set.length; i < cnt; i++) if (scope) {
		if (callback.call(scope, set[i], i) === false) break;
	} else if (callback(set[i], i) === false) break;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/set/Iterate.js
var Iterate = function(set, callback, scope) {
	var i = 0;
	for (var value of set) {
		if (scope) {
			if (callback.call(scope, value, i) === false) break;
		} else if (callback(value, i) === false) break;
		i++;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/methods/EachCell.js
var EachVisibleCell = function(callback, scope) {
	Each(this.visibleCells, callback, scope);
	return this;
};
var IterateVisibleCell = function(callback, scope) {
	Iterate(this.visibleCells, callback, scope);
	return this;
};
var EachCell = function(callback, scope) {
	this.table.cells.slice().forEach(callback, scope);
	return this;
};
var IterateCell = function(callback, scope) {
	this.table.cells.forEach(callback, scope);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/methods/SetCellsCount.js
var SetCellsCount = function(count) {
	var cellsCount = this.cellsCount;
	if (cellsCount === count) return this;
	if (cellsCount > count) this.removeCells(count, cellsCount - count);
	else this.insertNewCells(cellsCount, count - cellsCount);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/methods/InsertNewCells.js
var Clamp$6 = __webpack_exports__Math.Clamp;
var InsertNewCells = function(cellIdx, count) {
	if (typeof cellIdx === "object") cellIdx = cellIdx.index;
	if (count === void 0) count = 1;
	if (count <= 0) return this;
	cellIdx = Clamp$6(cellIdx, 0, this.cellsCount);
	this.table.insertNewCells(cellIdx, count);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/methods/RemoveCells.js
var RemoveCells = function(cellIdx, count) {
	if (typeof cellIdx === "object") cellIdx = cellIdx.index;
	if (count === void 0) count = 1;
	if (cellIdx < 0) {
		count += cellIdx;
		cellIdx = 0;
	}
	if (count <= 0) return this;
	if (cellIdx > this.cellsCount) return this;
	var cell;
	for (var i = cellIdx, endIdx = cellIdx + count; i < endIdx; i++) {
		cell = this.getCell(i, false);
		if (cell) {
			if (this.visibleCells.has(cell)) {
				HideCell.call(this, cell);
				this.visibleCells.delete(cell);
			}
			this.preVisibleCells.delete(cell);
		}
	}
	this.table.removeCells(cellIdx, count);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/methods/SetColumnCount.js
var SetColumnCount = function(count) {
	if (this.table.colCount === count) return this;
	this.table.setColumnCount(count);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/methods/SetGridSize.js
var SetGridSize = function(colCount, rowCount) {
	this.setCellsCount(colCount * rowCount);
	this.table.setColumnCount(colCount);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/methods/UpdateVisibleCell.js
var UpdateVisibleCell = function(cellIdx) {
	var cell = this.table.getCell(cellIdx, false);
	if (!cell || !cell.container) return this;
	ShowCell.call(this, cell);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/methods/ResetCellSizeFromCell.js
var GetSize = function(gameObject, displayKey, sizeKey) {
	var size = gameObject[displayKey];
	if (size === void 0) size = gameObject[sizeKey];
	if (typeof size !== "number" || !isFinite(size) || size <= 0) return null;
	return Math.ceil(size);
};
var ResetCellSizeFromCell = function(cellIdx) {
	if (cellIdx === void 0) cellIdx = 0;
	if (this._isResetCellSizeFromCell) return this;
	this._isResetCellSizeFromCell = true;
	var cell = this.table.getCell(cellIdx, true);
	if (!cell) {
		this._isResetCellSizeFromCell = false;
		return this;
	}
	this.showCell(cell);
	var cellContainer = cell.getContainer();
	if (!cellContainer) {
		this._isResetCellSizeFromCell = false;
		return this;
	}
	var cellWidth = GetSize(cellContainer, "displayWidth", "width");
	var cellHeight = GetSize(cellContainer, "displayHeight", "height");
	this.hideCell(cell);
	if (cellWidth !== null && cellHeight !== null) this.resetAllCellsSize(cellWidth, cellHeight);
	this._isResetCellSizeFromCell = false;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/methods/Methods.js
var methods$14 = {
	setTableOY: SetTableOY,
	setTableOX: SetTableOX,
	showCells: ShowCells,
	showCell: ShowCell,
	getCellTLX: GetCellTLX,
	getCellTLY: GetCellTLY,
	hideCells: HideCells,
	hideCell: HideCell,
	updateTable: UpdateTable,
	isCellVisible: IsCellVisible,
	pointToCellIndex: PointToCellIndex,
	pointToCellContainer: PointToCellContainer,
	eachVisibleCell: EachVisibleCell,
	iterateVisibleCell: IterateVisibleCell,
	eachCell: EachCell,
	iterateCell: IterateCell,
	setCellsCount: SetCellsCount,
	insertNewCells: InsertNewCells,
	removeCells: RemoveCells,
	setColumnCount: SetColumnCount,
	setGridSize: SetGridSize,
	updateVisibleCell: UpdateVisibleCell,
	resetCellSizeFromCell: ResetCellSizeFromCell
};
Object.assign(methods$14, ChildrenMaskMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/gridtable/GridTable.js
var Group = __webpack_exports__GameObjects.Group;
var GetValue$104 = __webpack_exports__Utils.Objects.GetValue;
var GridTable$1 = class extends ContainerLite {
	constructor(scene, x, y, width, height, config) {
		if (config === void 0) config = {};
		super(scene, x, y, width, height);
		this.type = "rexGridTable";
		this._tableOX = 0;
		this._tableOY = 0;
		this.visibleCells = /* @__PURE__ */ new Set();
		this.preVisibleCells = /* @__PURE__ */ new Set();
		this.execeedTopState = false;
		this.execeedBottomState = false;
		this.execeedLeftState = false;
		this.execeedRightState = false;
		if (GetValue$104(config, "reuseCellContainer", false)) this.cellContainersPool = new Group(scene);
		var callback = GetValue$104(config, "cellVisibleCallback", null);
		if (callback !== null) {
			var scope = GetValue$104(config, "cellVisibleCallbackScope", void 0);
			this.on("cellvisible", callback, scope);
		}
		callback = GetValue$104(config, "cellInvisibleCallback", null);
		if (callback !== null) {
			var scope = GetValue$104(config, "cellInvisibleCallbackScope", void 0);
			this.on("cellinvisible", callback, scope);
		}
		if (GetValue$104(config, "enableLayer", false)) this.enableLayer();
		this.setupChildrenMask(GetValue$104(config, "mask", void 0));
		this.setScrollMode(GetValue$104(config, "scrollMode", 0));
		this.setClampMode(GetValue$104(config, "clampTableOXY", true));
		this.setStartFromBottomEnable(GetValue$104(config, "startFromBottom", false));
		var cellWidth, cellHeight, columns;
		var scrollY = this.scrollMode === 0;
		if (scrollY) {
			cellWidth = config.cellWidth;
			cellHeight = config.cellHeight;
			columns = config.columns;
		} else {
			cellWidth = config.cellHeight;
			cellHeight = config.cellWidth;
			columns = GetValue$104(config, "rows", config.columns);
		}
		this.fixedCellSize = GetValue$104(config, "fixedCellSize", false);
		this.expandCellSize = !this.fixedCellSize && cellWidth === void 0;
		if (!columns) columns = 1;
		if (this.fixedCellSize) columns = Math.max(Math.floor(this.instWidth / cellWidth), 1);
		else if (this.expandCellSize) {
			var width = scrollY ? this.width : this.height;
			cellWidth = width / columns;
		}
		config.cellWidth = cellWidth;
		config.cellHeight = cellHeight;
		config.columns = columns;
		this.table = new Table(this, config);
		this.updateTable();
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		this.destroyChildrenMask();
		this.table.destroy(fromScene);
		this.table = void 0;
		if (this.cellContainersPool) {
			this.cellContainersPool.destroy(true);
			this.cellContainersPool = void 0;
		}
		super.destroy(fromScene);
	}
	setScrollMode(mode) {
		if (typeof mode === "string") mode = SCROLLMODE[mode.toLowerCase()];
		this.scrollMode = mode;
		return this;
	}
	setClampMode(mode) {
		if (mode === void 0) mode = true;
		this.clampTableOXY = mode;
		return this;
	}
	setStartFromBottomEnable(enable) {
		if (enable === void 0) enable = true;
		this.startFromBottomEnable = enable;
		return this;
	}
	get tableOY() {
		return this._tableOY;
	}
	get tableOX() {
		return this._tableOX;
	}
	set tableOY(oy) {
		this.setTableOY(oy).updateTable();
	}
	set tableOX(ox) {
		this.setTableOX(ox).updateTable();
	}
	setTableOXY(ox, oy) {
		this.setTableOY(oy).setTableOX(ox);
		return this;
	}
	addTableOY(dy) {
		this.setTableOY(this.tableOY + dy);
		return this;
	}
	addTableOX(dx) {
		this.setTableOX(this.tableOX + dx);
		return this;
	}
	addTableOXY(dx, dy) {
		this.addTableOY(dy).addTableOX(dx);
		return this;
	}
	setTableOYByPercentage(percentage) {
		this.setTableOY(-this.tableVisibleHeight * percentage);
		return this;
	}
	getTableOYPercentage() {
		var tableVisibleHeight = this.tableVisibleHeight;
		if (tableVisibleHeight === 0) return 0;
		return this.tableOY / -tableVisibleHeight;
	}
	setTableOXByPercentage(percentage) {
		this.setTableOX(-this.tableVisibleWidth * percentage);
		return this;
	}
	getTableOXPercentage() {
		var tableVisibleWidth = this.tableVisibleWidth;
		if (tableVisibleWidth === 0) return 0;
		return this.tableOX / -tableVisibleWidth;
	}
	set t(value) {
		this.setTableOYByPercentage(value).updateTable();
	}
	get t() {
		return this.getTableOYPercentage();
	}
	set s(value) {
		this.setTableOXByPercentage(value).updateTable();
	}
	get s() {
		return this.getTableOXPercentage();
	}
	scrollToBottom() {
		this.t = 1;
		if (this.t === 0) return this;
		do
			this.t = 1;
		while (this.t !== 1);
		return this;
	}
	scrollToRow(rowIndex) {
		this.scrollToBottom();
		var height = this.table.rowIndexToHeight(0, rowIndex - 1);
		this.setTableOY(-height).updateTable();
		return this;
	}
	scrollToNextRow(rowCount) {
		if (rowCount === void 0) rowCount = 1;
		this.scrollToRow(this.startRowIndex + rowCount);
		return this;
	}
	getCell(cellIdx) {
		return this.table.getCell(cellIdx, true);
	}
	getCellContainer(cellIdx) {
		var cell = this.table.getCell(cellIdx, false);
		var container;
		if (cell) container = cell.getContainer();
		return container;
	}
	getAllCellContainers(out) {
		if (out === void 0) out = [];
		this.iterateVisibleCell(function(cell) {
			var cellContainer = cell ? cell.getContainer() : null;
			if (cellContainer) out.push(cellContainer);
		});
		return out;
	}
	get cellsCount() {
		return this.table.cellsCount;
	}
	get columnCount() {
		return this.table.colCount;
	}
	setCellHeight(cellIdx, height) {
		var cell;
		if (typeof cellIdx === "number") cell = this.table.getCell(cellIdx, true);
		else cell = cellIdx;
		cell.height = height;
		return this;
	}
	setCellWidth(cellIdx, width) {
		var cell;
		if (typeof cellIdx === "number") cell = this.table.getCell(cellIdx, true);
		else cell = cellIdx;
		cell.width = width;
		return this;
	}
	resetAllCellsSize(width, height) {
		if (this.scrollMode === 1) {
			var temp = width;
			width = height;
			height = temp;
		}
		this.table.setDefaultCellHeight(height).setDefaultCellWidth(width);
		var cells = this.table.cells;
		for (var i = 0, cnt = cells.length; i < cnt; i++) {
			var cell = cells[i];
			if (!cell) continue;
			cell.deltaHeight = 0;
		}
		if (this.fixedCellSize) {
			var colCount = Math.floor(this.instWidth / width);
			this.table.setColumnCount(colCount);
		}
		this.updateTable(true, true);
		return this;
	}
	get instHeight() {
		return this.scrollMode === 0 ? this.height : this.width;
	}
	get instWidth() {
		return this.scrollMode === 0 ? this.width : this.height;
	}
	get tableHeight() {
		return this.table.totalRowsHeight;
	}
	get tableWidth() {
		return this.table.totalColumnWidth;
	}
	get tableOYOffset() {
		if (this.startFromBottomEnable) {
			var h = this.tableHeight - this.instHeight;
			if (h < 0) return -h;
		}
		return 0;
	}
	get topTableOY() {
		return 0;
	}
	get bottomTableOY() {
		return -this.tableVisibleHeight;
	}
	get leftTableOX() {
		return 0;
	}
	get rightTableOX() {
		return -this.tableVisibleWidth;
	}
	get tableVisibleHeight() {
		var h = this.tableHeight - this.instHeight;
		if (h < 0) h = 0;
		return h;
	}
	get tableVisibleWidth() {
		var w = this.tableWidth - this.instWidth;
		if (w < 0) w = 0;
		return w;
	}
	get topLeftX() {
		return -(this.displayWidth * this.originX);
	}
	get topLeftY() {
		return -(this.displayHeight * this.originY);
	}
	get topRightX() {
		return -(this.displayWidth * this.originX) + this.displayWidth;
	}
	get topRightY() {
		return this.topLeftY;
	}
	get bottomLeftY() {
		return -(this.displayHeight * this.originY) + this.displayHeight;
	}
	get bottomLeftX() {
		return this.topLeftX;
	}
	get bottomRightX() {
		return this.topRightX;
	}
	get bottomRightY() {
		return this.bottomLeftY;
	}
	get bottomBound() {
		if (this.scrollMode === 0) return this.bottomLeftY;
		else return this.topRightX;
	}
	get rightBound() {
		if (this.scrollMode === 0) return this.topRightX;
		else return this.bottomLeftY;
	}
	resize(width, height) {
		if (this.width === width && this.height === height) return this;
		super.resize(width, height);
		if (this.fixedCellSize) {
			var colCount = Math.floor(this.instWidth / this.table.defaultCellWidth);
			this.table.setColumnCount(colCount);
		} else if (this.expandCellSize) this.table.setDefaultCellWidth(this.instWidth / this.table.colCount);
		this.updateTable(true, true);
		return this;
	}
};
Object.assign(GridTable$1.prototype, methods$14);
var SCROLLMODE = {
	v: 0,
	vertical: 0,
	h: 1,
	horizontal: 1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gridtable.js
var gridtable_default = GridTable$1;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridtable/InjectProperties.js
var InjectProperties$1 = function(table) {
	Object.defineProperty(table, "childOY", {
		configurable: true,
		get: function() {
			return table.tableOY;
		},
		set: function(value) {
			table.tableOY = value;
		}
	});
	Object.defineProperty(table, "childOX", {
		configurable: true,
		get: function() {
			return table.tableOX;
		},
		set: function(value) {
			table.tableOX = value;
		}
	});
	Object.defineProperty(table, "topChildOY", { get: function() {
		return table.topTableOY;
	} });
	Object.defineProperty(table, "bottomChildOY", { get: function() {
		return table.bottomTableOY;
	} });
	Object.defineProperty(table, "leftChildOX", { get: function() {
		return table.leftTableOX;
	} });
	Object.defineProperty(table, "rightChildOX", { get: function() {
		return table.rightTableOX;
	} });
	Object.defineProperty(table, "childVisibleHeight", { get: function() {
		return table.instHeight;
	} });
	Object.defineProperty(table, "childHeight", { get: function() {
		return table.tableHeight;
	} });
	Object.defineProperty(table, "childVisibleWidth", { get: function() {
		return table.instWidth;
	} });
	Object.defineProperty(table, "childWidth", { get: function() {
		return table.tableWidth;
	} });
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridtable/TableOnCellVisible.js
var TableOnCellVisible = function(table) {
	table.on("cellvisible", function(cell, cellContainer, table) {
		var callback = this.createCellContainerCallback;
		var scope = this.createCellContainerCallbackScope;
		cell.item = this.items[cell.index];
		cell.items = this.items;
		cell.gridTable = this;
		var cellContainer;
		if (scope) cellContainer = callback.call(scope, cell, cellContainer, this);
		else cellContainer = callback(cell, cellContainer, this);
		if (cellContainer) {
			if (cell.cellContainerAlign == null) {
				if (cellContainer.isRexSizer && !cellContainer.dirty) cellContainer.changeOrigin(0);
				else if (cellContainer.setOrigin) cellContainer.setOrigin(0);
			}
			if (cellContainer.isRexSizer) cellContainer.layout();
		}
		cell.item = void 0;
		cell.items = void 0;
		cell.gridTable = void 0;
		cell.setContainer(cellContainer);
	}, this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridtable/input/PointerToCellIndex.js
var PointerToCellIndex = function(table, pointer, worldX, worldY) {
	var camera = pointer.camera;
	if (!camera) return null;
	if (worldX === void 0) {
		worldX = pointer.worldX;
		worldY = pointer.worldY;
	}
	var x = worldX + camera.scrollX * (table.scrollFactorX - 1);
	var y = worldY + camera.scrollY * (table.scrollFactorY - 1);
	return table.pointToCellIndex(x, y);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridtable/input/EmitCellEvent.js
var EmitCellEvent = function(eventEmitter, eventName, table, worldX, worldY, pointer, event) {
	var cellIndex = null;
	if (worldY === void 0) cellIndex = worldX;
	else cellIndex = PointerToCellIndex(table, pointer, worldX, worldY);
	if (cellIndex === null) return;
	var cellContainer = table.getCellContainer(cellIndex);
	if (cellContainer) eventEmitter.emit(eventName, cellContainer, cellIndex, pointer, event);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridtable/input/PointerUpDownCell.js
var PointerUpDownCell = function(table, tableConfig) {
	table.on("pointerdown", function(pointer, localX, localY, event) {
		EmitCellEvent(this.eventEmitter, "cell.down", table, pointer.worldX, pointer.worldY, pointer, event);
	}, this).on("pointerup", function(pointer, localX, localY, event) {
		EmitCellEvent(this.eventEmitter, "cell.up", table, pointer.worldX, pointer.worldY, pointer, event);
	}, this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridtable/input/OverCell.js
var GetValue$103 = __webpack_exports__Utils.Objects.GetValue;
var OverCell = function(table, tableConfig) {
	var overConfig = GetValue$103(tableConfig, "over", true);
	if (overConfig === false) return;
	if (GetValue$103(overConfig, "mode", void 0) === "boundary") {
		var OnMove = function(pointer, currentlyOver) {
			this.pointerOverTest(pointer);
		};
		table.scene.input.on("pointermove", OnMove, this);
	} else {
		var OnMove = function(pointer, localX, localY, event) {
			this.pointerOverTest(pointer, event);
		};
		var OnOut = function(pointer, event) {
			var table = this.childrenMap.child;
			var cellIndex = table.input.lastOverCellIndex;
			table.input.lastOverCellIndex = void 0;
			EmitCellEvent(this.eventEmitter, "cell.out", table, cellIndex, void 0, pointer, event);
		};
		table.on("pointermove", OnMove, this).on("pointerover", OnMove, this).on("pointerout", OnOut, this);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridtable/input/ClickCell.js
var GetValue$102 = __webpack_exports__Utils.Objects.GetValue;
var ClickCell = function(table, tableConfig) {
	var buttonConfig = GetValue$102(tableConfig, "click", void 0);
	if (buttonConfig === false) return;
	else if (buttonConfig === void 0) buttonConfig = {};
	buttonConfig.threshold = 10;
	table._click = new Button(table, buttonConfig);
	table._click.on("click", function(button, gameObject, pointer, event) {
		EmitCellEvent(this.eventEmitter, "cell.click", gameObject, pointer.worldX, pointer.worldY, pointer, event);
	}, this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridtable/input/TapCell.js
var GetValue$101 = __webpack_exports__Utils.Objects.GetValue;
var TapCell = function(table, tableConfig) {
	var tapConfig = GetValue$101(tableConfig, "tap", void 0);
	if (tapConfig === false) return;
	table._tap = new Tap_default(table, tapConfig);
	table._tap.on("tap", function(tap, gameObject, lastPointer) {
		var eventName = `cell.${tap.tapsCount}tap`;
		EmitCellEvent(this.eventEmitter, eventName, tap.gameObject, tap.worldX, tap.worldY, lastPointer);
	}, this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridtable/input/PressCell.js
var GetValue$100 = __webpack_exports__Utils.Objects.GetValue;
var PressCell = function(table, tableConfig) {
	var pressConfig = GetValue$100(tableConfig, "press", void 0);
	if (pressConfig === false) return;
	table._press = new Press_default(table, pressConfig);
	table._press.on("pressstart", function(press, gameObject, lastPointer) {
		EmitCellEvent(this.eventEmitter, "cell.pressstart", table, press.worldX, press.worldY, lastPointer);
	}, this).on("pressend", function(press, gameObject, lastPointer) {
		EmitCellEvent(this.eventEmitter, "cell.pressend", table, press.worldX, press.worldY, lastPointer);
	}, this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridtable/input/SwipeCell.js
var GetValue$99 = __webpack_exports__Utils.Objects.GetValue;
var SwipeCell = function(table, tableConfig) {
	var swipeConfig = GetValue$99(tableConfig, "swipe", void 0);
	if (swipeConfig === false) return;
	else if (swipeConfig === void 0) swipeConfig = {};
	swipeConfig.dir = "4dir";
	table._swipe = new Swipe_default(table, swipeConfig);
	table._swipe.on("swipe", function(swipe, gameObject, lastPointer) {
		var dirName = swipe.left ? "left" : swipe.right ? "right" : swipe.up ? "up" : "down";
		EmitCellEvent(this.eventEmitter, `cell.swipe${dirName}`, table, swipe.worldX, swipe.worldY, lastPointer);
	}, this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridtable/input/TableSetInteractive.js
var TableSetInteractive = function(table, tableConfig) {
	table.setInteractive();
	PointerUpDownCell.call(this, table, tableConfig);
	OverCell.call(this, table, tableConfig);
	ClickCell.call(this, table, tableConfig);
	TapCell.call(this, table, tableConfig);
	PressCell.call(this, table, tableConfig);
	SwipeCell.call(this, table, tableConfig);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridtable/SetItems.js
var SetItems = function(items, updateTable) {
	if (items === void 0) this.items = [];
	else this.items = items;
	if (updateTable === void 0) updateTable = true;
	var table = this.childrenMap.child;
	table.setCellsCount(this.items.length);
	if (updateTable) {
		table.updateTable(true);
		this.resizeController();
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridtable/Refresh.js
var Refresh = function() {
	this.setItems(this.items);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridtable/ScrollMethods.js
var ScrollMethods_default$1 = {
	scrollToRow(rowIndex, duration, ease) {
		var startChildOY = this.childOY;
		this.childrenMap.child.scrollToRow(rowIndex);
		if (duration === void 0 || duration <= 0) this.updateController();
		else {
			var endChildOY = this.childOY;
			if (this._easeScrollChildOY === void 0) this._easeScrollChildOY = new EaseValueTask(this);
			this._easeScrollChildOY.restart({
				key: "childOY",
				from: startChildOY,
				to: endChildOY,
				duration,
				ease
			});
		}
		return this;
	},
	scrollToNextRow(rowCount, duration, ease) {
		this.childrenMap.child.scrollToNextRow(rowCount, duration, ease);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/input/CreateFakeInputEvent.js
var CreateFakeInputEvent = function() {
	return {
		cancelled: false,
		isSynthetic: true,
		stopPropagation() {
			this.cancelled = true;
		}
	};
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridtable/input/PointerOverTestMethods.js
var PointerOverTestMethods_default = {
	pointerOverTest(pointer, event) {
		var pointers, pointersTotal;
		if (pointer === void 0) {
			pointers = this.scene.input.manager.pointers;
			pointersTotal = pointers.length;
		} else {
			pointers = [pointer];
			pointersTotal = 1;
		}
		var table = this.childrenMap.child;
		var previousIndex = table.input.lastOverCellIndex;
		for (var i = 0; i < pointersTotal; i++) {
			pointer = pointers[i];
			if (!pointer) continue;
			var currentIndex = PointerToCellIndex(table, pointer);
			if (currentIndex === previousIndex) return this;
			if (event === void 0) event = CreateFakeInputEvent();
			table.input.lastOverCellIndex = currentIndex;
			if (previousIndex != null) EmitCellEvent(this.eventEmitter, "cell.out", table, previousIndex, void 0, pointer, event);
			if (currentIndex != null) EmitCellEvent(this.eventEmitter, "cell.over", table, currentIndex, void 0, pointer, event);
			return this;
		}
		return this;
	},
	resetPointerOver() {
		var table = this.childrenMap.child;
		var previousIndex = table.input.lastOverCellIndex;
		table.input.lastOverCellIndex = null;
		if (previousIndex != null) EmitCellEvent(this.eventEmitter, "cell.out", table, previousIndex, void 0);
		this.pointerOverTest();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridtable/GridTable.js
var GetValue$98 = __webpack_exports__Utils.Objects.GetValue;
var GridTable = class extends Scrollable$1 {
	constructor(scene, config) {
		if (config === void 0) config = {};
		var scrollMode = GetScrollMode(config);
		var tableConfig = GetValue$98(config, "table", void 0);
		if (tableConfig === void 0) tableConfig = {};
		tableConfig.scrollMode = scrollMode === 2 ? 0 : scrollMode;
		tableConfig.clampTableOXY = GetValue$98(config, "clampChildOY", false);
		var tableWidth = GetValue$98(tableConfig, "width", void 0);
		var tableHeight = GetValue$98(tableConfig, "height", void 0);
		var table = new gridtable_default(scene, 0, 0, tableWidth, tableHeight, tableConfig);
		scene.add.existing(table);
		var expandWidth, expandHeight;
		if (tableConfig.scrollMode === 0) {
			expandWidth = tableWidth === void 0;
			expandHeight = tableHeight === void 0;
		} else {
			expandWidth = tableHeight === void 0;
			expandHeight = tableWidth === void 0;
		}
		InjectProperties$1(table);
		table._minWidth = tableWidth === void 0 ? 0 : void 0;
		table._minHeight = tableHeight === void 0 ? 0 : void 0;
		config.type = "rexGridTable";
		config.child = {
			gameObject: table,
			expandWidth,
			expandHeight
		};
		var spaceConfig = GetValue$98(config, "space", void 0);
		if (spaceConfig) spaceConfig.child = GetValue$98(spaceConfig, "table", 0);
		super(scene, config);
		this.addChildrenMap("table", table);
		this.addChildrenMap("mask", table.childrenMaskGameObject);
		this.eventEmitter = GetValue$98(config, "eventEmitter", this);
		var callback = GetValue$98(config, "createCellContainerCallback", NOOP);
		var scope = GetValue$98(config, "createCellContainerCallbackScope", void 0);
		this.setCreateCellContainerCallback(callback, scope);
		TableOnCellVisible.call(this, table);
		this.resizeControllerFlag = false;
		var eventName = scrollMode === 0 ? "cellheightchange" : "cellwidthchange";
		table.on(eventName, function() {
			this.resizeControllerFlag = true;
		}, this);
		if (GetValue$98(tableConfig, "interactive", true)) TableSetInteractive.call(this, table, tableConfig);
		this.setItems(GetValue$98(config, "items"), false);
		scene.game.events.on("poststep", this.onPostStep, this);
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		this.scene.game.events.off("poststep", this.onPostStep, this);
		super.destroy(fromScene);
	}
	setCreateCellContainerCallback(callback, scope) {
		this.createCellContainerCallback = callback;
		this.createCellContainerCallbackScope = scope;
		return this;
	}
	refresh() {
		this.setItems(this.items);
		return this;
	}
	getCell(cellIdx) {
		return this.childrenMap.child.getCell(cellIdx);
	}
	getCellContainer(cellIdx) {
		return this.childrenMap.child.getCellContainer(cellIdx);
	}
	getAllCellContainers(out) {
		return this.childrenMap.child.getAllCellContainers(out);
	}
	updateVisibleCell(cellIdx) {
		return this.childrenMap.child.updateVisibleCell(cellIdx);
	}
	resetAllCellsSize(width, height) {
		this.childrenMap.child.resetAllCellsSize(width, height);
		return this;
	}
	resetCellSizeFromCell(cellIndex) {
		this.childrenMap.child.resetCellSizeFromCell(cellIndex);
		return this;
	}
	onPostStep() {
		if (this.resizeControllerFlag) {
			this.resizeController();
			this.resizeControllerFlag = false;
		}
	}
	get startRowIndex() {
		return this.childrenMap.child.startRowIndex;
	}
};
var methods$13 = {
	setItems: SetItems,
	refresh: Refresh
};
Object.assign(GridTable.prototype, ScrollMethods_default$1, PointerOverTestMethods_default, methods$13);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/gridtable/Factory.js
ObjectFactory.register("gridTable", function(config) {
	var gameObject = new GridTable(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.GridTable", GridTable);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/menu/methods/GetEaseConfig.js
var GetEaseConfig = function(easeConfig, menu) {
	if (easeConfig.sameOrientation) easeConfig.orientation = menu.orientation;
	else easeConfig.orientation = menu.orientation === 0 ? 1 : 0;
	return easeConfig;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/menu/methods/SetTransitCallbackMethods.js
var PopUp = function(menu, duration) {
	menu.popUp(GetEaseConfig(menu.root.easeIn, menu));
};
var ScaleDown$1 = function(menu, duration) {
	menu.scaleDown(GetEaseConfig(menu.root.easeOut, menu));
};
var SetTransitCallbackMethods_default = {
	setTransitInCallback(callback) {
		if (callback === void 0) callback = PopUp;
		this.transitInCallback = callback;
		return this;
	},
	setTransitOutCallback(callback) {
		if (callback === void 0) callback = ScaleDown$1;
		this.transitOutCallback = callback;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/time/PostUpdateDelayCall.js
var PostUpdateDelayCall = function(gameObject, delay, callback, scope, args) {
	var scene = GetSceneObject(gameObject);
	return scene.time.delayedCall(delay, function() {
		scene.sys.events.once("postupdate", function() {
			callback.call(scope, args);
		});
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/menu/methods/DelayCallMethods.js
var DelayCallMethods_default = {
	delayCall(delay, callback, scope) {
		this.timer = PostUpdateDelayCall(this, delay, callback, scope);
		return this;
	},
	removeDelayCall() {
		if (this.timer) {
			this.timer.remove(false);
			this.timer = void 0;
		}
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/menu/methods/ExpandSubMenu.js
var ExpandSubMenu = function(parentButton, items) {
	var subMenu = this.childrenMap.subMenu;
	if (subMenu && subMenu.parentButton === parentButton) return this;
	this.collapseSubMenu();
	var orientation;
	if (this.root.toggleOrientation) orientation = this.orientation === 0 ? 1 : 0;
	else orientation = this.orientation;
	var subMenu = new this.constructor(this.scene, {
		items,
		orientation,
		space: this.space,
		createBackgroundCallback: this.root.createBackgroundCallback,
		createBackgroundCallbackScope: this.root.createBackgroundCallbackScope,
		createButtonCallback: this.root.createButtonCallback,
		createButtonCallbackScope: this.root.createButtonCallbackScope,
		easeIn: this.root.easeIn,
		easeOut: this.root.easeOut,
		_rootMenu: this.root,
		_parentMenu: this,
		_parentButton: parentButton
	});
	this.pin(subMenu);
	this.childrenMap.subMenu = subMenu;
	this.root.emit("expand", subMenu, parentButton, this);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/menu/methods/Collapse.js
var Collapse = function() {
	var root = this.root;
	root.emit("collapse", this, this.parentButton, root);
	var duration = root.easeOut.duration;
	root.transitOutCallback(this, duration);
	this.collapseSubMenu();
	this.delayCall(duration, this.destroy, this);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/menu/methods/CollapseSubMenu.js
var CollapseSubMenu = function() {
	var subMenu = this.childrenMap.subMenu;
	if (subMenu === void 0) return this;
	this.childrenMap.subMenu = void 0;
	this.remove(subMenu);
	subMenu.collapse();
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/menu/methods/Methods.js
var Methods$8 = {
	expandSubMenu: ExpandSubMenu,
	collapse: Collapse,
	collapseSubMenu: CollapseSubMenu
};
Object.assign(Methods$8, SetTransitCallbackMethods_default, DelayCallMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/menu/methods/CreateBackground.js
var CreateBackground$1 = function(scene, items, callback, scope) {
	var background;
	if (callback) {
		items.scene = scene;
		if (scope) background = callback.call(scope, items);
		else background = callback(items);
		items.scene = void 0;
	}
	return background;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/menu/methods/CreateButtons.js
var CreateButtons$4 = function(scene, items, callback, scope) {
	var item;
	var buttons = [], button;
	if (items && callback) for (var i = 0, cnt = items.length; i < cnt; i++) {
		item = items[i];
		item.scene = scene;
		if (scope) button = callback.call(scope, item, i, items);
		else button = callback(item, i, items);
		item.scene = void 0;
		buttons.push(button);
	}
	return buttons;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/menu/methods/MenuSetInteractive.js
var MenuSetInteractive = function(menu) {
	menu.on(menu.root.expandEventName, function(button, index) {
		if (this._isPassedEvent) return;
		var childrenKey = this.root.childrenKey;
		var subItems = this.items[index][childrenKey];
		if (subItems) this.expandSubMenu(button, subItems);
	}, menu).on("button.click", function(button, index, pointer, event) {
		if (this !== this.root) {
			this.root._isPassedEvent = true;
			this.root.emit("button.click", button, index, pointer, event);
			this.root._isPassedEvent = false;
		}
	}, menu).on("button.over", function(button, index, pointer, event) {
		if (this !== this.root) {
			this.root._isPassedEvent = true;
			this.root.emit("button.over", button, index, pointer, event);
			this.root._isPassedEvent = false;
		}
	}, menu).on("button.out", function(button, index, pointer, event) {
		if (this !== this.root) {
			this.root._isPassedEvent = true;
			this.root.emit("button.out", button, index, pointer, event);
			this.root._isPassedEvent = false;
		}
	}, menu);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/menu/methods/ParseEaseConfig.js
var ParseEaseConfig = function(menu, easeConfig) {
	if (typeof easeConfig === "number") easeConfig = { duration: easeConfig };
	if (easeConfig.hasOwnProperty("orientation") && easeConfig.orientation !== void 0) easeConfig.sameOrientation = GetOrientationMode_default(easeConfig.orientation) === menu.orientation;
	else easeConfig.sameOrientation = true;
	easeConfig.destroy = false;
	return easeConfig;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/menu/methods/Expand.js
var Expand = function() {
	var root = this.root;
	var duration = root.easeIn.duration;
	root.transitInCallback(this, duration);
	if (this !== this.root) this.delayCall(duration, function() {
		this.root.emit("popup.complete", this);
	}, this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/menu/Menu.js
var GetValue$97 = __webpack_exports__Utils.Objects.GetValue;
var Menu = class extends Buttons$1 {
	constructor(scene, config) {
		if (config === void 0) config = {};
		if (!config.hasOwnProperty("orientation")) config.orientation = 1;
		var rootMenu = config._rootMenu;
		var parentMenu = config._parentMenu;
		var parentButton = config._parentButton;
		var popUp = GetValue$97(config, "popup", true);
		var items = GetValue$97(config, "items", void 0);
		var createBackgroundCallback = GetValue$97(config, "createBackgroundCallback", void 0);
		var createBackgroundCallbackScope = GetValue$97(config, "createBackgroundCallbackScope", void 0);
		config.background = CreateBackground$1(scene, items, createBackgroundCallback, createBackgroundCallbackScope);
		var createButtonCallback = GetValue$97(config, "createButtonCallback", void 0);
		var createButtonCallbackScope = GetValue$97(config, "createButtonCallbackScope", void 0);
		config.buttons = CreateButtons$4(scene, items, createButtonCallback, createButtonCallbackScope);
		super(scene, config);
		this.type = "rexMenu";
		this.items = items;
		this.root = rootMenu === void 0 ? this : rootMenu;
		this.isRoot = this.root === this;
		this.parentMenu = parentMenu;
		this.parentButton = parentButton;
		this.timer = void 0;
		if (this.isRoot) {
			this.isPopUpMode = popUp;
			var bounds = config.bounds;
			if (bounds === void 0) bounds = GetViewport(scene);
			this.bounds = bounds;
			this.subMenuSide = [this.y < bounds.centerY ? SUBMENU_DOWN : SUBMENU_UP, this.x < bounds.centerX ? SUBMENU_RIGHT : SUBMENU_LEFT];
			var subMenuSide = GetValue$97(config, "subMenuSide", void 0);
			if (subMenuSide !== void 0) {
				if (typeof subMenuSide === "string") subMenuSide = SubMenuSideMode[subMenuSide];
				this.subMenuSide[this.orientation] = subMenuSide;
			}
			this.toggleOrientation = GetValue$97(config, "toggleOrientation", false);
			this.expandEventName = GetValue$97(config, "expandEvent", "button.click");
			this.easeIn = ParseEaseConfig(this, GetValue$97(config, "easeIn", 0));
			this.easeOut = ParseEaseConfig(this, GetValue$97(config, "easeOut", 0));
			this.setTransitInCallback(GetValue$97(config, "transitIn"));
			this.setTransitOutCallback(GetValue$97(config, "transitOut"));
			this.createBackgroundCallback = createBackgroundCallback;
			this.createBackgroundCallbackScope = createBackgroundCallbackScope;
			this.createButtonCallback = createButtonCallback;
			this.createButtonCallbackScope = createButtonCallbackScope;
			this.childrenKey = GetValue$97(config, "childrenKey", "children");
			this._isPassedEvent = false;
			this.pointerDownOutsideCollapsing = GetValue$97(config, "pointerDownOutsideCollapsing", true);
			if (this.pointerDownOutsideCollapsing) scene.input.on("pointerdown", this.onPointerDownOutside, this);
		}
		var originX = 0, originY = 0;
		if (!this.root.easeIn.sameOrientation) {
			var easeOrientation = GetEaseConfig(this.root.easeIn, this).orientation;
			var menuOrientation = parentMenu ? parentMenu.orientation : this.orientation;
			var subMenuSide = this.root.subMenuSide[menuOrientation];
			if (easeOrientation === 0 && subMenuSide === SUBMENU_LEFT) originX = 1;
			if (easeOrientation === 1 && subMenuSide === SUBMENU_UP) originY = 1;
		}
		if (popUp) this.setOrigin(originX, originY).layout();
		if (!this.isRoot) {
			this.setScale(this.root.scaleX, this.root.scaleY);
			var subMenuSide = this.root.subMenuSide[parentMenu.orientation];
			switch (subMenuSide) {
				case SUBMENU_LEFT:
					this.alignTop(parentButton.top).alignRight(parentButton.left);
					break;
				case SUBMENU_RIGHT:
					this.alignTop(parentButton.top).alignLeft(parentButton.right);
					break;
				case SUBMENU_UP:
					this.alignLeft(parentButton.left).alignBottom(parentButton.top);
					break;
				case SUBMENU_DOWN: this.alignLeft(parentButton.left).alignTop(parentButton.bottom);
			}
		}
		MenuSetInteractive(this);
		if (popUp) {
			this.pushIntoBounds(this.root.bounds);
			Expand.call(this);
		}
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		if (this.isRoot && this.pointerDownOutsideCollapsing) this.scene.input.off("pointerdown", this.onPointerDownOutside, this);
		super.destroy(fromScene);
		this.removeDelayCall();
	}
	isInTouching(pointer) {
		if (super.isInTouching(pointer)) return true;
		else if (this.childrenMap.subMenu) return this.childrenMap.subMenu.isInTouching(pointer);
		else return false;
	}
	onPointerDownOutside(pointer) {
		if (this.isInTouching(pointer)) return;
		if (this.isPopUpMode) this.collapse();
		else this.collapseSubMenu();
	}
};
var SUBMENU_LEFT = 2;
var SUBMENU_RIGHT = 0;
var SUBMENU_UP = 3;
var SUBMENU_DOWN = 1;
var SubMenuSideMode = {
	up: SUBMENU_UP,
	down: SUBMENU_DOWN,
	left: SUBMENU_LEFT,
	right: SUBMENU_RIGHT
};
Object.assign(Menu.prototype, Methods$8);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/menu/Factory.js
ObjectFactory.register("menu", function(config) {
	var gameObject = new Menu(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.Menu", Menu);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/dropdownlist/methods/listpanel/ConfigurationMethods.js
var methods$12 = {
	setWrapEnable(enable) {
		if (enable === void 0) enable = true;
		this.listWrapEnable = enable;
		return this;
	},
	setCreateButtonCallback(callback) {
		this.listCreateButtonCallback = callback;
		return this;
	},
	setCreateListBackgroundCallback(callback) {
		this.listCreateBackgroundCallback = callback;
		return this;
	},
	setCreateListSliderTrackCallback(callback) {
		this.listCreateSliderTrackCallback = callback;
		return this;
	},
	setCreateListSliderThumbCallback(callback) {
		this.listCreateSliderThumbCallback = callback;
		return this;
	},
	setListSliderAdaptThumbSizeEnable(enable) {
		if (enable === void 0) enable = true;
		this.listSliderAdaptThumbSizeEnable = enable;
		return this;
	},
	setListScrollerConfig(config) {
		if (config === void 0) config = {};
		this.listScrollerConfig = config;
		return this;
	},
	setListMouseWheelScrollerConfig(config) {
		this.listMouseWheelScrollerConfig = config;
		return this;
	},
	setButtonClickCallback(callback) {
		this.listOnButtonClick = callback;
		return this;
	},
	setButtonOverCallback(callback) {
		this.listOnButtonOver = callback;
		return this;
	},
	setButtonOutCallback(callback) {
		this.listOnButtonOut = callback;
		return this;
	},
	setListExpandDirection(direction) {
		if (typeof direction === "string") direction = ListExpandDirections[direction];
		this.listExpandDirection = direction;
		return this;
	},
	setListEaseInDuration(duration) {
		if (duration === void 0) duration = 0;
		this.listEaseInDuration = duration;
		return this;
	},
	setListEaseOutDuration(duration) {
		if (duration === void 0) duration = 0;
		this.listEaseOutDuration = duration;
		return this;
	},
	setListTransitInCallback(callback) {
		this.listTransitInCallback = callback;
		return this;
	},
	settListTransitOutCallback(callback) {
		this.listTransitOutCallback = callback;
		return this;
	},
	setListBounds(bounds) {
		this.listBounds = bounds;
		return this;
	},
	setListWidth(width) {
		this.listWidth = width;
		return this;
	},
	setListHeight(height) {
		this.listHeight = height;
		return this;
	},
	setListSize(width, height) {
		this.setListWidth(width).setListHeight(height);
		return this;
	},
	setListMaxHeight(height) {
		this.listMaxHeight = height;
		return this;
	},
	setListAlignmentMode(mode) {
		this.listAlignMode = mode;
		return this;
	},
	setListAlignmentSide(side) {
		if (side === void 0) side = "";
		this.listAlignSide = side;
		return this;
	},
	setListSpace(space) {
		if (space === void 0) space = {};
		this.listSpace = space;
		return this;
	},
	setListDraggable(enable) {
		if (enable === void 0) enable = true;
		this.listDraggable = enable;
		return this;
	}
};
var ListExpandDirections = {
	down: 0,
	up: 1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/scrollablepanel/scrollableblock/GetChildrenWidth.js
var GetChildrenWidth = function() {
	if (this.rexSizer.hidden) return 0;
	var childWidth;
	var child = this.child, childConfig = child.rexSizer;
	var hasUnknownChildWidth = false;
	if (childConfig.hidden) childWidth = 0;
	else switch (this.scrollMode) {
		case 0:
			childWidth = this.getChildWidth(child);
			if (childWidth === void 0) hasUnknownChildWidth = true;
			break;
		case 1:
			childWidth = 0;
			break;
		default: childWidth = 0;
	}
	if (hasUnknownChildWidth) return;
	return childWidth;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/scrollablepanel/scrollableblock/GetChildrenHeight.js
var GetChildrenHeight = function() {
	if (this.rexSizer.hidden) return 0;
	var childHeight;
	var child = this.child, childConfig = child.rexSizer;
	var hasUnknownChildHeight = false;
	if (childConfig.hidden) childHeight = 0;
	else switch (this.scrollMode) {
		case 0:
			childHeight = 0;
			break;
		case 1:
			childHeight = this.getChildHeight(child);
			if (childHeight === void 0) hasUnknownChildHeight = true;
			break;
		default: childHeight = 0;
	}
	if (hasUnknownChildHeight) return;
	return childHeight;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/scrollablepanel/scrollableblock/GetChildrenSizers.js
var GetChildrenSizers = function(out) {
	if (out === void 0) out = [];
	if (this.child && this.child.isRexSizer) out.push(this.child);
	return out;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/scrollablepanel/scrollableblock/ResetChildPosition.js
var ResetChildPosition = function() {
	var x = this.left;
	var y = this.top;
	switch (this.scrollMode) {
		case 0:
			y += this.childOY;
			break;
		case 1:
			x += this.childOY;
			break;
		default:
			y += this.childOY;
			x += this.childOX;
	}
	this.setChildTopLeftPosition(x, y);
	this.resetChildPositionState(this.child);
	this.setMaskChildrenFlag();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/scrollablepanel/scrollableblock/LayoutChildren.js
var LayoutChildren = function() {
	var child = this.child, childConfig = child.rexSizer;
	var childWidth, childHeight;
	if (!childConfig.hidden) {
		switch (this.scrollMode) {
			case 0:
				childWidth = this.width * this.scaleX;
				break;
			case 1: childHeight = this.height * this.scaleY;
		}
		if (child.isRexSizer) child.runLayout(this, childWidth, childHeight);
		else if (!childConfig.noResize) ResizeGameObject(child, childWidth, childHeight);
		this.resetChildPosition();
		this.layoutChildrenMask();
		this.maskChildren();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/scrollablepanel/scrollableblock/RemoveChildCallback.js
var RemoveChildCallback = function(gameObject, destroyChild) {
	if (destroyChild) return;
	if (gameObject.clearMask) gameObject.clearMask(false);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/scrollablepanel/scrollableblock/Methods.js
var methods$11 = {
	getChildrenWidth: GetChildrenWidth,
	getChildrenHeight: GetChildrenHeight,
	getChildrenSizers: GetChildrenSizers,
	resetChildPosition: ResetChildPosition,
	layoutChildren: LayoutChildren,
	removeChildCallback: RemoveChildCallback
};
Object.assign(methods$11, ChildrenMaskMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/scrollablepanel/scrollableblock/ScrollableBlock.js
var IsPlainObject$14 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$96 = __webpack_exports__Utils.Objects.GetValue;
var ALIGN_LEFTTOP = __webpack_exports__Display.Align.TOP_LEFT;
var ScrollableBlock = class extends Base$3 {
	constructor(scene, x, y, minWidth, minHeight, config) {
		if (IsPlainObject$14(x)) {
			config = x;
			x = GetValue$96(config, "x", 0);
			y = GetValue$96(config, "y", 0);
			minWidth = GetValue$96(config, "width", void 0);
			minHeight = GetValue$96(config, "height", void 0);
		} else if (IsPlainObject$14(minWidth)) {
			config = minWidth;
			minWidth = GetValue$96(config, "width", void 0);
			minHeight = GetValue$96(config, "height", void 0);
		}
		super(scene, x, y, minWidth, minHeight, config);
		this.type = "rexScrollableBlock";
		this.child = void 0;
		this._childOY = 0;
		this._childOX = 0;
		this.execeedTopState = false;
		this.execeedBottomState = false;
		this.execeedLeftState = false;
		this.execeedRightState = false;
		this.setScrollMode(GetValue$96(config, "scrollMode", 0));
		var clampChildOY = GetValue$96(config, "clampChildOY", true);
		var clampChildOX = GetValue$96(config, "clampChildOX", clampChildOY);
		this.setClampMode(clampChildOY, clampChildOX);
		var child = GetValue$96(config, "child", void 0);
		var expand = GetValue$96(config, "expand", true);
		if (GetValue$96(config, "childOrigin0", true) && child.setOrigin) child.setOrigin(0);
		this.add(child);
		this.sizerChildren = [child];
		var sizerConfig = this.getSizerConfig(child);
		sizerConfig.align = ALIGN_LEFTTOP;
		sizerConfig.expand = expand;
		this.child = child;
		if (GetValue$96(config, "enableLayer", false)) this.enableLayer();
		this.setupChildrenMask(GetValue$96(config, "mask", void 0));
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		this.destroyChildrenMask();
		this.child = void 0;
		super.destroy(fromScene);
	}
	setScrollMode(mode) {
		if (typeof mode === "string") mode = ScrollModeConst_default[mode.toLowerCase()];
		this.scrollMode = mode;
		return this;
	}
	setClampMode(clampChildOY, clampChildOX) {
		this.clampChildOY = clampChildOY;
		this.clampChildOX = clampChildOX;
		return this;
	}
	get instHeight() {
		if (this.scrollMode === 0 || this.scrollMode === 2) return this.displayHeight;
		else return this.displayWidth;
	}
	get instWidth() {
		if (this.scrollMode === 0 || this.scrollMode === 2) return this.displayWidth;
		else return this.displayHeight;
	}
	get childHeight() {
		if (this.scrollMode === 0 || this.scrollMode === 2) return GetDisplayHeight(this.child);
		else return GetDisplayWidth(this.child);
	}
	get childWidth() {
		if (this.scrollMode === 0 || this.scrollMode === 2) return GetDisplayWidth(this.child);
		else return GetDisplayHeight(this.child);
	}
	get topChildOY() {
		return 0;
	}
	get bottomChildOY() {
		return -this.visibleHeight;
	}
	get leftChildOX() {
		return 0;
	}
	get rightChildOX() {
		return -this.visibleWidth;
	}
	get childVisibleHeight() {
		return this.instHeight;
	}
	get childVisibleWidth() {
		return this.instWidth;
	}
	get visibleHeight() {
		var h = this.childHeight - this.childVisibleHeight;
		if (h < 0) h = 0;
		return h;
	}
	get visibleWidth() {
		var w = this.childWidth - this.childVisibleWidth;
		if (w < 0) w = 0;
		return w;
	}
	childOYExceedTop(oy) {
		if (oy === void 0) oy = this.childOY;
		return oy > this.topChildOY;
	}
	childOYExeceedBottom(oy) {
		if (oy === void 0) oy = this.childOY;
		return oy < this.bottomChildOY;
	}
	childOXExceedLeft(ox) {
		if (ox === void 0) ox = this.childOX;
		return ox > this.leftChildOX;
	}
	childOXExeceedRight(ox) {
		if (ox === void 0) ox = this.childOX;
		return ox < this.rightChildOX;
	}
	get childOY() {
		return this._childOY;
	}
	set childOY(oy) {
		var topChildOY = this.topChildOY;
		var bottomChildOY = this.bottomChildOY;
		var childOYExceedTop = this.childOYExceedTop(oy);
		var childOYExeceedBottom = this.childOYExeceedBottom(oy);
		if (this.clampChildOY) {
			if (this.childVisibleHeight > this.childHeight) oy = 0;
			else if (childOYExceedTop) oy = topChildOY;
			else if (childOYExeceedBottom) oy = bottomChildOY;
		}
		if (this._childOY !== oy) {
			this._childOY = oy;
			this.resetChildPosition();
		}
		if (childOYExceedTop) {
			if (!this.execeedTopState) this.emit("execeedtop", this, oy, topChildOY);
		}
		this.execeedTopState = childOYExceedTop;
		if (childOYExeceedBottom) {
			if (!this.execeedBottomState) this.emit("execeedbottom", this, oy, bottomChildOY);
		}
		this.execeedBottomState = childOYExeceedBottom;
	}
	get childOX() {
		return this._childOX;
	}
	set childOX(ox) {
		var leftChildOX = this.leftChildOX;
		var rightChildOX = this.rightChildOX;
		var childOXExceedLeft = this.childOXExceedLeft(ox);
		var childOXExeceedRight = this.childOXExeceedRight(ox);
		if (this.clampChildOX) {
			if (this.childVisibleWidth > this.childWidth) ox = 0;
			else if (childOXExceedLeft) ox = leftChildOX;
			else if (childOXExeceedRight) ox = rightChildOX;
		}
		if (this._childOX !== ox) {
			this._childOX = ox;
			this.resetChildPosition();
		}
		if (childOXExceedLeft) {
			if (!this.execeedLeftState) this.emit("execeedleft", this, ox, leftChildOX);
		}
		this.execeedLeftState = childOXExceedLeft;
		if (childOXExeceedRight) {
			if (!this.execeedRightState) this.emit("execeedright", this, ox, rightChildOX);
		}
		this.execeedRightState = childOXExeceedRight;
	}
	setChildOY(oy) {
		this.childOY = oy;
		return this;
	}
	setChildOX(ox) {
		this.childOX = ox;
		return this;
	}
	set t(value) {
		this.childOY = -this.visibleHeight * value;
	}
	get t() {
		var visibleHeight = this.visibleHeight;
		if (visibleHeight === 0) return 0;
		return this.childOY / -visibleHeight;
	}
	set s(value) {
		this.childOX = -this.visibleWidth * value;
	}
	get s() {
		var visibleWidth = this.visibleWidth;
		if (visibleWidth === 0) return 0;
		return this.childOX / -visibleWidth;
	}
	setChildOYByPercentage(percentage) {
		this.t = percentage;
		return this;
	}
	setChildOXByPercentage(percentage) {
		this.s = percentage;
		return this;
	}
	setChildTopLeftPosition(x, y) {
		var child = this.child;
		x += GetDisplayWidth(child) * child.originX;
		y += GetDisplayHeight(child) * child.originY;
		child.setPosition(x, y);
		return this;
	}
};
Object.assign(ScrollableBlock.prototype, methods$11);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/scrollablepanel/methods/ScrollToChild.js
var ScrollToChild = function(child, align, duration, ease) {
	if (!this.hasChild(child)) return this;
	var newChildOY, newChildOX;
	switch (this.scrollMode) {
		case 0:
			newChildOY = this.childOY + AlignChild.call(this, child, "y", align);
			break;
		case 1:
			newChildOY = this.childOY + AlignChild.call(this, child, "x", align);
			break;
		default:
			newChildOY = this.childOY + AlignChild.call(this, child, "y", align);
			newChildOX = this.childOX + AlignChild.call(this, child, "x", align);
	}
	if (duration === void 0 || duration <= 0) {
		this.childOY = newChildOY;
		if (this.scrollMode === 2) this.childOX = newChildOX;
	} else {
		if (this._easeScrollChildOY === void 0) this._easeScrollChildOY = new EaseValueTask(this);
		this._easeScrollChildOY.restart({
			key: "childOY",
			to: newChildOY,
			duration,
			ease
		});
		if (this.scrollMode === 2) {
			if (this._easeScrollChildOX === void 0) this._easeScrollChildOX = new EaseValueTask(this);
			this._easeScrollChildOX.restart({
				key: "childOX",
				to: newChildOX,
				duration,
				ease
			});
		}
	}
	return this;
};
var AlignYModes = [
	"top",
	"bottom",
	"centerY",
	"center"
];
var AlignXModes = [
	"left",
	"right",
	"centerX",
	"center"
];
var AlignChild = function(child, axis, align) {
	axis = axis.toUpperCase();
	var isAxisY = axis === "Y";
	var scrollableBlock = this.childrenMap.child;
	var delta;
	if (isAxisY) {
		if (align) for (var i = 0, cnt = AlignYModes.length; i < cnt; i++) {
			var modeName = AlignYModes[i];
			if (align.indexOf(modeName) !== -1) {
				align = modeName;
				break;
			}
		}
		switch (align) {
			case "top":
				delta = scrollableBlock.top - GetTopLeft(child).y;
				break;
			case "bottom":
				delta = scrollableBlock.bottom - GetBottomLeft(child).y;
				break;
			case "centerY":
			case "center":
				delta = scrollableBlock.centerY - GetCenter(child).y;
				break;
			default:
				var dTop = scrollableBlock.top - GetTopLeft(child).y;
				var dBottom = scrollableBlock.bottom - GetBottomLeft(child).y;
				if (dTop <= 0 && dBottom >= 0) delta = 0;
				else delta = Math.abs(dTop) <= Math.abs(dBottom) ? dTop : dBottom;
		}
	} else {
		if (align) for (var i = 0, cnt = AlignXModes.length; i < cnt; i++) {
			var modeName = AlignXModes[i];
			if (align.indexOf(modeName) !== -1) {
				align = modeName;
				break;
			}
		}
		switch (align) {
			case "left":
				delta = scrollableBlock.left - GetTopLeft(child).x;
				break;
			case "right":
				delta = scrollableBlock.right - GetTopRight(child).x;
				break;
			case "centerX":
			case "center":
				delta = scrollableBlock.centerX - GetCenter(child).x;
				break;
			default:
				var dLeft = scrollableBlock.left - GetTopLeft(child).x;
				var dRight = scrollableBlock.right - GetTopRight(child).x;
				if (dLeft <= 0 && dRight >= 0) delta = 0;
				else delta = Math.abs(dLeft) <= Math.abs(dRight) ? dLeft : dRight;
		}
	}
	return delta;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/scrollablepanel/ScrollablePanel.js
var GetValue$95 = __webpack_exports__Utils.Objects.GetValue;
var ScrollablePanel = class extends Scrollable$1 {
	constructor(scene, config) {
		if (config === void 0) config = {};
		var scrollMode = GetScrollMode(config);
		var panelConfig = GetValue$95(config, "panel", void 0);
		if (panelConfig === void 0) panelConfig = {};
		panelConfig.scrollMode = scrollMode;
		panelConfig.clampChildOY = GetValue$95(config, "clampChildOY", false);
		panelConfig.clampChildOX = GetValue$95(config, "clampChildOX", false);
		var scrollableBlock = new ScrollableBlock(scene, panelConfig);
		scene.add.existing(scrollableBlock);
		var expandPanelWidth, expandPanelHeight;
		switch (scrollMode) {
			case 0:
				expandPanelWidth = GetValue$95(config, "expand.panel", true);
				expandPanelHeight = true;
				break;
			case 1:
				expandPanelWidth = true;
				expandPanelHeight = GetValue$95(config, "expand.panel", true);
				break;
			default:
				expandPanelWidth = true;
				expandPanelHeight = true;
		}
		config.type = "rexScrollablePanel";
		config.child = {
			gameObject: scrollableBlock,
			expandWidth: expandPanelWidth,
			expandHeight: expandPanelHeight,
			align: GetValue$95(config, "align.panel", "center")
		};
		var spaceConfig = GetValue$95(config, "space", void 0);
		if (spaceConfig) spaceConfig.child = GetValue$95(spaceConfig, "panel", 0);
		super(scene, config);
		this.addChildrenMap("panel", scrollableBlock.child);
		this.addChildrenMap("panelLayer", scrollableBlock.maskLayer);
		this.addChildrenMap("mask", scrollableBlock.childrenMaskGameObject);
		this.addChildrenMap("scrollableBlock", scrollableBlock);
	}
	setChildrenInteractive(config) {
		if (config === void 0) config = {};
		if (!config.hasOwnProperty("eventEmitter")) config.eventEmitter = this;
		if (!config.hasOwnProperty("targets")) config.targets = [this.childrenMap.panel];
		SetChildrenInteractive$1(this.childrenMap.child, config);
		return this;
	}
};
var methods$10 = { scrollToChild: ScrollToChild };
Object.assign(ScrollablePanel.prototype, methods$10);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/dropdownlist/methods/listpanel/CreateListPanel.js
var GetValue$94 = __webpack_exports__Utils.Objects.GetValue;
var CreateListPanel = function() {
	var scene = this.scene;
	var background;
	var createBackgroundCallback = this.listCreateBackgroundCallback;
	if (createBackgroundCallback) {
		background = createBackgroundCallback.call(this, scene);
		scene.add.existing(background);
	}
	var buttons = [];
	var createButtonCallback = this.listCreateButtonCallback;
	if (createButtonCallback) {
		var options = this.options;
		for (var i = 0, cnt = options.length; i < cnt; i++) {
			var button = createButtonCallback.call(this, scene, options[i], i, options);
			if (!button) continue;
			scene.add.existing(button);
			buttons.push(button);
		}
	}
	var width = this.listWidth;
	if (width === void 0) {
		if (this.listAlignMode === "text") width = this.getElement("text").width;
		else width = this.width;
	}
	var height = this.listHeight;
	var buttonConfig = {
		width,
		buttons,
		space: this.listSpace
	};
	var buttons, listPanel;
	var isScrollable;
	if (this.listCreateSliderThumbCallback) isScrollable = height > 0 || this.listMaxHeight > 0;
	else isScrollable = false;
	if (!isScrollable) {
		buttonConfig.height = height;
		buttonConfig.background = background;
		buttons = CreateButtons$3(scene, buttonConfig, this.listWrapEnable);
		listPanel = buttons;
	} else {
		var buttons = CreateButtons$3(scene, buttonConfig, this.listWrapEnable);
		if (this.listMaxHeight > 0) {
			buttons.layout();
			if (buttons.height <= this.listMaxHeight) listPanel = buttons;
		}
		if (!listPanel) {
			if (height === 0) height = this.listMaxHeight;
			var track = CreateGameObject(scene, this.listCreateSliderTrackCallback);
			var thumb = CreateGameObject(scene, this.listCreateSliderThumbCallback);
			listPanel = new ScrollablePanel(scene, {
				height,
				scrollMode: 0,
				background,
				panel: {
					child: buttons,
					mask: { padding: 1 }
				},
				slider: {
					track,
					thumb,
					adaptThumbSize: this.listSliderAdaptThumbSizeEnable
				},
				scrollDetectionMode: 1,
				scroller: this.listScrollerConfig,
				mouseWheelScroller: this.listMouseWheelScrollerConfig,
				space: { panel: GetValue$94(this.listSpace, "panel", 0) }
			});
			scene.add.existing(listPanel);
		}
	}
	if (this.listDraggable) listPanel.setDraggable(true);
	if (listPanel !== buttons) buttons.on("button.over", function(button, index, pointer, event) {
		listPanel.emit("button.over", button, index, pointer, event);
	}).on("button.out", function(button, index, pointer, event) {
		listPanel.emit("button.out", button, index, pointer, event);
	}).on("button.click", function(button, index, pointer, event) {
		listPanel.emit("button.click", button, index, pointer, event);
	});
	return listPanel;
};
var CreateButtons$3 = function(scene, config, isWrapEnable) {
	var gameObject;
	if (!isWrapEnable) {
		config.orientation = "y";
		gameObject = new Buttons$1(scene, config);
	} else {
		config.orientation = "x";
		gameObject = new Buttons(scene, config);
	}
	scene.add.existing(gameObject);
	return gameObject;
};
var CreateGameObject = function(scene, callback, scope) {
	var gameObject;
	if (callback) {
		gameObject = callback.call(scope, scene);
		scene.add.existing(gameObject);
	}
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/scale/ScaleDown.js
var ScaleDown = function(gameObject, duration, orientation, ease, scale) {
	if (ease === void 0) ease = "Linear";
	var config = {};
	config.mode = 0;
	switch (orientation) {
		case 0:
		case "x":
			config.end = { x: 0 };
			break;
		case 1:
		case "y":
			config.end = { y: 0 };
			break;
		default: config.end = 0;
	}
	config.duration = duration;
	config.ease = ease;
	if (scale === void 0) scale = new Scale(gameObject, config);
	else scale.resetFromJSON(config);
	scale.restart();
	return scale;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/object/GetValueFromAliasKeys.js
var GetValueFromAliasKeys = function(source, key0, key1, key2, defaultValue) {
	if (HasValue(source, key0)) return GetValue$279(source, key0);
	else if (key1 && HasValue(source, key1)) return GetValue$279(source, key1);
	else if (key2 && HasValue(source, key2)) return GetValue$279(source, key2);
	else return defaultValue;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/dropdown/SetPosition.js
var GetValue$93 = __webpack_exports__Utils.Objects.GetValue;
var SetPosition = function(gameObject, config) {
	var expandDirection = GetValue$93(config, "expandDirection", void 0);
	if (typeof expandDirection === "string") expandDirection = ExpandDirections[expandDirection];
	var alignTargetX = GetValueFromAliasKeys(config, "alignTarget", "alignTargetX");
	var alignTargetY = GetValue$93(config, "alignTargetY", alignTargetX);
	var alignOffsetX = GetValue$93(config, "alignOffsetX", 0);
	var alignOffsetY = GetValue$93(config, "alignOffsetY", 0);
	var alignRight = GetValue$93(config, "alignSide", "").includes("right");
	var positionBounds = GetValue$93(config, "bounds");
	var isExpandDown = expandDirection === 0;
	var flexExpand = !isExpandDown && !(expandDirection === 1);
	var originX = alignRight ? 1 : 0;
	var originY = isExpandDown || flexExpand ? 0 : 1;
	gameObject.setOrigin(originX, originY);
	var x, y;
	if (alignRight) x = alignTargetX.getTopRight().x;
	else x = alignTargetX.getTopLeft().x;
	y = alignTargetY.getBottomLeft().y;
	gameObject.setPosition(x + alignOffsetX, y + alignOffsetY);
	var bounds = positionBounds;
	if (!bounds) bounds = GetViewport(gameObject.scene);
	if (flexExpand && gameObject.getBottomLeft().y > bounds.bottom) {
		y = alignTargetY.getTopLeft().y;
		gameObject.setOrigin(0, 1).setPosition(x + alignOffsetX, y + alignOffsetY);
	}
};
var ExpandDirections = {
	down: 0,
	up: 1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/dropdown/DropDown.js
var GetValue$92 = __webpack_exports__Utils.Objects.GetValue;
var DropDown = class extends OpenCloseTransition {
	constructor(gameObject, config) {
		if (config === void 0) config = {};
		if (config.transitIn == null) config.transitIn = function(gameObject, duration) {
			popup_default(gameObject, duration, "y", "Cubic");
		};
		if (config.transitOut == null) config.transitOut = function(gameObject, duration) {
			ScaleDown(gameObject, duration, "y", "Linear");
		};
		config.manualClose = true;
		config.clickOutsideClose = true;
		config.destroy = true;
		super(gameObject, config);
		SetPosition(gameObject, config);
		if (gameObject.isRexSizer) gameObject.layout();
		var touchOutsideClose = GetValue$92(config, "touchOutsideClose", false);
		var anyTouchClose = GetValue$92(config, "anyTouchClose", false);
		if (anyTouchClose) touchOutsideClose = false;
		if (anyTouchClose) this.once("open", this.anyTouchClose, this);
		else if (touchOutsideClose) this.once("open", this.touchOutsideClose, this);
		this.requestOpen();
	}
	shutdown(fromScene) {
		if (this.isShutdown) return;
		this.scene.input.off("pointerup", this.touchCloseCallback, this);
		super.shutdown(fromScene);
	}
	touchOutsideClose() {
		this.scene.input.on("pointerup", this.touchCloseCallback, this);
		this.clickOutsideTest = true;
		return this;
	}
	anyTouchClose() {
		this.scene.input.once("pointerup", this.touchCloseCallback, this);
		return this;
	}
	touchCloseCallback(pointer) {
		if (this.clickOutsideTest && IsPointInBounds(this.parent, pointer.worldX, pointer.worldY)) return;
		this.requestClose();
	}
	onOpen() {
		this.emit("open", this.parent, this);
		super.onOpen();
	}
	onClose() {
		this.emit("close", this.parent, this);
		super.onClose();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/dropdown/DropDown.js
var DropDown_default = DropDown;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/dropdownlist/methods/listpanel/OpenListPanel.js
var OpenListPanel = function() {
	if (this.listPanel) return this;
	if (this.options.length === 0) return this;
	var listPanel = CreateListPanel.call(this);
	listPanel.on("button.over", function(button, index, pointer, event) {
		this.currentOverIndex = index;
		if (this.listOnButtonOver) this.listOnButtonOver.call(this, button, index, pointer, event);
		this.emit("button.over", this, listPanel, button, index, pointer, event);
	}, this).on("button.out", function(button, index, pointer, event) {
		if (this.currentOverIndex === index) this.currentOverIndex = void 0;
		if (this.listOnButtonOut) this.listOnButtonOut.call(this, button, index, pointer, event);
		this.emit("button.out", this, listPanel, button, index, pointer, event);
	}, this);
	var alignTargetX;
	if (!this.listAlignMode || this.listAlignMode === "label") alignTargetX = this;
	else alignTargetX = this.getElement(this.listAlignMode);
	var dropDownBehavior = new DropDown_default(listPanel, {
		duration: {
			in: this.listEaseInDuration,
			out: this.listEaseOutDuration
		},
		transitIn: this.listTransitInCallback,
		transitOut: this.listTransitOutCallback,
		expandDirection: this.listExpandDirection,
		alignTargetX,
		alignTargetY: this,
		alignSide: this.listAlignSide,
		bounds: this.listBounds
	}).on("open", function() {
		listPanel.on("button.click", function(button, index, pointer, event) {
			if (this.listOnButtonClick) this.listOnButtonClick.call(this, button, index, pointer, event);
			this.emit("button.click", this, listPanel, button, index, pointer, event);
			this.dropDownBehavior.requestClose();
		}, this);
		this.emit("list.open", this, listPanel);
	}, this).on("close", function() {
		this.listPanel = void 0;
		this.dropDownBehavior = void 0;
		this.emit("list.close", this);
	}, this);
	listPanel.onClickOutside(function() {
		dropDownBehavior.requestClose();
	});
	this.listPanel = listPanel;
	this.dropDownBehavior = dropDownBehavior;
	this.pin(listPanel);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/dropdownlist/methods/listpanel/CloseListPanel.js
var CloseListPanel = function() {
	if (!this.dropDownBehavior) return this;
	this.dropDownBehavior.requestClose();
	this.currentOverIndex = void 0;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/dropdownlist/methods/listpanel/ToggleListPanel.js
var ToggleListPanel = function() {
	if (!this.listPanel) this.openListPanel();
	else this.closeListPanel();
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/dropdownlist/methods/EmitButtonClick.js
var EmitListButtonClick = function(index) {
	if (index === void 0) index = this.currentOverIndex;
	if (index === void 0) return this;
	var listPanel = this.listPanel;
	var button = listPanel ? listPanel.getButton(index) : this.options[index];
	if (this.listOnButtonClick) this.listOnButtonClick.call(this, button, index);
	this.emit("button.click", this, listPanel, button, index);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/dropdownlist/methods/EmitButtonOver.js
var EmitButtonOver = function(index) {
	var listPanel = this.listPanel;
	if (!listPanel) return this;
	listPanel.emitButtonOver(index);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/dropdownlist/methods/FocusButtonMethods.js
var FocusButtonMethods_default = {
	focusNextButton() {
		if (!this.isOpened) return this;
		var currentIndex = this.currentOverIndex;
		var nextIndex;
		if (currentIndex === void 0) nextIndex = 0;
		else {
			var total = this.listPanel.getButtons().length;
			nextIndex = (currentIndex + 1) % total;
		}
		this.emitButtonOver(nextIndex);
		return this;
	},
	focusPrevButton() {
		if (!this.isOpened) return this;
		var currentIndex = this.currentOverIndex;
		var nextIndex;
		if (currentIndex === void 0) nextIndex = 0;
		else {
			var total = this.listPanel.getButtons().length;
			nextIndex = (currentIndex - 1 + total) % total;
		}
		this.emitButtonOver(nextIndex);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/dropdownlist/methods/Methods.js
var Methods$7 = {
	openListPanel: OpenListPanel,
	closeListPanel: CloseListPanel,
	toggleListPanel: ToggleListPanel,
	emitButtonClick: EmitListButtonClick,
	emitButtonOver: EmitButtonOver
};
Object.assign(Methods$7, methods$12, FocusButtonMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/dropdownlist/DropDownList.js
var GetValue$91 = __webpack_exports__Utils.Objects.GetValue;
var DropDownList = class extends Label {
	constructor(scene, config) {
		super(scene, config);
		this.type = "rexDropDownList";
		this.timer = void 0;
		this.listPanel = void 0;
		this.currentOverIndex = void 0;
		this.setOptions(GetValue$91(config, "options"));
		var listConfig = GetValue$91(config, "list");
		this.setWrapEnable(GetValue$91(listConfig, "wrap", false));
		this.setCreateButtonCallback(GetValue$91(listConfig, "createButtonCallback"));
		this.setCreateListBackgroundCallback(GetValue$91(listConfig, "createBackgroundCallback"));
		this.setCreateListSliderTrackCallback(GetValue$91(listConfig, "createTrackCallback"));
		this.setCreateListSliderThumbCallback(GetValue$91(listConfig, "createThumbCallback"));
		this.setListSliderAdaptThumbSizeEnable(GetValue$91(listConfig, "sliderAdaptThumbSize", false));
		this.setListScrollerConfig(GetValue$91(listConfig, "scroller"));
		this.setListMouseWheelScrollerConfig(GetValue$91(listConfig, "mouseWheelScroller"));
		this.setButtonClickCallback(GetValue$91(listConfig, "onButtonClick"));
		this.setButtonOverCallback(GetValue$91(listConfig, "onButtonOver"));
		this.setButtonOutCallback(GetValue$91(listConfig, "onButtonOut"));
		this.setListExpandDirection(GetValue$91(listConfig, "expandDirection"));
		this.setListEaseInDuration(GetValue$91(listConfig, "easeIn", 500));
		this.setListEaseOutDuration(GetValue$91(listConfig, "easeOut", 100));
		this.setListTransitInCallback(GetValue$91(listConfig, "transitIn"));
		this.settListTransitOutCallback(GetValue$91(listConfig, "transitOut"));
		this.setListMaxHeight(GetValue$91(listConfig, "maxHeight", 0));
		this.setListSize(GetValue$91(listConfig, "width"), GetValue$91(listConfig, "height", 0));
		this.setListAlignmentMode(GetValue$91(listConfig, "alignParent", "text"));
		this.setListAlignmentSide(GetValue$91(listConfig, "alignSide", ""));
		this.setListBounds(GetValue$91(listConfig, "bounds"));
		this.setListSpace(GetValue$91(listConfig, "space"));
		this.setListDraggable(GetValue$91(listConfig, "draggable", false));
		this.setValueChangeCallback(GetValue$91(config, "setValueCallback"), GetValue$91(config, "setValueCallbackScope"));
		this.setValue(GetValue$91(config, "value"));
		this.onClick(this.toggleListPanel, this);
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		if (this.listPanel) {
			this.listPanel.destroy(fromScene);
			this.listPanel = void 0;
		}
		super.destroy(fromScene);
	}
	get isOpened() {
		return !!this.listPanel;
	}
	setOptions(options) {
		if (options === void 0) options = [];
		this.options = options;
		return this;
	}
	setValueChangeCallback(callback, scope) {
		this.valueChangeCallback = callback;
		this.valueChangeCallbackScope = scope;
		return this;
	}
	setValue(value) {
		this.value = value;
		return this;
	}
	get value() {
		return this._value;
	}
	set value(value) {
		if (this._value === value) return;
		var previousValue = this._value;
		this._value = value;
		var callback = this.valueChangeCallback, scope = this.valueChangeCallbackScope;
		if (callback) {
			if (scope) callback.call(scope, this, value, previousValue);
			else callback(this, value, previousValue);
		}
		this.emit("valuechange", this, value, previousValue);
	}
};
Object.assign(DropDownList.prototype, Methods$7);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/dropdownlist/Factory.js
ObjectFactory.register("dropDownList", function(config) {
	var gameObject = new DropDownList(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.DropDownList", DropDownList);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/build/BuildListConfig.js
var BuildListConfig = function(scene, config, creators) {
	config = config ? DeepClone(config) : {};
	if (creators === void 0) creators = {};
	var labelConfig = config.label || config.button;
	var listButtonConfig = config.button || config.label;
	delete config.label;
	delete config.button;
	var labelCreator = creators.label || creators.button || creators;
	var listButtonCreator = creators.button || creators.label || creators;
	var listConfig = BuildLabelConfig(scene, labelConfig, labelCreator);
	listConfig.list = config.list || {};
	listConfig.list.createButtonCallback = function(scene, option) {
		var gameObject = CreateLabel(scene, listButtonConfig, listButtonCreator).resetDisplayContent(option);
		if (option.hasOwnProperty("value")) gameObject.value = option.value;
		return gameObject;
	};
	var trackConfig = config.track;
	if (trackConfig) {
		listConfig.list.createTrackCallback = function(scene) {
			return CreateBackground$2(scene, trackConfig);
		};
		delete config.track;
	}
	var thumbConfig = config.thumb;
	if (thumbConfig) {
		listConfig.list.createThumbCallback = function(scene) {
			return CreateBackground$2(scene, thumbConfig);
		};
		delete config.thumb;
	}
	listConfig.list.onButtonOver = function(button, index, pointer, event) {
		if (button.setHoverState) button.setHoverState(true);
	};
	listConfig.list.onButtonOut = function(button, index, pointer, event) {
		if (button.setHoverState) button.setHoverState(false);
	};
	return listConfig;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/simpledropdownlist/SimpleDropDownList.js
var SimpleDropDownList = class extends DropDownList {
	constructor(scene, config, creators) {
		config = BuildListConfig(scene, config, creators);
		super(scene, config);
		this.type = "rexSimpleDropDownList";
	}
	setOptions(options) {
		if (options === void 0) options = [];
		for (var i = 0, cnt = options.length; i < cnt; i++) {
			var option = options[i];
			if (typeof option === "string") options[i] = {
				text: option,
				value: option
			};
		}
		super.setOptions(options);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/simpledropdownlist/Factory.js
ObjectFactory.register("simpleDropDownList", function(config, creators) {
	var gameObject = new SimpleDropDownList(this.scene, config, creators);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.SimpleDropDownList", SimpleDropDownList);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/text/TextHeightToLineCount.js
var TextHeightToLineCount = function(textObject) {
	var textObjectType = GetTextObjectType(textObject);
	var height, lineSpacing, lineHeight;
	switch (textObjectType) {
		case 0:
			height = textObject.height - textObject.padding.top - textObject.padding.bottom;
			lineSpacing = textObject.lineSpacing;
			lineHeight = textObject.style.metrics.fontSize + textObject.style.strokeThickness;
			break;
		case 1:
			height = textObject.height - textObject.padding.top - textObject.padding.bottom;
			lineSpacing = textObject.lineSpacing;
			lineHeight = textObject.style.metrics.fontSize;
			break;
		case 2:
			height = textObject.height;
			lineSpacing = 0;
			var scale = textObject.fontSize / textObject.fontData.size;
			lineHeight = textObject.fontData.lineHeight * scale;
	}
	return (height + lineSpacing) / (lineHeight + lineSpacing);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/textpage/methods/GetLines.js
var GetLines = function(startLineIndex, endLineIdx) {
	if (startLineIndex === void 0) startLineIndex = this.startLineIndex;
	if (endLineIdx === void 0) {
		if (this.isVariableLineHeightMode) {
			var pageHeight = this.pageHeight;
			if (pageHeight <= 0) endLineIdx = this.totalLinesCount;
			else {
				endLineIdx = this.getLineIndexByHeight(startLineIndex, pageHeight);
				if (endLineIdx <= startLineIndex) endLineIdx = startLineIndex + 1;
			}
		} else {
			var pageLinesCount = this.pageLinesCount;
			if (pageLinesCount > 0) endLineIdx = startLineIndex + pageLinesCount;
			else endLineIdx = this.totalLinesCount;
		}
	}
	if (endLineIdx > this.totalLinesCount) endLineIdx = this.totalLinesCount;
	var text;
	switch (this.textObjectType) {
		case 0:
		case 2:
			text = this.lines.slice(startLineIndex, endLineIdx).join("\n");
			break;
		case 1:
			var startIdx = this.lines.getLineStartIndex(startLineIndex);
			var endIdx = this.lines.getLineEndIndex(endLineIdx - 1);
			text = this.lines.getSliceTagText(startIdx, endIdx, true);
			if ((text.match(/\n/g) || []).length > endLineIdx - startLineIndex - 1) text = text.substring(0, text.length - 1);
	}
	return text;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/textpage/methods/SetContentMethods.js
var AppendPageByLineHeight = function(pageStartIndex) {
	var lines = this.lines.lines;
	if (!lines || lines.length <= pageStartIndex) return this;
	var pageHeight = this.pageHeight;
	this.pageStartIndexes.push(pageStartIndex);
	if (pageHeight <= 0) return this;
	var i = pageStartIndex;
	var len = lines.length;
	while (i < len) {
		var endLineIdx = this.getLineIndexByHeight(i, pageHeight);
		if (endLineIdx <= i) endLineIdx = i + 1;
		if (endLineIdx >= len) break;
		this.pageStartIndexes.push(endLineIdx);
		i = endLineIdx;
	}
	return this;
};
var SetContentMethods_default = {
	clearText() {
		this.sections.length = 0;
		this.pageStartIndexes.length = 0;
		this.lines.length = 0;
		return this;
	},
	appendPage(text) {
		var pageStartIndex = this.totalLinesCount;
		this.sections.push(GetString(text));
		var text = this.sections.join("\n");
		this.lines = TextToLines(this.parent, text, this.lines);
		var newLinesCount = this.totalLinesCount - pageStartIndex;
		if (newLinesCount <= 0) return this;
		if (this.isVariableLineHeightMode) AppendPageByLineHeight.call(this, pageStartIndex);
		else {
			var pageLinesCount = this.pageLinesCount;
			var pageCount;
			if (pageLinesCount > 0) pageCount = Math.ceil(newLinesCount / this.pageLinesCount);
			else pageCount = 1;
			for (var i = 0; i < pageCount; i++) this.pageStartIndexes.push(pageStartIndex + i * this.pageLinesCount);
		}
		return this;
	},
	setText(text, resetIndex) {
		if (resetIndex === void 0) resetIndex = true;
		if (resetIndex) this.resetIndex();
		this.clearText();
		var sections = GetString(text).split(this.pageBreak);
		for (var i = 0, cnt = sections.length; i < cnt; i++) this.appendPage(sections[i]);
		return this;
	},
	appendText(text) {
		var content = this.content + GetString(text);
		this.setText(content, false);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/textpage/methods/GetPageMethods.js
var Clamp$5 = __webpack_exports__Math.Clamp;
var GetPageMethods_default = {
	resetIndex() {
		this.pageIndex = -1;
		this.startLineIndex = -1;
		this.endLineIndex = void 0;
		return this;
	},
	setPageIndex(idx) {
		idx = Clamp$5(idx, 0, this.lastPageIndex);
		this.pageIndex = idx;
		this.startLineIndex = this.pageStartIndexes[idx];
		this.endLineIndex = this.pageStartIndexes[idx + 1];
		return this;
	},
	getPage(idx) {
		if (idx === void 0) idx = this.pageIndex;
		return this.setPageIndex(idx).getLines(this.startLineIndex, this.endLineIndex);
	},
	getNextPage() {
		return this.getPage(this.pageIndex + 1);
	},
	getPreviousPage() {
		return this.getPage(this.pageIndex - 1);
	},
	getFirstPage() {
		return this.getPage(0);
	},
	getLastPage() {
		return this.getPage(this.lastPageIndex);
	},
	setStartLineIndex(idx) {
		if (this.isVariableLineHeightMode) {
			var pageHeight = this.pageHeight;
			if (pageHeight <= 0) {
				this.startLineIndex = 0;
				this.endLineIndex = this.totalLinesCount;
				return this;
			}
			var lastStartLineIndex = this.getLastStartLineIndexByHeight(pageHeight);
			idx = Clamp$5(idx, 0, lastStartLineIndex);
			this.startLineIndex = idx;
			var endLineIndex = this.getLineIndexByHeight(this.startLineIndex, pageHeight);
			if (endLineIndex <= idx) endLineIndex = idx + 1;
			this.endLineIndex = endLineIndex;
		} else {
			var lastStartLineIndex = Math.max(this.totalLinesCount - this.pageLinesCount, 0);
			idx = Clamp$5(idx, 0, lastStartLineIndex);
			this.startLineIndex = idx;
			this.endLineIndex = idx + this.pageLinesCount;
		}
		return this;
	},
	getPageByLineIndex(idx) {
		return this.setStartLineIndex(idx).getLines(this.startLineIndex, this.endLineIndex);
	},
	getPageOfNextLine() {
		return this.getPageByLineIndex(this.startLineIndex + 1);
	},
	getPageOfPreviousLine() {
		return this.getPageByLineIndex(this.startLineIndex - 1);
	},
	getPageOfFirstLine() {
		return this.getPageByLineIndex(0);
	},
	getPageOfLastLine() {
		return this.getPageByLineIndex(this.totalLinesCount);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/textpage/methods/ShowMethods.js
var ShowMethods_default = {
	showPage(idx) {
		this.displayText(this.getPage(idx));
		return this;
	},
	showNextPage() {
		this.displayText(this.getNextPage());
		return this;
	},
	showPreviousPage() {
		this.displayText(this.getPreviousPage());
		return this;
	},
	showFirstPage() {
		this.displayText(this.getFirstPage());
		return this;
	},
	showLastPage() {
		this.displayText(this.getLastPage());
		return this;
	},
	show() {
		this.displayText(this.getLines());
		return this;
	},
	showPageByLineIndex(lineIndex) {
		this.displayText(this.getPageByLineIndex(lineIndex));
		return this;
	},
	showNextLine() {
		this.displayText(this.getPageOfNextLine());
		return this;
	},
	showPreviousLine() {
		this.displayText(this.getPageOfPreviousLine());
		return this;
	},
	showFirstLine() {
		this.displayText(this.getPageOfFirstLine());
		return this;
	},
	showLastLine() {
		this.displayText(this.getPageOfLastLine());
		return this;
	},
	displayText(text) {
		SetNoWrapText(this.parent, text);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/textpage/methods/Methods.js
var Methods$6 = { getLines: GetLines };
Object.assign(Methods$6, SetContentMethods_default, GetPageMethods_default, ShowMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/textpage/TextPage.js
var GetValue$90 = __webpack_exports__Utils.Objects.GetValue;
__webpack_exports__Math.Clamp;
var TextPage = class extends ComponentBase {
	constructor(gameObject, config) {
		super(gameObject, { eventEmitter: false });
		this.textObjectType = GetTextObjectType(this.parent);
		this.pageStartIndexes = [];
		this.lines = TextToLines(this.parent, "");
		this.sections = [];
		this.resetFromJSON(config);
	}
	resetFromJSON(o) {
		this.setMaxLines(GetValue$90(o, "maxLines", void 0));
		this.setPageBreak(GetValue$90(o, "pageBreak", "\f\n"));
		this.setText(GetValue$90(o, "text", ""));
		this.startLineIndex = GetValue$90(o, "start", -1);
		this.endLineIndex = GetValue$90(o, "end", void 0);
		var pageIndex = GetValue$90(o, "page");
		if (pageIndex === void 0) this.resetIndex();
		else this.setPageIndex(pageIndex);
		return this;
	}
	toJSON() {
		return {
			maxLines: this.maxLines,
			text: this.content,
			start: this.startLineIndex,
			end: this.endLineIndex,
			page: this.pageIndex,
			pageBreak: this.pageBreak
		};
	}
	shutdown(fromScene) {
		if (this.isShutdown) return;
		switch (this.textObjectType) {
			case 0:
				this.lines.length = 0;
				break;
			case 1:
				this.lines.destroy();
				break;
			case 2: this.lines.length = 0;
		}
		this.pageStartIndexes.length = 0;
		this.sections.length = 0;
		this.lines = void 0;
		this.pageStartIndexes = void 0;
		this.sections = void 0;
		super.shutdown(fromScene);
	}
	setMaxLines(maxLines) {
		this.maxLines = maxLines;
		return this;
	}
	setPageBreak(pageBreak) {
		this.pageBreak = pageBreak;
		return this;
	}
	get pageCount() {
		return this.pageStartIndexes.length;
	}
	get lastPageIndex() {
		return this.pageCount - 1;
	}
	get isFirstPage() {
		return this.pageIndex <= 0;
	}
	get isLastPage() {
		return this.pageIndex >= this.pageCount - 1;
	}
	get pageHeight() {
		var padding = this.parent.padding;
		return this.parent.height - padding.top - padding.bottom;
	}
	get totalLinesCount() {
		return this.lines ? this.lines.length : 0;
	}
	get pageLinesCount() {
		if (this.maxLines !== void 0) return this.maxLines;
		else if (this.isVariableLineHeightMode) throw new Error("pageLinesCount is not supported when isVariableLineHeightMode is enabled");
		else {
			var count;
			switch (this.textObjectType) {
				case 0:
				case 1:
					var maxLines = this.parent.style.maxLines;
					if (maxLines > 0) count = maxLines;
					else count = Math.floor(TextHeightToLineCount(this.parent));
					break;
				case 2: count = this.totalLinesCount;
			}
			return count;
		}
	}
	get isVariableLineHeightMode() {
		return this.textObjectType === 1 && !this.parent.style.fixedLineHeightMode && this.maxLines === void 0 && this.parent.style.maxLines <= 0;
	}
	getPageIndexByLineIndex(lineIndex) {
		var pageCount = this.pageStartIndexes.length;
		if (pageCount === 0) return 0;
		if (lineIndex == null || lineIndex <= this.pageStartIndexes[0]) return 0;
		for (var i = pageCount - 1; i >= 0; i--) if (lineIndex >= this.pageStartIndexes[i]) return i;
		return 0;
	}
	getLineIndexByHeight(lineIndex, pageHeight) {
		var lines = this.lines.lines;
		if (!lines || lines.length === 0) return 0;
		if (lineIndex < 0) lineIndex = 0;
		else if (lineIndex >= lines.length) return lines.length;
		var target = lines[lineIndex].startOffset + pageHeight;
		var left = lineIndex;
		var right = lines.length - 1;
		var result = lines.length;
		while (left <= right) {
			var mid = left + right >> 1;
			if (lines[mid].endOffset > target) {
				result = mid;
				right = mid - 1;
			} else left = mid + 1;
		}
		return result;
	}
	getLastStartLineIndexByHeight(pageHeight) {
		if (pageHeight <= 0) return 0;
		var lines = this.lines.lines;
		if (!lines || lines.length === 0) return 0;
		var targetStartOffset = lines[lines.length - 1].endOffset - pageHeight;
		if (targetStartOffset <= 0) return 0;
		return this.getLineIndexByStartOffsetCeil(targetStartOffset);
	}
	getLineIndexByStartOffset(targetOffset) {
		var lines = this.lines.lines;
		if (!lines || lines.length === 0) return 0;
		if (targetOffset <= lines[0].startOffset) return 0;
		var left = 0;
		var right = lines.length - 1;
		var result = 0;
		while (left <= right) {
			var mid = left + right >> 1;
			if (lines[mid].startOffset <= targetOffset) {
				result = mid;
				left = mid + 1;
			} else right = mid - 1;
		}
		return result;
	}
	getLineIndexByStartOffsetCeil(targetOffset) {
		var lines = this.lines.lines;
		if (!lines || lines.length === 0) return 0;
		if (targetOffset <= lines[0].startOffset) return 0;
		var left = 0;
		var right = lines.length - 1;
		var result = lines.length - 1;
		while (left <= right) {
			var mid = left + right >> 1;
			if (lines[mid].startOffset >= targetOffset) {
				result = mid;
				right = mid - 1;
			} else left = mid + 1;
		}
		return result;
	}
	get isFirstLine() {
		return this.startLineIndex <= 0;
	}
	get isLastLine() {
		return this.endLineIndex === this.totalLinesCount;
	}
	get content() {
		return this.sections.join(this.pageBreak);
	}
};
Object.assign(TextPage.prototype, Methods$6);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textpage/TextPage.js
var TextPage_default = TextPage;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/texttyping/methods/SetTextMethods.js
var SetTextMethods_default$1 = {
	setText(text) {
		if (this.setTextCallback) {
			if (this.setTextCallbackScope) text = this.setTextCallback.call(this.setTextCallbackScope, text, this.isLastChar, this.insertIndex);
			else text = this.setTextCallback(text, this.isLastChar, this.insertIndex);
		}
		if (this.textWrapEnable) SetNoWrapText(this.parent, text);
		else this.parent.setText(text);
	},
	appendText(text) {
		var newText = this.text.concat(GetString(text));
		if (this.isTyping) this.setTypingContent(newText);
		else this.start(newText, void 0, this.textLength);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/texttyping/methods/StartTyping.js
var StartTyping = function(text, speed, startIndex, timerStartAt) {
	if (text !== void 0) this.setTypingContent(text);
	if (speed !== void 0) this.speed = speed;
	if (startIndex === void 0) startIndex = 0;
	this.typingIndex = startIndex + 1;
	if (this.speed === 0) this.stop(true);
	else {
		this.setText("");
		this.startTimer(timerStartAt);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/text/GetPlainText.js
var GetPlainText = function(textObject, text) {
	if (textObject.getPlainText) text = textObject.getPlainText(text);
	return text;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/texttyping/methods/StartTypingFromLine.js
var StartTypingFromLine = function(text, lineIndex, speed, offsetIndex, timerStartAt) {
	var startIdx;
	if (lineIndex > 0) {
		if (offsetIndex === void 0) offsetIndex = 0;
		startIdx = GetNewLineIndex(GetPlainText(this.parent, text), lineIndex) + offsetIndex;
	}
	return this.start(text, speed, startIdx, timerStartAt);
};
var GetNewLineIndex = function(s, n) {
	var index = void 0;
	for (var i = 0; i < n; i++) {
		index = s.indexOf("\n", index + 1);
		if (index === -1) break;
	}
	return index;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/text/GetSubString.js
var GetSubString = function(textObject, text, startIdx, endIdx) {
	var result;
	if (textObject.getSubString) result = textObject.getSubString(text, startIdx, endIdx);
	else result = text.slice(startIdx, endIdx);
	return result;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/texttyping/utils/GetTypingString.js
var GetTypingString = function(text, typeIdx, textLength, typeMode) {
	var textObject = this.parent;
	var result;
	if (typeMode === 0) {
		var startIdx = 0;
		var endIdx = typeIdx;
		this.insertIndex = endIdx;
		result = GetSubString(textObject, text, startIdx, endIdx);
	} else if (typeMode === 1) {
		var endIdx = textLength;
		var startIdx = endIdx - typeIdx;
		this.insertIndex = 0;
		result = GetSubString(textObject, text, startIdx, endIdx);
	} else if (typeMode === 2) {
		var midIdx = textLength / 2;
		var startIdx = Math.floor(midIdx - typeIdx / 2);
		var endIdx = startIdx + typeIdx;
		this.insertIndex = typeIdx % 2 ? typeIdx : 0;
		result = GetSubString(textObject, text, startIdx, endIdx);
	} else if (typeMode === 3) {
		var lowerLen = Math.floor(typeIdx / 2);
		var lowerResult;
		if (lowerLen > 0) {
			var endIdx = textLength;
			var startIdx = endIdx - lowerLen;
			lowerResult = GetSubString(textObject, text, startIdx, endIdx);
		} else lowerResult = "";
		var upperLen = typeIdx - lowerLen;
		var upperResult;
		if (upperLen > 0) {
			var startIdx = 0;
			var endIdx = startIdx + upperLen;
			this.insertIndex = endIdx;
			upperResult = GetSubString(textObject, text, startIdx, endIdx);
		} else {
			upperResult = "";
			this.insertIndex = 0;
		}
		result = upperResult + lowerResult;
	}
	this.insertChar = result.charAt(this.insertIndex - 1);
	return result;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/texttyping/methods/StopTyping.js
var StopTyping = function(showAllText) {
	if (this.getTimer()) this.freeTimer();
	if (showAllText) {
		while (!this.isLastChar) {
			GetTypingString.call(this, this.text, this.typingIndex, this.textLength, this.typeMode);
			this.emit("typechar", this.insertChar);
			this.typingIndex++;
		}
		this.setText(this.text);
		this.emit("type");
		this.emit("complete", this, this.parent);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/texttyping/methods/PauseTyping.js
var PauseTyping = function() {
	var timer = this.getTimer();
	if (timer) timer.paused = true;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/texttyping/methods/ResumeTyping.js
var ResumeTyping = function() {
	var timer = this.getTimer();
	if (timer) timer.paused = false;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/texttyping/methods/Methods.js
var methods$9 = {
	start: StartTyping,
	startFromLine: StartTypingFromLine,
	stop: StopTyping,
	pause: PauseTyping,
	resumeTyping: ResumeTyping
};
Object.assign(methods$9, SetTextMethods_default$1);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/text/GetWrapText.js
var GetWrapText = function(textObject, text) {
	switch (GetTextObjectType(textObject)) {
		case 0:
			textObject.style.syncFont(textObject.canvas, textObject.context);
			text = textObject.runWordWrap(text);
			break;
		case 1:
			text = textObject.getText(text, void 0, void 0, true);
			break;
		case 2: text = textObject.setText(text).getTextBounds().wrappedText;
	}
	return text;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/texttyping/TextTyping.js
var GetFastValue = __webpack_exports__Utils.Objects.GetFastValue;
var GetValue$89 = __webpack_exports__Utils.Objects.GetValue;
var TextTyping = class extends ComponentBase {
	constructor(gameObject, config) {
		super(gameObject, config);
		this.timer = null;
		this.resetFromJSON(config);
	}
	resetFromJSON(o) {
		this.setTextWrapEnable(GetValue$89(o, "wrap", false));
		this.setTypeMode(GetValue$89(o, "typeMode", 0));
		this.setTypingSpeed(GetValue$89(o, "speed", 333));
		this.setTextCallback = GetFastValue(o, "setTextCallback", null);
		this.setTextCallbackScope = GetFastValue(o, "setTextCallbackScope", null);
		this.setTypingContent(GetFastValue(o, "text", ""));
		this.typingIndex = GetFastValue(o, "typingIndex", 0);
		this.insertIndex = null;
		this.insertChar = null;
		var elapsed = GetFastValue(o, "elapsed", null);
		if (elapsed !== null) this.start(void 0, void 0, this.typingIndex, elapsed);
		return this;
	}
	shutdown(fromScene) {
		if (this.isShutdown) return;
		this.freeTimer();
		super.shutdown(fromScene);
	}
	setTypeMode(m) {
		if (typeof m === "string") m = TYPEMODE[m];
		this.typeMode = m;
		return this;
	}
	setTypeSpeed(speed) {
		this.speed = speed;
		return this;
	}
	setTypingSpeed(speed) {
		this.speed = speed;
		return this;
	}
	setTextWrapEnable(enable) {
		if (enable === void 0) enable = true;
		this.textWrapEnable = enable;
		return this;
	}
	set text(value) {
		var text = GetString(value);
		if (this.textWrapEnable) text = GetWrapText(this.parent, text);
		this._text = text;
	}
	get text() {
		return this._text;
	}
	get isTyping() {
		return this.getTimer() !== null;
	}
	get isLastChar() {
		return this.typingIndex === this.textLength;
	}
	setTypingContent(text) {
		this.text = text;
		this.textLength = GetPlainText(this.parent, this.text).length;
		return this;
	}
	onTyping() {
		var newText = GetTypingString.call(this, this.text, this.typingIndex, this.textLength, this.typeMode);
		this.setText(newText);
		this.emit("typechar", this.insertChar);
		this.emit("type");
		if (this.isLastChar) {
			this.freeTimer();
			this.scene.sys.events.once("preupdate", function() {
				this.emit("complete", this, this.parent);
			}, this);
		} else {
			this.timer.delay = this.speed;
			this.typingIndex++;
		}
	}
	startTimer(timerStartAt) {
		if (this.timer) this.freeTimer();
		var startAt;
		if (timerStartAt === void 0) startAt = 0;
		else {
			this.speed;
			startAt = timerStartAt;
		}
		this.timer = this.scene.time.addEvent({
			delay: 1e-4,
			startAt,
			loop: true,
			callback: this.onTyping,
			callbackScope: this
		});
		return this;
	}
	getTimer() {
		return this.timer;
	}
	freeTimer() {
		if (this.timer) {
			this.timer.remove();
			this.timer = null;
		}
		return this;
	}
	setText(text) {
		if (this.setTextCallback) {
			if (this.setTextCallbackScope) text = this.setTextCallback.call(this.setTextCallbackScope, text, this.isLastChar, this.insertIndex);
			else text = this.setTextCallback(text, this.isLastChar, this.insertIndex);
		}
		if (this.textWrapEnable) SetNoWrapText(this.parent, text);
		else this.parent.setText(text);
	}
};
var TYPEMODE = {
	"left-to-right": 0,
	"right-to-left": 1,
	"middle-to-sides": 2,
	"sides-to-middle": 3
};
Object.assign(TextTyping.prototype, methods$9);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/texttyping/TextTyping.js
var TextTyping_default = TextTyping;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textbox/Base.js
var GetValue$88 = __webpack_exports__Utils.Objects.GetValue;
var TextBoxBase = function(GOClass, type) {
	if (type === void 0) type = "rexTextBox";
	class TextBox extends GOClass {
		constructor(scene, config) {
			super(scene, config);
			this.type = type;
			this.isRunning = false;
			this._isPageEnd = false;
			var text = this.childrenMap.text;
			var expandTextWidth = GetValue$88(config, "expandTextWidth", false);
			var expandTextHeight = GetValue$88(config, "expandTextHeight", false);
			if (expandTextWidth || expandTextHeight) {
				var textObjectType = GetTextObjectType(text);
				switch (textObjectType) {
					case 0:
					case 1:
						text.resize = function(width, height) {
							var fixedWidth = expandTextWidth ? width : 0;
							var fixedHeight = expandTextHeight ? height : 0;
							text.setFixedSize(fixedWidth, fixedHeight);
							if (fixedWidth > 0) text.setWordWrapWidth(fixedWidth);
						};
						if (textObjectType === 1) {
							var style = text.style;
							if (style.wrapMode === 0) style.wrapMode = 1;
						}
				}
				if (expandTextWidth) text._minWidth = 0;
				if (expandTextHeight) text._minHeight = 0;
			}
			this.setTypingMode(GetValue$88(config, "typingMode", "page"));
			this.page = new TextPage_default(text, GetValue$88(config, "page", void 0));
			this.typing = new TextTyping_default(text, GetValue$88(config, "typing", config.type));
			this.typing.on("complete", this.onTypingComplete, this).on("type", this.onType, this).on("typechar", this.onTypeChar, this);
			this.textWidthSave = text.width;
			this.textHeightSave = text.height;
		}
		setTypingMode(mode) {
			if (typeof mode === "string") mode = TypingMode[mode];
			this.typingMode = mode;
			return this;
		}
		start(text, speed) {
			if (speed !== void 0) this.setTypingSpeed(speed);
			this.isRunning = true;
			this.page.setText(text);
			this.emit("start");
			if (this.typingMode === 0) this.typeNextPage();
			else this.typeNextLine();
			return this;
		}
		more(text, speed) {
			if (speed !== void 0) this.setTypingSpeed(speed);
			if (this.isRunning) {
				this.page.appendText(text);
				this.typing.appendText(text);
			} else {
				this.isRunning = true;
				this.page.appendText(text);
				this.emit("start");
				if (this.typingMode === 0) {
					this._isPageEnd = false;
					var txt = this.page.getPage();
					var startIndex = this.typing.textLength;
					this.typing.start(txt, void 0, startIndex);
				}
				return this;
			}
		}
		typeNextPage() {
			if (!this.isRunning) return this;
			if (!this.isLastPage) {
				this._isPageEnd = false;
				var txt = this.page.getNextPage();
				this.typing.start(txt);
			} else this.emit("complete");
			return this;
		}
		typeNextLine() {
			if (!this.isRunning) return this;
			if (!this.isLastLine) {
				var txt = this.page.getPageOfNextLine();
				var startLineIndex;
				if (this.isFirstLine) startLineIndex = 0;
				else startLineIndex = this.page.pageLinesCount - 1;
				this.typing.startFromLine(txt, startLineIndex);
			} else {
				this.isRunning = false;
				this.emit("pageend");
				this.emit("complete");
			}
		}
		pause() {
			if (!this.isRunning) return this;
			if (this.isTyping) {
				this.typing.pause();
				this.emit("pause");
			}
			return this;
		}
		resume() {
			if (!this.isRunning) return this;
			if (!this.isTyping) {
				this.emit("resume");
				this.typing.resume();
			}
			return this;
		}
		stop(showAllText) {
			if (!this.isRunning) return this;
			this.typing.stop(showAllText);
			return this;
		}
		showLastPage() {
			if (!this.isRunning) return this;
			this.typing.stop();
			if (this.typingMode === 0) this.page.showLastPage();
			else this.page.showLastLine();
			this.emit("type");
			this.onTypingComplete();
			return this;
		}
		setTypeSpeed(speed) {
			this.typing.setTypingSpeed(speed);
			return this;
		}
		setTypingSpeed(speed) {
			this.typing.setTypingSpeed(speed);
			return this;
		}
		get isTyping() {
			return this.typing.isTyping;
		}
		get isPageEnd() {
			return this._isPageEnd;
		}
		get isLastPage() {
			return this.page.isLastPage;
		}
		get isFirstPage() {
			return this.page.isFirstPage;
		}
		get pageCount() {
			return this.page.pageCount;
		}
		get pageIndex() {
			return this.page.pageIndex;
		}
		get isLastLine() {
			return this.page.isLastLine;
		}
		get isFirstLine() {
			return this.page.isFirstLine;
		}
		get lineCound() {
			return this.page.totalLinesCount;
		}
		get startLineIndex() {
			return this.page.startLineIndex;
		}
		get endLineIndex() {
			return this.page.endLineIndex;
		}
		get typingSpeed() {
			return this.typing.speed;
		}
		onType() {
			var text = this.childrenMap.text;
			if (this.textWidthSave !== text.width || this.textHeightSave !== text.height) {
				this.textWidthSave = text.width;
				this.textHeightSave = text.height;
				this.getTopmostSizer().layout();
			}
			this.emit("type");
		}
		onTypeChar(char) {
			this.emit("typechar", char);
		}
		onTypingComplete() {
			if (this.typingMode === 0) {
				this._isPageEnd = true;
				var isLastPage = this.isLastPage;
				this.isRunning = !isLastPage;
				this.emit("pageend");
				if (isLastPage) this.emit("complete");
			} else this.typeNextLine();
		}
	}
	return TextBox;
};
var TypingMode = {
	page: 0,
	line: 1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textbox/TextBox.js
var TextBox = class extends TextBoxBase(TitleLabel) {
	constructor(scene, config) {
		if (config === void 0) config = {};
		if (!config.hasOwnProperty("layoutMode")) config.layoutMode = 1;
		super(scene, config);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textbox/Factory.js
ObjectFactory.register("textBox", function(config) {
	var gameObject = new TextBox(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.TextBox", TextBox);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/simpletextbox/SimpleTextBox.js
var SimpleTextBox = class extends TextBox {
	constructor(scene, config, creators) {
		config = BuildTitleLabelConfig(scene, config, creators);
		super(scene, config);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/simpletextbox/Factory.js
ObjectFactory.register("simpleTextBox", function(config) {
	var gameObject = new SimpleTextBox(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.SimpleTextBox", SimpleTextBox);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/numberbar/NumberBar.js
var GetValue$87 = __webpack_exports__Utils.Objects.GetValue;
var NumberBar = class extends Sizer {
	constructor(scene, config) {
		super(scene, config);
		this.type = "rexNumberBar";
		var background = GetValue$87(config, "background", void 0);
		var icon = GetValue$87(config, "icon", void 0);
		var iconMask = GetValue$87(config, "iconMask", void 0);
		var sliderConfig = GetValue$87(config, "slider", void 0);
		var text = GetValue$87(config, "text", void 0);
		var iconSpace = GetValue$87(config, "space.icon", 0);
		var sliderSpace = GetValue$87(config, "space.slider", 0);
		if (background) this.addBackground(background);
		if (icon) {
			var padding;
			if (this.orientation === 0) {
				if (sliderConfig || text) padding = { right: iconSpace };
			} else if (sliderConfig || text) padding = { bottom: iconSpace };
			this.add(icon, {
				proportion: 0,
				align: "center",
				padding
			});
			if (iconMask) iconMask = AddChildCircleMask(this, icon);
		}
		var slider;
		if (sliderConfig) {
			sliderConfig.orientation = this.orientation;
			sliderConfig.eventEmitter = this;
			sliderConfig.value = null;
			if (!sliderConfig.hasOwnProperty("input")) sliderConfig.input = -1;
			slider = new Slider(scene, sliderConfig);
			scene.add.existing(slider);
			var padding;
			if (this.orientation === 0) {
				if (text) padding = { right: sliderSpace };
			} else if (text) padding = { bottom: sliderSpace };
			var proportion;
			if (this.orientation === 0) proportion = GetValue$87(sliderConfig, "width", void 0) === void 0 ? 1 : 0;
			else proportion = GetValue$87(sliderConfig, "height", void 0) === void 0 ? 1 : 0;
			this.add(slider, {
				proportion,
				align: "center",
				padding
			});
		}
		if (text) this.add(text);
		this.addChildrenMap("background", background);
		this.addChildrenMap("icon", icon);
		this.addChildrenMap("iconMask", iconMask);
		this.addChildrenMap("slider", slider);
		this.addChildrenMap("text", text);
		var callback = GetValue$87(config, "valuechangeCallback", null);
		if (callback !== null) {
			var scope = GetValue$87(config, "valuechangeCallbackScope", void 0);
			this.on("valuechange", callback, scope);
		}
		this.setEnable(GetValue$87(config, "enable", void 0));
		this.setValue(GetValue$87(config, "value", 0));
	}
	get enable() {
		if (this.childrenMap.slider) return this.childrenMap.slider.enable;
		else return false;
	}
	set enable(value) {
		if (this.childrenMap.slider) this.childrenMap.slider.setEnable(value);
	}
	setEnable(enable) {
		if (enable === void 0) enable = true;
		this.enable = enable;
		return this;
	}
	get value() {
		if (this.childrenMap.slider) return this.childrenMap.slider.value;
		else return 0;
	}
	set value(value) {
		if (!this.childrenMap.slider) return;
		this.childrenMap.slider.value = value;
	}
	setValue(value, min, max) {
		if (this.childrenMap.slider) this.childrenMap.slider.setValue(value, min, max);
		return this;
	}
	addValue(inc, min, max) {
		if (this.childrenMap.slider) this.childrenMap.slider.addValue(inc, min, max);
		return this;
	}
	getValue(min, max) {
		if (this.childrenMap.slider) return this.childrenMap.slider.getValue(min, max);
		else return 0;
	}
	easeValueTo(value, min, max) {
		if (this.childrenMap.slider) this.childrenMap.slider.easeValueTo(value, min, max);
		return this;
	}
	stopEaseValue() {
		if (this.childrenMap.slider) this.childrenMap.slider.stopEaseValue();
		return this;
	}
	setEaseValueDuration(duration) {
		if (this.childrenMap.slider) this.childrenMap.slider.setEaseValueDuration(duration);
		return this;
	}
	setEaseValueFunction(ease) {
		if (this.childrenMap.slider) this.childrenMap.slider.setEaseValueFunction(ease);
		return this;
	}
	get text() {
		var textObject = this.childrenMap.text;
		if (textObject === void 0) return "";
		var value;
		if (textObject.text) value = textObject.text;
		else value = textObject.getData("text");
		return value;
	}
	set text(value) {
		var textObject = this.childrenMap.text;
		if (textObject === void 0) return;
		if (textObject.setText) textObject.setText(value);
		else textObject.setData("text", value);
	}
	setText(value) {
		this.text = value;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/numberbar/Factory.js
ObjectFactory.register("numberBar", function(config) {
	var gameObject = new NumberBar(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.NumberBar", NumberBar);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/scrollbar/Factory.js
ObjectFactory.register("scrollBar", function(config) {
	var gameObject = new ScrollBar(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.ScrollBar", ScrollBar);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/badgelabel/BadgeLabel.js
var GetValue$86 = __webpack_exports__Utils.Objects.GetValue;
var BadgeKeys = {
	leftTop: "left-top",
	centerTop: "center-top",
	rightTop: "right-top",
	leftCenter: "left-center",
	center: "center",
	rightCenter: "right-center",
	leftBottom: "left-bottom",
	centerBottom: "center-bottom",
	rightBottom: "right-bottom"
};
var Badge = class extends OverlapSizer {
	constructor(scene, config) {
		super(scene, config);
		this.type = "rexBadge";
		var background = GetValue$86(config, "background", void 0);
		if (background) this.addBackground(background);
		this.addChildrenMap("background", background);
		var main = GetValue$86(config, "main", void 0);
		if (main) this.add(main, {
			key: "main",
			align: "center",
			expand: false
		});
		this.addChildrenMap("main", main);
		for (var key in BadgeKeys) {
			var badge = GetValue$86(config, key, void 0);
			if (badge) {
				this.add(badge, {
					key,
					align: BadgeKeys[key],
					expand: false
				});
				this.addChildrenMap(key, badge);
			}
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/badgelabel/Factory.js
ObjectFactory.register("badgeLabel", function(config) {
	var gameObject = new Badge(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.BadgeLabel", Badge);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/pages/methods/AddChildMethods.js
var OverlapSizerAdd = OverlapSizer.prototype.add;
var Add = function(gameObject, childKey, align, padding, expand, minWidth, minHeight, offsetX, offsetY) {
	gameObject.setVisible(false);
	OverlapSizerAdd.call(this, gameObject, childKey, align, padding, expand, minWidth, minHeight, offsetX, offsetY);
	return this;
};
var AddChildMethods_default = {
	add: Add,
	addPage: Add
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/pages/methods/GetPage.js
var GetPage$1 = function(key) {
	if (key === void 0) return null;
	else if (!this.sizerChildren.hasOwnProperty(key)) return null;
	else return this.sizerChildren[key];
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/pages/methods/SwapPage.js
var ContainerSetChildVisible$1 = Container_default.prototype.setChildVisible;
var SwapPage$1 = function(key, fadeInDuration) {
	this._previousKey = this._currentKey;
	var prevoiusPage = this.previousPage;
	if (prevoiusPage) {
		if (this.swapMode === 0) {
			ContainerSetChildVisible$1.call(this, prevoiusPage, false);
			this.emit("pageinvisible", prevoiusPage, this._previousKey, this);
		} else prevoiusPage.destroy();
	}
	if (key && !this.sizerChildren.hasOwnProperty(key)) this.emit("createpage", key, this);
	this._currentKey = key;
	var currentPage = this.currentPage;
	if (currentPage) {
		ContainerSetChildVisible$1.call(this, currentPage, true);
		this.emit("pagevisible", currentPage, this._currentKey, this);
		if (fadeInDuration === void 0) fadeInDuration = this.fadeInDuration;
		if (fadeInDuration > 0) currentPage.setAlpha(0).fadeIn(fadeInDuration, 1);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/pages/methods/HasPage.js
var HasPage = function(key) {
	return this.sizerChildren.hasOwnProperty(key);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/pages/methods/Methods.js
var methods$8 = {
	getPage: GetPage$1,
	swapPage: SwapPage$1,
	hasPage: HasPage
};
Object.assign(methods$8, AddChildMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/pages/Pages.js
var GetValue$85 = __webpack_exports__Utils.Objects.GetValue;
var Pages = class extends OverlapSizer {
	constructor(scene, config) {
		super(scene, config);
		this.type = "rexPages";
		this.childrenMap = this.sizerChildren;
		this._previousKey = void 0;
		this._currentKey = void 0;
		this.setSwapMode(GetValue$85(config, "swapMode", 0));
		this.setFadeInDuration(GetValue$85(config, "fadeIn", 0));
	}
	setSwapMode(mode) {
		if (typeof mode === "string") mode = SWAPMODE[mode];
		this.swapMode = mode;
		return this;
	}
	setFadeInDuration(duration) {
		this.fadeInDuration = duration;
		return this;
	}
	get previousKey() {
		return this._previousKey;
	}
	get currentKey() {
		return this._currentKey;
	}
	set currentKey(key) {
		this.swapPage(key);
	}
	get currentPage() {
		return this.getPage(this.currentKey);
	}
	get previousPage() {
		return this.getPage(this.previousKey);
	}
	get keys() {
		return Object.keys(this.sizerChildren);
	}
};
Object.assign(Pages.prototype, methods$8);
var SWAPMODE = {
	invisible: 0,
	destroy: 1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/pages/Factory.js
ObjectFactory.register("pages", function(config) {
	var gameObject = new Pages(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.Pages", Pages);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/mesh2dwrapper/image/render/WebGLRenderer.js
var Mesh2D$2 = __webpack_exports__GameObjects.Mesh2D;
var WebGLRenderer = function(renderer, src, drawingContext, parentMatrix) {
	if (!src) src = this;
	if (src.skipRender()) return;
	Mesh2D$2.prototype.renderWebGL.call(this, renderer, src, drawingContext, parentMatrix);
	if (src.debugCallback) src.runDebugCallback(drawingContext, parentMatrix);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/mesh2dwrapper/image/render/SkipRender.js
var SkipRender = function() {
	return false;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/mesh2dwrapper/image/render/Render.js
var Render_default = {
	renderWebGL: WebGLRenderer,
	skipRender: SkipRender
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/mesh2dwrapper/utils/WorldXY.js
var RotateAround$2 = __webpack_exports__Math.RotateAround;
var LocalXYToWorldXY = function(gameObject, localX, localY, out) {
	if (out === void 0) out = {};
	else if (out === true) out = GlobalXY$1;
	var ox = gameObject.displayOriginX;
	var oy = gameObject.displayOriginY;
	out.x = localX - ox;
	out.y = localY - oy;
	RotateAround$2(out, 0, 0, gameObject.rotation);
	out.x *= gameObject.scaleX;
	out.y *= gameObject.scaleY;
	out.x += gameObject.x;
	out.y += gameObject.y;
	return out;
};
var WorldXYToLocalXY = function(gameObject, worldX, worldY, out) {
	if (out === void 0) out = {};
	else if (out === true) out = GlobalXY$1;
	var ox = gameObject.displayOriginX;
	var oy = gameObject.displayOriginY;
	out.x = worldX - gameObject.x;
	out.y = worldY - gameObject.y;
	out.x /= gameObject.scaleX;
	out.y /= gameObject.scaleY;
	RotateAround$2(out, 0, 0, -gameObject.rotation);
	out.x += ox;
	out.y += oy;
	return out;
};
var GlobalXY$1 = {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/mesh2dwrapper/image/VertexObject.js
var Linear$3 = __webpack_exports__Math.Linear;
var RotateAround$1 = __webpack_exports__Math.RotateAround;
var VertexObject = class {
	constructor(parent, index, u, v) {
		if (index === void 0) index = -1;
		if (u === void 0) u = 0;
		if (v === void 0) v = 0;
		this.parent = void 0;
		this.index = index;
		this.name = "";
		this.u = u;
		this.v = v;
		this._frameU = 0;
		this._frameV = 0;
		this._frameX = 0;
		this._frameY = 0;
		this._dx = 0;
		this._dy = 0;
		this._localX = 0;
		this._localY = 0;
		this._alpha = 1;
		this._color = 16777215;
		this.xyz = [
			0,
			0,
			0
		];
		if (parent) this.setParent(parent, index);
	}
	setParent(parent, index) {
		this.parent = parent;
		if (index !== void 0) this.index = index;
		this.writeToMesh();
		return this;
	}
	setName(name) {
		this.name = name;
		return this;
	}
	get frameU() {
		return this._frameU;
	}
	set frameU(value) {
		if (this._frameU === value) return;
		this._frameU = value;
		this.writeUVToMesh();
	}
	get frameV() {
		return this._frameV;
	}
	set frameV(value) {
		if (this._frameV === value) return;
		this._frameV = value;
		this.writeUVToMesh();
	}
	get frameX() {
		return this._frameX;
	}
	set frameX(value) {
		if (this._frameX === value) return;
		this._frameX = value;
		this._localX = value + this._dx;
		this.writePositionToMesh();
	}
	get frameY() {
		return this._frameY;
	}
	set frameY(value) {
		if (this._frameY === value) return;
		this._frameY = value;
		this._localY = value + this._dy;
		this.writePositionToMesh();
	}
	get localX() {
		return this._localX;
	}
	set localX(value) {
		if (this._localX === value) return;
		this._localX = value;
		this._dx = value - this._frameX;
		this.writePositionToMesh();
	}
	get localY() {
		return this._localY;
	}
	set localY(value) {
		if (this._localY === value) return;
		this._localY = value;
		this._dy = value - this._frameY;
		this.writePositionToMesh();
	}
	get alpha() {
		return this._alpha;
	}
	set alpha(value) {
		this._alpha = value;
	}
	get color() {
		return this._color;
	}
	set color(value) {
		this._color = value;
	}
	setUV(u, v) {
		this.u = u;
		this.v = v;
		return this;
	}
	setFrameUV(frameU0, frameV0, frameU1, frameV1) {
		this.frameU = Linear$3(frameU0, frameU1, this.u);
		this.frameV = Linear$3(frameV0, frameV1, this.v);
		return this;
	}
	setFrameSize(frameWidth, frameHeight, frameX, frameY) {
		if (frameX === void 0) frameX = 0;
		if (frameY === void 0) frameY = 0;
		this.frameX = frameX + this.u * frameWidth;
		this.frameY = frameY + this.v * frameHeight;
		return this;
	}
	resetPosition() {
		this.localX = this.frameX;
		this.localY = this.frameY;
		return this;
	}
	setLocalPosition(x, y) {
		this.localX = x;
		this.localY = y;
		return this;
	}
	rotateAround(ox, oy, rotation) {
		GlobalXY.x = this.localX;
		GlobalXY.y = this.localY;
		RotateAround$1(GlobalXY, ox, oy, rotation);
		this.localX = GlobalXY.x;
		this.localY = GlobalXY.y;
		return this;
	}
	setAlpha(value) {
		this.alpha = value;
		return this;
	}
	setColor(value) {
		this.color = value;
		return this;
	}
	getWorldXY(out) {
		if (this.parent) return LocalXYToWorldXY(this.parent, this.localX, this.localY, out);
		else return null;
	}
	setWorldXY(x, y) {
		var out = WorldXYToLocalXY(this.parent, x, y, true);
		this.setLocalPosition(out.x, out.y);
		return this;
	}
	setPosition(x, y) {
		this.setWorldXY(x, y);
		return this;
	}
	get x() {
		if (this.parent) return this.getWorldXY(true).x;
		else return null;
	}
	set x(value) {
		this.setWorldXY(value, this.y);
	}
	get y() {
		if (this.parent) return this.getWorldXY(true).y;
		else return null;
	}
	set y(value) {
		this.setWorldXY(this.x, value);
	}
	writePositionToMesh() {
		var parent = this.parent;
		if (!parent || this.index < 0) return this;
		var offset = this.index * 4;
		parent.vertices[offset] = this._localX - parent.displayOriginX;
		parent.vertices[offset + 1] = this._localY - parent.displayOriginY;
		return this;
	}
	writeUVToMesh() {
		var parent = this.parent;
		if (!parent || this.index < 0) return this;
		var offset = this.index * 4;
		parent.vertices[offset + 2] = this._frameU;
		parent.vertices[offset + 3] = this._frameV;
		return this;
	}
	writeToMesh() {
		this.writePositionToMesh();
		this.writeUVToMesh();
		return this;
	}
};
var GlobalXY = {};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/mesh2dwrapper/image/methods/VertexMethods.js
var VertexMethods_default = {
	clear() {
		this.vertexObjects.length = 0;
		this.faceIndices.length = 0;
		this.vertices.length = 0;
		this.indices.length = 0;
		this.indicesOrdered = null;
		this.setUseOrderedIndices(false);
		return this;
	},
	createVertexObject(u, v) {
		var index = this.vertexObjects.length;
		var vertexObject = new VertexObject(null, index, u, v);
		this.vertexObjects.push(vertexObject);
		this.vertices.push(0, 0, 0, 0);
		vertexObject.setParent(this, index);
		this.updateVertexObjectFrame(vertexObject);
		return vertexObject;
	},
	addVertexObject(vertexObject) {
		if (this.vertexObjects.includes(vertexObject)) return this;
		var index = this.vertexObjects.length;
		this.vertexObjects.push(vertexObject);
		this.vertices.push(0, 0, 0, 0);
		vertexObject.setParent(this, index);
		this.updateVertexObjectFrame(vertexObject);
		return this;
	},
	setVertexObjects(vertexObjects) {
		this.vertexObjects.length = 0;
		this.vertices.length = 0;
		for (var i = 0, cnt = vertexObjects.length; i < cnt; i++) this.addVertexObject(vertexObjects[i]);
		return this;
	},
	resetVertexObjects() {
		var vertexObjects = this.vertexObjects;
		for (var i = 0, cnt = vertexObjects.length; i < cnt; i++) vertexObjects[i].resetPosition();
		return this;
	},
	writeVertexObjectPositions() {
		var vertexObjects = this.vertexObjects;
		for (var i = 0, cnt = vertexObjects.length; i < cnt; i++) vertexObjects[i].writePositionToMesh();
		return this;
	},
	rebuildVerticesFromVertexObjects() {
		var vertexObjects = this.vertexObjects;
		this.vertices.length = vertexObjects.length * 4;
		for (var i = 0, cnt = vertexObjects.length; i < cnt; i++) vertexObjects[i].setParent(this, i);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/mesh2dwrapper/image/methods/IndexMethods.js
var IndexMethods_default = {
	setFaceIndices(faceIndices, texturePage) {
		if (texturePage === void 0) texturePage = this.texturePage;
		else this.texturePage = texturePage;
		this.faceIndices.length = 0;
		this.indices.length = 0;
		for (var i = 0, cnt = faceIndices.length; i < cnt; i += 3) {
			var a = faceIndices[i];
			var b = faceIndices[i + 1];
			var c = faceIndices[i + 2];
			this.faceIndices.push(a, b, c);
			this.indices.push(a, b, c, texturePage);
		}
		this.updateOrderedIndices();
		return this;
	},
	setMeshIndices(indices) {
		this.indices.length = 0;
		this.faceIndices.length = 0;
		for (var i = 0, cnt = indices.length; i < cnt; i += 4) {
			this.indices.push(indices[i], indices[i + 1], indices[i + 2], indices[i + 3]);
			this.faceIndices.push(indices[i], indices[i + 1], indices[i + 2]);
		}
		this.updateOrderedIndices();
		return this;
	},
	setUseOrderedIndexOptimization(enabled, strategy) {
		this.autoBuildOrderedIndices = !!enabled;
		if (strategy !== void 0) this.orderedIndicesStrategy = strategy;
		return this.updateOrderedIndices();
	},
	setUseOrderedIndicesOptimization(enabled, strategy) {
		return this.setUseOrderedIndexOptimization(enabled, strategy);
	},
	updateOrderedIndices() {
		if (this.autoBuildOrderedIndices && this.indices.length > 0) {
			this.setRenderAsTriangles(false);
			this.buildOrderedIndices(this.orderedIndicesStrategy, true);
		} else this.setUseOrderedIndices(false);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/mesh2dwrapper/image/methods/FrameMethods.js
var Mesh2D$1 = __webpack_exports__GameObjects.Mesh2D;
var FrameMethods_default = {
	setTexture(key, frame) {
		Mesh2D$1.prototype.setTexture.call(this, key, frame);
		if (this.vertexObjects) this.syncVertexObjectsFrame();
		return this;
	},
	setSizeToFrame(frame) {
		if (!frame) frame = this.frame;
		if (!frame) return this;
		this.width = frame.realWidth;
		this.height = frame.realHeight;
		return this;
	},
	updateDisplayOrigin() {
		Mesh2D$1.prototype.updateDisplayOrigin.call(this);
		if (this.vertexObjects) this.writeVertexObjectPositions();
		return this;
	},
	setDisplayOrigin(x, y) {
		Mesh2D$1.prototype.setDisplayOrigin.call(this, x, y);
		if (this.vertexObjects) this.writeVertexObjectPositions();
		return this;
	},
	resetFaceSize() {
		return this.syncVertexObjectsFrame();
	},
	syncVertexObjectsFrame() {
		this.setSizeToFrame();
		this.updateDisplayOrigin();
		var vertexObjects = this.vertexObjects;
		for (var i = 0, cnt = vertexObjects.length; i < cnt; i++) this.updateVertexObjectFrame(vertexObjects[i]);
		return this;
	},
	updateVertexObjectFrame(vertexObject) {
		var frame = this.frame;
		if (!frame) {
			vertexObject.writeToMesh();
			return this;
		}
		vertexObject.setFrameSize(frame.cutWidth, frame.cutHeight, frame.x, frame.y).setFrameUV(frame.u0, frame.v0, frame.u1, frame.v1);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/mesh2dwrapper/image/methods/DebugMethods.js
var GetCalcMatrix = __webpack_exports__GameObjects.GetCalcMatrix;
var DebugMethods_default = {
	setDebug(graphic, callback) {
		this.debugGraphic = graphic;
		if (!graphic && !callback) this.debugCallback = null;
		else if (!callback) this.debugCallback = this.renderDebugVerts;
		else this.debugCallback = callback;
		return this;
	},
	renderDebugVerts(src, meshLength, verts) {
		var graphic = src.debugGraphic;
		for (var i = 0; i < meshLength; i += 6) {
			var x0 = verts[i + 0];
			var y0 = verts[i + 1];
			var x1 = verts[i + 2];
			var y1 = verts[i + 3];
			var x2 = verts[i + 4];
			var y2 = verts[i + 5];
			graphic.lineBetween(x0, y0, x1, y1);
			graphic.lineBetween(x1, y1, x2, y2);
			graphic.lineBetween(x2, y2, x0, y0);
		}
	},
	runDebugCallback(drawingContext, parentMatrix) {
		var camera = drawingContext.camera;
		var calcMatrix = GetCalcMatrix(this, camera, parentMatrix, !drawingContext.useCanvas).calc;
		var faceIndices = this.faceIndices;
		var vertexObjects = this.vertexObjects;
		var displayOriginX = this.displayOriginX;
		var displayOriginY = this.displayOriginY;
		var verts = [];
		for (var i = 0, cnt = faceIndices.length; i < cnt; i++) {
			var vertex = vertexObjects[faceIndices[i]];
			var x = vertex.localX - displayOriginX;
			var y = vertex.localY - displayOriginY;
			verts.push(calcMatrix.getX(x, y), calcMatrix.getY(x, y));
		}
		this.debugCallback.call(this, this, verts.length, verts);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/mesh2dwrapper/image/methods/Methods.js
var Methods$5 = {};
Object.assign(Methods$5, VertexMethods_default, IndexMethods_default, FrameMethods_default, DebugMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/mesh2dwrapper/image/Image.js
var Mesh2D = __webpack_exports__GameObjects.Mesh2D;
var IsPlainObject$13 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$84 = __webpack_exports__Utils.Objects.GetValue;
var Image$3 = class extends Mesh2D {
	constructor(scene, x, y, key, frame, config) {
		if (IsPlainObject$13(x)) {
			config = x;
			x = GetValue$84(config, "x", 0);
			y = GetValue$84(config, "y", 0);
			key = GetValue$84(config, "key", "__DEFAULT");
			frame = GetValue$84(config, "frame", null);
		}
		if (x === void 0) x = 0;
		if (y === void 0) y = 0;
		if (key === void 0 || key === null) key = "__DEFAULT";
		super(scene, x, y, key, [], [], GetValue$84(config, "flipV", false));
		this.type = "rexMeshImage";
		this.vertexObjects = [];
		this.faceIndices = [];
		this.texturePage = GetValue$84(config, "texturePage", 0);
		this.autoBuildOrderedIndices = GetValue$84(config, "useOrderedIndices", false);
		this.orderedIndicesStrategy = GetValue$84(config, "orderedIndicesStrategy", 2);
		this.debugCallback = null;
		this.debugGraphic = null;
		this.setRenderAsTriangles(GetValue$84(config, "renderAsTriangles", false));
		this.setUseOrderedIndices(this.autoBuildOrderedIndices);
		if (frame !== void 0 && frame !== null) this.setFrame(frame);
		this.setSizeToFrame();
		this.setOriginFromFrame();
	}
	get frame() {
		return this._frame;
	}
	set frame(value) {
		if (this._frame === value) return;
		this._frame = value;
		if (this.vertexObjects) this.syncVertexObjectsFrame();
	}
};
Object.assign(Image$3.prototype, Render_default, Methods$5);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/mesh2dwrapper/sprite/AnmiationMethods.js
var AnmiationMethods_default = {
	play(key, ignoreIfPlaying) {
		return this.anims.play(key, ignoreIfPlaying);
	},
	playReverse(key, ignoreIfPlaying) {
		return this.anims.playReverse(key, ignoreIfPlaying);
	},
	playAfterDelay(key, delay) {
		return this.anims.playAfterDelay(key, delay);
	},
	playAfterRepeat(key, repeatCount) {
		return this.anims.playAfterRepeat(key, repeatCount);
	},
	chain(key) {
		return this.anims.chain(key);
	},
	stop() {
		return this.anims.stop();
	},
	stopAfterDelay(delay) {
		return this.anims.stopAfterDelay(delay);
	},
	stopAfterRepeat(repeatCount) {
		return this.anims.stopAfterRepeat(repeatCount);
	},
	stopOnFrame(frame) {
		return this.anims.stopOnFrame(frame);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/mesh2dwrapper/sprite/Sprite.js
var AnimationState = __webpack_exports__Animations.AnimationState;
var Sprite = class extends Image$3 {
	constructor(scene, x, y, texture, frame) {
		super(scene, x, y, texture, frame);
		this.type = "rexMeshSprite";
		this.anims = new AnimationState(this);
	}
	preUpdate(time, delta) {
		this.anims.update(time, delta);
	}
	preDestroy() {
		this.anims.destroy();
		this.anims = void 0;
	}
};
Object.assign(Sprite.prototype, AnmiationMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/perspective/image/methods/RenderMethods.js
var RenderMethods_default = { skipRender() {
	return this.hideBackFace && this.isBackFace;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/perspective/utils/RotateMethods.js
var RotateMethods_default = {
	setRotationXYZ(rotationX, rotationY, rotationZ) {
		if (rotationX !== void 0) this.rotationX = rotationX;
		if (rotationY !== void 0) this.rotationY = rotationY;
		if (rotationZ !== void 0) this.rotationZ = rotationZ;
		return this;
	},
	setRotationX(rotationX) {
		this.rotationX = rotationX;
		return this;
	},
	setRotationY(rotationY) {
		this.rotationY = rotationY;
		return this;
	},
	setRotationZ(rotationZ) {
		this.rotationZ = rotationZ;
		return this;
	},
	setAngleXYZ(angleX, angleY, angleZ) {
		if (angleX !== void 0) this.angleX = angleX;
		if (angleY !== void 0) this.angleY = angleY;
		if (angleZ !== void 0) this.angleZ = angleZ;
		return this;
	},
	setAngleX(angleX) {
		this.angleX = angleX;
		return this;
	},
	setAngleXY(angleY) {
		this.angleY = angleY;
		return this;
	},
	setAngleXZ(angleZ) {
		this.angleZ = angleZ;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/perspective/image/methods/Methods.js
var Methods$4 = {};
Object.assign(Methods$4, RenderMethods_default, RotateMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/mesh2dwrapper/utils/RotateXYZ.js
var RotateXYZ = function(gameObject, rotationX, rotationY, rotationZ, centerX, centerY) {
	var vertexObjects = gameObject.vertexObjects;
	if (!vertexObjects) return;
	if (rotationX === void 0) rotationX = 0;
	if (rotationY === void 0) rotationY = 0;
	if (rotationZ === void 0) rotationZ = 0;
	if (centerX === void 0) centerX = gameObject.width / 2;
	if (centerY === void 0) centerY = gameObject.height / 2;
	var i, cnt, vertexObject, xyz;
	if (rotationX === 0 && rotationY === 0 && rotationZ === 0) for (i = 0, cnt = vertexObjects.length; i < cnt; i++) {
		vertexObject = vertexObjects[i];
		vertexObject.resetPosition();
		if (!vertexObject.hasOwnProperty("xyz")) vertexObject.xyz = [
			0,
			0,
			0
		];
		xyz = vertexObject.xyz;
		xyz[0] = vertexObject.frameX - centerX;
		xyz[1] = vertexObject.frameY - centerY;
		xyz[2] = 0;
	}
	else {
		var x, y, z, xTemp, yTemp, zTemp;
		var cosX = Math.cos(rotationX), sinX = Math.sin(rotationX);
		var cosY = Math.cos(rotationY), sinY = Math.sin(rotationY);
		var cosZ = Math.cos(rotationZ), sinZ = Math.sin(rotationZ);
		var perspective = gameObject.scene.scale.gameSize.width, scale;
		for (i = 0, cnt = vertexObjects.length; i < cnt; i++) {
			vertexObject = vertexObjects[i];
			x = vertexObject.frameX - centerX;
			y = vertexObject.frameY - centerY;
			z = 0;
			yTemp = y * cosX - z * sinX;
			zTemp = y * sinX + z * cosX;
			y = yTemp;
			z = zTemp;
			xTemp = x * cosY + z * sinY;
			zTemp = -x * sinY + z * cosY;
			x = xTemp;
			z = zTemp;
			xTemp = x * cosZ - y * sinZ;
			yTemp = x * sinZ + y * cosZ;
			x = xTemp;
			y = yTemp;
			scale = perspective / (perspective - z);
			vertexObject.localX = x * scale + centerX;
			vertexObject.localY = y * scale + centerY;
			if (!vertexObject.hasOwnProperty("xyz")) vertexObject.xyz = [
				0,
				0,
				0
			];
			xyz = vertexObject.xyz;
			xyz[0] = x;
			xyz[1] = y;
			xyz[2] = z;
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/mesh2dwrapper/utils/IsBackFace.js
var IsBackFace = function(vertexObjects, faceIndices, offset) {
	if (offset === void 0) offset = 0;
	if (!vertexObjects || !faceIndices || faceIndices.length < offset + 3) return false;
	var vertex0 = vertexObjects[faceIndices[offset]];
	var vertex1 = vertexObjects[faceIndices[offset + 1]];
	var vertex2 = vertexObjects[faceIndices[offset + 2]];
	if (!vertex0 || !vertex1 || !vertex2) return false;
	var v0 = vertex0.xyz;
	var v1 = vertex1.xyz;
	var v2 = vertex2.xyz;
	if (!v0 || !v1 || !v2) return false;
	var edge1 = [
		v1[0] - v0[0],
		v1[1] - v0[1],
		v1[2] - v0[2]
	];
	var edge2 = [
		v2[0] - v0[0],
		v2[1] - v0[1],
		v2[2] - v0[2]
	];
	return edge1[0] * edge2[1] - edge1[1] * edge2[0] < 0;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/object/IsPlainObject.js
var IsPlainObject$12 = function(obj) {
	if (typeof obj !== "object" || obj.nodeType || obj === obj.window) return false;
	try {
		if (obj.constructor && !{}.hasOwnProperty.call(obj.constructor.prototype, "isPrototypeOf")) return false;
	} catch (e) {
		return false;
	}
	return true;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/mesh2dwrapper/utils/GenerateGridVertices.js
var GenerateGridVertices = function(gameObject, columns, rows, sharedVertexMode) {
	if (IsPlainObject$12(columns)) {
		var config = columns;
		columns = config.columns;
		rows = config.rows;
		sharedVertexMode = config.sharedVertexMode;
	}
	if (columns === void 0) columns = 1;
	if (rows === void 0) rows = 1;
	var vertexObjects = [];
	var faceIndices = [];
	for (var r = 0; r <= rows; r++) for (var c = 0; c <= columns; c++) vertexObjects.push(gameObject.createVertexObject(c / columns, r / rows));
	for (var r = 0; r < rows; r++) for (var c = 0; c < columns; c++) {
		var indexTL = r * (columns + 1) + c;
		var indexTR = indexTL + 1;
		var indexBL = (r + 1) * (columns + 1) + c;
		var indexBR = indexBL + 1;
		faceIndices.push(indexTL, indexBR, indexBL, indexTL, indexTR, indexBR);
	}
	gameObject.setFaceIndices(faceIndices);
	return vertexObjects;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/perspective/image/Image.js
var IsPlainObject$11 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$83 = __webpack_exports__Utils.Objects.GetValue;
var RadToDeg$2 = __webpack_exports__Math.RadToDeg;
var DegToRad$2 = __webpack_exports__Math.DegToRad;
var Image$2 = class extends Sprite {
	constructor(scene, x, y, key, frame, config) {
		if (IsPlainObject$11(x)) {
			config = x;
			x = GetValue$83(config, "x", 0);
			y = GetValue$83(config, "y", 0);
			key = GetValue$83(config, "key", null);
			frame = GetValue$83(config, "frame", null);
		}
		if (config === void 0) config = {};
		if (config.renderAsTriangles === void 0) config.renderAsTriangles = false;
		if (config.useOrderedIndices === void 0) config.useOrderedIndices = true;
		if (config.orderedIndicesStrategy === void 0) config.orderedIndicesStrategy = 1;
		super(scene, x, y, key, frame, config);
		this.type = "rexPerspectiveImage";
		this._rotationX = 0;
		this._rotationY = 0;
		this._rotationZ = 0;
		this.isBackFace = false;
		this.hideBackFace = GetValue$83(config, "hideBackFace", true);
		var gridWidth = GetValue$83(config, "gridWidth", 0);
		var gridHeight = GetValue$83(config, "gridHeight", gridWidth);
		this.resetVertices(gridWidth, gridHeight);
	}
	resetVertices(gridWidth, gridHeight) {
		if (gridWidth !== void 0) this.gridWidth = gridWidth;
		if (gridHeight !== void 0) this.gridHeight = gridHeight;
		this.clear();
		if (this.width === 0 || this.height === 0) return this;
		var frameWidth = this.frame.cutWidth, frameHeight = this.frame.cutHeight;
		var gridWidth, gridHeight;
		if (this.gridWidth === 0) gridWidth = Math.max(frameWidth / 8, 32);
		else gridWidth = this.gridWidth;
		if (this.gridHeight === 0) gridHeight = Math.max(frameHeight / 8, 32);
		else gridHeight = this.gridHeight;
		this.addGridFaces({
			columns: Math.ceil(frameWidth / gridWidth),
			rows: Math.ceil(frameHeight / gridHeight),
			sharedVertexMode: true
		});
		this.syncRotation();
		return this;
	}
	addGridFaces(columns, rows, sharedVertexMode) {
		GenerateGridVertices(this, columns, rows, sharedVertexMode);
		return this;
	}
	setSizeToFrame(frame) {
		super.setSizeToFrame(frame);
		if (this._rotationX !== void 0) this.syncRotation();
		return this;
	}
	syncRotation() {
		Rotate(this, this._rotationX, this._rotationY, this._rotationZ);
		return this;
	}
	get rotationX() {
		return this._rotationX;
	}
	set rotationX(value) {
		if (this._rotationX === value) return;
		this._rotationX = value;
		this.syncRotation();
	}
	get angleX() {
		return RadToDeg$2(this.rotationX);
	}
	set angleX(value) {
		this.rotationX = DegToRad$2(value);
	}
	get rotationY() {
		return this._rotationY;
	}
	set rotationY(value) {
		if (this._rotationY === value) return;
		this._rotationY = value;
		this.syncRotation();
	}
	get angleY() {
		return RadToDeg$2(this.rotationY);
	}
	set angleY(value) {
		this.rotationY = DegToRad$2(value);
	}
	get rotationZ() {
		return this._rotationZ;
	}
	set rotationZ(value) {
		if (this._rotationZ === value) return;
		this._rotationZ = value;
		this.syncRotation();
	}
	get angleZ() {
		return RadToDeg$2(this.rotationZ);
	}
	set angleZ(value) {
		this.rotationZ = DegToRad$2(value);
	}
};
var Rotate = function(gameObject, rotationX, rotationY, rotationZ) {
	RotateXYZ(gameObject, rotationX, rotationY, rotationZ);
	if (gameObject.faceIndices.length > 0) gameObject.isBackFace = IsBackFace(gameObject.vertexObjects, gameObject.faceIndices, 0);
	else gameObject.isBackFace = false;
};
Object.assign(Image$2.prototype, Methods$4);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/rendertexture/CreateDynamicTexture.js
var DynamicTexture = __webpack_exports__Textures.DynamicTexture;
var CreateDynamicTexture = function(scene, width, height) {
	if (width === void 0) width = 2;
	if (height === void 0) height = 2;
	return new DynamicTexture(scene.sys.textures, null, width, height);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/mesh2dwrapper/utils/InstallRTSetSizeHook.js
var InstallRTSetSizeHook = function(rt, callback) {
	var setSize = rt.setSize;
	rt.setSize = function(width, height, forceEven) {
		var result = setSize.call(this, width, height, forceEven);
		callback();
		return result;
	};
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/perspective/rendertexture/RenderTexture.js
var IsPlainObject$10 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$82 = __webpack_exports__Utils.Objects.GetValue;
var RenderTexture = class extends Image$2 {
	constructor(scene, x, y, width, height, config) {
		if (IsPlainObject$10(x)) {
			config = x;
			x = GetValue$82(config, "x", 0);
			y = GetValue$82(config, "y", 0);
			width = GetValue$82(config, "width", 32);
			height = GetValue$82(config, "height", 32);
		}
		var texture = CreateDynamicTexture(scene, width, height);
		super(scene, x, y, texture, null, config);
		this.type = "rexPerspectiveRenderTexture";
		this.rt = this.texture;
		var self = this;
		InstallRTSetSizeHook(this.rt, function() {
			self.setSizeToFrame();
		});
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		super.destroy(fromScene);
		this.rt.destroy();
		this.rt = null;
	}
	setSizeToFrame(frame) {
		var width = this.width;
		var height = this.height;
		super.setSizeToFrame(frame);
		this.updateDisplayOrigin();
		if (this.width !== width || this.height !== height) {
			if (this.gridWidth !== void 0) this.resetVertices();
		}
		return this;
	}
	snapshot(gameObjects, config) {
		if (config === void 0) config = {};
		config.gameObjects = gameObjects;
		config.renderTexture = this.rt;
		Snapshot(config);
		this.setSizeToFrame();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/perspective/utils/ForEachFace.js
var ForEachFace = function(faces, callback, scope, ignoreInvalid) {
	if (Array.isArray(faces)) {
		var isBreak = false;
		for (var i = 0, cnt = faces.length; i < cnt; i++) {
			var face = faces[i];
			if (ignoreInvalid && !face) continue;
			if (scope) isBreak = callback.call(scope, face, i, faces);
			else isBreak = callback(face, i, faces);
			if (isBreak) return;
		}
	} else {
		var isBreak = false;
		for (var name in faces) {
			var face = faces[name];
			if (ignoreInvalid && !face) continue;
			if (scope) isBreak = callback.call(scope, face, name, faces);
			else isBreak = callback(face, name, faces);
			if (isBreak) return;
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/perspective/utils/FaceContainer.js
var RadToDeg$1 = __webpack_exports__Math.RadToDeg;
var DegToRad$1 = __webpack_exports__Math.DegToRad;
var FaceContainer = class extends ContainerLite {
	constructor(scene, x, y, width, height, faces) {
		super(scene, x, y, width, height);
		this.faces = faces;
		ForEachFace(faces, function(face) {
			face.setPosition(x, y);
			this.add(face);
		}, this, true);
	}
	get rotationX() {
		return 0;
	}
	set rotationX(value) {}
	get angleX() {
		return RadToDeg$1(this.rotationX);
	}
	set angleX(value) {
		this.rotationX = DegToRad$1(value);
	}
	get rotateX() {
		return RadToDeg$1(this.rotationX);
	}
	set rotateX(value) {
		this.rotationX = DegToRad$1(value);
	}
	get rotationY() {
		return 0;
	}
	set rotationY(value) {}
	get angleY() {
		return RadToDeg$1(this.rotationY);
	}
	set angleY(value) {
		this.rotationY = DegToRad$1(value);
	}
	get rotateY() {
		return RadToDeg$1(this.rotationY);
	}
	set rotateY(value) {
		this.rotationY = DegToRad$1(value);
	}
	get rotationZ() {
		return 0;
	}
	set rotationZ(value) {}
	get angleZ() {
		return RadToDeg$1(this.rotationZ);
	}
	set angleZ(value) {
		this.rotationZ = DegToRad$1(value);
	}
	get rotateZ() {
		return RadToDeg$1(this.rotationZ);
	}
	set rotateZ(value) {
		this.rotationZ = DegToRad$1(value);
	}
	setDebug(graphic, callback) {
		ForEachFace(this.faces, function(face) {
			face.setDebug(graphic, callback);
		}, null, true);
		return this;
	}
	forEachFace(callback, scope, ignoreInvalid) {
		ForEachFace(this.faces, callback, scope, ignoreInvalid);
		return this;
	}
};
Object.assign(FaceContainer.prototype, RotateMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/perspective/utils/CreatePerspectiveObject.js
var IsPlainObject$9 = __webpack_exports__Utils.Objects.IsPlainObject;
var DefaultImageConfig = { key: "__WHITE" };
var ClassMap = {
	image: Image$2,
	rendertexture: RenderTexture
};
var CreatePerspectiveObject = function(scene, config) {
	if (config === void 0) config = DefaultImageConfig;
	var perspectiveObject;
	if (IsPlainObject$9(config)) {
		if (!config.hasOwnProperty("type")) {
			if (config.hasOwnProperty("key")) config.type = "image";
			else if (config.hasOwnProperty("width")) config.type = "rendertexture";
		}
		perspectiveObject = new ClassMap[config.type](scene, config);
		scene.add.existing(perspectiveObject);
	} else perspectiveObject = config;
	return perspectiveObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/perspective/utils/CreateFaces.js
var CreateFaces = function(scene, config, faceNames) {
	var faces;
	if (faceNames === void 0) {
		faces = [];
		var face, faceConfig;
		for (var i = 0, cnt = config.length; i < cnt; i++) {
			faceConfig = config[i];
			if (faceConfig) face = CreatePerspectiveObject(scene, faceConfig);
			else face = null;
			faces.push(face);
		}
	} else {
		faces = {};
		var face, name;
		for (var i = 0, cnt = faceNames.length; i < cnt; i++) {
			name = faceNames[i];
			if (config.hasOwnProperty(name)) face = CreatePerspectiveObject(scene, config[name]);
			else face = null;
			faces[name] = face;
		}
	}
	return faces;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/perspective/card/LayoutFaces.js
var LayoutFaces = function(parent, faces) {
	var backFace = faces.back;
	if (backFace) {
		if (parent.orientation === 0) backFace.angleY = 180;
		else backFace.angleX = 180;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/perspective/card/Flip.js
var GetValue$81 = __webpack_exports__Utils.Objects.GetValue;
var Linear$2 = __webpack_exports__Math.Linear;
var Flip$1 = class extends EaseValueTaskBase {
	constructor(gameObject, config) {
		super(gameObject, config);
		this.resetFromJSON(config);
		this.boot();
	}
	resetFromJSON(o) {
		super.resetFromJSON(o);
		this.setEase(GetValue$81(o, "ease", "Cubic"));
		this.setFrontToBackDirection(GetValue$81(o, "frontToBack", 0));
		this.setBackToFrontDirection(GetValue$81(o, "backToFront", 1));
		return this;
	}
	setFrontToBackDirection(direction) {
		if (typeof direction === "string") direction = DIRMODE[direction];
		this.endAngleFB = direction === 0 ? 180 : -180;
		return this;
	}
	setBackToFrontDirection(direction) {
		if (typeof direction === "string") direction = DIRMODE[direction];
		this.endAngleBF = direction === 0 ? -180 : 180;
		return this;
	}
	start(duration, repeat) {
		if (this.timer.isRunning) return this;
		this.timer.setDelay(this.delay).setDuration(duration);
		var loop = repeat + 1;
		if (this.parent.face === 0) {
			this.startAngle = 0;
			this.endAngle = this.endAngleFB * loop;
		} else {
			this.startAngle = this.endAngleBF;
			this.endAngle = this.startAngle - this.endAngleBF * loop;
		}
		super.start();
		return this;
	}
	flip(duration, repeat) {
		if (this.isRunning) return this;
		if (duration === void 0) duration = this.duration;
		if (repeat === void 0) repeat = 0;
		this.start(duration, repeat);
		this.emit("start", this.parent, this);
		this.parent.currentFaceIndex = (this.parent.currentFaceIndex + repeat + 1) % 2;
		return this;
	}
	flipRight(duration, repeat) {
		if (this.parent.currentFaceIndex === 0) this.setFrontToBackDirection(0);
		else this.setBackToFrontDirection(0);
		this.flip(duration, repeat);
		return this;
	}
	flipLeft(duration, repeat) {
		if (this.parent.currentFaceIndex === 0) this.setFrontToBackDirection(1);
		else this.setBackToFrontDirection(1);
		this.flip(duration, repeat);
		return this;
	}
	updateTarget(gameObject, timer) {
		var t = this.easeFn(timer.t);
		var value = Linear$2(this.startAngle, this.endAngle, t);
		if (gameObject.orientation === 0) gameObject.angleY = value;
		else gameObject.angleX = value;
	}
};
var DIRMODE = {
	"right": 0,
	"left-to-right": 0,
	"left": 1,
	"right-to-left": 1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/perspective/card/Card.js
var IsPlainObject$8 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$80 = __webpack_exports__Utils.Objects.GetValue;
var FaceNames = ["back", "front"];
var Card = class extends FaceContainer {
	constructor(scene, x, y, config) {
		if (IsPlainObject$8(x)) {
			config = x;
			x = GetValue$80(config, "x", 0);
			y = GetValue$80(config, "y", 0);
		}
		var faces = CreateFaces(scene, config, FaceNames);
		var backFace = faces.back;
		var frontFace = faces.front;
		var width = GetValue$80(config, "width");
		var height = GetValue$80(config, "height");
		if (width === void 0 || height === void 0) {
			if (width === void 0) {
				var frontFaceWidth = frontFace ? frontFace.width : 0;
				var backFaceWidth = backFace ? backFace.width : 0;
				width = Math.max(frontFaceWidth, backFaceWidth);
			}
			if (height === void 0) {
				var frontFaceHeight = frontFace ? frontFace.height : 0;
				var backFaceHeight = backFace ? backFace.height : 0;
				height = Math.max(frontFaceHeight, backFaceHeight);
			}
		}
		super(scene, x, y, width, height, faces);
		this.type = "rexPerspectiveCard";
		this.frontFaceRotationX = 0;
		this.frontFaceRotationY = 0;
		this.frontFaceRotationZ = 0;
		ForEachFace(faces, function(face, name) {
			this[`${name}Face`] = face;
		}, this);
		var flipConfig = GetValue$80(config, "flip", void 0);
		if (flipConfig !== false) this.flip = new Flip$1(this, flipConfig);
		this.setOrientation(GetValue$80(config, "orientation", 0));
		LayoutFaces(this, faces);
		this.setFace(GetValue$80(config, "face", 0));
	}
	get rotationX() {
		return this.frontFaceRotationX;
	}
	set rotationX(value) {
		if (this.frontFaceRotationX === value) return;
		var delta = value - this.frontFaceRotationX;
		this.frontFaceRotationX = value;
		ForEachFace(this.faces, function(face) {
			face.rotationX += delta;
		}, null, true);
	}
	get rotationY() {
		return this.frontFaceRotationY;
	}
	set rotationY(value) {
		if (this.frontFaceRotationY === value) return;
		var delta = value - this.frontFaceRotationY;
		this.frontFaceRotationY = value;
		ForEachFace(this.faces, function(face) {
			face.rotationY += delta;
		}, null, true);
	}
	get rotationZ() {
		return this.frontFaceRotationZ;
	}
	set rotationZ(value) {
		if (this.frontFaceRotationZ === value) return;
		var delta = value - this.frontFaceRotationZ;
		this.frontFaceRotationZ = value;
		ForEachFace(this.faces, function(face) {
			face.rotationZ += delta;
		}, null, true);
	}
	setOrientation(orientation) {
		if (typeof orientation === "string") orientation = ORIENTATIONMODE$1[orientation];
		this.orientation = orientation;
		return this;
	}
	get face() {
		return this.currentFaceIndex;
	}
	set face(index) {
		if (typeof index === "string") index = FACEMODE$1[index];
		this.currentFaceIndex = index;
		var angle = index === 1 ? 180 : 0;
		if (this.orientation === 0) this.angleY = angle;
		else this.angleX = angle;
	}
	setFace(face) {
		this.face = face;
		return this;
	}
	toggleFace() {
		var newFace = this.face === 0 ? 1 : 0;
		this.setFace(newFace);
		return this;
	}
};
var ORIENTATIONMODE$1 = {
	x: 0,
	horizontal: 0,
	h: 0,
	y: 1,
	vertical: 1,
	v: 1
};
var FACEMODE$1 = {
	front: 0,
	back: 1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/rendertexture/Init.js
var GetValue$79 = __webpack_exports__Utils.Objects.GetValue;
var Init = function(parentContainer, rtOwner, config) {
	rtOwner.visibleSibling = [];
	rtOwner.isRunning = false;
	rtOwner.useParentBounds = GetValue$79(config, "useParentBounds", false);
	rtOwner.setPosition(parentContainer.x, parentContainer.y).setVisible(false);
	parentContainer.pin(rtOwner);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/rendertexture/Exit.js
var Exit = function(parentContainer, rtOwner) {
	if (!parentContainer) return false;
	var visibleSibling = rtOwner.visibleSibling;
	for (var i = 0, cnt = visibleSibling.length; i < cnt; i++) parentContainer.setChildVisible(visibleSibling[i], true);
	visibleSibling.length = 0;
	parentContainer.setChildVisible(rtOwner, false);
	rtOwner.isRunning = false;
	return true;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/rendertexture/Enter.js
var Enter = function(parentContainer, rtOwner) {
	if (!parentContainer) return false;
	Exit(parentContainer, rtOwner);
	var useParentBounds = rtOwner.useParentBounds;
	Snapshot({
		gameObjects: parentContainer.getAllVisibleChildren(),
		renderTexture: rtOwner.rt,
		x: rtOwner.x,
		y: rtOwner.y,
		width: useParentBounds ? parentContainer.displayWidth : void 0,
		height: useParentBounds ? parentContainer.displayHeighth : void 0,
		originX: useParentBounds ? parentContainer.originX : void 0,
		originY: useParentBounds ? parentContainer.originY : void 0
	});
	parentContainer.setChildVisible(rtOwner, true);
	var visibleSibling = rtOwner.visibleSibling;
	var children = parentContainer.children;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		if (child.visible && child !== rtOwner) {
			parentContainer.setChildVisible(child, false);
			visibleSibling.push(child);
		}
	}
	rtOwner.isRunning = true;
	return true;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/container/containerlite/rendertexture/MeshRenderTextureBase.js
var MeshRenderTextureBase = function(RenderTextureOwnerClass) {
	return class Base extends RenderTextureOwnerClass {
		constructor(parentContainer, config) {
			var scene = parentContainer.scene;
			super(scene, 0, 0, 1, 1, config);
			scene.add.existing(this);
			Init(parentContainer, this, config);
		}
		destroy(fromScene) {
			if (!this.scene || this.ignoreDestroy) return;
			this.exit();
			super.destroy(fromScene);
		}
		enter() {
			if (Enter(this.rexContainer.parent, this)) this.setSizeToFrame();
			return this;
		}
		exit() {
			Exit(this.rexContainer.parent, this);
			return this;
		}
	};
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/containerperspective/ContainerPerspective.js
var ContainerPerspective = class extends MeshRenderTextureBase(RenderTexture) {
	get perspectiveState() {
		return this.isRunning;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/perspectivecard/CreatePerspectiveCardMesh.js
var GetValue$78 = __webpack_exports__Utils.Objects.GetValue;
var CreatePerspectiveCardMesh = function(config) {
	var scene = this.scene;
	this.setSnapshotPadding(GetValue$78(config, "snapshotPadding", 0));
	config = Clone$2(config);
	delete config.width;
	delete config.height;
	config.front = {
		width: 1,
		height: 1
	};
	config.back = {
		width: 1,
		height: 1
	};
	var card = new Card(scene, config);
	scene.add.existing(card);
	var flip = card.flip;
	if (flip) {
		var parent = this;
		flip.on("start", function() {
			parent.enterPerspectiveMode();
		}).on("complete", function() {
			parent.exitPerspectiveMode();
		});
	}
	return card;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/perspectivecard/PerspectiveMethods.js
var FaceIndexMap = ["front", "back"];
var PerspectiveMethods_default = {
	enterPerspectiveMode() {
		if (this.isInPerspectiveMode) return this;
		this.setChildVisible(this.perspectiveCard, true);
		this.snapshotFace(0);
		this.snapshotFace(1);
		this.setChildVisible(this.childrenMap.front, false);
		this.setChildVisible(this.childrenMap.back, false);
		this.perspectiveCard.setSize(this.width, this.height);
		return this;
	},
	exitPerspectiveMode() {
		if (!this.isInPerspectiveMode) return this;
		this.setChildVisible(this.perspectiveCard, false);
		var isFrontFace = this.perspectiveCard.face === 0;
		this.setChildVisible(this.childrenMap.front, isFrontFace);
		this.setChildVisible(this.childrenMap.back, !isFrontFace);
		return this;
	},
	setSnapshotPadding(padding) {
		this.snapshotPadding = padding;
		return this;
	},
	snapshotFace(face) {
		if (typeof face === "number") face = FaceIndexMap[face];
		var cardFace = this.perspectiveCard.faces[face];
		var faceChild = this.childrenMap[face];
		cardFace.rt.clear();
		var faceChildVisibleSave = faceChild.visible;
		faceChild.visible = true;
		var gameObjects = faceChild.isRexContainerLite ? faceChild.getAllVisibleChildren() : faceChild;
		cardFace.snapshot(gameObjects, { padding: this.snapshotPadding });
		faceChild.visible = faceChildVisibleSave;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/perspectivecard/PerspectiveCard.js
var GetValue$77 = __webpack_exports__Utils.Objects.GetValue;
var PerspectiveCard = class extends OverlapSizer {
	constructor(scene, config) {
		super(scene, config);
		this.type = "rexPerspectiveCard";
		var backFace = config.back;
		var backFaceExpand = GetValue$77(config, "expand.back", true);
		this.add(backFace, {
			key: "back",
			expand: backFaceExpand
		});
		var frontFace = config.front;
		var frontFaceExpand = GetValue$77(config, "expand.front", true);
		this.add(frontFace, {
			key: "front",
			expand: frontFaceExpand
		});
		this.perspectiveCard = CreatePerspectiveCardMesh.call(this, config);
		this.pin(this.perspectiveCard);
		this.exitPerspectiveMode(false);
	}
	get flip() {
		return this.perspectiveCard.flip;
	}
	get face() {
		return this.perspectiveCard.face;
	}
	set face(index) {
		if (this.flip && this.flip.isRunning) return;
		this.perspectiveCard.face = index;
		var isFrontFace = index === 0;
		var frontFace = this.childrenMap.front;
		var backFace = this.childrenMap.back;
		this.setChildVisible(frontFace, isFrontFace);
		this.setChildVisible(backFace, !isFrontFace);
	}
	setFace(face) {
		this.face = face;
		return this;
	}
	toggleFace() {
		var newFace = this.face === 0 ? 1 : 0;
		this.setFace(newFace);
		return this;
	}
	get isInPerspectiveMode() {
		return this.perspectiveCard.visible;
	}
	get rotationX() {
		return this.perspectiveCard.rotationX;
	}
	set rotationX(value) {
		this.enterPerspectiveMode();
		this.perspectiveCard.rotationX = value;
	}
	get angleX() {
		return this.perspectiveCard.angleX;
	}
	set angleX(value) {
		this.enterPerspectiveMode();
		this.perspectiveCard.angleX = value;
	}
	get rotationY() {
		return this.perspectiveCard.rotationY;
	}
	set rotationY(value) {
		this.enterPerspectiveMode();
		this.perspectiveCard.rotationY = value;
	}
	get angleY() {
		return this.perspectiveCard.angleY;
	}
	set angleY(value) {
		this.enterPerspectiveMode();
		this.perspectiveCard.angleY = value;
	}
	get rotationZ() {
		return this.perspectiveCard.rotationZ;
	}
	set rotationZ(value) {
		this.enterPerspectiveMode();
		this.perspectiveCard.rotationZ = value;
	}
	get angleZ() {
		return this.perspectiveCard.angleZ;
	}
	set angleZ(value) {
		this.enterPerspectiveMode();
		this.perspectiveCard.angleZ = value;
	}
	panX(v) {
		this.enterPerspectiveMode();
		this.perspectiveCard.panX(v);
		return this;
	}
	panY(v) {
		this.enterPerspectiveMode();
		this.perspectiveCard.panY(v);
		return this;
	}
	panZ(v) {
		this.enterPerspectiveMode();
		this.perspectiveCard.panZ(v);
		return this;
	}
	transformVerts(x, y, z, rotateX, rotateY, rotateZ) {
		this.enterPerspectiveMode();
		this.perspectiveCard.transformVerts(x, y, z, rotateX, rotateY, rotateZ);
		return this;
	}
	forEachFace(callback, scope, ignoreInvalid) {
		this.enterPerspectiveMode();
		this.perspectiveCard.forEachFace(callback, scope, ignoreInvalid);
		return this;
	}
};
Object.assign(PerspectiveCard.prototype, PerspectiveMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/perspectivecard/Factory.js
ObjectFactory.register("perspectiveCard", function(config) {
	var gameObject = new PerspectiveCard(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.PerspectiveCard", PerspectiveCard);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tabpages/methods/GetPageKeyByIndex.js
var GetPageKeyByIndex = function(index) {
	var buttons = this.getElement("tabs.buttons");
	if (index >= buttons.length) return;
	return buttons[index].name;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tabpages/methods/GetPageIndexByKey.js
var GetPageIndexByKey = function(key) {
	var buttons = this.getElement("tabs.buttons");
	for (var i = 0, cnt = buttons.length; i < cnt; i++) if (buttons[i].name === key) return i;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tabpages/methods/AddPage.js
var IsPlainObject$7 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$76 = __webpack_exports__Utils.Objects.GetValue;
var UUID$2 = __webpack_exports__Utils.String.UUID;
var AddPage = function(key, tabGameObject, pageGameObject) {
	if (IsPlainObject$7(key)) {
		var config = key;
		key = GetValue$76(config, "key");
		tabGameObject = GetValue$76(config, "tab");
		pageGameObject = GetValue$76(config, "page");
	}
	if (!key) key = UUID$2();
	tabGameObject.name = key;
	this.childrenMap.tabs.addButton(tabGameObject);
	this.childrenMap.pages.addPage(pageGameObject, { key });
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tabpages/methods/SwapPageMethods.js
var SwapPage = function(key, fadeInDuration) {
	var index;
	if (typeof key === "number") index = key;
	else index = this.getPageIndex(key);
	if (index != null) {
		var fadeInDurationSave;
		if (fadeInDuration !== void 0) {
			fadeInDurationSave = this.childrenMap.pages.fadeInDuration;
			this.childrenMap.pages.fadeInDuration = fadeInDuration;
		}
		this.childrenMap.tabs.emitButtonClick(index);
		if (fadeInDurationSave !== void 0) this.childrenMap.pages.fadeInDuration = fadeInDurationSave;
	}
	return this;
};
var SwapFirstPage = function(fadeInDuration) {
	this.swapPage(0, fadeInDuration);
	return this;
};
var SwapLastPage = function(fadeInDuration) {
	var index = this.getElement("tabs.buttons").length - 1;
	this.swapPage(index, fadeInDuration);
	return this;
};
var SwapPageMethods_default = {
	swapPage: SwapPage,
	swapFirstPage: SwapFirstPage,
	swapLastPage: SwapLastPage
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tabpages/methods/RemovePageMethods.js
var RemovePageMethods_default = {
	removePage(key, destroyChild) {
		if (typeof key === "number") key = this.getPageKey(key);
		var tabs = this.childrenMap.tabs;
		var tabGameObject = tabs.getByName(key);
		var pages = this.childrenMap.pages;
		var pageGameObject = pages.getElement(key);
		if (!tabGameObject || !pageGameObject) return this;
		pages.removeChildrenMap(key);
		tabs.removeButton(tabGameObject, destroyChild);
		pages.remove(pageGameObject, destroyChild);
		return this;
	},
	removeAllPages(destroyChild) {
		var buttons = this.getElement("tabs.buttons");
		for (var i = buttons.length - 1; i >= 0; i--) this.removePage(buttons[i].name, destroyChild);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tabpages/methods/GetPage.js
var GetPage = function(key) {
	if (typeof key === "number") key = this.getPageKey(key);
	return this.childrenMap.pages.getPage(key);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tabpages/methods/GetTab.js
var GetTab = function(key) {
	var index;
	if (typeof key === "number") index = key;
	else index = this.getPageIndex(key);
	return this.getElement("tabs.buttons")[index];
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tabpages/methods/SetTabPosition.js
var TabsPositionToIndex = {
	top: 1,
	left: 3,
	right: 5,
	bottom: 7
};
var TabsPositionToTabsPaddingKey = {
	top: "bottom",
	left: "right",
	right: "left",
	bottom: "top"
};
var SetTabPosition = function(tabsPosition) {
	var newIndex = TabsPositionToIndex[tabsPosition];
	if (newIndex === void 0) return this;
	var tabs = this.childrenMap.tabs;
	var currentIndex = this.sizerChildren.indexOf(tabs);
	if (currentIndex === newIndex) return this;
	this.sizerChildren[currentIndex] = null;
	this.sizerChildren[newIndex] = tabs;
	var tabPadding = this.getSizerConfig(tabs).padding;
	var currentPaddingKey = TabsPositionToTabsPaddingKey[this.tabsPosition];
	var newPaddingKey = TabsPositionToTabsPaddingKey[tabsPosition];
	tabPadding[newPaddingKey] = tabPadding[currentPaddingKey];
	tabPadding[currentPaddingKey] = 0;
	this.tabsPosition = tabsPosition;
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tabpages/methods/TabPaddingMethods.js
var TabPaddingMethods_default = {
	setTabsPadding(key, value) {
		this.childrenMap.tabs.setOuterPadding(key, value);
		return this;
	},
	getTabsPadding(key) {
		return this.childrenMap.tabs.getOuterPadding(key);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tabpages/methods/Methods.js
var methods$7 = {
	getPageKey: GetPageKeyByIndex,
	getPageIndex: GetPageIndexByKey,
	addPage: AddPage,
	getPage: GetPage,
	getTab: GetTab,
	setTabPosition: SetTabPosition
};
Object.assign(methods$7, SwapPageMethods_default, RemovePageMethods_default, TabPaddingMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tabpages/TabPages.js
var GetValue$75 = __webpack_exports__Utils.Objects.GetValue;
var SizerAdd = GridSizer.prototype.add;
var TabPages$1 = class extends GridSizer {
	constructor(scene, config) {
		if (config === void 0) config = {};
		config.column = 3;
		config.row = 3;
		config.columnProportions = [
			0,
			0,
			0
		];
		config.rowProportions = [
			0,
			0,
			0
		];
		var expandPages = GetValue$75(config, "expand.pages", true);
		if (expandPages) {
			config.columnProportions[1] = 1;
			config.rowProportions[1] = 1;
		}
		super(scene, config);
		this.type = "rexTabPages";
		var background = GetValue$75(config, "background", void 0);
		if (background) this.addBackground(background);
		var pages = new Pages(scene, GetValue$75(config, "pages"));
		scene.add.existing(pages);
		var tabsPosition = GetValue$75(config, "tabsPosition", void 0);
		if (tabsPosition === void 0) tabsPosition = GetValue$75(config, "tabPosition", "top");
		var wrapTabs = GetValue$75(config, "wrapTabs", false);
		var ButtonsClass = wrapTabs ? Buttons : Buttons$1;
		var tabsConfig = GetValue$75(config, "tabs", void 0);
		if (tabsConfig === void 0) tabsConfig = {};
		tabsConfig.orientation = tabsPosition === "top" || tabsPosition === "bottom" ? "x" : "y";
		tabsConfig.buttonsType = "radio";
		if (!wrapTabs && !tabsConfig.hasOwnProperty("expand")) tabsConfig.expand = GetValue$75(config, "expand.tabs", false);
		var tabs = new ButtonsClass(scene, tabsConfig);
		scene.add.existing(tabs);
		SizerAdd.call(this, pages, {
			column: 1,
			row: 1,
			expand: expandPages
		});
		var tabColumnIndex, tabRowIndex;
		var tabPadding = GetValue$75(config, "space.item", 0);
		switch (tabsPosition) {
			case "top":
				tabColumnIndex = 1;
				tabRowIndex = 0;
				tabPadding = { bottom: tabPadding };
				break;
			case "bottom":
				tabColumnIndex = 1;
				tabRowIndex = 2;
				tabPadding = { top: tabPadding };
				break;
			case "left":
				tabColumnIndex = 0;
				tabRowIndex = 1;
				tabPadding = { right: tabPadding };
				break;
			case "right":
				tabColumnIndex = 2;
				tabRowIndex = 1;
				tabPadding = { left: tabPadding };
		}
		SizerAdd.call(this, tabs, {
			column: tabColumnIndex,
			row: tabRowIndex,
			padding: tabPadding,
			expand: wrapTabs ? true : GetValue$75(config, "expand.tabs", false),
			align: GetValue$75(config, "align.tabs", "left")
		});
		this.addChildrenMap("background", background);
		this.addChildrenMap("tabs", tabs);
		this.addChildrenMap("pages", pages);
		this.tabsPosition = tabsPosition;
		tabs.on("button.click", function(tab) {
			var key = tab.name;
			if (pages.hasPage(key)) pages.swapPage(key);
		});
		tabs.on("button.statechange", function(tab, index, value, previousValue) {
			var eventName = value ? "tab.focus" : "tab.blur";
			this.emit(eventName, tab, tab.name);
		}, this);
		pages.on("pagevisible", function(pageObject, key, pages) {
			this.emit("page.focus", pageObject, key);
		});
		pages.on("pageinvisible", function(pageObject, key, pages) {
			this.emit("page.blur", pageObject, key);
		});
	}
	get currentKey() {
		return this.getElement("pages").currentKey;
	}
	set currentKey(key) {
		this.showPage(key);
	}
	get keys() {
		return this.getElement("pages").keys;
	}
	get currentPage() {
		return this.getElement("pages").currentPage;
	}
	get previousPage() {
		return this.getElement("pages").previousPage;
	}
};
Object.assign(TabPages$1.prototype, methods$7);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tabpages/Factory.js
ObjectFactory.register("tabPages", function(config) {
	var gameObject = new TabPages$1(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.TabPages", TabPages$1);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/folder/methods/ChildTransition.js
var Transition = class extends OpenCloseTransition {
	constructor(gameObject, config) {
		if (config === void 0) config = {};
		config.destroy = false;
		super(gameObject, config);
	}
	onOpen() {
		this.emit("open", this.parent, this);
		super.onOpen();
	}
	onClose() {
		this.emit("close", this.parent, this);
		super.onClose();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/folder/methods/ExpandMethods.js
var ExpandMethods_default = {
	expand(duration) {
		if (this.expanded === true) return this;
		if (duration === void 0) duration = this.transitionDuration;
		this.expanded = true;
		var title = this.childrenMap.title;
		var child = this.childrenMap.child;
		this.show(child);
		if (this.reLayoutEnable) (this.reLayoutTarget ? this.reLayoutTarget : this.getTopmostSizer()).layout();
		title.emit("folder.expand", duration, this);
		child.emit("folder.expand", duration, this);
		this.emit("expand.start", this);
		this.childTransition.once("open", function() {
			this.emit("expand.complete", this);
		}, this).requestOpen(null, duration);
		return this;
	},
	collapse(duration) {
		if (this.expanded === false) return this;
		if (duration === void 0) duration = this.transitionDuration;
		this.expanded = false;
		var title = this.childrenMap.title;
		var child = this.childrenMap.child;
		title.emit("folder.collapse", duration, this);
		child.emit("folder.collapse", duration, this);
		this.emit("collapse.start", this);
		this.childTransition.once("close", function() {
			this.setChildScale(child, 1, 1).hide(child);
			if (this.reLayoutEnable) (this.reLayoutTarget ? this.reLayoutTarget : this.getTopmostSizer()).layout();
			this.emit("collapse.complete", this);
		}, this).requestClose(null, duration);
		return this;
	},
	toggle(duration) {
		if (this.expanded) this.collapse(duration);
		else this.expand(duration);
		return this;
	},
	setExpandedState(expanded) {
		this.reLayoutEnable = false;
		if (expanded === void 0) this.expanded = void 0;
		else if (expanded) this.expand(0);
		else this.collapse(0);
		this.reLayoutEnable = true;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/folder/methods/ConfigurationMethods.js
var DefaultExpandCallback = function(gameObject, duration) {
	methods$33.popUp.call(gameObject, duration, this.expandDirection);
};
var DefaultCollapseCallback = function(gameObject, duration) {
	methods$33.scaleDown.call(gameObject, duration, this.expandDirection);
};
var ConfigurationMethods_default$2 = {
	setTransitionDuration(duration) {
		this.transitionDuration = duration;
		this.childTransition.setTransitInTime(duration).setTransitOutTime(duration);
		return this;
	},
	setExpandCallback(callback) {
		if (callback === void 0) callback = DefaultExpandCallback.bind(this);
		this.childTransition.setTransitInCallback(callback);
		return this;
	},
	setCollapseCallback(callback) {
		if (callback === void 0) callback = DefaultCollapseCallback.bind(this);
		this.childTransition.setTransitOutCallback(callback);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/folder/Folder.js
var GetValue$74 = __webpack_exports__Utils.Objects.GetValue;
var Folder$1 = class extends Sizer {
	constructor(scene, config) {
		if (config === void 0) config = {};
		if (!config.hasOwnProperty("orientation")) config.orientation = 1;
		super(scene, config);
		this.type = "rexFolder";
		this.reLayoutEnable = true;
		this.expanded = void 0;
		this.expandDirection = this.orientation === 1 ? "y" : "x";
		var background = config.background;
		var title = config.title;
		var child = config.child;
		if (background) this.addBackground(background);
		var spaceConfig = GetValue$74(config, "space");
		var defaultAlign = this.orientation === 1 ? "left" : "top";
		var align = GetValue$74(config, "align.title", defaultAlign);
		var expand = GetValue$74(config, "expand.title", true);
		this.add(title, {
			proportion: 0,
			align,
			expand,
			padding: {
				left: GetValue$74(spaceConfig, "titleLeft", 0),
				right: GetValue$74(spaceConfig, "titleRight", 0),
				top: GetValue$74(spaceConfig, "titleTop", 0),
				bottom: GetValue$74(spaceConfig, "titleBottom", 0)
			}
		});
		var toggleByTarget = GetValue$74(config, "toggleByTarget", void 0);
		var toggleClickConfig = GetValue$74(config, "toggleClickConfig");
		if (toggleByTarget === void 0) toggleByTarget = title;
		if (toggleByTarget) OnClick(toggleByTarget, function() {
			this.toggle();
		}, this, toggleClickConfig);
		this.childTransition = new Transition(child);
		if (!GetValue$74(config, "customChildOrigin", false)) {
			var origin = !this.rtl ? 0 : 1;
			child.setOrigin(origin);
		}
		var align = GetValue$74(config, "align.child", "left");
		var expand = GetValue$74(config, "expand.child", true);
		var proportion = expand ? 1 : 0;
		this.add(child, {
			proportion,
			align,
			expand,
			padding: {
				left: GetValue$74(spaceConfig, "childLeft", 0),
				right: GetValue$74(spaceConfig, "childRight", 0),
				top: GetValue$74(spaceConfig, "childTop", 0),
				bottom: GetValue$74(spaceConfig, "childBottom", 0)
			}
		});
		this.addChildrenMap("title", title);
		this.addChildrenMap("child", child);
		this.addChildrenMap("background", background);
		var transitionConfig = config.transition;
		this.setTransitionDuration(GetValue$74(transitionConfig, "duration", 200));
		this.setExpandCallback(GetValue$74(transitionConfig, "expandCallback", void 0));
		this.setCollapseCallback(GetValue$74(transitionConfig, "collapseCallback", void 0));
		this.reLayoutTarget = GetValue$74(config, "reLayoutTarget", void 0);
		var onExpandStart = config.onExpandStart;
		if (onExpandStart) this.on("expand.start", onExpandStart);
		var onExpandComplete = config.onExpandComplete;
		if (onExpandComplete) this.on("expand.complete", onExpandComplete);
		var onCollapseStart = config.onCollapseStart;
		if (onCollapseStart) this.on("collapse.start", onCollapseStart);
		var onCollapseComplete = config.onCollapseComplete;
		if (onCollapseComplete) this.on("collapse.complete", onCollapseComplete);
		var expanded = GetValue$74(config, "expanded", void 0);
		if (expanded !== void 0) this.setExpandedState(expanded);
	}
};
Object.assign(Folder$1.prototype, ExpandMethods_default, ConfigurationMethods_default$2);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/folder/Factory.js
ObjectFactory.register("folder", function(config) {
	var gameObject = new Folder$1(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.Folder", Folder$1);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/methods/PointToChild.js
var PointToChild$1 = function(x, y, preTest, postTest) {
	for (var nodeKey in this.treesMap) {
		var tree = this.treesMap[nodeKey];
		if (ContainsPoint$1(tree.nodeBody, x, y, preTest, postTest)) return tree;
		var child = tree.pointToChild(x, y, preTest, postTest);
		if (child) return child;
	}
	return null;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/methods/SetChildrenInteractive.js
var SetChildrenInteractive = function(config) {
	if (config === void 0) config = {};
	config.targetMode = "parent";
	config.targetSizers = [this];
	SetChildrenInteractive$1(this, config);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/tree/node/NodeBase.js
var ExtendNodeClass = function(GOClass) {
	return class Base extends GOClass {
		get isTree() {
			return false;
		}
		get isNode() {
			return false;
		}
		setText(text) {
			this.text = text;
			return this;
		}
		get text() {
			return this.nodeBody.text;
		}
		set text(value) {
			var textObject = this.nodeBody;
			if (textObject.setText) textObject.setText(value);
		}
		setTexture(key, frame) {
			var imageObject = this.nodeBody;
			if (imageObject.setTexture) imageObject.setTexture(key, frame);
			return this;
		}
		get texture() {
			var imageObject = this.nodeBody;
			if (!imageObject) return;
			return imageObject.texture;
		}
		get frame() {
			var imageObject = this.nodeBody;
			if (!imageObject) return;
			return imageObject.frame;
		}
	};
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/tree/methods/PointToChild.js
var PointToChild = function(x, y, preTest, postTest) {
	for (var nodeKey in this.nodesMap) {
		var node = this.nodesMap[nodeKey];
		if (this.isTreeObject(node)) {
			var tree = node;
			if (ContainsPoint$1(tree.nodeBody, x, y, preTest, postTest)) return tree;
			var child = tree.pointToChild(x, y, preTest, postTest);
			if (child) return child;
		} else if (ContainsPoint$1(node, x, y, preTest, postTest)) return node;
	}
	return null;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/tree/methods/ParentMethods.js
var ParentMethods_default = {
	getTreePatent(gameObject) {
		if (gameObject === void 0) gameObject = this;
		if (!gameObject.rexSizer) return;
		return gameObject.rexSizer.treeParent;
	},
	getTreeRoot(gameObject) {
		if (gameObject === void 0) gameObject = this;
		var treeParent;
		while (1) {
			if (!gameObject.rexSizer) return;
			treeParent = gameObject.rexSizer.treeParent;
			if (treeParent === null) return gameObject;
			gameObject = treeParent;
		}
	},
	getTreesSizer(gameObject) {
		var root = this.getTreeRoot(gameObject);
		return root ? root.getParentSizer() : null;
	},
	isGrandsonNode(gameObject) {
		if (gameObject === void 0 || !gameObject.rexSizer) return false;
		var treeParent;
		while (1) {
			if (!gameObject.rexSizer) return false;
			treeParent = gameObject.rexSizer.treeParent;
			if (treeParent === this) return true;
			else if (treeParent === null) return false;
			gameObject = treeParent;
		}
		return false;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/builders/CreateGameObjectFromConfig.js
var CreateGameObjectFromConfig = function(scene, config, callbackData, defaultCallback, isRequired = false) {
	var gameObject;
	if (IsGameObject(config)) {
		gameObject = config;
		delete config[key];
	} else if (IsFunction(config)) {
		gameObject = config(scene, callbackData);
		if (gameObject) scene.add.existing(gameObject);
	} else if ((isRequired || config) && defaultCallback) gameObject = defaultCallback(scene, config, callbackData);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/builders/DefaultCreateBackgroundCallback.js
var DefaultCreateBackgroundCallback = function(scene, config, createCallbackData) {
	return CreateBackground$2(scene, config);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/builders/DefaultCreateNodeBodyCallback.js
var DefaultCreateNodeBodyCallback = function(scene, config, createCallbackData) {
	var gameObject = new SimpleLabel(scene, config);
	scene.add.existing(gameObject);
	gameObject.resetDisplayContent("");
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/tree/node/Node.js
var GetValue$73 = __webpack_exports__Utils.Objects.GetValue;
var Node = class extends ExtendNodeClass(Sizer) {
	constructor(scene, config, createCallbackData) {
		var spaceConfig = config.space;
		super(scene, {
			orientation: "x",
			space: {
				left: GetValue$73(spaceConfig, "nodeLeft", 0),
				right: GetValue$73(spaceConfig, "nodeRight", 0),
				top: GetValue$73(spaceConfig, "nodeTop", 0),
				bottom: GetValue$73(spaceConfig, "nodeBottom", 0)
			}
		});
		this.type = "rexTreeNode";
		var background = CreateGameObjectFromConfig(scene, GetValue$73(config, "nodeBackground"), createCallbackData, DefaultCreateBackgroundCallback, false);
		var nodeBody = CreateGameObjectFromConfig(scene, GetValue$73(config, "nodeBody"), createCallbackData, DefaultCreateNodeBodyCallback, true);
		if (background) this.addBackground(background);
		this.add(nodeBody, { proportion: 1 });
		this.nodeBody = nodeBody;
		this.addChildrenMap("background", background);
		this.addChildrenMap("nodeBody", nodeBody);
	}
	get isNode() {
		return true;
	}
	getTreePatent() {
		return this.rexSizer.treeParent;
	}
	getTreeRoot() {
		var treeParent = this.rexSizer.treeParent;
		if (!treeParent) return null;
		return treeParent.getTreeRoot();
	}
	getTreesSizer() {
		var root = this.getTreeRoot();
		return root ? root.getParentSizer() : null;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/methods/SyncDisplayList.js
var SyncDisplayList = function(sourceGameObject, targetGameObject) {
	var p3Container = sourceGameObject.parentContainer;
	if (p3Container) {
		if (targetGameObject.isRexContainerLite) targetGameObject.addToContainer(p3Container);
		else p3Container.add(targetGameObject);
		return;
	}
	var layer = sourceGameObject.displayList;
	if (IsLayerGameObject(layer)) {
		if (targetGameObject.isRexContainerLite) targetGameObject.addToLayer(layer);
		else layer.add(targetGameObject);
		return;
	}
	sourceGameObject.scene.add.existing(targetGameObject);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/tree/methods/AddNodeMethods.js
var UUID$1 = __webpack_exports__Utils.String.UUID;
var AddNodeMethods_default = {
	addTree(config, nodeKey) {
		if (IsGameObject(config)) config = {
			nodeBody: config,
			nodeKey
		};
		return this.insertTree(void 0, config);
	},
	insertTree(index, config) {
		var nodeKey;
		if (typeof config === "string") {
			nodeKey = config;
			config = void 0;
		} else if (config) {
			nodeKey = config.nodeKey;
			delete config.nodeKey;
		}
		if (nodeKey === void 0) nodeKey = UUID$1();
		var tree = this.createTree(config);
		SyncDisplayList(this, tree);
		this.insertNode(index, tree, { expand: true });
		tree._postAddCallback();
		return tree;
	},
	addNode(gameObject, config) {
		return this.insertNode(void 0, gameObject, config);
	},
	insertNode(index, gameObject, config) {
		var nodeKey;
		if (!IsGameObject(gameObject)) {
			config = gameObject;
			gameObject = new Node(this.scene, this.configSave, { isLeaf: true });
		}
		if (typeof config === "string") {
			nodeKey = config;
			config = void 0;
		} else if (config) {
			nodeKey = config.nodeKey;
			delete config.nodeKey;
		}
		if (nodeKey === void 0) nodeKey = UUID$1();
		if (config === void 0) config = {};
		if (!config.hasOwnProperty("expand")) config.expand = true;
		this.removeNode(gameObject, false);
		gameObject.rexSizer.treeParent = this;
		if (this.nodesMap.hasOwnProperty(nodeKey)) {
			console.error(`[Trees] Duplicate nodeKey '${nodeKey}'`);
			this.removeNode(this.nodesMap[nodeKey], true);
		}
		gameObject.nodeKey = nodeKey;
		this.nodesMap[nodeKey] = gameObject;
		this.childrenMap.child.insert(index, gameObject, config);
		return gameObject;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/tree/methods/RemoveNodeMethods.js
var RemoveNodeMethods_default = {
	removeNode(gameObject, destroyChild) {
		if (typeof gameObject === "string") gameObject = this.getTree(gameObject);
		if (!gameObject) return this;
		var treeParent = this.getTreePatent(gameObject);
		if (!treeParent) return this;
		delete treeParent.nodesMap[gameObject.nodeKey];
		gameObject.nodeKey = null;
		gameObject.rexSizer.treeParent = null;
		treeParent.childrenMap.child.remove(gameObject, destroyChild);
		return this;
	},
	removeAllNodes(destroyChild) {
		var nodesMap = this.nodesMap;
		for (var nodeKey in nodesMap) this.removeNode(nodesMap[nodeKey], destroyChild);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/tree/methods/GetNodeMethods.js
var GetNodeMethods_default = {
	getNode(mapNameList) {
		if (typeof mapNameList === "string") mapNameList = mapNameList.split(".");
		if (mapNameList.length === 0) return;
		var name = mapNameList.shift();
		var element = this.nodesMap[name];
		if (mapNameList.length === 0) return element;
		else if (element && this.isTreeObject(element)) return element.getNode(mapNameList);
		else return null;
	},
	getNodes(out) {
		var nodesMap = this.nodesMap;
		if (!out) out = nodes;
		else for (var nodeKey in nodesMap) out.push(nodesMap[nodeKey]);
		return out;
	},
	getAllNodes(out) {}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/tree/methods/Methods.js
var methods$6 = { pointToChild: PointToChild };
Object.assign(methods$6, ParentMethods_default, AddNodeMethods_default, RemoveNodeMethods_default, GetNodeMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/builders/DefaultCreateToggleButtonCallback.js
var DefaultCreateToggleButtonCallback = function(scene, config, createCallbackData) {
	var gameObject = new Triangle_default(scene, config);
	scene.add.existing(gameObject);
	gameObject.on("expand.start", function(gameObject) {
		gameObject.setDirection("down");
	}).on("collapse.complete", function(gameObject) {
		gameObject.setDirection("right");
	});
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/tree/node/CreateTitleSizer.js
var GetValue$72 = __webpack_exports__Utils.Objects.GetValue;
var CreateTitleSizer = function(scene, config) {
	var nodeSizer = new Node(scene, config, { isLeaf: false });
	scene.add.existing(nodeSizer);
	var toggleButton = CreateGameObjectFromConfig(scene, GetValue$72(config, "toggleButton"), { isLeaf: false }, DefaultCreateToggleButtonCallback, true);
	nodeSizer.insert(0, toggleButton, {
		padding: { right: GetValue$72(config, "space.toggleButton", 0) },
		fitRatio: 1
	});
	nodeSizer.addChildrenMap("toggleButton", toggleButton);
	return nodeSizer;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/tree/node/CreateChildrenSizer.js
var GetValue$71 = __webpack_exports__Utils.Objects.GetValue;
var CreateChildrenSizer = function(scene, config) {
	var childrenSizer = new Sizer(scene, { orientation: GetValue$71(config, "childrenOrientation", "y") });
	scene.add.existing(childrenSizer);
	var childrenBackground = CreateGameObjectFromConfig(scene, GetValue$71(config, "childrenBackground"), { isLeaf: false }, DefaultCreateBackgroundCallback, false);
	if (childrenBackground) childrenSizer.addBackground(childrenBackground);
	return childrenSizer;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/methods/Merge.js
var Clone$1 = __webpack_exports__Utils.Objects.Clone;
var Merge$2 = function(defaultConfig, overrideConfig) {
	var config = defaultConfig ? Clone$1(defaultConfig) : {};
	if (!overrideConfig) return config;
	for (var name in overrideConfig) config[name] = overrideConfig[name];
	return config;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/tree/Tree.js
var GetValue$70 = __webpack_exports__Utils.Objects.GetValue;
var Tree = class Tree extends ExtendNodeClass(Folder$1) {
	constructor(scene, config) {
		if (config === void 0) config = {};
		var background = CreateGameObjectFromConfig(scene, GetValue$70(config, "background"), { isLeaf: false }, DefaultCreateBackgroundCallback, false);
		var child = CreateChildrenSizer(scene, config);
		var childrenNodes = child.childrenMap.items;
		var title = CreateTitleSizer(scene, config);
		var toggleButton = title.childrenMap.toggleButton;
		var nodeBody = title.childrenMap.nodeBody;
		var orientation = GetOrientationMode_default(GetValue$70(config, "orientation", "y"));
		var spaceConfig = config.space;
		if (spaceConfig) {
			var indent = GetValue$70(spaceConfig, "indent", 0);
			spaceConfig.childLeft = GetValue$70(spaceConfig, "indentLeft", orientation === 1 ? indent : 0);
			spaceConfig.childRight = GetValue$70(spaceConfig, "indentRight", 0);
			spaceConfig.childTop = GetValue$70(spaceConfig, "indentTop", orientation === 0 ? indent : 0);
			spaceConfig.childBottom = GetValue$70(spaceConfig, "indentBottom", 0);
		}
		var folderConfig = {
			background,
			title,
			child,
			toggleByTarget: toggleButton,
			transition: config.transition,
			orientation,
			space: spaceConfig,
			align: config.align,
			expand: config.expand
		};
		super(scene, folderConfig);
		this.type = "rexTree";
		this.rexSizer.treeParent = null;
		this.nodesMap = {};
		this.configSave = config;
		this.nodeBody = nodeBody;
		this.addChildrenMap("toggleButton", toggleButton);
		this.addChildrenMap("nodeBody", nodeBody);
		this.addChildrenMap("childrenNodes", childrenNodes);
		this.on("expand.start", function() {
			toggleButton.emit("expand.start", toggleButton);
			FireTreesSizerEvent(this, "expand.start");
		}, this).on("expand.complete", function() {
			toggleButton.emit("expand.complete", toggleButton);
			FireTreesSizerEvent(this, "expand.complete");
		}).on("collapse.start", function() {
			toggleButton.emit("collapse.start", toggleButton);
			FireTreesSizerEvent(this, "collapse.start");
		}).on("collapse.complete", function() {
			toggleButton.emit("collapse.complete", toggleButton);
			FireTreesSizerEvent(this, "collapse.complete");
		});
		var tree = this;
		tree._postAddCallback = function() {
			var expanded = GetValue$70(config, "expanded", true);
			if (expanded !== void 0) tree.setExpandedState(expanded);
			delete tree._postAddCallback;
		};
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		this.configSave = void 0;
		Clear(this.nodesMap);
		this.nodesMap = void 0;
		super.destroy(fromScene);
	}
	get isTree() {
		return true;
	}
	createTree(config) {
		return Tree.CreateTree(this.scene, this.configSave, config);
	}
	isTreeObject(gameObject) {
		return gameObject && gameObject instanceof Tree;
	}
};
Tree.CreateTree = function(scene, defaultConfig, overrideConfig) {
	return new Tree(scene, Merge$2(defaultConfig, overrideConfig));
};
var FireTreesSizerEvent = function(tree, eventName) {
	var treesSizer = tree.getTreesSizer();
	if (treesSizer) treesSizer.emit(eventName, tree);
};
Object.assign(Tree.prototype, methods$6);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/methods/AddTreeMethods.js
var UUID = __webpack_exports__Utils.String.UUID;
var AddTreeMethods_default = {
	addTree(config) {
		if (IsGameObject(config)) config = {
			nodeBody: config,
			nodeKey
		};
		return this.insertTree(void 0, config);
	},
	insertTree(index, config) {
		var nodeKey;
		if (typeof config === "string") {
			nodeKey = config;
			config = void 0;
		} else if (config) {
			nodeKey = config.nodeKey;
			delete config.nodeKey;
		}
		if (nodeKey === void 0) nodeKey = UUID();
		var tree = Tree.CreateTree(this.scene, this.treeConfig, config);
		SyncDisplayList(this, tree);
		tree.nodeKey = nodeKey;
		this.treesMap[nodeKey] = tree;
		this.insert(index, tree, { expand: true });
		tree._postAddCallback();
		return tree;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/methods/RemoveTreeMethods.js
var RemoveTreeMethods_default = {
	removeTree(gameObject, destroyChild) {
		if (typeof gameObject === "string") gameObject = this.getTree(gameObject);
		if (!gameObject) return this;
		delete this.treesMap[gameObject.nodeKey];
		gameObject.nodeKey = null;
		this.remove(gameObject, destroyChild);
		return this;
	},
	removeAllNodes(destroyChild) {
		var treesMap = this.treesMap;
		for (var nodeKey in treesMap) this.removeTree(treesMap[nodeKey], destroyChild);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/methods/GetTreeMethods.js
var GetTreeMethods_default = {
	getTree(nodeKey) {
		return this.treesMap[nodeKey];
	},
	getTrees(out) {
		if (out === void 0) out = [];
		for (var nodeKey in this.treesMap) out.push(this.treesMap[nodeKey]);
		return out;
	},
	getNode(nodeKey) {
		var dotIndex = nodeKey.indexOf(".");
		if (dotIndex === -1) return this.getTree(nodeKey);
		var tree = this.getTree(nodeKey.substring(0, dotIndex));
		if (!tree) return;
		return tree.getNode(nodeKey.substring(dotIndex + 1));
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/methods/Methods.js
var methods$5 = {
	pointToChild: PointToChild$1,
	setChildrenInteractive: SetChildrenInteractive
};
Object.assign(methods$5, AddTreeMethods_default, RemoveTreeMethods_default, GetTreeMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/Trees.js
var GetValue$69 = __webpack_exports__Utils.Objects.GetValue;
var Trees = class extends Sizer {
	constructor(scene, config) {
		if (config === void 0) config = {};
		if (!config.hasOwnProperty("orientation")) config.orientation = "y";
		super(scene, config);
		this.type = "rexTrees";
		this.treesMap = {};
		this.treeConfig = GetValue$69(config, "tree");
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		this.treeConfig = void 0;
		Clear(this.treesMap);
		this.treesMap = void 0;
		super.destroy(fromScene);
	}
};
Object.assign(Trees.prototype, methods$5);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/trees/Factory.js
ObjectFactory.register("trees", function(config) {
	var gameObject = new Trees(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.Trees", Trees);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textarea/Factory.js
ObjectFactory.register("textArea", function(config) {
	var gameObject = new TextArea(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.TextArea", TextArea);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textareainput/InjectProperties.js
var InjectProperties = function(inputText) {
	Object.defineProperty(inputText, "childOY", {
		configurable: true,
		get: function() {
			return inputText.textOY;
		},
		set: function(value) {
			inputText.textOY = value;
		}
	});
	Object.defineProperty(inputText, "topChildOY", { get: function() {
		return inputText.topTextOY;
	} });
	Object.defineProperty(inputText, "bottomChildOY", { get: function() {
		return inputText.bottomTextOY;
	} });
	Object.defineProperty(inputText, "childVisibleHeight", { get: function() {
		return inputText.textVisibleHeight;
	} });
	Object.defineProperty(inputText, "childHeight", { get: function() {
		return inputText.contentHeight;
	} });
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textareainput/SetTextMethods.js
var SetTextMethods_default = {
	setText(text) {
		this.childrenMap.child.setText(text);
		return this;
	},
	appendText(text) {
		this.setText(this.text + text);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textareainput/ScrollMethods.js
var ScrollMethods_default = {
	scrollToLine(lineIndex) {
		this.setChildOY(-this.lineHeight * lineIndex);
		return this;
	},
	scrollToNextLine(lineCount) {
		if (lineCount === void 0) lineCount = 1;
		var lineIndex = this.lineIndex + lineCount;
		this.scrollToLine(lineIndex);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textareainput/TextAreaInput.js
var GetValue$68 = __webpack_exports__Utils.Objects.GetValue;
var TextAreaInput = class extends Scrollable$1 {
	constructor(scene, config) {
		if (config === void 0) config = {};
		var inputTextConfig = GetValue$68(config, "text");
		var inputText;
		if (IsGameObject(inputTextConfig)) inputText = inputTextConfig;
		else {
			if (inputTextConfig === void 0) inputTextConfig = {};
			inputTextConfig.textArea = true;
			inputText = new CanvasInput_default(scene, inputTextConfig);
			scene.add.existing(inputText);
		}
		InjectProperties(inputText);
		var expandInputTextWidth = inputTextConfig.width === void 0;
		var expandInputTextHeight = inputTextConfig.height === void 0;
		if (expandInputTextWidth) inputText.minWidth = 0;
		if (expandInputTextHeight) inputText.minHeight = 0;
		config.scrollMode = 0;
		config.type = "rexTextAreaInput";
		config.child = {
			gameObject: inputText,
			expandWidth: expandInputTextWidth,
			expandHeight: expandInputTextHeight
		};
		var spaceConfig = GetValue$68(config, "space", void 0);
		if (spaceConfig) spaceConfig.child = GetValue$68(spaceConfig, "text", 0);
		config.scroller = false;
		super(scene, config);
		this.addChildrenMap("text", inputText);
		var prevTextOY, prevContentHeight;
		inputText.on("cursorin", function() {
			var textOY = inputText.textOY, contentHeight = inputText.contentHeight;
			var isTextOYChanged = prevTextOY !== textOY;
			var isContentHeightChanged = prevContentHeight !== contentHeight;
			prevTextOY = textOY;
			prevContentHeight = contentHeight;
			if (isContentHeightChanged) this.resizeController();
			if (isTextOYChanged || isContentHeightChanged) this.t = inputText.t;
		}, this);
		inputText.on("textchange", function(text) {
			this.emit("textchange", text, this);
		}, this).on("close", function() {
			this.emit("close", this.text, this);
		}, this);
		var content = GetValue$68(config, "content", void 0);
		if (content) this.setText(content);
	}
	get text() {
		return this.childrenMap.child.text;
	}
	set text(value) {
		if (value == null) value = "";
		else value = value.toString();
		if (this.childrenMap.child.text === value) return;
		this.setText(value);
	}
	get lineHeight() {
		return this.childrenMap.child.lineHeight;
	}
	get lineIndex() {
		return Math.floor(-this.childOY / this.lineHeight);
	}
	get linesCount() {
		return this.childrenMap.child.linesCount;
	}
	get contentHeight() {
		return this.childrenMap.child.contentHeight;
	}
	get readOnly() {
		return this.childrenMap.child.readOnly;
	}
	set readOnly(value) {
		var inputText = this.childrenMap.child;
		inputText.readOnly = value;
	}
	setReadOnly(value) {
		this.childrenMap.child.setReadOnly(value);
		return this;
	}
	get value() {
		return this.text;
	}
	set value(value) {
		this.text = value;
	}
};
Object.assign(TextAreaInput.prototype, SetTextMethods_default, ScrollMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textareainput/Factory.js
ObjectFactory.register("textAreaInput", function(config) {
	var gameObject = new TextAreaInput(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.TextAreaInput", TextAreaInput);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/scrollablepanel/Factory.js
ObjectFactory.register("scrollablePanel", function(config) {
	var gameObject = new ScrollablePanel(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.ScrollablePanel", ScrollablePanel);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/toast/methods/DefaultTransitionCallbacks.js
var DefaultTransitionCallbacks_default = {
	popUp: function(gameObject, duration) {
		gameObject.popUp(duration);
	},
	scaleDown: function(gameObject, duration) {
		gameObject.scaleDown(duration);
	},
	fadeIn: function(gameObject, duration) {
		gameObject.fadeIn(duration);
	},
	fadeOut: function(gameObject, duration) {
		gameObject.fadeOut(duration);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/toast/methods/TransitionMode.js
var TransitionMode = {
	popUp: 0,
	fadeIn: 1,
	scaleDown: 0,
	fadeOut: 1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/toast/methods/ConfigurationMethods.js
var ConfigurationMethods_default$1 = {
	setDisplayTime(time) {
		this.displayTime = time;
		return this;
	},
	setTransitOutTime(time) {
		this.transitOutTime = time;
		return this;
	},
	setTransitInTime(time) {
		this.transitInTime = time;
		return this;
	},
	setTransitInCallback(callback) {
		if (typeof callback === "string") callback = TransitionMode[callback];
		switch (callback) {
			case TransitionMode.popUp:
				callback = DefaultTransitionCallbacks_default.popUp;
				break;
			case TransitionMode.fadeIn: callback = DefaultTransitionCallbacks_default.fadeIn;
		}
		if (!callback) callback = NOOP;
		this.transitInCallback = callback;
		return this;
	},
	setTransitOutCallback(callback) {
		if (typeof callback === "string") callback = TransitionMode[callback];
		switch (callback) {
			case TransitionMode.scaleDown:
				callback = DefaultTransitionCallbacks_default.scaleDown;
				break;
			case TransitionMode.fadeOut: callback = DefaultTransitionCallbacks_default.fadeOut;
		}
		if (!callback) callback = NOOP;
		this.transitOutCallback = callback;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/toast/methods/MessageMethods.js
var MessageMethods_default$1 = {
	showMessage(message) {
		if (this.scaleX0 === void 0) this.scaleX0 = this.scaleX;
		if (this.scaleY0 === void 0) this.scaleY0 = this.scaleY;
		if (message === void 0) {
			if (this.messages.length === 0) return this;
			message = this.messages.shift();
		}
		if (this.player.isPlaying) {
			this.messages.push(message);
			return this;
		}
		this.setScale(this.scaleX0, this.scaleY0).setVisible(true);
		if (typeof message === "string") this.setText(message);
		else message(this);
		this.layout();
		var commands = [
			[0, [
				this.transitInCallback,
				this,
				this.transitInTime
			]],
			[0, [
				this.emit,
				"transitin",
				this,
				this.transitInTime
			]],
			[this.transitInTime, [NOOP]],
			[this.displayTime, [
				this.transitOutCallback,
				this,
				this.transitOutTime
			]],
			[0, [
				this.emit,
				"transitout",
				this,
				this.transitOutTime
			]],
			[this.transitOutTime, [this.setVisible, false]],
			[30, [NOOP]]
		];
		this.player.load(commands, this).once("complete", function() {
			this.showMessage();
		}, this).start();
		return this;
	},
	removeAllMessages() {
		this.messages.length = 0;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/toast/Toast.js
var GetValue$67 = __webpack_exports__Utils.Objects.GetValue;
var Toast = class extends Label {
	constructor(scene, config) {
		if (config === void 0) config = { text: createDefaultTextObject(scene) };
		super(scene, config);
		this.type = "rexToast";
		this.setTransitInTime(GetValue$67(config, "duration.in", 200));
		this.setDisplayTime(GetValue$67(config, "duration.hold", 1200));
		this.setTransitOutTime(GetValue$67(config, "duration.out", 200));
		this.setTransitInCallback(GetValue$67(config, "transitIn", TransitionMode.popUp));
		this.setTransitOutCallback(GetValue$67(config, "transitOut", TransitionMode.scaleDown));
		this.player = new Player(this, { dtMode: 1 });
		this.messages = [];
		this.scaleX0 = void 0;
		this.scaleY0 = void 0;
		this.setVisible(false);
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		this.player.destroy();
		this.player = void 0;
		this.messages = void 0;
		super.destroy(fromScene);
	}
	setScale(scaleX, scaleY) {
		if (scaleY === void 0) scaleY = scaleX;
		this.scaleX0 = scaleX;
		this.scaleY0 = scaleY;
		super.setScale(scaleX, scaleY);
		return this;
	}
	get isShowingMessage() {
		return this.player.isPlaying;
	}
};
Object.assign(Toast.prototype, ConfigurationMethods_default$1, MessageMethods_default$1);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/toast/Factory.js
ObjectFactory.register("toast", function(config) {
	var gameObject = new Toast(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.Toast", Toast);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/toastqueue/methods/ConfigurationMethods.js
var QueueDirectionMap = {
	"bottom-to-top": 1,
	"top-to-bottom": 0,
	"right-to-left": 1,
	"left-to-right": 0
};
var ConfigurationMethods_default = {
	setQueueDirection(direction) {
		if (typeof direction === "string") direction = QueueDirectionMap[direction];
		this.rtl = direction === 0;
		if (this.orientation === 1) {
			if (direction) this.queueDirection = 0;
			else this.queueDirection = 1;
		} else if (direction) this.queueDirection = 2;
		else this.queueDirection = 3;
		return this;
	},
	setCreateMessageLabelCallback(callback) {
		this.createMessageLabelCallback = callback;
		return this;
	},
	setDisplayTime(time) {
		this.displayTime = time;
		return this;
	},
	setTransitOutTime(time) {
		this.transitOutTime = time;
		return this;
	},
	setTransitInTime(time) {
		this.transitInTime = time;
		return this;
	},
	setTransitInCallback(callback) {
		if (!callback) callback = NOOP;
		this.transitInCallback = callback;
		return this;
	},
	setTransitOutCallback(callback) {
		if (!callback) callback = NOOP;
		this.transitOutCallback = callback;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/toastqueue/methods/MessageMethods.js
var DelayCall = DelayCallMethods_default$2.delayCall;
var MoveTo = methods$31.moveTo;
var CreateChild = function(parent, callback, message) {
	var child = callback(parent.scene, message, parent);
	OnClick(child, function() {
		parent.removeMessage(child);
	});
	if (parent.displayTime) {
		var delay = parent.transitInTime + parent.displayTime + 10;
		DelayCall.call(child, delay, function() {
			parent.removeMessage(child);
		});
	}
	return child;
};
var GetChildrenPosition = function(parent) {
	var children = parent.childrenMap.items, child;
	var positionList = [];
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		child = children[i];
		positionList.push({
			x: child.x,
			y: child.y
		});
	}
	return positionList;
};
var RunLayout = function(parent) {
	var children = parent.childrenMap.items, child;
	var scaleList = [];
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		child = children[i];
		scaleList.push({
			x: child.scaleX,
			y: child.scaleY
		});
		child.setScale(1);
	}
	parent.layout();
	var newPositionList = GetChildrenPosition(parent);
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var scaleData = scaleList[i];
		children[i].setScale(scaleData.x, scaleData.y);
	}
	return newPositionList;
};
var EaseChildren = function(parent, prevPositionList, newPositionList, duration) {
	var children = parent.childrenMap.items;
	var listLength = Math.min(prevPositionList.length, newPositionList.length, children.length);
	var child, prevPositionData, newPostionData;
	var queueDirection = parent.queueDirection;
	for (var i = 0; i < listLength; i++) {
		child = children[i];
		prevPositionData = prevPositionList[i];
		newPostionData = newPositionList[i];
		child.setPosition(prevPositionData.x, prevPositionData.y);
		switch (queueDirection) {
			case 0:
				if (prevPositionData.y <= newPostionData.y) continue;
				break;
			case 1:
				if (prevPositionData.y >= newPostionData.y) continue;
				break;
			case 2:
				if (prevPositionData.x <= newPostionData.x) continue;
				break;
			case 3: if (prevPositionData.x >= newPostionData.x) continue;
		}
		MoveTo.call(child, duration, newPostionData.x, newPostionData.y);
	}
};
var PushChild = function(parent, child, duration) {
	var prevPositionList = GetChildrenPosition(parent);
	parent.add(child);
	EaseChildren(parent, prevPositionList, RunLayout(parent), duration);
	parent.transitInCallback(child, duration, parent);
};
var MessageMethods_default = {
	showMessage(message) {
		var child = CreateChild(this, this.createMessageLabelCallback, message);
		PushChild(this, child, this.transitInTime);
		return this;
	},
	removeMessage(messageLabel) {
		if (this.getParentSizer(messageLabel) !== this) return this;
		if (messageLabel.__isDestroying) return;
		messageLabel.__isDestroying = true;
		var duration = this.transitOutTime;
		this.transitOutCallback(messageLabel, duration, this);
		DelayCall.call(messageLabel, duration + 10, function() {
			delete messageLabel.__isDestroying;
			messageLabel.destroy();
		});
		return this;
	},
	removeAllMessages() {
		var children = this.childrenMap.items;
		for (var i = 0, cnt = children.length; i < cnt; i++) this.removeMessage(children[i]);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/toastqueue/methods/Methods.js
var Methods$3 = {};
Object.assign(Methods$3, ConfigurationMethods_default, MessageMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/toastqueue/methods/DefaultTransitionMethods.js
var DefaultTransitionMethods_default = {
	transitIn(gameObject, duration, parent) {
		switch (parent.queueDirection) {
			case 0:
				gameObject.setOrigin(.5, 1);
				methods$33.popUp.call(gameObject, duration, "y");
				break;
			case 1:
				gameObject.setOrigin(.5, 0);
				methods$33.popUp.call(gameObject, duration, "y");
				break;
			case 2:
				gameObject.setOrigin(1, .5);
				methods$33.popUp.call(gameObject, duration, "x");
				break;
			case 3:
				gameObject.setOrigin(0, .5);
				methods$33.popUp.call(gameObject, duration, "x");
		}
		methods$32.fadeIn.call(gameObject, duration);
	},
	transitOut(gameObject, duration, parent) {
		methods$32.fadeOut.call(gameObject, duration);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/toastqueue/ToastQueue.js
var GetValue$66 = __webpack_exports__Utils.Objects.GetValue;
var ToastQueue = class extends Sizer {
	constructor(scene, config) {
		if (config === void 0) config = {};
		if (!config.hasOwnProperty("orientation")) config.orientation = 1;
		super(scene, config);
		this.type = "rexToastQueue";
		this.setCreateMessageLabelCallback(GetValue$66(config, "createMessageLabelCallback"));
		this.setQueueDirection(GetValue$66(config, "queueDirection", 1));
		this.setTransitInTime(GetValue$66(config, "duration.in", 200));
		this.setDisplayTime(GetValue$66(config, "duration.hold", 2e3));
		this.setTransitOutTime(GetValue$66(config, "duration.out", 200));
		this.setTransitInCallback(GetValue$66(config, "transitIn", DefaultTransitionMethods_default.transitIn));
		this.setTransitOutCallback(GetValue$66(config, "transitOut", DefaultTransitionMethods_default.transitOut));
	}
};
Object.assign(ToastQueue.prototype, Methods$3);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/toastqueue/Factory.js
ObjectFactory.register("toastQueue", function(config) {
	var gameObject = new ToastQueue(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.ToastQueue", ToastQueue);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/colorinput/colorinputbase/methods/CreateSwatch.js
var CreateSwatch = function(scene, config) {
	if (config === false) return null;
	else if (IsGameObject(config)) return config;
	var swatch = new RoundRectangle_default(scene, config);
	scene.add.existing(swatch);
	return swatch;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/color/GetHexColorString.js
var Pad = __webpack_exports__Utils.String.Pad;
var GetHexColorString = function(value, prefix) {
	if (prefix === void 0) prefix = "0x";
	var colorString = value.toString(16).toUpperCase();
	colorString = Pad(colorString, 6, 0, 1);
	colorString = prefix + colorString;
	return colorString;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/colorinput/colorinputbase/methods/SetSwatchColor.js
var SetSwatchColor = function(swatch, color) {
	if (!swatch) return;
	if (swatch.setTint) swatch.setTint(color);
	else if (swatch.setFillStyle) swatch.setFillStyle(color);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/colorinput/colorinputbase/ColorInputBase.js
var GetValue$65 = __webpack_exports__Utils.Objects.GetValue;
var IsPlainObject$6 = __webpack_exports__Utils.Objects.IsPlainObject;
var Clamp$4 = __webpack_exports__Math.Clamp;
var ColorInput$1 = class extends Sizer {
	constructor(scene, config) {
		if (config === void 0) config = {};
		config.orientation = 0;
		super(scene, config);
		this.type = "rexColorInputLite";
		var background = GetValue$65(config, "background", void 0);
		var swatchConfig = GetValue$65(config, "swatch");
		var swatchSize;
		if (IsPlainObject$6(swatchConfig)) swatchSize = GetValue$65(swatchConfig, "size");
		var swatch = CreateSwatch(scene, GetValue$65(config, "swatch"));
		var inputTextConfig = GetValue$65(config, "inputText", {});
		var inputText;
		if (inputTextConfig) inputText = CreateInputText$1(scene, inputTextConfig);
		if (background) this.addBackground(background);
		if (swatch) {
			swatchSize = GetValue$65(config, "swatchSize", swatchSize);
			var squareExpandSwatch;
			if (swatchSize !== void 0) {
				ResizeGameObject(swatch, swatchSize, swatchSize);
				squareExpandSwatch = false;
			} else squareExpandSwatch = GetValue$65(config, "squareExpandSwatch", true);
			var fitRatio = squareExpandSwatch ? 1 : 0;
			this.add(swatch, {
				proportion: 0,
				expand: true,
				fitRatio
			});
		}
		if (inputText) {
			var proportion = GetValue$65(inputTextConfig, "width") === void 0 ? 1 : 0;
			var expand = GetValue$65(inputTextConfig, "height") === void 0 ? true : false;
			this.add(inputText, {
				proportion,
				expand
			});
		}
		this.addChildrenMap("background", background);
		this.addChildrenMap("swatch", swatch);
		this.addChildrenMap("inputText", inputText);
		if (inputText) inputText.on("close", function() {
			this.setValue(inputText.value);
		}, this);
		var callback = GetValue$65(config, "valuechangeCallback", null);
		if (callback !== null) {
			var scope = GetValue$65(config, "valuechangeCallbackScope", void 0);
			this.on("valuechange", callback, scope);
		}
		this.setValue(GetValue$65(config, "value", 0));
	}
	get value() {
		return this._value;
	}
	set value(value) {
		if (typeof value === "string") {
			value = ColorStringToInteger(value);
			if (value == null) {
				var inputText = this.childrenMap.inputText;
				if (inputText) inputText.setText(GetHexColorString(this._value));
				return;
			}
		} else value = Clamp$4(Math.floor(value), 0, 16777215);
		if (this._value === value) return;
		this._value = value;
		var swatch = this.childrenMap.swatch;
		if (swatch) SetSwatchColor(swatch, value);
		var inputText = this.childrenMap.inputText;
		if (inputText) inputText.setText(GetHexColorString(value));
		this.emit("valuechange", this._value);
	}
	setValue(value) {
		this.value = value;
		return this;
	}
	get color() {
		return this._value;
	}
	set color(color) {
		this.value = color;
	}
	setColor(color) {
		this.color = color;
		return this;
	}
	get readOnly() {
		return this._readOnly;
	}
	set readOnly(value) {
		var inputText = this.childrenMap.inputText;
		if (inputText) inputText.setReadOnly(value);
		this._readOnly = value;
	}
	setReadOnly(enable) {
		if (enable === void 0) enable = true;
		this.readOnly = enable;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/colorinput/colorinput/methods/ConfigurationMethods.js
var methods$4 = {
	setCreateColorPickerBackgroundCallback(callback) {
		this.colorPickerCreateBackgroundCallback = callback;
		return this;
	},
	setColorPickerHPalettePosition(position) {
		this.colorPickerHPalettePosition = position;
		return this;
	},
	setColorPickerExpandDirection(direction) {
		if (typeof direction === "string") direction = ColorPickerExpandDirections[direction];
		this.colorPickerExpandDirection = direction;
		return this;
	},
	setColorPickerEaseInDuration(duration) {
		if (duration === void 0) duration = 0;
		this.colorPickerEaseInDuration = duration;
		return this;
	},
	setColorPickerEaseOutDuration(duration) {
		if (duration === void 0) duration = 0;
		this.colorPickerEaseOutDuration = duration;
		return this;
	},
	setColorPickerTransitInCallback(callback) {
		this.colorPickerTransitInCallback = callback;
		return this;
	},
	setColorPickerTransitOutCallback(callback) {
		this.colorPickerTransitOutCallback = callback;
		return this;
	},
	setColorPickerBounds(bounds) {
		this.colorPickerBounds = bounds;
		return this;
	},
	setColorPickerWidth(width) {
		this.colorPickerWidth = width;
		return this;
	},
	setColorPickerHeight(height) {
		this.colorPickerHeight = height;
		return this;
	},
	setColorPickerSize(width, height) {
		this.setColorPickerWidth(width).setColorPickerHeight(height);
		return this;
	},
	setColorPickerSpace(space) {
		if (space === void 0) space = {};
		this.colorPickerSpace = space;
		return this;
	},
	setColorComponentsHeight(height) {
		this.colorComponentsHeight = height;
		return this;
	},
	setColorComponentsFormatLabelConfig(config) {
		this.colorComponentsFormatLabelConfig = config;
		return this;
	},
	setColorComponentsInputTextConfig(config) {
		this.colorComponentsInputTextConfig = config;
		return this;
	},
	setColorComponentsSpace(space) {
		if (space === void 0) space = {};
		this.colorComponentsSpace = space;
		return this;
	}
};
var ColorPickerExpandDirections = {
	down: 0,
	up: 1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/canvas/DrawHSVPalette.js
var Color$3 = __webpack_exports__Display.Color;
var Percent$2 = __webpack_exports__Math.Percent;
var DrawHPalette = function(canvas, context, verticalMode) {
	if (verticalMode === void 0) verticalMode = false;
	var width = canvas.width;
	var height = canvas.height;
	var color = new Color$3();
	if (verticalMode) for (var iy = 0; iy < height; iy++) {
		var h = Percent$2(iy, 0, height);
		color.setFromHSV(h, 1, 1);
		context.fillStyle = color.rgba;
		context.fillRect(0, iy, width, 1);
	}
	else for (var ix = 0; ix < width; ix++) {
		var h = Percent$2(ix, 0, width);
		color.setFromHSV(h, 1, 1);
		context.fillStyle = color.rgba;
		context.fillRect(ix, 0, 1, height);
	}
};
var DrawSVPalette = function(canvas, context, h) {
	var width = canvas.width;
	var height = canvas.height;
	var imgData = context.getImageData(0, 0, width, height);
	var data = imgData.data;
	var color = new Color$3();
	for (var iy = 0; iy < height; iy++) for (var ix = 0; ix < width; ix++) {
		var s = Percent$2(ix, 0, width);
		var v = 1 - Percent$2(iy, 0, height);
		color.setFromHSV(h, s, v);
		var i = (iy * width + ix) * 4;
		data[i] = color.red;
		data[i + 1] = color.green;
		data[i + 2] = color.blue;
		data[i + 3] = 255;
	}
	context.putImageData(imgData, 0, 0);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/colorinput/colorpicker/methods/HPaletteCanvas.js
var Color$2 = __webpack_exports__Display.Color;
var Percent$1 = __webpack_exports__Math.Percent;
var ColorToRGBA$2 = __webpack_exports__Display.Color.ColorToRGBA;
var HSVToRGB$2 = __webpack_exports__Display.Color.HSVToRGB;
var HPaletteCanvas = class extends Canvas_default {
	constructor(scene, x, y, width, height, orientation) {
		if (x === void 0) x = 0;
		if (y === void 0) y = 0;
		if (width === void 0) width = 2;
		if (height === void 0) height = 2;
		super(scene, x, y, width, height);
		this.type = "rexColorPicker.HPaletteCanvas";
		this.colorObject = new Color$2();
		this.setOrientation(orientation);
		this.setSize(width, height);
	}
	setOrientation(orientation) {
		this.orientation = GetOrientationMode_default(orientation);
		return this;
	}
	updateTexture() {
		DrawHPalette(this.canvas, this.context, this.orientation);
		super.updateTexture();
		return this;
	}
	get color() {
		return this.colorObject.color;
	}
	get hue() {
		return this._hue;
	}
	set hue(value) {
		this._hue = value;
	}
	getHue(localX, localY) {
		if (localX === void 0) return this.hue;
		if (this.orientation === 0) this.hue = Percent$1(localX, 0, this.width);
		else this.hue = Percent$1(localY, 0, this.height);
		return this.hue;
	}
	getColor(localX, localY) {
		if (localX === void 0) return this.color;
		var h = this.getHue(localX, localY);
		this.colorObject.setFromRGB(HSVToRGB$2(h, 1, 1));
		return this.colorObject.color;
	}
	setColor(color) {
		if (this.color === color) return this;
		return this;
	}
	colorToLocalPosition(color, out) {
		if (out === void 0) out = {};
		else if (out === true) {
			if (LocalXY$1 === void 0) LocalXY$1 = {};
			out = LocalXY$1;
		}
		this.colorObject.setFromRGB(ColorToRGBA$2(color));
		if (this.orientation === 0) {
			out.x = this.width * this.colorObject.h;
			out.y = this.height / 2;
		} else {
			out.x = this.width / 2;
			out.y = this.height * this.colorObject.h;
		}
		return out;
	}
};
var LocalXY$1 = void 0;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/colorinput/colorpicker/methods/Transform.js
var RotateAround = __webpack_exports__Math.RotateAround;
var LocalToWorld = function(gameObject, localX, localY, out) {
	if (out === void 0) out = {};
	else if (out === true) {
		if (GlobOut === void 0) GlobOut = {};
		out = GlobOut;
	}
	localX -= gameObject.width * gameObject.originX;
	localY -= gameObject.height * gameObject.originY;
	RotateAround({
		x: localX * gameObject.scaleX,
		y: localY * gameObject.scaleY
	}, 0, 0, -gameObject.rotation);
	out.x = gameObject.x + localX;
	out.y = gameObject.y + localY;
	return out;
};
var GlobOut;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/colorinput/colorpicker/methods/HPalette.js
var HPalette = class extends OverlapSizer {
	constructor(scene, config) {
		if (config === void 0) config = {};
		super(scene, config);
		var orientation = config.width != null ? 1 : 0;
		var paletteCanvas = new HPaletteCanvas(scene).setOrientation(orientation);
		scene.add.existing(paletteCanvas);
		this.type = "rexColorPicker.HPalette";
		paletteCanvas.setInteractive().on("pointerdown", this.onPaletteCanvasPointerDown, this).on("pointermove", this.onPaletteCanvasPointerDown, this);
		var marker = new RoundRectangle_default(scene, {
			strokeColor: 16777215,
			strokeWidth: 2
		});
		scene.add.existing(marker);
		this.add(paletteCanvas, {
			key: "paletteCanvas",
			expand: true
		}).add(marker, {
			key: "marker",
			expand: false
		});
	}
	resize(width, height) {
		if (this.width === width && this.height === height) return this;
		super.resize(width, height);
		var size = Math.min(width, height);
		this.childrenMap.marker.setSize(size, size);
		return this;
	}
	onPaletteCanvasPointerDown(pointer, localX, localY, event) {
		if (!pointer.isDown) return;
		var color = this.childrenMap.paletteCanvas.getColor(localX, localY);
		this.setMarkerPosition(color);
		this.emit("input", color);
	}
	get color() {
		return this.childrenMap.paletteCanvas.color;
	}
	setColor(color) {
		if (this.color === color) return this;
		this.childrenMap.paletteCanvas.setColor(color);
		this.setMarkerPosition(color);
		return this;
	}
	setMarkerPosition(color) {
		var paletteCanvas = this.childrenMap.paletteCanvas;
		var marker = this.childrenMap.marker;
		var localXY = paletteCanvas.colorToLocalPosition(color, true);
		LocalToWorld(paletteCanvas, localXY.x, localXY.y, marker);
		this.resetChildPositionState(marker);
		return this;
	}
	getHue(localX, localY) {
		return this.childrenMap.paletteCanvas.getHue(localX, localY);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/colorinput/colorpicker/methods/SVPaletteCanvas.js
var Color$1 = __webpack_exports__Display.Color;
var Percent = __webpack_exports__Math.Percent;
var ColorToRGBA$1 = __webpack_exports__Display.Color.ColorToRGBA;
var HSVToRGB$1 = __webpack_exports__Display.Color.HSVToRGB;
var SVPaletteCanvas = class extends Canvas_default {
	constructor(scene, x, y, width, height, hue) {
		if (x === void 0) x = 0;
		if (y === void 0) y = 0;
		if (width === void 0) width = 2;
		if (height === void 0) height = 2;
		super(scene, x, y, width, height);
		this.type = "rexColorPicker.SVPaletteCanvas";
		if (hue === void 0) hue = 1;
		this.colorObject = new Color$1();
		this.setHue(hue);
		this.setSize(width, height);
	}
	get color() {
		return this.colorObject.color;
	}
	get hue() {
		return this._hue;
	}
	set hue(hue) {
		if (this._hue === hue) return;
		this._hue = hue;
		this.colorObject.h = hue;
		this.dirty = true;
	}
	setHue(hue) {
		this.hue = hue;
		return this;
	}
	updateTexture() {
		DrawSVPalette(this.canvas, this.context, this.hue);
		super.updateTexture();
		return this;
	}
	getColor(localX, localY) {
		if (localX === void 0) return this.colorObject.color;
		var s = Percent(localX, 0, this.width);
		var v = 1 - Percent(localY, 0, this.height);
		this.colorObject.setFromRGB(HSVToRGB$1(this.hue, s, v));
		return this.colorObject.color;
	}
	setColor(color) {
		if (this.color === color) return this;
		this.colorObject.setFromRGB(ColorToRGBA$1(color));
		this.setHue(this.colorObject.h);
		return this;
	}
	colorToLocalPosition(color, out) {
		if (out === void 0) out = {};
		else if (out === true) {
			if (LocalXY === void 0) LocalXY = {};
			out = LocalXY;
		}
		this.colorObject.setFromRGB(ColorToRGBA$1(color));
		out.x = this.width * this.colorObject.s;
		out.y = this.height * (1 - this.colorObject.v);
		return out;
	}
};
var LocalXY = void 0;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/colorinput/colorpicker/methods/SVPalette.js
var SVPalette = class extends OverlapSizer {
	constructor(scene, config) {
		super(scene, config);
		var paletteCanvas = new SVPaletteCanvas(scene);
		scene.add.existing(paletteCanvas);
		this.type = "rexColorPicker.SVPalette";
		paletteCanvas.setInteractive().on("pointerdown", this.onPaletteCanvasPointerDown, this).on("pointermove", this.onPaletteCanvasPointerDown, this);
		var marker = new RoundRectangle_default(scene, {
			radius: 5,
			strokeColor: 16777215,
			strokeWidth: 2
		});
		scene.add.existing(marker);
		this.add(paletteCanvas, {
			key: "paletteCanvas",
			expand: true
		}).add(marker, {
			key: "marker",
			expand: false
		});
	}
	onPaletteCanvasPointerDown(pointer, localX, localY, event) {
		if (!pointer.isDown) return;
		var color = this.childrenMap.paletteCanvas.getColor(localX, localY);
		this.setMarkerPosition(color);
		this.emit("input", color);
	}
	get color() {
		return this.childrenMap.paletteCanvas.color;
	}
	setHue(hue) {
		this.childrenMap.paletteCanvas.setHue(hue);
		return this;
	}
	setColor(color) {
		if (this.color === color) return this;
		this.childrenMap.paletteCanvas.setColor(color);
		this.setMarkerPosition(color);
		return this;
	}
	setMarkerPosition(color) {
		var paletteCanvas = this.childrenMap.paletteCanvas;
		var marker = this.childrenMap.marker;
		var localXY = paletteCanvas.colorToLocalPosition(color, true);
		LocalToWorld(paletteCanvas, localXY.x, localXY.y, marker);
		this.resetChildPositionState(marker);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/colorinput/colorpicker/ColorPicker.js
var GetValue$64 = __webpack_exports__Utils.Objects.GetValue;
var ColorPicker = class extends Sizer {
	constructor(scene, config) {
		super(scene, config);
		this.type = "rexColorPicker";
		this.freezePalettes = false;
		var hPalettePosition = GetValue$64(config, "hPalette.position", 0);
		if (typeof hPalettePosition === "string") hPalettePosition = HPalettePositionNamesMap[hPalettePosition];
		var orientation = hPalettePosition === 0 || hPalettePosition === 2 ? 1 : 0;
		this.setOrientation(orientation);
		var background = GetValue$64(config, "background", void 0);
		var hPaletteWidth, hPaletteHeight;
		if (this.orientation === 0) {
			var hPaletteWidth = GetValue$64(config, "hPalette.width", void 0);
			if (hPaletteWidth === void 0) hPaletteWidth = GetValue$64(config, "hPalette.size", 10);
		} else {
			hPaletteHeight = GetValue$64(config, "hPalette.height", void 0);
			if (hPaletteHeight === void 0) hPaletteHeight = GetValue$64(config, "hPalette.size", 10);
		}
		var hPalette = new HPalette(scene, {
			width: hPaletteWidth,
			height: hPaletteHeight
		});
		scene.add.existing(hPalette);
		var svPaletteWidth = GetValue$64(config, "svPalette.width", void 0);
		var svPaletteHeight = GetValue$64(config, "svPalette.height", void 0);
		var svPalette = new SVPalette(scene, {
			width: svPaletteWidth,
			height: svPaletteHeight
		});
		scene.add.existing(svPalette);
		if (background) this.addBackground(background);
		var hPaletteAddConfig = {
			proportion: 0,
			expand: true
		};
		var svPaletteProportion, svPaletteExpand;
		if (this.orientation === 0) {
			svPaletteProportion = svPaletteWidth === void 0 ? 1 : 0;
			svPaletteExpand = svPaletteHeight === void 0 ? true : false;
		} else {
			svPaletteProportion = svPaletteHeight === void 0 ? 1 : 0;
			svPaletteExpand = svPaletteWidth === void 0 ? true : false;
		}
		var svPaletteAddConfig = {
			proportion: svPaletteProportion,
			expand: svPaletteExpand
		};
		if (hPalettePosition === 0 || hPalettePosition === 3) this.add(svPalette, svPaletteAddConfig).add(hPalette, hPaletteAddConfig);
		else this.add(hPalette, hPaletteAddConfig).add(svPalette, svPaletteAddConfig);
		hPalette.on("input", function() {
			svPalette.setHue(hPalette.getHue());
			this.setValue(svPalette.color, true);
		}, this);
		svPalette.on("input", function() {
			this.setValue(svPalette.color, true);
		}, this);
		this.addChildrenMap("background", background);
		this.addChildrenMap("hPalette", hPalette);
		this.addChildrenMap("svPalette", svPalette);
		var callback = GetValue$64(config, "valuechangeCallback", null);
		if (callback !== null) {
			var scope = GetValue$64(config, "valuechangeCallbackScope", void 0);
			this.on("valuechange", callback, scope);
		}
		this.setValue(GetValue$64(config, "value", 16777215));
	}
	get value() {
		return this._value;
	}
	set value(value) {
		if (this._value === value) return;
		var oldValue = this._value;
		this._value = value;
		if (!this.freezePalettes) this.updatePalettes();
		this.emit("valuechange", value, oldValue, this);
	}
	setValue(value, freezePalettes) {
		this.freezePalettes = !!freezePalettes;
		this.value = value;
		this.freezePalettes = false;
		return this;
	}
	get color() {
		return this._value;
	}
	set color(color) {
		this.value = color;
	}
	setColor(color) {
		this.color = color;
		return this;
	}
	updatePalettes() {
		this.childrenMap.hPalette.setColor(this.color);
		this.childrenMap.svPalette.setColor(this.color);
		return this;
	}
	postLayout(parent, newWidth, newHeight) {
		this.childrenMap.hPalette.setMarkerPosition(this.value);
		this.childrenMap.svPalette.setMarkerPosition(this.value);
		super.postLayout(parent, newWidth, newHeight);
		return this;
	}
};
var HPalettePositionNamesMap = {
	bottom: 0,
	left: 1,
	top: 2,
	right: 3
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/colorinput/colorcomponents/ColorComponents.js
var GetValue$63 = __webpack_exports__Utils.Objects.GetValue;
var Color = __webpack_exports__Display.Color;
var ColorToRGBA = __webpack_exports__Display.Color.ColorToRGBA;
var HSVToRGB = __webpack_exports__Display.Color.HSVToRGB;
var Clamp$3 = __webpack_exports__Math.Clamp;
var ColorComponents = class extends Sizer {
	constructor(scene, config) {
		if (config === void 0) config = {};
		config.orientation = 0;
		super(scene, config);
		this.type = "rexColorComponents";
		this.colorObject = new Color();
		var background = GetValue$63(config, "background", void 0);
		var formatLabel = GetValue$63(config, "formatLabel", void 0);
		if (!IsGameObject(formatLabel)) formatLabel = CreateLabel(scene, formatLabel).resetDisplayContent();
		var components = [];
		if (config.inputText0 && config.inputText1 && config.inputText2) {
			components.push(config.inputText0);
			components.push(config.inputText1);
			components.push(config.inputText2);
		} else {
			var inputTextConfig = GetValue$63(config, "inputText");
			for (var i = 0; i < 3; i++) {
				var inputText = CreateInputText$1(scene, inputTextConfig).setMaxLength(3).setNumberInput();
				components.push(inputText);
			}
		}
		if (background) this.addBackground(background);
		var proportion = GetValue$63(config, "proportion.formatLabel", 0);
		var defaultExpand = formatLabel.isRexContainerLite ? true : false;
		var expand = GetValue$63(config, "expand.formatLabel", defaultExpand);
		this.add(formatLabel, {
			proportion,
			expand
		});
		var proportion = GetValue$63(inputTextConfig, "width") === void 0 ? 1 : 0;
		var expand = GetValue$63(inputTextConfig, "height") === void 0 ? true : false;
		for (var i = 0, cnt = components.length; i < cnt; i++) this.add(components[i], {
			proportion,
			expand
		});
		this.addChildrenMap("background", background);
		this.addChildrenMap("formatLabel", formatLabel);
		this.addChildrenMap("components", components);
		this.onClick(formatLabel, this.toggleColorFormat, this);
		for (var i = 0, cnt = components.length; i < cnt; i++) components[i].on("close", function() {
			this.updateColorObject();
			this.setValue(this.colorObject.color);
		}, this);
		var callback = GetValue$63(config, "valuechangeCallback", null);
		if (callback !== null) {
			var scope = GetValue$63(config, "valuechangeCallbackScope", void 0);
			this.on("valuechange", callback, scope);
		}
		formatLabel.setText("RGB");
		this.setValue(GetValue$63(config, "value", 16777215));
	}
	get value() {
		return this._value;
	}
	set value(value) {
		value = Clamp$3(Math.floor(value), 0, 16777215);
		if (this._value === value) return;
		this._value = value;
		this.colorObject.setFromRGB(ColorToRGBA(value));
		this.updateComponents();
		this.emit("valuechange", this._value);
	}
	setValue(value) {
		this.value = value;
		return this;
	}
	get color() {
		return this._value;
	}
	set color(color) {
		this.value = color;
	}
	setColor(color) {
		this.color = color;
		return this;
	}
	get colorFormat() {
		return this.childrenMap.formatLabel.text;
	}
	set colorFormat(value) {
		if (this.colorFormat === value) return;
		this.childrenMap.formatLabel.setText(value);
		this.updateComponents();
	}
	setColorFormat(colrType) {
		this.colorFormat = colrType;
		return this;
	}
	toggleColorFormat() {
		this.colorFormat = this.colorFormat === "RGB" ? "HSV" : "RGB";
		return this;
	}
	updateComponents() {
		var components = this.childrenMap.components;
		var value0, value1, value2;
		if (this.colorFormat === "RGB") {
			value0 = this.colorObject.red;
			value1 = this.colorObject.green;
			value2 = this.colorObject.blue;
		} else {
			value0 = Math.floor(this.colorObject.h * 360);
			value1 = Math.floor(this.colorObject.s * 100);
			value2 = Math.floor(this.colorObject.v * 100);
		}
		components[0].setValue(value0);
		components[1].setValue(value1);
		components[2].setValue(value2);
		return this;
	}
	updateColorObject() {
		var components = this.childrenMap.components;
		if (this.colorFormat === "RGB") {
			var red = Clamp$3(components[0].value, 0, 255);
			var green = Clamp$3(components[1].value, 0, 255);
			var blue = Clamp$3(components[2].value, 0, 255);
			this.colorObject.setTo(red, green, blue);
		} else {
			var h = Clamp$3(components[0].value, 0, 359) / 360;
			var s = Clamp$3(components[1].value, 0, 100) / 100;
			var v = Clamp$3(components[2].value, 0, 100) / 100;
			this.colorObject.setFromRGB(HSVToRGB(h, s, v));
		}
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/toucheventstop/TouchEventStop.js
var TouchEventStop_default = toucheventstop_default;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/colorinput/colorinput/methods/ColorPicker.js
var GetValue$62 = __webpack_exports__Utils.Objects.GetValue;
var ColorPickerPanel = class extends Sizer {
	constructor(scene, config) {
		if (config === void 0) config = {};
		config.orientation = 1;
		super(scene, config);
		this.type = "rexColorInput.ColorPickerPanel";
		var background = GetValue$62(config, "background", void 0);
		var colorPicker = new ColorPicker(scene, {
			hPalette: config.hPalette || {},
			svPalette: config.svPalette || {},
			space: { item: GetValue$62(config, "space.hPalette", 8) }
		});
		scene.add.existing(colorPicker);
		var colorComponents;
		if (config.colorComponents) {
			colorComponents = new ColorComponents(scene, config.colorComponents);
			scene.add.existing(colorComponents);
		}
		if (background) {
			this.addBackground(background);
			new TouchEventStop_default(background, { stopAllLevels: false });
		}
		this.add(colorPicker, {
			proportion: 1,
			expand: true
		});
		if (colorComponents) this.add(colorComponents, {
			proportion: 0,
			expand: true
		});
		this.addChildrenMap("background", background);
		this.addChildrenMap("colorPicker", colorPicker);
		this.addChildrenMap("colorComponents", colorComponents);
		colorPicker.on("valuechange", function(value) {
			this.setValue(value);
		}, this);
		if (colorComponents) colorComponents.on("valuechange", function(value) {
			this.setValue(value);
		}, this);
		this.setValue(GetValue$62(config, "value", 16777215));
	}
	get value() {
		return this._value;
	}
	set value(value) {
		if (this._value === value) return;
		this._value = value;
		this.childrenMap.colorPicker.setValue(value);
		var colorComponents = this.childrenMap.colorComponents;
		if (colorComponents) colorComponents.setValue(value);
		this.emit("valuechange", value);
	}
	setValue(value) {
		this.value = value;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/colorinput/colorinput/methods/CreateColorPicker.js
__webpack_exports__Utils.Objects.GetValue;
var CreateColorPicker = function(scene) {
	var scene = this.scene;
	var background;
	var createBackgroundCallback = this.colorPickerCreateBackgroundCallback;
	if (createBackgroundCallback) {
		background = createBackgroundCallback.call(this, scene);
		scene.add.existing(background);
	}
	var width = this.colorPickerWidth;
	if (width === void 0) width = this.width;
	var height = this.colorPickerHeight;
	if (height === void 0) height = width;
	var colorComponentsConfig;
	if (this.colorComponentsHeight > 0) colorComponentsConfig = {
		height: this.colorComponentsHeight,
		formatLabel: this.colorComponentsFormatLabelConfig,
		inputText: this.colorComponentsInputTextConfig,
		space: this.colorComponentsSpace
	};
	else colorComponentsConfig = false;
	var colorPicker = new ColorPickerPanel(scene, {
		width,
		height,
		background,
		space: this.colorPickerSpace,
		hPalette: { position: this.colorPickerHPalettePosition },
		colorComponents: colorComponentsConfig,
		value: this.value
	});
	scene.add.existing(colorPicker);
	return colorPicker;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/colorinput/colorinput/methods/OpenColorPicker.js
var OpenColorPicker = function() {
	if (this.colorPicker) return;
	var colorPicker = CreateColorPicker.call(this).layout();
	var dropDownBehavior = new DropDown_default(colorPicker, {
		duration: {
			in: this.colorPickerEaseInDuration,
			out: this.colorPickerEaseOutDuration
		},
		transitIn: this.colorPickerTransitInCallback,
		transitOut: this.colorPickerTransitOutCallback,
		expandDirection: this.colorPickerExpandDirection,
		alignTargetX: this,
		alignTargetY: this,
		bounds: this.colorPickerBounds,
		touchOutsideClose: true
	}).on("open", function() {
		colorPicker.on("valuechange", function(value) {
			this.setValue(value);
		}, this);
	}, this).on("close", function() {
		this.colorPicker = void 0;
		this.dropDownBehavior = void 0;
	}, this);
	this.colorPicker = colorPicker;
	this.dropDownBehavior = dropDownBehavior;
	this.pin(colorPicker);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/colorinput/colorinput/methods/Methods.js
var methods$3 = { openColorPicker: OpenColorPicker };
Object.assign(methods$3, methods$4);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/colorinput/colorinput/ColorInput.js
var GetValue$61 = __webpack_exports__Utils.Objects.GetValue;
var ColorInput = class extends ColorInput$1 {
	constructor(scene, config) {
		if (config === void 0) config = {};
		super(scene, config);
		this.type = "rexColorInput";
		if (!config.hasOwnProperty("colorPicker")) config.colorPicker = { background: { color: 0 } };
		var colorPickerConfig = config.colorPicker;
		var hasColorPicker = colorPickerConfig !== false && colorPickerConfig !== null;
		if (hasColorPicker) {
			this.setColorPickerSize(GetValue$61(colorPickerConfig, "width", 160), GetValue$61(colorPickerConfig, "height", 170));
			var createBackgroundCallback;
			var background = GetValue$61(colorPickerConfig, "background");
			if (background) createBackgroundCallback = function(scene) {
				return CreateBackground$2(scene, background);
			};
			else createBackgroundCallback = GetValue$61(colorPickerConfig, "createBackgroundCallback");
			this.setCreateColorPickerBackgroundCallback(createBackgroundCallback);
			this.setColorPickerHPalettePosition(GetValue$61(colorPickerConfig, "hPalettePosition", 0));
			this.setColorPickerExpandDirection(GetValue$61(colorPickerConfig, "expandDirection"));
			this.setColorPickerEaseInDuration(GetValue$61(colorPickerConfig, "easeIn", 200));
			this.setColorPickerEaseOutDuration(GetValue$61(colorPickerConfig, "easeOut", 200));
			this.setColorPickerTransitInCallback(GetValue$61(colorPickerConfig, "transitIn"));
			this.setColorPickerTransitOutCallback(GetValue$61(colorPickerConfig, "transitOut"));
			this.setColorPickerBounds(GetValue$61(colorPickerConfig, "bounds"));
			var colorPickerSpaceConfig = GetValue$61(colorPickerConfig, "space");
			if (colorPickerSpaceConfig === void 0) colorPickerSpaceConfig = {
				left: 10,
				right: 10,
				top: 10,
				bottom: 10,
				item: 8
			};
			this.setColorPickerSpace(colorPickerSpaceConfig);
		}
		var colorComponentsConfig = config.colorComponents;
		if (hasColorPicker && colorComponentsConfig !== false && colorComponentsConfig !== null) {
			this.setColorComponentsHeight(GetValue$61(colorComponentsConfig, "height", 30));
			this.setColorComponentsFormatLabelConfig(GetValue$61(colorComponentsConfig, "formatLabel"));
			var colorComponentsInputTextConfig = GetValue$61(colorComponentsConfig, "inputText");
			if (!colorComponentsInputTextConfig) colorComponentsInputTextConfig = GetValue$61(config, "inputText");
			this.setColorComponentsInputTextConfig(colorComponentsInputTextConfig);
			var colorComponentsSpace = GetValue$61(colorComponentsConfig, "space");
			if (colorComponentsSpace === void 0) colorComponentsSpace = { item: 8 };
			this.setColorComponentsSpace(colorComponentsSpace);
		}
		var swatch = this.childrenMap.swatch;
		if (swatch && hasColorPicker) this.onClick(swatch, this.openColorPicker, this);
	}
	get readOnly() {
		return this._readOnly;
	}
	set readOnly(value) {
		var swatch = this.childrenMap.swatch;
		if (swatch) {
			if (value) this.disableClick(swatch);
			else this.enableClick(swatch);
		}
		super.readOnly = value;
	}
};
Object.assign(ColorInput.prototype, methods$3);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/colorinput/colorinput/Factory.js
ObjectFactory.register("colorInput", function(config) {
	var gameObject = new ColorInput(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.ColorInput", ColorInput);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/colorinput/colorinputbase/Factory.js
ObjectFactory.register("colorInputLite", function(config) {
	var gameObject = new ColorInput$1(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.ColorInputBase", ColorInput$1);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/colorinput/colorpicker/Factory.js
ObjectFactory.register("colorPicker", function(config) {
	var gameObject = new ColorPicker(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.ColorPicker", ColorPicker);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/colorinput/colorcomponents/Factory.js
ObjectFactory.register("colorComponents", function(config) {
	var gameObject = new ColorComponents(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.ColorComponents", ColorComponents);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/input/RequestDrag.js
var RequestDrag = function(gameObject) {
	var inputPlugin = gameObject.scene.input;
	var inputManager = inputPlugin.manager;
	var pointersTotal = inputManager.pointersTotal;
	var pointers = inputManager.pointers, pointer;
	for (var i = 0; i < pointersTotal; i++) {
		pointer = pointers[i];
		if (!pointer.primaryDown || inputPlugin.getDragState(pointer) !== 0 || !IsPointerInHitArea(gameObject, pointer)) continue;
		inputPlugin.setDragState(pointer, 1);
		inputPlugin._drag[pointer.id] = [gameObject];
		if (inputPlugin.dragDistanceThreshold === 0 || inputPlugin.dragTimeThreshold === 0) {
			inputPlugin.setDragState(pointer, 3);
			inputPlugin.processDragStartList(pointer);
		} else inputPlugin.setDragState(pointer, 2);
		return true;
	}
	return false;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/input/drag/Drag.js
var GetValue$60 = __webpack_exports__Utils.Objects.GetValue;
var DistanceBetween = __webpack_exports__Math.Distance.Between;
var RotateAroundDistance = __webpack_exports__Math.RotateAroundDistance;
var Drag = class extends ComponentBase {
	constructor(gameObject, config) {
		super(gameObject, { eventEmitter: false });
		this._enable = void 0;
		gameObject.setInteractive(GetValue$60(config, "inputConfig", void 0));
		this.resetFromJSON(config);
		this.boot();
	}
	resetFromJSON(o) {
		this.pointer = void 0;
		this.setEnable(GetValue$60(o, "enable", true));
		this.setAxisMode(GetValue$60(o, "axis", 0));
		this.setAxisRotation(GetValue$60(o, "rotation", 0));
		return this;
	}
	toJSON() {
		return {
			enable: this.enable,
			axis: this.axisMode,
			rotation: this.axisRotation
		};
	}
	boot() {
		var gameObject = this.parent;
		gameObject.on("dragstart", this.onDragStart, this);
		gameObject.on("drag", this.onDrag, this);
		gameObject.on("dragend", this.onDragEnd, this);
	}
	shutdown(fromScene) {
		if (this.isShutdown) return;
		this.pointer = void 0;
		super.shutdown(fromScene);
	}
	get enable() {
		return this._enable;
	}
	set enable(e) {
		if (this._enable === e) return;
		if (!e) this.dragend();
		this._enable = e;
		this.scene.input.setDraggable(this.parent, e);
		return this;
	}
	setEnable(e) {
		if (e === void 0) e = true;
		this.enable = e;
		return this;
	}
	toggleEnable() {
		this.setEnable(!this.enable);
		return this;
	}
	setAxisMode(m) {
		if (typeof m === "string") m = DIRECTIONNODE[m];
		this.axisMode = m;
		return this;
	}
	setAxisRotation(a) {
		this.axisRotation = a;
		return this;
	}
	drag() {
		RequestDrag(this.parent);
		return this;
	}
	dragend() {
		if (!this.isDragging) return;
		this.scene.input.setDragState(this.pointer, 5);
		return this;
	}
	onDragStart(pointer, dragX, dragY) {
		if (this.isDragging) return;
		this.pointer = pointer;
	}
	onDrag(pointer, dragX, dragY) {
		if (this.pointer !== pointer) return;
		var gameObject = this.parent;
		if (this.axisMode === 0) {
			gameObject.x = dragX;
			gameObject.y = dragY;
		} else if (this.axisRotation === 0) {
			if (this.axisMode === 1) gameObject.x = dragX;
			else if (this.axisMode === 2) gameObject.y = dragY;
		} else {
			var dist;
			var p1 = {
				x: dragX,
				y: dragY
			};
			dist = DistanceBetween(p1.x, p1.y, gameObject.x, gameObject.y);
			p1 = RotateAroundDistance(p1, gameObject.x, gameObject.y, -this.axisRotation, dist);
			if (this.axisMode === 1) p1.y = gameObject.y;
			else if (this.axisMode === 2) p1.x = gameObject.x;
			dist = DistanceBetween(p1.x, p1.y, gameObject.x, gameObject.y);
			p1 = RotateAroundDistance(p1, gameObject.x, gameObject.y, this.axisRotation, dist);
			gameObject.x = p1.x;
			gameObject.y = p1.y;
		}
	}
	onDragEnd(pointer, dragX, dragY, dropped) {
		if (this.pointer !== pointer) return;
		this.pointer = void 0;
	}
	get isDragging() {
		return this.pointer !== void 0;
	}
};
var DIRECTIONNODE = {
	"both": 0,
	"h&v": 0,
	"x&y": 0,
	"horizontal": 1,
	"h": 1,
	"x": 1,
	"vertical": 2,
	"v": 2,
	"y": 2
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/splitpanels/methods/OnDragSplitter.js
var OnDragSplitter = function() {
	var firstPanel = this.leftPanel;
	var splitter = this.splitter;
	var secondPanel = this.rightPanel;
	var firstChildSizerPadding = this.getSizerConfig(firstPanel).padding, splitterSizerPadding = this.getSizerConfig(splitter).padding, secondChildSizerPadding = this.getSizerConfig(secondPanel).padding;
	if (this.childrenMap.splitterSizer.orientation === 0) {
		var firstChildInnerLeft = this.innerLeft + firstChildSizerPadding.left;
		var secondChildInnerRight = this.innerRight - secondChildSizerPadding.right;
		var splitterWidth = GetDisplayWidth(splitter);
		var splitterLeft = splitter.x + splitterWidth * (0 - splitter.originX);
		var splitterRight = splitter.x + splitterWidth * (1 - splitter.originX);
		var firstChildInnerRight = splitterLeft - splitterSizerPadding.left - this.space.item - firstChildSizerPadding.right;
		var secondChildInnerLeft = splitterRight + splitterSizerPadding.right + this.space.item + secondChildSizerPadding.left;
		var firstChildInnerWidth = firstChildInnerRight - firstChildInnerLeft;
		var secondChildInnerWidth = secondChildInnerRight - secondChildInnerLeft;
		var totalChildrenInnerWidth = firstChildInnerWidth + secondChildInnerWidth;
		if (this.minFirstChildSize > 0 && firstChildInnerWidth < this.minFirstChildSize) {
			firstChildInnerWidth = this.minFirstChildSize;
			secondChildInnerWidth = totalChildrenInnerWidth - firstChildInnerWidth;
		}
		if (this.minSecondChildSize > 0 && secondChildInnerWidth < this.minSecondChildSize) {
			secondChildInnerWidth = this.minSecondChildSize;
			firstChildInnerWidth = totalChildrenInnerWidth - secondChildInnerWidth;
		}
		this.setSplitRatio(firstChildInnerWidth / totalChildrenInnerWidth);
	} else {
		var firstChildInnerTop = this.innerTop + firstChildSizerPadding.top;
		var secondChildInnerBottom = this.innerBottom - secondChildSizerPadding.bottom;
		var splitterHeight = GetDisplayHeight(splitter);
		var splitterTop = splitter.y + splitterHeight * (0 - splitter.originY);
		var splitterBottom = splitter.y + splitterHeight * (1 - splitter.originY);
		var firstChildInnerBottom = splitterTop - splitterSizerPadding.top - this.space.item - firstChildSizerPadding.bottom;
		var secondChildInnerTop = splitterBottom + splitterSizerPadding.bottom + this.space.item + secondChildSizerPadding.top;
		var firstChildInnerHeight = firstChildInnerBottom - firstChildInnerTop;
		var secondChildInnerHeight = secondChildInnerBottom - secondChildInnerTop;
		var totalChildrenInnerHeight = firstChildInnerHeight + secondChildInnerHeight;
		if (this.minFirstChildSize > 0 && firstChildInnerHeight < this.minFirstChildSize) {
			firstChildInnerHeight = this.minFirstChildSize;
			secondChildInnerHeight = totalChildrenInnerHeight - firstChildInnerHeight;
		}
		if (this.minSecondChildSize > 0 && secondChildInnerHeight < this.minSecondChildSize) {
			secondChildInnerHeight = this.minSecondChildSize;
			firstChildInnerHeight = totalChildrenInnerHeight - secondChildInnerHeight;
		}
		this.setSplitRatio(firstChildInnerHeight / totalChildrenInnerHeight);
	}
	var minWidthSave = this.minWidth, minHeightSave = this.minHeight;
	this.setMinSize(this.width, this.height).layout().setMinSize(minWidthSave, minHeightSave);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/splitpanels/SplitPanels.js
var GetValue$59 = __webpack_exports__Utils.Objects.GetValue;
var Clamp$2 = __webpack_exports__Math.Clamp;
var SplitPanels$1 = class extends Sizer {
	constructor(scene, config) {
		var panelOrientation = config.leftPanel ? 0 : 1;
		if (!config.hasOwnProperty("orientation")) config.orientation = panelOrientation ? 0 : 1;
		super(scene, config);
		this.type = "rexSplit";
		var background = GetValue$59(config, "background", void 0);
		if (background) this.addBackground(background);
		var header = GetValue$59(config, "header", void 0);
		if (header) {
			var align = GetValue$59(config, "align.header", "center");
			var headerSpace = GetValue$59(config, "space.header", 0);
			var padding;
			if (config.orientation === 0) padding = { bottom: headerSpace };
			else padding = { right: headerSpace };
			this.add(header, {
				proportion: 0,
				align,
				padding,
				expand: GetValue$59(config, "expand.header", true)
			});
		}
		var firstChildKey, secondChildKey;
		var minFirstChildSizeKey, minSecondChildSizeKey;
		if (panelOrientation === 0) {
			firstChildKey = "leftPanel";
			secondChildKey = "rightPanel";
			minFirstChildSizeKey = "minLeftPanelWidth";
			minSecondChildSizeKey = "minRightPanelWidth";
		} else {
			firstChildKey = "topPanel";
			secondChildKey = "bottomPanel";
			minFirstChildSizeKey = "minTopPanelHeight";
			minSecondChildSizeKey = "minBottomPanelHeight";
		}
		var firstChild = GetValue$59(config, firstChildKey, void 0);
		var splitter = GetValue$59(config, "splitter", void 0);
		var secondChild = GetValue$59(config, secondChildKey, void 0);
		var spaceConfig = GetValue$59(config, "space", void 0);
		var splitterSizer = new Sizer(scene, { orientation: panelOrientation });
		scene.add.existing(splitterSizer);
		this.add(splitterSizer, {
			proportion: 1,
			expand: true
		});
		splitterSizer.add(firstChild, {
			proportion: 1,
			expand: true,
			padding: {
				left: GetValue$59(spaceConfig, `${firstChildKey}Left`, 0),
				right: GetValue$59(spaceConfig, `${firstChildKey}Right`, 0),
				top: GetValue$59(spaceConfig, `${firstChildKey}Top`, 0),
				bottom: GetValue$59(spaceConfig, `${firstChildKey}Bottom`, 0)
			}
		});
		splitterSizer.add(splitter, {
			proportion: 0,
			expand: true,
			padding: {
				left: GetValue$59(spaceConfig, "splitterLeft", 0),
				right: GetValue$59(spaceConfig, "splitterRight", 0),
				top: GetValue$59(spaceConfig, "splitterTop", 0),
				bottom: GetValue$59(spaceConfig, "splitterBottom", 0)
			}
		});
		splitterSizer.add(secondChild, {
			proportion: 1,
			expand: true,
			padding: {
				left: GetValue$59(spaceConfig, `${secondChildKey}Left`, 0),
				right: GetValue$59(spaceConfig, `${secondChildKey}Right`, 0),
				top: GetValue$59(spaceConfig, `${secondChildKey}Top`, 0),
				bottom: GetValue$59(spaceConfig, `${secondChildKey}Bottom`, 0)
			}
		});
		var footer = GetValue$59(config, "footer", void 0);
		if (footer) {
			var align = GetValue$59(config, "align.footer", "center");
			var footerSpace = GetValue$59(config, "space.footer", 0);
			var padding;
			if (config.orientation === 0) padding = { top: footerSpace };
			else padding = { left: footerSpace };
			this.add(footer, {
				proportion: 0,
				align,
				padding,
				expand: GetValue$59(config, "expand.footer", true)
			});
		}
		this.addChildrenMap("background", background);
		this.addChildrenMap("header", header);
		this.addChildrenMap("splitterSizer", splitterSizer);
		this.addChildrenMap("footer", footer);
		this.addChildrenMap(firstChildKey, firstChild);
		this.addChildrenMap("splitter", splitter);
		this.addChildrenMap(secondChildKey, secondChild);
		this.minFirstChildSize = GetValue$59(config, minFirstChildSizeKey, 0);
		this.minSecondChildSize = GetValue$59(config, minSecondChildSizeKey, 0);
		this.setSplitRatio(GetValue$59(config, "splitRatio", .5));
		this.splitterDragBehavior = new Drag(splitter, { axis: panelOrientation === 0 ? 1 : 2 });
		splitter.on("dragstart", function() {
			this.emit("splitter.dragstart", splitter, this.splitRatio);
		}, this).on("dragend", function() {
			this.emit("splitter.dragend", splitter, this.splitRatio);
		}, this).on("drag", function() {
			OnDragSplitter.call(this);
			this.emit("splitter.drag", splitter, this.splitRatio);
		}, this).on("pointerover", function() {
			this.emit("splitter.over", splitter, this.splitRatio);
		}, this).on("pointerout", function() {
			this.emit("splitter.out", splitter, this.splitRatio);
		}, this);
	}
	get splitterEnable() {
		return this.splitterDragBehavior.enable;
	}
	set splitterEnable(value) {
		this.splitterDragBehavior.setEnable(value);
	}
	setSplitterEnable(enable) {
		if (enable === void 0) enable = true;
		this.splitterEnable = enable;
		return this;
	}
	get leftPanel() {
		return this.childrenMap.splitterSizer.sizerChildren[0];
	}
	get topPanel() {
		return this.leftPanel;
	}
	get splitter() {
		return this.childrenMap.splitterSizer.sizerChildren[1];
	}
	get rightPanel() {
		return this.childrenMap.splitterSizer.sizerChildren[2];
	}
	get bottomPanel() {
		return this.rightPanel;
	}
	set minLeftPanelWidth(value) {
		this.minFirstChildSize = value;
	}
	get minLeftPanelWidth() {
		return this.minFirstChildSize;
	}
	set minRightPanelWidth(value) {
		this.minSecondChildSize = value;
	}
	get minRightPanelWidth() {
		return this.minSecondChildSize;
	}
	set minTopPanelHeight(value) {
		this.minFirstChildSize = value;
	}
	get minTopPanelHeight() {
		return this.minFirstChildSize;
	}
	set minBottomPanelHeight(value) {
		this.minSecondChildSize = value;
	}
	get minBottomPanelHeight() {
		return this.minSecondChildSize;
	}
	setMinLeftPanelWidth(value) {
		this.minLeftPanelWidth = value;
		return this;
	}
	setMinRightPanelWidth(value) {
		this.minRightPanelWidth = value;
		return this;
	}
	setMinTopPanelHeight(value) {
		this.minTopPanelHeight = value;
		return this;
	}
	setMinBottomPanelHeight(value) {
		this.minBottomPanelHeight = value;
		return this;
	}
	get splitRatio() {
		return this._splitRatio;
	}
	set splitRatio(value) {
		value = Clamp$2(value, 0, 1);
		if (this._splitRatio === value) return;
		this._splitRatio = value;
		this.getSizerConfig(this.leftPanel).proportion = value;
		this.getSizerConfig(this.rightPanel).proportion = 1 - value;
	}
	setSplitRatio(value) {
		this.splitRatio = value;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/splitpanels/Factory.js
ObjectFactory.register("splitPanels", function(config) {
	var gameObject = new SplitPanels$1(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.SplitPanels", SplitPanels$1);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sides/defaultcallbacks/VisibleCallbacks.js
var GetShowCallback$1 = function() {
	return function(child, key, sides, reset) {
		if (key !== "panel") sides.setChildVisible(child, true);
	};
};
var GetHideCallback$1 = function() {
	return function(child, key, sides, reset) {
		if (key !== "panel") sides.setChildVisible(child, false);
	};
};
var VisibleCallbacks_default = {
	show: GetShowCallback$1,
	hide: GetHideCallback$1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sides/defaultcallbacks/FadeCallbacks.js
var GetShowCallback = function(duration, alpha) {
	if (alpha === void 0) alpha = 1;
	return function(child, key, sides, reset) {
		if (key !== "panel") sides.fadeChild(child, reset ? 0 : duration, alpha);
	};
};
var GetHideCallback = function(duration, alpha) {
	if (alpha === void 0) alpha = 0;
	return function(child, key, sides, reset) {
		if (key !== "panel") sides.fadeChild(child, reset ? 0 : duration, alpha);
	};
};
var FadeCallbacks_default = {
	show: GetShowCallback,
	hide: GetHideCallback
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sides/defaultcallbacks/MoveCallbacks.js
var GetCallback$2 = function(duration, ease) {
	return function(child, key, sides, reset) {
		if (key !== "panel") sides.moveChild(child, reset ? 0 : duration, ease);
	};
};
var MoveCallbacks_default = {
	show: GetCallback$2,
	hide: GetCallback$2
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sides/defaultcallbacks/MovePanelCallbacks.js
var GetCallback$1 = function(duration, ease) {
	return function(child, key, sides, reset) {
		if (key === "panel") sides.moveChild(child, reset ? 0 : duration, ease);
	};
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sides/defaultcallbacks/GetDefaultCallbacks.js
var DefaultCallbacks = {
	visible: VisibleCallbacks_default,
	fade: FadeCallbacks_default,
	move: MoveCallbacks_default,
	"move-panel": {
		show: GetCallback$1,
		hide: GetCallback$1
	}
};
var GetDefaultCallbacks = function(config) {
	var callbackType, callbackParams;
	[callbackType, ...callbackParams] = typeof config === "string" ? [config] : config;
	var showCallback, hideCallback;
	if (DefaultCallbacks.hasOwnProperty(callbackType)) {
		showCallback = DefaultCallbacks[callbackType].show.apply(null, callbackParams);
		hideCallback = DefaultCallbacks[callbackType].hide.apply(null, callbackParams);
	} else {
		showCallback = NOOP;
		hideCallback = NOOP;
	}
	return {
		show: showCallback,
		hide: hideCallback
	};
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sides/ShowChildMethods.js
var ShowChildMethods_default = {
	showChild(key, reset) {
		var child = this.sizerChildren[key];
		if (child) {
			this.emit("showchild", child, key, this, reset);
			this.resetChildState(child);
		}
		return this;
	},
	hideChild(key, reset) {
		var child = this.sizerChildren[key];
		if (child) {
			this.emit("hidechild", child, key, this, reset);
			this.resetChildState(child);
		}
		return this;
	},
	swapChild(key, reset) {
		if (this.currentChildKey === key) {} else if (this.currentChildKey === "panel" || key === "panel") {
			this.previousChildKey = this.currentChildKey;
			this.currentChildKey = key;
			this.hideChild(this.previousChildKey, reset);
			this.showChild(this.currentChildKey, reset);
		} else {
			this.swapChild("panel", reset);
			this.swapChild(key, reset);
		}
		return this;
	},
	showPanel(reset) {
		this.swapChild("panel", reset);
		return this;
	},
	showLeftSide() {
		this.swapChild("leftSide");
		return this;
	},
	showRightSide() {
		this.swapChild("rightSide");
		return this;
	},
	showTopSide() {
		this.swapChild("topSide");
		return this;
	},
	showBottomSide() {
		this.swapChild("bottomSide");
		return this;
	},
	hideLeftSide() {
		if (this.currentChildKey == "leftSide") this.showPanel();
		return this;
	},
	hideRightSide() {
		if (this.currentChildKey == "rightSide") this.showPanel();
		return this;
	},
	hideTopSide() {
		if (this.currentChildKey == "topSide") this.showPanel();
		return this;
	},
	hideBottomSide() {
		if (this.currentChildKey == "bottomSide") this.showPanel();
		return this;
	},
	toggleLeftSide() {
		var key = this.currentChildKey !== "panel" ? "panel" : "leftSide";
		this.swapChild(key);
		return this;
	},
	toggleRightSide() {
		var key = this.currentChildKey !== "panel" ? "panel" : "rightSide";
		this.swapChild(key);
		return this;
	},
	toggleTopSide() {
		var key = this.currentChildKey !== "panel" ? "panel" : "topSide";
		this.swapChild(key);
		return this;
	},
	toggleBottomSide() {
		var key = this.currentChildKey !== "panel" ? "panel" : "bottomSide";
		this.swapChild(key);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sides/childbehaviors/Visible.js
var ContainerSetChildVisible = Container_default.prototype.setChildVisible;
var Visible_default = { setChildVisible(child, visible) {
	var key;
	if (typeof child === "string") {
		var key = child;
		child = this.sizerChildren[key];
	} else key = IndexOf(this.sizerChildren, child);
	if (visible === void 0) visible = this.currentChildKey === key ? true : false;
	ContainerSetChildVisible.call(this, child, visible);
	return this;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sides/childbehaviors/Fade.js
var Fade_default = {
	fadeChild(child, duration, alpha) {
		var key;
		if (typeof child === "string") {
			key = child;
			child = this.sizerChildren[key];
		} else key = IndexOf(this.sizerChildren, child);
		if (duration === void 0) duration = 500;
		if (alpha === void 0) alpha = this.currentChildKey === key ? 1 : 0;
		child.fadeIn(duration, {
			start: child.alpha,
			end: alpha
		});
		return this;
	},
	fadeChildPromise(child, duration, alpha) {
		if (typeof child === "string") child = this.sizerChildren[key];
		this.fadeChild(child, duration, alpha);
		if (child._fade) return WaitComplete(child._fade);
		else return Promise.resolve();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sides/childbehaviors/Move.js
var Move_default = {
	moveChild(child, duration, ease, distance) {
		var key;
		if (typeof child === "string") {
			key = child;
			child = this.sizerChildren[key];
		} else key = IndexOf(this.sizerChildren, child);
		if (duration === void 0) duration = 500;
		var isShownChild = this.currentChildKey === key;
		if (distance === void 0) switch (key) {
			case "leftSide":
			case "rightSide":
				distance = GetDisplayWidth(child);
				break;
			case "topSide":
			case "bottomSide":
				distance = GetDisplayHeight(child);
				break;
			default: if (isShownChild) switch (this.previousChildKey) {
				case "leftSide":
				case "rightSide":
					distance = GetDisplayWidth(this.sizerChildren[this.previousChildKey]);
					break;
				case "topSide":
				case "bottomSide":
					distance = GetDisplayHeight(this.sizerChildren[this.previousChildKey]);
					break;
				default: distance = 0;
			}
			else switch (this.currentChildKey) {
				case "leftSide":
				case "rightSide":
					distance = GetDisplayWidth(this.sizerChildren[this.currentChildKey]);
					break;
				case "topSide":
				case "bottomSide":
					distance = GetDisplayHeight(this.sizerChildren[this.currentChildKey]);
					break;
				default: distance = 0;
			}
		}
		var moveLeft, moveRight, moveUp, moveDown;
		if (isShownChild) switch (key) {
			case "panel":
				switch (this.previousChildKey) {
					case "leftSide":
						moveLeft = true;
						break;
					case "rightSide":
						moveRight = true;
						break;
					case "topSide":
						moveUp = true;
						break;
					case "bottomSide": moveDown = true;
				}
				break;
			case "leftSide":
				moveRight = true;
				break;
			case "rightSide":
				moveLeft = true;
				break;
			case "topSide":
				moveDown = true;
				break;
			case "bottomSide": moveUp = true;
		}
		else switch (key) {
			case "panel":
				switch (this.currentChildKey) {
					case "leftSide":
						moveRight = true;
						break;
					case "rightSide":
						moveLeft = true;
						break;
					case "topSide":
						moveDown = true;
						break;
					case "bottomSide": moveUp = true;
				}
				break;
			case "leftSide":
				moveLeft = true;
				break;
			case "rightSide":
				moveRight = true;
				break;
			case "topSide":
				moveUp = true;
				break;
			case "bottomSide": moveDown = true;
		}
		if (moveLeft) child.moveTo(duration, `-=${distance}`, void 0, ease);
		else if (moveRight) child.moveTo(duration, `+=${distance}`, void 0, ease);
		else if (moveUp) child.moveTo(duration, void 0, `-=${distance}`, ease);
		else if (moveDown) child.moveTo(duration, void 0, `+=${distance}`, ease);
		else child.moveTo(0);
		return this;
	},
	moveChildPromise(child, duration, ease, distance) {
		if (typeof child === "string") child = this.sizerChildren[key];
		this.moveChild(child, duration, ease, distance);
		if (child._easeMove) return WaitComplete(child._easeMove);
		else return Promise.resolve();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sides/childbehaviors/index.js
var methods$2 = {};
Object.assign(methods$2, Visible_default, Fade_default, Move_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sides/Sides.js
var GetValue$58 = __webpack_exports__Utils.Objects.GetValue;
var Sides = class extends OverlapSizer {
	constructor(scene, config) {
		super(scene, config);
		this.type = "rexSides";
		this.childrenMap = this.sizerChildren;
		this.previousChildKey = void 0;
		this.currentChildKey = void 0;
		var showChildCallback = GetValue$58(config, "showChildCallback", void 0);
		if (showChildCallback) {
			if (IsFunction(showChildCallback)) {
				var showChildCallbackScope = GetValue$58(config, "showChildCallbackScope", void 0);
				this.on("showchild", showChildCallback, showChildCallbackScope);
				var hideChildCallback = GetValue$58(config, "hideChildCallback", void 0);
				var hideChildCallbackScope = GetValue$58(config, "hideChildCallbackScope", void 0);
				this.on("hidechild", hideChildCallback, hideChildCallbackScope);
			} else {
				var defaultCallbacks = GetDefaultCallbacks(showChildCallback);
				this.on("showchild", defaultCallbacks.show);
				this.on("hidechild", defaultCallbacks.hide);
			}
		}
		var background = GetValue$58(config, "background", void 0);
		var panel = GetValue$58(config, "panel", void 0);
		var leftSide = GetValue$58(config, "leftSide", void 0);
		var rightSide = GetValue$58(config, "rightSide", void 0);
		var topSide = GetValue$58(config, "topSide", void 0);
		var bottomSide = GetValue$58(config, "bottomSide", void 0);
		if (background) this.addBackground(background);
		if (panel) this.add(panel, "panel", "center", 0, true);
		if (leftSide) {
			var expand = GetValue$58(config, "expand.left", true);
			this.add(leftSide, "leftSide", "left-top", 0, { height: expand });
		}
		if (rightSide) {
			var expand = GetValue$58(config, "expand.right", true);
			this.add(rightSide, "rightSide", "right-top", 0, { height: expand });
		}
		if (topSide) {
			var expand = GetValue$58(config, "expand.top", true);
			this.add(topSide, "topSide", "left-top", 0, { width: expand });
		}
		if (bottomSide) {
			var expand = GetValue$58(config, "expand.bottom", true);
			this.add(bottomSide, "bottomSide", "left-bottom", 0, { width: expand });
		}
	}
	reset() {
		this.previousChildKey = void 0;
		this.currentChildKey = "panel";
		this.showChild("panel", true);
		this.hideChild("leftSide", true);
		this.hideChild("rightSide", true);
		this.hideChild("topSide", true);
		this.hideChild("bottomSide", true);
		return this;
	}
};
Object.assign(Sides.prototype, ShowChildMethods_default, methods$2);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/sides/Factory.js
ObjectFactory.register("sides", function(config) {
	var gameObject = new Sides(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.Sides", Sides);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/methods/RegisterInputHandler.js
var RegisterInputHandler = function(config) {
	var isValidInputHandler = true;
	if (!config.hasOwnProperty("accept")) {
		isValidInputHandler = false;
		console.error(`[Tweaker] Can't register inputHandler '${config.name}', missing 'accept' callback.`);
	}
	if (!config.hasOwnProperty("build")) {
		isValidInputHandler = false;
		console.error(`[Tweaker] Can't register inputHandler '${config.name}', missing 'build' callback.`);
	}
	if (isValidInputHandler) this.inputHandlers.unshift(config);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/methods/RemoveInputHandler.js
var GetFirst = __webpack_exports__Utils.Array.GetFirst;
var Remove = __webpack_exports__Utils.Array.Remove;
var RemoveInputHandler = function(name) {
	var handler = GetFirst(this.inputHandlers, "name", name);
	if (handler) Remove(this.inputHandlers, handler);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/label/Title.js
var Title$1 = class extends Label {
	constructor(scene, config) {
		config = BuildLabelConfig(scene, config);
		super(scene, config);
		this.type = "rexTweaker.Title";
	}
	setTitle(config) {
		config = config ? DeepClone(config) : {};
		if (config.hasOwnProperty("text")) {} else if (config.hasOwnProperty("title")) config.text = config.title;
		else config.text = "";
		this.resetDisplayContent(config);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/label/FolderTitle.js
var FolderTitle = class extends Title$1 {
	constructor(scene, config) {
		if (config === void 0) config = {};
		super(scene, config);
		this.type = "rexTweaker.FolderTitle";
		var expandedIcon = new Triangle_default(scene, config.expandedIcon);
		scene.add.existing(expandedIcon);
		this.addSpace().add(expandedIcon, {
			proportion: 0,
			expand: false,
			padding: 1,
			fitRatio: 1
		});
		this.addChildrenMap("expandedIcon", expandedIcon);
	}
	setExpandedState(expanded) {
		if (expanded === void 0) expanded = true;
		var direction = expanded ? "down" : "right";
		this.childrenMap.expandedIcon.setDirection(direction);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/object/Merge.js
/**
* @author       Richard Davey <rich@photonstorm.com>
* @copyright    2019 Photon Storm Ltd.
* @license      {@link https://opensource.org/licenses/MIT|MIT License}
*/
/**
* Creates a new Object using all values from obj1 and obj2.
* If a value exists in both obj1 and obj2, the value in obj1 is used.
* 
* This is only a shallow copy. Deeply nested objects are not cloned, so be sure to only use this
* function on shallow objects.
*
* @function Phaser.Utils.Objects.Merge
* @since 3.0.0
*
* @param {object} obj1 - The first object.
* @param {object} obj2 - The second object.
*
* @return {object} A new object containing the union of obj1's and obj2's properties.
*/
var Merge$1 = function(obj1, obj2) {
	var clone = Clone$2(obj1);
	for (var key in obj2) if (!clone.hasOwnProperty(key)) clone[key] = obj2[key];
	return clone;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/utils/CreateBackground.js
var CreateBackground = function(scene, config, style) {
	return CreateBackground$2(scene, Merge$1(config, style));
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/folder/BindingTargetMethods.js
var BindingTargetMethods_default$9 = { setBindingTarget(target) {
	var child = this.childrenMap.child;
	if (child.setBindingTarget) child.setBindingTarget(target);
	return this;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/folder/InputRowTitleWidthMethods.js
var InputRowTitleWidthMethods_default$8 = {
	getMaxInputRowTitleWidth() {
		return this.childrenMap.child.getMaxInputRowTitleWidth() + this.getInnerPadding("left");
	},
	setInputRowTitleWidth(width) {
		width -= this.getInnerPadding("left");
		this.childrenMap.child.setInputRowTitleWidth(width);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/folder/SetReadOnlyMethods.js
var SetReadOnlyMethods_default$6 = { setReadOnly(value) {
	var child = this.childrenMap.child;
	if (child.setReadOnly) child.setReadOnly(value);
	return this;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/folder/Folder.js
var Folder = class extends Folder$1 {
	constructor(scene, config) {
		if (config === void 0) config = {};
		config.orientation = 1;
		super(scene, config);
		this.type = "rexTweaker.Folder";
	}
	setTitle(config) {
		this.childrenMap.title.setTitle(config);
		return this;
	}
};
Object.assign(Folder.prototype, BindingTargetMethods_default$9, InputRowTitleWidthMethods_default$8, SetReadOnlyMethods_default$6);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/CreateFolder.js
var GetValue$57 = __webpack_exports__Utils.Objects.GetValue;
var CreateFolder = function(tweaker, config, style) {
	if (!config) config = {};
	if (!style) style = {};
	var scene = tweaker.scene;
	var background = CreateBackground(scene, config.background || {}, style.background || {});
	var title = new FolderTitle(scene, style.title || {});
	scene.add.existing(title);
	title.on("folder.expand", function() {
		title.setExpandedState(true);
	}).on("folder.collapse", function() {
		title.setExpandedState(false);
	});
	var tweakerConfig = {
		root: tweaker.root,
		styles: tweaker.styles
	};
	var folder = new Folder(scene, {
		title,
		child: tweaker.createTweaker(tweakerConfig),
		background,
		space: style.space,
		transition: { duration: GetValue$57(style, "transition.duration", 200) }
	});
	scene.add.existing(folder);
	return folder;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/methods/AddFolder.js
var GetValue$56 = __webpack_exports__Utils.Objects.GetValue;
var AddFolder = function(config) {
	if (config === void 0) config = {};
	var folderStyle = GetValue$56(this.styles, "folder") || {};
	var folder = CreateFolder(this, config, folderStyle);
	this.add(folder, {
		proportion: folder.minWidth === 0 ? 1 : 0,
		expand: true
	});
	folder.setTitle(config);
	var expanded = GetValue$56(config, "expanded", true);
	if (expanded !== void 0) folder.setExpandedState(expanded);
	var childTweaker = folder.childrenMap.child;
	if (config.key) this.root.addChildrenMap(config.key, childTweaker);
	return childTweaker;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/tabpages/BindingTargetMethods.js
var BindingTargetMethods_default$8 = { setBindingTarget(target) {
	var children = this.childrenMap.pages.children;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		if (!child.setBindingTarget) continue;
		child.setBindingTarget(target);
	}
	return this;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/tabpages/InputRowTitleWidthMethods.js
var InputRowTitleWidthMethods_default$7 = {
	getMaxInputRowTitleWidth() {
		var maxTitleWidth = 0;
		var children = this.childrenMap.pages.children;
		for (var i = 0, cnt = children.length; i < cnt; i++) maxTitleWidth = Math.max(maxTitleWidth, children[i].getMaxInputRowTitleWidth());
		return maxTitleWidth + this.getInnerPadding("left");
	},
	setInputRowTitleWidth(width) {
		width -= this.getInnerPadding("left");
		var children = this.childrenMap.pages.children;
		for (var i = 0, cnt = children.length; i < cnt; i++) children[i].setInputRowTitleWidth(width);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/tabpages/SetReadOnlyMethods.js
var SetReadOnlyMethods_default$5 = { setReadOnly(value) {
	var children = this.childrenMap.pages.children;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		if (!child.setReadOnly) continue;
		child.setReadOnly(value);
	}
	return this;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/tabpages/TabPages.js
var TabPages = class extends TabPages$1 {
	constructor(scene, config) {
		super(scene, config);
		this.type = "rexTweaker.TabPages";
	}
};
Object.assign(TabPages.prototype, BindingTargetMethods_default$8, InputRowTitleWidthMethods_default$7, SetReadOnlyMethods_default$5);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/CreateTab.js
__webpack_exports__Utils.Objects.GetValue;
var CreateTab = function(tweaker, config, style) {
	if (!config) config = {};
	if (!style) style = {};
	var scene = tweaker.scene;
	var tabPages = new TabPages(scene, style);
	scene.add.existing(tabPages);
	var tabConfig = style.tab;
	var tweakerConfig = {
		root: tweaker.root,
		styles: tweaker.styles
	};
	var pages = config.pages || [];
	for (var i = 0, cnt = pages.length; i < cnt; i++) {
		var page = pages[i];
		tabPages.addPage({
			key: page.title,
			tab: CreateLabel(scene, tabConfig).setActiveState(false).resetDisplayContent({ text: page.title }),
			page: tweaker.createTweaker(tweakerConfig)
		});
	}
	tabPages.on("tab.focus", function(tab, key) {
		tab.setActiveState(true);
	}).on("tab.blur", function(tab, key) {
		tab.setActiveState(false);
	});
	return tabPages;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/methods/AddTab.js
var GetValue$55 = __webpack_exports__Utils.Objects.GetValue;
var AddTab = function(config) {
	if (config === void 0) config = {};
	var tabStyle = GetValue$55(this.styles, "tab") || {};
	var tab = CreateTab(this, config, tabStyle);
	this.add(tab, {
		proportion: tab.minWidth === 0 ? 1 : 0,
		expand: true
	});
	var pagesConfig = GetValue$55(config, "pages") || [];
	var pages = [];
	var shownPageIndex = 0;
	for (var i = 0, cnt = pagesConfig.length; i < cnt; i++) {
		var childTweaker = tab.getPage(i);
		if (pagesConfig[i].show) shownPageIndex = i;
		var key = pagesConfig[i].key;
		if (key) this.root.addChildrenMap(key, childTweaker);
		pages.push(childTweaker);
	}
	tab.swapPage(shownPageIndex, 0);
	return pages;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/columns/BindingTargetMethods.js
var BindingTargetMethods_default$7 = { setBindingTarget(target) {
	var children = this.childrenMap.columns;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		if (!child.setBindingTarget) continue;
		child.setBindingTarget(target);
	}
	return this;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/columns/InputRowTitleWidthMethods.js
var InputRowTitleWidthMethods_default$6 = {
	getMaxInputRowTitleWidth() {
		return 0;
	},
	setInputRowTitleWidth(width) {
		var children = this.childrenMap.columns;
		for (var i = 0, cnt = children.length; i < cnt; i++) {
			var child = children[i];
			child.setInputRowTitleWidth(child.getMaxInputRowTitleWidth());
		}
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/columns/SetReadOnlyMethods.js
var SetReadOnlyMethods_default$4 = { setReadOnly(value) {
	var children = this.childrenMap.columns;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		if (!child.setReadOnly) continue;
		child.setReadOnly(value);
	}
	return this;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/columns/Columns.js
var GetValue$54 = __webpack_exports__Utils.Objects.GetValue;
var Columns = class extends Sizer {
	constructor(scene, config) {
		if (config === void 0) config = {};
		config.orientation = "y";
		super(scene, config);
		this.type = "rexTweaker.Columns";
		this.alignInputRowTitleStartFlag = GetValue$54(config, "alignTitle");
		var background = GetValue$54(config, "background", void 0);
		var title = GetValue$54(config, "title", void 0);
		if (background) this.addBackground(background);
		if (title) this.add(title, {
			expand: true,
			space: { bottom: GetValue$54(config, "space.title", 0) }
		});
		var columnsSizer = new Sizer(scene, {
			orientation: "x",
			space: { item: GetValue$54(config, "space.column", 0) }
		});
		scene.add.existing(columnsSizer);
		var columnConfigArray = GetValue$54(config, "columns", void 0);
		var columnConfig;
		var columnChild;
		for (var i = 0, cnt = columnConfigArray.length; i < cnt; i++) {
			columnConfig = columnConfigArray[i];
			columnChild = columnConfig.child;
			columnsSizer.add(columnConfig.child, {
				proportion: columnChild.minWidth === 0 ? 1 : 0,
				expand: GetValue$54(columnConfig, "expand", true)
			});
		}
		this.add(columnsSizer, { expand: true });
		this.addChildrenMap("title", title);
		this.addChildrenMap("columnsSizer", columnsSizer);
		this.addChildrenMap("columns", columnsSizer.childrenMap.items);
	}
	preLayout() {
		super.preLayout();
		if (this.alignInputRowTitleStartFlag) this.setInputRowTitleWidth(this.getMaxInputRowTitleWidth());
	}
	setTitle(config) {
		var title = this.childrenMap.title;
		if (config.text || config.title || config.icon) title.show().setTitle(config);
		else title.hide();
		return this;
	}
	getColumns() {
		return this.childrenMap.columns;
	}
	getColumn(index) {
		return this.childrenMap.columns[index];
	}
};
Object.assign(Columns.prototype, BindingTargetMethods_default$7, InputRowTitleWidthMethods_default$6, SetReadOnlyMethods_default$4);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/utils/CreateTitleLabel.js
var CreateTitleLabel = function(scene, config, style) {
	var gameObject = new Title$1(scene, style);
	scene.add.existing(gameObject);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/CreateColumns.js
var GetValue$53 = __webpack_exports__Utils.Objects.GetValue;
var CreateColumns = function(tweaker, config, style) {
	if (!config) config = {};
	if (!style) style = {};
	var scene = tweaker.scene;
	var background = CreateBackground(scene, config.background || {}, style.background || {});
	var title = CreateTitleLabel(scene, void 0, style.title);
	var tweakerConfig = {
		root: tweaker.root,
		styles: tweaker.styles
	};
	var columnConfigArray = GetValue$53(config, "columns", 2);
	if (typeof columnConfigArray === "number") {
		var columnCount = columnConfigArray;
		columnConfigArray = [];
		for (var i = 0, cnt = columnCount; i < cnt; i++) columnConfigArray.push({});
	}
	for (var i = 0, cnt = columnConfigArray.length; i < cnt; i++) {
		var columnConfig = columnConfigArray[i];
		tweakerConfig.width = GetValue$53(columnConfig, "width", 0);
		columnConfig.child = tweaker.createTweaker(tweakerConfig);
	}
	var columns = new Columns(scene, {
		title,
		columns: columnConfigArray,
		background,
		space: style.space,
		alignTitle: tweaker.root.alignTitle
	});
	scene.add.existing(columns);
	return columns;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/methods/AddColumns.js
var GetValue$52 = __webpack_exports__Utils.Objects.GetValue;
var AddColumns = function(config) {
	if (config === void 0) config = {};
	if (typeof config === "number") config = { columns: config };
	var columnsStyle = GetValue$52(this.styles, "columns") || {};
	var columns = CreateColumns(this, config, columnsStyle);
	this.add(columns, {
		proportion: columns.minWidth === 0 ? 1 : 0,
		expand: true
	});
	columns.setTitle(config);
	var columnConfigArray = GetValue$52(config, "columns", void 0);
	if (columnConfigArray && Array.isArray(columnConfigArray)) for (var i = 0, cnt = columnConfigArray.length; i < cnt; i++) {
		var key = columnConfigArray[i].key;
		if (key) this.root.addChildrenMap(key, columns.getColumn(i));
	}
	return columns.getColumns();
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/splitpanels/BindingTargetMethods.js
var BindingTargetMethods_default$6 = { setBindingTarget(target) {
	var leftPanel = this.leftPanel;
	var rightPanel = this.rightPanel;
	if (leftPanel && leftPanel.setBindingTarget) leftPanel.setBindingTarget(target);
	if (rightPanel && rightPanel.setBindingTarget) rightPanel.setBindingTarget(target);
	return this;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/splitpanels/InputRowTitleWidthMethods.js
var InputRowTitleWidthMethods_default$5 = {
	getMaxInputRowTitleWidth() {
		return 0;
	},
	setInputRowTitleWidth(width) {
		var leftPanel = this.leftPanel;
		var rightPanel = this.rightPanel;
		if (leftPanel) leftPanel.setInputRowTitleWidth(leftPanel.getMaxInputRowTitleWidth());
		if (rightPanel) rightPanel.setInputRowTitleWidth(rightPanel.getMaxInputRowTitleWidth());
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/splitpanels/SetReadOnlyMethods.js
var SetReadOnlyMethods_default$3 = { setReadOnly(value) {
	var leftPanel = this.leftPanel;
	var rightPanel = this.rightPanel;
	if (leftPanel && leftPanel.setReadOnly) leftPanel.setReadOnly(value);
	if (rightPanel && rightPanel.setReadOnly) rightPanel.setReadOnly(value);
	return this;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/splitpanels/SplitPanels.js
var GetValue$51 = __webpack_exports__Utils.Objects.GetValue;
var SplitPanels = class extends SplitPanels$1 {
	constructor(scene, config) {
		super(scene, config);
		this.type = "rexTweaker.SplitPanels";
		this.alignInputRowTitleStartFlag = GetValue$51(config, "alignTitle");
	}
	preLayout() {
		super.preLayout();
		if (this.alignInputRowTitleStartFlag) this.setInputRowTitleWidth(this.getMaxInputRowTitleWidth());
	}
	setTitle(config) {
		var title = this.childrenMap.header;
		if (config.text || config.title || config.icon) title.show().setTitle(config);
		else title.hide();
		return this;
	}
};
Object.assign(SplitPanels.prototype, BindingTargetMethods_default$6, InputRowTitleWidthMethods_default$5, SetReadOnlyMethods_default$3);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/CreateSplitPanels.js
var GetValue$50 = __webpack_exports__Utils.Objects.GetValue;
var DefaultSplitStyle$1 = {
	width: 10,
	height: 10,
	color: 0,
	alpha: .001
};
var CreateSplitPanels = function(tweaker, config, style) {
	if (!config) config = {};
	if (!style) style = {};
	var scene = tweaker.scene;
	var background = CreateBackground(scene, config.background || {}, style.background || {});
	var title = CreateTitleLabel(scene, style.title || {});
	var tweakerConfig = {
		root: tweaker.root,
		styles: tweaker.styles
	};
	var leftPanel = tweaker.createTweaker(tweakerConfig);
	var rightPanel = tweaker.createTweaker(tweakerConfig);
	var splitterConfig = config.splitter;
	var splitterStyle = style.splitter;
	var splitter;
	if (!splitterConfig && !splitterStyle) splitterStyle = DefaultSplitStyle$1;
	splitter = CreateBackground(scene, splitterConfig || {}, splitterStyle || {});
	var splitPanels = new SplitPanels(scene, {
		header: title,
		leftPanel,
		rightPanel,
		splitter,
		background,
		splitRatio: GetValue$50(config, "splitRatio", .5),
		minLeftPanelWidth: GetValue$50(config, "minLeftPanelWidth", 0),
		minRightPanelWidth: GetValue$50(config, "minRightPanelWidth", 0),
		space: style.space,
		alignTitle: tweaker.root.alignTitle
	});
	scene.add.existing(splitPanels);
	return splitPanels;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/methods/Add2Columns.js
var GetValue$49 = __webpack_exports__Utils.Objects.GetValue;
var AddSplit = function(config) {
	if (config === void 0) config = {};
	var splitPanelStyle = GetValue$49(this.styles, "2columns") || {};
	var splitPanels = CreateSplitPanels(this, config, splitPanelStyle);
	this.add(splitPanels, {
		proportion: splitPanels.minWidth === 0 ? 1 : 0,
		expand: true
	});
	splitPanels.setTitle(config);
	var leftPanel = splitPanels.childrenMap.leftPanel;
	var rightPanel = splitPanels.childrenMap.rightPanel;
	var leftConfig = GetValue$49(config, "left", void 0);
	if (leftConfig && leftConfig.key) this.root.addChildrenMap(leftConfig.key, leftPanel);
	var rightConfig = GetValue$49(config, "right", void 0);
	if (rightConfig && rightConfig.key) this.root.addChildrenMap(rightConfig.key, rightPanel);
	return {
		left: leftPanel,
		right: rightPanel
	};
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/wrap/BindingTargetMethods.js
var BindingTargetMethods_default$5 = { setBindingTarget(target) {
	var children = this.childrenMap.child;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		if (!child.setBindingTarget) continue;
		child.setBindingTarget(target);
	}
	return this;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/wrap/InputRowTitleWidthMethods.js
var InputRowTitleWidthMethods_default$4 = {
	getMaxInputRowTitleWidth() {
		return 0;
	},
	setInputRowTitleWidth(width) {
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/wrap/SetReadOnlyMethods.js
var SetReadOnlyMethods_default$2 = { setReadOnly(value) {
	var children = this.childrenMap.child;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		if (!child.setReadOnly) continue;
		child.setReadOnly(value);
	}
	return this;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/wrap/Wrap.js
var GetValue$48 = __webpack_exports__Utils.Objects.GetValue;
var Wrap = class extends Sizer {
	constructor(scene, config) {
		if (config === void 0) config = {};
		config.orientation = "y";
		super(scene, config);
		this.type = "rexTweaker.Wrap";
		var background = GetValue$48(config, "background", void 0);
		var title = GetValue$48(config, "title", void 0);
		if (background) this.addBackground(background);
		if (title) this.add(title, {
			expand: true,
			space: { bottom: GetValue$48(config, "space.title", 0) }
		});
		var child = GetValue$48(config, "child", void 0);
		this.add(child, { expand: true });
		this.addChildrenMap("title", title);
		this.addChildrenMap("child", child);
	}
	setTitle(config) {
		var title = this.childrenMap.title;
		if (config.title || config.icon) title.show().setTitle(config);
		else title.hide();
		return this;
	}
};
Object.assign(Wrap.prototype, BindingTargetMethods_default$5, InputRowTitleWidthMethods_default$4, SetReadOnlyMethods_default$2);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/CreateWrap.js
var GetValue$47 = __webpack_exports__Utils.Objects.GetValue;
var CreateWrap = function(tweaker, config, style) {
	if (!config) config = {};
	if (!style) style = {};
	var scene = tweaker.scene;
	var background = CreateBackground(scene, config.background || {}, style.background || {});
	var title = CreateTitleLabel(scene, void 0, style.title || {});
	var tweakerConfig = {
		root: tweaker.root,
		styles: tweaker.styles,
		space: style.space,
		align: GetValue$47(style, "align", 5),
		wrap: true,
		itemWidth: GetValue$47(config, "itemWidth", 0, style),
		itemHeight: GetValue$47(config, "itemHeight", 0, style)
	};
	var wrap = new Wrap(scene, {
		title,
		child: tweaker.createTweaker(tweakerConfig),
		background
	});
	scene.add.existing(wrap);
	return wrap;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/methods/AddWrap.js
var GetValue$46 = __webpack_exports__Utils.Objects.GetValue;
var AddWrap = function(config) {
	if (config === void 0) config = {};
	var wrapStyle = GetValue$46(this.styles, "wrap") || {};
	var wrap = CreateWrap(this, config, wrapStyle);
	this.add(wrap, { expand: true });
	wrap.setTitle(config);
	var childTweaker = wrap.childrenMap.child;
	if (config.key) this.root.addChildrenMap(config.key, childTweaker);
	return childTweaker;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/CreateSlider.js
var CreateSlider$1 = function(scene, config, style) {
	if (!style && !config) return;
	if (!config) config = {};
	var slider = style ? DeepClone(style) : {};
	var trackStyle = style.track;
	var trackConfig = config.track;
	if (trackStyle || trackConfig) slider.track = CreateBackground(scene, trackConfig || {}, trackStyle || {});
	var thumbStyle = style.thumb;
	var thumbConfig = config.thumb;
	if (thumbStyle || thumbConfig) slider.thumb = CreateBackground(scene, thumbConfig || {}, thumbStyle || {});
	return slider;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/scrollable/BindingTargetMethods.js
var BindingTargetMethods_default$4 = { setBindingTarget(target) {
	var child = this.childrenMap.panel;
	if (child.setBindingTarget) child.setBindingTarget(target);
	return this;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/scrollable/InputRowTitleWidthMethods.js
var InputRowTitleWidthMethods_default$3 = {
	getMaxInputRowTitleWidth() {
		return this.childrenMap.panel.getMaxInputRowTitleWidth() + this.getInnerPadding("left");
	},
	setInputRowTitleWidth(width) {
		width -= this.getInnerPadding("left");
		this.childrenMap.panel.setInputRowTitleWidth(width);
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/scrollable/SetReadOnlyMethods.js
var SetReadOnlyMethods_default$1 = { setReadOnly(value) {
	var child = this.childrenMap.panel;
	if (child.setReadOnly) child.setReadOnly(value);
	return this;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/scrollable/Scrollable.js
var Scrollable = class extends ScrollablePanel {
	constructor(scene, config) {
		super(scene, config);
		this.type = "rexTweaker.Scrollable";
	}
	setTitle(config) {
		var title = this.childrenMap.header;
		if (config.text || config.title || config.icon) title.show().setTitle(config);
		else title.hide();
		return this;
	}
};
Object.assign(Scrollable.prototype, BindingTargetMethods_default$4, InputRowTitleWidthMethods_default$3, SetReadOnlyMethods_default$1);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/CreateScrollable.js
var GetValue$45 = __webpack_exports__Utils.Objects.GetValue;
var CreateScrollable = function(tweaker, config, style) {
	if (!config) config = {};
	if (!style) style = {};
	var scene = tweaker.scene;
	var background = CreateBackground(scene, config.background || {}, style.background || {});
	var title = CreateTitleLabel(scene, void 0, style.title || {});
	var tweakerConfig = {
		root: tweaker.root,
		styles: tweaker.styles
	};
	var child = tweaker.createTweaker(tweakerConfig);
	var slider = CreateSlider$1(scene, config.slider, style.slider);
	var scrollable = new Scrollable(scene, {
		scrollMode: 0,
		header: title,
		panel: {
			child,
			mask: { padding: 1 }
		},
		slider,
		background,
		height: GetValue$45(config, "height", 0),
		space: style.space
	});
	scene.add.existing(scrollable);
	return scrollable;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/methods/AddScrollable.js
var GetValue$44 = __webpack_exports__Utils.Objects.GetValue;
var AddScrollable = function(config) {
	if (config === void 0) config = {};
	var scrollableStyle = GetValue$44(this.styles, "scrollable") || {};
	var scrollable = CreateScrollable(this, config, scrollableStyle);
	this.add(scrollable, {
		proportion: scrollable.minWidth === 0 ? 1 : 0,
		expand: true
	});
	scrollable.setTitle(config);
	var childTweaker = scrollable.childrenMap.panel;
	if (config.key) this.root.addChildrenMap(config.key, childTweaker);
	return childTweaker;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/string/GetLeafKey.js
var GetLeafKey = function(key, delimiter = ".") {
	var lastDotIndex = key.lastIndexOf(delimiter);
	return lastDotIndex === -1 ? key : key.slice(lastDotIndex + 1);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/arraytable/gridtable/BindingTargetMethods.js
var BindingTargetMethods_default$3 = {
	setBindingTarget(target, bindingKey) {
		this.bindingTarget = target;
		if (arguments.length === 2) this.setBindingTargetKey(bindingKey);
		var items;
		if (!this.isRootTarget) items = GetValue$279(target, bindingKey);
		else items = target;
		this.setItems(items);
		return this;
	},
	setBindingTargetKey(bindingKey) {
		this.bindTargetKey = bindingKey;
		this.isRootTarget = !bindingKey || bindingKey === "";
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/arraytable/gridtable/MonitorTargetMethods.js
var MonitorTargetMethods_default$2 = {
	startMonitorTarget() {
		if (this.isMonitoring) return this;
		this.lastItemsCount = this.items ? this.items.length : 0;
		this.isMonitoring = true;
		this.scene.events.on("postupdate", this.onMonitorTarget, this);
		return this;
	},
	stopMonitorTarget() {
		if (!this.isMonitoring) return this;
		this.isMonitoring = false;
		this.scene.events.off("postupdate", this.onMonitorTarget, this);
		return this;
	},
	onMonitorTarget() {
		var lastItemsCount = this.items ? this.items.length : 0;
		if (this.lastItemsCount === lastItemsCount) return;
		this.lastItemsCount = lastItemsCount;
		this.refresh();
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/arraytable/gridtable/InputRowTitleWidthMethods.js
var InputRowTitleWidthMethods_default$2 = {
	getMaxInputRowTitleWidth() {
		return 0;
	},
	setInputRowTitleWidth(width) {
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/arraytable/gridtable/InstallAddButton.js
var InstallAddButton$1 = function(config) {
	var button = config.addButton;
	if (!button) return;
	var createDefaultItemCallback = button.createDefaultItem;
	button.onClick(function() {
		if (this.readOnly) return;
		var item = createDefaultItemCallback();
		this.addItemWithTransition(item);
	}, this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/arraytable/gridtable/InstallClearButton.js
var InstallClearButton$1 = function(config) {
	var button = config.clearButton;
	if (!button) return;
	button.onClick(function() {
		if (this.readOnly) return;
		this.clearItemsWithTransition();
	}, this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/arraytable/gridtable/InstallCellInteractiveEvents.js
var InstallCellInteractiveEvents$1 = function(config) {
	this.on("cell.over", function(cellContainer, cellIndex) {
		cellContainer.onOver();
	}).on("cell.out", function(cellContainer, cellIndex) {
		cellContainer.onOut();
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/arraytable/gridtable/OnClickButtonMethods.js
var OnClickButtonMethods_default$3 = {
	deleteItemByIndex(index) {
		this.items.splice(index, 1);
		this.lastItemsCount = this.items.length;
		this.refresh();
		return this;
	},
	addItem(item) {
		this.items.push(item);
		this.lastItemsCount = this.items.length;
		this.refresh();
		return this;
	},
	clearItems() {
		this.items.length = 0;
		this.lastItemsCount = this.items.length;
		this.refresh();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/arraytable/gridtable/TransitionMethods.js
var TransitionMethods_default$1 = {
	deleteItemWithTransition(cellContainer) {
		if (!cellContainer || cellContainer.cellIndex == null) return this;
		var self = this;
		cellContainer.tweenSelf({
			alpha: 0,
			scaleY: 0,
			duration: 500,
			onComplete() {
				var cellIndex = cellContainer.cellIndex;
				cellContainer.cellIndex = void 0;
				self.deleteItemByIndex(cellIndex);
				self.resetPointerOver();
			}
		});
		return this;
	},
	addItemWithTransition(item) {
		this.addItem(item);
		this.scrollToBottom();
		var newItemIndex = this.items.length - 1;
		var cellContainer = this.getCellContainer(newItemIndex);
		if (!cellContainer) {
			this.resetPointerOver();
			return this;
		}
		this.setChildLocalAlpha(cellContainer, 0).setChildLocalScale(cellContainer, 1, 0);
		var self = this;
		cellContainer.tweenSelf({
			alpha: 1,
			scaleY: 1,
			duration: 500,
			onComplete() {
				self.resetPointerOver();
			}
		});
		return this;
	},
	clearItemsWithTransition() {
		var cellContainers = this.getAllCellContainers();
		if (!cellContainers || cellContainers.length === 0) {
			this.clearItems();
			this.resetPointerOver();
			return this;
		}
		var self = this;
		for (var i = 0, cnt = cellContainers.length; i < cnt; i++) if (i === 0) cellContainers[i].tweenSelf({
			alpha: 0,
			duration: 500,
			onComplete() {
				self.clearItems();
				self.resetPointerOver();
			}
		});
		else cellContainers[i].tweenSelf({
			alpha: 0,
			duration: 490
		});
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/arraytable/gridtable/ArrayTable.js
var ArrayTable = class extends GridTable {
	constructor(scene, config) {
		SetValue(config, "reuseCellContainer", true);
		SetValue(config, "table.enableLayer", true);
		SetValue(config, "table.over.mode", "boundary");
		super(scene, config);
		this.type = "rexTweaker.ArrayTable";
		this.resetCellSizeFlag = true;
		this.lastItemsCount = void 0;
		this.setReadOnly(false);
		InstallClearButton$1.call(this, config);
		InstallAddButton$1.call(this, config);
		InstallCellInteractiveEvents$1.call(this, config);
		var header = this.childrenMap.header;
		if (header) this.bringChildToTop(header);
		var footer = this.childrenMap.footer;
		if (footer) this.bringChildToTop(footer);
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		this.stopMonitorTarget();
		super.destroy(fromScene);
	}
	setItems(items, updateTable) {
		if (this.resetCellSizeFlag && items.length > 0) {
			this.resetCellSizeFlag = false;
			super.setItems(items, false);
			this.resetCellSizeFromCell();
		} else super.setItems(items, updateTable);
		return this;
	}
	setTitle(config) {
		var title = this.childrenMap.header;
		if (config && (config.title || config.icon)) title.show().setTitle(config);
		else title.hide();
		return this;
	}
	setReadOnly(value) {
		if (value === void 0) value = true;
		this.readOnly = value;
		var cellConteiners = this.getAllCellContainers();
		for (var i = 0, cnt = cellConteiners.length; i < cnt; i++) cellConteiners[i].setReadOnly(value);
		return this;
	}
};
Object.assign(ArrayTable.prototype, BindingTargetMethods_default$3, MonitorTargetMethods_default$2, InputRowTitleWidthMethods_default$2, OnClickButtonMethods_default$3, TransitionMethods_default$1);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/utils/ButtonStyleBase.js
var ButtonStyleBase = {
	text: null,
	action: null
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/utils/CreateAddButton.js
var GetValue$43 = __webpack_exports__Utils.Objects.GetValue;
var DefaultCallback = function() {
	return {};
};
var CreateAddButton = function(parent, config, style) {
	if (!GetValue$43(config, "createDefaultItem", DefaultCallback)) return null;
	var scene = parent;
	var addButton;
	var addButtonStyle = style.addButton;
	if (addButtonStyle) {
		addButtonStyle = Object.assign(DeepClone(ButtonStyleBase), addButtonStyle);
		addButton = CreateLabel(scene, addButtonStyle);
	} else {
		addButtonStyle = GetValue$43(style, "tweaker.inputRow.button") || {};
		addButton = CreateLabel(scene, addButtonStyle);
		addButton.resetDisplayContent(GetValue$43(config, "addLabel", "Add"));
	}
	addButton.createDefaultItem = GetValue$43(config, "createDefaultItem", DefaultCallback);
	return addButton;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/utils/CreateClearButton.js
var GetValue$42 = __webpack_exports__Utils.Objects.GetValue;
var CreateClearButton = function(parent, config, style) {
	if (!GetValue$42(config, "clearItems", true)) return null;
	var scene = parent;
	var clearButton;
	var clearButtonStyle = style.clearButton;
	if (clearButtonStyle) {
		clearButtonStyle = Object.assign(DeepClone(ButtonStyleBase), clearButtonStyle);
		clearButton = CreateLabel(scene, clearButtonStyle);
	} else {
		clearButtonStyle = GetValue$42(style, "tweaker.inputRow.button") || {};
		clearButton = CreateLabel(scene, clearButtonStyle);
		clearButton.resetDisplayContent(GetValue$42(config, "clearLabel", "Clear"));
	}
	return clearButton;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/utils/CreateDeleteButton.js
var GetValue$41 = __webpack_exports__Utils.Objects.GetValue;
var CreateDeleteButton = function(scene, config, style) {
	var deleteButtonStyle = GetValue$41(style, "deleteButton");
	var deleteLabel;
	if (deleteButtonStyle) deleteButtonStyle = Object.assign(DeepClone(ButtonStyleBase), deleteButtonStyle);
	else {
		deleteButtonStyle = GetValue$41(style, "tweaker.inputRow.button") || {};
		deleteLabel = GetValue$41(config, "deleteLabel", "X");
	}
	var deleteButton = CreateLabel(scene, deleteButtonStyle);
	if (deleteLabel) deleteButton.resetDisplayContent(deleteLabel);
	return deleteButton;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/utils/CreateMoveUpButton.js
var GetValue$40 = __webpack_exports__Utils.Objects.GetValue;
var CreateMoveUpButton = function(scene, config, style) {
	var moveUpButtonStyle = GetValue$40(style, "moveUpButton");
	var moveUpLabel;
	if (moveUpButtonStyle) moveUpButtonStyle = Object.assign(DeepClone(ButtonStyleBase), moveUpButtonStyle);
	else {
		moveUpButtonStyle = GetValue$40(style, "tweaker.inputRow.button") || {};
		moveUpLabel = GetValue$40(config, "moveUpLabel", "Up");
	}
	var moveUpButton = CreateLabel(scene, moveUpButtonStyle);
	if (moveUpLabel) moveUpButton.resetDisplayContent(moveUpLabel);
	return moveUpButton;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/utils/CreateMoveDownButton.js
var GetValue$39 = __webpack_exports__Utils.Objects.GetValue;
var CreateMoveDownButton = function(scene, config, style) {
	var moveDownButtonStyle = GetValue$39(style, "moveDownButton");
	var moveDownLabel;
	if (moveDownButtonStyle) moveDownButtonStyle = Object.assign(DeepClone(ButtonStyleBase), moveDownButtonStyle);
	else {
		moveDownButtonStyle = GetValue$39(style, "tweaker.inputRow.button") || {};
		moveDownLabel = GetValue$39(config, "moveDownLabel", "Down");
	}
	var moveDownButton = CreateLabel(scene, moveDownButtonStyle);
	if (moveDownLabel) moveDownButton.resetDisplayContent(moveDownLabel);
	return moveDownButton;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/utils/CreatExpandContainer.js
var CreatExpandContainer = function(scene, orientation) {
	var container = new Sizer(scene, { orientation });
	scene.add.existing(container);
	return container;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/arraytable/cellcontainer/LayoutMode0.js
var GetValue$38 = __webpack_exports__Utils.Objects.GetValue;
var LayoutMode0$1 = function(config) {
	var scene = this.scene;
	var indexLabel = config.indexLabel;
	var inputTweaker = config.inputTweaker;
	var deleteButton = config.deleteButton;
	var moveUpButton = config.moveUpButton;
	var moveDownButton = config.moveDownButton;
	var space = config.space || {};
	this.orientation = 0;
	this.add(indexLabel, {
		proportion: 0,
		expand: true,
		padding: { right: GetValue$38(space, "index", 0) }
	});
	this.add(inputTweaker, {
		proportion: 1,
		expand: true,
		padding: { right: GetValue$38(space, "tweaker", 0) }
	});
	var moveUpDownButtons = CreatExpandContainer(scene, 1);
	moveUpDownButtons.add(moveUpButton, {
		proportion: 1,
		expand: true
	}).add(moveDownButton, {
		proportion: 1,
		expand: true
	});
	this.add(moveUpDownButtons, {
		proportion: 0,
		expand: true
	});
	this.add(deleteButton, {
		proportion: 0,
		expand: true
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/arraytable/cellcontainer/LayoutMode1.js
var GetValue$37 = __webpack_exports__Utils.Objects.GetValue;
var LayoutMode1$1 = function(config) {
	var scene = this.scene;
	var indexLabel = config.indexLabel;
	var inputTweaker = config.inputTweaker;
	var deleteButton = config.deleteButton;
	var space = config.space || {};
	this.orientation = 1;
	var header = CreatExpandContainer(scene, 0);
	this.add(header, {
		proportion: 0,
		expand: true,
		padding: { bottom: GetValue$37(space, "index", 0) }
	});
	header.add(indexLabel, {
		proportion: 0,
		expand: true
	}).addSpace().add(deleteButton, {
		proportion: 0,
		expand: true
	});
	this.add(inputTweaker, {
		proportion: 1,
		expand: true
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/arraytable/cellcontainer/Build.js
var GetValue$36 = __webpack_exports__Utils.Objects.GetValue;
var LayoutCallbacks$1 = [LayoutMode0$1, LayoutMode1$1];
var Build$1 = function(config) {
	if (config === void 0) config = {};
	var background = config.background;
	var indexLabel = config.indexLabel;
	var inputTweaker = config.inputTweaker;
	var deleteButton = config.deleteButton;
	var moveUpButton = config.moveUpButton;
	var moveDownButton = config.moveDownButton;
	RemoveFromParent(background);
	RemoveFromParent(indexLabel);
	RemoveFromParent(inputTweaker);
	RemoveFromParent(deleteButton);
	RemoveFromParent(moveUpButton);
	RemoveFromParent(moveDownButton);
	this.clear(true);
	if (background) this.addBackground(background);
	(LayoutCallbacks$1[GetValue$36(config, "layoutMode", 0)] || LayoutCallbacks$1[0]).call(this, config);
	this.addChildrenMap("background", background);
	this.addChildrenMap("index", indexLabel);
	this.addChildrenMap("inputTweaker", inputTweaker);
	this.addChildrenMap("delete", deleteButton);
	this.addChildrenMap("moveUp", moveUpButton);
	this.addChildrenMap("moveDown", moveDownButton);
	deleteButton.offClick(this.onClickDeleteButton, this).onClick(this.onClickDeleteButton, this);
	moveUpButton.offClick(this.onClickMoveUpButton, this).onClick(this.onClickMoveUpButton, this);
	moveDownButton.offClick(this.onClickMoveDownButton, this).onClick(this.onClickMoveDownButton, this);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/arraytable/cellcontainer/OnClickButtonMethods.js
var OnClickButtonMethods_default$2 = {
	onClickDeleteButton() {
		if (this.gridTable.readOnly) return;
		if (!this.gridTable.isInTouching("mask")) return;
		this.gridTable.deleteItemWithTransition(this);
	},
	onClickMoveUpButton() {
		if (this.gridTable.readOnly) return;
		if (!this.gridTable.isInTouching("mask")) return;
		var items = this.gridTable.items;
		if (!items || items.length <= 1) return;
		var currentIndex = this.cellIndex;
		var targetIndex = currentIndex === 0 ? items.length - 1 : currentIndex - 1;
		var tmp = items[currentIndex];
		items[currentIndex] = items[targetIndex];
		items[targetIndex] = tmp;
		this.gridTable.refresh().scrollToRow(targetIndex);
	},
	onClickMoveDownButton() {
		if (this.gridTable.readOnly) return;
		if (!this.gridTable.isInTouching("mask")) return;
		var items = this.gridTable.items;
		if (!items || items.length <= 1) return;
		var currentIndex = this.cellIndex;
		var targetIndex = currentIndex === items.length - 1 ? 0 : currentIndex + 1;
		var tmp = items[currentIndex];
		items[currentIndex] = items[targetIndex];
		items[targetIndex] = tmp;
		this.gridTable.refresh().scrollToRow(targetIndex);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/arraytable/cellcontainer/OnPointerOverOutMethods.js
var OnPointerOverOutMethods_default$1 = {
	onOver() {
		this.getElement("background").setHoverState(true);
	},
	onOut() {
		this.getElement("background").setHoverState(false);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/arraytable/cellcontainer/CellContainer.js
var CellContainer$1 = class extends Sizer {
	constructor(scene, config) {
		super(scene, { space: config.space });
		this.type = "rexTweaker.ArrayTable.CellContainer";
		this.gridTable = config.gridTable;
		this.build(config);
	}
	setItem(items, index) {
		var item = items[index];
		this.cellIndex = index;
		if (typeof item === "object") this.setBindingTarget(item);
		else this.setBindingTarget(items, index);
		this.setReadOnly(this.gridTable.readOnly);
		return this;
	}
	setIndexLabel(content) {
		var indexLabel = this.childrenMap.index;
		if (typeof content === "string") indexLabel.setText(content);
		else indexLabel.setTitle(content);
		return this;
	}
	setBindingTarget(target, key) {
		this.childrenMap.inputTweaker.setBindingTarget(target, key);
		return this;
	}
	get visible() {
		return super.visible;
	}
	set visible(value) {
		super.visible = value;
		if (!value) this.setBindingTarget();
	}
	setReadOnly(value) {
		this.childrenMap.inputTweaker.setReadOnly(value);
		return this;
	}
};
var Methods$2 = { build: Build$1 };
Object.assign(CellContainer$1.prototype, Methods$2, OnClickButtonMethods_default$2, OnPointerOverOutMethods_default$1);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/createarraytable/GenerateCreateCellContainerCallback.js
var GetValue$35 = __webpack_exports__Utils.Objects.GetValue;
var Format$1 = __webpack_exports__Utils.String.Format;
var CreateCellContainer$1 = function(tweaker, cell, config) {
	var scene = tweaker.scene;
	var { space, gridTable, indexStyle, createDeleteButton, createMoveUpButton, createMoveDownButton, tweakerConfig, tweakerAddRowsParameters, backgroundStyle } = config;
	var background = CreateBackground(scene, {}, backgroundStyle);
	var indexLabel = CreateTitleLabel(scene, void 0, indexStyle);
	var deleteButton = createDeleteButton(scene);
	var moveUpButton = createMoveUpButton(scene);
	var moveDownButton = createMoveDownButton(scene);
	var properties = tweakerAddRowsParameters.properties;
	var isObjectItem = properties.length >= 1 && properties[0].hasOwnProperty("$key");
	var target;
	if (isObjectItem) target = cell.item;
	else {
		target = cell.items;
		properties = [{
			...properties[0],
			title: false,
			$key: cell.index
		}];
	}
	var cellContainer = new CellContainer$1(scene, {
		space,
		gridTable,
		background,
		indexLabel,
		inputTweaker: tweaker.createTweaker(tweakerConfig).setAlignInputRowTitleEnable(isObjectItem).addRows(properties, target, tweakerAddRowsParameters.monitor),
		deleteButton,
		moveUpButton,
		moveDownButton
	});
	scene.add.existing(cellContainer);
	return cellContainer;
};
var GenerateCreateCellContainerCallback$1 = function(tweaker, config, style) {
	if (style === void 0) style = {};
	var space = GetValue$35(config, "space.cell", void 0, style) || {};
	var indexStyle = style.index;
	if (!indexStyle) indexStyle = GetValue$35(style, "tweaker.inputRow.title") || {};
	var indexLabelCallback = GetValue$35(config, "indexLabel", "%1");
	if (typeof indexLabelCallback === "string") {
		var indexLabelTemplate = indexLabelCallback;
		indexLabelCallback = function(index, item, items) {
			return { "title": Format$1(indexLabelTemplate, [index, items.length]) };
		};
	}
	var createDeleteButton = function(scene) {
		return CreateDeleteButton(scene, config, style);
	};
	var createMoveUpButton = function(scene) {
		return CreateMoveUpButton(scene, config, style);
	};
	var createMoveDownButton = function(scene) {
		return CreateMoveDownButton(scene, config, style);
	};
	var tweakerConfig = {
		root: tweaker.root,
		styles: tweaker.styles,
		expandInputRowHeight: true
	};
	var properties = GetValue$35(config, "$properties") || {};
	if (!Array.isArray(properties)) properties = [properties];
	var monitor = GetValue$35(config, "monitor", false);
	var tweakerAddRowsParameters = {
		properties,
		monitor
	};
	var backgroundStyle = GetValue$35(style, "cellBackground");
	var callback = function(cell, cellContainer, gridTable) {
		var width = cell.width;
		cell.height;
		var item = cell.item;
		var items = cell.items;
		var index = cell.index;
		if (cellContainer === null) cellContainer = CreateCellContainer$1(tweaker, cell, {
			space,
			gridTable,
			indexStyle,
			createDeleteButton,
			createMoveUpButton,
			createMoveDownButton,
			tweakerConfig,
			tweakerAddRowsParameters,
			backgroundStyle
		});
		cell.setCellContainerAlign("center");
		cellContainer.setIndexLabel(indexLabelCallback(index, item, items)).setItem(items, index).setMinSize(width, 0).setOrigin(.5, 0).setDirty(true).layout().setDirty(false);
		return cellContainer;
	};
	return callback;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/createarraytable/CreateArrayTable.js
var GetValue$34 = __webpack_exports__Utils.Objects.GetValue;
var CreateArrayTable = function(tweaker, config, style) {
	if (!config) config = {};
	if (!style) style = {};
	var scene = tweaker.scene;
	var background = CreateBackground(scene, config.background || {}, style.background || {});
	var title = CreateTitleLabel(scene, void 0, style.title || {});
	var tableConfig = GetValue$34(config, "table", void 0, style) || {};
	var slider = CreateSlider$1(scene, config.slider, style.slider);
	var footer;
	var clearButton = CreateClearButton(scene, config, style);
	var addButton = CreateAddButton(scene, config, style);
	if (clearButton || addButton) {
		footer = new Sizer(scene, { space: { item: GetValue$34(config, "space.button", 0, style) } });
		scene.add.existing(footer);
		if (clearButton) footer.add(clearButton, {
			proportion: 0,
			expand: true
		});
		footer.addSpace();
		if (addButton) footer.add(addButton, {
			proportion: 0,
			expand: true
		});
	}
	var arrayTable = new ArrayTable(scene, {
		table: tableConfig,
		clampChildOY: true,
		header: title,
		footer,
		clearButton,
		addButton,
		slider,
		background,
		height: GetValue$34(config, "height", 0, style),
		space: Merge$1(config.space || {}, style.space || {}),
		createCellContainerCallback: GenerateCreateCellContainerCallback$1(tweaker, config, style)
	});
	scene.add.existing(arrayTable);
	return arrayTable;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/BindingTargetMethods.js
var BindingTargetMethods_default$2 = {
	setBindingTarget(target, bindingKey) {
		if (this.leftPanel && this.leftPanel.setBindingTarget) {
			if (arguments.length >= 2) this.leftPanel.setBindingTarget(target, bindingKey);
			else this.leftPanel.setBindingTarget(target);
		}
		if (this.leftPanel.items.length > 0) this.selectItem(0, false);
		else this.clearSelection();
		return this;
	},
	startMonitorTarget() {
		this.leftPanel.startMonitorTarget();
		return this;
	},
	stopMonitorTarget() {
		this.leftPanel.stopMonitorTarget();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/InputRowTitleWidthMethods.js
var InputRowTitleWidthMethods_default$1 = {
	getMaxInputRowTitleWidth() {
		return 0;
	},
	setInputRowTitleWidth(width) {
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/scrollable/Title.js
var GetValue$33 = __webpack_exports__Utils.Objects.GetValue;
var Title = class extends Sizer {
	constructor(scene, config) {
		super(scene, { orientation: 0 });
		this.type = "rexTweaker.ListDetail.EditorContainer.Title";
		var indexLabel = GetValue$33(config, "editorIndexLabel");
		var displayNameLabel = GetValue$33(config, "editorDisplayNameLabel");
		if (indexLabel) this.add(indexLabel, {
			proportion: 0,
			expand: true
		});
		if (displayNameLabel) this.add(displayNameLabel, {
			proportion: 1,
			expand: true
		});
		this.addChildrenMap("index", indexLabel);
		this.addChildrenMap("displayName", displayNameLabel);
	}
	setTitle(indexConfig, displayNameConfig) {
		SetLabel(this.childrenMap.index, indexConfig);
		SetLabel(this.childrenMap.displayName, displayNameConfig);
		return this;
	}
};
var SetLabel = function(label, config) {
	if (!label) return;
	if (config === void 0) config = "";
	if (typeof config === "string") label.setText(config);
	else label.setTitle(config);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/scrollable/Toolbar.js
var GetValue$32 = __webpack_exports__Utils.Objects.GetValue;
var Toolbar = class extends Sizer {
	constructor(scene, config) {
		super(scene, { orientation: 0 });
		this.type = "rexTweaker.ListDetail.EditorContainer.Toolbar";
		var deleteButton = GetValue$32(config, "editorDeleteButton");
		var duplicateButton = GetValue$32(config, "editorDuplicateButton");
		var resetButton = GetValue$32(config, "editorResetButton");
		var previousButton = GetValue$32(config, "editorPreviousButton");
		var nextButton = GetValue$32(config, "editorNextButton");
		if (deleteButton) this.add(deleteButton, {
			proportion: 0,
			expand: true
		});
		this.addSpace();
		if (duplicateButton) this.add(duplicateButton, {
			proportion: 0,
			expand: true
		});
		if (resetButton) this.add(resetButton, {
			proportion: 0,
			expand: true
		});
		if (previousButton) this.add(previousButton, {
			proportion: 0,
			expand: true
		});
		if (nextButton) this.add(nextButton, {
			proportion: 0,
			expand: true
		});
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/scrollable/EditorContainer.js
var EditorContainer = class extends Scrollable {
	constructor(scene, config) {
		if (config === void 0) config = {};
		var title = new Title(scene, config);
		scene.add.existing(title);
		SetValue(config, "header", title);
		SetValue(config, "expand.header", true);
		var toolbar;
		if (HasToolbar(config)) {
			toolbar = new Toolbar(scene, config);
			scene.add.existing(toolbar);
			if (toolbar) {
				SetValue(config, "footer", toolbar);
				SetValue(config, "expand.footer", true);
			}
		}
		super(scene, config);
		this.type = "rexTweaker.ListDetail.EditorContainer";
		this.addChildrenMap("title", title);
		this.addChildrenMap("inputTweaker", config.editor);
		this.addChildrenMap("toolbar", toolbar);
		var deleteButton = config.editorDeleteButton;
		var duplicateButton = config.editorDuplicateButton;
		var resetButton = config.editorResetButton;
		var previousButton = config.editorPreviousButton;
		var nextButton = config.editorNextButton;
		this.addChildrenMap("deleteButton", deleteButton);
		this.addChildrenMap("duplicateButton", duplicateButton);
		this.addChildrenMap("resetButton", resetButton);
		this.addChildrenMap("previousButton", previousButton);
		this.addChildrenMap("nextButton", nextButton);
		if (deleteButton) deleteButton.offClick(this.onClickDeleteButton, this).onClick(this.onClickDeleteButton, this);
		if (duplicateButton) duplicateButton.offClick(this.onClickDuplicateButton, this).onClick(this.onClickDuplicateButton, this);
		if (resetButton) resetButton.offClick(this.onClickResetButton, this).onClick(this.onClickResetButton, this);
		if (previousButton) previousButton.offClick(this.onClickPreviousButton, this).onClick(this.onClickPreviousButton, this);
		if (nextButton) nextButton.offClick(this.onClickNextButton, this).onClick(this.onClickNextButton, this);
	}
	onClickDeleteButton() {
		this.emit("toolbar.delete");
	}
	onClickDuplicateButton() {
		this.emit("toolbar.duplicate");
	}
	onClickResetButton() {
		this.emit("toolbar.reset");
	}
	onClickPreviousButton() {
		this.emit("toolbar.previous");
	}
	onClickNextButton() {
		this.emit("toolbar.next");
	}
	setTitle(indexConfig, displayNameConfig) {
		this.childrenMap.header.setTitle(indexConfig, displayNameConfig);
		return this;
	}
	setReadOnly(value) {
		if (value === void 0) value = true;
		this.readOnly = value;
		this.childrenMap.inputTweaker.setReadOnly(value);
		return this;
	}
};
var HasToolbar = function(config) {
	return !!config.editorDeleteButton || !!config.editorDuplicateButton || !!config.editorResetButton || !!config.editorPreviousButton || !!config.editorNextButton;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/gridtable/BindingTargetMethods.js
var BindingTargetMethods_default$1 = {
	setBindingTarget(target, bindingKey) {
		this.bindingTarget = target;
		if (arguments.length === 2) this.setBindingTargetKey(bindingKey);
		var items;
		if (!this.isRootTarget) items = GetValue$279(target, bindingKey);
		else items = target;
		this.setItems(items);
		return this;
	},
	setBindingTargetKey(bindingKey) {
		this.bindTargetKey = bindingKey;
		this.isRootTarget = !bindingKey || bindingKey === "";
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/gridtable/MonitorTargetMethods.js
var MonitorTargetMethods_default$1 = {
	startMonitorTarget() {
		if (this.isMonitoring) return this;
		this.lastItemsCount = this.items ? this.items.length : 0;
		this.isMonitoring = true;
		this.scene.events.on("postupdate", this.onMonitorTarget, this);
		return this;
	},
	stopMonitorTarget() {
		if (!this.isMonitoring) return this;
		this.isMonitoring = false;
		this.scene.events.off("postupdate", this.onMonitorTarget, this);
		return this;
	},
	onMonitorTarget() {
		var lastItemsCount = this.items ? this.items.length : 0;
		if (this.lastItemsCount === lastItemsCount) return;
		this.lastItemsCount = lastItemsCount;
		this.refresh();
		this.emit("items.change", "monitor");
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/gridtable/InputRowTitleWidthMethods.js
var InputRowTitleWidthMethods_default = {
	getMaxInputRowTitleWidth() {
		return 0;
	},
	setInputRowTitleWidth(width) {
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/gridtable/InstallAddButton.js
var InstallAddButton = function(config) {
	var button = config.addButton;
	if (!button) return;
	var createDefaultItemCallback = button.createDefaultItem;
	button.onClick(function() {
		if (this.readOnly) return;
		var item = createDefaultItemCallback();
		this.addItemWithTransition(item);
	}, this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/gridtable/InstallClearButton.js
var InstallClearButton = function(config) {
	var button = config.clearButton;
	if (!button) return;
	button.onClick(function() {
		if (this.readOnly) return;
		this.clearItemsWithTransition();
	}, this);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/gridtable/InstallCellInteractiveEvents.js
var InstallCellInteractiveEvents = function(config) {
	this.on("cell.click", function(cellContainer, cellIndex) {
		this.emit("select", cellContainer, cellIndex);
	}, this).on("cell.over", function(cellContainer, cellIndex) {
		cellContainer.onOver();
	}).on("cell.out", function(cellContainer, cellIndex) {
		cellContainer.onOut();
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/gridtable/OnClickButtonMethods.js
var OnClickButtonMethods_default$1 = {
	deleteItemByIndex(index) {
		var removedItem = this.items[index];
		this.items.splice(index, 1);
		this.lastItemsCount = this.items.length;
		this.refresh();
		this.emit("items.change", "delete", {
			index,
			item: removedItem
		});
		return this;
	},
	addItem(item) {
		this.items.push(item);
		this.lastItemsCount = this.items.length;
		this.refresh();
		this.emit("items.change", "add", {
			index: this.items.length - 1,
			item
		});
		return this;
	},
	clearItems() {
		this.items.length = 0;
		this.lastItemsCount = this.items.length;
		this.refresh();
		this.emit("items.change", "clear");
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/gridtable/TransitionMethods.js
var TransitionMethods_default = {
	deleteItemWithTransition(cellContainer) {
		if (!cellContainer || cellContainer.cellIndex == null) return this;
		var self = this;
		cellContainer.tweenSelf({
			alpha: 0,
			scaleY: 0,
			duration: 500,
			onComplete() {
				var cellIndex = cellContainer.cellIndex;
				cellContainer.cellIndex = void 0;
				self.deleteItemByIndex(cellIndex);
				self.resetPointerOver();
			}
		});
		return this;
	},
	addItemWithTransition(item) {
		this.addItem(item);
		this.scrollToBottom();
		var newItemIndex = this.items.length - 1;
		var cellContainer = this.getCellContainer(newItemIndex);
		if (!cellContainer) {
			this.resetPointerOver();
			return this;
		}
		this.setChildLocalAlpha(cellContainer, 0).setChildLocalScale(cellContainer, 1, 0);
		var self = this;
		cellContainer.tweenSelf({
			alpha: 1,
			scaleY: 1,
			duration: 500,
			onComplete() {
				self.resetPointerOver();
			}
		});
		return this;
	},
	clearItemsWithTransition() {
		var cellContainers = this.getAllCellContainers();
		if (!cellContainers || cellContainers.length === 0) {
			this.clearItems();
			this.resetPointerOver();
			return this;
		}
		var self = this;
		for (var i = 0, cnt = cellContainers.length; i < cnt; i++) if (i === 0) cellContainers[i].tweenSelf({
			alpha: 0,
			duration: 500,
			onComplete() {
				self.clearItems();
				self.resetPointerOver();
			}
		});
		else cellContainers[i].tweenSelf({
			alpha: 0,
			duration: 490
		});
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/gridtable/ListTable.js
var ListTable = class extends GridTable {
	constructor(scene, config) {
		SetValue(config, "reuseCellContainer", true);
		SetValue(config, "table.enableLayer", true);
		SetValue(config, "table.over.mode", "boundary");
		super(scene, config);
		this.type = "rexTweaker.ListDetail.ListTable";
		this.resetCellSizeFlag = true;
		this.lastItemsCount = void 0;
		InstallClearButton.call(this, config);
		InstallAddButton.call(this, config);
		InstallCellInteractiveEvents.call(this, config);
		var footer = this.childrenMap.footer;
		if (footer) this.bringChildToTop(footer);
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		this.stopMonitorTarget();
		super.destroy(fromScene);
	}
	setItems(items, updateTable) {
		if (this.resetCellSizeFlag && items.length > 0) {
			this.resetCellSizeFlag = false;
			super.setItems(items, false);
			this.resetCellSizeFromCell();
		} else super.setItems(items, updateTable);
		return this;
	}
	setTitle(config) {
		var title = this.childrenMap.header;
		if (config.title || config.icon) title.show().setTitle(config);
		else title.hide();
		return this;
	}
	setReadOnly(value) {
		if (value === void 0) value = true;
		this.readOnly = value;
		return this;
	}
};
Object.assign(ListTable.prototype, BindingTargetMethods_default$1, MonitorTargetMethods_default$1, InputRowTitleWidthMethods_default, OnClickButtonMethods_default$1, TransitionMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/object/DeepMerge.js
var DeepMerge = function(toObj, fromObj) {
	if (fromObj === void 0) return toObj;
	for (var key in fromObj) if (!toObj.hasOwnProperty(key)) toObj[key] = DeepClone(fromObj[key]);
	else {
		var value = toObj[key];
		if (value && typeof value === "object") DeepMerge(value, fromObj[key]);
	}
	return toObj;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/OnToolbarMethods.js
var IsObjectValue = function(value) {
	return !!value && typeof value === "object" && !Array.isArray(value);
};
var OnToolbarMethods_default = {
	onToolbarDelete() {
		if (this.readOnly) return;
		var index = this.selectedIndex;
		if (index == null) return;
		var listTable = this.leftPanel;
		var cellContainer = listTable.getCellContainer(index);
		if (cellContainer) {
			listTable.deleteItemWithTransition(cellContainer);
			return;
		}
		listTable.deleteItemByIndex(index);
		if (listTable.resetPointerOver) listTable.resetPointerOver();
	},
	onToolbarDuplicate() {
		if (this.readOnly) return;
		var index = this.selectedIndex;
		if (index == null) return;
		var listTable = this.leftPanel;
		var items = listTable.items;
		var currentItem = items[index];
		if (!IsObjectValue(currentItem)) {
			console.error("[Tweaker][ListDetail] Duplicate aborted. Current selected item is not an object. This is an application-level design error.");
			return;
		}
		var defaultItem = this.createDefaultItemFromToolbar("duplicateButton", "Duplicate");
		if (!defaultItem) return;
		var newItem = DeepClone(currentItem);
		DeepMerge(newItem, defaultItem);
		var insertIndex = index + 1;
		items.splice(insertIndex, 0, newItem);
		listTable.lastItemsCount = items.length;
		listTable.refresh();
		listTable.emit("items.change", "add", {
			index: insertIndex,
			item: newItem
		});
		this.selectItem(insertIndex, true);
	},
	onToolbarReset() {
		if (this.readOnly) return;
		var index = this.selectedIndex;
		if (index == null) return;
		var listTable = this.leftPanel;
		var items = listTable.items;
		var currentItem = items[index];
		if (!IsObjectValue(currentItem)) {
			console.error("[Tweaker][ListDetail] Reset aborted. Current selected item is not an object. This is an application-level design error.");
			return;
		}
		var defaultItem = this.createDefaultItemFromToolbar("resetButton", "Reset");
		if (!defaultItem) return;
		for (var key in currentItem) if (currentItem.hasOwnProperty(key)) delete currentItem[key];
		DeepMerge(currentItem, defaultItem);
		this.rightPanel.setBindingTarget(currentItem);
		listTable.updateVisibleCell(index);
		this.updateEditorTitle(index, currentItem, items);
	},
	onToolbarPrevious() {
		var index = this.selectedIndex;
		if (index == null) return;
		if (index <= 0) return;
		this.selectItem(index - 1, true);
	},
	onToolbarNext() {
		var index = this.selectedIndex;
		if (index == null) return;
		if (index >= this.leftPanel.items.length - 1) return;
		this.selectItem(index + 1, true);
	},
	createDefaultItemFromToolbar(buttonKey, actionName) {
		var button = this.rightPanel.childrenMap[buttonKey];
		var callback = button ? button.createDefaultItem : void 0;
		if (!callback) {
			console.error(`[Tweaker][ListDetail] ${actionName} aborted. createDefaultItem is required and should return an object. This is an application-level design error.`);
			return null;
		}
		var defaultItem = callback();
		if (!IsObjectValue(defaultItem)) {
			console.error(`[Tweaker][ListDetail] ${actionName} aborted. createDefaultItem() returned ${defaultItem === null ? "null" : typeof defaultItem}, expected object. This is an application-level design error.`);
			return null;
		}
		return defaultItem;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/ListDetail.js
var GetValue$31 = __webpack_exports__Utils.Objects.GetValue;
var Clamp$1 = __webpack_exports__Math.Clamp;
var ListDetail = class extends SplitPanels$1 {
	constructor(scene, config) {
		var { isHorizontalView, space = {}, height, header, splitter, background, listTableConfig, createListCellContainerCallback, listTableSlider, listTableFooter, clearButton, addButton, editor, editorSlider, editorIndexLabel, editorDisplayNameLabel, indexLabelCallback, displayNameLabelCallback, editorDeleteButton, editorDuplicateButton, editorResetButton, editorPreviousButton, editorNextButton, splitRatio, minLeftPanelWidth, minRightPanelWidth, minTopPanelWidth, minBottomPanelWidth } = config;
		var listTable = new ListTable(scene, {
			table: listTableConfig,
			clampChildOY: true,
			footer: listTableFooter,
			clearButton,
			addButton,
			slider: listTableSlider,
			createCellContainerCallback: createListCellContainerCallback,
			space: {
				table: space.table,
				footer: space.footer
			}
		});
		scene.add.existing(listTable);
		var editorContainer = new EditorContainer(scene, {
			scrollMode: 0,
			panel: {
				child: editor,
				mask: { padding: 1 }
			},
			slider: editorSlider,
			editor,
			editorIndexLabel,
			editorDisplayNameLabel,
			editorDeleteButton,
			editorDuplicateButton,
			editorResetButton,
			editorPreviousButton,
			editorNextButton,
			space: {
				panel: space.table,
				header: space.header,
				footer: space.footer
			}
		});
		scene.add.existing(editorContainer);
		super(scene, {
			orientation: 1,
			header,
			leftPanel: isHorizontalView ? listTable : void 0,
			topPanel: isHorizontalView ? void 0 : listTable,
			rightPanel: isHorizontalView ? editorContainer : void 0,
			bottomPanel: isHorizontalView ? void 0 : editorContainer,
			splitter,
			splitRatio,
			minLeftPanelWidth,
			minRightPanelWidth,
			minTopPanelWidth,
			minBottomPanelWidth,
			background,
			height,
			space: {
				left: space.left,
				right: space.right,
				top: space.top,
				bottom: space.bottom,
				leftPanelRight: space.splitter,
				rightPanelLeft: space.splitter,
				topPanelBottom: space.splitter,
				bottomPanelTop: space.splitter
			}
		});
		this.type = "rexTweaker.ListDetail";
		this.alignInputRowTitleStartFlag = GetValue$31(config, "alignTitle");
		this.selectedItem = void 0;
		this.selectedIndex = void 0;
		this.indexLabelCallback = indexLabelCallback;
		this.displayNameLabelCallback = displayNameLabelCallback;
		listTable.on("select", this.onSelectCell, this).on("items.change", this.onItemsChange, this);
		editorContainer.on("toolbar.delete", this.onToolbarDelete, this).on("toolbar.duplicate", this.onToolbarDuplicate, this).on("toolbar.reset", this.onToolbarReset, this).on("toolbar.previous", this.onToolbarPrevious, this).on("toolbar.next", this.onToolbarNext, this);
		editor.on("valuechange", this.onEditorValueChange, this);
	}
	onSelectCell(cellContainer, cellIndex) {
		this.selectItem(cellIndex);
	}
	onItemsChange(changeType, detail) {
		var items = this.leftPanel.items;
		if (items.length === 0) {
			this.clearSelection();
			return;
		}
		var nextIndex;
		var selectedItem = this.selectedItem;
		if (selectedItem !== void 0) {
			nextIndex = items.indexOf(selectedItem);
			if (nextIndex === -1) nextIndex = void 0;
		}
		var selectedIndex = this.selectedIndex;
		if (selectedIndex == null) nextIndex = changeType === "add" ? detail.index : 0;
		else switch (changeType) {
			case "delete":
				if (detail.index < selectedIndex) nextIndex = selectedIndex - 1;
				else nextIndex = selectedIndex;
				break;
			case "move":
				var fromIndex = detail.fromIndex;
				var toIndex = detail.toIndex;
				if (selectedIndex === fromIndex) nextIndex = toIndex;
				else if (selectedIndex === toIndex) nextIndex = fromIndex;
				else nextIndex = selectedIndex;
				break;
			case "clear":
				this.clearSelection();
				return;
			default: nextIndex = selectedIndex;
		}
		if (nextIndex !== void 0) this.selectItem(nextIndex, false);
	}
	onEditorValueChange(value, oldValue, bindingTarget, bindingKey) {
		if (!bindingTarget) return;
		var listTable = this.leftPanel;
		var items = listTable.items;
		var index = items.indexOf(bindingTarget);
		if (index === -1) return;
		listTable.updateVisibleCell(index);
		this.updateEditorTitle(index, bindingTarget, items);
	}
	selectItem(index, scrollToRow) {
		if (scrollToRow === void 0) scrollToRow = false;
		var listTable = this.leftPanel;
		var items = listTable.items;
		if (items.length === 0) return this.clearSelection();
		index = Clamp$1(index, 0, items.length - 1);
		this.selectedIndex = index;
		this.selectedItem = items[index];
		listTable.selectedIndex = index;
		this.rightPanel.setBindingTarget(this.selectedItem);
		this.updateEditorTitle(index, this.selectedItem, items);
		if (scrollToRow) listTable.scrollToRow(index, 500);
		this.updateVisibleCellSelection();
		this.emit("selectitem", this.selectedItem, index, items);
		return this;
	}
	clearSelection() {
		this.selectedIndex = void 0;
		this.selectedItem = void 0;
		var listTable = this.leftPanel;
		listTable.selectedIndex = void 0;
		this.rightPanel.setBindingTarget(void 0);
		this.updateEditorTitle();
		this.updateVisibleCellSelection();
		this.emit("selectitem", void 0, void 0, listTable.items);
		return this;
	}
	updateEditorTitle(index, item, items) {
		if (index === void 0) {
			this.rightPanel.setTitle(void 0, void 0);
			return this;
		}
		if (items === void 0) items = this.leftPanel.items;
		var indexLabelCallback = this.indexLabelCallback;
		var indexConfig = indexLabelCallback ? indexLabelCallback(index, item, items) : void 0;
		var displayNameLabelCallback = this.displayNameLabelCallback;
		var displayNameConfig = displayNameLabelCallback ? displayNameLabelCallback(index, item, items) : void 0;
		this.rightPanel.setTitle(indexConfig, displayNameConfig);
		return this;
	}
	updateVisibleCellSelection() {
		var cellContainers = this.leftPanel.getAllCellContainers();
		for (var i = 0, cnt = cellContainers.length; i < cnt; i++) {
			var cellContainer = cellContainers[i];
			cellContainer.setSelectedState(cellContainer.cellIndex === this.selectedIndex);
		}
		return this;
	}
	preLayout() {
		super.preLayout();
		if (this.alignInputRowTitleStartFlag) this.setInputRowTitleWidth(this.getMaxInputRowTitleWidth());
	}
	setTitle(config) {
		var title = this.childrenMap.header;
		if (config.text || config.title || config.icon) title.show().setTitle(config);
		else title.hide();
		return this;
	}
	setReadOnly(value) {
		if (value === void 0) value = true;
		this.readOnly = value;
		this.leftPanel.setReadOnly(value);
		this.rightPanel.setReadOnly(value);
		return this;
	}
};
Object.assign(ListDetail.prototype, BindingTargetMethods_default$2, InputRowTitleWidthMethods_default$1, OnToolbarMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/createlistdetail/IndexLabelMethods.js
var GetValue$30 = __webpack_exports__Utils.Objects.GetValue;
var Format = __webpack_exports__Utils.String.Format;
var GetIndexLabelStyle = function(style) {
	var indexStyle = GetValue$30(style, "index");
	if (!indexStyle) indexStyle = GetValue$30(style, "tweaker.inputRow.title") || {};
	return indexStyle;
};
var GetIndexLabelCallback = function(config) {
	var indexLabelCallback = GetValue$30(config, "indexLabel", "%1");
	if (typeof indexLabelCallback === "string") {
		var indexLabelTemplate = indexLabelCallback;
		indexLabelCallback = function(index, item, items) {
			return { "title": Format(indexLabelTemplate, [index, items.length]) };
		};
	}
	return indexLabelCallback;
};
var CreateIndexLabel = function(scene, style) {
	return CreateTitleLabel(scene, void 0, style);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/createlistdetail/DisplayNameLabelMethods.js
var GetValue$29 = __webpack_exports__Utils.Objects.GetValue;
var GetDisplayNameStyle = function(style) {
	var displayNameStyle = GetValue$29(style, "displayName");
	if (!displayNameStyle) displayNameStyle = GetValue$29(style, "tweaker.inputRow.title") || {};
	return displayNameStyle;
};
var GetDisplayNameLabelCallback = function(config) {
	var displayNameLabelCallback = GetValue$29(config, "displayNameLabel");
	if (!displayNameLabelCallback) displayNameLabelCallback = function(index, item, items) {
		return { text: "" };
	};
	return displayNameLabelCallback;
};
var CreateDisplayNameLabel = function(scene, style) {
	return CreateTitleLabel(scene, void 0, style);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/cellcontainer/LayoutMode0.js
var GetValue$28 = __webpack_exports__Utils.Objects.GetValue;
var LayoutMode0 = function(config) {
	var scene = this.scene;
	var indexLabel = config.indexLabel;
	var displayNameLabel = config.displayNameLabel;
	var deleteButton = config.deleteButton;
	var moveUpButton = config.moveUpButton;
	var moveDownButton = config.moveDownButton;
	var space = config.space || {};
	this.orientation = 0;
	this.add(indexLabel, {
		proportion: 0,
		expand: true,
		padding: { right: GetValue$28(space, "index", 0) }
	});
	this.add(displayNameLabel, {
		proportion: 1,
		expand: true,
		padding: { right: GetValue$28(space, "tweaker", 0) }
	});
	var moveUpDownButtons = CreatExpandContainer(scene, 1);
	moveUpDownButtons.add(moveUpButton, {
		proportion: 1,
		expand: true
	}).add(moveDownButton, {
		proportion: 1,
		expand: true
	});
	this.add(moveUpDownButtons, {
		proportion: 0,
		expand: true
	});
	this.add(deleteButton, {
		proportion: 0,
		expand: true
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/cellcontainer/LayoutMode1.js
var GetValue$27 = __webpack_exports__Utils.Objects.GetValue;
var LayoutMode1 = function(config) {
	var scene = this.scene;
	var indexLabel = config.indexLabel;
	var displayNameLabel = config.displayNameLabel;
	var deleteButton = config.deleteButton;
	var space = config.space || {};
	this.orientation = 1;
	var header = CreatExpandContainer(scene, 0);
	this.add(header, {
		proportion: 0,
		expand: true,
		padding: { bottom: GetValue$27(space, "index", 0) }
	});
	header.add(indexLabel, {
		proportion: 0,
		expand: true
	}).addSpace().add(deleteButton, {
		proportion: 0,
		expand: true
	});
	this.add(displayNameLabel, {
		proportion: 1,
		expand: true
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/cellcontainer/Build.js
var GetValue$26 = __webpack_exports__Utils.Objects.GetValue;
var LayoutCallbacks = [LayoutMode0, LayoutMode1];
var Build = function(config) {
	if (config === void 0) config = {};
	var background = config.background;
	var indexLabel = config.indexLabel;
	var displayNameLabel = config.displayNameLabel;
	var deleteButton = config.deleteButton;
	var moveUpButton = config.moveUpButton;
	var moveDownButton = config.moveDownButton;
	RemoveFromParent(background);
	RemoveFromParent(indexLabel);
	RemoveFromParent(displayNameLabel);
	RemoveFromParent(deleteButton);
	RemoveFromParent(moveUpButton);
	RemoveFromParent(moveDownButton);
	this.clear(true);
	if (background) this.addBackground(background);
	(LayoutCallbacks[GetValue$26(config, "layoutMode", 0)] || LayoutCallbacks[0]).call(this, config);
	this.addChildrenMap("background", background);
	this.addChildrenMap("index", indexLabel);
	this.addChildrenMap("displayName", displayNameLabel);
	this.addChildrenMap("delete", deleteButton);
	this.addChildrenMap("moveUp", moveUpButton);
	this.addChildrenMap("moveDown", moveDownButton);
	deleteButton.offClick(this.onClickDeleteButton, this).onClick(this.onClickDeleteButton, this);
	moveUpButton.offClick(this.onClickMoveUpButton, this).onClick(this.onClickMoveUpButton, this);
	moveDownButton.offClick(this.onClickMoveDownButton, this).onClick(this.onClickMoveDownButton, this);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/cellcontainer/OnClickButtonMethods.js
var OnClickButtonMethods_default = {
	onClickDeleteButton() {
		if (this.gridTable.readOnly) return;
		if (!this.gridTable.isInTouching("mask")) return;
		this.gridTable.deleteItemWithTransition(this);
	},
	onClickMoveUpButton() {
		if (this.gridTable.readOnly) return;
		if (!this.gridTable.isInTouching("mask")) return;
		var items = this.gridTable.items;
		if (!items || items.length <= 1) return;
		var currentIndex = this.cellIndex;
		var targetIndex = currentIndex === 0 ? items.length - 1 : currentIndex - 1;
		var tmp = items[currentIndex];
		items[currentIndex] = items[targetIndex];
		items[targetIndex] = tmp;
		this.gridTable.refresh().scrollToRow(targetIndex);
		this.gridTable.emit("items.change", "move", {
			fromIndex: currentIndex,
			toIndex: targetIndex
		});
	},
	onClickMoveDownButton() {
		if (this.gridTable.readOnly) return;
		if (!this.gridTable.isInTouching("mask")) return;
		var items = this.gridTable.items;
		if (!items || items.length <= 1) return;
		var currentIndex = this.cellIndex;
		var targetIndex = currentIndex === items.length - 1 ? 0 : currentIndex + 1;
		var tmp = items[currentIndex];
		items[currentIndex] = items[targetIndex];
		items[targetIndex] = tmp;
		this.gridTable.refresh().scrollToRow(targetIndex);
		this.gridTable.emit("items.change", "move", {
			fromIndex: currentIndex,
			toIndex: targetIndex
		});
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/cellcontainer/OnPointerOverOutMethods.js
var OnPointerOverOutMethods_default = {
	onOver() {
		this.getElement("background").setHoverState(true);
	},
	onOut() {
		this.getElement("background").setHoverState(false);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/listdetail/cellcontainer/CellContainer.js
var CellContainer = class extends Sizer {
	constructor(scene, config) {
		super(scene, { space: config.space });
		this.type = "rexTweaker.ListDetail.ListTable.CellContainer";
		this.gridTable = config.gridTable;
		this.build(config);
	}
	setItem(items, index) {
		var item = items[index];
		this.cellIndex = index;
		this.setBindingTarget(item);
		return this;
	}
	setIndexLabel(content) {
		var indexLabel = this.childrenMap.index;
		if (typeof content === "string") indexLabel.setText(content);
		else indexLabel.setTitle(content);
		return this;
	}
	setDisplayNameLabel(content) {
		var displayNameLabel = this.childrenMap.displayName;
		if (typeof content === "string") displayNameLabel.setText(content);
		else displayNameLabel.setTitle(content);
		return this;
	}
	setBindingTarget(target, key) {
		this.bindingTarget = target;
		this.bindingKey = key;
		return this;
	}
	setSelectedState(enable) {
		var background = this.childrenMap.background;
		if (background.setActiveState) background.setActiveState(enable);
		return this;
	}
	get visible() {
		return super.visible;
	}
	set visible(value) {
		super.visible = value;
		if (!value) this.setBindingTarget();
	}
};
var Methods$1 = { build: Build };
Object.assign(CellContainer.prototype, Methods$1, OnClickButtonMethods_default, OnPointerOverOutMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/createlistdetail/GenerateCreateCellContainerCallback.js
var GetValue$25 = __webpack_exports__Utils.Objects.GetValue;
var CreateCellContainer = function(parent, cell, config) {
	var scene = parent.scene;
	var { space, gridTable, indexStyle, displayNameStyle, createDeleteButton, createMoveUpButton, createMoveDownButton, backgroundStyle } = config;
	var cellContainer = new CellContainer(scene, {
		space,
		gridTable,
		background: CreateBackground(scene, {}, backgroundStyle),
		indexLabel: CreateIndexLabel(scene, indexStyle),
		displayNameLabel: CreateDisplayNameLabel(scene, displayNameStyle),
		deleteButton: createDeleteButton(scene),
		moveUpButton: createMoveUpButton(scene),
		moveDownButton: createMoveDownButton(scene)
	});
	scene.add.existing(cellContainer);
	return cellContainer;
};
var GenerateCreateCellContainerCallback = function(parent, config, style) {
	var space = GetValue$25(config, "space.cell", void 0, style) || {};
	var indexStyle = GetIndexLabelStyle(style);
	var indexLabelCallback = GetIndexLabelCallback(config);
	var displayNameStyle = GetDisplayNameStyle(style);
	var displayNameLabelCallback = GetDisplayNameLabelCallback(config);
	var createDeleteButton = function(scene) {
		return CreateDeleteButton(scene, config, style);
	};
	var createMoveUpButton = function(scene) {
		return CreateMoveUpButton(scene, config, style);
	};
	var createMoveDownButton = function(scene) {
		return CreateMoveDownButton(scene, config, style);
	};
	var backgroundStyle = GetValue$25(style, "cellBackground");
	var callback = function(cell, cellContainer, gridTable) {
		var width = cell.width;
		cell.height;
		var item = cell.item;
		var items = cell.items;
		var index = cell.index;
		if (cellContainer === null) cellContainer = CreateCellContainer(parent, cell, {
			space,
			gridTable,
			indexStyle,
			displayNameStyle,
			createDeleteButton,
			createMoveUpButton,
			createMoveDownButton,
			backgroundStyle
		});
		cell.setCellContainerAlign("center");
		cellContainer.setIndexLabel(indexLabelCallback(index, item, items)).setDisplayNameLabel(displayNameLabelCallback(index, item, items)).setItem(items, index).setSelectedState(index === gridTable.selectedIndex).setMinSize(width, 0).setOrigin(.5, 0).setDirty(true).layout().setDirty(false);
		return cellContainer;
	};
	return callback;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/utils/CreateDuplicateButton.js
var GetValue$24 = __webpack_exports__Utils.Objects.GetValue;
var CreateDuplicateButton = function(scene, config, style) {
	var duplicateButtonStyle = GetValue$24(style, "duplicateButton");
	var duplicateLabel;
	if (duplicateButtonStyle) duplicateButtonStyle = Object.assign(DeepClone(ButtonStyleBase), duplicateButtonStyle);
	else {
		duplicateButtonStyle = GetValue$24(style, "tweaker.inputRow.button") || {};
		duplicateLabel = GetValue$24(config, "duplicateLabel", "Duplicate");
	}
	var duplicateButton = CreateLabel(scene, duplicateButtonStyle);
	if (duplicateLabel) duplicateButton.resetDisplayContent(duplicateLabel);
	return duplicateButton;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/utils/CreateResetButton.js
var GetValue$23 = __webpack_exports__Utils.Objects.GetValue;
var CreateResetButton = function(scene, config, style) {
	var resetButtonStyle = GetValue$23(style, "resetButton");
	var resetLabel;
	if (resetButtonStyle) resetButtonStyle = Object.assign(DeepClone(ButtonStyleBase), resetButtonStyle);
	else {
		resetButtonStyle = GetValue$23(style, "tweaker.inputRow.button") || {};
		resetLabel = GetValue$23(config, "resetLabel", "Reset");
	}
	var resetButton = CreateLabel(scene, resetButtonStyle);
	if (resetLabel) resetButton.resetDisplayContent(resetLabel);
	return resetButton;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/utils/CreatePreviousButton.js
var GetValue$22 = __webpack_exports__Utils.Objects.GetValue;
var CreatePreviousButton = function(scene, config, style) {
	var previousButtonStyle = GetValue$22(style, "previousButton");
	var previousLabel;
	if (previousButtonStyle) previousButtonStyle = Object.assign(DeepClone(ButtonStyleBase), previousButtonStyle);
	else {
		previousButtonStyle = GetValue$22(style, "tweaker.inputRow.button") || {};
		previousLabel = GetValue$22(config, "previousLabel", "Previous");
	}
	var previousButton = CreateLabel(scene, previousButtonStyle);
	if (previousLabel) previousButton.resetDisplayContent(previousLabel);
	return previousButton;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/utils/CreateNextButton.js
var GetValue$21 = __webpack_exports__Utils.Objects.GetValue;
var CreateNextButton = function(scene, config, style) {
	var nextButtonStyle = GetValue$21(style, "nextButton");
	var nextLabel;
	if (nextButtonStyle) nextButtonStyle = Object.assign(DeepClone(ButtonStyleBase), nextButtonStyle);
	else {
		nextButtonStyle = GetValue$21(style, "tweaker.inputRow.button") || {};
		nextLabel = GetValue$21(config, "nextLabel", "Next");
	}
	var nextButton = CreateLabel(scene, nextButtonStyle);
	if (nextLabel) nextButton.resetDisplayContent(nextLabel);
	return nextButton;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/createlistdetail/CreateListDetail.js
var GetValue$20 = __webpack_exports__Utils.Objects.GetValue;
var DefaultSplitStyle = {
	width: 10,
	height: 10,
	color: 0,
	alpha: .001
};
var CreateListDetail = function(tweaker, config, style) {
	if (!config) config = {};
	if (!style) style = {};
	var scene = tweaker.scene;
	var background = CreateBackground(scene, config.background || {}, style.background || {});
	var title = CreateTitleLabel(scene, void 0, style.title || {});
	var listTableConfig = GetValue$20(config, "table", void 0, style) || {};
	var createCellContainerCallback = GenerateCreateCellContainerCallback(tweaker, config, style);
	var listTableSlider = CreateSlider$1(scene, config.slider, style.slider);
	var listTableFooter;
	var clearButton = CreateClearButton(scene, config, style);
	var addButton = CreateAddButton(scene, config, style);
	if (clearButton || addButton) {
		listTableFooter = new Sizer(scene, { space: { item: GetValue$20(config, "space.button", 0, style) } });
		scene.add.existing(listTableFooter);
		if (clearButton) listTableFooter.add(clearButton, {
			proportion: 0,
			expand: true
		});
		listTableFooter.addSpace();
		if (addButton) listTableFooter.add(addButton, {
			proportion: 0,
			expand: true
		});
	}
	var editor = tweaker.createTweaker({
		root: tweaker.root,
		styles: tweaker.styles
	}).addRows(config.$properties, GetValue$20(config, "monitor", false));
	var editorSlider = CreateSlider$1(scene, config.slider, style.slider);
	var indexLabelCallback = GetIndexLabelCallback(config);
	var displayNameLabelCallback = GetDisplayNameLabelCallback(config);
	var editorToolbarStyle = GetValue$20(style, "editorToolbar") || {};
	var editorIndexLabelStyle = GetValue$20(editorToolbarStyle, "index");
	if (!editorIndexLabelStyle) editorIndexLabelStyle = GetIndexLabelStyle(style);
	var editorIndexLabel = CreateIndexLabel(scene, editorIndexLabelStyle);
	var editorDisplayNameStyle = GetValue$20(editorToolbarStyle, "displayName");
	if (!editorDisplayNameStyle) editorDisplayNameStyle = GetDisplayNameStyle(style);
	var editorDisplayNameLabel = CreateDisplayNameLabel(scene, editorDisplayNameStyle);
	var editorDeleteButton = CreateDeleteButton(scene, config, editorToolbarStyle);
	var editorDuplicateButton = CreateDuplicateButton(scene, config, editorToolbarStyle);
	var editorResetButton = CreateResetButton(scene, config, editorToolbarStyle);
	var editorPreviousButton = CreatePreviousButton(scene, config, editorToolbarStyle);
	var editorNextButton = CreateNextButton(scene, config, editorToolbarStyle);
	var createDefaultItemCallback = GetValue$20(config, "createDefaultItem");
	if (editorDuplicateButton) editorDuplicateButton.createDefaultItem = createDefaultItemCallback;
	if (editorResetButton) editorResetButton.createDefaultItem = createDefaultItemCallback;
	var splitterConfig = config.splitter;
	var splitterStyle = style.splitter;
	if (!splitterConfig && !splitterStyle) splitterStyle = DefaultSplitStyle;
	var splitter = CreateBackground(scene, splitterConfig || {}, splitterStyle || {});
	var listDetail = new ListDetail(scene, {
		isHorizontalView: config.view === "detail-h",
		header: title,
		listTableConfig,
		createListCellContainerCallback: createCellContainerCallback,
		listTableSlider,
		listTableFooter,
		clearButton,
		addButton,
		editor,
		editorSlider,
		editorIndexLabel,
		editorDisplayNameLabel,
		indexLabelCallback,
		displayNameLabelCallback,
		editorDeleteButton,
		editorDuplicateButton,
		editorResetButton,
		editorPreviousButton,
		editorNextButton,
		splitter,
		splitRatio: GetValue$20(config, "splitRatio", .5),
		minLeftPanelWidth: GetValue$20(config, "minLeftPanelWidth", 0),
		minRightPanelWidth: GetValue$20(config, "minRightPanelWidth", 0),
		minTopPanelWidth: GetValue$20(config, "minTopPanelWidth", 0),
		minBottomPanelWidth: GetValue$20(config, "minBottomPanelWidth", 0),
		background,
		height: GetValue$20(config, "height", 0, style),
		space: Merge$1(config.space || {}, style.space || {}),
		alignTitle: GetValue$20(style, "root.alignTitle")
	});
	scene.add.existing(listDetail);
	return listDetail;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/methods/AddArrayTable.js
var GetValue$19 = __webpack_exports__Utils.Objects.GetValue;
var AddArrayTable = function(target, bindingKey, config) {
	if (arguments.length === 1) {
		config = target;
		target = config.bindingTarget;
		bindingKey = config.bindingKey;
	} else {
		if (config === void 0) config = {};
		config.bindingTarget = target;
		config.bindingKey = bindingKey;
	}
	var isRootTarget = !bindingKey || bindingKey === "";
	if (!config.title) {
		if (!isRootTarget) config.title = GetLeafKey(bindingKey);
		else config.title = "Root";
	}
	if (!isRootTarget) config.value = GetValue$19(target, bindingKey, void 0) || [];
	else config.value = target;
	var arrayTableStyle = GetValue$19(this.styles, "arrayTable") || {};
	var createArrayTableCallback;
	var view = GetValue$19(config, "view", "inline");
	if (view === "detail") view = "detail-v";
	view = view.toLowerCase();
	config.view = view;
	switch (view) {
		case "detail-v":
		case "detail-h":
			var properties = config.$properties;
			if (Array.isArray(properties) && properties.length >= 1 && properties[0].hasOwnProperty("$key")) createArrayTableCallback = CreateListDetail;
			else createArrayTableCallback = CreateArrayTable;
			break;
		default: createArrayTableCallback = CreateArrayTable;
	}
	var arrayTable = createArrayTableCallback(this, config, arrayTableStyle);
	this.add(arrayTable, {
		proportion: arrayTable.minWidth === 0 ? 1 : 0,
		expand: true
	});
	arrayTable.setTitle(config);
	arrayTable.setBindingTarget(target, bindingKey);
	if (config.monitor) arrayTable.startMonitorTarget();
	if (config.readOnly) arrayTable.setReadOnly();
	if (config.key) this.root.addChildrenMap(config.key, arrayTable);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/inputrow/BindingTargetMethods.js
var BindingTargetMethods_default = {
	setupBinding() {
		this.childrenMap.inputField.on("valuechange", function(value) {
			if (!this.autoUpdateEnable) return;
			this.setTargetValue(value);
		}, this);
		return this;
	},
	setAutoUpdateEnable(enable) {
		if (enable === void 0) enable = true;
		this.autoUpdateEnable = enable;
		return this;
	},
	setBindingTarget(target, bindingKey) {
		this.bindingTarget = target;
		if (bindingKey !== void 0) this.setBindingTargetKey(bindingKey);
		this.syncTargetValue();
		var inputField = this.childrenMap.inputField;
		if (inputField.onBindTarget) inputField.onBindTarget(target, bindingKey);
		return this;
	},
	setBindingTargetKey(bindingKey) {
		this.bindTargetKey = bindingKey;
		return this;
	},
	setValueCallbacks(config) {
		this.onGetValue = GetValue$279(config, "onGetValue");
		this.onSetValue = GetValue$279(config, "onSetValue");
		return this;
	},
	getTargetValue() {
		if (!this.bindingTarget) return;
		if (this.bindTargetKey != null) return GetValue$279(this.bindingTarget, this.bindTargetKey);
		if (this.onGetValue) return this.onGetValue(this.bindingTarget);
	},
	setTargetValue(value) {
		if (!this.bindingTarget) return this;
		if (this.bindTargetKey != null) {
			SetValue(this.bindingTarget, this.bindTargetKey, value);
			return this;
		}
		if (this.onSetValue) this.onSetValue(this.bindingTarget, value);
		return this;
	},
	syncTargetValue() {
		if (!this.bindingTarget) return this;
		var inputField = this.childrenMap.inputField;
		if (inputField.syncValue) inputField.syncValue(this.getTargetValue());
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/inputrow/MonitorTargetMethods.js
var MonitorTargetMethods_default = {
	startMonitorTarget() {
		if (this.isMonitoring) return this;
		this.isMonitoring = true;
		this.scene.events.on("postupdate", this.onMonitorTarget, this);
		return this;
	},
	stopMonitorTarget() {
		if (!this.isMonitoring) return this;
		this.isMonitoring = false;
		this.scene.events.off("postupdate", this.onMonitorTarget, this);
		return this;
	},
	onMonitorTarget() {
		if (!this.bindingTarget) return;
		var newValue = this.getTargetValue();
		var inputField = this.childrenMap.inputField;
		if (inputField.value === newValue) return;
		inputField.syncValue(newValue);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/inputrow/SetReadOnlyMethods.js
var SetReadOnlyMethods_default = { setReadOnly(value) {
	var inputField = this.childrenMap.inputField;
	if (inputField.setReadOnly) inputField.setReadOnly(value);
	return this;
} };
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/inputrow/MinTitleWidthMethods.js
var MinTitleWidthMethods_default = {
	getMinTitleWidth() {
		var title = this.childrenMap.title;
		if (!title || title.orientation !== 0) return 0;
		var padding = title.rexSizer.padding;
		return this.getChildWidth(this.childrenMap.title) + (padding.left + padding.right) * title.scaleX + this.getInnerPadding("left");
	},
	setMinTitleWidth(width) {
		var title = this.childrenMap.title;
		if (!title || title.orientation !== 0) return this;
		var padding = title.rexSizer.padding;
		width -= (padding.left + padding.right) * title.scaleX;
		title.minWidth = width;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/inputrow/InputRow.js
var GetValue$18 = __webpack_exports__Utils.Objects.GetValue;
var InputRow = class extends Sizer {
	constructor(scene, config) {
		super(scene, config);
		this.type = "rexTweaker.InputRow";
		this.bindingTarget = void 0;
		this.bindTargetKey = void 0;
		this.autoUpdateEnable = true;
		var inputTitle = config.inputTitle;
		var inputField = config.inputField;
		var background = config.background;
		var border = config.border;
		if (inputTitle) {
			var proportion = GetValue$18(config, "proportion.title", 0);
			var titleSpace = GetValue$18(config, "space.title", 0);
			var padding;
			if (this.orientation === 0) padding = { right: titleSpace };
			else padding = { bottom: titleSpace };
			this.add(inputTitle, {
				proportion,
				expand: true,
				padding
			});
		}
		var defaultProportion = inputField.defaultProportion;
		if (defaultProportion === void 0) defaultProportion = config.defaultExpandWidth ? 1 : 0;
		var proportion = GetValue$18(config, "proportion.inputField", defaultProportion);
		this.add(inputField, {
			proportion,
			expand: true
		});
		if (background) this.addBackground(background);
		if (border) this.addBackground(border);
		this.addChildrenMap("title", inputTitle);
		this.addChildrenMap("inputField", inputField);
		this.addChildrenMap("background", background);
		this.addChildrenMap("border", border);
		this.setupBinding();
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		this.stopMonitorTarget();
		super.destroy(fromScene);
	}
	setTitle(config) {
		var title = this.childrenMap.title;
		if (!title) return this;
		title.setTitle(config);
		return this;
	}
	preLayout() {
		var title = this.childrenMap.title;
		if (title) title.minWidth = 0;
		super.preLayout();
	}
};
Object.assign(InputRow.prototype, BindingTargetMethods_default, MonitorTargetMethods_default, SetReadOnlyMethods_default, MinTitleWidthMethods_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/inputfield/GenerateInputFieldClass.js
var GenerateInputFieldClass = function(BaseClass) {
	if (BaseClass === void 0) BaseClass = Sizer;
	class InputFiled extends BaseClass {
		get bindingTarget() {
			return this.getParentSizer().bindingTarget;
		}
		get bindingKey() {
			return this.getParentSizer().bindTargetKey;
		}
		get value() {
			return this._value;
		}
		get root() {
			return this.tweaker.root;
		}
		onBindTarget(target, key) {
			if (this.onBindTargetCallback) this.onBindTargetCallback(this, target, key);
		}
		validate(newValue) {
			if (this.syncValueFlag || !this.validateCallback) return true;
			return this.validateCallback(newValue, this._value, this.bindingTarget, this.bindingKey);
		}
		getFotmatText(value) {
			if (this.textFormatCallback) value = this.textFormatCallback(value);
			else value = value.toString();
			return value;
		}
		set value(value) {
			if (this._value === value) return;
			if (!this.validate(value)) value = this._value;
			if (this.filterValueCallback) value = this.filterValueCallback(this, value);
			if (this.displayValueCallback) this.displayValueCallback(this, value);
			if (this._value === value) return;
			var oldValue = this._value;
			this._value = value;
			if (!this.syncValueFlag) {
				var bindingTarget = this.bindingTarget;
				var bindingKey = this.bindingKey;
				this.emit("valuechange", value, oldValue, bindingTarget, bindingKey);
				this.tweaker.emit("valuechange", value, oldValue, bindingTarget, bindingKey);
				this.root.emit("valuechange", value, oldValue, bindingTarget, bindingKey);
			}
		}
		getValue() {
			return this.value;
		}
		setValue(value) {
			this.value = value;
			return this;
		}
		syncValue(value) {
			this.syncValueFlag = true;
			this.value = value;
			this.syncValueFlag = false;
			return this;
		}
		setReadOnly(value) {
			value = !!value;
			if (!this.setReadOnlyCallback) return this;
			this.setReadOnlyCallback(this, value);
			this._readOnly = value;
			return this;
		}
		get readOnly() {
			return this._readOnly;
		}
		set readOnly(value) {
			this.setReadOnly(value);
		}
		setTweaker(tweaker) {
			this.tweaker = tweaker;
			return this;
		}
		setup(config, setDefaults) {
			if (setDefaults === void 0) setDefaults = false;
			if (setDefaults || config.hasOwnProperty("format")) this.setTextFormatCallback(config.format);
			if (setDefaults || config.hasOwnProperty("onValidate")) this.setValidateCallback(config.onValidate);
			if (this.setupCallback) this.setupCallback(this, config, setDefaults);
			return this;
		}
		setSetupCallback(callback) {
			this.setupCallback = callback;
			return this;
		}
		setFilterValueCallback(callback) {
			this.filterValueCallback = callback;
			return this;
		}
		setDisplayValueCallback(callback) {
			this.displayValueCallback = callback;
			return this;
		}
		setOnBindTargetCallback(callback) {
			this.onBindTargetCallback = callback;
			return this;
		}
		setTextFormatCallback(callback) {
			this.textFormatCallback = callback;
			return this;
		}
		setValidateCallback(callback) {
			this.validateCallback = callback;
			return this;
		}
		setSetReadOnlyCallback(callback) {
			this.setReadOnlyCallback = callback;
			return this;
		}
	}
	return InputFiled;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/CreateInputField.js
var CreateInputField = function(tweaker, config, inputRowStyle, styles) {
	var scene = tweaker.scene;
	var inputField;
	var inputHandlers = tweaker.inputHandlers;
	for (var i = 0, cnt = inputHandlers.length; i < cnt; i++) {
		var handler = inputHandlers[i];
		if (handler.accept(config)) {
			inputField = new (GenerateInputFieldClass(handler.baseClass))(scene);
			scene.add.existing(inputField);
			inputField.setTweaker(tweaker).setSetupCallback(handler.setup).setFilterValueCallback(handler.filterValue).setDisplayValueCallback(handler.displayValue).setOnBindTargetCallback(handler.onBindTarget).setSetReadOnlyCallback(handler.setReadOnly);
			handler.build(inputField, config, inputRowStyle, styles);
			break;
		}
	}
	if (inputField) inputField.setup(config, true);
	return inputField;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/CreateInputRow.js
var CreateInputRow = function(tweaker, config, inputRowStyle, styles) {
	if (!config) config = {};
	if (!inputRowStyle) inputRowStyle = {};
	var scene = tweaker.scene;
	var background = CreateBackground(scene, config.background || {}, inputRowStyle.background || {});
	var inputField = CreateInputField(tweaker, config, inputRowStyle, styles);
	if (!inputField) return null;
	var inputTitle;
	if (config.title !== false && config.title !== null) {
		var titleStyle = inputRowStyle.title || {};
		inputTitle = CreateTitleLabel(scene, config, titleStyle);
	}
	var border = CreateBackground(scene, config.border || {}, inputRowStyle.border || {});
	var inputRow = new InputRow(scene, {
		...inputRowStyle,
		...config,
		inputTitle,
		inputField,
		background,
		border
	});
	scene.add.existing(inputRow);
	inputRow.setTitle(config);
	inputRow.setValueCallbacks(config);
	return inputRow;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/methods/AddInput.js
var AddInput = function(target, bindingKey, config) {
	if (arguments.length === 1) {
		config = target;
		target = config.bindingTarget;
		bindingKey = config.bindingKey;
	} else {
		if (config === void 0) config = {};
		config.bindingTarget = target;
		config.bindingKey = bindingKey;
	}
	if (config.title === void 0) config.title = GetLeafKey(bindingKey);
	config.value = GetValue$279(target, bindingKey, void 0);
	var inputRowStyle = this.styles.inputRow || {};
	if (!this.isWrapMode) inputRowStyle.defaultExpandWidth = this.styles.orientation === 1;
	else inputRowStyle.defaultExpandWidth = true;
	var inputSizer = CreateInputRow(this, config, inputRowStyle, this.styles);
	if (!inputSizer) {
		console.error(`[Tweaker] Can't add Input
    title: ${config.title}
    view: ${config.view}
`);
		return this;
	}
	if (!this.isWrapMode) {
		var proportion;
		if (this.orientation === 1) proportion = this.expandInputRowHeight ? 1 : 0;
		else {
			proportion = this.itemWidth > 0 ? 0 : 1;
			if (inputSizer.minWidth === 0) inputSizer.setMinWidth(this.itemWidth);
		}
		if (inputSizer.minHeight === 0) inputSizer.setMinHeight(this.itemHeight);
		this.add(inputSizer, {
			proportion,
			expand: true
		});
	} else {
		inputSizer.setMinWidth(this.itemWidth);
		inputSizer.setMinHeight(this.itemHeight);
		this.add(inputSizer);
	}
	if (config.onValueChange) inputSizer.childrenMap.inputField.on("valuechange", config.onValueChange);
	inputSizer.setAutoUpdateEnable(config.autoUpdate);
	inputSizer.setBindingTarget(target, bindingKey);
	if (config.readOnly) inputSizer.setReadOnly();
	if (config.monitor) inputSizer.startMonitorTarget();
	if (config.key) this.root.addChildrenMap(config.key, inputSizer);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/gameobjects/utils/CreateButtons.js
var GetValue$17 = __webpack_exports__Utils.Objects.GetValue;
var CreateButtons$2 = function(scene, config) {
	var gameObject = new (GetValue$17(config, "wrap", false) ? Buttons : Buttons$1)(scene, config);
	scene.add.existing(gameObject);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/CreateButtons.js
var GetValue$16 = __webpack_exports__Utils.Objects.GetValue;
var CreateButtons$1 = function(tweaker, config, style) {
	if (!config) config = {};
	if (!style) style = {};
	var scene = tweaker.scene;
	var background = CreateBackground(scene, config.background || {}, style.background || {});
	var title;
	if (config.title !== false && config.title !== null) {
		var titleStyle = style.title || {};
		title = CreateTitleLabel(scene, config, titleStyle);
	}
	var buttonsConfig = config.buttons;
	var buttonStyle = GetValue$16(style, "button") || {};
	var buttons = [];
	for (var i = 0, cnt = buttonsConfig.length; i < cnt; i++) {
		var button = CreateLabel(scene, buttonStyle);
		buttons.push(button);
		var buttonConfig = buttonsConfig[i];
		button.resetDisplayContent(buttonConfig.label);
		button.callback = buttonConfig.callback;
	}
	var buttonsSizer = CreateButtons$2(scene, {
		buttons,
		expand: true,
		wrap: GetValue$16(config, "wrap", false)
	});
	buttonsSizer.defaultProportion = 1;
	buttonsSizer.setReadOnly = function(readOnly) {
		if (readOnly === void 0) readOnly = true;
		buttonsSizer.setButtonEnable(!readOnly);
		return this;
	};
	var border = CreateBackground(scene, config.border || {}, style.border || {});
	var inputRow = new InputRow(scene, {
		...style,
		inputTitle: title,
		inputField: buttonsSizer,
		background,
		border
	});
	scene.add.existing(inputRow);
	inputRow.setTitle(config);
	buttonsSizer.on("button.click", function(button) {
		button.callback(inputRow.bindingTarget);
	});
	return inputRow;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/methods/AddButtons.js
var GetValue$15 = __webpack_exports__Utils.Objects.GetValue;
var AddButtons = function(config) {
	if (config === void 0) config = {};
	this.scene;
	var target = config.bindingTarget;
	delete config.bindingTarget;
	var buttonsStyle = GetValue$15(this.styles, "inputRow") || {};
	var buttons = CreateButtons$1(this, config, buttonsStyle);
	this.add(buttons, {
		proportion: buttons.minWidth === 0 ? 1 : 0,
		expand: true
	});
	if (target) buttons.setBindingTarget(target);
	if (config.readOnly) buttons.setReadOnly();
	if (config.key) this.root.addChildrenMap(config.key, buttons);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/methods/AddButton.js
var AddButton = function(config) {
	if (config === void 0) config = {};
	config.buttons = [{
		label: config.label,
		callback: config.callback
	}];
	delete config.label;
	delete config.callback;
	this.addButtons(config);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/builders/CreateSeparator.js
var CreateSeparator = function(tweaker, config, style) {
	var scene = tweaker.scene;
	return CreateBackground$2(scene, Merge$1(config, style));
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/methods/AddSeparator.js
var GetValue$14 = __webpack_exports__Utils.Objects.GetValue;
var AddSeparator = function(config) {
	if (config === void 0) config = {};
	var separatorStyle = GetValue$14(this.styles, "separator");
	var separator = CreateSeparator(this, config, separatorStyle);
	this.add(separator, { expand: true });
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/methods/AddRows.js
var AddRows = function(properties, target, monitor) {
	if (typeof target === "boolean") {
		monitor = target;
		target = void 0;
	}
	if (monitor === void 0) monitor = true;
	AddProperties(this, DeepClone(properties), target, monitor);
	return this;
};
var AddProperties = function(tweaker, properties, target, monitor) {
	if (!properties) return;
	for (var i = 0, cnt = properties.length; i < cnt; i++) {
		var property = properties[i];
		if (property.hasOwnProperty("$target")) {
			target = property.$target;
			delete property.$target;
		}
		var type = property.$type;
		delete property.$type;
		switch (type) {
			case "folder":
				AddProperties(tweaker.addFolder(property), property.$properties, target, monitor);
				break;
			case "tab":
				var pages = tweaker.addTab(property);
				for (var pIdx = 0, pcnt = pages.length; pIdx < pcnt; pIdx++) AddProperties(pages[pIdx], property.pages[pIdx].$properties, target, monitor);
				break;
			case "columns":
				var columns = tweaker.addColumns(property);
				for (var pIdx = 0, pcnt = columns.length; pIdx < pcnt; pIdx++) AddProperties(columns[pIdx], property.columns[pIdx].$properties, target, monitor);
				break;
			case "2columns":
				var splitPanels = tweaker.add2Columns(property);
				var leftProperties, rightProperties;
				if (property.columns) {
					var columns = property.columns;
					if (columns[0] && columns[0].$properties) leftProperties = columns[0].$properties;
					if (columns[1] && columns[1].$properties) rightProperties = columns[1].$properties;
				} else {
					if (property.left && property.left.$properties) leftProperties = property.left.$properties;
					if (property.right && property.right.$properties) rightProperties = property.right.$properties;
				}
				if (leftProperties) AddProperties(splitPanels.left, leftProperties, target, monitor);
				if (rightProperties) AddProperties(splitPanels.right, rightProperties, target, monitor);
				break;
			case "wrap":
				AddProperties(tweaker.addWrap(property), property.$properties, target, monitor);
				break;
			case "scrollable":
				AddProperties(tweaker.addScrollable(property), property.$properties, target, monitor);
				break;
			case "arrayTable":
				var key = property.$key;
				delete property.$key;
				property.bindingTarget = target;
				property.bindingKey = key;
				if (!property.hasOwnProperty("monitor")) property.monitor = monitor;
				tweaker.addArrayTable(property);
				break;
			case "separator":
				tweaker.addSeparator();
				break;
			case "button":
				property.bindingTarget = target;
				tweaker.addButton(property);
				break;
			case "buttons":
				property.bindingTarget = target;
				tweaker.addButtons(property);
				break;
			default:
				var key = property.$key;
				delete property.$key;
				property.bindingTarget = target;
				property.bindingKey = key;
				if (!property.hasOwnProperty("monitor")) property.monitor = monitor;
				tweaker.addInput(property);
		}
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/methods/SetBindingTarget.js
var SetBindingTarget = function(target, key) {
	var children = this.sizerChildren;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		if (!child.setBindingTarget) continue;
		child.setBindingTarget(target, key);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/methods/GetMaxInputRowTitleWidth.js
var GetMaxInputRowTitleWidth = function() {
	var maxTitleWidth = 0;
	var children = this.sizerChildren;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		if (child.rexSizer.hidden) continue;
		if (child.getMinTitleWidth) maxTitleWidth = Math.max(maxTitleWidth, child.getMinTitleWidth());
		else if (child.getMaxInputRowTitleWidth) maxTitleWidth = Math.max(maxTitleWidth, child.getMaxInputRowTitleWidth());
	}
	return maxTitleWidth + this.getInnerPadding("left");
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/methods/SetInputRowTitleWidth.js
var SetInputRowTitleWidth = function(width) {
	if (!this.isWrapMode) width -= this.getInnerPadding("left");
	var children = this.sizerChildren;
	for (var i = 0, cnt = children.length; i < cnt; i++) {
		var child = children[i];
		if (child.rexSizer.hidden) continue;
		if (child.setMinTitleWidth) child.setMinTitleWidth(width);
		else if (child.setInputRowTitleWidth) child.setInputRowTitleWidth(width);
	}
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/methods/Methods.js
var methods$1 = {
	registerInputHandler: RegisterInputHandler,
	removeInputHandler: RemoveInputHandler,
	addFolder: AddFolder,
	addTab: AddTab,
	addColumns: AddColumns,
	add2Columns: AddSplit,
	addWrap: AddWrap,
	addScrollable: AddScrollable,
	addArrayTable: AddArrayTable,
	addInput: AddInput,
	addButtons: AddButtons,
	addButton: AddButton,
	addSeparator: AddSeparator,
	addRows: AddRows,
	setBindingTarget: SetBindingTarget,
	getMaxInputRowTitleWidth: GetMaxInputRowTitleWidth,
	setInputRowTitleWidth: SetInputRowTitleWidth
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/GenerateTweakerShellClass.js
var GetValue$13 = __webpack_exports__Utils.Objects.GetValue;
var GenerateTweakerShellClass = function(config) {
	var isWrapMode = GetValue$13(config, "wrap", false);
	var BaseClass = !isWrapMode ? Sizer : FixWidthSizer;
	class TweakerShell extends BaseClass {
		constructor(scene, config) {
			if (config === void 0) config = {};
			if (config.orientation === void 0) config.orientation = !isWrapMode ? 1 : 0;
			super(scene, config);
			this.type = "rexTweakerShell";
			this.isWrapMode = isWrapMode;
			if (!config.root) {
				this.root = this;
				this.inputHandlers = [];
			} else {
				this.root = config.root;
				this.inputHandlers = this.root.inputHandlers;
			}
			this.styles = GetValue$13(config, "styles") || {};
			this.styles.orientation = this.orientation;
			this.itemWidth = GetValue$13(config, "itemWidth", 0, this.styles);
			this.itemHeight = GetValue$13(config, "itemHeight", 0, this.styles);
			if (this.isRoot) {
				var alignTitle = GetValue$13(config, "inputRow.alignTitle");
				if (alignTitle === void 0) {
					alignTitle = !GetValue$13(this.styles, "inputRow.proportion.title");
					SetValue(config, "inputRow.alignTitle", alignTitle);
				} else if (!alignTitle) SetValue(this.styles, "inputRow.proportion.title", 0);
				this.alignTitle = alignTitle;
			}
			if (this.isRoot && this.orientation === 1 || isWrapMode) this.setAlignInputRowTitleEnable(this.root.alignTitle);
			else this.setAlignInputRowTitleEnable(false);
			this.setExpandInputRowHeightEnable(GetValue$13(config, "expandInputRowHeight", false));
			var background = CreateBackground(scene, void 0, config.background);
			if (background) this.addBackground(background);
		}
		get isRoot() {
			return this.root === this;
		}
		setAlignInputRowTitleEnable(enable) {
			if (enable === void 0) enable = true;
			this.alignInputRowTitleStartFlag = enable;
			return this;
		}
		setExpandInputRowHeightEnable(enable) {
			if (enable === void 0) enable = true;
			this.expandInputRowHeight = enable;
			return this;
		}
		preLayout() {
			super.preLayout();
			if (this.alignInputRowTitleStartFlag) this.setInputRowTitleWidth(this.getMaxInputRowTitleWidth());
		}
		createTweaker(config, addToScene) {
			if (addToScene === void 0) addToScene = true;
			var gameObject = new (GenerateTweakerShellClass(config))(this.scene, config);
			if (addToScene) this.scene.add.existing(gameObject);
			return gameObject;
		}
		get readOnly() {
			return this._readOnly;
		}
		set readOnly(value) {
			value = !!value;
			var children = this.sizerChildren;
			for (var i = 0, cnt = children.length; i < cnt; i++) {
				var child = children[i];
				if (!child.setReadOnly) continue;
				child.setReadOnly(value);
			}
		}
		setReadOnly(value) {
			if (value === void 0) value = true;
			this.readOnly = value;
			return this;
		}
	}
	Object.assign(TweakerShell.prototype, methods$1);
	return TweakerShell;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/utils/CreateInputText.js
var CreateInputText_default = CreateInputText$1;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/TextInputHandler.js
var SetInputTextReadOnly$4 = function(gameObject, readOnly, force) {
	if (readOnly === void 0) readOnly = true;
	if (force === void 0) force = false;
	var inputText = gameObject.childrenMap.inputText;
	if (force) {
		gameObject.inputTextReadOnly = readOnly;
		inputText.setReadOnly(readOnly);
	} else if (!gameObject.inputTextReadOnly) inputText.setReadOnly(readOnly);
};
var TextInputHandler_default = {
	name: "TextInput",
	accept(config) {
		if (config.hasOwnProperty("view")) return config.view === "string";
		return typeof config.value === "string";
	},
	build(gameObject, config, inputRowStyle, styles) {
		var scene = gameObject.scene;
		gameObject.type = "rexTweaker.TextInput";
		var inputTextConfig = inputRowStyle.inputText;
		var inputText = CreateInputText_default(scene, inputTextConfig);
		gameObject.add(inputText, {
			proportion: 1,
			expand: true,
			key: "inputText"
		});
		inputText.on("close", function() {
			gameObject.setValue(inputText.value);
		});
	},
	setup(gameObject, config, setDefaults) {
		if (setDefaults || config.hasOwnProperty("inputTextReadOnly")) SetInputTextReadOnly$4(gameObject, !!config.inputTextReadOnly, true);
	},
	displayValue(gameObject, value) {
		gameObject.childrenMap.inputText.setText(gameObject.getFotmatText(value));
	},
	setReadOnly(gameObject, readOnly) {
		if (readOnly === void 0) readOnly = true;
		SetInputTextReadOnly$4(gameObject, readOnly);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/build/CreateInputTextArea.js
var CreateInputTextArea = function(scene, config, deepCloneConfig) {
	if (deepCloneConfig === void 0) deepCloneConfig = true;
	if (deepCloneConfig) config = config ? DeepClone(config) : {};
	else if (!config) config = {};
	var inputText = new TextAreaInput(scene, config);
	scene.add.existing(inputText);
	return inputText;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/utils/CreateInputTextArea.js
var CreateInputTextArea_default = CreateInputTextArea;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/TextAreaInputHandler.js
var SetInputTextReadOnly$3 = function(gameObject, readOnly, force) {
	if (readOnly === void 0) readOnly = true;
	if (force === void 0) force = false;
	var inputText = gameObject.childrenMap.inputText;
	if (force) {
		gameObject.inputTextReadOnly = readOnly;
		inputText.setReadOnly(readOnly);
	} else if (!gameObject.inputTextReadOnly) inputText.setReadOnly(readOnly);
};
var TextAreaInputHandler_default = {
	name: "TextAreaInput",
	accept(config) {
		if (config.hasOwnProperty("view")) return config.view === "textarea";
		return false;
	},
	build(gameObject, config, inputRowStyle, styles) {
		var scene = gameObject.scene;
		this.type = "rexTweaker.TextAreaInput";
		var inputTextAreaConfig = inputRowStyle.inputTextArea;
		if (inputTextAreaConfig === void 0) inputTextAreaConfig = {};
		if (!inputTextAreaConfig.hasOwnProperty("text")) inputTextAreaConfig.text = inputRowStyle.inputText;
		if (!inputTextAreaConfig.hasOwnProperty("slider")) inputTextAreaConfig.slider = inputRowStyle.slider;
		var inputText = CreateInputTextArea_default(scene, inputTextAreaConfig);
		gameObject.add(inputText, {
			proportion: 1,
			expand: true,
			key: "inputText"
		});
		inputText.on("close", function() {
			gameObject.setValue(inputText.value);
		});
	},
	setup(gameObject, config, setDefaults) {
		if (setDefaults || config.hasOwnProperty("inputTextReadOnly")) SetInputTextReadOnly$3(gameObject, !!config.inputTextReadOnly);
	},
	displayValue(gameObject, value) {
		gameObject.childrenMap.inputText.setText(gameObject.getFotmatText(value));
	},
	onBindTarget(gameObject) {
		gameObject.childrenMap.inputText.scrollToTop();
	},
	setReadOnly(gameObject, readOnly) {
		if (readOnly === void 0) readOnly = true;
		SetInputTextReadOnly$3(gameObject, readOnly);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/NumberInputHandler.js
var SetInputTextReadOnly$2 = function(gameObject, readOnly, force) {
	if (readOnly === void 0) readOnly = true;
	if (force === void 0) force = false;
	var inputText = gameObject.childrenMap.inputText;
	if (force) {
		gameObject.inputTextReadOnly = readOnly;
		inputText.setReadOnly(readOnly);
	} else if (!gameObject.inputTextReadOnly) inputText.setReadOnly(readOnly);
};
var NumberInputHandler_default = {
	name: "NumberInput",
	accept(config) {
		if (config.hasOwnProperty("view")) return config.view === "number";
		return typeof config.value === "number";
	},
	build(gameObject, config, inputRowStyle, styles) {
		var scene = gameObject.scene;
		gameObject.type = "rexTweaker.NumberInput";
		var inputText = CreateInputText_default(scene, inputRowStyle.inputNumber || inputRowStyle.inputText).setNumberInput();
		gameObject.add(inputText, {
			proportion: 1,
			expand: true,
			key: "inputText"
		});
		inputText.on("close", function() {
			gameObject.setValue(inputText.value);
		});
	},
	setup(gameObject, config, setDefaults) {
		if (setDefaults || config.hasOwnProperty("inputTextReadOnly")) SetInputTextReadOnly$2(gameObject, !!config.inputTextReadOnly, true);
		gameObject.isFloatType = !config.int;
	},
	filterValue(gameObject, value) {
		if (gameObject.isFloatType) return value;
		else return Math.floor(value);
	},
	displayValue(gameObject, value) {
		gameObject.childrenMap.inputText.setText(gameObject.getFotmatText(value));
	},
	setReadOnly(gameObject, readOnly) {
		if (readOnly === void 0) readOnly = true;
		SetInputTextReadOnly$2(gameObject, readOnly);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/utils/CreateSlider.js
var CreateSlider = function(scene, config) {
	var gameObject = new Slider(scene, config);
	scene.add.existing(gameObject);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/RangeInputHandler.js
var GetValue$12 = __webpack_exports__Utils.Objects.GetValue;
var Linear$1 = __webpack_exports__Math.Linear;
var SnapFloor = __webpack_exports__Math.Snap.Floor;
var SetRange = function(gameObject, min, max, step) {
	gameObject.minValue = min;
	gameObject.maxValue = max;
	gameObject.step = step;
	gameObject.childrenMap.slider.setGap(step, min, max);
};
var SetInputTextReadOnly$1 = function(gameObject, readOnly, force) {
	if (readOnly === void 0) readOnly = true;
	if (force === void 0) force = false;
	var inputText = gameObject.childrenMap.inputText;
	if (force) {
		gameObject.inputTextReadOnly = readOnly;
		inputText.setReadOnly(readOnly);
	} else if (!gameObject.inputTextReadOnly) inputText.setReadOnly(readOnly);
};
var SetSliderReadOnly = function(gameObject, readOnly) {
	if (readOnly === void 0) readOnly = true;
	gameObject.childrenMap.slider.setEnable(!readOnly);
};
var RangeInputHandler_default = {
	name: "RangeInput",
	accept(config) {
		if (config.hasOwnProperty("view")) return config.view === "range";
		return typeof config.value === "number" && config.hasOwnProperty("min") && config.hasOwnProperty("max");
	},
	build(gameObject, config, inputRowStyle, styles) {
		var scene = gameObject.scene;
		gameObject.type = "rexTweaker.RangeInput";
		var sliderConfig = inputRowStyle.slider;
		var trackSize = GetValue$12(sliderConfig, gameObject.orientation === 0 ? "track.height" : "track.width");
		var slider = CreateSlider(scene, sliderConfig);
		var defaultProportion = inputRowStyle.defaultExpandWidth ? 2 : 0;
		var proportion = GetValue$12(inputRowStyle, "proportion.range.slider", defaultProportion);
		var expand = trackSize === void 0;
		gameObject.add(slider, {
			proportion,
			expand,
			key: "slider"
		});
		var inputText = CreateInputText_default(scene, inputRowStyle.inputNumber || inputRowStyle.inputText).setNumberInput();
		var defaultProportion = inputRowStyle.defaultExpandWidth ? 1 : 0;
		var proportion = GetValue$12(inputRowStyle, "proportion.range.inputText", defaultProportion);
		gameObject.add(inputText, {
			proportion,
			expand: true,
			key: "inputText"
		});
		inputText.on("close", function() {
			gameObject.setValue(inputText.value);
		});
		slider.on("valuechange", function() {
			var value = Linear$1(gameObject.minValue, gameObject.maxValue, slider.value);
			if (gameObject.step) value = SnapFloor(value, gameObject.step, gameObject.minValue);
			gameObject.setValue(value);
		});
	},
	setup(gameObject, config, setDefaults) {
		if (setDefaults || config.hasOwnProperty("max")) SetRange(gameObject, config.min, config.max, config.step);
		if (setDefaults || config.hasOwnProperty("inputTextReadOnly")) SetInputTextReadOnly$1(gameObject, !!config.inputTextReadOnly, true);
	},
	displayValue(gameObject, value) {
		gameObject.childrenMap.slider.setValue(value, gameObject.minValue, gameObject.maxValue);
		gameObject.childrenMap.inputText.setText("").setText(gameObject.getFotmatText(value));
	},
	setReadOnly(gameObject, readOnly) {
		if (readOnly === void 0) readOnly = true;
		SetInputTextReadOnly$1(gameObject, readOnly);
		SetSliderReadOnly(gameObject, readOnly);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/utils/CreateButtons.js
var CreateButtons = function(scene, config) {
	var gameObject = new Buttons$1(scene, config);
	scene.add.existing(gameObject);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/utils/CreateLabel.js
var CreateLabel_default = CreateLabel;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/IncDecInputHandler.js
var GetValue$11 = __webpack_exports__Utils.Objects.GetValue;
var SetInputTextReadOnly = function(gameObject, readOnly, force) {
	if (readOnly === void 0) readOnly = true;
	if (force === void 0) force = false;
	var inputText = gameObject.childrenMap.inputText;
	if (force) {
		gameObject.inputTextReadOnly = readOnly;
		inputText.setReadOnly(readOnly);
	} else if (!gameObject.inputTextReadOnly) inputText.setReadOnly(readOnly);
};
var SetButtonsReadOnly$1 = function(gameObject, readOnly) {
	if (readOnly === void 0) readOnly = true;
	gameObject.childrenMap.buttons.setButtonEnable(!readOnly);
};
var IncDecInputHandler_default = {
	name: "RangeInput",
	accept(config) {
		if (config.hasOwnProperty("view")) return config.view === "incdec";
		return false;
	},
	build(gameObject, config, inputRowStyle, styles) {
		var scene = gameObject.scene;
		gameObject.type = "rexTweaker.IncDecInput";
		var incDecConfig = GetValue$11(inputRowStyle, "incDec") || {};
		var buttonConfigBase = {
			text: null,
			action: null
		};
		var buttons = CreateButtons(scene, { expand: false });
		var proportion = inputRowStyle.defaultExpandWidth ? 1 : 0;
		gameObject.add(buttons, {
			proportion,
			expand: true
		});
		var inputText = CreateInputText_default(scene, inputRowStyle.inputNumber || inputRowStyle.inputText).setNumberInput();
		inputText.on("close", function() {
			gameObject.setValue(inputText.value);
		});
		var incButton = CreateLabel_default(scene, Object.assign(DeepClone(buttonConfigBase), incDecConfig.incButton || {}));
		var decButton = CreateLabel_default(scene, Object.assign(DeepClone(buttonConfigBase), incDecConfig.decButton || {}));
		buttons.addButton(incButton);
		buttons.addButton(decButton);
		var inputTextIndex = incDecConfig.inputTextIndex || 0;
		buttons.insert(inputTextIndex, inputText, {
			proportion: 1,
			expand: true
		});
		gameObject.step = 1;
		gameObject.minValue = void 0;
		gameObject.maxValue = void 0;
		buttons.on("button.click", function(button, index, pointer, event) {
			var value = gameObject.value;
			if (index === 0) value += gameObject.step;
			else value -= gameObject.step;
			if (gameObject.maxValue !== void 0 && value > gameObject.maxValue) value = gameObject.maxValue;
			if (gameObject.minValue !== void 0 && value < gameObject.minValue) value = gameObject.minValue;
			gameObject.setValue(value);
		});
		gameObject.addChildrenMap("inputText", inputText);
		gameObject.addChildrenMap("incButton", incButton);
		gameObject.addChildrenMap("decButton", decButton);
		gameObject.addChildrenMap("buttons", buttons);
	},
	setup(gameObject, config, setDefaults) {
		if (setDefaults || config.hasOwnProperty("inputTextReadOnly")) SetInputTextReadOnly(gameObject, !!config.inputTextReadOnly, true);
		if (setDefaults || config.hasOwnProperty("step")) gameObject.step = config.hasOwnProperty("step") ? config.step : 1;
		if (setDefaults || config.hasOwnProperty("min")) gameObject.minValue = config.min;
		if (setDefaults || config.hasOwnProperty("max")) gameObject.maxValue = config.max;
	},
	displayValue(gameObject, value) {
		gameObject.childrenMap.inputText.setText("").setText(gameObject.getFotmatText(value));
	},
	setReadOnly(gameObject, readOnly) {
		if (readOnly === void 0) readOnly = true;
		SetInputTextReadOnly(gameObject, readOnly);
		SetButtonsReadOnly$1(gameObject, readOnly);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/build/CreateColorInput.js
var CreateColorInput = function(scene, config, deepCloneConfig) {
	if (deepCloneConfig === void 0) deepCloneConfig = true;
	if (deepCloneConfig) config = config ? DeepClone(config) : {};
	else if (!config) config = {};
	var inputText = new ColorInput(scene, config);
	scene.add.existing(inputText);
	return inputText;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/utils/CreateColorInput.js
var CreateColorInput_default = CreateColorInput;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/ColorInputHandler.js
var SetColorInputReadOnly = function(gameObject, readOnly) {
	if (readOnly === void 0) readOnly = true;
	gameObject.childrenMap.colorInput.setReadOnly(readOnly);
};
var ColorInputHandler_default = {
	name: "ColorInput",
	accept(config) {
		if (config.hasOwnProperty("view")) return config.view === "color";
		return false;
	},
	build(gameObject, config, inputRowStyle, styles) {
		var scene = gameObject.scene;
		gameObject.type = "rexTweaker.ColorInput";
		var colorInputConfig = inputRowStyle.colorInput;
		if (colorInputConfig === void 0) colorInputConfig = {};
		if (!colorInputConfig.hasOwnProperty("inputText")) colorInputConfig.inputText = inputRowStyle.inputText;
		var colorInput = CreateColorInput_default(scene, colorInputConfig);
		gameObject.add(colorInput, {
			proportion: 1,
			expand: true,
			key: "colorInput"
		});
		colorInput.on("valuechange", function(value) {
			gameObject.setValue(value);
		});
	},
	displayValue(gameObject, value) {
		gameObject.childrenMap.colorInput.setValue(value);
	},
	setReadOnly(gameObject, readOnly) {
		if (readOnly === void 0) readOnly = true;
		SetColorInputReadOnly(gameObject, readOnly);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/utils/CreateCheckbox.js
var CreateCheckbox = function(scene, config) {
	var gameObject = new Checkbox_default(scene, config);
	scene.add.existing(gameObject);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/CheckboxInputHandler.js
var SetCheckboxReadOnly = function(gameObject, readOnly) {
	if (readOnly === void 0) readOnly = true;
	gameObject.childrenMap.checkbox.setReadOnly(readOnly);
};
var CheckboxInputHandler_default = {
	name: "CheckboxInput",
	accept(config) {
		if (config.hasOwnProperty("view")) return config.view === "boolean";
		return typeof config.value === "boolean";
	},
	build(gameObject, config, inputRowStyle, styles) {
		var scene = gameObject.scene;
		gameObject.type = "rexTweaker.CheckboxInput";
		var checkboxConfig = inputRowStyle.checkbox;
		var checkbox = CreateCheckbox(scene, checkboxConfig);
		var size = checkboxConfig.size;
		if (size !== void 0) checkbox.setSize(size, size);
		var fitRatio = size !== void 0 ? 0 : 1;
		gameObject.add(checkbox, {
			proportion: 0,
			expand: false,
			fitRatio,
			key: "checkbox"
		});
		checkbox.on("valuechange", function(value) {
			gameObject.setValue(value);
		});
	},
	displayValue(gameObject, value) {
		gameObject.childrenMap.checkbox.setValue(value);
	},
	setReadOnly(gameObject, readOnly) {
		if (readOnly === void 0) readOnly = true;
		SetCheckboxReadOnly(gameObject, readOnly);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/utils/CreateToggleSwitch.js
var CreateToggleSwitch = function(scene, config) {
	var gameObject = new ToggleSwitch_default(scene, config);
	scene.add.existing(gameObject);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/ToggleSwitchInputHandler.js
var SetToggleSwitchReadOnly = function(gameObject, readOnly) {
	if (readOnly === void 0) readOnly = true;
	gameObject.childrenMap.toggleSwitch.setReadOnly(readOnly);
};
var ToggleSwitchInputHandler_default = {
	name: "ToggleSwitchInput",
	accept(config) {
		if (config.hasOwnProperty("view")) return config.view === "toggleSwitch";
		return false;
	},
	build(gameObject, config, inputRowStyle, styles) {
		var scene = gameObject.scene;
		gameObject.type = "rexTweaker.ToggleSwitchInput";
		var toggleSwitchConfig = inputRowStyle.toggleSwitch;
		var toggleSwitch = CreateToggleSwitch(scene, toggleSwitchConfig);
		var size = toggleSwitchConfig.size;
		if (size !== void 0) toggleSwitch.setSize(size, size);
		var fitRatio = size !== void 0 ? 0 : 1;
		gameObject.addSpace().add(toggleSwitch, {
			proportion: 0,
			expand: false,
			fitRatio,
			key: "toggleSwitch"
		});
		toggleSwitch.on("valuechange", function(value) {
			gameObject.setValue(value);
		});
	},
	displayValue(gameObject, value) {
		gameObject.childrenMap.toggleSwitch.setValue(value);
	},
	setReadOnly(gameObject, readOnly) {
		if (readOnly === void 0) readOnly = true;
		SetToggleSwitchReadOnly(gameObject, readOnly);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/utils/build/CreateDropDownList.js
var CreateDropDownList = function(scene, config) {
	config = BuildListConfig(scene, config);
	var gameObject = new DropDownList(scene, config);
	scene.add.existing(gameObject);
	return gameObject;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/utils/CreateDropDownList.js
var CreateDropDownList_default = CreateDropDownList;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/utils/OptionsMethods.js
var GetOptionIndex = function(options, value) {
	for (var i = 0, cnt = options.length; i < cnt; i++) if (options[i].value === value) return i;
};
var GetOption = function(options, value) {
	var index = GetOptionIndex(options, value);
	if (index == null) return;
	return options[index];
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/ListInputHandler.js
var SetOptions$1 = function(gameObject, options) {
	gameObject.childrenMap.list.setOptions(options);
};
var SetListReadOnly = function(gameObject, readOnly) {
	if (readOnly === void 0) readOnly = true;
	var list = gameObject.childrenMap.list;
	if (readOnly) list.disableClick();
	else list.enableClick();
};
var ListInputHandler_default = {
	name: "ListInput",
	accept(config) {
		if (config.hasOwnProperty("view")) return config.view === "list";
		return config.hasOwnProperty("options");
	},
	build(gameObject, config, inputRowStyle, styles) {
		var scene = gameObject.scene;
		gameObject.type = "rexTweaker.ListInput";
		var list = CreateDropDownList_default(scene, inputRowStyle.list);
		gameObject.add(list, {
			proportion: 1,
			expand: true,
			key: "list"
		});
		list.on("button.click", function(dropDownList, listPanel, button, index, pointer, event) {
			gameObject.setValue(button.value);
		});
	},
	setup(gameObject, config, setDefaults) {
		if (setDefaults || config.hasOwnProperty("options")) SetOptions$1(gameObject, config.options);
	},
	displayValue(gameObject, value) {
		var list = gameObject.childrenMap.list;
		var option = GetOption(list.options, value);
		list.resetDisplayContent(option).setMinSize(list.width, list.height).layout().setMinSize(0, 0);
	},
	setReadOnly(gameObject, readOnly) {
		if (readOnly === void 0) readOnly = true;
		SetListReadOnly(gameObject, readOnly);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/utils/SetButtonsActiveState.js
var SetButtonsActiveStateByIndex = function(buttons, index) {
	for (var i = 0, cnt = buttons.length; i < cnt; i++) {
		var button = buttons[i];
		if (!button) continue;
		button.setActiveState(i === index);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/ButtonsInputHandler.js
var GetValue$10 = __webpack_exports__Utils.Objects.GetValue;
var SetOptions = function(gameObject, options) {
	var buttons = gameObject.childrenMap.buttons;
	buttons.options = options;
	var scene = gameObject.scene;
	var buttonConfig = buttons.buttonConfig;
	buttons.clearButtons(true);
	for (var i = 0, cnt = options.length; i < cnt; i++) {
		var option = options[i];
		var button = CreateLabel_default(scene, buttonConfig).setActiveState(false).resetDisplayContent({ text: option.text });
		buttons.addButton(button);
	}
};
var SetButtonsReadOnly = function(gameObject, readOnly) {
	if (readOnly === void 0) readOnly = true;
	gameObject.childrenMap.buttons.setButtonEnable(!readOnly);
};
var ButtonsInputHandler_default = {
	name: "ButtonsInput",
	accept(config) {
		if (config.hasOwnProperty("view")) return config.view === "buttons";
		return false;
	},
	build(gameObject, config, inputRowStyle, styles) {
		var scene = gameObject.scene;
		gameObject.type = "rexTweaker.ButtonsInput";
		var buttonConfig = inputRowStyle.button ? DeepClone(inputRowStyle.button) : {};
		var buttonExpand = GetValue$10(buttonConfig, "expand", true);
		if (buttonExpand) buttonConfig.align = "center";
		delete buttonConfig.expand;
		var buttons = CreateButtons(scene, { expand: buttonExpand });
		buttons.buttonConfig = buttonConfig;
		gameObject.add(buttons, {
			proportion: 1,
			expand: true,
			key: "buttons"
		});
		buttons.on("button.click", function(button, index, pointer, event) {
			var option = buttons.options[index];
			if (!option) return;
			gameObject._selectedIndex = index;
			gameObject.setValue(option.value);
			gameObject._selectedIndex = void 0;
		});
	},
	setup(gameObject, config, setDefaults) {
		if (setDefaults || config.hasOwnProperty("options")) SetOptions(gameObject, config.options);
	},
	displayValue(gameObject, value) {
		var buttons = gameObject.childrenMap.buttons;
		var index = gameObject._selectedIndex;
		if (index === void 0) index = GetOptionIndex(buttons.options, value);
		SetButtonsActiveStateByIndex(buttons.childrenMap.buttons, index);
	},
	setReadOnly(gameObject, readOnly) {
		if (readOnly === void 0) readOnly = true;
		SetButtonsReadOnly(gameObject, readOnly);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/inputhandlers/ArrayInputHandler.js
var SetArrayTableReadOnly = function(gameObject, readOnly) {
	if (readOnly === void 0) readOnly = true;
	gameObject.childrenMap.arrayTable.setReadOnly(readOnly);
};
var CreateDefaultNumberItem = function() {
	return 0;
};
var CreateDefaultStringItem = function() {
	return "";
};
var CreateDefaultBooleanItem = function() {
	return false;
};
var ArrayInputHandler_default = {
	name: "ArrayInput",
	accept(config) {
		if (config.hasOwnProperty("view")) {
			var view = config.view;
			return view === "numbers" || view === "strings" || view === "booleans" || view === "array";
		}
		var value = config.value;
		if (Array.isArray(value)) {
			switch (typeof value[0]) {
				case "number":
					config.view = "numbers";
					break;
				case "strig":
					config.view = "strings";
					break;
				case "boolean":
					config.view = "booleans";
					break;
				default: config.view = "array";
			}
			return true;
		} else return false;
	},
	build(gameObject, config, inputRowStyle, styles) {
		this.type = "rexTweaker.ArrayInput";
		var arrayTableConfig = inputRowStyle.arrayTable || styles.arrayTable || {};
		if (!config.hasOwnProperty("createDefaultItem")) {
			var view = config.view;
			if (view === "numbers") config.createDefaultItem = CreateDefaultNumberItem;
			else if (view === "numbers") config.createDefaultItem = CreateDefaultStringItem;
			else if (view === "booleans") config.createDefaultItem = CreateDefaultBooleanItem;
		}
		var arrayTable = CreateArrayTable(gameObject.tweaker, config, arrayTableConfig).setTitle();
		gameObject.add(arrayTable, {
			proportion: 1,
			expand: true,
			key: "arrayTable"
		});
		if (!arrayTableConfig.hasOwnProperty("orientation") && !config.hasOwnProperty("orientation")) config.orientation = 1;
	},
	setup(gameObject, config, setDefaults) {},
	displayValue(gameObject, value) {
		gameObject.childrenMap.arrayTable.setItems(value, true);
	},
	onBindTarget(gameObject) {
		gameObject.childrenMap.arrayTable.scrollToTop();
	},
	setReadOnly(gameObject, readOnly) {
		if (readOnly === void 0) readOnly = true;
		SetArrayTableReadOnly(gameObject, readOnly);
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/methods/RegisterDefaultInputHandlers.js
var RegisterDefaultInputHandlers = function() {
	this.registerInputHandler(TextInputHandler_default).registerInputHandler(TextAreaInputHandler_default).registerInputHandler(NumberInputHandler_default).registerInputHandler(RangeInputHandler_default).registerInputHandler(IncDecInputHandler_default).registerInputHandler(ColorInputHandler_default).registerInputHandler(CheckboxInputHandler_default).registerInputHandler(ToggleSwitchInputHandler_default).registerInputHandler(ListInputHandler_default).registerInputHandler(ButtonsInputHandler_default).registerInputHandler(ArrayInputHandler_default);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/Tweaker.js
var Tweaker = class extends GenerateTweakerShellClass() {
	constructor(scene, config) {
		if (config === void 0) config = {};
		if (config.hasOwnProperty("style")) config.styles = config.style;
		if (config.styles === void 0) config.styles = {};
		config.background = config.styles.background || {};
		config.space = config.styles.space || {};
		super(scene, config);
		this.type = "rexTweaker";
		RegisterDefaultInputHandlers.call(this);
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		super.destroy(fromScene);
		this.inputHandlers = void 0;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tweaker/Factory.js
ObjectFactory.register("tweaker", function(config) {
	var gameObject = new Tweaker(this.scene, config);
	this.scene.add.existing(gameObject);
	return gameObject;
});
SetValue(window, "RexPlugins.UI.Tweaker", Tweaker);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/click/Factory.js
ObjectFactory.register("click", function(gameObject, config) {
	return new Click_default(gameObject, config);
});
SetValue(window, "RexPlugins.UI.Click", Click_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/clickoutside/Factory.js
ObjectFactory.register("clickOutside", function(gameObject, config) {
	return new ClickOutside_default(gameObject, config);
});
SetValue(window, "RexPlugins.UI.ClickOutside", ClickOutside_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/intouching/Factory.js
ObjectFactory.register("inTouching", function(gameObject, config) {
	return new InTouching_default(gameObject, config);
});
SetValue(window, "RexPlugins.UI.InTouching", InTouching_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/tap/Factory.js
ObjectFactory.register("tap", function(gameObject, config) {
	if (!IsGameObject(gameObject)) {
		config = gameObject;
		gameObject = this.scene;
	}
	return new Tap_default(gameObject, config);
});
SetValue(window, "RexPlugins.UI.Tap", Tap_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/press/Factory.js
ObjectFactory.register("press", function(gameObject, config) {
	if (!IsGameObject(gameObject)) {
		config = gameObject;
		gameObject = this.scene;
	}
	return new Press_default(gameObject, config);
});
SetValue(window, "RexPlugins.UI.Press", Press_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/swipe/Factory.js
ObjectFactory.register("swipe", function(gameObject, config) {
	if (!IsGameObject(gameObject)) {
		config = gameObject;
		gameObject = this.scene;
	}
	return new Swipe_default(gameObject, config);
});
SetValue(window, "RexPlugins.UI.Swipe", Swipe_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/pan/Pan.js
var Pan_default = Pan;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/pan/Factory.js
ObjectFactory.register("pan", function(gameObject, config) {
	if (!IsGameObject(gameObject)) {
		config = gameObject;
		gameObject = this.scene;
	}
	return new Pan_default(gameObject, config);
});
SetValue(window, "RexPlugins.UI.Pan", Pan_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/drag/Drag.js
var Drag_default = Drag;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/drag/Factory.js
ObjectFactory.register("drag", function(gameObject, config) {
	return new Drag_default(gameObject, config);
});
SetValue(window, "RexPlugins.UI.Drag", Drag_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/pinch/Pinch.js
var Pinch_default = Pinch;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/pinch/Factory.js
ObjectFactory.register("pinch", function(config) {
	return new Pinch_default(this.scene, config);
});
SetValue(window, "RexPlugins.UI.Pinch", Pinch_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/rotate/Rotate.js
var Rotate_default = Rotate$1;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/rotate/Factory.js
ObjectFactory.register("rotate", function(config) {
	return new Rotate_default(this.scene, config);
});
SetValue(window, "RexPlugins.UI.Rotate", Rotate_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/flip/GetFaceUpdatingCallback.js
var IsPlainObject$5 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$9 = __webpack_exports__Utils.Objects.GetValue;
var GetFrameUpdatingCallback = function(key, frame, gameObject) {
	var callback;
	if (key === void 0) {
		key = gameObject.texture.key;
		frame = gameObject.frame.name;
	} else if (IsPlainObject$5(key)) {
		var config = key;
		key = GetValue$9(config, "key", gameObject.texture.key);
		frame = GetValue$9(config, "frame", gameObject.frame.name);
	} else if (typeof key === "string") {} else callback = key;
	if (callback === void 0) callback = function(gameObject) {
		gameObject.setTexture(key, frame);
	};
	return callback;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/flip/Flip.js
var GetValue$8 = __webpack_exports__Utils.Objects.GetValue;
var GetAdvancedValue = __webpack_exports__Utils.Objects.GetAdvancedValue;
var Linear = __webpack_exports__Math.Linear;
var Flip = class extends EaseValueTaskBase {
	constructor(gameObject, config) {
		super(gameObject, config);
		this.resetFromJSON(config);
		this.boot();
	}
	resetFromJSON(o) {
		super.resetFromJSON(o);
		this.setDuration(GetAdvancedValue(o, "duration", 500));
		this.setEase(GetValue$8(o, "ease", "Sine"));
		this.setOrientation(GetValue$8(o, "orientation", 0));
		this.setFrontFace(GetValue$8(o, "front", void 0));
		this.setBackFace(GetValue$8(o, "back", void 0));
		this.setFace(GetValue$8(o, "face", 0));
		return this;
	}
	setOrientation(orientation) {
		if (typeof orientation === "string") orientation = ORIENTATIONMODE[orientation];
		this.orientation = orientation;
		return this;
	}
	get face() {
		return this._face;
	}
	set face(face) {
		if (typeof face === "string") face = FACEMODE[face];
		this._face = face;
		if (face === 0 && this.frontFaceCallback) this.frontFaceCallback(this.parent);
		else if (face === 1 && this.backFaceCallback) this.backFaceCallback(this.parent);
	}
	setFace(face) {
		this.face = face;
		return this;
	}
	toggleFace() {
		var newFace = this.face === 0 ? 1 : 0;
		this.setFace(newFace);
		return this;
	}
	setFrontFace(key, frame) {
		this.frontFaceCallback = GetFrameUpdatingCallback(key, frame, this.parent);
		return this;
	}
	setBackFace(key, frame) {
		this.backFaceCallback = GetFrameUpdatingCallback(key, frame, this.parent);
		return this;
	}
	start() {
		if (this.timer.isRunning) return this;
		var gameObject = this.parent;
		if (this.orientation === 0) this.scale0 = gameObject.scaleX;
		else this.scale0 = gameObject.scaleY;
		this.timer.setDelay(this.delay).setDuration(this.duration / 2).setRepeat(1);
		super.start();
		return this;
	}
	flip(duration) {
		if (this.isRunning) return this;
		if (duration !== void 0) this.setDuration(duration);
		this.start();
		return this;
	}
	updateTarget(gameObject, timer) {
		if (timer.justRestart) this.toggleFace();
		var t = timer.t;
		if (timer.isOddIteration) t = 1 - t;
		t = this.easeFn(t);
		var value = Linear(this.scale0, 0, t);
		if (this.orientation === 0) gameObject.scaleX = value;
		else gameObject.scaleY = value;
	}
};
var ORIENTATIONMODE = {
	x: 0,
	horizontal: 0,
	y: 1,
	vertical: 1
};
var FACEMODE = {
	front: 0,
	back: 1
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/flip/Flip.js
var Flip_default = Flip;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/flip/Factory.js
ObjectFactory.register("flip", function(gameObject, config) {
	return new Flip_default(gameObject, config);
});
SetValue(window, "RexPlugins.UI.Flip", Flip_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/shake/Factory.js
ObjectFactory.register("shake", function(gameObject, config) {
	return new Shake_default(gameObject, config);
});
SetValue(window, "RexPlugins.UI.Shake", Shake_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/toucheventstop/Factory.js
ObjectFactory.register("touchEventStop", function(gameObject, config) {
	return new TouchEventStop_default(gameObject, config);
});
SetValue(window, "RexPlugins.UI.TouchEventStop", TouchEventStop_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/perspective/Perspective.js
var Perspective_default = ContainerPerspective;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/perspective/Factory.js
ObjectFactory.register("perspective", function(gameObject, config) {
	return new Perspective_default(gameObject, config);
});
SetValue(window, "RexPlugins.UI.Perspective", Perspective_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/quad/image/Image.js
var IsPlainObject$4 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$7 = __webpack_exports__Utils.Objects.GetValue;
var Image$1 = class extends Sprite {
	constructor(scene, x, y, key, frame, config) {
		if (IsPlainObject$4(x)) {
			config = x;
			x = GetValue$7(config, "x", 0);
			y = GetValue$7(config, "y", 0);
			key = GetValue$7(config, "key", null);
			frame = GetValue$7(config, "frame", null);
		}
		if (config === void 0) config = {};
		if (config.renderAsTriangles === void 0) config.renderAsTriangles = true;
		if (config.useOrderedIndices === void 0) config.useOrderedIndices = false;
		super(scene, x, y, key, frame, config);
		this.type = "rexQuadImage";
		this.isNinePointMode = GetValue$7(config, "ninePointMode", false);
		this.fourPointsModeRTL = GetValue$7(config, "rtl", false);
		this.initVertices();
	}
	initVertices() {
		var isNinePointMode = this.isNinePointMode;
		var pointsPerSide = isNinePointMode ? 3 : 2;
		var vertices = [];
		for (var r = 0; r < pointsPerSide; r++) for (var c = 0; c < pointsPerSide; c++) {
			var vertex = this.createVertexObject(c / (pointsPerSide - 1), r / (pointsPerSide - 1));
			vertices.push(vertex);
		}
		if (isNinePointMode) {
			this.setFaceIndices(NinePointsIndices);
			this.topLeft = vertices[0];
			this.topCenter = vertices[1];
			this.topRight = vertices[2];
			this.centerLeft = vertices[3];
			this.center = vertices[4];
			this.centerRight = vertices[5];
			this.bottomLeft = vertices[6];
			this.bottomCenter = vertices[7];
			this.bottomRight = vertices[8];
		} else {
			this.setFaceIndices(!this.fourPointsModeRTL ? FourPointsIndices : FourPointsIndicesRTL);
			this.topLeft = vertices[0];
			this.topRight = vertices[1];
			this.bottomLeft = vertices[2];
			this.bottomRight = vertices[3];
		}
		return this;
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		super.destroy(fromScene);
	}
};
var FourPointsIndices = [
	0,
	2,
	3,
	0,
	3,
	1
];
var FourPointsIndicesRTL = [
	1,
	3,
	2,
	1,
	2,
	0
];
var NinePointsIndices = [
	0,
	3,
	4,
	0,
	4,
	1,
	1,
	4,
	2,
	4,
	5,
	2,
	3,
	6,
	4,
	6,
	7,
	4,
	4,
	7,
	8,
	4,
	8,
	5
];
__webpack_exports__Utils.Objects.IsPlainObject;
__webpack_exports__Utils.Objects.GetValue;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/quad/skewimage/Skew.js
var Skew = function(gameObject, skewX, skewY) {
	if (skewX === void 0) skewX = 0;
	if (skewY === void 0) skewY = 0;
	var ox = gameObject.displayOriginX;
	var oy = gameObject.displayOriginY;
	var xOffset = Math.tan(skewX) * oy;
	var yOffset = Math.tan(skewY) * ox;
	var vertices = gameObject.vertexObjects;
	for (var i = 0, cnt = vertices.length; i < cnt; i++) {
		var vertex = vertices[i];
		var frameX = vertex.frameX;
		var frameY = vertex.frameY;
		if (frameY > oy) vertex.localX = frameX + xOffset;
		else if (frameY < oy) vertex.localX = frameX - xOffset;
		if (frameX > ox) vertex.localY = frameY + yOffset;
		else if (frameX < ox) vertex.localY = frameY - yOffset;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/quad/skewimage/SkewImage.js
var IsPlainObject$2 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$5 = __webpack_exports__Utils.Objects.GetValue;
var DegToRad = __webpack_exports__Math.DegToRad;
var RadToDeg = __webpack_exports__Math.RadToDeg;
var SkewImage = class extends Image$1 {
	constructor(scene, x, y, key, frame, config) {
		if (IsPlainObject$2(x)) {
			config = x;
			x = GetValue$5(config, "x", 0);
			y = GetValue$5(config, "y", 0);
			key = GetValue$5(config, "key", null);
			frame = GetValue$5(config, "frame", null);
		}
		if (config === void 0) config = {};
		if (config.renderAsTriangles === void 0) config.renderAsTriangles = false;
		if (config.useOrderedIndices === void 0) config.useOrderedIndices = true;
		if (config.orderedIndicesStrategy === void 0) config.orderedIndicesStrategy = 2;
		super(scene, x, y, key, frame, config);
		this.type = "rexSkewmage";
		this._skewX = 0;
		this._skewY = 0;
	}
	get skewX() {
		return this._skewX;
	}
	set skewX(value) {
		this._skewX = value;
		Skew(this, this._skewX, this._skewY);
	}
	get skewXDeg() {
		return RadToDeg(this._skewX);
	}
	set skewXDeg(value) {
		this.skewX = DegToRad(value);
	}
	get skewY() {
		return this._skewY;
	}
	set skewY(value) {
		this._skewY = value;
		Skew(this, this._skewX, this._skewY);
	}
	get skewYDeg() {
		return RadToDeg(this._skewY);
	}
	set skewYDeg(value) {
		this.skewY = DegToRad(value);
	}
	setSkewX(skewX) {
		this.skewX = skewX;
		return this;
	}
	setSkewY(skewY) {
		this.skewY = skewY;
		return this;
	}
	setSkew(skewX, skewY) {
		if (skewY === void 0) skewY = skewX;
		this.skewX = skewX;
		this.skewY = skewY;
		return this;
	}
	setSkewXDeg(skewX) {
		this.skewXDeg = skewX;
		return this;
	}
	setSkewYDeg(skewY) {
		this.skewYDeg = skewY;
		return this;
	}
	setSkewDeg(skewX, skewY) {
		if (skewY === void 0) skewY = skewX;
		this.skewXDeg = skewX;
		this.skewYDeg = skewY;
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/gameobjects/mesh/quad/skewrendertexture/SkewRenderTexture.js
var IsPlainObject$1 = __webpack_exports__Utils.Objects.IsPlainObject;
var GetValue$4 = __webpack_exports__Utils.Objects.GetValue;
var SkewRenderTexture = class extends SkewImage {
	constructor(scene, x, y, width, height) {
		if (IsPlainObject$1(x)) {
			var config = x;
			x = GetValue$4(config, "x", 0);
			y = GetValue$4(config, "y", 0);
			width = GetValue$4(config, "width", 32);
			height = GetValue$4(config, "height", 32);
		}
		var texture = CreateDynamicTexture(scene, width, height);
		super(scene, x, y, texture, null);
		this.type = "rexSkewRenderTexture";
		this.rt = this.texture;
		var self = this;
		InstallRTSetSizeHook(this.rt, function() {
			self.setSizeToFrame();
		});
	}
	destroy(fromScene) {
		if (!this.scene || this.ignoreDestroy) return;
		super.destroy(fromScene);
		this.rt.destroy();
		this.rt = null;
	}
	setSizeToFrame(frame) {
		super.setSizeToFrame(frame);
		this.updateDisplayOrigin();
		var vertexObjects = this.vertexObjects;
		for (var i = 0, cnt = vertexObjects.length; i < cnt; i++) this.updateVertexObjectFrame(vertexObjects[i]);
		return this;
	}
	snapshot(gameObjects, config) {
		if (config === void 0) config = {};
		config.gameObjects = gameObjects;
		config.renderTexture = this.rt;
		Snapshot(config);
		this.setSizeToFrame();
		return this;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/containerskew/ContainerSkew.js
var ContainerSkew = class extends MeshRenderTextureBase(SkewRenderTexture) {
	get skewState() {
		return this.isRunning;
	}
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/skew/Skew.js
var Skew_default = ContainerSkew;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/skew/Factory.js
ObjectFactory.register("skew", function(gameObject, config) {
	return new Skew_default(gameObject, config);
});
SetValue(window, "RexPlugins.UI.Skew", Skew_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/anchor/Factory.js
ObjectFactory.register("anchor", function(gameObject, config) {
	return new Anchor_default(gameObject, config);
});
SetValue(window, "RexPlugins.UI.Anchor", Anchor_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/texttyping/Factory.js
ObjectFactory.register("textTyping", function(gameObject, config) {
	return new TextTyping_default(gameObject, config);
});
SetValue(window, "RexPlugins.UI.TextTyping", TextTyping_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textpage/Factory.js
ObjectFactory.register("textPage", function(gameObject, config) {
	return new TextPage_default(gameObject, config);
});
SetValue(window, "RexPlugins.UI.TextPage", TextPage_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/textedit/methods/LastOpenedEditor.js
var LastOpenedEditor = void 0;
var SetLastOpenedEditor = function(editor) {
	if (editor === LastOpenedEditor) return;
	if (LastOpenedEditor !== void 0) LastOpenedEditor.close();
	LastOpenedEditor = editor;
};
var CloseLastOpenEditor = function(editor) {
	if (editor !== LastOpenedEditor) return;
	LastOpenedEditor = void 0;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/textedit/methods/CreateInputText.js
var GetValue$3 = __webpack_exports__Utils.Objects.GetValue;
var Clone = __webpack_exports__Utils.Objects.Clone;
var CreateInputText = function(text, config) {
	if (config === void 0) config = {};
	config = Clone(config);
	var scene = text.scene;
	var style = text.style;
	var backgroundColor = GetValue$3(config, "backgroundColor", style.backgroundColor);
	if (backgroundColor === null) backgroundColor = "transparent";
	config.text = GetValue$3(config, "text", text.text);
	config.fontFamily = GetValue$3(config, "fontFamily", style.fontFamily);
	config.fontSize = GetValue$3(config, "fontSize", style.fontSize);
	config.color = GetValue$3(config, "color", style.color);
	config.backgroundColor = backgroundColor;
	config.direction = GetValue$3(config, "rtl", style.rtl) ? "rtl" : "ltr";
	config.align = GetValue$3(config, "align", GetHAlign(style));
	if (config.direction === "rtl" && IsTextGameObject(text)) config.align = "right";
	var padding = text.padding;
	if (padding.left > 0) config.paddingLeft = `${padding.left}px`;
	if (padding.right > 0) config.paddingRight = `${padding.right}px`;
	if (style.backgroundCornerRadius) config.borderRadius = GetValue$3(config, "borderRadius", `${style.backgroundCornerRadius}px`);
	var inputText = new InputText(scene, text.x, text.y, GetValue$3(config, "width", text.width), GetValue$3(config, "height", text.height), config);
	inputText.setScale(text.scaleX, text.scaleY).setOrigin(text.originX, text.originY).setScrollFactor(text.scrollFactorX, text.scrollFactorY);
	var textParentContainer = text.parentContainer;
	if (!textParentContainer) scene.add.existing(inputText);
	else textParentContainer.add(inputText);
	return inputText;
};
var GetHAlign = function(style) {
	if (style.hasOwnProperty("align")) return style.align;
	else if (style.hasOwnProperty("halign")) return style.halign;
	else return "left";
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/utils/time/NextTick.js
var NextTick = function(scene, callback, scope) {
	return scene.time.delayedCall(0, callback, [], scope);
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/textedit/methods/Open.js
var GetValue$2 = __webpack_exports__Utils.Objects.GetValue;
var Merge = __webpack_exports__Utils.Objects.Merge;
var Open = function(config, onCloseCallback) {
	if (config === void 0) config = {};
	config = Merge(config, this.openConfig);
	SetLastOpenedEditor(this);
	if (IsFunction(config)) {
		onCloseCallback = config;
		config = void 0;
	}
	var textType = GetValue$2(config, "inputType", void 0);
	if (textType === void 0) textType = GetValue$2(config, "type", "text");
	if (onCloseCallback === void 0) onCloseCallback = GetValue$2(config, "onClose", void 0);
	var onCreateInputTextCallback = GetValue$2(config, "onCreate", void 0);
	var onOpenCallback = GetValue$2(config, "onOpen", void 0);
	var customOnTextChanged = GetValue$2(config, "onTextChanged", void 0);
	this.inputText = CreateInputText(this.parent, config);
	if (onCreateInputTextCallback) onCreateInputTextCallback(this.parent, this.inputText);
	this.inputText.on("textchange", function(inputText) {
		var text = inputText.text;
		if (customOnTextChanged) customOnTextChanged(this.parent, text);
		else this.parent.text = text;
	}, this).setFocus();
	this.parent.setVisible(false);
	this.onClose = onCloseCallback;
	if (GetValue$2(config, "enterClose", textType !== "textarea")) this.scene.input.keyboard.once("keydown-ENTER", this.close, this);
	this.delayCall = NextTick(this.scene, function() {
		this.scene.input.once("pointerdown", this.close, this);
		if (onOpenCallback) onOpenCallback(this.parent, this.inputText);
		this.emit("open", this.parent, this.inputText);
	}, this);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/textedit/methods/Close.js
var Close = function() {
	CloseLastOpenEditor(this);
	this.parent.setVisible(true);
	if (this.inputText) {
		this.inputText.destroy();
		this.inputText = void 0;
	}
	if (this.delayCall) {
		this.delayCall.remove();
		this.delayCall = void 0;
	}
	this.scene.input.keyboard.off("keydown-ENTER", this.close, this);
	this.scene.input.off("pointerdown", this.close, this);
	if (this.onClose) this.onClose(this.parent);
	this.emit("close", this.parent);
	return this;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/textedit/methods/Methods.js
var Methods = {
	open: Open,
	close: Close
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/textedit/TextEdit.js
var GetValue$1 = __webpack_exports__Utils.Objects.GetValue;
var TextEdit = class extends ComponentBase {
	constructor(gameObject, config) {
		super(gameObject);
		this.inputText = void 0;
		this.onClose = void 0;
		this.delayCall = void 0;
		this.setOpenConfig(config);
		if (GetValue$1(config, "clickEnable", true)) gameObject.on("pointerdown", function() {
			this.open();
		}, this).setInteractive();
	}
	shutdown(fromScene) {
		if (this.isShutdown) return;
		this.close();
		super.shutdown(fromScene);
	}
	setOpenConfig(config) {
		if (config === void 0) config = {};
		this.openConfig = config;
		return this;
	}
	get isOpened() {
		return this.inputText !== void 0;
	}
	get text() {
		return this.isOpened ? this.inputText.text : this.parent.text;
	}
};
Object.assign(TextEdit.prototype, Methods);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/plugins/behaviors/textedit/Edit.js
var Edit = function(gameObject, config, onCloseCallback) {
	if (!gameObject._edit) gameObject._edit = new TextEdit(gameObject, { clickEnable: false });
	gameObject._edit.open(config, onCloseCallback);
	return gameObject._edit;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textedit/TextEdit.js
var TextEdit_default = TextEdit;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textedit/Factory.js
ObjectFactory.register("textEdit", function(gameObject, config) {
	return new TextEdit_default(gameObject, config);
});
SetValue(window, "RexPlugins.UI.TextEdit", TextEdit_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fullscreenbutton/FullscreenButton.js
var GetValue = __webpack_exports__Utils.Objects.GetValue;
var IsPlainObject = __webpack_exports__Utils.Objects.IsPlainObject;
var FullscreenButton = class extends Click_default {
	constructor(gameObject, config) {
		super(gameObject, config);
		var onEnter = GetCallback(GetValue(config, "onEnter"));
		var onLeave = GetCallback(GetValue(config, "onLeave"));
		var scale = gameObject.scene.scale;
		this.on("click", function(button, gameObject, pointer, event) {
			if (scale.isFullscreen) {
				scale.stopFullscreen();
				if (onLeave) onLeave(gameObject);
			} else {
				scale.startFullscreen();
				if (onEnter) onEnter(gameObject);
			}
		});
	}
};
var GetCallback = function(config) {
	var callback;
	if (typeof config === "string") {
		var key = config;
		callback = function(gameObject) {
			gameObject.setTexture(key);
		};
	} else if (IsPlainObject(config)) {
		var key = config.key;
		var frame = config.frame;
		callback = function(gameObject) {
			gameObject.setTexture(key, frame);
		};
	} else callback = config;
	return callback;
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/fullscreenbutton/Factory.js
ObjectFactory.register("fullscreenButton", function(gameObject, config) {
	return new FullscreenButton(gameObject, config);
});
SetValue(window, "RexPlugins.UI.FullscreenButton", FullscreenButton);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/layermanager/LayerManager.js
var LayerManager_default = LayerManager;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/layermanager/Factory.js
ObjectFactory.register("layerManager", function(config) {
	return new LayerManager_default(this.scene, config);
});
SetValue(window, "RexPlugins.UI.LayerManager", LayerManager_default);
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/confirmdialog/ConfirmActionPromise.js
var ConfirmActionPromise = function(scene, config) {
	var dialog = ConfirmAction(scene, config);
	return new Promise(function(resolve, reject) {
		dialog.once("modal.close", function(closeEventData) {
			resolve(closeEventData);
		});
	});
};
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/textedit/Edit.js
var Edit_default = Edit;
//#endregion
//#region ../node_modules/phaser4-rex-plugins/templates/ui/ui-plugin.js
var UIPlugin = class extends __webpack_exports__Plugins.ScenePlugin {
	constructor(scene, pluginManager) {
		super(scene, pluginManager);
		this.add = new ObjectFactory(scene);
	}
	boot() {
		this.scene.events.on("destroy", this.destroy, this);
	}
	destroy() {
		this.add.destroy();
		super.destroy();
	}
	isInTouching(gameObject, pointer, preTest, postTest) {
		if (!gameObject.visible) return false;
		return IsPointerInBounds(gameObject, pointer, preTest, postTest);
	}
	get viewport() {
		return GetViewport(this.scene, this.scene.cameras.main, true);
	}
};
var methods = {
	getParentSizer: GetParent$1,
	getTopmostSizer: GetTopmostParent$1,
	removeFromParent: RemoveFromParent,
	hide: Hide,
	show: Show,
	isShown: IsShown,
	confirmAction: ConfirmAction,
	confirmActionPromise: ConfirmActionPromise,
	edit: Edit_default,
	wrapExpandText: WrapExpandText,
	fontSizeExpandText: FontSizeExpandText,
	fontSizeResize: FontSizeFit,
	setFontSizeToFitWidth: FontSizeFit,
	waitEvent: WaitEvent,
	waitComplete: WaitComplete,
	delayPromise: Delay,
	setChildrenInteractive: SetChildrenInteractive$1,
	fadeIn: fade_in_default,
	fadeOutDestroy: fade_out_destroy_default,
	easeMoveTo: EaseMoveTo,
	easeMoveFrom: EaseMoveFrom,
	modal: Modal$2,
	modalPromise: ModalPromise,
	modalClose: ModalClose,
	requestDrag: RequestDrag,
	openFileChooser: Open$1
};
Object.assign(UIPlugin.prototype, methods);
//#endregion
export { UIPlugin as default };
